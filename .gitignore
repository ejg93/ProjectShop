node_modules/
.next/
out/
dist/
build/
target/
*.tsbuildinfo
.env
.env.local
.DS_Store
Thumbs.db

# 관측 로그. 지워도 되는 것이라 저장소에 안 넣는다(D16)
logs/

# 스킬 본체는 .agents/ 에 있고 그건 커밋한다. 여기 있는 것은 그리로 가는 심볼릭 링크인데,
# core.symlinks=false 라 절대경로가 든 텍스트 파일로 들어가서 다른 기계에서는 쓰레기다.
# settings.local.json 도 이름 그대로 로컬 설정이다.
.claude/

# IntelliJ 설정. SDK 이름·경로가 기계마다 달라서 공유하면 남의 환경을 덮어쓴다.
.idea/
