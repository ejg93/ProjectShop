-- V90 을 되돌린다. 후기 운영정책이 `V85` 의 제1판으로 돌아간다.
--
-- **되돌리면 없는 화면을 가리키는 문안이 다시 공개된다**(`Q160`).
-- 제1판은 「내 후기 목록에서 확인할 수 있습니다」라고 적고 있고 그 화면은 `Q167` 이 세운다 —
-- 그 청크가 끝나기 전에 되돌리면 막힌 경로를 다시 알리는 상태가 된다(`D2` `R27`).
delete from policy_document where code = 'review_policy' and version = 2;
