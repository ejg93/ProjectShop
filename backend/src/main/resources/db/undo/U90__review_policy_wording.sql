-- V90 은 되돌리지 않는다. **시행된 고지는 지울 수 없다**(`Q173`).
--
-- V90 이 한 일은 후기 운영정책 제2판을 넣은 것 하나뿐이고, 넣는 순간 시행된다(`effective_at = now()`).
-- 시행된 판은 `policy_document_immutable` 이 지우지 못하게 막고, 그것이 맞다 — 공개한 고지를 거두는 것은
-- 새 판을 쌓는 것이다(전자문서법 제4조의2). **이전 문안으로 돌아가려면 그 문안으로 새 판을 넣는다.**
--
-- 전에는 여기서 `delete` 를 했는데 **한 번도 돌려 보지 않아서** 트리거에 막힌다는 것을 몰랐다 —
-- `MigrationUndoRunTest` 가 처음 돌려 보고 찾았다. 파일은 짝(`MigrationUndoTest`)으로 남기고 아무것도 안 한다.
do $$
begin
    raise notice 'V90 의 고지는 시행된 판이라 되돌리지 않는다. 이전 문안은 새 판으로 쌓는다';
end
$$;
