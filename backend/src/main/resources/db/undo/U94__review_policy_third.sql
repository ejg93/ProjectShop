-- V94 는 되돌리지 않는다. **시행된 고지는 지울 수 없다**(`Q173`).
--
-- V94 가 한 일은 후기 운영정책 제3판을 넣은 것 하나뿐이고, 넣는 순간 시행된다(`effective_at = now()`).
-- 시행된 판은 `policy_document_immutable` 이 지우지 못하게 막고, 그것이 맞다 — 공개한 고지를 거두는 것은
-- 새 판을 쌓는 것이다(전자문서법 제4조의2). **제2판 문안으로 돌아가려면 그 문안으로 새 판을 넣는다** —
-- 다만 제2판은 없는 「고객센터」를 가리켜서 계약내용 서면(`V27`)과 서로 다른 말을 한다(`Q171`).
do $$
begin
    raise notice 'V94 의 고지는 시행된 판이라 되돌리지 않는다. 이전 문안은 새 판으로 쌓는다';
end
$$;
