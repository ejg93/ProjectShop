-- 시드가 남긴 아웃박스 사건을 걷는다(`Q67`).
--
-- **시드는 「없던 일을 있었던 것처럼」 만드는 것이라 그 사건은 사실이 아니다.**
-- `V902` 가 `sku_stock` 에 넣으면 `V41` 의 `record_initial_stock` 이 `sku_stock_movement` 로 옮기고,
-- 그것이 `V70` 의 트리거를 타서 **SKU 수만큼 미발행 `shop.sku.stock_moved` 가 쌓인다.**
-- 발행기(`33b`)가 서면 그것이 **진짜 재고 이동과 구별 없이** 나간다 — 받는 쪽은 가를 방법이 없다.
--
-- `V41` 의 백필은 `V70` 앞이라 안 걸리는데 시드는 뒤다. 그 차이가 이 파일의 이유다.
--
-- **여기서 통째로 지우는 것이 맞다.** 시드는 빈 DB 에 한 번 도므로 이 시점에 표에 있는 것은
-- 전부 시드가 만든 것이다. 조건을 붙이면 「무엇이 시드 것인가」를 또 정해야 하고,
-- 그 지식은 **발행기가 알면 안 되는 것**이다(선택지 (나)를 버린 이유 — 사용자 결정 2026-09-17).
--
-- **`V900`~`V902` 를 직접 안 고친다.** 이미 적용된 파일을 고치면 그 체크섬으로 도는 로컬 DB 가 깨진다
-- (`V902` 가 같은 이유로 덧댄 자리다). 되돌리는 대신 덧댄다.
--
-- **불변 트리거는 `delete` 를 안 막는다** — `assert_outbox_event_immutable` 은 `before update` 고
-- `assert_outbox_write_allowed` 는 `before insert` 다. 파기 배치도 같은 길로 지운다(`33b`).
delete from outbox_event;
