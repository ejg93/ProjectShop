/**
 * 비밀번호 규칙 문구(`Q180`). **가입과 재설정이 같은 문구를 쓴다** — 화면마다 두면 한쪽만 고치는 날
 * 적힌 대로 쳤는데 거절당하는 화면이 생긴다.
 *
 * 두 수(15·64)는 서버 `@Password` 와 같아야 하고 `PasswordHintScreenTest` 가 이 파일을 읽어 대조한다.
 * 문장은 화면 몫이라 다듬어도 된다 — 수만 굳어 있다.
 */
export const PASSWORD_HINT = "15자 이상 64자 이하, 영문·숫자·기호를 쓸 수 있습니다.";
