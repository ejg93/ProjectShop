/**
 * 자리만 잡아 둔 첫 화면. 청크 13-2 가 로그인 화면으로 바꾼다.
 *
 * 생성기가 깔아 준 안내 페이지를 지웠다 — 남겨 두면 그 마크업이 이 저장소의 관례로 읽힌다.
 */
export default function Home() {
  return (
    <main className="flex min-h-full flex-col items-center justify-center gap-2 p-8">
      <h1 className="text-2xl font-semibold">ProjectShop</h1>
      <p className="text-sm text-zinc-500 dark:text-zinc-400">
        화면은 아직 없습니다. 로그인 화면부터 만듭니다.
      </p>
    </main>
  );
}
