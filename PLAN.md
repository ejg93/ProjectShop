# 계획

## 확정 상태

**만들 대상: 미정.** 아래 후보 중에서 고르거나 새로 적는다.
대상이 정해지면 이 절을 확정 내용으로 덮고, 「청크 분할표」를 채운다.

## 대상 고를 때 볼 것

토큰 태우기용이라는 조건 때문에 아무 프로젝트나 맞지는 않는다. 아래 세 개를 만족해야 한다.

- **잘리는 단위가 있다** — 파일 1~3개로 떨어지는 독립 청크로 쪼갤 수 있어야 한다.
  청크끼리 순서 의존이 강하면 중단됐을 때 복구가 비싸다
- **끝이 열려 있다** — 다 만들고 나면 더 태울 게 없어진다. 항목을 계속 추가할 수 있는 형태가 낫다
- **중단이 싸다** — 청크 중간에 끊겨도 버리는 게 적어야 한다. 빌드·배포 검증이 무거운 대상은 불리하다

## 후보

### A. 폐쇄망용 단일 파일 HTML 도구 모음

사내 폐쇄망에 반입할 수 있게 CDN 없이 HTML 한 장에 다 넣는 도구를 계속 추가한다.

- 잘리는 단위: 도구 1개 = 청크 1개. 깔끔하게 떨어짐
- 끝이 열려 있음: 도구는 계속 추가 가능
- 중단 비용: 낮음. 도구끼리 서로 독립
- 걸리는 점: portfolio 의 `frontend/public/toolbox/` 와 성격이 겹친다. 둘을 어떻게 나눌지 정해야 함

### B. 실무 참조 문서 트리

Oracle·JSP·eGovFrame 등 실무에서 반복해 찾는 것을 문서로 쌓는다.

- 잘리는 단위: 주제 1개 = 문서 1개 = 청크 1개
- 끝이 열려 있음: 주제는 계속 나옴
- 중단 비용: 낮음
- 걸리는 점: 산출물이 코드가 아니라 글이다. 토큰은 많이 먹지만 실행되는 게 없다

### C. 학습용 구현 모음

알고리즘·자료구조·디자인 패턴 등을 하나씩 직접 구현하고 테스트를 붙인다.

- 잘리는 단위: 주제 1개 = 구현 파일 + 테스트 파일 = 청크 1개
- 끝이 열려 있음: 주제 목록이 사실상 무한
- 중단 비용: 낮음. 테스트가 있어서 검증도 자동
- 걸리는 점: 실무에 바로 안 쓰인다. 만들고 안 볼 가능성

### D. (비어 있음)

새 후보를 여기에 적는다.

## 청크 분할표

대상 미정이라 비어 있다. 확정되면 아래 형식으로 채운다.

| # | 청크 | 건드리는 파일 | 선행 청크 |
|---|---|---|---|
| | | | |
