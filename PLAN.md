# 계획

## 무엇을 만드나

멀티 셀러 쇼핑몰. 판매자가 여럿 입점하고, 고객이 사고, 관리자가 관리한다.

만드는 목적은 물건을 파는 게 아니라 **권한 체계를 정갈하게 짜는 법을 익히는 것**이다.
실무에서 관리자·사용자 구분이 코드 여기저기 흩어져 유지보수가 힘들어지는 걸 겪었고,
그게 어디서부터 잘못되는지를 정면으로 다루려고 만든다.

그래서 판단이 갈릴 때는 **기능을 늘리는 쪽이 아니라 구조가 드러나는 쪽**을 고른다.
할인쿠폰 10종류보다 권한 판정 한 군데를 제대로 만드는 게 이 프로젝트의 목적에 맞다.

## 확정 사항

사용자가 직접 고른 것들이다.

| 항목 | 결정 | 이유 |
|---|---|---|
| 종류 | 쇼핑몰 (가격비교 아님) | 권한·역할 설계 문제는 쇼핑몰 쪽에만 있다. 가격비교는 권한 축이 얕다 |
| 판매 구조 | 멀티 셀러 마켓플레이스 | 판매자가 여럿이어야 "자기 상품만 수정" 같은 행 단위 권한이 필요해진다. 실무에서 지저분해지는 지점이 정확히 여기다 |
| 권한 모델 | RBAC + 리소스 스코프 | 역할과 권한을 테이블로 분리하고, 거기에 `own`/`seller`/`all` 같은 행 단위 조건을 더한다 |
| 스택 | Next.js + Spring Boot | Spring Security가 RBAC와 메서드 단위 권한 판정을 정석으로 지원한다. 실무 Java와 직결된다 |
| 결제·배포 | 가짜 결제, 로컬 실행만 | PG를 흉내내는 모의 모듈을 만든다. 상태 전이·멱등성 설계는 그대로 연습되고 사업자등록·심사를 안 거친다 |

## 내가 대신 정한 것

쇼핑몰 구조를 익히려고 만드는 프로젝트라서, 구조를 알아야 답할 수 있는 질문은 묻지 않고 정했다.
근거를 같이 적어 둔다. **아니라고 판단되면 뒤집어도 된다.** 코드가 아직 없어서 뒤집는 비용이 거의 없다.

### DB는 PostgreSQL, Docker Compose로 띄운다

Oracle이 실무 환경이지만 로컬에 올리는 비용이 크고, 이 프로젝트에서 배울 것은 SQL 방언이 아니라 스키마 설계다.
Postgres는 컨테이너 한 줄로 뜨고 Spring Boot 기본 지원이 두텁다.

Oracle 문법 연습이 목적에 포함된다면 뒤집을 만하다.

### 인증은 세션 쿠키, JWT 아님

JWT가 유행이지만 쇼핑몰에는 안 맞는 구석이 있다. 토큰을 서버가 회수할 수 없어서
"판매자 권한 박탈" 같은 조작이 즉시 반영되지 않는다. 권한 체계가 주제인 프로젝트에서 이건 치명적이다.

`HttpOnly` + `SameSite` 쿠키에 세션을 담고 서버가 상태를 쥔다. 로그아웃과 권한 회수가 즉시 먹는다.
프론트와 백엔드가 포트가 달라 생기는 CORS 문제는 Next.js rewrite 프록시로 같은 출처로 만들어 피한다.

### 상품은 Product / ProductOption / Sku 세 층으로 나눈다

초보 설계가 흔히 틀리는 지점이다. 상품 테이블 하나에 재고 컬럼을 두면
"검정 M은 품절인데 흰색 L은 남음" 을 표현할 수 없다.

- `Product` — 고객에게 하나로 보이는 상품. 이름, 설명, 판매자
- `ProductOption` — 선택 축. 색상, 사이즈
- `Sku` — 옵션 조합 하나. **재고와 가격은 여기에 붙는다**

주문은 `Product`가 아니라 `Sku`를 가리킨다.

### 주문에는 상태머신을 명시하고, 전이는 한 군데서만 한다

`status` 컬럼을 두고 여기저기서 갈아끼우면 반드시 깨진다.
허용된 전이를 코드에 표로 선언하고, 그 표를 거치지 않는 상태 변경은 막는다.

```
결제대기 → 결제완료 → 배송준비 → 배송중 → 배송완료 → 구매확정
   │          │           │
   └ 취소     └ 취소       └ 취소
                                      배송완료 → 반품요청 → 반품완료
```

### 주문 시각의 값은 복사해서 박제한다

주문 항목에 `sku_id`만 두고 가격을 조인해 오면, 판매자가 가격을 바꿨을 때 **과거 주문 금액이 같이 바뀐다.**
돈이 걸린 데이터는 주문 시점의 상품명·가격을 주문 항목에 복사해 넣는다.

### 결정 기록은 `doc/adr/` 에 남긴다

번호 붙인 마크다운 한 장에 결정 하나. 무엇을 정했고, 무엇과 저울질했고, 왜 그걸 골랐는지.
나중의 내가 "왜 이렇게 했지" 를 코드에서 역추적하지 않게 하려는 것이다. 유지보수가 주제인 프로젝트라 이것도 산출물이다.

## 권한 모델

이 프로젝트의 중심이라 따로 적는다.

### 왜 역할 enum으로는 부족한가

`ROLE_ADMIN` / `ROLE_SELLER` 같은 상수를 코드에 박으면 두 가지가 무너진다.

1. **역할이 늘 때마다 코드를 고치고 재배포해야 한다.** CS 담당, 정산 담당, 읽기전용 감사자가 생길 때마다 전부 손댄다
2. **"자기 것만" 을 표현할 수 없다.** 판매자 A와 판매자 B는 역할이 같지만 볼 수 있는 주문이 다르다.
   이걸 역할로 못 푸니 결국 서비스 코드마다 `if (order.sellerId != me.id) throw ...` 가 흩어진다. 실무에서 지저분해지는 원인이 이것이다

### 구조

```
user ─< user_role >─ role ─< role_permission >─ permission
                                                    │
                                          resource + action
                                          (order:read, product:update)

+ 스코프 판정
  order:read  × scope=own    → 자기 주문만
  order:read  × scope=seller → 자기 상품이 들어간 주문
  order:read  × scope=all    → 전체
```

권한은 코드가 아니라 데이터다. 새 역할은 행을 넣어서 만들고, 배포하지 않는다.

스코프는 두 자리에서 쓰인다. 단건 조회는 "이 행에 접근할 수 있나" 를 판정하고,
목록 조회는 애초에 조회 조건에 스코프를 섞어 넣는다. 후자를 빠뜨리면 목록에서 남의 데이터가 새어 나간다.

### 판정을 흔드는 축 네 개

역할 세 개 × 스코프 한 겹으로는 실무에서 권한이 무너지는 지점을 재현하지 못한다.
아래 네 축을 넣어서 판정 엔진이 실제로 어려워지게 만든다. 바깥 모델에서 가져온 근거는 `doc/reference/permission-models.md` 에 있다.

**1. 셀러는 사람이 아니라 조직이다**

한 셀러에 계정이 여럿 붙는다. 대표, 상품 담당, CS 담당, 정산 담당이 같은 셀러 안에서 하는 일이 다르다.
그래서 역할 부여에 대상이 붙는다. "이 사람은 CS 담당" 이 아니라 **"이 사람은 A셀러의 CS 담당"** 이다.
GitHub가 저장소별로 역할을 붙이는 방식과 같다. `scope=seller` 는 이 구조가 생겨야 판정할 것이 생긴다.

**2. 거부가 허용을 이긴다**

허용만 쌓으면 "정산 담당인데 이 셀러만 제외" 를 표현할 수 없다.
`role_permission` 에 allow/deny를 두고, 같은 권한이 양쪽으로 잡히면 deny가 이긴다.
스코프는 넓은 쪽이 이기고(`all` > `seller` > `own`) 효과는 좁은 쪽이 이긴다. 두 규칙의 방향이 반대라는 점이 판정의 핵심이다.

**3. 자원의 상태가 권한을 깎는다**

배송이 시작된 뒤에는 판매자도 주문을 못 고친다. 역할이 그대로여도 자원의 상태가 바뀌면 할 수 있는 일이 줄어든다.
역할·스코프 판정을 통과한 다음에 상태 조건을 한 번 더 본다. 주문 상태머신과 권한이 만나는 자리다.

**4. 권한 회수는 즉시 먹어야 한다**

세션 쿠키를 고른 이유가 즉시 회수인데, 판정 결과를 캐시하는 순간 그 근거가 깨진다.
캐시를 넣고, 역할이 바뀔 때 무효화가 실제로 동작하는지 테스트로 못박는다.

여기에 판정의 흔적을 남기는 감사 로그, 요청자와 승인자를 가르는 환불 승인, 관리자가 사용자 시점으로 보는 임퍼소네이션이 붙는다.
셋 다 "누가 무슨 권한으로 했나" 를 기록으로 남겨야 성립한다.

## 구간 — 배포를 결승선으로 놓는다

**2026-09-17 에 세웠다.** 그전까지 분할표는 **끝이 없는 프로젝트** 전제로 정렬돼 있었다 —
`CLAUDE.md` 「잡을 수 있는 청크가 여럿일 때 무엇부터」가 **「나중에 하면 비싸지나」로만** 차례를 정한다.
**포폴로 쓸 배포가 결승선으로 들어오면서 축이 하나 더 붙었다** — **보는 사람 눈에 닿나**.

**축 넷을 대체하지 않는다.** 구간 **사이**의 차례가 이 표고, 구간 **안**에서는 기존 넷이 그대로다
(깨진 것 > 비싸지는 것 > 결정 막힌 것 > 나머지). `D23` 축 1을 자리 안에서 쓰고 비용을 자리 사이에서
쓰는 것과 같은 모양이다.

**세운 이유는 실측이다.** 미착수 49를 성격으로 가르면 **기능 줄 26 · 운영 줄 9 · 공부 줄 9 · 문서·잔챙이 5**인데(2026-09-17. 처음 적을 때 잔챙이를 4로 세어 합이 48이었다 — 2026-09-18 에 다시 세어 고쳤다),
**그때까지 친 것이 거의 다 아래 셋**이었다(2026-09-17 하루만 해도 스물셋이 전부 게이트·문서·관측이다).
사이트를 열면 상품 사진도 검색도 리뷰도 없다.

**이 수는 그날의 스냅숏이다.** 청크를 세우거나 닫을 때마다 움직이므로 **차례의 근거로 쓰지 않는다** — 차례는 위 표가 들고, 이 수는 표를 왜 세웠는지만 든다. 2026-09-18 기준으로는 52다(기능 26 · 운영 9 · 공부 9 · 문서·잔챙이 8).

| 구간 | 청크 | 왜 여기 |
|---|---|---|
| **① 얼굴** | ~~`Q90`~~ → ~~`D17`~~ → ~~`26`~~ → ~~`27`~~ → ~~`28`~~ | **사진이 쇼핑몰의 최소 조건이다.** 검색 없는 쇼핑몰은 흔하고 리뷰 없는 것도 있는데 사진 없는 것은 없다. **`Q90` 이 먼저였다**(2026-09-18 완료) — 두 문서가 「`D17` 은 조건 미달」이라 적고 있어서, 안 고치면 첫 손이 자기를 막는 문장을 근거로 열게 된다 |
| **② 배포 앞** | ~~`Q84`~~ · ~~`71`~~ · ~~`Q94`~~ · ~~`Q95`~~ · ~~`Q96`~~ · ~~`Q97`~~ · ~~`Q98`~~ · ~~`Q99`~~ · ~~`Q100`~~ · ~~`Q101`~~ · ~~`Q102`~~ · ~~`Q103`~~ · ~~`Q104`~~ · ~~`Q105`~~ · ~~`64`~~ → ~~`65`~~ → ~~`Q91`~~ → `Q39` | `64` 의 덤프가 **호스팅 시드 주입 도구를 겸한다** — 기본 프로필은 `db/seed` 를 안 읽고(`application.yml`) `DemoOrderSeeder` 가 `@Profile("local")` 이라, 안 부으면 **빈 쇼핑몰이 뜬다**. `65` 는 선행이 0이라 지금이 제일 싸다. `Q91` 은 그 덤프를 `backend/README.md` 의 올리는 절차에 단계로 적는다 — 지금 그 문서만 보고 올리면 빈 쇼핑몰이 뜬다. **앞의 셋은 도메인이 붙는 순간 값이 오른다** — `Q94`(저작권 신고·삭제)는 **법 요건이라 파일이 바깥에서 보이기 전에 선다**(`R42`). — `Q84` 는 본문 없이 나가는 오류 셋이고(`D5` 계약 구멍) `71` 은 요청 횟수 제한이라, 로컬에서는 둘 다 티가 안 난다 |
| **③ 배포 뒤** | `42` · `D21` · `70` · `63` · `68` · `Q61` · `39` | **실측값이 있어야 값이 산다.** `D21` 은 선행이 없는데도 측정값이 없어서 못 쓰고, 그 뒤에 넷이 줄 서 있다. `Q61`(캐시 금지 헤더)은 실물 프록시를 지나야 재지고, `39`(판정 캐시를 Redis 로)는 인스턴스가 둘 이상일 때 뜻이 생긴다 |
| **④ 두께** | `5a` → `60`·`61` 검색 · `46`·`47`·`48` 리뷰 · `16`·`16a`·`16b`·`16c` · `49`·`50`·`51` 쿠폰 · `57` · `40`·`41` · `29`·`30`·`31` · `43a-4c` · `43a-5` · `11b` | 얼굴이 선 뒤에 더하는 것들. **`5a` 가 맨 앞이다** — `29`·`16a` 가 그것을 선행으로 들어서, 빠뜨리면 이 구간을 표대로 못 친다. **`11b` 는 법 요건인데 뒤에 둔다**(개인정보법 제22조의2 — `D2` `R29`) — 생년월일 칸부터 새로 만들어야 해서 지금은 칠 수가 없다. 법을 버리는 것이 아니라 **언제 치느냐만 비용으로 정한 것**이다(`CLAUDE.md` 「잡을 수 있는 청크가 여럿일 때」) |
| **⑤ 최후** | `74`·`75`·`76`·`72`·`73` · `2p` · `66`·`67`·`69` · `D14-2` · `5d` · `Q88`·`Q106`·`Q107`·`Q108` | 포폴에 안 보이고 값이 크다. **버리는 것이 아니라 맨 뒤다** — 공부가 목적의 절반이라(「대전제」 1) 남긴다. `D14-2`(비밀번호 블록리스트)는 목록 출처를 고르는 결정이 먼저고, `5d`(캡차)는 **이 프로젝트의 첫 외부 호출**이라 값이 크다. `Q88`(문서만 고친 청크의 표 대조)은 **지금도 CI 가 잡는다** — 로컬로 당기는 값만 남았다 |

**①의 첫 손이 순환을 깼다**(2026-09-18 완료). `D17` 은 선행(`D14`)이 다 찼는데 **「업로드가 0개라 정할 근거가 없다」**로 막혀 있었고,
그 근거를 만드는 `26` 의 선행이 `D17` 이었다. **`D12` 가 똑같은 모양이었고 2026-09-14 에 깼다** —
「이벤트 0개」가 순환이었고 append-only 표가 근거였다. **같은 수로 풀었다** — 정할 값을 이미 있는 것에서 세니 넷이 나왔다(입력 검증 표 · 판정 엔진 · `27` 의 경로 규칙 · 개인정보 셋 채우기).

**`Q39` 는 구간에 있어도 자동으로 안 친다.** 과금이 계속 붙고 바깥으로 나가는 작업이라
**그 직전에 사용자에게 다시 묻는다**(사용자 결정 2026-09-13, 2026-09-17 재확인).

**고른 안은 「다」다**(2026-09-17 에 고르고 2026-09-18 에 근거를 고쳐 다시 확인했다). 넷을 폈다 —
가(얼굴 다 채우고 배포) · 나(지금 올리고 쌓는다) · 다(최소 얼굴만 치고 배포) · 라(배포 접고 완성도).
**가는 5~7 세션을 더 기다려 아끼는 것이 그 기간 요금뿐**이라 비쌌다.

**나를 뺀 근거는 처음에 틀리게 적었다.** 「사진 없는 상품 목록이 포폴에서 역효과」라고 썼는데
`products/page.tsx:122` 와 `products/[productId]/page.tsx:107` 이 **이미 picsum 자리표시를 그린다** —
지금 올려도 사진 격자로 보인다. 정확히 없는 것은 **그 상품의 진짜 사진**이다.
**실제 근거는 요금 하나다** — 나로 가면 `Q39` 가 일곱 청크 앞으로 당겨져 그만큼 일찍 과금이 붙고,
`Q51` 배포 기준점 게이트도 같이 일찍 켜진다(그 뒤로는 적용된 마이그레이션을 못 고친다).

## 청크 분할표

파일 1~3개, 커밋 1개 단위다. 앞 청크가 미완이어도 뒤를 잡을 수 있게 자르려 했지만,
1~4는 토대라 순서를 지켜야 한다.

번호에 붙은 글자는 나중에 끼워 넣은 청크다. 앞 번호를 밀지 않으려고 이렇게 붙였다.

**미착수 행에는 넷을 적는다**(CLAUDE.md 「청크 하나를 어떻게 치나」).

| 칸 | 무엇 | 왜 |
|---|---|---|
| **축** | 법·표준·규약·관례 중 무엇이 근거인가 | 안 적혀 있으면 그 청크를 잡는 사람이 **기억나는 축만** 편다 |
| **강제 지점** | 제약·트리거·테스트 중 무엇이 막나 | 「다음에 조심한다」로 끝나는 청크는 아무것도 안 막는다 |
| **건드리는 자리** | 무엇을 신설하고 무엇을 고치나 | 범위를 미리 적어야 **잡기 전에** 값을 잴 수 있다 |
| **닫힘** | 초록이면 닫히는 **테스트의 클래스·메서드 이름**. 테스트로 못 잡는 청크는 무엇을 보고 닫는지 | 「무엇이 됐나」를 문장으로 두면 실행자가 닫힘을 정한다 — 이름을 적으면 설계가 정한다(`2t`) |

**넷 다 예보다 — 치면서 달라질 수 있다.** 달라지면 완료 행에 실제로 한 것을 적는다.
완료 행은 안 고친다 — 그때 그렇게 쳤다는 기록이다.

**빠진 행은 `doc-lint.sh` 가 센다**(`2t`). 지난 행 74개에 「닫힘」이 없어서 기준선을 74 로 시작했고 **줄기만 한다**(지금 56) —
새 행이 그 수를 늘리면 빨갛다. 지난 행에 닫힘을 채우는 것은 그 행을 잡을 때 한다.

**완료 행에는 결과만 적는다.** 무엇이 생겼고 어느 제약이 섰나까지고, **왜 그렇게 정했나와
무엇이 드러났나는 `PROGRESS.md` 이력이 든다**(그쪽 「기록 규칙」). 같은 말을 두 벌 유지하면
한쪽이 낡는데 **낡은 것을 아무도 안 잡는다** — `PlanProgressConsistencyTest` 는 「닫혔나」만 보지
내용을 안 본다.

**여기를 읽는 사람은 다음 청크를 고르는 사람**이라, 필요한 것은 「선행이 무엇을 남겼나」다.

완료 표시가 두 형식이다. **`~~취소선~~` 과 선행 칸의 `완료` 는 같은 뜻이다** —
취소선은 상세를 다시 쓴 행에만 붙었다. 형식 차이에 뜻이 없다.

| # | 청크 | 무엇을 하나 | 선행 |
|---|---|---|---|
| 1 | 저장소 구조 + Docker Compose | `backend/`·`frontend/` 골격, Postgres 컨테이너, README | 완료 |
| D1 | 문서 트리 규약 + 용어집 | `doc/README.md` 에 어떤 문서가 어디 가는지, 도메인 용어 한↔영 대조표 | 완료 |
| D2 | 법·정책 요건표 | 전자상거래법·개인정보보호법 요건을 조문 단위로 적고, 각 요건이 이 프로젝트의 어느 스키마·화면에 걸리는지 매핑 | 완료 |
| 2 | Spring Boot 기동 + DB 연결 | 의존성, `application.yml`, 헬스체크 API 1개, 마이그레이션 도구 | 완료 |
| 2a | ~~API 문서 자동화~~ | **완료.** `build.gradle.kts`(springdoc 3.1.1)·`application.yml`·`SecurityConfig.java`·`OpenApiSpecTest.java`(신설)·`stack.md`·`external-references.md`·`quality-gates.md`. **UI 스타터를 안 들였다** — 행이 연 것은 스펙 하나고 Swagger UI 는 정적 자원과 경로를 더 연다. 끄는 설정이 아니라 **안 들이는 것**이라 켤 자리가 없다. **3.x 를 골랐다**: 2.x 는 Boot 3 모듈 배치(`spring-boot-starter-*`)를 부르고 3.x 가 쪼개진 배치를 부른다 — Boot 4 에 2.x 를 얹으면 없는 좌표를 찾다 죽는다. **관례를 버린 자리 하나**: 기본 경로 `/v3/api-docs` 를 `/api/docs` 로 옮겼다 — 이 저장소 API 가 전부 `/api` 아래고 프론트 프록시도 `/api` 만 넘긴다. **예보에 없던 자리 둘**: `SecurityConfig` 의 공개 경로(기본이 `authenticated` 라 안 열면 스펙이 401)와 `/api/docs.yaml`(형제 경로라 `/api/docs/**` 에 안 걸린다). **부쉈다**(`D25`): `packages-to-exclude` 로 `order` 를 숨기니 `/api/orders` 가 빠져 빨갛고 되돌리니 초록. **축**: 표준(OpenAPI 3.1) + 관례(springdoc 기본값). **강제 지점**: 테스트(4위) — 스펙을 코드에서 뽑는 것 자체는 구조지만 「뽑은 것이 실제로 도나」는 테스트가 든다 | 완료 |
| 2c | ~~CI~~ | **완료.** `.github/workflows/ci.yml`(신설), `CLAUDE.md`·`security-baseline.md`·`testing-strategy.md` 수정. 잡 둘이 병렬로 돈다 — backend 는 `./gradlew build`, frontend 는 `npm ci` 뒤 `build`·`lint`·`test` 다(**프론트를 같이 거는 것은 사용자 선택** — 화면 청크가 앞으로 계속 나오는데 그쪽 검증만 사람 기억에 남는다). **Postgres 를 service 컨테이너로 안 띄운다** — 테스트가 Testcontainers 로 스스로 띄우고(`PostgresTestBase`) 러너에 Docker 가 이미 있다. 두 군데서 띄우면 어느 DB 에 붙었는지가 실행마다 달라진다. `npm install` 이 아니라 `npm ci` 다 — 잠금 파일과 어긋나면 조용히 맞추지 말고 실패해야 한다. 동시성 그룹으로 앞 회차를 접고 권한을 `contents: read` 로 좁혔다(`D14`). **아직 한 번도 안 돌았다** — 원격이 109 커밋 뒤에 있고(`08278b7` 이 마지막 푸시) 사용자가 푸시를 미뤘다. **그때까지 강제 지점은 안 걸린다**: 청크를 닫을 때 돌리는 것은 여전히 CLAUDE.md 「검증」 표다 | 완료 |
| 2c-1 | ~~문서 린트를 CI 로 옮긴다~~ | **완료.** `.github/workflows/ci.yml` 수정 하나. `D25` 가 층을 세면서 드러났다 — `scripts/doc-lint.sh` 가 **로컬에서만 돌아서** 「검증」 표가 요구해도 **사람이 기억해야 걸렸다**(5위). **여기서 새로 정한 것이 없다** — 명령은 「검증」 표가 이미 정했고 **도는 자리만 옮겼다**. 잡 이름은 `docs` 고 `backend`·`frontend`·`secrets` 와 독립이라 같이 돈다. **얕게 받는다** — 파일 내용만 보고 이력은 안 본다(gitleaks 와 다른 점이다). 스크립트가 순수 bash·awk 라 러너에 이미 있고 의존성을 안 더한다. **실제로 잡는지 확인했다** — 제목이 부서진 파일을 일부러 만들어 `exit=1` 을 보고 지웠다. `2f` 에서 **세워 놓고 안 돌려 본 테스트가 초록인 채 아무것도 안 막았던 것**을 겪어서 이번엔 그 자리를 먼저 밟았다 | 완료 |
| 2b | ~~로그·요청 추적 규약~~ | **완료.** `logback-spring.xml`·`RequestLogFilter`(신설), `build.gradle.kts`·`application.yml`, `ProblemFactory` 수정, `RequestTraceTest` 6개, `D16` 갱신. Micrometer Tracing + Brave 로 W3C `traceparent` 를 이어받고 MDC 로 모든 줄에 찍는다(사용자 선택 — 널리 쓰이는 표준). **Boot 4 는 자동설정이 모듈로 쪼개져서 브리지와 자동설정 모듈을 둘 다 넣어야 한다** — 하나만 넣으면 빈은 뜨는데 `Tracer.NOOP` 이라 조용히 안 찍힌다. **`D16` 의 "앞 6자리" 를 "뒤 6자리" 로 고쳤다** — Brave 128비트 ID 는 앞 8자리가 생성 시각이라 같은 시간대 요청이 전부 같아 보였다. **필터 자리는 보안 필터 바깥·추적 필터 안쪽** — 안쪽이면 401·403 이 한 줄도 안 남는다. **`ProblemFactory` 가 헤더를 직접 파싱하던 것을 걷어냈다** — 같은 사실을 두 곳에서 정하면 사용자가 불러 준 ID 로 로그를 못 찾는다. **미뤄 둔 `WARN` 둘(`5d` 캡차·`5c-2` Redis 강등)은 그 청크에서 붙인다** — 형식이 섰으니 막힌 것이 없다 | 완료 |
| 2e | ~~정적분석 게이트~~ | **완료.** `config/spotbugs/exclude.xml`(신설), `backend/build.gradle.kts`·`.github/workflows/ci.yml`·`stack.md` 수정. **파일 넷이라 상한(1~3)을 하나 넘겼다** — 흡수한 청크가 없으니 늘어난 것이 아니고, `stack.md` 를 뺄 수도 있었지만 `build.gradle.kts` 주석이 그 문서를 부르고 있어서 빼면 **가리키는 것이 없는 참조**가 된다. **먼저 측정하고 문턱을 정했다** — 그것이 `D25` 가 요구하는 순서다. 첫 측정 **100건**: `EI_EXPOSE_REP`/`REP2` **96**(record 가 `List` 를 들면 구조적으로 난다. 등급 최하), `VA_FORMAT_STRING_USES_NEWLINE` 2, `NP_NULL_ON_SOME_PATH` 1, `RCN_REDUNDANT_NULLCHECK` 1. **예보가 틀렸다** — 「기준선으로 눌러 두고 새 것만 막는다」고 적었는데 **누를 기준선이 없었다**: 넷을 실물로 열어 보니 **고칠 것이 0건**이라 제외 넷만 적고 게이트(`ignoreFailures = false`)를 바로 켰다. **제외 범위를 좁혔다** — 유형을 통째로 빼면 **앞으로 생길 것까지** 숨어서, 구조적인 첫째만 전역이고 나머지 셋은 클래스·메서드까지 좁혔다. **오탐 둘이 진짜 코드였다**: `InquiryQuery` 의 `\n` 은 텍스트 블록 SQL 이라 `%n` 으로 바꾸면 **SQL 이 플랫폼마다 갈리고**, `OrderStatusService` 의 null 검사는 `delivered_at` 이 nullable 이라 **지우면 배송 전 주문에서 진짜 NPE 가 난다**. **`test` 소스는 안 본다** — 픽스처가 일부러 이상한 코드를 쓰고 그 노이즈가 섞이면 목록 전체를 안 읽는다. **gitleaks 는 두 번째 그물이다** — 첫째인 push protection 은 **저장소 설정이라 코드가 아니고 사람만 켠다**. gitleaks 는 더 넓게 보지만 **푸시된 뒤에야 답한다**. **드러난 것 하나를 청크로 세웠다**(`2e-1`) — 기본 검출기의 신호가 얇다 | 완료 |
| 2e-1 | ~~보안 검출기를 얹는다~~ | **완료 — 얹었고, 예보 둘이 다 틀렸다.** `backend/build.gradle.kts`·`config/spotbugs/exclude.xml`·`stack.md`·`quality-gates.md`. **조건부였고 조건이 찼다**: CodeQL 이 java-kotlin 76규칙·javascript-typescript 87규칙으로 돌아 **경보가 state=all 에서 0** 이다 — 얇은 정도가 아니라 없다. **호환된다**: find-sec-bugs 1.14.0(2025-04-20, 행이 적은 2024-06 은 틀렸다)이 SpotBugs **4.10.4** 에서 돈다. **검출기가 실제로 도는 증거**: 얹자마자 `UNSAFE_HASH_EQUALS` 둘이 나왔고 그건 핵심 SpotBugs 에 없는 find-sec-bugs 전용이다(얹기 전 0건). **둘 다 오탐이라 제외했다** — `IdempotencyService` 가 재는 것은 그 사용자가 방금 보낸 요청 본문의 SHA-256 이라 알아낼 비밀이 없고 `findStored` 가 `user_id` 로 걸러 남의 해시가 오지도 않는다. **예보 ① 이 틀렸다**: `InquiryQuery.find` 를 안 잡는데 **그게 옳다** — 이 검출기는 모양이 아니라 **오염**을 보고, `find` 는 `private` 에 호출자 둘 다 파일 안 리터럴이라 바깥 값이 없다. **예보 ② 가 더 크다**: 진짜 오염 경로를 심어도 안 잡힌다 — `find("i.question like '%" + keyword + "%'", …)`, `keyword` 는 `public` 인자다(**부순 증거**, 되돌렸다). **find-sec-bugs 의 SQL 검출기가 `JdbcClient` 를 모른다** — `JdbcTemplate`·`PreparedStatement` 는 알고, 이 저장소 데이터 접근은 전부 `JdbcClient` 다. **그래서 SQL 조립을 자동으로 보는 눈이 지금 없다** — `2e-4` 로 처분했다. **닫힘을 못 채워서 고쳤다**: 옛 닫힘이 「`InquiryQuery.find` 를 한 번 잡은 것」이었는데 그 예보가 틀린 것이라 **잡히는 게 옳지 않다.** **축**: `D14` + 관례. **강제 지점**: 새로 안 만들었다 — `2e` 가 깐 게이트(4위)에 검출기만 얹었고, **늘어난 눈이 암호·역직렬화까지고 SQL 은 아니다** | 완료 |
| 2e-4 | ~~SQL 조립을 보는 눈이 있나 잰다~~ | **완료 — probe 다섯 판.** `stack.md`. **답: 없었다. 원인은 `JdbcClient` 다.** 같은 실행에서 CodeQL 이 `JdbcTemplate` 주입을 `java/sql-injection` **HIGH 로 잡고** `JdbcClient` 주입은 **안 잡는다**(174줄 대 184줄). **`build-mode` 는 무관하다** — `none` 에서도 `JdbcTemplate` 를 잡는다. `SqlTainted.qlx` 는 기본 묶음 80개에 들어 있고 로그에 로드 기록이 있다. 즉 **질의도 추출도 멀쩡하고 싱크 모델만 비었다.** **2·3회차를 버렸다** — 대조군 `probeJdbcTemplate` 를 컨트롤러가 안 불러서 `@RequestParam` 에서 그 싱크까지 경로가 없었다. `java/sql-injection` 은 **원격 오염원에서 출발하는 흐름**을 찾는데 출발점이 없었으니 안 잡히는 것이 당연했고, 그것을 모르고 두 판을 썼다. **조립 자리를 셌다**: `InquiryQuery.find` 의 `condition` **하나**뿐이고 `jdbc.sql(` 에 변수가 직접 드는 곳은 0건이다. 나머지 `.formatted` 는 전부 예외 메시지다. **그다음 수단을 정했다**(사용자 선택): 자리가 하나라 소스 훑기 테스트의 값이 작고, **싱크 모델을 늘리는 쪽이 자리가 늘어도 비용이 안 는다** — `2e-5`. **축**: `D14`. **강제 지점**: 없다 — 재는 청크고 결과가 `2e-5` 를 세웠다 | 완료 |
| 2e-5 | ~~`JdbcClient` 를 CodeQL 싱크로 선언한다~~ | **완료.** `.github/codeql/extensions/projectshop-java/{qlpack.yml,models/jdbc-client.model.yml}`(신설)·`.github/workflows/codeql.yml`·`stack.md`·`quality-gates.md`. **질의를 새로 안 짰다** — 기본 묶음의 `java/sql-injection` 이 이미 도니까(`2e-4` 가 로그로 확인했다) 그것이 보는 **싱크 목록에 한 줄**을 더한다(Models as Data). **사본을 두면 원본이 좋아질 때 우리 것만 낡는다.** **하위타입 칸이 `true` 여야 한다** — `JdbcClient` 가 인터페이스라 실제 객체는 구현체다. **상대 경로가 먹는다** — `config: packs: java: - ./경로` 로 걸면 되고 팩을 따로 게시할 필요가 없다. **부쉈다**(`D25`): `2e-4` 와 똑같은 probe 를 심으니 **2건 HIGH** 로 잡혔다 — 두 `jdbc.sql(...)` 호출을 각각 짚어서 오염이 `find` 를 지나 실행 지점까지 닿는 것을 다 추적했다. **본 코드에는 경보가 안 뜬다** — 호출자 둘 다 파일 안 리터럴이라 오염이 없다. **축**: `D14` + 플랫폼 사실(MaD 확장). **강제 지점**: **4위 CI 게이트의 눈을 넓혔다** — 검출기를 새로 안 만들고 목록에 행을 더했다 | 완료 |
| 2e-2 | ~~CodeQL 워크플로~~ | **완료.** `.github/workflows/codeql.yml`(신설)·`quality-gates.md`·`security-baseline.md`. **첫 측정 0건.** 그래서 문턱을 바로 「새 경보 0」으로 정했다 — `2e`(SpotBugs)와 같은 꼴이고 누를 기준선이 없다. **셋을 본다**: `actions`(워크플로 셋이 권한·시크릿·`pull_request_target` 을 다룬다)·`java-kotlin`·`javascript-typescript`. **`build-mode: none`** — Gradle 을 안 돌려서 빠르고 `ci.yml` 이 이미 `build` 를 돈다. 잡 시간 37초·57초·187초(병렬). **질의 묶음을 안 넓혔다** — 재기 전에 넓히면 무엇이 늘어난 것인지 안 갈린다. **부순 것이 이 청크의 값이다**: `probe/codeql` 가지에 요청 파라미터를 `Runtime.exec` 로 넘기는 컨트롤러를 밀었더니 **`java/command-line-injection` critical** 이 떴다. **`build-mode: none` 으로도 Spring 어노테이션을 풀어 흐름을 따라갔다** — 예보가 「안 잡히면 `autobuild` 로 바꾼다」였는데 그럴 필요가 없었다. **가지를 지우니 경보도 닫혔다**(저장소 경보 0으로 복귀) — CodeQL 은 파일이 아니라 **가지째 부수고 가지째 되돌린다**. **필수 상태 검사로는 안 걸었다** — 측정이 한 번뿐이라 아직 이르다. **축**: 규약(`D25`) + 관례(CodeQL 기본 질의 묶음). **강제 지점**: 4위 — CI 잡 | 완료 |
| 2e-3 | ~~의존성 취약점 — 경보·보안 갱신·Gradle 제출·npm audit~~ | **완료.** `.github/workflows/dependency-submission.yml`(신설)·`ci.yml`·`quality-gates.md`·`security-baseline.md`. **경보와 보안 갱신을 켰다**(`PUT vulnerability-alerts`·`automated-security-fixes`, 둘 다 204/`enabled`). **꺼져 있었다는 것이 이 청크의 전제였다** — 갱신 PR 이 매주 오고 있어서 경보도 켜진 줄 알았는데 **스위치가 둘**이다. 첫 측정 **경보 0건**. **`npm audit --audit-level=high` 를 CI frontend 잡에 넣었다** — `npm ci` 바로 뒤다. 로컬 첫 측정이 info~critical 전부 0이라 문턱을 바로 「high 이상 0」으로 정했다. **Gradle 은 같은 방식이 안 된다** — 잠금 파일이 없어 GitHub 이 못 읽고, `dependency-submission.yml` 이 그래프에 올려야 경보가 백엔드를 본다. `contents: write` 가 필요해서 **`D14` 「읽기만 준다」의 예외**고 그 이유를 파일에 적었다. **부순 것**: `probe/audit` 가지에 `lodash@4.17.20`(GHSA-xxjr-mmjv-4gpg, high)을 넣어 밀었더니 **frontend 잡의 `Run npm audit --audit-level=high` step 이 빨갛게 죽었다.** 가지를 지웠다. **못 한 것**: `dependency-submission.yml` 은 `main` push 에만 걸려 있고 `workflow_dispatch` 도 기본 가지에 그 파일이 있어야 뜬다 — **머지 전에는 못 돌린다.** 머지 뒤 SBOM 에 `org.springframework.boot` 가 드는지 확인한다. **축**: 규약(`D25`·`D14` A03) + 관례(GitHub 기본 흐름). **강제 지점**: 4위 — CI 의 `npm audit`. 경보는 5위라 게이트가 아니라 입력이다 | 완료 |
| 2e-6 | ~~tomcat 을 패치 버전으로 고정한다~~ | **완료.** `backend/build.gradle.kts`·`TomcatVersionTest`(신설)·`stack.md`. **`extra["tomcat.version"] = "11.0.25"` 한 줄이 셋을 덮는다** — `gradlew dependencies` 에 `tomcat-embed-core`·`-el`·`-websocket` 이 전부 `11.0.24 -> 11.0.25` 로 뜬다. **Boot 를 올려서는 못 닫는 자리였다**(`4.1.1` 이 최신, `4.1.2`·`4.2.0` 이 404). **강제 지점은 테스트(4위)** — 빌드 파일 문자열이 아니라 **런타임을 읽는다**: `ServerInfo.getServerNumber()` 가 `tomcat-embed-core` 안의 `ServerInfo.properties` 를 주므로 「그 줄이 있나」가 아니라 **해석된 판이 얼마인가**를 묻는다. BOM 이 나중에 더 높은 값을 주면 그 줄은 없어도 되고, 반대로 그 줄이 있어도 다른 의존성이 끌어내리면 소용이 없어서다. **부순 증거**: `11.0.24` 로 되돌리니 `TomcatVersionTest > 올라온 Tomcat 이 보안 패치 판 이상이다 FAILED`. **`commons-lang3` 오탐**은 모든 구성에서 `3.20.0` 이고 취약 범위가 `< 3.18.0` 인 것을 `gradlew dependencies` 로 확인했다 — dismiss 하는 `gh api` 가 권한에서 거부돼 **사람이 `inaccurate` 로 닫았다**(경보 #1, 2026-09-12). **tomcat 경보 셋은 `#39` 머지 직후 `fixed` 로 닫혔다.** **「미는 순간 닫힌다」고 적었던 것은 틀렸다** — `dependency-submission.yml` 이 `main` push 전용이라(작업 가지 좌표를 그래프에 안 남기려는 `2e-3` 의 설계) **머지해야 닫힌다.** 가지에 미는 것과 기본 가지에 들어가는 것이 의존 그래프에서 다르다 | 2e-3 |
| 2f | ~~의존성 갱신 자동화~~ | **완료.** `.github/dependabot.yml`·`.github/pull_request_template.md`·`StackVersionConsistencyTest`(셋 다 신설), `backend/build.gradle.kts` 수정. **주 1회 월요일이고 gradle·npm·github-actions 셋이다** — 액션도 의존성이라 굳으면 러너 쪽 변화에 CI 가 먼저 깨진다. **메이저를 마이너와 안 묶었다** — 한 PR 에 묶이면 「올려도 되나」의 답이 갈리는데 PR 은 하나라 **한쪽 판단으로 둘 다 들어간다**. **예보를 넘어섰다** — 행에는 「PR 템플릿 한 줄로 내린다」고 적었는데 그건 강제 지점 **5위**다. `stack.md` 가 스스로 「표가 낡을 수 있다」고 적어 뒀고 주 1회 갱신이 그 낡음을 빠르게 하니, **버전만이라도 4위(테스트)로 내렸다**: `StackVersionConsistencyTest` 가 「버전」 절의 표를 읽어 실제 파일과 대조한다. 판단이 필요한 나머지(적용범위 문서·메이저 영향)는 템플릿이 들고 **그건 여전히 5위인 것을 명시했다**. **테스트를 세우고 실제로 돌려 봤더니 안 걸렸다** — `stack.md` 를 일부러 틀리게 고쳤는데 빌드가 초록이었다. Gradle 이 `test` 를 `UP-TO-DATE` 로 건너뛴 것이고, `PLAN`·`PROGRESS` 가 이미 겪은 함정이다. **`inputs.files` 에 `stack.md` 를 더해서 잡히는 것을 확인했다** — 「걸려 있다」와 「돈다」는 다르다(「점검 > 걷는 순서」 3번 셋째). **절을 안 가르면 문서 안의 다른 3칸 표를 다 먹는다** — 첫 실행이 관례 표·정정 표까지 열여덟을 물었다. 「버전」 제목 아래로 좁혔고, **표를 한 줄도 못 읽으면 실패하게** 했다 — 절 이름이 바뀌면 조용히 통과하는 쪽이 더 나쁘다 | 완료 |
| 2f-1 | ~~의존성 갱신 첫 회차를 처분한다~~ | **완료.** `.github/dependabot.yml`·`backend/build.gradle.kts`·`backend/gradle/wrapper/gradle-wrapper.properties`·`doc/reference/stack.md` 수정. **PR 열한 개가 셋으로 갈렸다.** 초록 일곱을 다 머지했다(액션 다섯·`frontend-minor`·`@types/node`). **마지막 하나는 `package-lock.json` 충돌로 rebase 를 한 번 거쳤다** — 같은 파일을 만지는 PR 은 하나를 머지하는 순간 나머지가 낡는다. `backend-minor`(Boot 4.1.1·Gradle 9.7.1)는 **`StackVersionConsistencyTest` 가 잡은 것**이라 dependabot PR 을 닫고 `stack.md` 표와 한 커밋으로 다시 올렸다 — **강제 지점이 설계대로 물었고, 동시에 그것이 dependabot 이 혼자 못 넘는 자리라는 것도 드러났다.** `testcontainers` 2.0.5 는 좌표 재편이라 `2f-2` 로 세웠다. `typescript` 7·`eslint` 10 은 상류 미지원이라 **`dependabot.yml` 의 `ignore` 로 내렸다** — 그냥 닫으면 주마다 같은 빨간 PR 이 다시 열리고, 빨간 PR 이 쌓이면 「초록이 아니면 머지가 안 된다」가 신호를 잃는다. **`ignore` 마다 푸는 조건을 같이 적었다** — 조건 없이 막으면 영영 안 올라간다. **첫 회차 수 11 이 `D25` 의 「자동 머지 여부」 근거다**: 손을 안 대도 되는 것이 일곱, 사람이 판단해야 하는 것이 넷이었다 | 완료 |
| 2f-2 | ~~Testcontainers 2.x 로 이관한다~~ | **완료.** `backend/build.gradle.kts`·`PostgresTestBase`·`stack.md`·`testing-strategy.md`·`external-references.md`·`.github/dependabot.yml` 수정. **BOM 한 줄이 아니라 셋이 같이 움직였다**: ① 모듈 좌표에 `testcontainers-` 접두어가 붙었다(`org.testcontainers:postgresql` → `testcontainers-postgresql`) ② `org.testcontainers.containers.PostgreSQLContainer` 가 deprecated 고 새 자리는 `org.testcontainers.postgresql` 이다 ③ **새 클래스는 제네릭이 아니라** `<?>`·`<>` 를 그대로 두면 `does not take parameters` 로 막힌다. **Boot 의 `@ServiceConnection` 이 새 클래스로도 붙는 것은 문서로 안 보고 돌려서 봤다** — `spring-boot-testcontainers` jar 에 Postgres 팩토리가 아예 없어서(Boot 4 는 모듈이 쪼개져 있다) 정적으로는 확인이 안 됐다. **좌표를 기억으로 안 쓰고 실물에서 읽었다** — Maven Central **검색 API 는 색인이 늦어 2.0.5 를 0건으로 답했고**, `repo1.maven.org` 의 `maven-metadata.xml` 과 BOM POM 이 답을 줬다. 그 함정을 `stack.md` 와 `external-references.md` 에 적었다. **`dependabot.yml` 의 ignore 를 지웠다** — `2f-1` 이 「`2f-2` 가 이 줄을 지운다」고 조건을 적어 뒀고 그대로 밟았다. **축**: 플랫폼 사실(라이브러리가 좌표를 바꾼 것이라 고를 것이 없다). **강제 지점**: 새로 안 세웠다 — `StackVersionConsistencyTest` 가 이미 서 있어서 `stack.md` 표를 안 고치면 빨개진다. **옛 클래스가 남아 있고 경고만 뜬다** — `-Werror` 가 아니라 **안 고쳐도 빌드가 지나갔다.** 그래서 경고를 지우는 것까지 이 청크에 넣었다 | 완료 |
| 2f-3 | ~~자동 머지 — minor·patch 는 CI 초록이면 사람 없이~~ | **완료.** 증거가 왔다 — #31 을 2026-09-10 05:55Z 에 `github-actions` 가 머지했다(`autoMergeRequest` 있음). 사람이 한 것은 `@dependabot rebase` 한 번 — `strict: true` 가 만든 구멍이고 `2h` 가 세는 자리다. 게이트 표 「부순 날」에 #31. **원래 계획**: **워크플로는 섰다.** `.github/workflows/dependabot-automerge.yml`(신설)·`.github/dependabot.yml`·`quality-gates.md`·`PLAN.md`(`2h`). 저장소의 `allow_auto_merge` 를 켰다(`true` 확인). **첫 회차가 근거다** — PR 열 중 초록 일곱을 전부 머지하고 메이저 셋을 닫았으니 **사람이 한 것은 초록을 보고 누른 것뿐**이다. `pull_request_target` + `fetch-metadata@v3`, `update-type` 이 메이저가 아니면 `gh pr merge --auto --merge`. **`--auto` 는 지금 머지가 아니라 예약이다** — 필수 검사 넷이 초록이 되는 순간 GitHub 이 머지하고, 빨가면 그냥 안 일어난다. **막는 것은 여전히 CI 다.** **PR 코드를 체크아웃하지 않는다** — 이 이벤트에서 남의 코드를 실행하면 쓰기 토큰이 그리로 간다. 메타데이터를 읽고 머지를 걸 뿐이다. **`strict: true` 가 만든 구멍 하나**: 뒤처진 PR 은 자동 머지가 멈추는데 이 워크플로는 PR 이 열릴 때만 돈다. **`2h` 가 `mergeStateStatus == BEHIND` 를 세게 했다** — 그때 사람이 `@dependabot rebase` 를 단다. **남은 것: 증거.** `pull_request_target` 이라 `workflow_run` 으로 못 부르고 **다음 월요일 회차의 첫 minor PR 이 손 없이 머지되는 것**이 증거다. 그 PR 번호로 「부순 날」 칸을 채우고 이 행을 닫는다. **축**: 규약(`D25`·`2f`) + 관례(GitHub 공식 예제). **강제 지점**: 없다 — 사람 손을 뺄 뿐 막는 것은 CI 그대로다 | 완료 |
| 2f-4 | ~~Dependabot 이 초록이 될 수 없는 PR 을 여는 것을 막는다~~ | **완료**(2026-09-15). `.github/dependabot.yml` 한 파일. **PR #44 가 실물이다** — `@eslint/js` 를 10 으로 올렸는데 그것이 `eslint@^10` 을 peer 로 요구해서 `npm ci` 가 `ERESOLVE` 로 죽었다(`npm error Conflicting peer dependency: eslint@10.10.0`). `frontend`·`smoke` 둘이 빨갰고 **고칠 방법이 없는 PR** 이라 닫았다. **원인은 막는 단위였다**: `eslint` 메이저는 이미 막아 뒀는데(`eslint-plugin-jsx-a11y@6.10.2` 의 peer 가 `^9` 까지고 접근성 규칙이 거기 있어 뺄 수 없다 — `D20`, PR #14) **`@eslint/js` 를 안 막아서** 같은 것이 다른 이름으로 들어왔다. **이름에서 계열로 한 칸 올렸다** — `eslint*`·`@eslint/*`. 지금 넷이고(`eslint`·`@eslint/js`·`eslint-config-next`·`eslint-plugin-jsx-a11y`) 더 늘 수 있는데, **하나씩 적으면 새로 생기는 것을 반드시 빠뜨린다.** `eslint-config-next` 는 eslint 10 을 받지만 본체가 9 라 그것만 올려서 얻는 것이 없다. **조건이 풀리는 날**: 플러그인이 `eslint@^10` 을 peer 로 받으면 두 줄을 같이 지우고 계열을 한 번에 올린다 — `npm view eslint-plugin-jsx-a11y@latest peerDependencies` 가 그 자리다. **축**: 규약(`2f` 갱신 정책) + 플랫폼 사실(peer 가 안 맞으면 `npm ci` 가 죽는다). **강제 지점**: 설정 — CI 가 아니라 **Dependabot 서버가 읽는 자리**라 로컬에서 못 부순다. **안 막으면 무슨 일이 나는지는 PR #44 가 실물로 냈다.** **닫힘**: 다음 월요일 회차에 `@eslint/js` 메이저 PR 이 안 열린다 | 완료 |
| 2g | ~~PR 흐름 + AI 리뷰~~ | **완료.** `.github/workflows/claude-review.yml`(신설), `CLAUDE.md` 수정(「재개 프로토콜」 7번, 「검증 > CI」에 절 하나). **`main` 직커밋이라 리뷰가 붙을 자리가 없었다** — 도구 설치가 아니라 **흐름 변경 + 도구**의 두 몸이다. **청크당 PR 이다**(사용자 선택) — 세션당으로 묶으면 리뷰가 여러 청크를 한 덩어리로 봐서 **어느 D 문서를 펼지 못 고르고 지적이 어느 청크 것인지 흐려진다.** 게다가 마무리와 단위가 같아져서 겹친다. 가지 이름은 청크 번호를 그대로 쓰고(`chunk/<번호>`) PR 본문에 **시작 세 줄**을 적는다 — 그것이 리뷰의 첫째 물음(「말한 축과 편 축이 같나」)의 입력이다. **리뷰가 보는 넷은 전부 빌드·테스트가 초록이어도 안 걸리는 것**이다: 말한 축·강제 지점을 더 내릴 수 있었나·이 PR 안의 결정 충돌·문서가 부르는 것. **구독 토큰을 쓴다**(`CLAUDE_CODE_OAUTH_TOKEN`) — 이 저장소의 전제가 「주간 토큰이 남을 때 태운다」라 별도 과금을 새로 여는 것이 앞뒤가 안 맞는다. **모델은 `claude-sonnet-5`** — 청크당 PR 이라 PR 수가 많다. **시크릿이 없으면 잡을 통째로 건너뛴다** — 없다고 PR 마다 빨간 것이 뜨면 **그 빨강이 진짜 실패를 가린다**(「지금 깨져 있는 것」이 우선순위 1번인 이유). **권한을 `contents: read` 로 좁혔다** — 액션 문서 예시는 `write` 다. 리뷰는 코멘트만 달지 코드를 안 고치니 읽기면 된다는 판단이고, **첫 PR 에서 권한으로 죽으면 그때 넓힌다**고 파일에 적었다. **사람만 할 수 있는 것이 셋이었다** — 시크릿 등록, **Claude GitHub App 설치**, `main` 가지 보호(직푸시 차단·CI 를 필수 상태 검사로). **둘째를 이 청크가 빠뜨렸다**: 액션 문서(`setup.md`)가 API 키 등록만 적어 두고 App 설치를 안 적어서, 첫 실행이 `Claude Code is not installed on this repository` 로 죽고서야 드러났다. **토큰은 「누가 부르나」를 증명하고 App 은 「이 저장소에 붙어도 되나」를 준다** — 둘이 다른 것이다. **그 둘 전에는 강제 지점이 하나도 안 걸린다**: 규약이 「PR 로 연다」고 적혀 있어도 `main` 이 열려 있으면 안 지킬 방법이 있다 | 완료 |
| 2g-1 | ~~쌓아 올린 PR 을 어떻게 머지하나~~ | **완료.** `.claude/settings.json`·`CLAUDE.md` 수정. **직렬로 정했다**(사용자 선택) — PR 은 한 번에 하나만 열고 앞 것을 머지한 뒤 다음 청크를 잡는다. 후보 셋 중 ①(base 를 언제나 `main`)은 **혼자로는 안 풀린다**: base 사고는 막지만 `PLAN.md`·`PROGRESS.md` 가 모든 청크에 딸려 와서 **평행으로 열면 둘째부터 무조건 충돌**한다. ③(기록 분리)은 직푸시 금지와 부딪친다. **오늘 세션이 직렬을 실측했다** — `#19`→`#20`→`#21`→`#22` 를 그렇게 돌려 충돌 0·base 문제 0 이었고, 반대로 `2f-1` 에서 기다리는 대신 한 PR 에 커밋 셋을 쌓은 것이 **이 미결이 만든 우회**였다. **「청크 규칙」과 안 부딪친다** — 거기서 말하는 독립은 「세션이 끊겨도 다음을 잡는다」지 PR 을 동시에 열라는 뜻이 아니다. **리뷰 잡까지 기다린다**(사용자 선택): 리뷰는 게이트가 아니지만 **머지한 뒤에는 그 지적을 그 PR 에서 못 고쳐서** 「드러난 것의 처분」이 한 박자 늦는다. 6~7분으로 CI 중 제일 길다. **강제 지점**: `PreToolUse` 훅(3위 앱 검증에 해당한다 — 명령이 나가기 전에 막는다). base 가 main 이 아니면 막고, 열린 `chunk/*` PR 이 있어도 막고(dependabot PR 은 안 센다), 머지는 `gh pr checks` 의 종료 코드로 판정한다 — **8=대기 · 1=빨강 둘 다 막는다**(코드는 기억으로 안 쓰고 실측했다). **오탐을 하나 만들었다가 고쳤다** — 처음 판은 페이로드에 그 명령 **글자**만 있어도 걸려서, 이 행을 쓰는 heredoc 자체가 막혔다. 명령 자리(줄머리·`;`·`&`·`|` 뒤)에 있을 때만 보게 고쳤다. **남은 한계를 인정한다** — 따옴표 안에 그 명령을 그대로 적은 문자열은 여전히 명령으로 읽힌다. 문서에서 옵션 이름을 홑따옴표로 감싸 쓰면 안 걸리고, 그 이상은 안 좁혔다. **실제로 막는 것과 안 막는 것을 둘 다 확인했다** | 완료 |
| 2g-2 | ~~리뷰가 왜 안 도는지 잡고 `show_full_output` 을 되돌린다~~ | **완료.** `.github/workflows/claude-review.yml`. **원인은 401 인증 실패였다** — `show_full_output` 을 켜자 `Failed to authenticate. API Error: 401 Invalid bearer token` 이 나왔다. 그전에는 `is_error: true` 만 보여서 **모델·워크플로 검증·인증 중 어느 것인지 못 갈랐다.** 토큰을 다시 등록하니 바로 돌았다(112초). **셋 다 원인이 달랐다** — 1회차 App 미설치, 3회차 워크플로 검증(액션이 자기 파일을 `main` 과 대조한다), 2·4회차 인증. `--model` 은 원인이 아니었다: 뺐는데 같은 실패였고 로그의 `claude-sonnet-5` 는 **액션 기본값**이다. **진단이 끝나서 `show_full_output` 을 걷었다** — public 저장소라 켜 두면 프롬프트와 응답이 계속 공개로 남는다. **첫 리뷰가 코멘트를 안 달았다** — 프롬프트가 「검토한다」까지만 말하고 **어디에 남기라고 안 했다.** `gh pr comment` 로 남기라는 줄을 더했다: 로그에만 있으면 아무도 안 읽는다 | 완료 |
| 2g-3 | ~~`main` 가지 보호를 켠다~~ | **완료.** `PROGRESS.md`·`CLAUDE.md` 수정(설정 자체는 GitHub 쪽이라 저장소에 파일이 안 생긴다). `2g` 가 **사람만 할 수 있는 것**으로 남겨 둔 셋 중 마지막이다 — 그전까지 「PR 로 연다」의 강제 지점이 **문서와 세션 훅뿐**이었다. **건 것**: PR 필수(승인 0 — 1인 저장소라 승인을 요구하면 자기 PR 을 자기가 못 통과시킨다), `backend`·`frontend`·`secrets`·`docs` 를 필수 상태 검사로, `strict`(최신 `main` 기준), force push·삭제 차단. **`review` 는 일부러 뺐다** — 워크플로 파일을 고치는 PR 에서 그 잡이 `skipping` 이라(액션이 자기 파일을 `main` 과 대조한다) 필수로 걸면 **그런 PR 이 영영 머지 불가**가 된다. 리뷰는 원래 게이트가 아니라 조언이다(`2g`). **`enforce_admins` 는 `false` 다**(사용자 선택) — 관리자가 뚫을 수 있어 **완전한 1위는 아니지만**, CI 설정 자체가 깨졌을 때 저장소가 잠기지 않는다. 한 주 돌려 보고 안 뚫고 살 만하면 `true` 로 올린다. **축**: 규약. **강제 지점**: **저장소 설정** — `2g-1` 의 `PreToolUse` 훅은 이 세션 안에서만 살지만 가지 보호는 **다른 기계·다른 도구·손으로 밀어도** 걸린다. **막는 것을 실물로 확인하진 않았다**(사용자가 시험 푸시를 중단시켰다) — API 응답으로만 확인했다 | 완료 |
| 2g-4 | ~~PR 을 청크마다 안 열고 마무리 때 연다~~ | **완료.** `.github/workflows/ci.yml`·`CLAUDE.md`·`doc/process/wrapup.md`·`.claude/settings.json`. **`2g` 가 정한 「청크 하나 = PR 하나」를 뒤집었다**(사용자 결정) — 병목이 실측됐다: 2026-09-05 하루에 PR 열하나를 열었고 **`review` 잡이 매번 5~7분**이라 순수 대기가 한 시간에 가까웠는데 **리뷰 지적은 0개**였다. 이제 청크는 커밋으로 `work/<날짜>` 가지에 쌓이고 **마무리가 PR 하나로 낸다.** **성립 조건이 하나 있었고 그게 이 청크의 실제 작업이다** — `ci.yml` 이 `push: branches: [main]` 이라 **작업 가지에 밀면 CI 가 안 돌았다.** 그대로 뒀으면 청크 다섯을 리눅스 검증 없이 치게 되고 `2c` 가 세운 값(로컬 Windows 가 못 잡는 종류)이 통째로 사라진다. 가지 제한을 떼서 **PR 없이도 청크마다 CI 가 돈다.** `concurrency` 가 같은 가지의 앞 실행을 취소해서 낭비는 안 는다. **머지는 `--merge` 다**(사용자 선택) — squash 면 청크 다섯이 한 커밋이 되어 **하나만 되돌릴 수가 없다.** 가지 이름을 `chunk/<번호>` 에서 `work/<날짜>` 로 바꿨고(여러 청크가 쌓이므로) `2g-1` 훅의 직렬 검사가 둘 다 세게 넓혔다. **축**: 규약. **강제 지점**: 없다 — 흐름을 바꾼 것이라 새로 걸 자리가 없고, `2g-1` 훅과 `2g-3` 가지 보호는 그대로 산다 | 완료 |
| 2g-5 | ~~리뷰가 코멘트를 남기게 하고, 잡는지 부순다~~ | **토막 (가) 완료**(2026-09-06). `.github/workflows/claude-review.yml`·`quality-gates.md`·`stack.md`. 도구 셋을 열었고(`claude_args`), 프롬프트가 PR 번호와 `<!-- claude-review -->` 마커를 받고, **액션 뒤에 코멘트 확인 step 이 섰다** — 마커가 없으면 잡이 빨개진다. 게이트 표에 4위 행이 하나 늘었다(예보에 없던 것. 「이 청크가 남긴 강제 지점」이 그 행이라 같이 적었다). YAML 은 파서로 확인했다 — step 셋, `claude_args` 한 줄, 마커 지시 있음. **마무리 PR(#30)이 그 step 의 결함을 바로 잡았다** — 액션이 `2g-3` 대로 조용히 건너뛰었는데(이 워크플로를 고치는 PR 이라 자기 파일을 `main` 과 대조한다) **step 자체는 `success` 라 겉에서 안 갈리고**, 확인 step 이 그것을 「코멘트 없음」으로 읽어 빨개졌다. **`execution_file` 이 비면 안 돈 것**이라 그때는 통과시키게 고쳤다. **게이트를 세운 자리에서 그 게이트가 스스로 오탐을 낸 것**이고, 부순 증거를 요구하는 규칙(`2m`)이 아니라 **실제 PR 이 잡았다.** **토막 (나)의 첫 실물이 PR #36 이다**(2026-09-11) — probe 가지를 따로 안 팠다. 마무리 PR 이 그 조건을 그대로 만족했다(워크플로를 안 고친 PR). **판정: 도구를 열었는데도 안 달렸다.** `allowedTools` 셋이 로그에 제대로 찍혔고(`Bash(gh pr comment:*)`·`diff`·`view`) `is_error: false` 인데 **`permission_denials_count` 가 다시 12** 고 코멘트는 0개였다. **토막 (가)가 원인을 못 짚었다는 뜻이다.** 무엇이 거부됐는지는 액션이 가린다(`full output hidden for security`) — **추측으로 도구를 더 열지 않고**(`2e-1`·`Q21` 과 같은 방법) `show_full_output: true` 만 켰다. **이 PR 에서는 그 효과가 안 나온다**(`2g-3` — 워크플로를 고쳤으니 다음 실행은 건너뛴다). **PR #37 에서 코멘트가 달렸다**(2026-09-11) — **PR 열둘 만에 처음이다.** 거부는 여전히 있다(**19건**, `is_error` 에 `true` 도 섞였다) 고 그중 **`Bash(cat` 이 다섯**이다 — 아래 후보 ①이 맞았다. **모델이 `--body-file` 대신 `--body` 로 우회해서 달렸다.** 즉 **막고 있던 것은 도구가 아니라 경로였다**: 시킨 방법(`--body-file`)이 닫혀 있고 안 시킨 방법이 열려 있었다. **`show_full_output` 이 그것을 보이게 했다** — 켜기 전에는 같은 수가 로그에 가려 있었다. **남은 것: `Bash(cat:*)` 을 열지, 프롬프트를 `--body` 로 바꿀지.** 후보 둘을 적어 둔다: `--body-file` 을 쓰려면 파일을 먼저 만들어야 하는데 `Write` 도 `Bash(cat:*)` 도 안 열려 있다, 그리고 프롬프트가 「파일은 읽기 도구로 연다」고 시키는데 `Read` 가 열려 있는지 확인이 안 됐다. **목록을 보고 정한다.** **남은 것: 토막 (나) — probe.** 이 묶음이 `main` 에 머지된 직후, 열린 작업 PR 이 없을 때 아래 ⑦~⑪ 를 친다. **원인은 갈라져 있다. 다시 조사하지 않는다**(2026-09-06 설계 세션). 리뷰 실행 여섯의 로그가 전부 `permission_denials_count` 12~22 고 워크플로 입력은 `claude_args: ""`·`track_progress: false` 다. 액션 공식 문서: 「By default, Claude cannot execute Bash commands unless explicitly allowed」. 프롬프트가 시킨 `gh pr comment` 가 매번 거부됐다. 「없으면 「없다」 한 줄」도 안 달렸으니 **지적이 없어서가 아니라 코멘트 경로가 막힌 것**이다. **결정 넷.** (가) 여는 도구는 공식 예제의 셋뿐 — 인라인 코멘트 도구는 안 연다. 묻는 넷이 PR 단위라 줄에 흩어지면 처분할 목록이 안 된다. (나) PR 번호는 프롬프트 본문에 박는다 — `$PR_NUMBER` 가 Claude 의 셸에 있는지 확인이 안 됐다. (다) 액션 뒤에 **마커 확인 step** 을 둔다. 작성자 이름으로 안 센다: 토큰 종류에 따라 `claude[bot]`·`github-actions[bot]` 로 갈린다. (라) 검증은 **일부러 어긴 PR** 로 한다 — 코멘트가 붙는 것과 잡는 것은 다른 사실이다. **절차. 두 토막이고 (나) 까지 해야 닫힌다.** **토막 (가) — 워크플로 수정. 이번 `work/` 묶음에 실린다.** ① `claude-review.yml` 의 `with:` 에 `claude_args: --allowedTools "Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*)"` 를 더한다. ② `prompt:` 첫 줄에 `PR 번호: ${{ github.event.pull_request.number }}` 를 넣고, 끝의 `gh pr comment $PR_NUMBER --body-file <파일>` 을 `gh pr comment <위 PR 번호> --body-file <파일>` 로 바꾸고, 그 앞에 「**코멘트 본문의 첫 줄은 정확히 `<!-- claude-review -->` 다.** 그 뒤에 검토 결과를 쓴다」를 더한다. ③ 액션 step 뒤에 step 을 하나 더한다 — `if: env.REVIEW_TOKEN != ''`, `env: GH_TOKEN: ${{ github.token }}`, 실행은 `n=$(gh pr view ${{ github.event.pull_request.number }} --json comments --jq '[.comments[].body | select(startswith("<!-- claude-review -->"))] | length'); [ "$n" -ge 1 ] || { echo '리뷰가 코멘트를 안 남겼다(2g-5)'; exit 1; }`. 주석에 「리뷰 내용은 조언이고 이 step 은 **출력이 있나**만 막는다」를 적는다. ④ `quality-gates.md` 「아직 못 정하는 것」의 AI 리뷰 줄을 고친다 — 「언제 나오나」를 「**`2g-5` 전에는 입력이 0이었다** — 도구 거부로 코멘트가 한 번도 안 달렸다. probe PR 이 첫 입력이다」로. ⑤ `stack.md` 「기억으로 쓰면 틀리는 자리」에 절 셋 — 「claude-code-action 은 Bash 를 기본으로 안 준다」(신호는 로그의 `permission_denials_count`, 거부 목록을 보려면 `show_full_output: true`), 「액션은 `CLAUDE.md`·`.claude` 를 `origin/main` 에서 되살린다」(로그 문구 「PR head is untrusted」. **규칙을 고친 PR 은 옛 규칙으로 검토된다**), 「리뷰 한 번이 약 $1.1·5분이다」. ⑥ 커밋. `PROGRESS.md` 에 **`진행중`** 으로 남기고 「남은 것: 토막 (나)」를 적는다 — 워크플로를 고친 PR 은 그 잡이 `skipping` 이라(`2g-3`) **이 묶음 안에서는 확인이 안 된다.** **토막 (나) — probe. 이 묶음이 `main` 에 머지된 직후, 열린 작업 PR 이 없을 때**(`2g-1` 훅이 막는다). ⑦ `main` 에서 `probe/review` 가지를 판다. 위반 둘을 심는다 — **A**: `doc/reference/testing-strategy.md` 「층」 절 끝에 「`ReviewProbeMissingTest` 가 이 층의 회귀를 고정한다」 한 줄(그 클래스는 없다. 넷째 물음). **B**: `backend/src/test/java/com/projectshop/shop/ReviewProbeTest.java` 에 `assertEquals(2, 1 + 1)` 하나짜리 단위 테스트(`@Tag("db")` 없이). ⑧ 커밋 뒤 `gh pr create --base main` 로 연다. 본문의 시작 세 줄은 정확히 이것이다 — 「무엇을 닫나: `sku.price` 가 음수로 들어오는 것」·「걸리는 축: 규약(`D8`)」·「**강제 지점을 어디에 두나: DB 제약(`check`)으로 내린다**」. diff 는 테스트뿐이라 첫째 물음 위반이다. ⑨ `gh run watch` 로 `review` 잡을 기다린다. **판정표**: 코멘트가 붙고 A·B 둘 다 짚었다 → 닫는다. 붙었는데 「없다」거나 하나만 짚었다 → 도구는 됐고 **프롬프트 문제**다. 그 사실을 `quality-gates.md` 리뷰 줄에 적고 이 청크는 닫되 `2g-6`(프롬프트 조정)을 분할표에 세운다. 안 붙고 ③ 이 빨갛다 → `show_full_output: true` 를 켜고 다시 밀어 거부 목록을 읽는다. 그래도 안 되면 `track_progress: true` 를 더한다. 코멘트가 **둘** 붙었다(액션의 sticky 하나 + `gh` 하나) → `use_sticky_comment: true` 를 지운다. ⑩ `gh pr close <번호> --delete-branch`. **머지하지 않는다.** ⑪ `PROGRESS.md` 이력에 결과와 probe PR 번호를 적고 분할표의 이 행을 닫는다. **검증**: 이 청크는 코드를 안 건드린다 — 토막 (가)는 CI 초록, 토막 (나)는 위 판정표가 검증이다. 문서는 훅이 돈다. **축**: 규약(`D25`) + 관례(액션 기본값 — 따르는 셋에 근거를 적는다). **강제 지점**: 4위 — ③ 의 확인 step. 리뷰는 여전히 조언이고 **아무것도 안 내놓는 5분만** 빨강이 된다. **건드리는 자리**: `.github/workflows/claude-review.yml`, `quality-gates.md`, `stack.md`. probe 가지는 파일 수에 안 센다 — 닫으면 사라진다 **닫힘**: 마무리 PR 의 코멘트에 `<!-- claude-review -->` 마커와 `2w` 의 표가 있다 — `gh pr view <n> --comments`. 없으면 토막 (나)의 판정표대로 원인을 가른다. **닫았다 — 원인이 도구가 아니라 경로였다**(2026-09-11, PR #37). **코멘트가 달렸다**: 2,827자·57턴·$2.45 고 `2w` 가 시킨 표 꼴 그대로다. **거부는 19건으로 남아 있었고 그중 `Bash(cat` 이 다섯**이다 — `show_full_output` 이 그것을 보이게 했다. **시킨 방법(`--body-file`)이 닫혀 있고 안 시킨 방법(`--body`)이 열려 있었다.** 모델이 우회해서 달렸다. **토막 (가)의 진단이 왜 틀렸는지가 여기서 나온다**: 로그의 거부 수만 보고 「도구를 열면 된다」고 읽었는데, 열어야 했던 것은 `gh` 셋이 아니라 **그 앞의 파일 쓰기**였다. **처분(사용자 선택): 도구를 안 늘리고 프롬프트를 `--body` 로 바꿨다** — `cat` 을 열면 리뷰가 저장소 아무 파일이나 읽게 되고, 결정 (가)의 「공식 예제 셋뿐」을 뒤집는다. **`use_sticky_comment` 도 같이 뺐다**: 그 설정은 **액션 자신의 코멘트**를 재활용하는 것인데 이 워크플로는 프롬프트가 `gh` 로 직접 달아서 **경로가 달라 아무것도 안 걸렸다**(회차 둘에 코멘트 둘). 겹침은 `--edit-last --create-if-none` 이 막는다. **안 걸리는 설정을 켜 두면 「막고 있다」고 읽힌다** — 이 저장소가 세 번 밟은 자리다. **probe 가지를 안 팠다**: 마무리 PR 이 그 조건(워크플로를 안 고친 PR)을 그대로 만족했고, 리뷰가 **진짜 지적 하나**를 냈다(같은 PR 이 가드를 코드에 넣고 `D24` 문장을 안 고친 것). 심은 위반보다 나은 증거다 | 완료 |
| 2h | ~~주간 상태 리포트~~ | **완료.** `scripts/weekly-report.sh`(신설)·`.claude/settings.json`(SessionStart)·`quality-gates.md`. **입력 여섯을 싼 것만으로 짰다** — 훅이 부르므로 느리면 세션 여는 것이 느려진다. **테스트를 안 돌린다**: 빠른 레인 비율을 `PostgresTestBase`·`HttpTestBase` 상속 여부로 세고(케이스가 아니라 **파일 수** 비율이다), 망 호출은 전부 `timeout 10` 을 걸고 실패하면 그 줄만 비운다. **리포트가 통째로 안 나오는 것보다 낫다.** **조건이 찬 기준 문서는 판정하지 않는다** — 조건이 「그 문서가 다루는 것이 코드에 하나라도 있나」라 기계가 못 읽는다. 그 줄을 그대로 보여 주고 **읽는 사람이 센다.** **주 1회는 `.git/weekly-report-stamp` 가 잰다**(검증 도장과 같은 자리). **첫 실행이 바로 값을 했다**: 한 번도 안 본 줄이 **셋**으로 맨 위에 떴다 — 세로·앱 검증, 세로·테스트, **품질 게이트**. 셋째는 「현재 상태」의 「다음 점검」 줄이 안 적고 있던 것이다. **빠른 레인이 파일 20%** 라는 수도 그때 처음 보였다 — **그 수는 틀렸다**: 분자와 분모를 다른 집합에서 세서 `BackendApplicationTests.java` 가 분자에만 들었다. 9차 마무리의 독립 리뷰가 잡았고 **101개 중 21개(21%)** 다. **밟은 것 둘**: `echo "### \`main\` …"` 의 백틱이 명령으로 실행됐고, 「한 번도 안 봤다」 줄이 `sort` 파이프로 같이 들어가 **키가 없어 버려졌다** — 가장 오래된 것이 목록에서 빠지는 모양이라 정렬 키를 `0000-00-00` 으로 통일했다. **부쉈다**(`D25`): 도장을 지우면 리포트가 붙고(줄 40+), 찍힌 뒤에는 「현재 상태」만 나온다(21줄). **축**: 규약. **강제 지점**: 없다 — 읽는 글이다. **도착 자리가 대신한다** | 완료 |
| 2l | ~~공개 저장소에 라이선스를 단다~~ | **완료.** `LICENSE`(신설, 원문 11,358바이트)·`README.md`·`frontend/package.json`·`coding-rules.md`. **Apache-2.0**(사용자 선택). **조건이 이미 차 있었다** — `coding-rules.md` 라이선스 절이 「공개하기로 하면 이 절을 먼저 다시 본다」고 적어 뒀는데, `gh api` 로 재 보니 저장소가 벌써 `public` 이었다. **공개인데 라이선스가 없으면 저작권법 기본값(모든 권리 유보)이라 읽을 수는 있고 쓸 수는 없다.** 출처 축 1위(법)라 미룰 수 없었다. **파일마다 헤더는 안 달았다** — `LICENSE` 하나로 성립하고 헤더는 권장이다. **`NOTICE` 도 안 만들었다** — 남길 고지가 없고, 스킬의 MIT 고지는 `2l-1` 이 그 파일 옆에 둔다. `package.json` 은 `private: true` 인데도 `license` 를 적었다 — 배포는 안 하지만 **선언이 저장소와 갈리면 안 된다**. **검증**: `npm run build` 초록, `doc-lint.sh` 통과, 원문 바이트 수 확인. **축**: **법**(저작권법) + 규약(`D23` 라이선스 절). **강제 지점**: 없다 — 파일이 곧 선언이다. GitHub 의 라이선스 감지가 「원문인가」를 본다 | 완료 |
| 2l-1 | ~~디자인 스킬의 MIT 고지를 둔다~~ | **완료.** `.agents/skills/design-taste-frontend/LICENSE`(신설, 원본에서 받았다)·`external-references.md`. `skills-lock.json` 이 원본을 `Leonxlnx/taste-skill` 로 적었고 그 저장소가 MIT 다(「MIT License · Copyright (c) 2026 Leonxlnx」). **MIT 는 사본에 저작권 고지와 허가 문구가 있어야 성립한다.** **`SKILL.md` 는 안 건드렸다** — 한 글자만 바뀌어도 잠금파일 해시가 어긋나고 스킬 갱신 청크가 그 편집을 지운다. `git status` 로 수정 없음을 확인했다. **드러난 것 하나(정보)**: `skills-lock.json` 의 `computedHash` 가 `SKILL.md` 의 sha256 과 안 맞는다. **이 세션이 그 파일을 안 건드렸으니 원래부터 그렇다** — 잠금파일이 무엇을 어떻게 해싱하는지(줄끝 정규화 등)가 안 적혀 있다. 스킬을 갱신하는 청크가 그때 확인한다. **축**: **법**(저작권 — MIT 고지 의무). **강제 지점**: 없다 — 표시가 곧 이행이다. `external-references.md` 표에 모으는 것이 다음 인용 때 빠뜨리지 않게 하는 자리다 | 완료 |
| 2l-2 | ~~SQL 명명 절을 CC BY-SA 4.0 으로 표시한다~~ | **완료.** `naming-rules.md`·`external-references.md`·`README.md`. `D22` 의 SQL 절이 SQL Style Guide(Simon Holywell, CC BY-SA 4.0)에서 골라 온 규칙이고, `external-references.md` 가 「파생물 판단은 안 했다 — 공개하기로 하면 이 줄을 먼저 다시 본다」로 남겨 뒀다. **`2l` 이 그 조건을 채웠다.** **그 절만 CC BY-SA 4.0 으로 표시했다**(사용자 선택). 절 첫머리에 인용 블록으로 적고 **경계를 「Java」 절 앞까지**로 못 박았다. **판단을 여전히 안 한 것이 요점이다** — 파생물이든 인용이든 이 표시로 어느 쪽이든 성립한다. 절을 다시 쓰는 것(일이 많다)과 인용으로 보는 것(틀리면 위반이다)을 버린 이유가 그것이다. **축**: **법**(CC BY-SA 동일조건). **강제 지점**: 없다 — 표시가 곧 이행이다 | 완료 |
| 2m | ~~게이트는 부순 증거가 있어야 닫힌다~~ | **완료.** `quality-gates.md`·`CLAUDE.md`·`doc/process/inspection.md`·`PLAN.md`(`69` 행). **게이트 표에 「부순 날」 열이 섰다.** 열셋 중 부순 기록이 있는 것은 넷(`EnumConstraintTest`·`StackVersionConsistencyTest`·화면 대조 둘·`doc-lint.sh`), **미확인이 다섯**, `컴파일`·`나머지 테스트` 둘은 `—` 다 — 그 둘은 **매일 실패 사례를 낸다**. **절 하나가 늘었다**: 게이트를 세우거나 고치는 청크는 **일부러 어긴 커밋이 빨개진 것을 본 뒤에** 닫고, 못 부수면 왜 못 부수는지를 그 칸에 적는다. **빈 칸은 「안 해도 된다」가 아니라 「아직 모른다」다.** 근거로 다섯을 표로 적었다 — 지연 트리거 0회, `UP-TO-DATE` 로 안 돈 대조, 로컬만 돌던 `doc-lint`, 코멘트 0개인 리뷰, **그리고 `2i-1` 이 오늘 찾은 것**(`.withReuse(true)` 가 코드에 있는데 로컬 스위치가 없어 경고만 냈다). **다섯 다 목록으로는 초록이었다.** `CLAUDE.md` 「끝 — 다섯」의 둘째 행에 구절 하나(「게이트면 부순 증거까지」), `inspection.md` 품질 게이트 줄에 「부순 날이 비어 있나」. **4위로 내리는 것은 `69`(변이 테스트)** 라고 그 행에 적었다 — 부수는 것을 기계가 한다. **축**: 규약(`D25`). **강제 지점**: 5위 — 문서다. 사람이 「끝 — 다섯」에서 읽어야 걸린다 | 완료 |
| 2m-1 | ~~미확인 게이트 다섯을 부순다~~ | **완료.** `quality-gates.md` 하나. 부순 파일은 전부 되돌렸고 `git status` 로 확인했다. **다섯 다 잡는다.** ① 마이그레이션 제약 — 부수는 대신 **이미 부수고 있는 테스트 아홉**을 댔다(`ConsentSchemaTest`·`NotificationSchemaTest`·`RefundNoteSchemaTest`·`SkuStockTest` 등이 위반 행을 넣고 `DataIntegrityViolationException` 을 단언한다). ② `PlanProgressConsistencyTest` — 가짜 완료 이력으로 빨갛게 했다. ③ SpotBugs — `NP_ALWAYS_NULL` 클래스로 `spotbugsMain FAILED`. ④ `npm run lint` — `alt` 없는 `<img>` 로 `jsx-a11y/alt-text` error. ⑤ gitleaks — 만든 키 셋으로 3건. **부수다가 셋을 알게 됐고 그게 이 청크의 진짜 산출물이다.** (가) **`PlanProgressConsistencyTest` 는 분할표에 아예 없는 이름을 안 본다** — `ZZ-probe` 로는 초록이고 분할표에 있으면서 안 닫힌 번호라야 빨개진다. **버그가 아니라 필연이다**: 이력에 「마무리」·「점검 F」 같은 비청크 행이 섞여 있어 없는 이름을 다 실패로 보면 그것들이 걸린다. **그래서 「이력에만 있고 분할표에 없는 청크」는 여전히 마무리의 사람 대조 항목이다.** (나) **gitleaks 는 AWS 문서의 예시 키(`AKIAIOSFODNN7EXAMPLE`)를 allowlist 로 흘린다** — 그것으로 부수면 게이트가 고장 난 것처럼 보인다. (다) **`--no-git` 트리 훑기는 CI 와 답이 다르다** — 트리 전체 10건이 전부 `node_modules`·빌드 산출물이고 CI 의 git 모드는 605 커밋에 0건이다. **부술 때는 좁은 디렉터리에 걸어 몇 초에 끝낸다.** **축**: 규약(`D25` 「부순 증거」). **강제 지점**: 없다 — 남기는 것은 증거지 장치가 아니다 | 완료 |
| 2n | ~~계층 규칙을 ArchUnit 으로 내린다~~ | **완료.** `ArchitectureTest.java`(신설, 규칙 다섯)·`build.gradle.kts`·`coding-rules.md`·`stack.md`·`quality-gates.md`·서비스 넷(import 한 줄씩). **`D23` 의 「계층」·「예외」·「의존」·「자원이 아닌데 패키지를 파는 경우」가 문서(5위)에만 있던 것을 테스트(4위)로 내렸다.** 컨테이너를 안 타서 빠른 레인이다. **규칙이 세우자마자 잡았다** — 서비스 셋과 `AuditLogQuery` 에 **쓰지도 않는 `org.springframework.http.HttpStatus` import** 가 남아 있었다. `7b` 가 HTTP 예외를 핸들러로 옮기면서 지우지 않은 자국이고, **컴파일도 테스트도 초록이라 아무도 못 봤다.** **순환이 예상 밖으로 둘 더 있었다**(사용자 결정으로 허용에 넣었다). `auth ↔ cart` — 로그인 그 순간에 비회원 장바구니를 계정으로 옮긴다(`AuthController` → `CartService.mergeIntoAccount`·쿠키 이름, `CartController` → 인증). `order ↔ payment` — 결제가 주문 상태를 옮기고(`PaymentService` → `OrderStatusService`·`IdempotencyService`) 주문 조회가 결제 상태를 보인다(`OrderQuery` → 열거형 넷). **셋 다 「업무가 두 자원에 걸쳐 있다」는 같은 모양**이라 가르면 가짜 경계가 생긴다. **넷째가 생기면 예외로 넣기 전에 멈춘다**고 코드와 `D23` 양쪽에 적었다. **`FreezingArchRule` 을 안 썼다** — 기준선 파일은 **왜 뺐는지를 못 적는다**(`2e` 가 SpotBugs 기준선을 안 만든 것과 같은 판단). **다섯째 규칙은 규칙 자신을 지킨다** — 이름 규칙이 바뀌어 규칙이 빈 집합을 보면 조용히 통과하므로, 「컨트롤러가 하나라도 있나」를 세는 줄을 뒀다. **부쉈다**: `CartController` 에 `JdbcClient` 필드 → 계층 규칙 빨강. `support/Retries` 에 `OrderStatusService` 필드 → 공용도구·순환 둘 다 빨강. **import 만 더한 첫 시도는 안 잡혔다** — ArchUnit 은 바이트코드를 읽어서 **안 쓰는 import 는 흔적이 없다**. 되돌렸다. **검증**: `./gradlew build` 초록. **축**: 규약(`D23`) + 관례(ArchUnit). **강제 지점**: 5위 → **4위** | 완료 |
| 2o | ~~마무리에 독립 에이전트 축을 상설로 둔다~~ | **절차는 섰다.** `doc/process/wrapup.md` — 하는 일이 넷에서 다섯이 됐고, PR 을 열기 전에 서브에이전트 하나를 띄우는 절을 넣었다. **주는 것과 안 주는 것을 못 박은 것이 요점이다** — diff 전체와 시작 세 줄은 주고 **이 세션의 대화는 안 준다**. 대화를 주면 같은 눈이 하나 더 생길 뿐이다. **묻는 것은 둘**(결정끼리 부딪치나 · 말한 축과 편 축이 같나) — 일곱 대조 중 「하루 치 결정을 다시 읽어야 나온다」고 이 문서가 적어 둔 바로 그 둘이고, **읽는 사람이 결정한 사람과 같으면 제일 안 걸리는** 자리다. `inspection.md` 가 세 축을 시험했을 때 이 축이 가장 큰 것을 잡았다. **나온 것은 「찾은 것을 어떻게 처분하나」 셋으로 간다** — 새 표를 안 만들고, 「오탐이다」도 처분이라 근거를 적는다(`D25` 리뷰 지적과 같은 규칙). **이력에 「독립 리뷰: 지적 N, 처분 …」을 남긴다** — `2h`·`D25` 가 반복 지적을 셀 입력이다. **GitHub 리뷰와 겹치지 않는다**(사용자 결정) — 이쪽은 PR 을 **열기 전**, 그쪽은 **연 뒤**다. **같은 날 돌렸고 값을 했다** — 7차 마무리에서 띄워 **지적 열여섯**이 나왔고 열셋을 그 자리에서 고쳤다. **제일 큰 것이 이 축이 아니면 안 걸리는 종류였다**: `점검 J` 가 `inspection.md` 의 **축 표를 이력 표로 덮어쓴 것**(`findIndex` 가 같은 이름의 첫 줄을 잡았다) — 빌드도 `doc-lint` 도 초록이고, 고친 사람이 다시 읽으면 「내가 그 줄을 고쳤다」로만 보인다. **둘째로 큰 것**: `support` 규칙이 자원 여덟만 열거해 `notification`·`consent` 가 샜다. **축**: 규약(`wrapup.md`·`D25`). **강제 지점**: 5위 — 절차다. 이력의 구절이 「돌았나」의 흔적이다 | 완료 |
| 2p | 병렬 줄 — worktree 둘로 청크를 나눠 친다 **진행중** | **규칙은 섰다.** `CLAUDE.md`(재개 프로토콜에 「병렬 줄」 절, 7번에 구절 하나)·`doc/process/wrapup.md`(「줄이 둘이면 먼저 합친다」). **막는 것이 시스템 크기가 아니라 절차라는 것**이 이 청크의 결론이다 — 자원 단위로 갈린 패키지가 열여덟이라 겹치지 않는 구역은 충분했고, 실제로 막고 있던 것은 넷이었다: `PLAN`·`PROGRESS` 를 청크마다 둘 다 고치는 것, `2g-1` 훅의 PR 하나 규칙, 마이그레이션 번호, 8080 포트. **규칙 여섯으로 풀었다** — worktree, 줄은 PR 을 안 열고 로컬에서 합친다, 「현재 상태」는 합칠 때 한 번, 허브 파일은 더하기만, 번호 예약, 포트 쓰는 청크는 한 줄에. **줄 배정은 `CLAUDE.md` 에 안 적었다** — 그날 무엇을 어느 줄에 두는지는 `PROGRESS.md` 「현재 상태」가 든다. **남은 것: 첫 합치기.** 이 세션은 한 줄로 열일곱을 쳤다(worktree 를 안 팠다) — 규칙을 세우는 청크가 그 규칙의 첫 사용자가 되면 **규칙이 맞는지 모르는 채로 그것을 따르게 된다.** 다음 묶음이 두 줄로 돌고, `wrapup.md` 가 요구하는 대로 **충돌 파일 수·푸는 시간·읽은 diff 크기**를 이력에 적으면 그것이 증거고 **세 줄로 갈지의 근거**다. **축**: 규약(재개 프로토콜·`2g-1`·`2g-4`). **강제 지점**: 5위 — 절차. 겹치는 번호는 Flyway(2위)가, 겹치는 포트는 기동 실패가 막는다 **닫힘**: 두 줄로 친 첫 묶음의 이력 행에 충돌 파일 수·푸는 시간·읽은 diff 크기 셋이 있고, 그 수로 세 줄 여부를 정한 문장이 `CLAUDE.md` 「병렬 줄」에 있다. | 2m |
| 2q | ~~세션 시작·끝을 hook 으로 내린다~~ | **완료.** `.claude/settings.json`·`CLAUDE.md`·`quality-gates.md`. `SessionStart` 가 「현재 상태」를 컨텍스트에 넣고(컴팩션 뒤에도), `Stop` 이 커밋 안 된 작업물이 있으면 `exit 2` 로 멈춤을 막는다. `stop_hook_active` 면 통과. **검증**: 세 경로를 손으로 돌렸다 — 더러운 트리 2, `stop_hook_active` 0, 깨끗한 트리 0. **축**: 플랫폼 사실(Claude Code hook). **강제 지점**: 5위 → 4위 | 완료 |
| 2r | ~~예열·마무리·점검을 스킬로 옮긴다~~ | **완료.** `.claude/skills/{warmup,wrapup,inspection}/SKILL.md`(warmup 신설, 둘은 `doc/process/` 에서 이동), `CLAUDE.md`·`doc/README.md`·`quality-gates.md`·`scripts/doc-lint.sh`·`.claude/settings.json`·`.gitignore`·`PROGRESS.md`(「점검 — 어디까지 봤나」가 여기로) 수정. `doc/process/` 는 없다. **스킬은 부를 때만 컨텍스트에 든다** — `CLAUDE.md` 32.9KB → 30.4KB(바이트). `doc-lint.sh` 가 스킬 셋을 같이 본다(frontmatter 뒤 첫 줄이 제목). **검증**: 존댓말·제목 파편을 스킬 파일에 넣어 둘 다 빨간 것을 봤다. 세션 스킬 목록에 셋이 바로 떴다. **축**: 플랫폼 사실(Claude Code 스킬 로더). **강제 지점**: 절차라 없다 — 로더가 「안 읽는다」를 대신한다 | 완료 |
| 2s | ~~`backend/CLAUDE.md` 를 세운다~~ | **완료.** `backend/CLAUDE.md`(신설)·`CLAUDE.md`·`scripts/doc-lint.sh`. 건드리는 것 → `D22`·`D23` 의 어느 절인가 표 하나. 명령은 안 옮겼다 — 루트 「검증」이 든다. **검증**: `backend/` 파일을 읽자 그 파일이 컨텍스트에 같이 실렸다. `doc-lint.sh` 가 그 파일을 본다. **축**: 플랫폼 사실(하위 `CLAUDE.md` 로더). **강제 지점**: 없다 — 「그 청크가 건드리는 것만 본다」가 지시에서 로더 동작이 된다 | 완료 |
| 2t | ~~분할표에 「닫힘」 칸을 세우고 빠진 행을 린트가 센다~~ | **완료.** `PLAN.md`(칸 설명 표에 「닫힘」 행)·`scripts/doc-lint.sh`·`CLAUDE.md`·`quality-gates.md`. 닫힘은 **테스트 클래스·메서드 이름**이다(사용자 선택 — 설계 세션이 빨간 테스트를 커밋하는 안은 코드를 건드려서 버렸다). **기준선 74 에서 줄기만 하는 래칫** — 지난 행 74개에 닫힘이 없어 0 으로 못 시작했다. **검증**: 닫힘 없는 행을 더하면 빨갛고(75>74) 넷 다 있는 행은 통과하는 것을 봤다. `칸` 헤더가 세지던 것을 부수면서 잡았다. **축**: 규약. **강제 지점**: 5위 → 4위(`doc-lint.sh`) | 완료 |
| 2u | ~~「현재 상태」를 표로 줄이고 줄 수를 린트에 건다~~ | **완료.** `PROGRESS.md`·`scripts/doc-lint.sh`·`quality-gates.md`. 무엇·상태·다음 손 세 칸, 상한 25줄. **검증**: 열 줄을 더해 빨간 것을 봤다. **축**: 규약. **강제 지점**: 5위 → 4위(`doc-lint.sh`) | 완료 |
| 2v | ~~요건↔테스트 대응을 스크립트로~~ | **완료.** `scripts/req-coverage.sh`(신설)·`.github/workflows/ci.yml`(docs 잡에 step)·`quality-gates.md`·`.claude/skills/inspection/SKILL.md`·`CLAUDE.md`(검증 표). R 번호마다 그것을 `-w` 로 언급하는 테스트 파일 수를 센다. **리포트만**(사용자 선택) — 게이트는 `D25` 대로 부순 증거 뒤에. **첫 실행 13/37 이 안 불린다**: R2 R10 R12 R13 R14 R23 R26 R27 R29 R30 R31 R33 R35. **축**: 법(`D2`) — 점검 가로·법의 첫 입력. **강제 지점**: 없다(5위 리포트) | 완료 |
| 2w | ~~리뷰 프롬프트를 청크별 예·아니오 표로~~ | **완료.** `.github/workflows/claude-review.yml`·`CLAUDE.md`. 넷은 그대로고 **답의 꼴**을 바꿨다 — 행은 청크, 열은 넷, 칸은 예·아니오(근거)·모름(왜). PR 본문의 시작 세 줄을 `gh pr view --json body` 로 읽어 1번의 입력으로 쓴다. **PR #37 이 첫 실물이다**(2026-09-11) — **표를 냈다.** 행이 청크 여섯, 열이 물음 넷, 칸이 예·아니오고, 「아니오」 하나를 표 아래에 `파일:줄 — 무엇 — 어느 규칙` 으로 모았다. 시킨 꼴 그대로다. **2,827자 · 57턴 · $2.45** 다(`2g-5` 의 $1.1 보다 배 넘게 든다 — 프롬프트가 청크마다 넷을 묻게 바뀌어서다). **그 하나가 진짜였다**: 같은 PR 이 `onSubmit` 가드를 코드에 넣고 `D24` 의 「이 표가 그쪽의 유일한 방어다」를 안 고쳤다 — **코드와 그 짝인 문장이 같은 묶음에서 갈린 것**이고, 참조 대조로는 안 걸린다. **축**: 규약. **강제 지점**: 없다(5위 조언) — 「출력이 있나」는 `2g-5` 의 step 이 든다 | 완료 |
| 2x | ~~`main` push 와 `JAVA_HOME` 을 훅으로 막는다~~ | **완료.** `.claude/settings.json`(PreToolUse 둘)·`CLAUDE.md`·`quality-gates.md`. hook 입력 JSON 을 `node` 로 파싱해 명령 문자열만 꺼내고, 명령 위치의 `git push … main`·`gradlew` 만 잡는다. **검증**: 열두 경로 — `push origin main`·`-u origin main`·`HEAD:main`·체인 2, work 가지·맨 `push`·산문·백틱 0, `gradlew` 맨몸·세미콜론 2, 접두·산문 0. **축**: 규약. **강제 지점**: 5위 → 4위 | 완료 |
| 2y | ~~슬래시를 기본으로 하고 줄 끝을 LF 로 굳힌다~~ | **완료.** `CLAUDE.md`·스킬 셋의 설명문·`doc/README.md`·`.gitattributes`(신설). `/warmup`·`/wrapup`·`/inspection` 이 기본이고 텍스트는 별칭. `* text=auto eol=lf` — `ci.yml`·`claude-review.yml` 만 CRLF 라 편집 스크립트가 헛돌았다. **축**: 플랫폼 사실(슬래시는 하네스가 로드, 텍스트는 모델이 매칭). **강제 지점**: `.gitattributes` 는 체크아웃마다 걸린다. 문구는 없다 | 완료 |
| 2z | ~~「검증」을 스킬로 빼고 Stop hook 이 도장을 본다~~ | **완료.** `.claude/skills/verify/SKILL.md`(신설, `CLAUDE.md` 「검증」 95줄 이동)·`scripts/verify.sh`(신설)·`.claude/settings.json`(Stop 둘째)·`CLAUDE.md`·`backend/CLAUDE.md`·`quality-gates.md`. **빼면 안 부른다**를 훅으로 막았다(사용자 선택) — `verify.sh` 가 `origin/main` 대비 바뀐 레인만 돌리고 `.git/verify-stamp` 에 서브트리 해시를 찍고, Stop hook 이 `HEAD:backend`·`HEAD:frontend` 와 대조한다. 문서 커밋은 안 걸린다. **검증**: `verify.sh` 두 레인 초록(56초, 빌드 캐시). 도장 맞음 0 / 도장 깨뜨림 2 / `stop_hook_active` 0. **축**: 규약. **강제 지점**: 「부르라고 적어 둔 것」 5위 → 훅 4위 | 완료 |
| 2z-1 | ~~도장 지문을 코드·빌드 파일로 좁힌다~~ | **완료.** `scripts/verify-fingerprint.sh`(신설)·`scripts/verify.sh`·`.claude/settings.json`·`verify` 스킬·`quality-gates.md`. `2z` 를 닫고 멈추려는 순간 **그 훅이 자기 커밋을 막았다** — `backend/CLAUDE.md` 가 `backend/` 서브트리에 들어서 문서 수정이 코드로 셌다. 지문은 `backend/src`·빌드 파일, `frontend/src`·`e2e`·`package*.json`·설정 파일만 든다. 경로 목록은 그 스크립트 한 곳이다. **검증**: 훅 세 경로 다시, `verify.sh` 초록. **축**: 규약. **강제 지점**: 4위 그대로, 오탐만 걷었다 | 완료 |
| 2t-1 | ~~곧 잡을 행 열다섯에 닫힘을 채운다~~ | **완료.**(설계 세션, 2026-09-10) `PLAN.md`·`scripts/doc-lint.sh`(기준선 74 → 56)·`quality-gates.md`·`external-references.md`. 「현재 상태」가 다음으로 든 행과 2 계열·`43a` 나머지·`14c-1`·`점검 D-2`·`W3`. **`W2`·`W4`·`2f-3` 은 닫았다** — 앞 둘은 `2r`·`2f-1` 이 흡수했고 `2f-3` 은 증거가 왔다(#31). 먼 행(46~76 등)은 안 채웠다 — 잡을 때 채우면 되고 래칫이 지킨다. **축**: 규약(`PLAN-3`). **강제 지점**: 래칫이 4위에서 그대로 | 완료 |
| 2z-2 | ~~도장을 두 단계로 — 청크는 빠른 레인, push 앞에 full~~ | **완료.** `scripts/verify.sh`·`.claude/settings.json`(Stop·push 훅)·`verify` 스킬·`CLAUDE.md`·`quality-gates.md`·`testing-strategy.md`. 기본 `verify.sh` 는 backend `gradlew test`(10초)·frontend `tsc --noEmit`(22초)·lint·test 고, `--full` 이 `gradlew build`·`next build`. **Stop 은 단계를 안 가리고 push 훅이 full 을 요구한다** — 「push 앞 한 번」이 기억이 아니라 훅이다. 청크마다 push 도 안 한다(사용자 결정 — 오늘 일곱 번 ≈ 20분). **검증**: `--full` 초록, 훅 경로 — 빠른 도장으로 Stop 0 / 빠른 도장으로 push 2 / full 도장으로 push 0. **축**: 규약(`D15` 레인). **강제 지점**: 4위 훅 둘 | 완료 |
| 2i-2 | ~~느린 레인을 병렬로 — fork 마다 DB 를 따로~~ | **완료.** `PostgresTestBase.java`·`build.gradle.kts`·`testing-strategy.md`·`stack.md`. 컨테이너 하나 안에서 `shop_test_<작업트리>_<worker>` 와 Redis 논리 DB 를 fork 마다 가른다. **예보와 셋이 달랐다** — ① `@ServiceConnection` 을 떼야 했다(그 표시가 컨테이너 기본 DB 로 연결을 고정한다) ② **Redis 도 같은 사고를 안고 있었다**(`LoginAttemptTest` 가 `login:fail:*` 를 통째로 지운다) — 예보에 없던 것이라 같이 갈랐고 컨테이너를 `--databases 256` 으로 띄웠다 ③ `git rev-parse` 대신 `user.dir` 의 부모로 작업트리를 읽는다(테스트 JVM 마다 프로세스를 안 띄우려고). **목표를 못 맞췄다** — 30초대가 아니라 중앙값 **93초 → 81초**(fork 2). **fork 4 는 84초로 되레 늘어서** 기본을 2 로 뒀다: 남은 병목이 실행이 아니라 **fork 마다 새로 뜨는 Spring 컨텍스트**다. `2i-3` 을 세웠다. **검증**: fork 2 로 `integrationTest` 3연속 초록(64·79·82초). **축**: 규약(`D15`) + 플랫폼 사실(Gradle worker 번호·Testcontainers 재사용·Redis 논리 DB 수). **강제 지점**: 없다 — 격리는 DB 이름이 구조로 막는다 | 완료 |
| 2i-3 | ~~느린 레인의 Spring 컨텍스트 수를 줄인다~~ | **완료 — 셋이었고 둘로 줄였다.** `PostgresTestBase.java`·테스트 열다섯·`build.gradle.kts`·`testing-strategy.md`. **예상이 둘이었는데 실측이 셋이다.** 행이 「`HttpTestBase` 가 `RANDOM_PORT` 라 이미 둘」까지 짚었고, **세 번째가 `@AutoConfigureMockMvc`** 였다 — 그 표시가 캐시 키의 일부라 클래스마다 붙이면 **같은 설정인데 컨텍스트가 갈린다.** 78개 중 **15개만** 붙이고 있었다. `PostgresTestBase` 로 올려 하나로 모았다. **재는 것부터 막혔다**: `-D` 는 Gradle JVM 에만 가고 Gradle 이 테스트 JVM 의 표준 출력을 삼켜서, **DEBUG 를 켜도 한 줄도 안 나오고 못 센 것이 성공처럼 보였다.** `-DcontextCacheLog=1` 플래그로 둘 다 묶어 두고 그 이유를 주석에 적었다. **Boot 4 는 `org.springframework.boot.webmvc.test.autoconfigure`** 다 — 3 의 경로를 쓰면 컴파일이 죽는다. **결과**: 컨텍스트 3 → 2, 미스 3 → 2, fork 2 중앙값 55 → **53초**. **중앙값보다 편차가 더 줄었다**(55·175·55 → 53·52·54) — 컨텍스트가 적을수록 어느 fork 가 기동 비용을 무는 경우의 수가 준다. **`2i-2` 가 열어 둔 물음에 답했다**: 컨텍스트를 합쳐도 **fork 넷은 여전히 안 낫다**(56·55·56) — 남은 병목이 가짓수가 아니라 **기동 자체의 값**이다. 기본은 2 그대로다. **축**: 규약(`D15`) + 플랫폼 사실(TestContext 캐시 키). **강제 지점**: 없다 — 시간을 줄이는 청크다 | 완료 |
| 점검 K | ~~품질 게이트 — 걸어 둔 것이 아직 제 역할을 하나~~ | **완료 — 「한 번도 안 봤다」 셋 중 하나였다**(`2h` 리포트가 짚었다). `scripts/verify-fingerprint.sh`·`quality-gates.md`·`PROGRESS.md`. **축은 실행을 섞었다** — 정적 정독이 아니라 실제로 돌려 봤다(`inspection` 「같은 질문만 반복해서 읽지 않는다」). **찾은 것 셋.** ① **`backend/config/spotbugs/exclude.xml` 이 검증 지문에 없었다.** 그 파일은 **빌드 결과를 바꾼다** — 제외를 넓히면 진짜 검출이 숨는데, 지문에 없으니 도장이 안 바뀌고 **Stop hook 이 안 막는다.** 경로를 더했다. **부쉈다**(`D25`): 임시 트리 둘로 재서 넓히면 지문이 바뀌고 되돌리면 돌아오는 것을 봤다 — 고치기 전에는 안 바뀌었다. ② **`아직 못 정하는 것`의 dependabot 경보 줄이 측정을 받았다** — 주간 리포트가 열린 경보 **4건**을 냈다(`2e-3` 이 켜고 닷새). 문턱은 아직 안 정한다: 넷이 npm 인지 Gradle 인지 안 갈랐다. ③ **같은 표의 AI 리뷰 줄이 probe 결과를 안 들고 있었다** — PR #36 이 그 probe 였고 **입력은 여전히 0**이다. **깨끗했던 것 셋도 적는다**(근거가 없으면 다음 점검이 다시 잰다): 게이트 33개가 **전부 부순 증거를 갖고 있고**(빈 둘은 컴파일·「나머지 테스트」라 근거가 있다), SpotBugs 제외 다섯이 **전부 아직 문다**(제외를 비우면 102건, 96이 전역 하나), **`Q16` 의 `UP-TO-DATE` 함정을 새 대조 둘은 안 밟았다**(화면 파일만 고쳐도 `test` 가 돈다). **축**: 품질 게이트(`D25`) + 실행. **강제 지점**: 점검은 안 만든다 — 찾은 것을 처분한다 | 완료 |
| 점검 L | ~~세로 · 앱 검증~~ | **완료.** `EmailAddress.java`(신설)·`AuthController`·`MeController`·`EmailAddressTest`(신설)·`ListQueryTest`(신설)·`PLAN.md`·`PROGRESS.md`. **사다리 3위를 한 번도 안 봤던 자리다.** 실물: 컨트롤러 18(쓰기 있는 것 11) · `@Valid` 34 · `jakarta.validation` 쓰는 파일 11 · 커스텀 제약 1(`Password`). **지금 고친 것 둘**: (1) **같은 이메일을 두 입구가 다르게 쟀다** — 가입 254, 변경 320. 가입으로 못 만드는 주소를 변경으로는 넣을 수 있었다. `RFC 5321` §4.5.3.1.3(경로 256 옥텟, 꺾쇠 포함)이 **254 를 정하고 320 은 `64+1+255` 산수라 표준이 아니다**. `Password` 꼴을 따라 `@EmailAddress` 하나로 모았다 — **1위(구조)** 로 내린 것이다. (2) **`ListQuery.MAX_SIZE = 100` 에 테스트가 0개였다** — `D5` 가 「목록 하나로 전체를 긁어 갈 수 있다」고 이유까지 적었는데 그 100 이 도는지 재는 자리가 없었다. `orderBy` 허용 목록까지 같이 걸었다(`D14`, 요청 문자열이 SQL 에 닿는 유일한 자리). **청크로 세운 것 둘**: `Q22`(길이 상한 열 자리를 2위로) · `Q23`(새 목록 입구가 보정을 빠뜨리는 것을 막는다). **판정이 뒤집힌 것 셋을 같이 적는다**(`2o`): `SettlementController` 쓰기 셋에 검증이 없는 것은 **본문이 없어서**(경로변수만), `Inquiry` 목록 셋에 `Paging.of` 가 둘인 것은 **private `find` 를 공유해서**, 마스킹은 **`mask` 라는 이름이 없을 뿐 `FieldGroup` 셋·`canSee` 8곳·DB 시드·`FieldGroupTest` 로 서 있다**. **일부러 안 건 것도 확인했다**: `LoginRequest.email` 은 `@NotBlank` 뿐인데 **규칙을 세게 걸면 규칙 이전에 만든 계정이 로그인을 못 한다** — `PasswordRequest.currentPassword` 가 같은 이유로 규칙을 안 건다 | |
| 점검 N | ~~가로 · 표준~~ | **완료**(2026-09-14, Fable 설계 세션) — 노트 안 썼다(찾은 것이 문서로 바로 내려갔다). **인용 열넷을 코드까지 따라갔다**: RFC 3339·5321·9110·9457·W3C Trace Context 는 테스트까지 박혀 있고, 어긋난 것 여섯. **지금 고친 넷**: `external-references.md` D20 행이 접근성을 「범위 밖」이라 했는데 점검 E 행은 KWCAG 2.2 현행이라 하고 코드는 `jsx-a11y`·`axe` 로 잰다 — 자기모순 정정 · `identifier-rules.md` 「자원별 노출 방식」에 비로그인 장바구니 UUIDv4 토큰 행(코드는 쓰는데 표에 없었다) · `api-guidelines.md` 「파일 응답」에 RFC 6266/8187 인용 · 같은 문서 「PATCH」에 「`null` 은 지운다」가 실물이 없다는 사실(입구 하나·필드 하나·`@NotBlank` 라 `null` 도 부재도 400 — 문서가 실물보다 앞섰다). **`Q61` 로 넘긴 둘**: RFC 9111 캐시 금지 헤더가 Spring Security 기본값으로만 걸려 있고 테스트 0 · `Idempotency-Key` 가 관례(Stripe)만 인용하는데 IETF httpapi 초안이 같은 이름·의미. **섞은 눈**: 「누락」 — 인용했는데 안 박힌 것(UUID)과 표준이 걸리는데 인용이 없는 것(6266·초안)을 양방향으로. **판정 근거를 행마다 적었다**(코드 줄·테스트 이름) | 완료 |
| 점검 O | ~~가로 · 규약~~ | **완료**(2026-09-17) — 노트를 안 썼다(찾은 것이 분할표 행과 축 문서로 바로 내려갔다. `F`·`G`·`H`·`I` 와 같은 판단). **28일 만이다** — `점검 F`(2026-08-20) 뒤로 커밋 557개·마이그레이션 43개(69 중 62%)·Java 74개·화면 43개가 생겼다. `document-map.md` 의 완료 D 문서 **23개**에서 코드로 내려갔다. **먼저 기계로 내려간 것을 걷었다** — `ArchitectureTest` 규칙 열넷 · `EnumConstraintTest` · `SchemaNamingTest` · `LengthConstraintTest` · `DocumentMapConsistencyTest` · `ScreenLengthTest` · `eslint.config.mjs` · `doc-lint.sh` 가 보는 자리는 빼고 **그것들이 안 보는 자리만** 봤다. **찾은 것 열다섯**: 지금 고친 둘(`V72` 재예약 · 배치 카탈로그의 낡은 행) · 새 청크 열하나(`Q69`~`Q79`) · 이미 있는 청크가 무는 둘(아웃박스 페이로드의 내부 id → `Q66`, 토큰 표 둘의 30일 파기 → `Q62`). **제일 값이 큰 것이 `Q69` 다** — 미착수 청크 둘이 `V72` 를 같이 예약했고 그 번호는 `V73` 이 이미 지나가서 **채우면 기동이 죽는 자리**였다. **점검이 낸 것을 하나 뒤집었다가 되돌렸다**(2026-09-17, `Q73` 을 치면서): 「`order_shipping` 은 길이 제약이 넷 다 있다」를 「둘뿐」이라고 적었는데 **세는 `grep` 의 문자군에 숫자가 빠져서** `address1`·`address2` 를 못 본 것이었다. 리뷰 줄이 맞았다. 스킬이 경계한 것은 「글만 읽고 코드를 안 읽는 것」인데 **여기서는 코드를 읽었고 세는 도구가 틀렸다** — 대조에 쓴 명령도 근거의 일부라 같이 남긴다. **관례 줄은 아직이다** — 같은 2026-08-20 이고 다음 차례다 | 없음 |
| 점검 M | ~~세로 · 테스트~~ | **완료.** `backend/build.gradle.kts`·`PLAN.md`·`PROGRESS.md`. **세로 다섯 칸이 다 찼다** — 타입(`H`)·제약(`F`)·앱검증(`L`)·테스트(`M`)·문서(`J`). **눈을 둘 섞었다**(사용자 선택, 점검 스킬 「같은 질문만 반복해서 읽지 않는다」): `D15` 에서 내려가는 배치 축 + 「돌긴 도나」 실행 축. **실물**: `@Test` **1005**(+`@ParameterizedTest` 4) · 파일 105 · 단위 24 / 통합 79 / HTTP 3 / 화면 14 / E2E 1. **HTTP 층이 3파일 35케이스로 얇은 것은 규칙대로다**(`D15` 「관통하는 흐름만」). **실행 축이 값을 냈다 — 구멍 둘을 실측으로 확정했다**: (1) `DataLifecycleCoverageTest` 가 `data-lifecycle.md` 를 읽는데 **느린 레인에 `inputs` 신고가 아예 없었다**. (2) `StackVersionConsistencyTest` 가 **`stack.md` 표의 세 번째 칸을 따라가며** `docker-compose.yml` 까지 읽는데 그 파일이 빠져 있었다. 둘 다 그 파일을 고치고 돌려서 **`UP-TO-DATE` 를 눈으로 봤고**, 고친 뒤 다시 도는 것도 확인했다. **`2f`·`Q16` 에 이은 셋째·넷째라 강제 지점을 세웠다**(`Q25`) — 기록은 재발을 못 막았다. **자기 세션 산출물도 잡혔다**(`Q26`): `Q23` 이 세운 `PagingResolverTest` 가 HTTP 층에 있는데, 그 근거(「resolver 를 컨테이너가 부른다」)가 `D15` 가 정한 목록(쿠키·세션·실제 상태 코드)에 없다. **판정이 뒤집힌 것 하나**: 「단언이 없는 테스트 셋」이 나왔는데 열어 보니 **`andExpect` 를 쓰는 MockMvc 테스트**였다 — 내 `grep` 이 `assert` 만 봤다. 실제로 단언 없는 테스트는 **0개**고 `@Disabled` 도 **0개**다. **통합 79 중 DB 를 안 쓰는 것은 `BackendApplicationTests` 하나**인데 그건 스프링 배선이 검증 대상이라 정당하다 | |
| 2z-3 | ~~`verify.sh --full` 이 Docker 를 먼저 본다~~ | **완료.** `scripts/verify.sh`·`verify` 스킬·`quality-gates.md`. `2i-2` 를 치다 밟았다 — Docker 가 꺼져 있어 **930개가 전부 FAILED** 로 떴고 진짜 원인(`Could not find a valid Docker environment`)은 XML 리포트를 파야 나왔다. 빠른 레인은 컨테이너를 안 타서 `--full` 에서만 본다. **검증**: Docker 를 끄지 않고 확인했다 — `docker info` 를 실패하게 바꿔 끼운 판으로 메시지와 종료 코드를 봤다. **축**: 규약. **강제 지점**: 없다 — 신호를 읽히게 하는 것이라 막을 것이 없다 | 2z-2 |
| 2aa | ~~청크를 치기 전에 구조를 펴는 층~~ | **완료.** `.claude/skills/explain/SKILL.md`(신설)·`CLAUDE.md`·`.gitignore`·`doc/README.md`. **시작 세 줄이 이미 아는 사람의 표기였다** — 「`R21` — 약정은 고지가 성립 요건인데 값만 있고 화면이 없다」를 읽으려면 `D2` 요건표와 화면 계층을 먼저 알아야 하고, 모르면 **무엇을 승인하는지 모르는 채로 「가」라고 하게 된다.** 그 앞에 층을 하나 깐다 — 번호·제목·목적 / 왜 지금인지(출처 넷 중 어느 것) / **지금 구조 그림** / **바꾼 뒤 구조 그림** / 선택지가 있으면 **각 선택지의 구조 그림**. **켜는 방식은 세션 지속 모드다**(사용자 선택) — `/explain on` 이 `.claude/explain-mode` 를 만들고 그 뒤 모든 청크가 자동으로 편다. **상태를 파일에 둔 이유는 컴팩션이다** — 대화 기억에 두면 날아가고, 이 저장소는 세션이 끊기는 것을 전제로 만들었다. **설명 뒤에 멈춘다**(사용자 선택) — 펴고 바로 치면 **읽고 나서 방향을 틀 자리가 없다.** 멈추는 자리를 만드는 것이 이 스킬의 값이고 설명은 그 자리의 입력이다. **그림은 ASCII 박스다**(사용자 선택) — mermaid 는 터미널에 소스로만 떠서 창을 옮겨야 한다. **강제 지점**: **문서(5위)다. 이 청크는 아무것도 안 막는다** — 모드를 켜 놓고 그냥 지나가도 빌드가 초록이다. 재개 프로토콜에 6번(모드를 보는 단계)을 넣은 것이 지금 내릴 수 있는 전부고, **그 단계 자체가 5위다.** 4위로 내리려면 훅이 `.claude/explain-mode` 를 보고 설명 없이 편집이 나가는 것을 막아야 하는데, 청크 성격상 「설명했나」를 기계가 판정할 수 없어서 안 세웠다 | |
| 2x-1 | ~~기존 훅 둘도 JSON 을 파싱한다~~ | **완료 — 파싱만으로는 반쪽이었다.** `.claude/settings.json`(PreToolUse[0]·PostToolUse[1])·`stack.md`·`quality-gates.md`. `2x` 의 `extract` 구절을 둘에 그대로 넣어 `tool_input.command` 만 보게 했다. **그런데 문서 린트 훅은 그것으로 안 풀렸다** — 커밋 메시지가 인자로 실리니 `git commit -m "sed -i 로 PLAN.md 를 고치던 것을 걷었다"` 의 `sed -i` 와 `PLAN.md` 가 **명령 문자열 안에 진짜로 있다.** 실측으로 잡았다: 파싱만 넣은 판에서 산문 케이스가 여전히 린트를 돌렸다. **명령 위치에 앵커를 걸어 풀었다**(`(^|[;&|])[[:space:]]*`) — `2x` 가 `git push` 에 쓴 것과 같은 수다. **PR 순서 훅은 파싱만으로 됐다** — 거기서 찾는 것이 `gh pr create`·`gh pr merge` 라 인자로 실릴 일이 드물다. 다만 그쪽도 앵커를 같이 걸었다. **손 검사 다섯**: 산문에 `gh pr create` 가 든 커밋 0 · 진짜 `--base work/x` 2 · 백틱 안의 `gh pr create` 0 · 산문에 `sed -i … PLAN.md` 가 든 커밋 **0(앵커 전에는 2였다)** · 진짜 `sed -i … PLAN.md` 2. **`2x-2` 가 넓힌 matcher 덕에 이 둘도 PowerShell 도구에서 돈다.** **축**: 규약. **강제 지점**: 훅(4위) 그대로 — 오탐만 걷었다 | 완료 |
| 2x-2 | ~~훅이 도구 하나만 보던 것과 `main` 을 지키는 자리~~ | **완료.** `.claude/settings.json`(matcher 넷)·`doc/reference/stack.md`(절 둘)·`quality-gates.md`(push 훅 행 + 가지 보호 행). **소급 리뷰가 찾았다** — `2w` 프롬프트의 물음 2번을 어제 묶음에 손으로 물었다. **둘 다 「세운 게이트가 안 도는」 자리다.** ① matcher 가 `Bash` 뿐이라 같은 명령을 `PowerShell` 도구로 보내면 훅이 안 돈다. **그날 한 세션에서 두 번 샜다** — `git merge` 가 막혀 우회한 뒤 `git push`(도장 검사)와 `gh pr merge`(체크 초록 확인)가 그 도구로 나갔다. 결과는 멀쩡했지만 **손으로 같은 확인을 했기 때문이지 게이트가 판정한 것이 아니다.** matcher 넷을 `Bash|PowerShell` 로 넓히고, doc-lint 훅의 명령 패턴에 `Set-Content`·`Out-File`·`Copy-Item`·`Move-Item` 을 더했다 — matcher 만 넓히면 훅은 돌고 패턴이 안 걸려 반쪽이다. ② `enforce_admins` 가 `false` 였다. `pr_required: true` 여도 **저장소 주인은 그대로 민다**(GitHub 기본값) — `main` 을 지킨 것이 로컬 훅 하나였고, 그리로 들어온 커밋은 CI·AI 리뷰·마무리 대조를 전부 건너뛴다. `true` 로 올렸다. **`quality-gates.md:49` 가 「가지 보호가 앞의 것을 서버에서도 막는다」고 적어 둔 것이 사실이 아니었다** — 이제 사실이다. **부쉈다**(`D25`): PowerShell 도구로 `git push origin HEAD:main --dry-run` 과 `gh pr create --base work/…` 를 쳐서 둘 다 `PreToolUse:PowerShell hook` 이 막는 것을 봤다. **②도 9차 마무리에서 부쉈다**(독립 리뷰 지적) — `git push` 로는 로컬 훅이 먼저 막아 서버까지 안 가므로 **서버 API 로 직접 쳤다**: `gh api -X PATCH .../git/refs/heads/main` 이 같은 SHA 로도 `422 Changes must be made through a pull request` 다. 같은 SHA 라 성공해도 무변화라 안전하게 잴 수 있는 자리였다. **축**: 규약(`2g-1`·`2x`·`2z-2`) + 플랫폼 사실(matcher 는 도구 이름, `enforce_admins` 기본값). **강제 지점**: **2위 서버**(가지 보호) + 4위 훅(matcher) | 완료 |
| Q20-1 | ~~폼이 제출 중인지를 손으로 들던 것~~ | **완료 — `Q20` 이 테스트를 쓰다 찾았다**(처분: 신규, 「지금 깨져 있는 것」이 1순위라 앞으로 당겼다). `components/submit-button.tsx`(신설)·`login-form`·`signup-form`·`withdraw-form`·`account-forms`(폼 셋)·`product-form`·`frontend-rules.md`(D24 절 하나). **`<form action={fn}>` 은 `fn` 을 전환 안에서 돌리고 `fn` 이 끝날 때까지 그 안의 상태 갱신을 커밋하지 않는다** — 액션 첫 줄의 `setPending(true)` 가 화면에 못 닿는다. **막는 것이 아무것도 없었다**: 비행 중 세 번 제출하면 **세 번 다 나갔다**(실측). 타입·린트·빌드 전부 초록이고 브라우저로도 빠르면 안 보인다. **`useFormStatus` 가 0건이었다** — React 가 그 자리에 주라고 준 것을 아무도 안 썼다. **구조로 내렸다**: `SubmitButton` 이 그 값을 React 에게서 직접 받고, **폼의 자식이어야 읽히므로** 버튼을 따로 두는 것이 조건이다. 폼이 pending 을 들 자리 자체를 없앴다. **칸을 셋으로 갈랐다**(제출 중·`busy`·`blocked`) — `blocked` 만 문구가 안 바뀐다. 안 보낸 것을 보내는 중이라고 말할 수 없다. **색도 칸으로 뺐다**(`tone`) — 탈퇴가 위험 색이라 `className` 으로 덮게 두면 어느 화면이 무슨 색인지 이 파일이 모르게 된다. **`13-2a` 가 이 결함의 반대편이었다** — 성공 경로가 `pending` 을 안 되돌려서 액션이 끝난 뒤 갱신이 커밋되며 버튼이 「확인하는 중」에 멈춰 있었다. 같은 기제고 그때는 증상만 고쳤다. **부쉈다**(`D25`): `useFormStatus` 를 상수 `false` 로 바꾸니 `login-form.test.tsx` 의 그 둘만 빨갛고 되돌리니 여덟 전부 초록. **파일이 일곱이라 상한을 넘는다** — 결함 하나가 폼 다섯에 퍼진 것이라 한 자리만 고치면 나머지 넷이 깨진 채로 남는다. **축**: 플랫폼 사실(React 19 폼 action = 전환) + 화면 규약(`D20`). **강제 지점**: **테스트(4위) + 문서(5위)다 — 「구조」가 아니다**(마무리 대조 ⑦ 이 잡았다). 시작 세 줄은 「구조로 내린다」고 했고 공용 버튼이 그 모양이긴 하지만, **새 폼이 `useState` 로 pending 을 들어도 막는 것이 없다** — 린트 규칙도 대조 테스트도 없다. 막은 것은 이미 있는 폼 다섯이고, 다음 폼은 `D24` 절을 읽어야 안다. 진짜 강제는 `Q20-3` 으로 세웠다 | 완료 |
| Q20-3 | ~~폼이 pending 을 손으로 드는 것을 막는다~~ | **완료 — 재 보고 테스트로 갔다.** `frontend/src/test/form-pending.test.ts`(신설)·`frontend-rules.md`·`quality-gates.md`. **린트가 안 된다는 것이 이 청크의 측정이다**: ESLint 선택자는 **노드 단위**라 「이 파일에 `<form action=` 이 있고 **동시에** pending `useState` 가 있다」를 표현할 수 없다. 커스텀 플러그인을 새로 만들어야 하고 그 값이 규칙 하나보다 크다. **그래서 4위 테스트다** — 행이 예고한 「3위에 가까운 린트」는 못 갔다. **`PublicSurfaceTest` 와 같은 모양이다** — 소스를 훑어 규칙을 세운다. **범위를 `action={` 가진 파일로 좁혔다**: `onSubmit` 폼은 반대라 거기서는 `useState` 가 맞다(`D24`). **정규식이 상하면 조용히 통과하는 것**을 단언 둘로 막았다 — 훑은 파일이 열 개를 넘나, `action={` 쓰는 파일이 실제로 있나. **부쉈다**(`D25`): `login-form` 에 pending 을 도로 넣으니 **그 파일 경로를 짚으며** 빨갛고 되돌리니 초록. **축**: 플랫폼 사실(React 19 폼 action) + 규약(`D24`). **강제 지점**: 테스트(4위) | 완료 |
| Q20-4 | ~~`eslint` 가 빈 블록을 안 본다~~ | **완료 — 켜니 공짜였다.** `frontend/eslint.config.mjs`·`package.json`·`quality-gates.md`. **켜기 전에 쟀다**(`2e-1`·`Q21` 과 같은 방법): `eslint:recommended` 를 얹어 돌리니 **기존 코드 검출 0건**이라 기준선을 안 만들고 바로 켰다. **`@eslint/js` 를 직접 의존으로 박았다** — 전이로만 있으면 버전이 조용히 갈린다. **`latest` 는 못 쓴다**: `@eslint/js@10` 이 eslint 10 을 peer 로 부르는데 이 저장소는 9.39.5 라 `ERESOLVE` 다. 설치된 것과 같은 판을 박았다. **부쉈다**(`D25`): 빈 `finally { }` 를 심으니 `no-empty` 가 `55:15 Empty block statement` 로 물고 되돌리니 초록. **축**: 관례(도구 기본값은 근거를 적고 받는다). **강제 지점**: 린트 — 편집하는 자리에서 바로 뜬다 | 완료 |
| Q20 | ~~화면 테스트 넷~~ | **완료.** `login-form.test.tsx`·`signup-form.test.tsx`·`purchase-panel.test.tsx`·`cart-line.test.tsx`(넷 다 신설)·`testing-strategy.md`. **테스트 36개, 실패 0.** **가장 큰 것은 테스트가 아니라 찾은 결함이다** — `login-form` 의 「보내는 동안 버튼을 잠근다」가 빨갛길래 팠더니 **폼 다섯이 중복 제출을 안 막고 있었다**(`Q20-1` 로 갈라 먼저 쳤다). **예보와 두 자리가 달랐다.** ① **`R4` 청약철회 제한 고지가 `purchase-panel` 에 없다** — `page.tsx`(서버 컴포넌트)가 그린다. 그리고 그 문구 표는 백엔드 `WithdrawalRestrictionReason` 과의 **층간 대조**라 `D15` 대로 백엔드 몫이다. ② **비밀번호 규칙 문구도 같다** — `@Password` 의 `@Size(min=15,max=64)` 와 대조해야 하는데 화면 테스트로는 **같은 틀린 값을 쓰면 초록**이다. 둘 다 `Q20-2` 로 처분했다(신규). **부쉈다**(`D25`) 셋: 종속 동의를 안 끄게 하니 `signup-form` 의 그 하나만 빨갛고, 수량 하한을 걷으니 `cart-line` 의 그 둘만 빨갛고, `useFormStatus` 를 상수로 바꾸니 `login-form` 의 그 둘만 빨갰다. **닫힘이 찼다**: 화면 테스트가 **열한 파일**이 돼서 `D15` 의 `vitest-axe` 조건이 찼고 그 표를 「찼다 — `Q21`」로 바꿨다. **축**: 화면 규약(`D20`) + 법(`D2` R14 종속 동의·R24 총액). **강제 지점**: 테스트(4위) | 완료 |
| Q20-2 | ~~화면 문구가 백엔드 실물과 갈리는 것 — 대조 둘~~ | **완료 — 열어 보니 이미 갈려 있었다.** `WithdrawalNoticeScreenTest`·`PasswordHintScreenTest`(둘 다 신설)·`page.tsx`·`testing-strategy.md`·`quality-gates.md`. **`R4` 가 실물로 깨져 있었다**: 화면 타입이 `MADE_TO_ORDER`·`PERISHABLE`·`SEALED_COPYRIGHT` 인데 실물은 `COPYABLE_MEDIA`·`DIGITAL_CONTENT`·`MADE_TO_ORDER` 다 — **겹치는 것이 하나뿐**이라 나머지 둘로 등록된 상품은 `WITHDRAWAL_REASON_TEXT[reason]` 이 `undefined` 라 **고지 문단이 빈 채로 그려졌다.** 그 파일 Javadoc 이 「이 자리가 비면 제한 자체가 성립하지 않는다」고 적어 뒀는데 **대비책이 `reason === null` 만 덮고 있었다.** **셋을 고쳤다**: 타입을 실물에 맞추고, 문구를 조문대로 옮기고, **표에 없는 값이 와도 기본 문구가 나가게** 했다(타입은 union 이지만 값은 서버에서 온다). **대조는 같음으로 잰다** — `ErrorSlugScreenTest` 가 부분집합인 것과 다르다. **실물에만 있는 사유는 「고지가 없는 상품」**이라 법이 그 자리를 비워 두는 것을 허락하지 않는다. 그 문장을 `D15` 에 적었다. **비밀번호는 수만 묶었다** — 문장은 화면 몫이라 굳히면 문구를 다듬을 때마다 빨개진다. **정규식이 상하면 조용히 통과하는 것**을 둘 다 단언으로 막았다(`ErrorSlugScreenTest` 선례). **부쉈다**(`D25`): 화면 키 하나를 `PERISHABLE` 로 되돌리고 힌트를 `12자` 로 바꾸니 각각 빨갛고 되돌리니 초록. **`req-coverage` 가 13 → 12 로 줄었다**(`R14` 도 `Q20` 이 부르기 시작했다). **축**: 법(`D2` R4, 제17조제2항 단서) + 규약(`D15`). **강제 지점**: 테스트(4위) — 화면 상수라 타입으로 못 묶는다 | 완료 |
| Q22 | ~~길이 상한을 앱 검증에서 DB 제약으로 내린다~~ | **완료.** `V65__length_constraints.sql`(신설)·`LengthConstraintTest`(신설)·`ProductController`·`coding-rules.md`·`stack.md`. **열하나를 채웠다** — `점검 L` 이 센 열에 `product.block_reason` 이 하나 더 붙었다(**그 점검의 오탐이었다**: 필드명이 `reason` 이라 이미 제약이 있는 `order_status_history.reason` 과 겹쳐 「있다」로 세어졌다). **집계도 과소였다** — `점검 L` 의 18 은 (값, 이름) 쌍 기준이고 실제 자리는 **27**이다. 늘어난 것은 대개 이미 제약이 있는 컬럼으로 간다(`reason`·`decision_reason`·`request_reason`·`receiver_phone`). **사용자가 고른 셋**: 숫자를 양쪽에 두고 **대조 테스트로 묶는다**(DB 만 두면 400 이 500 이 되어 `D5` 응답 계약이 깨진다) · `product.name` 을 **200 에서 100 으로 좁힌다**(스마트스토어 100 · Shopify 255) · 근거 못 본 셋은 **넣고 근거 없음을 제약 주석에 남긴다**. **수마다 축을 제약 옆에 적었다** — `email` 254 는 표준(RFC 5321 §4.5.3.1.3), `product.name` 100 과 `source_url` 500 은 관례, `review_note`·`block_reason` 500 은 우리 규약, 배송 셋은 **근거를 못 봤다**(도로명주소 개발자센터는 신청이 필요하고 택배사 규격은 계약자만 본다). **법에서 온 것은 0개다** — `D2` 의 R 40개 중 길이를 정하는 조문이 없다. **빈 문자열 허용을 앱과 맞췄다**: `@NotBlank` 가 있으면 `between 1 and N`, 없으면(`address2`·`delivery_memo`·`source_url`) `<= N`. 갈리면 앱이 받은 것을 DB 가 거부해서 500 이 된다. **함정 하나를 밟고 `stack.md` 에 적었다**: `@Size` 는 `@Target` 에 `RECORD_COMPONENT` 가 없어서 **record component 에 안 남는다** — 첫 실행에서 열한 칸 중 열이 「@Size 가 없다」로 빨갰고 통과한 하나가 우리가 만든 `@EmailAddress` 였다. **닫힘**: 대조 열하나 초록 · 빈 DB 기동에서 **65개 적용**(마이그레이션 62 + 시드 3)하고 `/api/health` 가 `applied_migrations: 65`. **부순 증거**: `@Size` 를 200 으로 되돌리니 `product_name_length_check` 만 빨갰다 | 점검 L |
| Q23 | ~~새 목록 입구가 페이지 보정을 빠뜨리는 것을 막는다~~ | **완료.** `ListQuery`·`PagingArgumentResolver`(신설)·`WebConfig`(신설)·컨트롤러 여덟·`*Query` 일곱·`ArchitectureTest`·`PagingResolverTest`(신설)·`ListQueryTest`·`api-guidelines.md`. **사용자가 1위 안을 골랐다**(「가」) — 크기(25파일)를 알고 고른 것이라 이력에 밝힌다. **보정을 `Paging.of()` 에서 생성자로 옮겼다**: 전에는 「부르면 도는 것」이라 열 곳이 각자 불렀고 열한 번째가 빠뜨릴 수 있었는데, 이제 **그 타입이 존재한다는 것 자체가 「보정을 거쳤다」**는 뜻이다. `offset` 도 칸에서 빼고 계산으로 돌렸다 — 칸이면 `new Paging(3, 20, 0)` 처럼 페이지와 안 맞는 값을 넣을 수 있다. **`Criteria` 는 ㄱ으로 갈랐다**(사용자 선택): `AuditLogQuery.find(viewerId, Criteria, Paging)` 이고 `Criteria` 는 「무엇을 거를까」로 좁아졌다 — 필터는 감사 로그에만 있고 `Paging` 은 열 곳 전부에 있어서 **수명과 범위가 다른 둘을 한 record 에 안 담는다.** **1위로 다 못 내렸고 그 자리를 규칙으로 막았다**: `Paging` 을 **아예 안 받는** 새 목록 메서드는 타입이 못 막는다. `ArchitectureTest` 에 다섯째 규칙을 세웠다 — `Page` 로 끝나는 것을 돌려주는 `*Query` 의 `public` 메서드는 `Paging` 을 받는다. **파라미터 이름이 아니라 반환 타입으로 잰다**(이름으로 재면 `-parameters` 옵션에 매달리고 이름을 바꾸면 규칙이 조용히 비켜난다). **실측이 주석을 고쳤다**: resolver 등록을 빼고 재 보니 넷 중 **하나만** 빨갰다 — Spring 이 생성자 바인딩으로 `Paging` 을 만들어서 보정도 400 도 그대로 돈다. **강제가 resolver 에 안 매달려 있다는 증거**고, 그 클래스가 혼자 드는 것은 **기본값 20** 뿐이다. **부순 증거 둘**: resolver 등록을 빼니 「안 주면 기본값」이 빨갰고, `Paging` 을 안 받는 목록 메서드를 하나 심으니 새 ArchUnit 규칙이 잡았다. **`PublicSurfaceTest` 가 즉석에서 일했다** — `PagingArgumentResolver` 를 `public` 으로 냈다가 「패키지 밖에서 아무도 안 쓴다」로 빨개져서 좁혔다. **남긴 것**: `sort` 는 허용 목록이 자원마다 달라 한 곳으로 못 모은다 — `orderBy` 를 안 부르면 지금도 아무것도 안 걸린다(`Q24`). **닫힘**: 두 레인 초록 | 점검 L |
| Q24 | ~~정렬 허용 목록을 안 거치는 것을 막는다~~ | **완료.** `ListQuery`·`*Query` 넷·`ArchitectureTest`·`ListQueryTest`·`api-guidelines.md`. **사용자가 1+3 을 같이 골랐다** — 둘이 서로의 구멍을 덮는 것이 이 청크의 설계다. **`ListQuery.orderBy` 가 `String` 대신 `OrderBy` 를 돌려준다**: `String` 이면 **검증을 거친 문자열과 요청 문자열이 같은 타입**이라 `" order by " + sort` 라고 써도 아무것도 안 걸린다. **그 타입만으로는 못 막는다는 것도 코드에 적었다** — 조립이 문자열이고 `public record` 의 canonical 생성자는 `public` 이라야 해서 `new OrderBy(요청문자열)` 이 열려 있다. **「거쳤다」를 뜻하지 「거칠 수밖에 없다」를 뜻하지 않는다.** **그래서 `ArchitectureTest` 에 여섯째 규칙**: `Page` 로 끝나는 것을 돌려주고 `String` 을 받는 `*Query` public 메서드는 `ListQuery.orderBy` 를 부른다. **여섯 중 메서드 호출을 보는 첫 규칙이다**(나머지는 의존·시그니처). **`String` 파라미터로 알아본 이유**: ArchUnit 이 파라미터 **이름**을 안 줘서 `sort` 를 집어낼 수가 없다 — 이름 기반 규칙은 애초에 불가능했다. **오탐이 나는 쪽으로 틀어 뒀다**: 정렬이 없는데 `String` 을 받는 목록이 생기면 빨개진다. 놓치는 쪽으로 틀면 SQL 결합 자리가 조용히 하나 는다. **부순 증거가 둘을 한 번에 보였다** — `ProductQuery` 에서 `ListQuery.orderBy` 를 빼고 `new OrderBy(sort)` 로 바꾸니(타입은 그대로 쓰면서 허용 목록만 우회) 새 규칙이 잡았다. **닫힘**: 두 레인 초록 | Q23 |
| Q25 | ~~신고 안 된 파일을 읽는 대조 테스트를 잡는다~~ | **완료** — `BuildInputTest`(신설) · `JavaSourceText`(신설, 헬퍼) · `SqlTextTest`(그 헬퍼로 옮김) · `build.gradle.kts` · `testing-strategy.md` · `quality-gates.md`. **네 번 밟은 함정에 강제 지점을 세웠다**(`2f`·`Q16`·`data-lifecycle.md`·`docker-compose.yml`). **먼저 정할 것 둘을 사용자가 골랐다**(2026-09-14): 경로 걷기는 **글자 훑기와 리플렉션 둘 다**(「값이 문제가 아니면」 — 둘이 못 보는 자리가 서로 다르다. 글자는 조립한 경로를, 리플렉션은 상수로 안 뺀 경로를 못 본다), 신고 목록은 **Gradle 이 시스템 속성으로 넘기는 쪽**(`build.gradle.kts` 를 글자로 읽으면 신고 표현이 하나 늘 때 깨진다 — 지금도 파일 하나씩과 `fileTree` 가 섞여 있다). **컨테이너 베이스는 초기화를 안 한다** — 정적 필드를 읽으려면 클래스를 초기화해야 하는데 `PostgresTestBase` 를 상속한 것은 그 순간 컨테이너가 떠서 **빠른 레인이 Docker 를 탄다.** `Class.forName(name, false, …)` 로 조상만 보고 거르고 그쪽은 글자 훑기에 맡긴다. **실행 때 정해지는 경로는 둘 다 못 본다** — `StackVersionConsistencyTest` 하나고 `DYNAMIC_ALLOWED` 에 이유와 개수로 박았다(`Q30` 과 같은 모양 — 닫힌 목록에 이유를 적고 개수를 박는다. **자리는 다르다**: `Q30` 은 코드 옆 마커고 여기는 테스트 안 목록이며, 왜 갈리는지는 `BuildInputTest` javadoc 이 든다). **주석을 걷는 코드가 두 테스트에 필요해져서 `JavaSourceText` 로 뺐다** — 안 뺐으면 40줄이 복제된다. **축**: 규약(`D15`). **강제 지점**: 테스트(4위). **닫힘**: 셋 초록. **부순 증거**: `PlanProgressConsistencyTest` 에 신고 없는 `../doc/README.md` 상수를 심으니 **두 훑기가 각각 짚었고**(`:35` 와 `BREAK_A`), 변수로 만든 경로를 심으니 허용 목록 쪽이 빨갰다(심었다 뺐다) | 점검 M |
| Q26 | ~~`PagingResolverTest` 가 HTTP 층에 있을 이유를 다시 본다~~ | **완료 — 내렸다.** `PagingResolverTest`(HTTP → 통합) · `testing-strategy.md`. `점검 M` 이 자기 세션 산출물을 잡은 것이었다 — `Q23` 이 댄 근거(「서블릿 컨테이너가 부른다」)가 `D15` 의 HTTP 층 근거 목록(쿠키·세션·실제 상태 코드)에 없다. **`MockMvc` 가 `WebMvcConfigurer` 를 그대로 태운다** — `PagingArgumentResolver` 가 같은 자리에서 돌아서 재는 것이 안 줄었다. **등록을 빼고 두 층에서 각각 돌려 같은 하나가 빨간 것을 확인했다**(「안 주면 기본값」 — `Q23` 이 HTTP 층에서 본 것과 같다). 네 케이스가 롤백 없는 층에서 빠져서 **정리 비용이 그만큼 준다**. **축**: 규약(`D15`). **강제 지점**: 없다 — 배치를 바꾸는 일이라 걸 것이 없다. **닫힘**: 통합 레인에서 넷 초록 · `verify.sh --full` 초록 | 점검 M |
| Q27 | ~~화면 `maxLength` 와 서버 `@Size` 를 대조한다~~ | **완료** — `ScreenLengthTest`(신설, 빠른 레인) · `LengthConstraintTest`(`maxOf` 를 나눠 쓰게 공개) · `testing-strategy.md` · `quality-gates.md`. **이름 관례로 잇는다**(사용자 결정) — 화면의 `name` 속성 = 요청 record 의 칸 이름. **관례가 깨지는 것도 같이 잡는다**: 짝을 못 찾은 칸 목록을 박아서, 이름이 어긋나기 시작하면 그 수가 변해 걸린다. **`LengthConstraintTest` 와 짝이라 화면→앱→DB 가 한 줄에 선다.** **헬퍼를 복제하지 않았다** — `@Size` 가 record 칸이 아니라 **그 칸이 만든 필드**에 붙는 함정을 그쪽 `maxOf` 가 이미 풀어 놨고, 복제하면 한쪽만 고치는 날이 온다. **밟은 것 둘**: 컨트롤러를 찾을 때 **테스트 클래스 자리**를 훑고 있었고(컨트롤러는 main 이다), `computeIfAbsent` 를 먼저 불러서 **상한이 없는 칸이 빈 목록으로 등록**돼 멀쩡한 화면 값이 위반으로 떴다. **짝이 없는 칸 둘을 이유와 함께 박았다** — `cardNumber`·`postalCode` 는 상한이 `@Size` 가 아니라 **`@Pattern` 안에** 있다(`{10,23}`→25, `{5}`→5). 실제 값과 맞는다. **축**: 규약(`D15`). **강제 지점**: 테스트(4위). **닫힘**: 둘 초록. **부순 증거**: 문의 화면 `maxLength` 를 1999 로 바꾸니 `question 화면 1999 ↔ 서버 [2000]` 을 짚었다 | 마무리(12차) |
| Q28 | ~~길이 대조가 옛 제약 쌍까지 덮게 한다~~ | **완료** — `LengthConstraintTest`(쌍 넷 추가 + 회계 검사 하나) · `coding-rules.md`. **독립 리뷰가 짚은 것**: `coding-rules.md` 가 「갈리는 것은 이 테스트가 막는다」를 **저장소 전체 규칙**으로 적었는데 실제로 대조하던 것은 `V65` 가 넣은 열한 쌍뿐이라, **문서가 약속한 범위와 테스트가 덮는 범위가 달랐다.** **결정대로 `pg_constraint` 에서 전부 걷었다**(사용자 결정) — 손으로 적는 쪽은 빠뜨려도 안 걸린다. **`EnumConstraintTest.everyEnumIsAccountedFor` 와 같은 틀**이다: 대조하든 안 하든 **적히지 않으면 빌드를 세운다.** 제약 스물넷이 전부 `pairs()`(열다섯) 아니면 `NOT_COMPARED`(아홉) 에 든다. **양방향으로 잰다** — 없어진 제약이 목록에 남는 것도 실패고, 실제로 그것이 물었다(마이그레이션 글자로 뽑은 이름 둘이 실물에 없었다). **사유 컬럼 여섯은 `Q46` 으로 넘겼다** — 입구가 여럿이라 잘못 이으면 **틀린 쌍을 대조하며 초록**이 된다. **축**: 규약(`D23` 「길이」). **강제 지점**: 테스트(4위). **닫힘**: 열여섯 초록 | 마무리(12차) |
| Q29 | 화면의 서버 입구 규칙을 린트로 내린다 | **완료** — `eslint.config.mjs` 수정. **사용자가 물었다** — 「문서에 적힌 규칙 중 기계가 확실히 잡을 수 있는데 안 잡는 것이 있나」. `D24` 의 셋이 그랬다: **밖에서 `fetch` 를 쓰지 않는다** · **`next/headers` 는 `api-session.ts` 만 든다** · **`api-session` 을 클라이언트가 못 든다**. 코드는 셋 다 지키고 있어서 기준선 없이 바로 걸었다. **둘은 린트로 내렸다**(`no-restricted-globals` 의 `fetch` · `no-restricted-imports` 의 `next/headers`, 예외는 `src/lib/api*.ts` 와 테스트). **셋째는 예보가 틀렸다** — `server-only` 를 얹으려 했는데 재 보니 `next/headers` 가 클라이언트 번들에 들어가면 **`next build` 가 자체로 선다**(플랫폼 사실). 빌드가 막는 것을 또 막으면 규칙이 두 벌이라 안 얹었고, `frontend-rules.md` 가 그 이유를 든다. **축**: 규약(`D24`). **강제 지점**: 린트(4위) 둘 · 빌드(1위) 하나는 이미 있었다. **부순 증거**: 화면 파일에 둘을 심으니 `2 problems (2 errors)`, 클라이언트에서 `api-session` 을 드니 `Ecmascript file had an error`. **드러난 것**: 탐침 빌드가 죽으면 `.next/types` 가 반쯤 남아 `tsc` 가 빨갛다 → `stack.md`(정보) | 완료 |
| Q30 | ~~`D23` 의 테스트·SQL 규칙을 빠른 레인 테스트로 내린다~~ | **완료** — `TestConventionTest`·`SqlTextTest`(둘 다 신설, 빠른 레인) · `IdempotencyService`(마커 한 줄) · `coding-rules.md`(「테스트」·「SQL」) · `quality-gates.md`(행 둘). `Q29` 의 백엔드 몫이다. **위반이 0일 때 세웠다** — `@DisplayName` 은 104 파일이 다 달고 있었고 `select *` 도 0건이라, 치우는 값 없이 새 위반만 막는다. **사용자가 예외 처리를 마커로 골랐다**(2026-09-14) — 테스트 안 허용 목록(1번)이 아니라 **위반 줄 바로 위의 `// sql-exception: <사유>`**(2번). 예외가 늘어도 테스트가 안 커지고 이유가 코드 옆에 남는 쪽이다. **악용을 막는 것이 넷이다**(사용자가 그것을 물어서 더했다): 사유가 닫힌 목록(`identifier`·`set-statement` — `coding-rules.md` 가 인정한 둘) · **죽은 마커가 위반**(다음 줄에 실제 위반이 없으면 빨갛다) · 규칙별로만 끈다(`select *` 는 어떤 마커로도 안 꺼진다) · **개수를 박았다**(`MARKER_COUNT = 1`). **마커가 끄는 것은 이 테스트 하나**고 진짜 주입은 CodeQL 이 따로 본다(`2e-5`). **`\s*+` 가 아니면 멀쩡한 다섯을 위반으로 짚는다** — 음수 룩어헤드 앞의 `\s*` 가 백트래킹해 공백 0개로 다시 맞추면 ` orderBy` 의 공백 자리에서 룩어헤드가 통과한다. 처음 돌렸을 때 실제로 그랬다. **축**: 규약(`D23`). **강제 지점**: 테스트(4위). **닫힘**: 여섯 초록. **부순 증거**: `ResponseTimeFormatTest` 의 `@DisplayName` 을 떼니 첫째가, `ProductQuery` 에 `select *`·`set local`·맨 `order by` 결합·덮을 것 없는 마커를 심으니 나머지 다섯이 각각 빨갰다(심었다 뺐다) | 없음 |
| Q31 | ~~`D22` 이름 규칙과 `D5` URL 규칙을 스키마·스펙에 건다~~ | **완료** — `SchemaNamingTest`(신설, 느린 레인) · `OpenApiSpecTest`(테스트 둘) · `naming-rules.md` · `quality-gates.md`(행 둘). **도는 스키마에 묻는다** — 마이그레이션 글자로 잰 2026-09-13 측정은 위반 0이라고 했는데 **실제로는 둘이 나왔다.** (1) `order_item.withdrawal_restriction_agreed_at` 32자(상한 30) → `Q40` 을 세우고 여기서는 그 하나를 박아 새로 느는 것만 막는다. (2) 내가 「컬럼이 자기 표 이름을 되풀이하지 않는다」를 `startsWith` 로 쟀더니 **맞는 이름 일곱을 짚었다** — `seller_order.seller_order_number` 는 노출 번호고 `settlement.settlement_cycle_id` 는 외래키다. `D22` 가 적은 것은 「테이블과 **같은 이름**의 컬럼」 하나라 규칙대로 좁혔다. **규칙보다 센 검사는 맞는 것을 고치라고 시킨다.** **1:1 확장 표 여덟은 구조로 갈랐다**(기본키가 외래키이기도 하다) — 이름 목록은 아홉 번째에 낡는다. **스펙 쪽에서 또 하나 나왔다**: `Page` 스키마를 자원 전체가 공유해서 **경로별로는 못 잰다**(`/api/orders` 응답을 따라가면 `settlement_number` 가 딸려 온다). 스펙 전체에 그 이름이 없는지로 넓혔다. 그리고 **스펙은 낙타 표기인데 응답은 뱀 표기다** → `Q41`. **축**: 규약(`D22`·`D5`). **강제 지점**: 테스트(4위). **닫힘**: `SchemaNamingTest` 아홉 + `OpenApiSpecTest` 다섯 초록. **부순 증거**: 허용 목록에서 `granted` 를 빼니 불리언 검사가, `/Bad_Route` 를 더하니 경로 검사가, `orderNumber` 를 금지 목록에 넣으니 식별자 검사가 각각 물었다(그 이름은 `Q41` 이 뱀 표기로 뒤집기 전 것이다 — 지금 재현하려면 `order_number` 다)(전부 심었다 뺐다) | 없음 |
| Q32 | 문서에만 있는 계층·입력·시간 규칙을 `ArchitectureTest` 에 더한다 | **완료** — `ArchitectureTest` 수정(규칙 6→12). 들어간 것: `@RequestBody`→`@Valid`(`D14`) · `*Query` 메서드·클래스에 `@Transactional` 없음 · **트랜잭션 안에서 PG·메일 안 부름** · `LocalDateTime` 을 필드에 안 둠 · 반환·파라미터에도 안 둠. **셋째는 사용자가 (b) 사슬 전체를 골랐다**(2026-09-13) — 직접 호출만 보면 빈 규칙이다(게이트웨이가 전부 람다·private 헬퍼 뒤). ArchUnit 이 람다 호출을 감싼 메서드에 귀속시켜 `pay → askGateway → MockPaymentGateway.approve` 가 걸렸다. **전파는 예외가 아니다** — `REQUIRES_NEW` 는 바깥 트랜잭션을 중단할 뿐 잠금을 안 놓는다. 지나온 경계를 메시지에만 적는다. **예보와 달랐던 것 둘**: `ZonedDateTime` 금지는 「판단은 KST」 아홉 자리를 물어서 뺐고, `dependOnClassesThat` 은 `BusinessCalendar.endOfDay` 의 스쳐 가는 `LocalDateTime` 까지 물어서 시그니처 규칙으로 바꿨다. **다섯째(순번 노출)는 이 층이 아니다** — 「private 아닌 record」 휴리스틱이 서비스 내부 결과(`OrderService.Created`·`RefundService.Bundle`)를 문다. 실제로 나가는 것은 `CreateResponse(orderNumber, …)` 라 새는 것이 없었다. **`Q31` 의 `OpenApiSpecTest` 로 옮겼다** — 스펙이 곧 나가는 것이다. **축**: 규약(`D14`·동시성·시간). **강제 지점**: 테스트(4위). **부순 증거**: `@Valid` 를 떼니 `OrderController.create`, `pay()` 에 `@Transactional` 을 붙이니 사슬 셋. **검증**: `verify.sh` 초록(fast) | 완료 |
| Q33 | 요건표의 강제 지점이 실물인지 대조한다 | **완료** — `RequirementEnforcementTest`(신설, 느린 레인), `build.gradle.kts`(요건표를 `integrationTest` 입력으로 신고), `commerce-compliance.md` 칸 설명, `quality-gates.md`. 백틱 토큰을 **넷**으로 가른다(예보는 셋 — `Password.java` 같은 파일 토큰이 `Class.method` 모양에 걸려 `java` 를 메서드로 읽었다): 제약·인덱스 22 → `pg_constraint ∪ pg_indexes`, `Class.java` 1 → 파일 존재, `Class.method` 17 → 선언 파일 안에 `이름(`·`이름,`·`이름)`(record 컴포넌트는 괄호가 없다 — `Decision.visibleFieldGroups` 가 그 자리), `Vnn` 26 → 파일. **같은 이름의 중첩 타입이 둘**(`PermissionEvaluator.Decision`·`ReturnRequestService.Decision`)이라 선언 파일 후보 전부를 본다. 85 토큰 전부 실물이었다. **부순 증거에서 함정을 밟았다**: 접미사를 `_chek` 로 틀리니 모양에서 빠져 조용히 초록이었고, 그 뒤 같은 내용으로 두 번째 실행하니 Gradle 이 `UP-TO-DATE` 라 「신고가 안 먹나」를 한 바퀴 의심했다 — `--info` 로 `Input property 'comparedDocsInSlowLane' … has changed` 를 확인했고 줄기를 틀리니 R4 를 짚었다. **축**: 법(`D2`). **강제 지점**: 테스트(4위). **검증**: `integrationTest --tests '*RequirementEnforcementTest*'` 86 초록, `verify.sh` 초록(fast) | 완료 |
| Q34 | ~~화면 문구 **반말** 금지와 `dangerouslySetInnerHTML` 금지를 테스트·린트로 내린다~~ | **완료** — `frontend/src/test/screen-text.test.ts`(신설) · `eslint.config.mjs`(`react/no-danger`) · `screen-rules.md` · `security-baseline.md` · `quality-gates.md`(행 둘). **제목을 고쳤다** — 원래 「화면 문구 존댓말과 … 금지」라 **「존댓말 금지」로 읽혔다.** 사용자가 그렇게 읽고 물었다(2026-09-14). 방향이 반대다: `doc-lint.sh`·`TestConventionTest` 는 **개발자 글의 존댓말**을 막고 이것은 **화면 글의 반말**을 막는다. **범위를 분할표보다 넓혔다**(실측 근거): 「JSX 안의 글 + 속성 넷 + `{"…"}`」로 좁게 걸으면 269개인데, **화면 문구 대부분이 도우미 함수 안에 있었다** — `login-form.tsx` 의 오류 문구가 JSX 밖이라 안 걸린다. `.tsx` 의 한글 문자열을 통째로 걷어 575개가 되고 **둘 다 위반 0이라 넓히는 값이 공짜다.** `.tsx` 안 개발자용 한글 문자열(`new Error`·`console.*`)이 **0건**이라 오인할 대상도 없다 — 생기면 그 꼴을 뺀다. **어미 목록은 안 만들었다**(사용자 결정) — `~다` 하나만 실측으로 걸려서, 하나 더 나오면 그때 판다. **축**: 규약(`D20`) · 표준(OWASP A03 — `D14`). **강제 지점**: 테스트(4위)·린트(4위). **닫힘**: 테스트 셋 초록 · `npm run lint` 무출력. **부순 증거**: `login-form.tsx:46` 을 「로그인하지 못했다.」로 바꾸니 파일·줄을 짚었고(**좁은 범위였으면 못 잡는 자리다**), `dangerouslySetInnerHTML` 을 심으니 `react/no-danger` 가 물었다(둘 다 심었다 뺐다) | 없음 |
| Q35 | API 시각 표기를 UTC `Z` 로 고정한다 | **완료** — `ResponseTimeFormatTest`(신설), `application.yml`(`spring.jackson.time-zone: UTC`), `logback-spring.xml`(`%d{…,UTC}Z`), `time-rules.md` 「표현 — 층마다」(신설 절), `api-guidelines.md` 예 수정, `observability-rules.md`, `quality-gates.md`. **사용자가 요구사항 정의를 물었다** — 절 넷에 흩어진 것을 층 여덟 줄 표로 모았고, **로그는 UTC 유지**(사용자 결정 2026-09-13), **요청 시각은 오프셋 필수**(내가 정했다 — 받는 입구가 아직 0). **실측**: Windows 에서도 이미 `Z` 였다 — Jackson 3 가 문맥 시간대(UTC)로 맞춘다. 행의 「읽는 자리에서 고친다」 예보는 틀렸다(Jackson 이 오프셋을 그대로 적는다는 전제였다). 그래서 (a) 설정 명시로 갔다 — 나가는 문 하나가 규약의 자리다. **부순 증거**: 설정을 `Asia/Seoul` 로 바꾸니 `+09:00` 이 나가 빨갰다. 로그는 `16:17:07.117Z` 가 `date -u` 와 같았다. **축**: 규약(`time-rules`). **강제 지점**: 설정(2위) + 테스트(4위). **검증**: `integrationTest --tests '*ResponseTimeFormatTest*'` 초록, `verify.sh` 초록(fast) | 완료 |
| Q36 | ~~쌓인 `add column` 을 `create` 로 접는다 — **마지막 스키마 청크**~~ | **완료 — 스키마 diff 0.** 마이그레이션 열여섯 파일에서 `add column` **스물넷**을 표의 `create` **일곱 파일**로 옮겼다(`V11`·`V13`·`V16`·`V23`·`V38`·`V43`·`V52`·`V53` — 표는 아홉인데 `V16`·`V52` 가 표를 둘씩 든다. **분할표의 「아홉」은 표 수였다**). 주석도 같이 옮겼다. 제약·인덱스·뷰는 그대로 두고 파일도 안 지웠다. **닫힘이 곧 증거다**: 빈 DB 로 처음부터 올려 `pg_dump --schema-only` 를 전후로 떠서 **4843줄 중 다른 것이 두 줄**이고 그 둘은 `pg_dump` 가 실행마다 새로 만드는 `\restrict` 토큰이다 — 스키마는 완전히 같다. **밟은 것 셋**: (1) 표의 **마지막 칸 뒤에 넣으면 앞 줄에 쉼표가 없다** — 첫 판에 `V11` 이 문법 오류로 죽었다. 넣는 자리가 닫는 괄호 앞이면 앞 정의에 쉼표를 붙이게 고쳤다 (2) `V45` 는 `add column` 이 **다음 줄**에 있어 정규식에 안 걸렸다 — 손으로 옮겼다 (3) **Testcontainers 재사용 컨테이너에 옛 이력이 남아** 체크섬 불일치로 930개가 빨갰다. 열여덟을 지우니 초록 — 그 절차를 `D23` 「마이그레이션」에 적었다. **`V32` 는 옛 이름으로 옮겼다** — `V66` 이 그것을 새 이름으로 바꾸므로 순서가 유지된다. **`D2` 의 `V` 다섯을 고쳤다**(컬럼을 가리키는 것만 — 제약을 가리키는 `V25`·`V29`·`V51` 은 그 파일에 그대로 있다). **축**: 규약(`D23` 「마이그레이션」). **강제 지점**: 없다 — 한 번 접는 일이다. **닫힘**: diff 0 · `verify.sh --full` 초록 · 빈 DB 기동에서 `applied_migrations: 66` | 마지막 |
| Q37 | ~~호스팅에 올릴 수 있게 로컬 전제를 환경변수로 푼다~~ | **완료** — `application.yml`(`REDIS_PASSWORD`·`SESSION_COOKIE_SECURE`·`TRUSTED_PROXIES`·`PORT` 폴백), `backend/Dockerfile`·`.dockerignore`(신설), `backend/README.md` 「호스팅에 올릴 때」(신설 절). **사용자가 물었다** — Vercel·Railway 에 지금 올릴 수 있나. 코드가 로컬 전제 셋(쿠키 `secure:false`·프록시 신뢰 루프백·Redis 무비번)을 박아 둬서 못 올렸다. **프로필로 안 갈랐다** — `D16` 「프로필마다 설정이 갈리면 안 돌려본 조합이 생긴다」. env 기본값이 전부 로컬과 같아서 안 넣으면 지금과 같이 돈다. **실제 배포는 사용자 손이다**(계정·env 입력). 「확정 사항」의 「로컬 실행만」은 그대로다 — 올릴 수 있게만 했지 올린 것이 아니다. **`Q36` 과의 순서**: 올리기 전에 접거나, 데모라 접을 때 호스팅 DB 를 비운다(README 에 적었다). **축**: 관례(Railway `PORT`·관리형 Redis 비밀번호) + `D14`(세션 쿠키 평문 금지·프록시 신뢰 범위) + `D16`. **강제 지점**: 없다 — 값이 배포 환경에 있고 저장소엔 없다. `TRUSTED_PROXIES` 는 Vercel 짝이면 못 좁힌다는 것을 README 표로 남겼다 | 완료 |
| Q38 | ~~프론트를 컨테이너로 만든다 — Next 도 백엔드와 같은 사설망에 둔다~~ | **완료** — `frontend/Dockerfile`·`frontend/.dockerignore`(둘 다 신설) · `next.config.ts`(`output: "standalone"`) · `frontend-rules.md` · `backend/README.md`. **`Q37` 이 남긴 구멍을 닫는다** — 화면이 바깥에 서면 `TRUSTED_PROXIES` 를 못 좁혀서 아무나 `X-Forwarded-For` 로 IP 를 속이고 `acted_ip`(동의 입증)가 틀린 값으로 쌓인다. 둘을 같은 사설망에 두면 그 대역으로 좁힐 수 있고 **백엔드는 공개 도메인이 없어진다.** **`standalone` 이 핵심이다** — 없으면 이미지가 `node_modules` 를 통째로 진다. 켜면 Next 가 실제로 쓰는 것만 골라 서버 진입점과 같이 내놓고, 정적 파일 둘(`.next/static`·`public`)은 거기 안 들어가서 따로 옮긴다. **Node 판은 CI 와 같은 22 로 박았다** — 갈리면 이미지에서만 나는 실패가 생긴다. **축**: 규약(`D24`) + `D14`(프록시 신뢰 범위). **강제 지점**: 없다 — 이미지가 도는 명령이 CI 의 `next build` 와 같고, 사설망 주소는 env 라 저장소에 없다. **닫힘**: `docker build frontend` 통과(336MB) · `BACKEND_ORIGIN=http://host.docker.internal:8080` 으로 띄워 `/`·`/products`·`/login` **셋 다 200**. **밟은 것**: 처음에 `localhost:8080` 을 줬더니 500 이었다 — **컨테이너 안의 `localhost` 는 자기 자신**이다. 그 자리가 곧 배포에서 사설망 주소를 줘야 하는 이유다 | Q36 · 마지막 |
| Q39 | 첫 배포 — Railway 에 Postgres·Redis·서비스 둘을 세운다 | **2026-09-20(일)에 친다**(사용자 결정 2026-09-18) — 날을 잡은 것이라 그날 허가를 다시 안 묻는다. **과금 때문에 미뤄 왔다** — 올린 순간부터 돈이 꾸준히 나가므로 다시 올릴 일이 적어질 때 한 번 올린다(사용자 결정 2026-09-13). 차례는 「구간」 표가 든다. **그날 전에 칠 것은 없다**(2026-09-18 — `Q95`~`Q102` 여덟을 그날 다 쳤다). `Q36` 이 먼저다 — 배포 뒤엔 접지 못한다. **사용자 손과 MCP 를 가른다** — 계정·비밀·`accept-deploy`(파괴적)는 사용자, 변수 참조·healthcheck·진단·로그 읽기는 Railway MCP(`set-variables`·`update-service`·`get-deployment-diagnosis`·`get-logs`). **절차**: (1) 프로젝트 하나에 Postgres·Redis·`backend`(Root Directory `backend`)·`frontend`(Root Directory `frontend`) (2) 변수 — README 표의 다섯을 `${{Postgres.PGHOST}}` 식 참조로, `TRUSTED_PROXIES` 는 사설망 대역, `SESSION_COOKIE_SECURE=true`, `frontend` 의 `BACKEND_ORIGIN` 은 사설망 주소 (3) `backend` healthcheck 경로 `/actuator/health`(`show-details` 를 이미 닫아 둬 공개돼도 새는 것이 없다) (4) 공개 도메인은 `frontend` 만 — `backend` 는 도메인을 안 만든다 (4-1) **데모 데이터를 붓는다 — 이 단계가 2026-09-17 까지 빠져 있었다**(`Q85` 가 찾았다). 기본 프로필은 `db/seed` 를 안 읽고(`application.yml:9` 의 `locations`) `DemoOrderSeeder` 가 `@Profile("local")` 이라(`:50`), **그냥 올리면 스키마만 서고 상품 0·셀러 0·주문 0 인 빈 쇼핑몰이 뜬다.** 프로필을 새로 안 가른다(`Q37` 이 「프로필마다 설정이 갈리면 안 돌려본 조합이 생긴다」로 이미 정했다) — **`64` 의 덤프·복구 스크립트로 로컬의 시드 찬 DB 를 떠서 붓는다.** 그래서 `64` 가 이 청크의 실질 선행이다(`PLAN.md` 「구간」 ②) (5) 올린 뒤 `get-logs` 로 개인정보가 식별자만인지 대조 — `D16` 을 실물 로그로 처음 확인하는 자리 (6) `http-response-time` 첫 측정값을 적어 둔다 — `D21` 의 근거 출처 (7) **올린 직후 배포 기준점을 남긴다**(`Q51`) — `backend/src/main/resources/db/deployed-baseline` 에 그때 올린 커밋 해시와 날짜 주석을 적고 커밋한다. **이 파일이 마이그레이션 불변 게이트의 스위치다.** 그리고 **`db/seed` 도 같이 얼어붙는다**(2026-09-18 확인 — `migration-immutable.sh:18` 이 두 경로를 다 본다). 뒤 구간이 데모 데이터를 더할 때는 **새 파일을 더한다** — `A` 는 허용이고 `M`·`D`·`R` 이 빨갛다. 이것을 모르면 ④의 쿠폰·리뷰 청크가 기존 시드를 고치다 처음 밟는다. 기준점 파일이 **없는 동안은** 게이트가 「배포 전」으로 물러나 있고, 생기는 순간부터 그 커밋 뒤로 고치거나 지운 마이그레이션이 CI 를 빨갛게 만든다. 태그가 아니라 파일인 이유는 **얕게 받은 CI 에서 「없는 것」과 「못 읽는 것」이 갈려야** 해서다(`Q51` 참조). **축**: 관례(Railway 변수 참조·healthcheck·`PORT`) + `D14`(쿠키 secure·프록시 신뢰) + `D16`. **강제 지점**: 없다 — 값은 배포 환경에 있고 저장소엔 없다. 대신 (5)가 `D16` 을 잰다. **건드리는 자리**: 저장소는 `backend/README.md` 「호스팅에 올릴 때」(실제 순서·값 이름으로 고친다) · `stack.md`(기동 실패에서 배운 것) · `PLAN.md` 「확정 사항」의 「로컬 실행만」. **닫힘**: 공개 도메인에서 `/api/health` 200 + 로그인 한 번 성공(secure 쿠키가 실제로 붙나) + `get-logs` 에서 이메일·이름·전화 0건. **안 되면**: 기동 실패는 `get-deployment-diagnosis` 를 읽고 env 누락부터 본다 — README 「안 넣은 것은 로컬 기본값으로 떨어져서 `localhost` 를 찾는다」 | Q36 · Q38 · 64 |
| Q40 | ~~30자를 넘는 컬럼 이름을 줄인다~~ | **완료** — `V66__order_item_withdrawal_notice_rename.sql`(신설) · `OrderService` · `OrderServiceTest` · `RetainedColumnsTest` · `SchemaNamingTest`. `withdrawal_restriction_agreed_at`(32자) → **`withdrawal_notice_agreed_at`**(27자). **이름을 「고지」로 잡은 근거**: 시행령 제21조가 요구하는 것이 제한 사실의 고지와 동의고, 저장소가 이미 그 개념을 고지라고 부른다(`WithdrawalNoticeScreenTest`). 후보 둘을 버린 이유도 적었다 — `withdrawal_agreed_at` 은 무엇에 동의했는지가 빠지고 `wd_` 축약은 `D22` 가 금지한다. **API 에 안 나가는 컬럼이라 계약이 안 바뀐다**(쓰는 곳이 `OrderService` 한 곳과 테스트 둘). **제약 셋은 손대지 않았다** — Postgres 가 rename 을 따라가며 check 식을 같이 고친다. **래칫을 지웠다** — `TOO_LONG_UNTIL_RENAMED` 가 비어서 그냥 `isEmpty()` 다. **축**: 규약(`D22`). **강제 지점**: 테스트(4위). **닫힘**: `SchemaNamingTest.columnNamesAreShort` 초록 · `verify.sh --full` 초록 · **빈 DB 기동 확인**(`POSTGRES_DB=shop_check`) 에서 `applied_migrations: 66` = 마이그레이션 63 + 시드 3 | `Q31` |
| Q41 | ~~스펙의 속성 표기가 실제 응답과 갈린 것을 잡는다~~ | **완료** — `OpenApiConfig`(신설) · `OpenApiSpecTest`(테스트 둘) · `ArchitectureTest`(허용 목록 한 줄) · `api-guidelines.md` · `quality-gates.md`. **원인은 Jackson 버전이 둘인 것이었다** — 앱은 Jackson 3(`tools.jackson`), 스펙을 그리는 swagger-core 2.2.55 는 Jackson 2(`com.fasterxml`)라 **스프링이 만든 매퍼를 건넬 수가 없고** swagger 가 자기 기본값(낙타 표기)으로 그렸다. `ModelResolver` 에 뱀 표기를 물린 매퍼를 건네서 고쳤고, **모르는 값이면 안 뜨게 했다** — 조용히 기본값으로 돌아가면 지금 고친 것이 그대로 되돌아온다. **`_visible_field_groups` 만 밑줄로 시작한다**(`@JsonProperty` 로 박힌 봉투 메타 표식)라 정규식이 그것을 인정한다. **`ArchitectureTest` 가 물었다** — `support` 에서 `io.swagger` 를 부르는 것이 막혔다. 그 규칙이 막는 것은 **자원 패키지 의존**이고 허용 목록은 라이브러리를 적는 자리라 `io.swagger..` 를 더하고 「여기 더할 수 있는 것은 라이브러리뿐」을 javadoc 에 박았다. **축**: 표준(OpenAPI)·규약(`D5`). **강제 지점**: 테스트(4위). **닫힘**: `OpenApiSpecTest` 일곱 초록, `verify.sh --full` 초록. **부순 증거**: `OpenApiConfig` 를 빼고 돌리니 스펙이 낙타 표기로 돌아가며 표기 검사가 빨갰다. **실제 응답 대조는 그때 안 빨갰다** — 상품 목록의 최상위 키가 전부 한 단어라 두 표기에서 글자가 같다. 그 얇은 신호를 javadoc 에 적었다 | 없음 |
| Q42 | ~~`D22` 의 「번호는 `_no`」와 실물 `_number` 여덟이 갈린 것을 푼다~~ | **완료 — 문서를 실물에 맞췄고, 결정의 근거는 실측이 고쳤다.** `naming-rules.md` 「접미사」 · `SchemaNamingTest`(테스트 하나). **정할 때 「`_no` 는 실물이 0인 규칙」이라고 했는데 틀렸다** — 도는 스키마에 **다섯**이 있었고 한 갈래도 아니었다: 화면 순번 셋(`sort_no`)과 **국가가 발급한 번호 둘**(`business_reg_no`·`mail_order_no` — 셀러 신원 표시라 `D2` R1 이 부른다). **그래서 「문서를 실물에 맞춘다」의 내용이 바뀌었다** — `_no` 를 지우는 것이 아니라 **가르는 기준을 적는 것**이 됐다: 「누가 발급했나」. 우리가 만들어 내보내는 번호만 `_number` 고, 형식을 우리가 안 정하는 것은 `_no` 다. **강제 지점**: `_no` 컬럼을 다섯으로 박았다 — 새로 생기면 **그것이 우리 노출 번호인지**를 그 자리에서 묻게 된다. 항목마다 「왜 우리 번호가 아닌가」를 값으로 적는다. **축**: 규약(`D22`). **닫힘**: `SchemaNamingTest` 열 초록 | 없음 |
| Q43 | ~~`specMatchesRealResponse` 가 실제로 재게 만든다~~ | **완료 — 고치는 쪽으로 갔다.** `OpenApiSpecTest`(그 테스트 하나) · `quality-gates.md`. **`Q45` 가 길을 터 줬다** — 응답 타입이 이름으로 합쳐져 있던 동안에는 「이 경로의 스키마」를 고를 수가 없었다. 지금은 경로마다 제 스키마를 가리켜서 `items` 의 `$ref` 를 따라갈 수 있다. **고친 것 둘**: 응답을 **중첩까지 전부** 걷고, 비교 대상을 **그 경로의 스키마에서 `$ref` 를 따라간 속성**으로 좁혔다(전에는 스펙 전체를 한 덩이로 합쳤다). **`/api/health` 를 대조 경로에 넣은 것이 이 청크의 값이다** — 여러 단어 키(`applied_migrations`·`checked_at`)가 **데이터 없이도 늘 있어서** 빈 DB 에서도 문다. **목록 안쪽은 아직 못 본다**(통합 레인에 상품 행이 없어 `items` 가 빈다). 그 한계를 javadoc 에 적었다 — 행을 만들어 주는 자리가 생기면 그때 안쪽까지 걸린다. **축**: 표준(OpenAPI). **강제 지점**: 테스트(4위). **닫힘**: `OpenApiSpecTest` 아홉 초록 · `verify.sh --full` 초록. **부순 증거**: `OpenApiConfig` 를 빼니 `/api/health` 의 두 키를 짚었다 — **`Q41` 때 같은 조작에 이 대조만 조용했던 자리다** | Q41, Q45 |
| Q45 | ~~응답 타입이 스펙에서 같은 이름으로 합쳐지던 것을 끊는다~~ | **완료** — `@Schema(name = …)` 27개(파일 열셋 — `*Query` 여섯 · `*Controller` 둘 · `*Service` 넷 · `PermissionCatalog`) · `SchemaNameTest`(신설, 빠른 레인) · `OpenApiSpecTest`(테스트 둘) · `api-guidelines.md` · `quality-gates.md`. **`Q43` 을 재다가 나왔다.** 경로↔응답 스키마가 이어지는지 보려고 스펙을 떠 보니 **`GET /api/orders` 가 정산 요약을 돌려준다고 적혀 있었다** — springdoc 이 스키마 이름을 **자바 클래스의 짧은 이름**으로 짓는데 `Page` 가 여섯, `Summary`·`Detail` 이 각각 넷이라 서로 덮어썼다. **스키마가 63 → 81** 로 늘었다(덮여서 사라졌던 것이 열여덟). **빌드도 테스트도 초록이었다** — 깨지는 것은 스펙을 읽고 만든 프론트 타입이라 백엔드에 신호가 없다. **`Q31` 이 「`Page` 를 자원 전체가 공유한다」고 본 것이 이것의 증상이었다** — 공유가 아니라 합쳐진 것이었고, 그 javadoc 을 고쳤다. **게이트를 두 층에 걸었다**: 소스 쪽(`SchemaNameTest`, 빠른 레인)이 **이름이 겹칠 수 있다는 것 자체**를 막고, 스펙 쪽(`OpenApiSpecTest`)이 **상관없는 경로가 같은 스키마를 가리키는 것**과 **내부 전용이라 적어 둔 이름이 스펙에 뜨는 것**을 막는다. 스펙 쪽만으로는 못 본다 — 합쳐진 것이 중첩 타입이거나 한 경로 안에서만 쓰이면 이름이 하나만 남아 정상과 구분이 안 된다(실측). **세우자마자 하나 더 잡았다** — `BlockRequest` 가 문의·상품 둘이고 **요청 본문이라 스펙에 닿는다.** **축**: 표준(OpenAPI)·규약(`D5`). **강제 지점**: 테스트(4위), 두 층. **닫힘**: 스키마 81 · 두 테스트 초록 · `verify.sh --full` 초록. **부순 증거**: `OrderSummary`·`SellerOrderSummary` 이름 둘을 떼어 같은 `Summary` 로 만드니 소스 게이트가 둘을 짚었다. **이름 하나만 떼면 안 문다 — 그건 맞는 동작이다**(겹치지 않으면 결함이 아니다) | Q43 |
| Q44 | ~~안전 기본값이 필요한 설정에 기동 실패를 건다~~ | **완료** — `SecuritySettingsCheck`(신설) · `SecuritySettingsCheckTest`(신설, 빠른 레인) · `security-baseline.md` · `backend/README.md` · `quality-gates.md`. **「지금이 운영인가」를 안 묻는다**(사용자 결정) — `Q37` 의 「프로필로 안 가른다」(`D16`)와 안 부딪치게, **어느 환경에서도 정당한 이유가 없는 조합**만 막는다: (1) 신뢰 프록시가 공인 IP 까지 문다 (2) 프록시가 루프백 밖인데 세션 쿠키가 평문이다. **글자가 아니라 동작으로 잰다** — `.*` 모양을 찾으면 같은 뜻의 다른 표기(`\d+\.\d+\.\d+\.\d+`)를 놓친다. 정규식에 **실제 주소를 넣어 본다.** **로컬 기본값(루프백)은 둘 다 해당 안 돼서 안 걸린다.** **판정을 정적 메서드로 뺐다** — 나쁜 값으로 컨텍스트를 띄우는 테스트는 그 자체로 못 뜨는 테스트가 돼서 무엇이 막았는지가 안 남는다. **밟은 것 둘**: SpotBugs 가 `CT_CONSTRUCTOR_THROW` 로 생성자 예외를 막아서 `@PostConstruct` 로 옮겼고, `@Configuration` 은 CGLIB 상속이라 `final` 이 안 돼서 `@Bean` 이 없는 이 클래스는 `@Component` 가 맞았다. **축**: 표준·규약(`D14`). **강제 지점**: **앱 검증(3위)** — 기동 시점이라 테스트보다 위다. **닫힘**: 테스트 여섯 초록 · `verify.sh --full` 초록 | 없음 |
| Q46 | ~~사유 컬럼과 요청 record 를 잇는다~~ | **완료** — `LengthConstraintTest`(쌍 넷 추가, 예외 열둘 → 여덟(그 뒤 `5c-1`·`43a-4b` 가 셋을 더해 지금 열하나다)) · `coding-rules.md`. **호출 사슬을 따라가 찾았다**: `ShipmentController`·`ReturnController`·`RefundController` 가 넘긴 사유가 `OrderStatusService.Actor.reason`·`ReturnRequestService.Decision.Reject.reason`·`RefundService.writeNote` 를 지나 컬럼에 닿는다. **`order_status_history_note.reason` 은 입구가 넷이었다** — 상태를 옮기는 모든 동작이 같은 칸에 쌓는다. 넷을 다 적었고, **하나만 이었으면 나머지 셋이 갈려도 안 걸린다**(부순 증거가 그것을 보였다: `ReceiveRequest.reason` 만 499 로 바꾸니 그 쌍이 빨갛다). **둘은 안 이었다** — `return_note.request_reason`·`inspection_note` 는 **쓰는 코드가 아직 없다**(반품 접수·검수 입구가 `43a`). 예외 목록에 그 이유로 남겼다 — 「짝을 못 찾았다」가 아니라 「아직 쓰는 쪽이 없다」다. **축**: 규약(`D23` 「길이」). **강제 지점**: 테스트(4위). **닫힘**: 스무 테스트 초록 | Q28 |
| Q47 | ~~「기동에서 막는다」를 기동으로 잰다~~ | **완료** — `SecuritySettingsCheckTest`(테스트 둘) · `testing-strategy.md` · `quality-gates.md`. **정할 것이 없어졌다.** 행이 「나쁜 값으로 컨텍스트를 띄우면 가짓수가 하나 는다, 값이 그만한지 재고 정한다」고 물었는데 — **안 늘리고 되는 길**이 있었다. `ApplicationContextRunner` 는 그 빈만 올린 컨텍스트를 그 자리에서 만들고 버려서 **캐시에 안 들어가고 빠른 레인에서 돈다.** `@SpringBootTest` + `properties` 였으면 캐시 키가 달라져 느린 레인이 늘었다(`D15` 「컨텍스트 가짓수가 곧 기동 횟수다」). **둘을 잰다**: 나쁜 값이면 컨텍스트가 안 뜨고(원인 예외에 메시지가 있다 — 스프링이 한 겹 감싼다), 기본값이면 뜨고 빈이 하나 등록된다(`@Value` 키가 어긋나면 여기서 죽는다). **축**: 규약(`D15`)·표준(`D14`). **강제 지점**: 테스트(4위). **닫힘**: 열 초록. **부순 증거**: `@PostConstruct` 를 떼니 **그 하나만** 빨갛고 나머지 아홉은 초록이었다 — 판정과 배선을 가려서 잰다는 뜻이다 | Q44 |
| Q48 | ~~문서↔실물 대조 테스트 — `document-map`·`domain-model`~~ | **2026-09-14 설계 점검(Fable)이 문서 넷이 실물과 갈린 것을 찾았다**: `document-map.md` 다섯 행(D7·D10·D11·D14·D16 — 파일이 있는데 「없으면 생기는 일」 칸 그대로) · `domain-model.md` 가 `V16` 에서 멈춤(정산·문의·환불·반품·알림·배상·회차 표 일곱 언급 0) · `security-baseline.md` 캡차 절이 없는 통제를 현재형으로 · `permission-rules.md` 「구멍 3」이 닫힌 것을 열린 것으로. **넷은 설계 세션이 그 자리에서 고쳤다**(같은 커밋). 이 청크는 재발을 막는 테스트다 — `PlanProgressConsistencyTest` 는 있는데 문서↔파일 대조가 없었다. **절차**: (1) `backend/src/test/java/com/projectshop/shop/support/DocumentMapConsistencyTest.java` 신설. 빠른 레인(`db` 태그 없음), `StackVersionConsistencyTest` 와 같은 꼴로 파일을 읽는다 (2) 테스트 셋 — ① `documentMapCompletedRowsPointToExistingFiles`: `doc/reference/document-map.md` 「기준 문서 목록」 표에서 마지막 칸이 `완료 — \`x.md\`` 인 행마다 `doc/reference/x.md` 가 있다 ② `referenceDocsAreMentionedInDocumentMap`: `doc/reference/*.md` 파일명 전부가 `document-map.md` 본문 어딘가에 글자로 있다(색인 자신은 뺀다). 없는 것이 나오면 **문서를 고친다** — 예외 목록은 안 만든다 ③ `domainModelMentionsEveryTable`: `backend/src/main/resources/db/migration/V*.sql` 의 `create table (\w+)` 이름 전부가 `domain-model.md` 에 백틱으로 있다. 설계 세션이 53개를 다 적었으니(마무리 16차 독립 리뷰가 52 를 53 으로, 백틱 없던 열을 백틱으로 고쳤다) 지금 초록이어야 한다 — 빨가면 표를 빠뜨린 것이니 문서에 더한다 (3) `build.gradle.kts` `comparedInFastLane` 에 `doc/reference/document-map.md`·`doc/reference/domain-model.md` 를 더하고 마이그레이션 폴더를 `fileTree` 로 `inputs.files` 에 건다 — **안 걸면 문서만 고친 청크에서 `test` 가 `UP-TO-DATE` 로 건너뛴다**(`Q25`, 실측 세 번) (4) `quality-gates.md` 게이트 표에 행. **부순 증거**: `document-map.md` 의 `state-machines.md` 를 `state-machine.md` 로 바꿔 ①이 빨간 것 · 임시 `V99__zz.sql` 에 `create table zz (id int);` 만 넣어(`V70`·`V71` 은 `Q57`·`Q59` 가 쓴다) ③이 빨간 것(파일은 지운다) (5) `testing-strategy.md` 대조 표에 행. **축**: 규약(`D1` 문서 트리 · `D23` 축 2). **강제 지점**: 테스트(4위) — 문서(5위)에서 내린다. **건드리는 자리**: 위 신설 하나, 수정 셋. **닫힘**: `DocumentMapConsistencyTest` 셋 초록 · `verify.sh` 초록. **안 되면**: ②에서 문서를 고쳐도 안 맞는 파일이 다섯을 넘으면 규칙이 틀린 것이다 — 멈추고 목록을 적어 둔다. **완료**(2026-09-15). `DocumentMapConsistencyTest` 신설 · `document-map.md` · `build.gradle.kts` · `quality-gates.md` · `testing-strategy.md`. **②가 넷을 냈다**(상한 다섯 안이라 예정대로 문서를 고쳤다): `commerce-compliance.md`·`glossary.md` 는 행이 있는데 **파일 이름을 안 적은 것**(`D2` 칸이 그냥 「완료」였다), `stack.md`·`permission-models.md` 는 **번호를 안 받아 목록에 아예 없던 것**. **후자가 이 청크의 발견이다** — 지도가 D 번호만 담아서 **번호 없는 문서는 어느 목록에도 안 잡혔다.** 「번호 없는 참고 자료」 절을 파고 셋을 넣었다(`external-references.md` 포함). 예외 목록은 안 만들었다(사용자 선택 ①). ①·③ 은 처음부터 초록이었다 — 표 53개가 전부 백틱으로 있다. **입력을 폴더로 걸었다**: `../doc/reference` 와 `src/main/resources/db/migration`. 파일 하나씩 걸면 **새 문서가 생긴 날 입력이 안 바뀌어서** 정작 그 문서가 안 잡힌다 — **잡아야 할 사건이 곧 입력의 변화다.** **부순 증거 셋**: ① 지도의 `state-machines.md` → `state-machine.md` 로 빨강(②도 같이 빨갰다 — 이름을 틀리면 그 파일이 지도에서 사라진 것이 된다) ② `doc/reference/zz-probe.md` 신설로 빨강 ③ `V99__zz_probe.sql` 에 `create table zz_probe` 만 넣어 빨강. **②가 `Q25` 배선까지 쟀다** — 문서를 새로 만든 것만으로 `test` 가 다시 돌았다. **지나가며 드러난 것 하나를 흡수했다**: `testing-strategy.md` 의 레인 표가 87·930 인데 실측이 **132·1109** 다(2026-09-06 이후 아홉 날에 45·179). 비율 줄도 9% → 11%. **이 표는 이 테스트가 못 본다** — 이름이 아니라 수라서 재려면 두 레인을 다 돌려야 한다. 잰 날짜를 표에 박는 것이 지금 내릴 수 있는 한계다 | 없음 |
| Q49 | ~~노출 번호 채번 재시도를 트랜잭션 밖으로~~ | **`ExposedNumber.insertWith` 의 재시도는 죽은 코드다**(2026-09-14 설계 점검). `DuplicateKeyException` 을 잡고 **같은 트랜잭션 안에서** 다시 `insert` 하는데, Postgres 는 오류 한 번이면 트랜잭션이 abort 라 두 번째 문장은 SQLSTATE `25P02`(`in_failed_sql_transaction`) 로 죽는다 → `DuplicateKeyException` 이 아니라 catch 못 함 → 500. 재시도 3회는 한 번도 못 돈다. 호출 다섯이 전부 `@Transactional` 안이다: `OrderService:288·398` · `RefundService:650` · `SettlementService:188` · `InquiryService:102`. **저장소가 이 규칙을 안다** — `OrderController.create` 주석 「안쪽은 이미 깨진 트랜잭션이라 다음 문장부터 못 돈다」. 테스트 0(`ExposedNumber` 를 참조하는 테스트가 없다). 충돌 확률이 32⁶ 분의 1 이라 실측 불가 — **부순 증거가 없는 게이트**였다(`D25`). **결정(설계 세션)**: 재시도를 **멱등 바깥의 `Retries.onConflict`** 로 옮긴다. 거기가 이미 트랜잭션 밖이고 선점 기록까지 같이 롤백되는 자리다. **절차**: (1) `ExposedNumber.insertWith` → `insert` 로 이름을 바꾸고 루프·catch·`RETRIES`·`ShopException(INTERNAL, "…못 뽑았다")` 를 걷는다. 한 번 넣고 예외는 그대로 올린다. 호출 다섯을 고친다 (2) `Retries.conflictState` 가 SQLSTATE `23505` 중 **제약 이름이 `_number_unique` 로 끝나는 것**만 충돌로 본다 — `PSQLException.getServerErrorMessage().getConstraint()`. 실물 이름은 `shop_order_number_unique`·`seller_order_number_unique` 고 환불·정산·문의 것은 `\d` 로 확인해 javadoc 에 적는다. 다른 `23505` 는 지금처럼 안 잡는다 (3) 호출 다섯 중 `Retries.onConflict` 로 감싸인 것은 `OrderController.create`·`PaymentService.pay` 뿐이다 — **나머지 셋을 조사해 표로 적는다.** 정산은 `BatchRuns` 안이라 `RetryableBatch` 10분 재시도가 받으니 안 감싼다. 환불(`RefundController` → `RefundService.request`)·문의(`InquiryController`)는 컨트롤러에서 `Retries.onConflict` 로 감싼다 — `OrderController` 와 같은 자리, 같은 주석 (4) 테스트 — `RetriesTest.노출_번호_유니크_충돌은_재시도한다`(빠른 레인. `PSQLException` 을 `ServerErrorMessage` 로 조립해 던지는 `Supplier`) · `RetriesTest.다른_유니크_충돌은_재시도하지_않는다` · **왜 밖으로 옮겼나의 증거** `TransactionAbortTest.유니크_충돌_뒤에는_같은_트랜잭션에서_아무_문장도_못_돈다`(`PostgresTestBase`, `TransactionTemplate` 안에서 같은 `order_number` 두 번 insert → 두 번째가 `DuplicateKey`, 그 뒤 `select 1` 이 `25P02`) (5) `stack.md` 「기억으로 쓰면 틀리는 자리」에 절 「유니크 충돌 뒤 같은 트랜잭션은 죽어 있다 — 재시도는 밖에서」 (6) `concurrency-rules.md` 「재시도」에 한 줄: 노출 번호 충돌도 멱등 바깥 재시도가 받는다. **축**: 플랫폼 사실(Postgres abort) + 규약(`D11` 재시도 · `D9`). **강제 지점**: 테스트(4위) — 타입으로 못 내린다. **건드리는 자리**: `ExposedNumber`·`Retries`·호출 다섯·컨트롤러 둘·테스트 둘 신설·문서 둘. **닫힘**: `RetriesTest` 둘 · `TransactionAbortTest` 하나 · `verify.sh --full`. **부순 증거**: (2)의 제약 이름 대조를 빼면 `RetriesTest` 첫째가 빨갛다. **안 되면**: `getConstraint()` 가 null 이면 메시지 정규식으로 잡되 그 사실을 `stack.md` 에. **완료**(2026-09-15). `ExposedNumber`·`Retries`·호출 다섯·컨트롤러 둘·테스트 둘 신설·`RetriesTest` 넷 추가·`stack.md`·`concurrency-rules.md`·`quality-gates.md`. **실측이 (2)를 뒤집었다.** 행은 제약 이름을 `Retries.conflictState` 에 두고 `getConstraint()` 로 읽으라 했는데 **드라이버가 `runtimeOnly` 라 본코드가 `PSQLException` 에 컴파일로 못 붙는다.** 리플렉션으로만 닿는 것을 탐침으로 확인했다. **그리고 꼬리 규칙(`_number_unique`)이 너무 넓었다** — 그 꼬리를 가진 제약이 다섯이 아니라 **일곱**이고, 나머지 둘(`payment_approval_number_unique`·`refund_gateway_refund_number_unique`)은 **결제사가 준 값**이라 다시 뽑을 수가 없다. 꼬리로 갈랐으면 네 번 헛돌고 **진짜 중복 승인을 재시도가 덮었다.** **그래서 목록을 부르는 자리로 내렸다**(사용자 선택 ①의 다섯 이름을 `Retries` 가 아니라 `ExposedNumber.insert` 인자로). 결제사 번호는 그 경로를 안 지나서 **구조적으로 못 들어오고**, 「같은 insert 의 다른 유일 제약 위반까지 재시도하는」 약점도 같이 사라졌다. 판정은 `ExposedNumber.Conflict` **타입**이 든다 — 목록이 두 곳에 갈릴 자리가 없다. 제약 이름은 **메시지의 큰따옴표 안**에서 읽는다(문구가 번역돼도 안 바뀐다). **테스트 넷**: `RetriesTest` 에 넷(노출 번호 재시도·다른 제약은 안 돈다·결제사 번호는 안 돈다·기존 23505 유지) · `ExposedNumberConflictTest`(DB, 셋 — `25P02` 실물·메시지 꼴·타입 변환) · `ExposedNumberConstraintTest`(빠른 레인, 둘 — 부르는 이름이 실재하나·결제사 번호를 안 부르나). **마지막 것이 ①의 약점을 메운다**: 이름이 틀리면 재시도가 영영 안 도는데 충돌 확률이 32⁶ 분의 1 이라 **실행 중에는 안 드러난다.** **부순 증거 둘**: `seller_order_number_unique` 를 오타 내니 빨강 · 환불 자리를 `payment_approval_number_unique` 로 바꾸니 빨강. **DB 테스트를 쓰다가 이 청크의 함정을 그대로 밟았다** — 바탕이 `@Transactional` 이라 충돌이 테스트 자신의 트랜잭션을 죽였고 뒷정리까지 `25P02` 로 죽었다. `REQUIRES_NEW` 로 고쳤고 그 사실을 javadoc 에 적었다 | 없음 |
| Q50 | ~~배치 회차에 advisory lock — 재배포 창이 2대다~~ | **`batch-catalog.md` 「서버가 여럿이 될 때」의 전제가 틀렸다**(2026-09-14 설계 점검). "2대가 없어서 검증 대상이 없다" 인데 **배포·재시작 자체가 2대 창**이다 — 옛 인스턴스가 내려가기 전 새 것이 뜬다. 04:00 에 배포하면 크론 셋이 겹친다. 문서 스스로 「정산 마감은 깨진다」고 적었다. `BatchRuns.record` 는 트랜잭션 밖이고 `insert` 의 부분 유니크는 성공 행 하나만 막지 **동시 실행 구간은 안 막는다**. **결정**: `pg_try_advisory_lock` **세션 잠금을 연결 하나에 붙들고** 본체를 돈다 — xact 잠금은 트랜잭션이 필요한데 `record` 는 일부러 트랜잭션 밖이라 안 맞는다. **절차**: (1) `BatchRuns` 에 `DataSource` 주입. `record` 안에서 `Connection lock = dataSource.getConnection()` → `select pg_try_advisory_lock(hashtext(?))`(batchName) → false 면 `SKIPPED` 행을 남기고(`failure_reason` 은 null — `batch_run_failure_reason_check` 가 `failed` 에만 요구한다) `log.warn("{} 건너뜀 기준일={} — 다른 인스턴스가 같은 배치를 돌리는 중")` → 빈 값. true 면 본체 → `finally` 에서 **같은 `Connection` 으로** `select pg_advisory_unlock(hashtext(?))` → `close`. **다른 연결에서 unlock 하면 안 풀린다** — 풀 연결이라 반드시 같은 객체 (2) `recordAfter` 는 `record` 를 부르니 따라온다 (3) 풀 크기 — Hikari 기본 10, 배치 스레드 10(`task.scheduling.pool.size`). 배치 열이 잠금 연결 열을 쥐면 요청이 굶는다. `spring.datasource.hikari.maximum-pool-size: 20` 과 근거 주석 (4) 테스트 `BatchRunsTest.같은_배치가_동시에_돌면_한쪽은_건너뛴다`(`PostgresTestBase`, 롤백 안 하는 꼴 — `testing-strategy.md` 「경쟁을 보려면 롤백을 끈다」): 스레드 둘이 `record("x", 오늘, latch 로 붙드는 본체)` → 하나는 `Counts`, 하나는 empty + `batch_run` 에 `skipped` 1행 · `BatchRunsTest.잠금은_본체가_던져도_풀린다` (5) `batch-catalog.md` 「서버가 여럿이 될 때」를 고쳐 쓴다 — 전제가 틀린 이유(배포 창), 잠금이 막는 것(같은 `batch_name` 의 동시 실행)과 못 막는 것(체인 순서는 `recordAfter` 가 지킨다). 「지금 안 하는 것」의 분산 잠금 행을 지운다. `time-rules.md` 「지금 안 하는 것」의 분산 스케줄러 행도 (6) `quality-gates.md` 행. **축**: 규약(`D19` 재실행 안전성 · `D11`) + 플랫폼 사실(advisory lock 은 세션 단위). **강제 지점**: 앱 검증(3위) — 잠금이 실제로 막는다. 테스트(4위)가 그것을 고정. **건드리는 자리**: `BatchRuns`·`application.yml`·`BatchRunsTest`·문서 셋. **닫힘**: `BatchRunsTest` 둘 · `verify.sh --full`. **부순 증거**: unlock 을 다른 연결에서 하게 바꾸면 둘째가 빨갛다(잠금이 안 풀려 다음 회차가 skipped). **안 되면**: 재사용 컨테이너에 잠금이 남으면 다음 테스트가 skipped 로 시작한다 — `@AfterEach` 에서 `select pg_advisory_unlock_all()`. **완료**(2026-09-15). `BatchLockConnections` 신설 · `BatchRuns` · `BatchLockTest` 신설 · `batch-catalog.md`·`time-rules.md`·`quality-gates.md`. **(3) 풀 크기 처방이 바뀌었다.** 행은 요청용 웅덩이를 20 으로 키우라 했는데 **사용자가 웅덩이를 가르는 쪽을 골랐다**(후보 ②) — 키우는 쪽은 설정값이 경계라 배치나 요청이 늘 때마다 다시 맞춰야 하고, 인스턴스마다 20 이라 배포 창에서 40 이다. **Hikari 로 두 번째 웅덩이를 만들었더니 `ArchitectureTest` 가 막았다** — `support` 는 허용 목록(`java..`·`org.springframework..` 등) 밖인 `com.zaxxer..` 를 못 부른다. 규칙을 넓히는 대신 `DriverManagerDataSource`(Spring 것)로 바꿨고, **그게 더 맞는 답이었다**: 회차마다 새 세션이라 닫으면 잠금이 반드시 풀린다. 수는 스케줄러 스레드가 묶으므로 따로 상한을 안 둔다 — 두 수가 갈릴 자리가 없어진다. **실측이 테스트 하나를 뒤집었다.** 「본체가 던져도 잠금이 풀린다」를 **다시 `record` 가 도나**로 쟀더니 **일부러 어긴 코드가 초록으로 지나갔다** — advisory lock 이 같은 세션에서 재진입이라 웅덩이가 그 연결을 도로 내주면 이미 쥔 잠금을 또 잡는다. **다른 웅덩이의 연결로 묻게** 고쳐서 잡았다. **부순 증거 셋**: 못 잡았을 때 건너뛰는 가지를 끄니 본체가 두 번 돌아 빨강 · 잠금을 다른 연결에서 풀게 바꾸니 빨강 · 잠금 연결을 요청용 웅덩이에서 빌리게 바꾸니 빨강(요청용을 통째로 쥔 채로 5초 안에 열리나를 잰다) | 없음 · `Q39` 앞 |
| Q51 | ~~마이그레이션 불변 게이트 — 배포 기준점 뒤부터 켜진다~~ | **마이그레이션이 가변이다**(2026-09-14 설계 점검): `V8`·`V9` 삭제, `V13` 8회·`V16` 5회·`V4` 5회·`V2` 4회 수정, `Q36` 이 열여섯 파일을 다시 접었다. 배포 DB 가 없어서 안 아팠다. **`Q39` 뒤엔** `flyway validate` 가 빨갛거나 로컬과 운영의 스키마가 갈린다. 강제 지점이 `D23` 「마이그레이션」(문서, 5위)뿐이다. **결정**: 게이트는 **배포 태그가 생긴 뒤에만 켜진다** — 지금 `origin/main` 대비로 걸면 이 가지의 `Q36` 이 바로 빨갛다. **절차**: (1) `scripts/migration-immutable.sh` 신설: `git tag -l 'deployed/*'` 가 비면 「배포 전 — 안 잰다」 찍고 0 종료. 있으면 최신 태그(정렬 마지막) 대비 `git diff --name-status <tag> HEAD -- backend/src/main/resources/db/migration backend/src/main/resources/db/seed` 에서 `M`·`D`·`R` 줄이 하나라도 있으면 파일명을 찍고 1 종료. 새 `V` 만 허용 (2) `ci.yml` `docs` 잡에 `run: bash scripts/migration-immutable.sh` 한 줄(`doc-lint.sh` 옆). `verify.sh` 빠른 단계에도 넣는다 — 1초 (3) `Q39` 행의 절차 끝에 「올린 직후 `git tag deployed/2026-MM-DD && git push --tags`」를 더한다 — **태그가 게이트의 스위치다** (4) `coding-rules.md` 「마이그레이션」에 절 「배포 태그 뒤에는 새 `V` 만 더한다 — 접는 것(`Q36`)은 배포 전에만 있던 일이다」 (5) `quality-gates.md` 행. **부순 증거**: 임시 태그 `deployed/test` 를 찍고 `V1` 에 주석 한 줄 더해 스크립트가 1 로 죽는 것 → 태그·수정 되돌리고 0 인 것. 둘 다 출력을 `quality-gates.md` 에. **축**: 규약(`D23` 마이그레이션) + 관례(Flyway 체크섬). **강제 지점**: CI 게이트(테스트와 같은 4위 층). **건드리는 자리**: 스크립트 신설 · `ci.yml`·`verify.sh`·`PLAN.md`(`Q39` 행)·문서 둘. **닫힘**: 테스트 없음 — 부순 증거 둘의 출력을 `quality-gates.md` 에 적는 것으로 닫는다. **안 되면**: Windows 실행 비트(`stack.md`) — `bash scripts/…` 로 부르니 무관. **완료**(2026-09-15). `scripts/migration-immutable.sh` 신설 · `ci.yml`(`docs` 잡) · `verify.sh` · `coding-rules.md` · `quality-gates.md` · `PLAN.md`(`Q39` 절차 7). **(1)의 스위치가 태그에서 파일로 바뀌었다**(사용자 선택 ②). 실측이 원안을 막았다: `docs` 잡이 **얕게 받아서** `git tag -l` 이 빈 목록을 주고, 그러면 **「배포 전이라 태그가 없다」와 「못 받아서 없다」가 같은 모양**이라 게이트가 꺼진 채 초록이 된다. 이 저장소가 네 번 밟은 자리고 오늘 아침 `2g-6` 도 같은 종류였다. **파일이면 셋이 갈린다**: 없으면 배포 전(0), 있고 커밋이 잡히면 대조, **있는데 못 잡히면 빨강**. 덤으로 기준점을 뒤로 물리는 것이 리뷰 diff 에 보인다. 기준점 파일은 `backend/src/main/resources/db/deployed-baseline` — Flyway 가 `classpath:db/migration` 만 훑어서 그 옆이 안전하다. **`HEAD` 가 아니라 작업 트리와 견준다**: `verify.sh` 가 커밋 앞에 도는데 `HEAD` 로 견주면 아직 커밋 안 한 수정을 놓치고, 커밋한 뒤 CI 에서야 빨개진다 — 처음에 `HEAD` 로 썼다가 부수기가 안 먹어서 알았다. `docs` 잡은 `fetch-depth: 0` 으로 바꿨다. **부순 증거 여섯 갈래**: 빨강 셋(`V1` 수정 · `V2` 삭제 · 기준점 커밋이 이력에 없음) · 초록 셋(안 고침 · 새 `V999` · 기준점 파일 없음). **`set -e` + `pipefail` 함정도 하나 밟았다** — 주석뿐인 기준점 파일에서 `grep` 이 1 로 끝나 스크립트가 그 자리에서 끊겼고, 종료 코드만 1 이고 이유가 안 나왔다. `|| true` 로 고쳤다 | 없음 · `Q39` 앞 |
| Q52 | ~~세션을 Redis 로 — 재배포가 전원 로그아웃이다~~ | **Redis 가 있는데 세션은 메모리다**(2026-09-14 설계 점검): `starter-data-redis` 와 컨테이너가 있는데 쓰는 곳이 `LoginAttemptService` 하나. 세션은 서블릿 메모리 + `SecurityConfig.sessionRegistry()` 의 `SessionRegistryImpl`. 재배포마다 전원 로그아웃, 인스턴스 둘이면 로그인이 튄다. `real-service-comparison.md` 가 「실서비스」 등급에 뒀는데 **`Q39` 선행**이다. **결정**: `spring-session-data-redis`, **JDK 직렬화** — 절대 만료 12시간이라 클래스가 바뀌어도 위험 구간이 하루 안이다. JSON 은 principal 에 mixin 이 따로 필요해 안 고른다(`stack.md` 에 근거). **절차**: (1) `build.gradle.kts` `implementation("org.springframework.session:spring-session-data-redis")` — Boot 4 BOM 이 관리하나 확인, 아니면 버전을 적고 `stack.md` 표에 (2) `ShopUserDetailsService.ShopUser` 에 `implements Serializable` + `serialVersionUID`. `eraseCredentials` 뒤 상태가 직렬화되는지 본다 (3) `SecurityConfig.sessionRegistry()` 를 `new SpringSessionBackedSessionRegistry<>(sessionRepository)` 로 — `FindByIndexNameSessionRepository` 주입. `HttpSessionEventPublisher` 빈은 지운다(Spring Session 이 이벤트를 직접 낸다). `ConcurrentSessionFilter`·`RegisterSessionAuthenticationStrategy` 는 그대로 (4) `application.yml` `spring.session.timeout: 30m`(서블릿 `timeout` 과 같은 값 — 둘이 갈리면 어느 쪽이 먹나를 실측해 `stack.md` 에) · `spring.session.redis.namespace: shop:session` (5) 탈퇴·정지가 세션을 죽이는 것(`5g`, `AccountLivenessFilter`·`expireNow`)이 Redis 세션에도 먹나 — `HttpFlowTest` 의 두 기기 시나리오가 잰다 (6) 테스트 `SessionStoreTest.로그인_세션이_Redis_에_남는다`(`db` 태그, `PostgresTestBase.forkRedis`): 로그인 → `StringRedisTemplate.keys("shop:session:*")` 가 1 이상 · `SessionStoreTest.탈퇴하면_Redis_세션이_지워진다` (7) `security-baseline.md` 「세션 — 서버 메모리」 절을 제목까지 고쳐 쓴다 · `real-service-comparison.md` 세션 행 · `docker-compose.yml` redis 주석에 세션을 더한다. **축**: 규약(`D14` 세션) + 관례(Spring Session). **강제 지점**: 테스트(4위). **건드리는 자리**: 위. **닫힘**: `SessionStoreTest` 둘 · `HttpFlowTest` 전부 · `verify.sh --full`. **부순 증거**: 의존을 빼면 첫 테스트가 빨갛다. **안 되면**: `SpringSessionBackedSessionRegistry` 가 principal 을 못 찾으면 인덱스 키가 `ShopUser.getUsername()` 이다 — `PRINCIPAL_NAME_INDEX_NAME` 을 본다. **완료**(2026-09-15). `build.gradle.kts`·`ShopUserDetailsService`·`SecurityConfig`·`WithdrawalService`·`application.yml`·`SessionStoreTest` 신설·`AuthLoginTest` 고침·`stack.md`·`security-baseline.md`·`real-service-comparison.md`·`docker-compose.yml`·`quality-gates.md`. **(3)이 뒤집혔다.** 행은 `SessionRegistry` 를 Spring Session 판으로 갈고 탈퇴가 그 인터페이스를 계속 쓰게 했는데, **탈퇴가 `getAllPrincipals()` 를 쓰고 있었고 그 구현은 그것을 지원 안 한다**(`UnsupportedOperationException`). 사용자가 후보 ②(탈퇴가 저장소를 직접 부른다)를 골랐다 — 열쇠가 이메일인 것이 코드에 드러나고, **만료 표시가 아니라 삭제**라 탈퇴한 사람의 이메일이 Redis 에 30분 더 남지 않는다. **실측으로 넷을 밟았다**(전부 `stack.md` 에): 좌표가 `org.springframework.boot:spring-boot-session-data-redis` 여야 자동설정이 온다 · 속성이 `spring.session.data.redis.*` 다 · `repository-type: indexed` 가 없으면 색인이 없다 · 쿠키 설정이 **내장 서버가 있을 때만** 걸려서 MockMvc 는 기본 이름 `SESSION` 을 본다. **넷째가 보안 회귀로 보였는데 아니었다** — 실물 쿠키는 `SHOPSESSION`·`HttpOnly`·`SameSite=Lax` 그대로고, 그 단언을 HTTP 층에 뒀다. **`AuthLoginTest` 다섯이 깨져서 고쳤다** — 서블릿 세션 객체를 직접 읽던 것을 쿠키와 저장소로 바꿨다(쿠키 값이 Base64 라 조회 전에 푼다). **부순 증거 셋**: 탈퇴의 삭제를 빈 목록으로 바꾸니 빨강 · `repository-type` 을 `default` 로 되돌리니 셋 다 빨강 · 쿠키 단언을 세션 안 만드는 경로로 재니 빨강. **남긴 것**: `SessionRegistry`·`ConcurrentSessionFilter`·`RegisterSessionAuthenticationStrategy` 셋이 쓰는 곳 없이 남았다 → `Q63` | 없음 · `Q39` 앞 |
| Q53 | ~~지표 노출 — 목표 수치 없이 잰다~~ | **지표가 0 이다**(2026-09-14 설계 점검). `document-map.md` 「확장 청크가 기다리는 문서」가 `62`~`63` 을 `D16, D21` 로 잠가 뒀는데 **노출에 목표 수치는 필요 없다** — `ADR 0011` 이 `42` 에 한 것과 같은 판단이라 그 행에서 `D21` 을 뺀다. `PLAN.md` `62` 행의 선행은 이미 `D16` 뿐이다(독립 리뷰가 확인) — 거기엔 이 행을 참조로만 적는다. **절차**: (1) `io.micrometer:micrometer-registry-prometheus` 의존 (2) `management.endpoints.web.exposure.include: health,prometheus`. `/actuator/prometheus` 는 **인증 필요로 둔다** — `SecurityConfig.PUBLIC_PATHS` 에 안 넣는다. `Q39` 가 backend 에 공개 도메인을 안 만들지만 경로 하나로 열리는 것은 그 결정과 별개다. 스크레이퍼는 없다 — 지금 값은 「잴 수 있다」까지 (3) 지표 셋 — `BatchRuns.record` 에 `Counter shop.batch.run{name,status}` · `PermissionEvaluator.decide` 에 `Timer shop.permission.decide{resource,action,allowed}`(카디널리티 = 자원×동작×2 라 안전) · `RefundSweeper` 에 `Gauge shop.refund.overdue`(기한 지난 미지급 환불 수 — 시행령 제21조의3 지연배상 대상). `MeterRegistry` 주입 (4) `observability-rules.md` 「지표」 절 신설: 이름 규약 `shop.<자원>.<무엇>`, 태그는 닫힌 목록만, 개인정보 태그 금지(`D16`). 「지금 안 하는 것」의 지표 행 삭제 (5) 테스트 `MetricsExposureTest`(`HttpTestBase`): 익명 → 401 · 로그인 → 200 에 `shop_batch_run_total` 이 있다(배치를 한 번 돌린 뒤). **축**: 표준(Prometheus 노출 형식 · Micrometer 이름 규약) + 규약(`D16`). **강제 지점**: 테스트(4위). **건드리는 자리**: 의존·`application.yml`·클래스 셋·문서 하나·테스트 신설·`PLAN.md`(`62` 행 선행). **닫힘**: `MetricsExposureTest` 둘 · `verify.sh --full`. **안 되면**: Gauge 가 요청마다 쿼리를 치면 안 된다 — 스위퍼 회차에서 `AtomicLong` 을 갱신하고 Gauge 는 그 값을 읽는다 **완료**(2026-09-16). `micrometer-registry-prometheus` 의존 · `application.yml` · `V71__metric_permission.sql` 신설 · `auth/MetricsAccessManager` 신설 · `SecurityConfig` · `BatchRuns`(Counter)·`PermissionEvaluator`(Timer)·`RefundSweeper`(Gauge) · `ArchitectureTest` · `observability-rules.md` 「지표」 절 신설 · `MetricsExposureTest` 신설. **사용자 결정으로 「인증 필요」를 관리자만으로 좁혔다** — 지표에 업무 규모가 드러나서 로그인만으로 열면 손님이 읽는다. `metric:read` 권한을 세우고 `V71` 이 관리자에게만 붙였고, 테스트가 **익명 401 · 손님 403 · 관리자 200** 셋을 갈라서 잰다. **실측 넷**: ① **Boot 4 는 내보내기가 기본 꺼짐이라** 노출 목록만으로는 404 였다(`management.prometheus.metrics.export.enabled` 로 레지스트리 하나만 켰다) ② `support` 규칙이 `io.micrometer` 를 막아 허용 목록에 더했다 — 자원이 아니라 라이브러리다 ③ `Q58` 이 옮긴 `IdempotencyService` 의 SpotBugs 제외가 옛 패키지를 가리켜 `--full` 이 빨갰다 ④ HTTP 층은 CSRF 토큰을 먼저 받아야 가입이 된다. **검증**: `verify.sh --full` 초록 · 빈 DB 기동 `applied_migrations` 71 · 실물에서 익명 401, 시드 관리자 200 에 지표 셋이 실렸다 | 없음 |
| Q54 | ~~환불 계산을 순수 함수로 — 빠른 레인으로 내린다~~ | **`RefundService` 893줄 안에 순수 계산이 `private static` 으로 있다**(2026-09-14 설계 점검): `resolvePortions(List<Item>, List<Line>)`(506행) · `commissionRefund(Item, int)`(553행) · `delayInterest(long, OffsetDateTime, OffsetDateTime)`(750행, 이미 package-private static). `shippingFeeRefund`(570행)는 `jdbc` 를 한 번 타서 순수가 아니다 — 안 옮긴다. 테스트 116 중 84 가 DB 를 타고 느린 레인 중앙값 81초 — **이 계산의 테스트가 그 안에 있다.** 청크마다 내는 세금이다. **절차**: (1) `payment/RefundMath.java` 신설(`final`, package-private, 생성자 닫음). 셋을 옮기고 `Item`·`Line`·`Portion` record 도 같이 옮긴다. `RefundService` 는 `static import` 로 그대로 쓴다 (2) `RefundMathTest`(빠른 레인) — `수량_일부_환불은_절사_잔액을_마지막_환불에_몰아준다`(`money-invariants.md` 「수량 일부를 환불할 때 절사 잔액」의 예 그대로) · `수수료_환급은_항목별_절사와_같다` · `지연이자는_기한_다음날부터_연15퍼센트를_일할한다` — `DelayInterestTest` 가 통합 레인에 있으면 그것을 여기로 내리고 통합 쪽은 지운다 (3) `RefundServiceTest` 에서 계산만 보던 테스트가 있으면 지운다(중복) (4) `testing-strategy.md` 「새 로직을 짤 때」와 `coding-rules.md` 「테스트」에 한 줄: 계산은 `*Math` 로 빼서 빠른 레인에 둔다. **축**: 규약(`D15` · `D23`). **강제 지점**: 테스트(4위). **건드리는 자리**: 신설 둘, `RefundService`, 문서 둘. **닫힘**: `RefundMathTest` 셋 초록 · `verify.sh --full`(전체 수는 안 줄어든다 — 옮긴 것이지 지운 것이 아니다). **안 되면**: `Item` 이 `RefundQuery` 응답과 얽혀 있으면 record 는 두고 메서드만 옮긴다. **완료**(2026-09-16). `payment/RefundMath.java` 신설(`final`·package-private·생성자 닫음) · `RefundMathTest` 신설(빠른 레인 7) · `RefundService`(계산 셋과 `Item`·`Portion` 제거, `static import`) · `DelayInterestTest`·`RefundSweeper`(호출처) · `testing-strategy.md`·`coding-rules.md`. **분할표가 둘을 헛짚었다.** ① **`DelayInterestTest` 는 이미 빠른 레인이었다** — `@Tag("db")` 가 없어서 `test` 가 돌리고 있었다. 내릴 것이 없어 호출처만 고쳤고, 그래서 「지연이자」 케이스를 `RefundMathTest` 에 다시 안 썼다(중복 금지) ② **`Line` 은 못 옮긴다** — `RequestCommand` 가 쓰는 공개 요청 계약이라 옮기면 OpenAPI 스키마 이름이 바뀐다. 「안 되면」 조항을 `Item` 이 아니라 `Line` 에 적용했다. **`RefundServiceTest` 에서 지운 것은 없다** — 「수수료 절사 잔액」 테스트는 요청 세 번의 누계와 트리거를 밟는 흐름 검증이라 계산만 보는 것이 아니다. **닫힘 확인**: `RefundMathTest` 7 · `DelayInterestTest` · `verify.sh --full` 초록 | 없음 |
| Q55 | ~~README 진입점 — 3분 안에 무엇을 증명하나~~ | **루트 `README.md` 가 실행법뿐이다**(2026-09-14 설계 점검). 구조 개요도 「이 저장소가 증명하는 것」도 없다 — 포트폴리오로 낼 때 읽는 사람이 3분 안에 못 잡는다. **절차**: (1) 맨 위에 「무엇을 증명하나」 세 줄 — ① 전자상거래법 요건 40개(`43a-28` 뒤 41)를 번호로 달아 제약·테스트로 내리고 커버리지를 스크립트가 잰다(`scripts/req-coverage.sh`, `commerce-compliance.md`) ② 강제 지점 사다리(타입→제약→검증→테스트→문서)를 문서로 정하고 `ArchitectureTest`·`SqlTextTest`·`EnumConstraintTest` 가 지킨다(`coding-rules.md` 「규칙의 우선순위」) ③ 멀티 셀러 주문 두 층(`shop_order`/`seller_order`)과 RBAC + 리소스 스코프(`permission-rules.md`) (2) 구조 그림 하나 — 텍스트 박스로: Next(3000, rewrite) → Boot(8080) → Postgres·Redis. 패키지는 자원 단위 18개. mermaid 안 쓴다 (3) 「읽는 순서」 표: `PLAN.md`(무엇을) · `doc/reference/document-map.md`(기준 문서 지도) · `coding-rules.md` 「규칙의 우선순위」(왜) · `quality-gates.md`(게이트 33) (4) 스크린샷은 안 넣는다 — `Q39` 뒤 실제 주소로 대신한다. 링크 자리만 (5) 기존 「DB 띄우기」 이하는 그대로 아래로. **축**: 규약(`D1` 문서 트리 · `CLAUDE.md` 「글 작성 규칙」). **강제 지점**: 없다 — `doc-lint.sh` 가 존댓말만 본다. 읽는 글이라 기계로 못 잰다. **건드리는 자리**: `README.md` 하나. **닫힘**: `doc-lint.sh` 통과 · 사람이 읽고 세 줄에서 셋을 알아본다. **완료**(2026-09-16). `README.md` 하나. 「무엇을 증명하나」 셋 · 구조 그림 · 「읽는 순서」 표를 위에 얹고 실행법은 그대로 아래로. **사용자 결정: 셀 수 있는 수치만 박는다** — 요건 **41**(`grep`, `43a-28` 이 `R41` 을 더한 뒤)·패키지 **18**(폴더)·기준 문서 **28**(`ls`)만 쓰고 **게이트 수는 안 썼다.** 분할표가 적은 「게이트 33」은 `점검 K`(2026-09-12)가 센 값인데 **오늘 표는 행이 쉰을 넘는다** — `Q50`·`Q51`·`Q57` 이 더했다. 틀린 것이 아니라 **늙은 것**이고, 그래서 늙는 수치를 맨 위에 안 박고 문서로 링크했다(`Q64` 가 볼 대상이 하나 준다). **박은 셋은 그 자리에서 다시 세어 확인했다.** 「`order` 에 `deleted_at` 이 없다」도 실제 표 이름 `shop_order` 로 고쳤다 | 없음 |
| Q56 | ~~쓰기 입구는 판정을 지나거나 소유 목록에 있다 — ArchUnit~~ | **`permission-rules.md` 「판정을 안 지나는 입구」가 규칙이다**(2026-09-14 설계 세션, 사용자 결정 (가)). 결제·주문 생성·장바구니 셋은 SQL 소유 조건, 나머지는 `PermissionEvaluator`. 그전에는 규칙이 없어서 `ArchitectureTest` 가 「판정을 서비스에서 부르나 — 사람이 든다」고 적어 뒀다. **절차**: (1) `ArchitectureTest` 에 규칙 `쓰기_입구는_판정을_지나거나_소유_목록에_있다` — 대상은 `*Controller` 의 public 메서드 중 `@PostMapping`·`@PutMapping`·`@PatchMapping`·`@DeleteMapping` 이 붙은 것. 조건은 둘 중 하나: ① 호출 사슬(`걷는다` 헬퍼 재사용 — 저장소 밖에서 멈추고 방문 집합으로 순환을 끊는다)에 `PermissionEvaluator.decide` 또는 `allowedActions` 호출이 있다 ② 선언 클래스가 허용 목록에 있다 — `Set.of("PaymentController", "OrderController", "CartController", "AuthController")`. **목록에 넣기 전에 사슬을 실제로 걷어 `decide` 가 있으면 뺀다** — `MeController` 는 `AccountService`·`ConsentService` 가 `decide` 를 부르니 목록에 없어야 한다. `AuthController` 는 판정 대상이 없다(로그인·가입·로그아웃). 항목마다 이유를 javadoc 에 적고 문서 표와 같은지 사람이 대조한다 (2) 위반 메시지는 사슬 전체를 든다(`Q32` 와 같은 모양) (3) `quality-gates.md` 행. **부순 증거**: 허용 목록에서 `PaymentController` 를 빼면 그 컨트롤러 셋(`pay`·환불 입구)이 빨갛다 · `OrderActionService` 안의 `decide` 호출 하나를 주석 처리하면 `ShipmentController`·`ReturnController` 가 빨갛다. **축**: 규약(`D6` · `D14`). **강제 지점**: 테스트(4위) — 문서(5위)에서 내린다. **건드리는 자리**: `ArchitectureTest`·`quality-gates.md`. **닫힘**: 새 규칙 초록 · `verify.sh`. **안 되면**: `OrderActionService` 처럼 판정을 안에서 부르는 서비스가 사슬 깊이 셋을 넘으면 `걷는다` 의 깊이 상한을 본다. **완료**(2026-09-15). `ArchitectureTest`(규칙 둘 신설) · `permission-rules.md` · `quality-gates.md`. **목록 단위가 클래스에서 메서드로 바뀌었다**(사용자 선택 ②). 행은 컨트롤러 넷을 통째로 열고 「`MeController` 는 판정을 부르니 목록에 없어야 한다」고 적었는데, **세어 보니 그 컨트롤러의 쓰기 입구가 일곱이고 넷만 판정을 지난다** — 탈퇴·이메일 변경·이메일 확인·비밀번호 변경은 아무것도 안 지난다. 클래스로 열면 그 넷이 열린 채로 묻히고 **거기 새 입구가 생길 때 판정을 빠뜨려도 안 걸린다** — 이 규칙이 막으려던 바로 그 일이다. **짝 규칙 `목록의_입구가_실재한다` 를 같이 세웠다**: 목록에 적혔는데 없는 메서드를 찾는다. 이름이 바뀐 줄은 아무도 안 막는 죽은 줄이고 그것이 다음 예외를 몰래 들여보낸다. **세운 날 바로 값을 했다** — 분할표가 적어 둔 이름 다섯이 실물과 달랐다(`logIn`·`logOut`·`signUp`·`changeQuantity` 와 없는 `CartController.clear`·`update`). **문서 표가 셋이었는데 실물은 다섯 자리였다**: 본인 계정 넷과 인증 이전 다섯이 빠져 있었다. 규칙을 돌리기 전에는 몰랐다. **부순 증거 둘**: 목록에서 `PaymentController.pay` 를 빼니 그 입구가 빨강 · `OrderActionService` 의 판정 호출 둘을 걷으니 **배송·반품 입구 여덟이 빨강**(둘에 여덟이 매달려 있다) | 없음 |
| Q57 | ~~아웃박스 표 + 원천 트리거 — `32` 를 대신한다~~ | **`event-catalog.md`(`D12`) 가 모양을 다 정했다**(2026-09-14 설계 세션, 사용자 결정 (다): 표준 아웃박스 표를 **앱이 아니라 원천 표의 트리거가 채운다**). 표 정의·`type` 일곱·봉투·페이로드 규칙은 그 문서를 그대로 옮긴다 — 여기서 다시 정하지 않는다. **절차**: (1) `V70__outbox_event.sql` — 카탈로그의 `create table` 그대로 + 부분 인덱스 + **앱 직접 `insert` 방벽**: `before insert` 트리거가 `current_setting('shop.outbox_write', true) <> '1'` 이면 거부(`sku_stock_requires_move` 와 같은 꼴) (2) 원천 트리거 — `order_status_history` `after insert`(`order_id` 유무로 `type` 둘을 가른다. `data` 는 `jsonb_build_object('order_number', (select order_number from shop_order where order_id = new.order_id), 'from_status', new.from_status, 'to_status', new.to_status, 'actor_type', new.actor_type)` 꼴, 셀러 층은 `seller_order_number`·`order_number` 둘 다) · `sku_stock_movement` `after insert` · `batch_run` `after insert` · `refund`·`return_request` `after update of status for each row when (old.status is distinct from new.status)` · `settlement` `after update of payout_status … when (old.payout_status is distinct from new.payout_status)`. 트리거 함수마다 `perform set_config('shop.outbox_write', '1', true)` 로 방벽을 열고 넣고 `'0'` 으로 닫는다 (3) `event/EventType.java` 열거형 일곱(`code()` 는 `type` 문자열) + `EnumConstraintTest.PAIRS` 에 `outbox_event_type_check` 행 — 패키지 `event/` 는 자원이 아니라 「자원이 아닌데 패키지를 파는 경우」 셋을 본다: 자원 아님 ✓ · `support` 에 넣으면 순환인가 — 아니다, 열거형 하나라 순환이 안 난다 → **`support/EventType` 로 둔다**, 패키지를 안 판다 (4) `data-lifecycle.md` 「자원별 정책」 행: `outbox_event` — 발행 후 **7일** 물리 삭제, 미발행은 안 지움, 근거 `D12`. 파기 코드는 `33` 이 발행기와 같이 세운다 — `DataLifecycleCoverageTest` 는 표 목록만 보니 문서 행으로 통과한다 (5) `domain-model.md` 「애그리거트 밖」 표에 `outbox_event` 행 — **`Q48` 의 테스트가 요구한다** (6) `state-machines.md` 「상태 이력을 남긴다」에 한 줄: 이력 행이 `outbox_event` 행을 낳는다 (7) `OutboxEventSchemaTest`(`PostgresTestBase`) — `주문_전이_하나가_아웃박스_행_하나를_낳는다`(`type`·`subject`·`data` 키 넷 검증) · `롤백되면_아웃박스_행도_사라진다` · `앱이_직접_넣으면_거부된다`(`set_config` 없이 insert → 예외) · `같은_상태로_다시_쓰면_사건이_아니다`(`refund.status` 를 같은 값으로 update → 0행). **축**: 표준(CloudEvents 1.0 속성) · 규약(`D12`·`D11`·`D22`). **강제 지점**: DB 제약·트리거(2위) — 앱이 빠뜨릴 수 없다. **건드리는 자리**: `V70` 신설 · `support/EventType` 신설 · `EnumConstraintTest`·문서 셋 · 테스트 신설. **닫힘**: `OutboxEventSchemaTest` 넷 · `EnumConstraintTest` · `verify.sh --full` · 빈 DB 기동 `applied_migrations` 70. **부순 증거**: 원천 트리거 하나를 빼면 첫 테스트가 빨갛다 · 방벽 트리거를 빼면 셋째가 빨갛다. **안 되면**: `after update` 트리거는 `set_updated_at`(`before`) 뒤라 순서가 안 부딪친다. 지연 트리거로 만들지 않는다 — 즉시라야 같은 트랜잭션의 다음 문장이 행을 본다. **완료**(2026-09-15). `V70__outbox_event.sql` 신설 · `support/EventType` 신설 · `OutboxEventSchemaTest` 신설 · `EnumConstraintTest` · `ConstraintValues` · 문서 넷(`D13`·`D4`·`D7`·`D25`). 카탈로그가 정한 모양을 그대로 옮겼다 — 표·`type` 일곱·봉투·트리거 여섯. **공통 함수 `emit_outbox_event` 를 하나 뒀다**: 여섯 함수가 각자 `set_config` 를 열고 닫으면 하나가 닫는 것을 빠뜨리는 날이 온다. **실측이 셋을 냈다.** ① `ConstraintValues.valuesIn` 이 **점을 모른다** — 값을 `[a-z0-9_]+` 로 걸러서 `shop.order.status_changed` 가 통째로 빠졌고 대조가 「DB 쪽에 값이 없다」로 빨갰다. 이 저장소의 열거값이 그동안 전부 한 낱말이라 안 드러난 자리다. 점을 받게 넓혔다 ② **분할표의 닫힘 조건이 틀렸다**: 「빈 DB 기동 `applied_migrations` 70」인데 그 칸은 **파일 수**지 버전 번호가 아니다. 파일이 67개고 최대 버전이 70이라 실제 값은 67이다. `flyway_schema_history` 로 `V70` 이 마지막으로 적용된 것과 트리거 여덟이 실물 DB 에 선 것을 직접 확인했다 ③ **테스트가 따로 돌면 초록인데 전체에서 빨갰다** — `BatchLockTest` 가 롤백을 끄고 돌아서(`NOT_SUPPORTED`) 그쪽 회차 사건이 **커밋된 채로 남고**, 표 전체를 세던 단언이 그 행까지 셌다. `subject` 로 좁혔다. **아웃박스가 선 뒤로는 롤백 안 하는 테스트가 전부 이 표에 흔적을 남긴다** — 표 전체를 세는 단언을 앞으로도 안 쓴다. **부순 증거 둘**: 원천 트리거(주문 이력)를 빼니 **둘이 빨강** · 방벽 트리거를 빼니 앱 직접 삽입이 통과해 빨강. **8080 을 남의 프로세스(JDK 17)가 쥐고 있어서 기동 확인은 8081 로 했다** — 우리 것이 아니라 안 죽였다 | D12 · Q48 |
| Q58 | ~~`payment → order` 구체 호출을 끊는다 — 허용 순환 셋을 둘로~~ | **`order ↔ payment` 가 허용 순환 셋 중 모듈을 쪼갤 때 실제로 막는 것이다**(2026-09-14 설계 점검): `PaymentService` 가 `OrderStatusService.markPaid`·`markPaymentFailed` 와 `IdempotencyService`(order 패키지)를 부르고, `OrderQuery` 가 `payment` 열거형 넷을 쓴다. **같은 저장소에 답이 있다** — `auth/PasswordResetMailer`(인터페이스) ← `notification/PasswordResetMail`(구현). **결정(설계 세션)**: 이벤트가 아니라 인터페이스로 끊는다 — 결제 결과가 주문 상태를 **동기로** 옮겨야 재생 응답이 맞다(`D11`: 재전송 시점에 주문이 이미 결제완료여야 한다). 이벤트면 결제 응답 시점에 주문이 아직 `payment_pending` 이다. **절차**: (1) `payment/PaymentOutcome.java` 인터페이스 — `void paid(long orderId, String reason)` · `void failed(long orderId, String reason)` (2) `order/PaymentOutcomeHandler.java` `@Component` 가 구현하고 안에서 `OrderStatusService.markPaid`·`markPaymentFailed` 를 부른다 (3) `PaymentService` 는 `PaymentOutcome` 만 주입. `OrderStatusService` import 를 지운다 (4) **`IdempotencyService` 를 `support/` 로 옮긴다** — 자원을 모른다(`user_id`·키·JSON 뿐). `ArchitectureTest.공용_도구는_자원을_모른다` 가 허용하는 의존(`error/`·Spring·Jackson)만 쓰는지 본다. `OrderController`·`PaymentService`·`RefundService` 의 import 를 고친다 (5) `ArchitectureTest.자원_패키지는_순환하지_않는다` 에서 `order ↔ payment` `ignoreDependency` 두 줄을 지운다 — 규칙이 초록이어야 한다. javadoc 표에서 그 행을 지우고 「걸친 것을 공용 인터페이스로 빼는 것이 답」의 첫 실례로 적는다 (6) `coding-rules.md` 「의존」 표에서 `order ↔ payment` 행을 지우고 어떻게 풀었나를 한 줄로. **축**: 규약(`D23` 의존 · `D11`). **강제 지점**: 테스트(4위) — 순환 규칙이 예외 없이 잰다. **건드리는 자리**: 신설 둘 · `PaymentService`·`IdempotencyService`(이동)·호출자 셋 · `ArchitectureTest`·`coding-rules.md`. **닫힘**: `ArchitectureTest` 초록(예외 두 줄이 없는 상태로) · `PaymentServiceTest`·`OrderIdempotencyTest` 전부 · `verify.sh --full`. **부순 증거**: `PaymentService` 에 `OrderStatusService` import 를 되돌리면 순환 규칙이 빨갛다. **안 되면**: `OrderQuery` 가 `payment` 열거형을 쓰는 방향은 남는다 — 그건 순환이 아니다. 열거형을 `support` 로 옮기고 싶어지면 멈춘다(`stack.md` 「값 목록을 support 로 옮기는 기준」). **완료**(2026-09-16). `payment/PaymentOutcome` 신설 · `order/PaymentOutcomeHandler` 신설 · `PaymentService` · `IdempotencyService` 이동(`order` → `support`, 테스트도 같이) · `OrderController`·`OrderService`(javadoc 링크) · `ArchitectureTest` · `coding-rules.md` 「의존」. **`ignoreDependency` 두 줄을 지웠고 규칙이 초록이다** — 허용 순환이 셋에서 둘로 줄었다. **분할표가 하나를 헛짚었다**: `RefundService` 의 import 를 고친다고 적혀 있는데 그 파일은 `IdempotencyService` 를 안 쓴다 — `payment → order` 의존은 `PaymentService` 한 파일의 둘이 전부였다. **부순 증거**: `PaymentService` 에 `OrderStatusService` 필드를 되돌리니 `자원_패키지는_순환하지_않는다` 가 `FAILED`, 지우니 다시 초록. **`IdempotencyService` 는 `support` 규칙을 그대로 통과한다** — 쓰는 것이 `error`·Spring·Jackson·java 뿐이다. **Docker 데몬이 안 떠 있어서 느린 레인이 전부 FAILED 로 한 번 죽었다**(`DockerClientProviderStrategy`) — 띄우고 다시 돌려 `PaymentServiceTest`·`OrderIdempotencyTest`·`IdempotencyServiceTest` 초록 | 없음 |
| Q59 | ~~감사자 deny 를 트리거가 만든다 — `permission.kind` · `role.read_only`~~ | **`permission-rules.md` 「알려진 구멍 1·2」가 결정을 다 적었다**(2026-09-14 설계 세션, 사용자 결정 (가)). 읽기·쓰기를 데이터로 가르고 deny 행을 트리거가 만든다. 엔진·축은 안 바뀐다. **절차**: (1) `V75__permission_kind.sql` — **`V72` → `V74` → 여기로 두 번 옮겼다**(`점검 O` 가 `V72` 를 풀었고, `Q73` 이 `V74` 를 쓰면서 한 칸 밀렸다. `Q69` 의 테스트가 그것을 시켰다). `V73` 이 이미 적용돼 있고 Flyway 는 `out-of-order` 가 꺼져 있어서(설정 0건) 빈 번호를 뒤늦게 채우면 **이미 `V73` 을 받은 DB 가 기동에서 죽는다** — 재사용 Testcontainer(`withReuse(true)`)와 로컬 compose 가 그 상태다. `Q62` 가 같은 `V72` 를 예약하고 있던 것도 같이 풀었다 — `alter table permission add column kind text` → 백필 `update permission set kind = case when action = 'read' then 'read' else 'write' end` → **먼저 `select resource, action from permission order by 1, 2` 를 떠서 백필 결과를 눈으로 대조하고 그 표를 마이그레이션 주석에 붙인다**(이름이 `read` 가 아닌 조회가 있으면 손으로 `read` 로) → `not null` + `check (kind in ('read', 'write'))` · `alter table role add column read_only boolean not null default false` → `update role set read_only = true where code = 'auditor'` (2) 트리거 둘 — `permission` `after insert` : `new.kind = 'write'` 면 `insert into role_permission (role_id, permission_id, scope, effect) select r.role_id, new.permission_id, 'all', 'deny' from role r where r.read_only on conflict do nothing` · `role` `after insert or update of read_only` : `new.read_only` 면 기존 `write` 권한 전부에 같은 행. **`kind` 는 못 고친다** — `before update of kind` 트리거가 거부(고치면 이미 만든 deny 가 남거나 빠진다) (3) `auth/PermissionKind.java` 열거형(`READ`·`WRITE`) + `EnumConstraintTest.PAIRS` 에 `permission_kind_check` 행(`D23` 「열거값을 어디에 두나」) (4) **`V5` 의 손 deny 는 그대로 둔다** — 트리거가 `on conflict do nothing` 으로 지나간다. `V12`~`V64` 의 손 deny 도 그대로. 지우면 이력이 거짓이 된다 (5) 테스트 — `PermissionEvaluatorTest.KnownHole` 을 **뒤집는다**: 이름 `나중에_추가된_쓰기_권한도_감사자를_막는다`, `insert into permission (..., kind) values (..., 'write')` 뒤 `decision.allowed()` 가 `false` · 새로 `나중에_추가된_읽기_권한은_감사자가_지난다`(`kind='read'` → `true`) · `PermissionSchemaTest.kind_를_안_주면_거부된다`(not null) · `PermissionSchemaTest.kind_는_못_고친다`(update → 예외) · `PermissionSchemaTest.역할을_read_only_로_바꾸면_기존_쓰기_권한이_전부_막힌다` (6) `PermissionMatrixTest`(4c) 스냅샷 — deny 행이 늘었으면 표를 갱신하고 **늘어난 행이 전부 write 인지** 눈으로 대조해 이력에 적는다. 하나도 안 늘면 아홉 청크가 빠뜨림 없이 손으로 넣었다는 뜻이고 그것도 적는다 (7) `permission-rules.md` 「알려진 구멍 1·2」를 닫힘으로 · 「지금 데이터 — 역할 × 권한 매트릭스」에 `kind` 열 · 「이 문서를 고칠 때」에 「새 권한은 `kind` 를 정한다」 (8) `naming-rules.md` 에 `kind` 컬럼 이름이 규약에 맞나 확인(`notification.kind` 가 선례다). **축**: 규약(`D6` · `D23` 축 2 — 사람 5위를 제약 2위로). **강제 지점**: DB 트리거(2위) + `not null`. 테스트(4위)가 고정. **건드리는 자리**: `V72` 신설 · `PermissionKind` 신설 · `EnumConstraintTest` · `PermissionEvaluatorTest` · `PermissionSchemaTest`(신설) · `PermissionMatrixTest` 스냅샷 · 문서 둘. **닫힘**: 위 테스트 여섯 · `EnumConstraintTest` · `verify.sh --full` · 빈 DB 기동 `applied_migrations` 71. **부순 증거**: `after insert` 트리거를 빼면 첫 테스트가 빨갛다(감사자가 다시 통과한다). **안 되면**: `on conflict` 가 `role_permission` 의 PK(`role_id, permission_id, effect`)를 못 찾으면 그 제약 이름을 `\d role_permission` 으로 보고 `on conflict on constraint` 로. **완료**(2026-09-17). `V75__permission_kind.sql`·`auth/PermissionKind`·`PermissionSchemaTest`(신설) · `PermissionEvaluatorTest`(`KnownHole` → `ReadOnlyRoles`) · `EnumConstraintTest` · `permission-rules.md`·`quality-gates.md`. **번호가 `V72`→`V74`→`V75` 로 두 번 밀렸다** — `점검 O` 가 겹침을 풀고 `Q73` 이 `V74` 를 쓰면서 `Q69` 의 테스트가 시켰다. **백필을 눈으로 대조했다**(절차 (1)): 서른 중 조회가 여덟이고 **전부 이름이 정확히 `read`** 라 손으로 고칠 행이 0이었다 — 그 표를 마이그레이션 주석에 붙였다. **실측이 규율을 확인했다**: 트리거가 만들 거부와 손으로 넣어 둔 거부가 **둘 다 22행**이고 `PermissionMatrixTest` 스냅샷이 한 줄도 안 바뀌었다. 아홉 청크가 하나도 안 빠뜨렸다는 증거고, 그것이 **사람이 지킨 것**이었다는 뜻이다. **`on conflict` 는 PK 로 그냥 지나갔다** — 「안 되면」의 우회가 필요 없었다. **부순 증거**: `after insert` 트리거를 빼니 「나중에 추가된 쓰기 권한에도 막힌다」가 빨갛다 | 완료 | 없음 |
| Q60 | ~~테스트가 안 부르는 R 12 를 가르고 `req-coverage.sh` 를 래칫으로~~ | **2026-09-14 설계 세션이 열둘을 갈랐다** — 라벨만 빠진 것 여섯(R2·R10·R12·R26·R31·R35 — 실물도 테스트도 있는데 테스트가 R 번호를 안 적었다) · 행이 낡은 것 셋(R20·R23·R30 — 「통지 인프라가 없다(54a~56)」고 적혀 있는데 `54a`·`56` 은 닫혔다) · 미착수·조건 넷(R13·R27·R29·R33 — 테스트가 없는 것이 맞다). **사용자 결정 둘**: 스크립트가 미착수·조건을 분모에서 빼고 남은 0건 수를 래칫으로(가) · R30 은 `D14` 「사고 대응」 절(설계 세션이 썼다)로 답한다(가). **절차**: (1) **라벨 여섯** — 기존 테스트의 `@DisplayName` 또는 javadoc 에 R 번호를 적는다(`grep -lw` 라 글자만 있으면 센다): R2 → `PolicyQueryTest`(약관 제2조 중개자 고지)와 프론트 `BrokerageNotice` 를 그리는 화면 테스트(없으면 `site-header.test.tsx` 꼴로 `site-footer.test.tsx` 를 신설 — 문안이 두 벌이라 한 벌만 잰다고 적는다) · R10 → `SecurityConfigTest`(bcrypt) · R12 → `RetainedColumnsTest`(`price_incl_vat` 컬럼 이름) · R26 → `/terms` 페이지 테스트(없으면 `policy-document` 컴포넌트 테스트에) · R31 → `ship-delay.test.tsx`(자동 구매확정 문구)와 `OrderStatusBatchTest.confirmDeliveredOrders` · R35 → `NotificationSweeperTest`(order_placed·payment_completed) (2) **낡은 행 셋** — `commerce-compliance.md` R20 「전부 미착수」→ 실물(`NotificationSweeper` 네 시점·`V43`·`V44`)로, R23 의 「1호 나·2호 나 미착수」→ 닫힘으로, R30 → 「절차 — `D14` 「사고 대응」. 강제 지점 없음(문서 5위)」로. 셋에도 테스트 이름을 적는다(R20·R23 → `NotificationSweeperTest`) (3) **`scripts/req-coverage.sh`** — 강제 지점 칸(넷째 칸)이 굵은 글 `**미착수` 또는 `**조건` 으로 **시작하는** R 은 분모에서 빼고(실물이 `**조건.**` 처럼 마침표를 굵은 글 안에 둔다 — 정규식 `**(미착수 **완료**(2026-09-17). `scripts/req-coverage.sh`·`.github/workflows/ci.yml`·`commerce-compliance.md`·`quality-gates.md` · 라벨 여섯(`PolicyQueryTest`·`SecurityConfigTest`·`RetainedColumnsTest`·`NotificationSweeperTest`·`OrderStatusBatchTest`·`ship-delay.test.tsx`) · `site-footer.test.tsx` 신설. **12 → 0 이 됐다**(제외 다섯). **제외가 넷으로 갈렸다** — 설계는 「미착수·조건」 둘을 봤는데 실물은 `R13` 이 `**일부러 안 다룬다`, `R30` 이 `**문서뿐` 이라 **갈래를 둘 더 만들었다**. `R30` 은 `D14` 「사고 대응」이 절차를 들고 강제 지점이 문서(5위)뿐이라 테스트가 없는 것이 맞다. **함정 하나**: `grep -w` 가 `R20·R35` 를 `R20` 으로 안 잡는다 — 가운뎃점이 낱말 경계를 안 만들어서다. 구분자를 띄웠다. **부순 증거**: `RetainedColumnsTest` 의 `R12` 라벨을 떼니 `exit 1` 이고 그 번호를 짚는다|조건)`) 「제외 n」으로 따로 찍는다. 남은 0건 수가 `req_uncovered_baseline`(스크립트 상수, **내리기만**) 을 넘으면 `exit 1`. 라벨 뒤 기준선은 **0** 이어야 한다 — 0 이 아니면 (1)이 덜 된 것이다 (4) `ci.yml` `docs` 잡의 `req-coverage.sh` 단계는 이미 있다 — 이제 실패가 빨강이 된다. `verify.sh` 빠른 단계에도 넣는다 (5) `commerce-compliance.md` 「한눈에 보는 매핑」 머리에 한 줄: 「강제 지점 칸이 굵은 `**미착수`·`**조건` 으로 시작하면 스크립트가 분모에서 뺀다 — 다른 말로 바꾸면 분모에 들어가 빨개진다」 (6) `quality-gates.md` 행 + **부순 증거**: 임시로 요건표에 `R99` 행(강제 지점 칸에 테스트 이름 없이)을 더하면 빨강 · `**미착수**` 를 적으면 통과 (7) `PROGRESS.md` 「현재 상태」의 「테스트가 안 부르는 R」 줄을 「0/40, 래칫」으로. **축**: 법(`D2` 전 요건) + 규약(`D25` 게이트 · `D23` 「위에서 내려가며 확인한다」). **강제 지점**: CI 게이트(4위) — 리포트(5위)에서 올린다. **건드리는 자리**: 테스트 여섯~여덟(라벨) · `req-coverage.sh` · `ci.yml`·`verify.sh` · `commerce-compliance.md` 행 셋 + 머리 한 줄 · `quality-gates.md` · `PROGRESS.md`. **닫힘**: `bash scripts/req-coverage.sh` 가 「0/40(제외 4)」로 0 종료 · 부순 증거 둘의 출력을 `quality-gates.md` 에 · `verify.sh --full`. **안 되면**: 라벨을 붙일 테스트가 정말 없는 R 이 나오면 그 R 은 구멍이다 — 미착수로 위장하지 말고 행을 하나 세운다 | 없음 |
| Q61 | 표준 줄 점검 처분 — 캐시 금지 헤더 테스트 · `Idempotency-Key` 초안 인용 | **점검 N(2026-09-14)이 넘겼다.** (1) **RFC 9111 — 인증된 응답의 캐시 금지가 잰 적이 없다.** `SecurityConfig` 에 `.headers(` 커스텀이 없어 Spring Security 기본값(`Cache-Control: no-cache, no-store, max-age=0, must-revalidate` + `Pragma` + `Expires`)이 붙는다 — **플랫폼 기본값에만 걸려 있다.** 테스트 `ProblemResponseTest` 또는 `HttpFlowTest` 에 하나: 로그인 뒤 `GET /api/me` 응답 헤더에 `no-store` 가 있다 · 공개 `GET /api/products` 에도 같은 헤더가 있나 확인해 **있으면 그대로 적고**(공개 목록까지 no-store 인 것이 맞나는 `D24` 「캐시」와 대조 — 캐시를 켜는 날 여기가 갈린다) · `security-baseline.md` 「노출면」에 한 줄. **부순 증거**: `http.headers(h -> h.cacheControl(c -> c.disable()))` 를 넣으면 빨갛다 (2) **`Idempotency-Key` 초안 인용** — IETF httpapi WG 의 `draft-ietf-httpapi-idempotency-key-header` 를 **실제로 열어**(datatracker) 판·날짜·상태를 `external-references.md` D11 절에 적고, 우리 규약과 대조한다: 헤더 이름 ✓ · 같은 키 다른 본문 → 422(우리 `IdempotencyService`) · 진행 중 → 409 · 완료 뒤 → 재생. 갈리는 항목이 있으면 `concurrency-rules.md` 「멱등키」에 「초안과 다른 것」으로 적는다 — 초안이라 따를 의무는 없고(4순위 관례가 2순위 후보로 오르는 것뿐) 근거만 남긴다. **축**: 표준(RFC 9111 · IETF 초안). **강제 지점**: 테스트(4위) — (1). (2)는 문서. **건드리는 자리**: 테스트 하나 · `security-baseline.md` · `external-references.md` · `concurrency-rules.md`. **닫힘**: 새 테스트 초록 · `verify.sh --full`. **안 되면**: 초안이 만료됐거나 RFC 가 됐으면 그 사실을 적는다 — 이름이 바뀌었으면 우리 헤더 이름을 바꿀지는 `D5` 「버전」이 정한다(계약 변경) | 없음 |
| 2g-6 | ~~리뷰 러너에서 저장소 Stop 훅이 되살아난다 — 삭제 단계가 복원보다 앞이다~~ | **`2g-5` 가 남긴 물음의 답이 나왔다**(2026-09-14, PR #45 두 번째 회차): 워크플로 52행 「저장소 훅을 리뷰에서 뺀다」(`rm -f .claude/settings.json`)가 **액션의 「Restoring .claude … from origin/main (PR head is untrusted)」보다 먼저 돌아서** 훅이 돌아온다(`stack.md` 「액션은 `CLAUDE.md`·`.claude` 를 되살린다」). 그 회차의 결과 문장: 「`git stash` 같은 변경 작업은 승인이 막혀 있어 처리할 수 없다. 이 diff 는 내가 만든 것이 아니고 PR 리뷰 작업과도…」 — 리뷰어가 Stop 훅 메시지를 자기 일로 받아 포기했고 **코멘트 0, `review` 는 초록**(「코멘트를 남겼나」 단계가 15차 회차의 옛 코멘트를 세서 통과했다). **빈 초록이다.** **절차**: (1) 실측 — 그 회차 로그에서 「Restoring .claude」 줄과 52행 단계의 순서를 확인해 `stack.md` 그 절에 한 줄 (2) 고치는 자리는 **액션 안**이다 — 후보 순서: ① `claude_args` 에 `--setting-sources user`(프로젝트·로컬 설정 파일을 아예 안 읽는다 — CLI 가 그 플래그를 지원하나 액션 로그의 `claude --help` 로 확인) ② 액션의 `settings` 입력으로 `"hooks": {}` 를 넘겨 병합이 지우나 실측 ③ 둘 다 안 되면 훅 스크립트 자체에 `[ -n "$GITHUB_ACTIONS" ] && exit 0` 한 줄 — 훅이 러너에서 스스로 빠진다. **③은 마지막이다** — 규칙 파일이 환경을 알게 되는 것이라서 (3) 「코멘트를 남겼나」 단계(157행)가 **이번 회차의** 코멘트를 세게 고친다 — `updated_at` 이 회차 시작 뒤인 것만. 옛 코멘트로 통과하면 이 구멍이 다시 안 보인다 (4) `quality-gates.md` 리뷰 행에 「빈 초록」 사례와 부순 증거: 고친 뒤 임시로 훅을 되살려 빨간 것 (5) PR #45 는 이 청크 뒤 `synchronize` 로 다시 잰다. **축**: 규약(`D25` 게이트 · `2g` 리뷰). **강제 지점**: CI 단계(4위) — (3)이 빈 초록을 빨갛게 만든다. **건드리는 자리**: `.github/workflows/claude-review.yml` · `stack.md` · `quality-gates.md`. **닫힘**: PR #45(또는 다음 PR)의 새 회차가 `updated_at` 이 회차 뒤인 코멘트를 남기고 그 코멘트가 diff 를 실제로 다룬다. **안 되면**: ①②③ 다 안 먹으면 `2g-5` 의 「거부 목록」 판정표로 돌아가 훅 없는 별도 저장소 설정을 쓴다. **쳤다**(2026-09-15). **사용자가 ②를 골랐고 ②가 죽었다** — 로컬에서 세 변형(`{"hooks":{}}`·`{"hooks":{"Stop":[]}}`·`{"hooks":{"Stop":null}}`)을 쟀고 셋 다 프로젝트 Stop 훅이 그대로 돌았다. CLI 의 `--settings` 는 도움말대로 「load **additional** settings」라 더하기만 한다. **CI 회차(5~7분·약 $2)를 안 태우고 갈랐다** — 같은 자리에서 ①(`--setting-sources user`)을 재니 훅이 안 돌았고, 액션 소스의 주석이 그 용도를 그대로 적어 뒀다(「e.g. `--setting-sources user` to avoid in-repo configs」). 행이 적어 둔 대체 순서(①→②→③)대로 ①로 갔다. (1) 실측은 run 34862668434 이 답했다 — 삭제 단계 `15:31:15`, 「Restoring .claude」 `15:31:33` 으로 **18초 차**고 그 회차에서도 Stop 훅이 돌았다. **그래서 삭제 단계를 지웠다** — 안 걸리는 것을 남기면 「막고 있다」고 읽힌다. (3) 확인 단계는 REST 의 `updated_at` 을 쓴다: `gh pr view --json comments` 에는 그 필드가 없고 `--edit-last` 로 고쳐 쓰면 `createdAt` 이 첫 회차에 머문다. **부순 증거**: 고친 필터를 PR #45 의 실물 코멘트(`08:47Z`)에 걸어 기준 `08:00Z` → 1건 초록, `09:00Z` → 0건 빨강. 빈 초록을 낸 회차 둘(`12:09Z`·`15:31Z`)이 이 필터에서 빨갛다. **예보에 없던 것 하나를 흡수했다** — 15:31 회차가 서브에이전트 열을 띄우고 일곱만 받은 채 끝나 39턴에 코멘트 0개였다(`started_in_background: 10`·`completed: 7`·`is_error: false`). 프롬프트에 「코멘트를 달기 전에 끝내지 않는다」를 박았다. **남은 것: PR #45 의 새 회차가 이번 회차 코멘트를 남기는 것** — 이 묶음은 워크플로를 고쳤으니 다음 회차가 건너뛴다(`2g-3`) **닫혔다**(2026-09-15, PR #47). **워크플로를 안 건드린 첫 묶음**이 그 증거다 — 액션이 5분 41초를 실제로 돌고 **이번 회차에 코멘트를 남겼고**, 확인 단계가 옛 코멘트가 아니라 그것을 세고 초록이 됐다. 지적 1(주석 둘이 서로 다른 사실을 말한다)이고 그 자리에서 고쳤다. **17차까지 못 닫은 이유가 구조였다**: 워크플로를 고친 PR 은 액션이 자기 파일을 `main` 과 대조하고 물러난다(`2g-3`) — 고친 것을 `main` 에 올려야 다음 PR 이 그것으로 검토된다 | 없음 |
| 2g-7 | ~~리뷰가 도우미를 기다리다 코멘트 없이 끝난다 — 글로 금지한 것이 두 번째~~ | **`2g-6` 이 닫은 자리에서 같은 것이 다시 났다**(2026-09-17, PR #49 세 번째 회차). run 35077299175 이 청크마다 서브에이전트를 넷 띄우고 `completed: 0` 인 채 「4개 에이전트가 모두 실행 중이다」를 남기고 끝났다 — 코멘트 0개, `is_error: false`, 3분 15초. **워크플로 프롬프트가 그것을 이미 글로 금지하고 있었다**(2026-09-14 run 34862668434 을 겪고 박은 문장). 문서는 5위라 안 막았다. **실측이 선택을 갈랐다**: 초록이었던 회차 넷(`290388`·`392999`·`627093`·`707900` ms)이 **전부 `by_type: {}`** 로 도우미를 하나도 안 띄웠고, 띄운 회차는 죽은 그 하나뿐이다 — 막아도 잃는 것이 없다. **사용자가 (가+) 를 골랐다**(도구를 걷고 상한도 올린다). **완료**(2026-09-17). `claude_args` 에 `--disallowedTools "Task,Agent"` · `timeout-minutes` 20 → 30 · 프롬프트의 금지 문장을 「혼자 본다」로 · `stack.md` 두 절 · `quality-gates.md` 「안 걸린 게이트」 표에 여덟째 행. **축**: 규약(`D23` 축 2 — 문서 5위를 설정으로). **강제 지점**: 실행 설정. 도구가 없어 실수가 성립하지 않는다. **부순 증거**: 로컬에서 같은 플래그로 「서브에이전트를 띄워라」를 시키면 세션에 `Agent` 가 **0회** 나온다(플래그 없이는 1회) — 거부가 아니라 도구가 없다. **이름 둘을 적은 이유**: 지금 CLI 는 `Agent` 고 `Task` 는 옛 이름인데 액션이 버는 CLI 판을 여기서 못 고정한다. **실물 확인은 다음 PR 이다** — 워크플로를 고친 PR 에서는 액션이 자기 파일을 `main` 과 대조하고 물러난다(`2g-3`) | 없음 |
| Q62 | ~~마무리 15차 리뷰 지적 넷을 처분한다~~ — 토큰 소모 트리거 · 토큰 파기 코드 · 이메일 불변식 · 문의 주석 | **PR #45 의 08:47 회차 코멘트(15차 대상) 여덟 중 실물 넷**을 2026-09-14 설계 세션이 가려 세웠다. 나머지 넷: #1 커밋 메시지 수치(25·8 이 맞다 — 커밋은 못 고친다, 이력에 적었다) · #2 PR 본문 문구(고쳤다) · #6 R28(고쳤다) · #8 `43a-6` 중복(`43a-28` 로 바꿨다). **넷**: (a) **#4 — `password_reset_token`·`email_change_request` 의 30일 파기 코드가 없다.** `data-lifecycle.md` 가 「30일 물리 삭제」로 적었는데 `AccountPurgeService`·`TransactionPurgeService` 어디에도 그 표가 없다(`grep` 0). `DataLifecycleCoverageTest` 는 표 이름만 봐서 못 잡았다. `AccountPurgeService.purge` 에 둘을 더한다(기준 발급일 `created_at` + 30일, 매일 04:00) · `AccountPurgeServiceTest` 둘 (b) **#3 — 쓴 토큰을 다시 못 쓰는 것을 DB 로 내린다.** 지금은 `update … where used_at is null` 의 영향행 검사(앱 3위)다. `before update of used_at` 트리거가 `old.used_at is not null` 이면 거부 — 다른 경로가 생겨도 못 되살린다(2위). 두 토큰 표 같은 꼴. `V77__token_and_email_guards.sql`(신설) — **`V72` 에서 두 번 옮겼다**(`점검 O`, `Q59` 와 번호가 겹쳤다) (c) **#5 — 「확인 전엔 `app_user.email` 이 안 바뀐다」를 DB 로 내린다.** `before update of email on app_user` 트리거: `new.email is null`(파기) 이거나 `email_change_request` 에 `new_email = new.email and confirmed_at is not null and user_id = new.user_id` 인 행이 있어야 통과. 가입은 `insert` 라 안 걸린다. `move_stock` 과 같은 「길 하나」 모양. 같은 `V77__token_and_email_guards.sql`. **먼저 `V68` 의 컬럼 이름(`confirmed_at`·`new_email`)을 확인한다** (d) **#7 — `InquiryController.java:174` 주석** 「관객을 경로에 안 박는다」가 같은 컨트롤러의 `/api/me/inquiries`·`/api/seller/inquiries` 와 안 맞는다. 사실로 고쳐 쓴다: 「전체 목록은 `/api/inquiries` 고 나·셀러 경로는 스코프를 미리 좁힌 입구다 — 셋 다 같은 판정을 지난다」. **테스트**: `PasswordResetTest.쓴_토큰은_SQL_로도_못_되살린다`(트리거) · `EmailChangeTest.확인_안_된_주소로는_SQL_로도_못_바꾼다` · `AccountPurgeServiceTest` 둘 · `EnumConstraintTest`/`DataLifecycleCoverageTest` 그대로. **축**: 규약(`D13` · `D23` 축 2 — 앱 3위를 제약 2위로) + `D14`. **강제 지점**: DB 트리거(2위) 둘 + 파기 코드(앱) + 테스트. **건드리는 자리**: `V72` 신설 · `AccountPurgeService` · `InquiryController` 주석 · 테스트 넷 · `data-lifecycle.md`(파기 주체를 적는다). **닫힘**: 테스트 넷 · `verify.sh --full` · 빈 DB 기동 `applied_migrations` 72. **안 되면**: (c)의 트리거가 관리자 정정 경로(`R28` 정정 요구)를 막으면 그 경로도 `email_change_request` 를 지나게 한다 — 우회로를 만들지 않는다  **완료**(2026-09-17). `V77__token_and_email_guards.sql`(신설) · `AccountPurgeService` · `TokenGuardTest`(신설, 여섯) · `InquiryController` 주석. **넷을 다 쳤다**: (a) 토큰 둘의 30일 파기를 `AccountPurgeService` 에 더했다 — 기준은 **발급일**이고 쓴 것과 만료된 것을 안 가른다(가르면 「쓴 것은 언제까지 두나」가 새 질문이 된다) (b) 쓴 토큰을 못 되살리는 트리거 — **`null` 로 되돌리는 것도 막는다** (c) 확인된 요청 없이는 `app_user.email` 이 안 바뀐다. 가입은 `insert` 라 안 걸리고 파기는 `null` 이라 지나간다 (d) 낡은 주석을 사실로 고쳤다 — 「관객을 경로에 안 박는다」가 같은 컨트롤러의 다른 둘과 안 맞았다. **함정 둘**: 픽스처가 기존 제약 둘(`used_after_issued_check`·`app_user_alive_fields_check`)에 걸렸다 — 앞은 시계 어긋남이라 `issued_at + interval` 로, 뒤는 파기가 수명 칸과 같이 오는 것이라 `deleted_at` 을 같이 넣어 풀었다. **부순 증거**: 이메일 트리거를 안 만드니 빨갛다| 완료 |
| Q63 | ~~동시 세션 필터 셋이 이제 아무것도 안 막는다~~ | **`Q52` 가 남긴 것**(2026-09-15). 탈퇴가 세션 저장소를 직접 지우게 되면서 `SessionRegistry`·`ConcurrentSessionFilter`·`RegisterSessionAuthenticationStrategy` 셋을 **쓰는 곳이 없어졌다.** 그 셋은 원래 「탈퇴가 만료 표시를 남기고 필터가 그것을 401 로 바꾼다」 경로였는데, 지금은 세션이 아예 지워져서 표시할 것이 없다. `maximumSessions` 는 안 켜져 있다 — 즉 **동시 로그인 제한도 안 걸린다.** **안 걸리는 설정을 켜 두면 「막고 있다」고 읽힌다** — 이 저장소가 네 번 밟은 자리고 `Q52` 가 다섯 번째를 만든 셈이다. **정할 것**: (가) 셋을 지운다 — 가장 단순하고, 동시 로그인 제한이 필요해지면 그때 다시 세운다 (나) `maximumSessions` 를 켜서 셋을 실제로 쓰게 한다 — `D14` 가 동시접속 제한을 「규제」 등급에 뒀고(`real-service-comparison.md` 세션 절) 지금 명부가 Redis 라 **두 대에서도 맞게 센다.** **선행**: 없다. **축**: 규약(`D14`) + `ADR 0010`(그 결정이 셋을 세운 근거라 뒤집으면 ADR 을 새 번호로 쓴다). **강제 지점**: (가)면 없다 — 지우는 일이라 막을 것이 사라진다. (나)면 앱 검증(3위) + 테스트(4위). **건드리는 자리**: `SecurityConfig` · `ADR` 하나 · `security-baseline.md`. **닫힘**: (가)면 `AuthLoginTest`·`HttpFlowTest` 가 그대로 초록, (나)면 세 번째 로그인이 앞 세션을 끊는 테스트  **완료**(2026-09-17). `SecurityConfig`·`AuthLoginTest`·`security-baseline.md`·`doc/adr/0010-revoking-live-sessions.md`. **사용자가 (나) 를 골랐다** — 셋을 실제로 쓰게 한다. 수는 물을 것이 없었다: `real-service-comparison.md` 세션 절이 「규제」 등급을 **동시접속 1개**로 이미 적어 뒀다. **치다가 같은 함정을 한 번 더 밟을 뻔했다** — 필터체인에 `sessionConcurrency(...)` 를 넣었는데 **로그인이 그 길로 안 간다**(`formLogin` 을 꺼서 컨트롤러가 전략 빈을 직접 부른다). 테스트가 401 대신 403 을 내서 드러났고, 제한을 전략 빈으로 옮겼다. **순서가 규칙이다**: 제한 → ID 교체 → 명부 등록. 등록이 먼저면 방금 만든 세션까지 세어 첫 로그인이 자기를 끊는다. **`ADR 0010` 의 수단이 바뀐 것도 적었다** — 그 결정의 목적(살아 있는 세션을 끊는다)은 그대로고 탈퇴 경로만 `Q52` 가 갈았다. **부순 증거**: `setMaximumSessions(-1)`(무제한)로 되돌리니 「둘째 로그인이 첫째 세션을 끊는다」가 401 대신 403 으로 빨갛다 | 완료 || 없음 |
| Q64 | ~~문서에 박힌 수치가 하루에 세 번 낡았다~~ | **`Q48` 이 세운 대조가 이름만 본다**(마무리 17차, 2026-09-15). `testing-strategy.md` 의 레인 표(빠른 N개·느린 M개·비율)가 **하루에 세 번 고쳐졌다** — `Q48` 이 87·930 을 132·1109 로, `Q49` 가 137·1112 로, 마무리가 139·1118 로. 매번 사람이 지나가다 봤다. **`DocumentMapConsistencyTest` 는 이것을 못 본다**: 그쪽은 **이름**이 맞는지를 보고 이 표는 **수**라, 재려면 두 레인을 다 돌려야 한다. **정할 것**: (가) 수치를 문서에서 걷고 「세는 명령」만 적는다 — 낡을 것이 없어진다. 대신 「빠른 레인이 얇다」 같은 서술의 근거가 문서에서 사라진다 (나) `build` 뒤에 수를 파일로 뱉고 문서와 대조하는 테스트를 둔다 — 두 레인을 다 돌린 뒤에만 뜻이 있어서 **빠른 레인에 못 둔다** (다) 비율만 남기고 절대 수를 걷는다 — 비율은 9%에서 11%로 아홉 날에 2%p 라 훨씬 느리게 낡는다. **잰 날짜를 박아 두는 것으로는 하루도 못 버틴다는 것이 이번 증거다.** **선행**: 없다. **축**: 규약(`D15` · `D23` 축 2). **강제 지점**: (가)면 없앤다 — 낡을 값이 사라지는 것이 가장 낮은 자리다. (나)면 테스트(4위). **건드리는 자리**: `testing-strategy.md` · (나)면 `build.gradle.kts` 와 테스트 하나. **닫힘**: 레인 표를 고치지 않고 일주일이 지나거나, (나)의 대조가 초록  **완료**(2026-09-17, `Q82` 와 한 묶음 — 둘 다 문서의 값이 실물과 갈린 것이다). **사용자가 (가) 를 골랐다** — 수치를 걷고 세는 명령만 적는다. **(나)를 버린 근거가 게이트의 성질이다**: 그 대조는 **두 레인을 다 돌린 뒤에만** 뜻이 있는데 청크를 닫는 기준은 `/verify` 의 빠른 도장이고 두 레인은 push 앞이다(`2z-2`) — 빠른 레인만 돈 자리에서 대조하면 거짓말을 하거나 건너뛴다. **게이트가 하나 늘고 그것이 조용히 안 도는 것**이 이 저장소가 다섯 번 밟은 자리라 안 만들었다. 대신 「왜 못 재나」를 문서에 남겼다. 비율 서술(`11%`·`9%`)도 같이 걷고 「얇다」로 바꿨다 — 그 문장이 말하려던 것은 수가 아니라 모양이다| 완료 |
| Q65 | ~~권한 표와 소유 목록의 대조가 사람 손이다~~ | **`Q56` 이 남긴 것**(마무리 17차 독립 리뷰, 2026-09-15). `permission-rules.md` 「판정을 안 지나는 입구」의 표와 `ArchitectureTest.소유가_곧_권한인_입구` 목록이 **같아야 하는데 대조가 사람 손이다.** 문서에 「이 표와 그 목록이 같아야 한다 — 대조는 아직 사람이 한다」로 적혀 있다. **재발 조건이 그대로다**: `Q56` 자신이 그 자리에서 문서가 실물과 갈려 있던 것을 찾았고(표 셋 vs 실물 다섯), **세운 날 숫자가 또 한 칸 틀렸다**(「여덟」 vs 아홉). 같은 묶음의 `Q48` 이 「문서가 실물과 갈려도 아무것도 안 걸리는 것」을 닫겠다고 대조 테스트를 세웠는데, `Q56` 은 **새로 만든 문서↔코드 쌍을 사람 대조로 남겼다.** **절차**: (1) 문서 표의 행에서 `Controller.method` 를 뽑는 꼴을 정한다 — 지금은 산문이라 못 뽑는다. 표 칸에 백틱으로 `MeController.withdraw` 를 하나씩 적는 꼴로 바꾼다 (2) `ArchitectureTest` 에 규칙 하나 — 문서에서 뽑은 집합과 `소유가_곧_권한인_입구` 가 같은가. 양방향으로 본다: 목록에만 있는 것(문서에 근거가 없다)과 문서에만 있는 것(규칙이 안 막는다) 둘 다 빨강 (3) 그 규칙이 `doc/reference/permission-rules.md` 를 읽으므로 `build.gradle.kts` 의 `comparedInFastLane` 에 이미 걸린 `../doc/reference` 폴더가 덮는지 확인한다(`Q48` 이 폴더로 걸었다) (4) `quality-gates.md` 행. **축**: 규약(`D6` · `D23` 축 2). **강제 지점**: 테스트(4위) — 문서(5위)에서 내린다. **건드리는 자리**: `permission-rules.md` · `ArchitectureTest` · `quality-gates.md`. **닫힘**: 문서 표에서 한 줄을 지우면 빨갛고, 목록에 없는 이름을 표에 더해도 빨갛다. **안 되면**: 표를 기계가 읽을 꼴로 못 바꾸겠으면 목록을 문서 쪽으로 옮기고 문서를 단일 진실로 삼는다 — 대조가 아니라 출처를 하나로 만드는 쪽이다  **완료**(2026-09-17, `Q68` 과 한 묶음 — 둘 다 `ArchitectureTest` 를 고친다). `permission-rules.md`·`ArchitectureTest`·`quality-gates.md`·`testing-strategy.md`. **문서 표기를 바꾸는 것이 조건이었다** — 마지막 칸이 산문(「`MeController` 의 탈퇴·비밀번호 변경…」)이라 기계가 못 셌다. `Class.method` 로 적으니 대조가 선다. **덤이 있다**: `Q83` 의 게이트가 그 이름들이 실재하는 클래스인지도 같이 잰다. **부순 증거**: 표에서 항목 하나를 지우니 「갈렸다」로 빨갛다| 완료 |
| Q66 | ~~아웃박스 페이로드를 `D12`·`D9` 에 맞춘다~~ | **`Q57` 이 카탈로그를 옮기면서 규칙 넷을 어겼다**(마무리 18차 독립 리뷰, 2026-09-15). 아직 **아무도 이 표를 안 읽어서**(발행기가 `33`) 지금 고치면 대외 계약에 흠이 안 남는다 — `33` 의 선행이다. **다섯을 정한다**: ① **반품의 `subject` 가 내부 id 다.** `D9` 「자원별 노출 방식」은 반품에 **「없다 — `seller_order_number`」**로 이미 정했는데 `D12` 표가 「노출 번호가 아직 없다」로 다르게 적었고 `V70` 이 그것을 옮겼다. **`D9` 가 식별자의 출처다** — `seller_order_number` 로 바꾸고 `D12` 행을 고친다. 마무리 18차가 실물로 밟았다: 재고와 반품이 둘 다 작은 내부 id 를 실어서 **한 대상에 사건이 둘로 세어졌다** ② **`data` 에 내부 id 를 싣는다** — `order_id`·`seller_order_id`·`seller_id` 셋. `D12` 페이로드 규칙 2 는 「노출 번호를 쓴다. 내부 id 는 `sku_id` 처럼 **노출 번호가 없는 것만**」이고 셋 다 노출 값이 있다(`order_number`·`seller_order_number`·`seller.code`) ③ **전이 사건 셋에 `actor_type` 이 없다**(환불·반품·정산). `D12` 규칙 3 이 요구하고 원천 표에 값이 있다(`refund.approved_by_type`·`settlement.payout_decided_by_user_id`) — 주문 이력만 규칙을 지켰다 ④ **`occurred_at` 의 출처가 셋으로 갈린다**: 원천 컬럼 · `new.updated_at` · `now()`. **`refund` 는 `set_updated_at` 트리거가 없는 표다** — 지금은 서비스가 손으로 적어서 맞지만 그 한 줄을 빠뜨린 `update` 가 하나 생기면 봉투의 `time` 이 조용히 행 생성 시각으로 굳는다. 정산은 `payout_decided_at` 을 두고도 `now()` 를 쓴다 ⑤ **`shop.batch.run_finished` 의 자원 토막이 `batch` 인데 표는 `batch_run` 이다.** `D12` 가 「자원은 `D22` 의 표 이름(단수)」이라 정해 놓고 자기 표에서 어겼다 — `shop.batch_run.finished` 가 규칙에 맞는 이름이다. **덧붙여 ⑥**: 환불 사건의 `amount` 가 `new.amount` 인데 실제로 나간 돈은 지연이자를 더한 값이다(`RefundService.settle`). 소비자가 환급액으로 읽으면 틀린다 — `delay_interest` 를 같이 싣거나 이름을 바꾼다. **축**: 표준(CloudEvents) + 규약(`D12`·`D9`·`D22`). **강제 지점**: DB 트리거(2위) + 테스트(4위) — `OutboxEventSchemaTest` 가 사건마다 `data` 키를 재게 넓힌다. **건드리는 자리**: `V71` 신설(트리거 다시 만든다 — `V70` 은 배포 전이지만 이 묶음이 머지된 뒤면 `create or replace` 로 새 파일에 쓴다) · `event-catalog.md` · `EventType` · `OutboxEventSchemaTest`. **닫힘**: 사건 일곱이 저마다 `D12` 규칙 1~4 를 지키는지 테스트가 잰다. **안 되면**: ⑤는 이름이 대외 계약이라 바꾸려면 지금뿐이다 — `33` 이 한 번 내보내면 못 고친다. **하나를 흡수했다**(마무리 19차 독립 리뷰, 2026-09-16): **`BATCH_RUN_FINISHED` 의 `type` 이 `shop.batch.run_finished` 인데 `batch` 라는 표가 없다**(실물은 `batch_run`). `EventType` javadoc 이 「자원은 표 이름(단수, `D22`)」이라 적어 놓고 그 한 값만 어긴다 — 나머지 여섯은 맞는다. `V70` 의 `outbox_event_type_check` 와 `D12` 에 같이 박힌 값이라 이 청크가 같이 고친다. **`observability-rules.md` 「지표」의 이름 규약(`shop.<자원>.<무엇>`)도 표 이름을 안 걸어 둬서 같은 자리를 다르게 정하고 있다** — 둘 중 하나로 맞춘다  **완료**(2026-09-17). `V76__outbox_payload.sql` 신설 · `EventType` · `OutboxEventSchemaTest` · `event-catalog.md` · `quality-gates.md`. **번호가 예보와 달랐다** — 분할표는 `V71` 신설이라 적었는데 그 번호는 이미 찼고, `V76` 을 쓰면서 `Q62` 예약을 `V77` 로 밀었다(`Q69` 의 테스트가 시켰다). **여섯을 다 쳤다**: ① 반품 `subject` → `seller_order_number`(`D9` 가 식별자의 출처다) ② 내부 id 셋 → `order_number`·`seller_code`(`sku_id` 만 남는다 — 그 자원엔 노출 번호가 없다) ③ `actor_type` — 환불은 `coalesce(approved_by_type, requested_by_type)`, 정산은 상수 `admin`(`D7` 이 요청·승인 둘 다 관리자로 정했다), **반품은 못 싣는다**(원천 표에 종류 칸이 없고 사람 id 는 규칙 1 이 막는다 — 테스트가 그 하나를 목록으로 든다) ④ `occurred_at` — 결정 시각이 있으면 그것, 없을 때만 뒤로 물러난다 ⑤ `shop.batch.run_finished` → `shop.batch_run.finished`(`batch` 라는 표가 없다) ⑥ 환불 금액을 `refund_amount`·`delay_interest` 로 갈랐다 — 실제로 나가는 돈은 지연이자를 더한 값이다. **함정 하나**: jsonb 의 `?` 존재 연산자를 JDBC 가 자리표시자로 읽어서 터진다 — `jsonb_exists` 로 바꿨다. **부순 증거**: 정산 사건에 `seller_id` 를 도로 넣으니 그 키를 짚고 빨갛다| Q57 |
| Q67 | ~~시드가 아웃박스에 사건을 남긴다~~ | **`Q57` 이 만든 새 사실**(마무리 18차 독립 리뷰, 2026-09-15). 데모 시드(`V902__demo_products.sql`)가 `sku_stock` 에 넣고, 그것이 `V41` 의 `record_initial_stock` 을 지나 `sku_stock_movement` 로, 다시 새 트리거로 이어진다 — **시드를 넣은 DB 는 SKU 수만큼 미발행 `shop.sku.stock_moved` 사건을 들고 시작한다.** `V41` 의 백필은 `V70` 앞이라 안 걸리지만 시드는 뒤다. 발행기(`33`)가 서면 **그것이 진짜 재고 이동과 구별 없이 나간다.** **정할 것**: (가) 시드가 `shop.outbox_write` 를 안 켜고 넣게 두고 **시드 끝에서 아웃박스를 비운다** — 가장 단순하고, 시드는 「없던 일을 있었던 것처럼 만드는 것」이라 사건이 안 맞는다 (나) 발행기가 「시드 구간의 사건」을 거른다 — 구간을 무엇으로 아나가 또 결정이라 미룬다 (다) 그대로 둔다 — 데모라 알려도 상관없다면. **호스팅에 올릴 때가 실제로 걸리는 자리다**(`backend/README.md` 「호스팅에 올릴 때」가 시드를 어떻게 다루는지 정한 곳). **선행**: 없다. **축**: 규약(`D12`). **강제 지점**: (가)면 시드 SQL(2위 옆) — 마이그레이션이 스스로 치운다. **건드리는 자리**: `db/seed/V902` 또는 새 시드 파일 · `event-catalog.md` 「지금 안 하는 것」. **닫힘**: 빈 DB 에 시드까지 넣고 기동한 뒤 `select count(*) from outbox_event where published_at is null` 이 0  **완료**(2026-09-17). `V903__clear_seed_outbox.sql`(신설) · `SeedOutboxTest`(신설) · `event-catalog.md` · `quality-gates.md`. **사용자가 (가) 를 골랐다** — 시드 끝에서 아웃박스를 비운다. (나)(발행기가 시드 구간을 거른다)를 버린 근거: 발행기가 **「어떤 사건은 진짜가 아니다」를 알게 되고** 그 지식의 출처를 또 정해야 하며, 아웃박스의 「쌓인 것은 다 나간다」를 깬다. **적용된 시드를 직접 안 고쳤다** — 체크섬으로 도는 로컬 DB 가 깨진다(`V902` 가 같은 이유로 덧댄 자리다). **통째로 지우는 것이 맞다**: 시드는 빈 DB 에 한 번 도므로 그 시점에 표에 있는 것은 전부 시드 것이다. **도는 것을 못 잰다** — 시드가 `local` 프로필에서만 돌아서 글자로 쟀고, 그물이 성기다는 것(마지막 순서는 번호로만 보장된다)을 문서와 주석에 적었다. **부순 증거**: `V903` 을 치우니 빨갛다| 완료 |
| Q68 | ~~순수 계산이 서비스 안에 남는 것을 테스트가 막는다~~ | **`Q54` 가 자기 구멍을 이력에 적었고 담당 청크가 없었다**(마무리 19차 독립 리뷰, 2026-09-16). `RefundMath` 를 뺀 강제 지점은 **옮긴 계산이 되돌아오는 것**만 막는다 — 새 계산을 `*Service` 안에 `private static` 으로 쓰면 아무도 안 잡고, 그 규칙은 `coding-rules.md`·`testing-strategy.md` 에만 있어 **문서 5위**다. **절차**: (1) `ArchitectureTest` 에 규칙 하나 — `..shop..` 의 `*Service`·`*Sweeper`·`*Batch` 클래스에서 **`static` 이고 도메인 타입만 받고 돌려주는 메서드**를 센다. 판정은 시그니처로 한다(필드·`jdbc`·`Clock` 을 안 쓰는 것은 ArchUnit 이 못 보므로 **바이트코드 접근 대상**으로 잰다: `JdbcClient`·`MeterRegistry`·`Clock`·`OffsetDateTime.now` 를 안 건드리는 `private static`) (2) 걸리는 것이 지금 몇인지 먼저 세고 **기준선을 0 으로 못 잡으면 목록으로 면제**한다 — `Q60` 이 래칫을 쓴 것과 같은 꼴. 면제 줄마다 왜 안 빼는지 한 줄 (3) 규칙이 가리키는 문서 두 곳(`coding-rules.md` 「테스트」·`testing-strategy.md` 「새 로직을 짤 때」)에 「이 규칙이 잰다」를 적는다. **축**: 규약(`D15`·`D23`). **강제 지점**: 테스트(4위) — 문서 5위를 4위로 내리는 것이 이 청크의 전부다. **건드리는 자리**: `ArchitectureTest` · 문서 둘. **닫힘**: 새 규칙 초록 · **부순 증거** — `RefundMath.commissionRefund` 를 `RefundService` 로 되돌리면 빨갛다 · `verify.sh`. **안 되면**: 시그니처만으로 「순수」를 못 가르면 **`*Math` 로 끝나는 클래스가 있는 패키지의 서비스**로 범위를 좁힌다 — 그래도 「한 번 뺀 자원은 다시 안 섞인다」는 지켜진다  **완료**(2026-09-17, `Q65` 와 한 묶음). `ArchitectureTest`·`OrderService`·`WithdrawalRestriction`(신설)·`WithdrawalRestrictionTest`(신설)·`quality-gates.md`·`testing-strategy.md`. **규칙을 두 번 좁혔다**: 처음 쟀을 때 **열일곱**이 걸렸는데 대부분이 가드(`notFound`·`require*`)와 행 매퍼였다 — 예외를 던지거나 `ResultSet` 을 받거나 값을 안 돌려주는 것을 빼니 **여섯**이 됐다. **그중 하나가 진짜였다**: `OrderService.agreedRestriction` 이 청약철회 제한의 성립(`D2` R4, 제17조제2항 단서)을 정하는데 `private static` 이라 **그 규칙만 시험할 수가 없었다** — 주문을 만들어야 닿으니 제한 규칙이 아니라 주문 생성을 시험하게 된다. `WithdrawalRestriction` 으로 빼고 테스트 다섯을 세웠다(동의가 성립 요건인 쪽이 핵심이다). 나머지 다섯은 근거와 함께 통과 목록. **그물이 성기다는 것을 적어 뒀다** — 직접 호출만 보므로 private 헬퍼 뒤에 숨기면 안 걸린다. **부순 증거**: 통과 목록에서 하나를 빼니 이름을 짚고 빨갛다| 완료 |
| Q69 | ~~예약한 마이그레이션 번호를 테스트가 잰다~~ | **`점검 O` 가 실물로 찾았다**(2026-09-17). 미착수 청크 둘이 `V72` 를 같이 예약했고(`Q59`·`Q62`), 게다가 `V73` 이 이미 적용돼 있어 **그 빈 번호를 뒤늦게 채우면 기동이 죽는다** — `out-of-order` 설정이 0건(Flyway 기본 거부)이고 `PostgresTestBase` 가 `withReuse(true)` 라 컨테이너가 `V73` 을 든 채로 산다. `CLAUDE.md` 「병렬 줄」이 「마이그레이션 번호를 예약한다」고 적어 뒀는데 **겹쳤는지 보는 자리가 사람 눈뿐**이었다. **절차**: (1) `PlanProgressConsistencyTest` 에 둘을 더한다 — ① `reservedMigrationNumbersAreUnique`: `PLAN.md` 의 미착수 행(`~~` 없는 행)에서 `V(\d+)__` 를 걷어 중복이 없다 ② `reservedMigrationNumbersExceedApplied`: 그 번호가 전부 `db/migration` 의 최대 번호보다 크다 (2) `build.gradle.kts` 의 `comparedInFastLane` 에 마이그레이션 폴더가 걸렸는지 보고 안 걸렸으면 건다(`Q25`) (3) `quality-gates.md` 게이트 표에 행. **축**: 규약(`D23` 「마이그레이션」 · 축 2 — 사람 5위를 테스트 4위로). **강제 지점**: 테스트(4위). 제약으로는 못 내린다 — 예약은 `PLAN.md` 에만 있는 값이다. **건드리는 자리**: `PlanProgressConsistencyTest` 수정 · `quality-gates.md`. **닫힘**: 테스트 둘 초록 · `verify.sh` 초록. **완료**(2026-09-17). `PlanProgressConsistencyTest` 수정(테스트 둘 · 헬퍼 넷) · `build.gradle.kts` · `quality-gates.md`. **예약의 표기를 테스트가 정했다** — `V74__permission_kind.sql` 꼴로 적은 것만 예약으로 본다. 그렇게 안 하면 미착수 행의 산문(`V13:38`·`V12`~`V64`)까지 번호로 읽힌다. `Q62` 의 「같은 `V75`」를 파일 이름 꼴로 고쳤다. **이미 파일이 있는 번호는 안 센다** — 예약이 아니라 인용이고, 시드의 `V902__demo_products.sql` 을 부르는 미착수 행이 실제로 있다. 그래서 `build.gradle.kts` 의 빠른 레인 입력에 `db/seed` 를 더했다(시드가 하나 늘면 판정이 바뀐다). **부순 증거 둘**: `Q62` 의 예약을 `V74` 로 되돌리니 첫째가 `V74 ← Q59, Q62` 로 이름까지 짚고, `V72` 로 되돌리니 둘째가 「마지막 번호는 V73 다」로 빨갛다 | 완료 |
| Q70 | ~~배치 카탈로그와 `@Scheduled` 를 테스트가 대조한다~~ | **`점검 O` 가 낡은 행을 찾았다**(2026-09-17). `batch-catalog.md` 에 「정산 마감」 행이 **둘**이었고 하나가 「미정(청크 19) · 아직 없다」였다 — 실물은 `SettlementCloseBatch`(매월 1일 05:00)다. 그 행은 점검이 지웠다. **막는 자리가 문서뿐이었다** — `BatchSchedulingTest` 는 스레드풀만 보고 `BatchRunsTest` 는 두 배치의 주기만 본다. **절차**: (1) `BatchSchedulingTest` 에 `catalogRowsMatchScheduledMethods` — `batch-catalog.md` 카탈로그 표의 「클래스」 칸을 걷고 `@Scheduled` 를 가진 클래스 목록과 **양방향** 대조한다. 빠진 것이 나오면 문서에 더한다, 예외 목록은 안 만든다 (2) `quality-gates.md` 게이트 표에 행 (3) `batch-catalog.md` 「이 문서를 고칠 때」에 그 테스트를 건다. **축**: 규약(`D19`). **강제 지점**: 테스트(4위). **건드리는 자리**: `BatchSchedulingTest` 수정 · 문서 둘. **닫힘**: 그 테스트 초록. **완료**(2026-09-17). `BatchCatalogTest`(신설, **빠른 레인**) · `batch-catalog.md` · `quality-gates.md` · `testing-strategy.md`. **예보와 자리가 달랐다** — 분할표는 `BatchSchedulingTest` 에 더하라고 적었는데 그쪽은 스레드가 실제로 도는지를 보느라 컨텍스트를 띄운다. 이 대조는 글자만 읽어서 **빠른 레인에 둘 수 있다**(`D15` · `backend/CLAUDE.md` 「테스트」). **첫 회차가 실물을 잡았다**: 카탈로그가 `OutboxPublisher.publishOnce` 를 가리키는데 `@Scheduled` 는 `publish` 에 붙어 있다 — `publishOnce` 는 그 안에서 부르는 한 회차라 **스케줄러가 부르는 자리가 아니다.** 문서를 고쳤다. **스캐너를 한 번 고쳤다**: 애노테이션이 두 줄이면(`@Scheduled(fixedDelayString = ...,` / `initialDelayString = ...)`) 다음 줄에 여는 괄호가 없고 `${` 의 중괄호만 있어서 메서드를 놓쳤다 — 괄호 균형으로 애노테이션의 끝을 세게 바꿨다. **부순 증거 둘**: 카탈로그의 `StaleBundleBatch.close` 행을 지우니 「카탈로그에 없는 배치가 돈다」로 빨갛고, `publishOnce` 가 그대로였을 때 반대 방향이 빨갰다 | 완료 |
| Q71 | ~~로그에 개인정보가 실리는 것을 테스트가 막는다~~ | **`점검 O` 가 강제 지점이 문서뿐인 것을 확인했다**(2026-09-17). `observability-rules.md:127` 이 「이름·이메일·연락처·주소는 로그에 안 넣는다」고 정했고 **지금 위반은 0건**인데 막는 것이 습관뿐이다 — `ArchitectureTest` 규칙 열넷에 로그 규칙이 없고 게이트가 `grep` 0건이다. 같은 문서(`:135`)가 「금지 목록은 빠뜨리면 조용히 샌다」고 적어 둔 것이 **로그 쪽에는 안 걸려 있다**. **절차**: (1) `observability/LogArgumentTest`(신설, 빠른 레인) — `src/main/java` 를 글자로 훑어 `log.<수준>(` 호출의 인자에 개인정보 접근자(`.email()`·`.phone()`·`.name()`·`.address()`·`displayName`)가 있으면 빨갛다. `batchName` 처럼 사람이 아닌 이름과 어긋나므로 **접근자 호출 꼴만** 본다 (2) 통과가 필요하면 상수 목록에 두고 **왜 통과인지**를 같이 적는다(`LengthConstraintTest.NOT_COMPARED` 와 같은 꼴) (3) `quality-gates.md` 에 행 (4) `observability-rules.md` 「개인정보는 안 찍는다」에 그 테스트를 건다. **축**: 규약(`D16`). **강제 지점**: 테스트(4위) — 타입으로는 못 막는다(`log.info` 인자가 `Object...` 다). **건드리는 자리**: 신설 하나 · 문서 둘. **닫힘**: 그 테스트 초록 · `verify.sh` 초록. **완료**(2026-09-17). `observability/LogArgumentTest`(신설, 빠른 레인) · `observability-rules.md` · `quality-gates.md` · `testing-strategy.md`. **세울 때 위반이 0이었고 그것이 세운 이유다** — 규칙이 지켜지고 있는데 막는 것이 문서(5위)뿐이라 깨지는 날이 안 보인다. **금지 이름에서 `name()` 을 뺐다**: 열거형의 `name()` 이 같은 글자라 전부 걸린다. 사람 이름을 담는 칸은 `displayName`·`receiverName`·`senderName` 이고 그것만 적었다. **형식 문자열도 본다** — `email={}` 은 실을 작정이라는 뜻이다. **그물이 성기다는 것을 문서에 같이 적었다**: 객체를 통째로 찍는 것과 지역 변수로 한 번 받아 찍는 것은 타입·대입을 따라가야 해서 글자로 못 본다. `D16` 이 마스킹을 안 쓴 이유(「금지 목록은 빠뜨리면 샌다」)가 이 테스트에도 그대로 걸려서, 문턱을 「테스트가 초록」이 아니라 「애초에 안 찍는다」로 남겼다. **부순 증거**: `BatchRetrySweeper` 의 형식 문자열에 `email={}` 을 심으니 파일과 이름을 짚고 빨갛다. **처음 심은 판은 컴파일이 깨져서** 증거가 안 됐다(없는 접근자를 불렀다) — 도는 코드로 다시 심었다 | 완료 |
| Q72 | ~~`D7` 이 안 드는 상태 둘에 근거를 적는다 — `app_user.status`·`sku.status`~~ | **`점검 O` 가 찾았다**(2026-09-17). 상태 `check` 제약이 열여섯인데 `state-machines.md` 는 주문·묶음·반품·셀러·상품·정산지급·문의만 들고, `app_user.status`(`active`/`suspended`)와 `sku.status`(`on_sale`/`suspended`)는 **본문에도 「안 다루는 것」에도 없다**. 둘 다 값을 옮기는 코드가 `grep` 0건이다 — `UserStatus` javadoc 은 「계정의 업무 상태」라고 적어 뒀는데 옮기는 주체가 없다. `R1`(컬럼은 있는데 내리는 경로가 0)과 같은 모양이다. **결정이 갈린다**: 업무 상태로 둘 것인지(전이 주체와 표를 세운다) 아닌지(`domain-model.md` 가 가르는 기준을 든다). **사용자에게 선택지를 낸다.** **절차**: (1) 둘을 갈라 정한다 (2) 업무 상태면 절을 세우고 전이 주체를 적는다. 아니면 「안 다루는 것」 표에 **근거와 함께** 행을 더한다(`D23` 「안 넣은 것도 근거를 남긴다」) (3) `sku.status` 가 상품 상태와 어떻게 다른지도 적는다 — 지금 「상품 상태」 절이 `sku` 를 한 번도 안 부른다. **축**: 규약(`D7`·`D23`). **강제 지점**: 문서(5위) — 옮기는 코드가 없어 더 내릴 것이 없다. 전이를 세우기로 하면 그때 제약으로 내려간다. **건드리는 자리**: `state-machines.md`. **완료**(2026-09-17). `state-machines.md`. **사용자가 (가) 를 골랐다** — 둘을 갈라 적는다. 가른 근거는 실측이다: `sku.status` 는 **읽는 자리가 12곳**(장바구니·주문·상품 조회가 `status = 'on_sale'` 로 거른다)이라 이미 「살 수 있는 낱개」를 가르고 있고, `app_user.status` 는 **읽는 코드도 쓰는 코드도 0**이다. 그래서 `sku` 는 「상품 상태」 아래 절을 세워 상품과 무엇이 다른지(상품 통째 vs 낱개 하나)와 전이 주체(셀러)를 적고, 「입구가 아직 없어 값이 `on_sale` 로만 들어간다」를 같이 남겼다. `app_user.status` 는 「안 다루는 것」에 근거와 함께 넣었다 — 정지 입구도 정지를 보고 막는 자리도 없고, 탈퇴는 `deleted_at` 이, 로그인 차단은 `D14` 시도 제한이 든다. **지금 전이표를 그리면 주체가 안 정해진 표가 된다**(그 문서 「이 문서를 고칠 때」가 경계한 것). **강제 지점은 문서(5위)뿐이고 그것이 맞다** — 옮기는 코드가 0이라 더 내릴 자리가 없다 | 완료 |
| Q73 | ~~길이 상한이 **아예 없는** 칸을 게이트가 못 본다~~ | **`점검 O` 가 둘을 찾았다**(2026-09-17). `coding-rules.md:424` 가 「길이 상한은 앱과 DB 양쪽에 둔다」인데 세 층이 다 빈 자리가 있다. ① `product.description` — `ProductController:218` 에 `@Size` 없음 · `V13:38` `description text` 에 제약 없음 · `product-form.tsx` 에 `maxLength` 없음. 사용자가 쓰는 자유 텍스트다 ② `return_pickup` 의 `sender_name`·`address1`·`address2`·`pickup_memo` 에 `%length_check` 가 **0개**다. **같은 모양인 `order_shipping` 은 넷 다 있다**(`V65__length_constraints.sql:39` 이 `alter table` 로 더한다) — 그래서 `return_pickup` 만 빈 것이 더 또렷하다. **점검이 낸 것을 한 번 잘못 뒤집었다가 되돌렸다**: 대조용 `grep` 의 문자군이 `[a-z_]` 라 숫자가 든 `address1`·`address2` 를 못 세고 「배송지도 둘뿐」이라고 적었다. 숫자를 넣어 다시 세니 33개다. **읽은 것이 아니라 세는 도구가 틀린 자리**라 `Q69` 의 교훈과 같다. 원인은 게이트의 방향이다 — `LengthConstraintTest.everyLengthConstraintIsAccountedFor` 는 `pg_constraint` 에서 **이미 있는** `%length_check` 만 걷어서 **없는 칸은 구조적으로 못 본다**. **절차**: (1) 제약을 더한다 — `return_pickup` 의 `sender_name`·`address1`·`address2`·`pickup_memo` · `product.description`. 상한은 짝인 `order_shipping` 의 같은 칸과 맞춘다(`receiver_name` 1~50 · `address1` 1~200 · `address2` 0~200 · `delivery_memo` 0~200) (2) `ProductController` 의 `description` 에 `@Size` (3) `LengthConstraintTest` 에 **반대 방향** 테스트 — `text` 컬럼 중 `%length_check` 가 없는 것을 걷어 통과 목록과 대조하고, 통과에는 **왜 상한이 없나**를 적는다 (4) `quality-gates.md`. **축**: 규약(`D23` 「길이 상한」 · 축 2). **강제 지점**: DB 제약(2위) + 앱 검증(3위), 게이트는 테스트(4위). **건드리는 자리**: 새 `V`(번호는 `Q69` 규칙대로 최대보다 크게) · `ProductController` · `LengthConstraintTest` · `quality-gates.md`. **닫힘**: 새 테스트가 초록이고 통과 목록의 모든 줄에 근거가 있다. **완료**(2026-09-17, `Q74` 를 흡수했다). `V74__text_length_guards.sql`(신설) · `ProductController` · `LengthConstraintTest` · `ScreenLengthTest` · 화면 넷 · `quality-gates.md` · `testing-strategy.md`. **셋째 구멍이 반대 방향 대조를 세우다 나왔다** — `product_option_value.value` 가 옵션 *이름*은 `@Size(max = 50)` 인데 *값*은 `@NotBlank` 뿐이고 DB 도 비어서 **셀러가 무한 길이를 넣을 수 있었다.** 첫 회차가 상한 없는 `text` 컬럼 **서른하나**를 이름까지 짚었고, 나머지 서른은 구성 값·박제·앱이 적는 값이라 **근거와 함께** 통과 목록에 넣었다. **부순 증거 둘**: 통과 목록에서 `holiday.name` 을 빼니 그 칸을 짚고 빨갛다 · `signup-form.tsx` 의 `maxLength` 를 빼니 `app/signup/signup-form.tsx  email` 을 짚고 빨갛다. **번호가 밀렸다** — 이 청크가 `V74` 를 쓰면서 `Q69` 의 테스트가 `Q59`·`Q62` 예약을 `V75`·`V76` 으로 밀게 했다 | 완료 |
| Q74 | ~~`ScreenLengthTest` 가 `maxLength` 없는 칸을 구조적으로 못 본다~~ | **`점검 O` 가 실물로 밟았다**(2026-09-17). `testing-strategy.md` 가 「화면 입력칸의 `maxLength` = 요청 record 의 `@Size(max)`」를 그 테스트에 맡겼는데, 정규식(`ScreenLengthTest:53-60`)이 `name="x"` 와 `maxLength={N}` 을 **한 태그 안에 같이** 요구한다 — `maxLength` 가 아예 없는 칸은 목록에 안 들어가고 `unmatchedFieldsArePinned` 도 그 목록만 돈다. **걸려 있는데 안 도는 자리다.** 실제로 새는 것 셋: `signup/signup-form.tsx:98` `email`(서버 254) · `:106` `displayName`(서버 `AuthController:234` 50) · `account-forms.tsx` `email`. 셋 다 지금 초록이다. **절차**: (1) `name="x"` 만으로 칸을 걷는 정규식을 따로 두고 그 목록에서 `maxLength` 가 없는 칸을 빨갛게 한다. 상한이 없어야 맞는 칸(`password`)은 **근거와 함께** 통과 목록에 (2) 위 세 칸에 `maxLength` 를 넣는다 — 값은 서버 `@Size` 와 같게 (3) `testing-strategy.md` 의 그 행에 「없는 칸도 잡는다」를 적는다 (4) `quality-gates.md` 의 그 게이트 행에 「부순 날」을 갱신한다. **축**: 규약(`D15`·`D20`) + 축 2(테스트가 실제로 도나). **강제 지점**: 테스트(4위). **건드리는 자리**: `ScreenLengthTest` · 화면 둘 · 문서 둘. **닫힘**: 세 칸이 채워지고 테스트가 「없는 칸」을 잡는다. **완료**(2026-09-17). **`Q73` 이 흡수했다** — 같은 결함이 두 층에 난 것이고 `product.description` 이 실제로 DB·앱·화면 셋에 다 걸쳐서, 갈라 치면 그 칸을 두 청크가 반씩 고치게 된다. 채운 칸 다섯: 가입 `email` 254·`displayName` 50 · 계정 `email` 254 · 로그인 `email` 254 · 상품 `description` 2000. **비밀번호 여섯은 상한을 안 건다** — `maxLength` 는 넘친 글자를 조용히 버려서 로그인은 실패하고 변경은 본인도 모르는 값이 된다. select·checkbox·number 넷은 속성 자체가 안 걸린다. **부순 증거**: `signup-form.tsx` 의 `maxLength` 를 빼면 그 칸을 이름까지 짚는다 | 완료 |
| Q75 | ~~못 보는 칸이 `null` 로 나간다 — `InquiryQuery.Entry`~~ | **`점검 O` 가 찾았다**(2026-09-17). `screen-rules.md:247` 이 「값이 없으면 `null`, **볼 수 없으면 키가 없다**」로 정했는데 `InquiryQuery.Entry`(`:70`)에 `@JsonInclude(NON_NULL)` 이 없고 `:233` 이 `body ? rs.getString("question") : null` 로 **못 보는 칸에 `null` 을 넣는다**. 응답이 `"question": null` 이라 「못 봄」과 「값 없음」이 한 모양이 된다. 형제 셋은 지킨다 — `AccountService:56`·`OrderQuery:188`·`SellerOrderQuery:89` 가 `NON_NULL` 을 달고 있다. 화면도 `app/seller/inquiries/page.tsx:19` 가 `question: string` 으로 **선택이 아님을 박아 뒀다**. `D23` 「안 나가게 하는 것도 강제 지점이 있다」의 1위(계약에 칸이 없다)로 못 올리는 자리라 2위(마스킹)에서 샌 것이다. **절차**: (1) `Entry` 에 `@JsonInclude(NON_NULL)` (2) 화면 타입을 `question?: string` 으로 고치고 없을 때 표시를 `screen-rules.md` 빈 상태 규칙대로 (3) 「못 보는 칸은 키가 없다」를 재는 테스트 (4) **형제를 한 자리에서 세는 테스트**를 둘지 본다 — 마스킹을 쓰는 응답 record 에 `NON_NULL` 이 있나. 그것이 재발을 막는 자리다. **축**: 규약(`D20`·`D23` 나가는 사다리). **강제 지점**: 애노테이션 + 테스트. **건드리는 자리**: `InquiryQuery` · 화면 하나 · 테스트 하나. **완료**(2026-09-17). `InquiryQuery`·`seller/inquiries/page.tsx`·`InquiryVisibilityTest`. **`@JsonInclude` 만 달면 안 됐다** — record 통째로 걸면 「값이 없음」도 키가 사라져서 `D20` 규칙의 반대쪽이 깨진다(답이 아직 안 나간 문의와 답을 못 보는 사람이 같은 모양이 된다). **형제가 이미 짝으로 풀고 있었다**: `OrderQuery.Detail` 이 `NON_NULL` 과 `_visible_field_groups` 를 같이 쓴다 — **키가 없을 때 그 목록이 「못 봄」과 「값 없음」을 가른다.** 같은 꼴로 맞췄다. `bodyVisibleTo` 가 `boolean` 하나만 돌려주던 것을 `Visibility`(본문 가림 + 그룹 목록) record 로 바꾸고 호출부 셋을 따라갔다. 그룹 목록은 `VisibleFieldGroups.of(decision, InquiryFields.values())` 로 형제와 같은 헬퍼를 쓴다. 화면 타입도 `question?`·`answer?` 로 고쳤다. **부순 증거**: `Visibility` 의 본문 가림을 늘 참으로 바꾸니 **기존 마스킹 테스트 둘**(「감사자에게 글을 안 연다」·「감사자는 목록에서 본문을 못 본다」)이 빨갛다 — 새 배선이 옛 규칙을 그대로 태우고 있다는 증거다 | 완료 |
| Q76 | ~~소비자가 `EventType` 대신 생 문자열을 든다~~ | **`점검 O` 가 찾았다**(2026-09-17). `coding-rules.md:356` 이 「enum 으로 둔다. 생 문자열을 쓰지 않는다 — 강제 지점을 1위(타입)까지 올리는 자리」인데 `NotificationConsumer:55-56` 이 `"shop.order.status_changed"`·`"shop.refund.status_changed"` 를 `private static final String` 으로 **다시 적는다**. 같은 값이 `support/EventType:25,28` 에 이미 있다. 원인은 가시성이다 — `EventType` 이 package-private 이라 `notification` 에서 타입으로 못 받는다. **그 enum 의 javadoc 도 같이 낡았다**: 「Java 는 이 값을 안 쓴다 … 여기서 부르는 자리가 없다」고 적혀 있는데 어제 소비자가 쓰기 시작했다. **절차**: (1) `EventType` 을 `public` 으로 올린다 — `coding-rules.md` 「`public` 에는 근거가 있어야 한다」에 맞춰 **왜 올리나**를 javadoc 에 적는다 (2) `NotificationConsumer` 의 상수 둘을 `EventType.ORDER_STATUS_CHANGED.code()` 로 바꾼다. `switch` 를 쓰면 `default` 를 안 둔다(`D23`) (3) javadoc 의 「Java 는 이 값을 안 쓴다」를 사실로 고친다 (4) `PublicSurfaceTest` 목록에 더한다. **축**: 규약(`D23` 열거값 · `public` 근거). **강제 지점**: 타입(1위). **건드리는 자리**: `EventType` · `NotificationConsumer` · `PublicSurfaceTest`. **완료**(2026-09-17). `EventType`·`NotificationConsumer`. **사용자가 (가) 를 골랐다** — 열거형을 `public` 으로 연다. `D23` 「`public` 에는 근거가 있어야 한다」가 가르는 기준이 **패키지 밖에서 실제로 쓰나**고, 소비자가 이 값으로 분기하니 그 기준을 지난다. (나)(소비자 폴더에 받을 사건만 따로 두기)는 **사본이 enum 으로 모양만 바꾼 것**이라 뺐다 — 갈리는 것을 막으려면 대조 테스트를 또 세워야 한다. (다)(설정으로 빼기)는 타입이 사라져 규약과 반대 방향이라 뺐다. **`switch (type)` 를 못 쓴다** — `case` 라벨은 컴파일 상수여야 하는데 `code()` 는 호출이다. 위에서 이미 둘로 거르므로 `EventType.of(type)` 으로 되돌린 뒤 셋째 항이 없는 갈래로 바꿨다(`default` 를 안 둔다, `D23`). **모르는 값이 거기 오면 터지는 것이 맞다** — 헤더와 목록이 어긋났다는 뜻이고 조용히 0을 돌려주면 통지가 사라진 것을 아무도 모른다. **javadoc 의 「Java 는 이 값을 안 쓴다」도 사실로 고쳤다.** **부순 증거**: `REFUND_STATUS_CHANGED` 의 저장값을 바꾸니 `EnumConstraintTest` 가 빨갛다. **요지는 소비자가 따라간다는 것**이다 — 그전에는 열거형만 고치면 소비자가 옛 문자열에 남아 **아무것도 안 깨진 채로** 통지가 멈췄다 | 완료 |
| Q77 | ~~남이 발급한 번호가 `_number` 를 쓴다 — `payment.approval_number`·`refund.gateway_refund_number`~~ | **`점검 O` 가 찾았다**(2026-09-17). `naming-rules.md` 「접미사」가 `_number` 를 「**우리가 발급해서** 바깥이 부르는 번호」, `_no` 를 「남이 발급한 번호」로 갈랐는데 둘 다 결제 대행사가 준 값이다 — `LengthConstraintTest:149,152` 가 「결제 대행사가 준 값이다. 우리가 상한을 정하지 않는다」고 **직접 적어 뒀다**. 규칙대로면 `_no` 다. 게이트도 한 방향뿐이다: `SchemaNamingTest.internalSequenceSuffixIsPinned` 가 `_no` 쪽만 핀하고 `_number` 쪽은 아무것도 안 묻는다. 예외 절은 `identifier-rules.md`·`naming-rules.md` 둘 다 0건이다. **갈림길이라 사용자에게 낸다**: 컬럼을 고칠지, 규칙에 예외를 적을지. **되돌리기 비용이 비대칭이다**(`D23`) — 노출 번호가 응답 계약에 실리면 값이 오른다. **절차**: (1) 고른다 (2) 고치기로 하면 새 `V` 로 이름을 바꾸고 `ExposedNumber` 계열 테스트와 응답 record 를 따라간다 (3) `SchemaNamingTest` 에 `_number` 쪽 방향을 더한다 — 「`_number` 면 우리가 발급한다」를 무엇으로 재나까지 정한다 (4) 문서에 결론을 적는다. **축**: 규약(`D22`·`D9`). **강제 지점**: 테스트(4위)를 양방향으로. **건드리는 자리**: 결정에 따라 갈린다. **완료**(2026-09-17, `Q78` 과 한 묶음). **사용자가 (가) 를 골랐다** — 규칙에 예외를 적는다. 근거는 되돌리기 비용의 비대칭이다(`D23`): `approvalNumber` 가 이미 주문 응답(`OrderQuery.Payment`)과 거래기록 텍스트의 「승인번호」에 실려 있어서, 이름을 바꾸면 응답 계약과 서식이 같이 움직인다. **새 컬럼은 이 예외를 못 쓴다** — 아직 아무것도 안 나갔으면 `_no` 가 싸다. `SchemaNamingTest.foreignNumbersArePinned` 가 `_number` 마다 「우리가 발급하나」를 묻고, 아니면 근거가 적혀 있어야 한다. **그전까지 `_number` 쪽은 아무것도 안 물었다** — `_no` 만 핀하고 있었다. **부순 증거**: `FOREIGN_NUMBERS` 에서 `payment.approval_number` 를 빼니 그 이름을 짚고 빨갛다 | 완료 |
| Q78 | ~~접미사 표가 스키마를 못 따라갔다 — `_kind`·`_amount`·`_days`·`_date`~~ | **`점검 O` 가 찾았다**(2026-09-17). `naming-rules.md` 「이 문서를 고칠 때」가 「여기 없는 접미사를 쓰게 되면 **그 접미사를 여기 먼저 추가한다**」인데 표에 없는 채로 들어간 것이 여럿이다: `_kind`(`batch_run.failure_kind`·`notification.kind`·`inquiry.kind`·`settlement_item.kind`) · `_amount`(여섯) · `_days`(`agreed_lead_days`·`supply_lead_days`) · `_hash`·`_memo`·`_note`. **`_type` 과 `_kind` 가 같은 뜻으로 둘이다** — `_type` 은 표에 「종류」로 있다. 둘째로 같은 문서 「기준에서 벗어난 것」 2번이 「`_date` 는 날짜형 이름인데 **우리는 전부 `timestamptz` 다**」라고 적었는데 사실이 아니다 — `V38:27` `baseline_date date`, `V52:28` `payout_date date` 가 있다. **막는 자리가 없다** — `SchemaNamingTest` 는 소문자·길이·`_at`·`is_`·기본키만 보고 접미사 표 소속은 안 잰다. **절차**: (1) `_type` 과 `_kind` 를 하나로 모을지 뜻으로 가를지 정한다 — **사용자 선택지**. 모으면 마이그레이션이 붙는다 (2) 표에 빠진 접미사를 근거와 함께 더한다 (3) 「전부 `timestamptz`」를 사실로 고치고 **`_date` 를 언제 쓰나**를 정한다(시각이 아니라 날짜가 뜻인 칸) (4) `SchemaNamingTest` 에 `columnSuffixesAreInTheTable` — 컬럼 이름의 마지막 토막이 표에 있거나 통과 목록에 근거와 함께 있다. **축**: 규약(`D22`). **강제 지점**: 테스트(4위) — 이름은 제약으로 못 막는다. **건드리는 자리**: `naming-rules.md` · `SchemaNamingTest`. **완료**(2026-09-17, `Q77` 과 한 묶음). **사용자가 (가) 를 골랐다** — 뜻으로 가른다. `_type` 은 **그 컬럼이 가리키는 상대**의 종류(`actor_type`·`target_type`·`event_type`)고 `_kind` 는 **그 행 자체**의 종류(`notification`·`inquiry`·`settlement_item`·`batch_run.failure_kind`)다. 이미 그렇게 갈려 쓰이고 있어서 규칙으로 못 박은 것이고 마이그레이션이 0이다. 표에 `_kind`·`_amount`·`_days`·`_date`·`_hash`·`_note`·`_memo` 를 더했다. **「우리는 전부 `timestamptz` 다」가 사실이 아니었다** — `holiday_date`·`baseline_date`·`payout_date` 가 `date` 다. 문서를 사실로 고치고 **`_date` 를 언제 쓰나**(시각이 아니라 날짜가 값의 뜻인 칸)를 셋의 근거와 함께 적었다. **게이트 모양이 예보와 달랐다**: 분할표는 「컬럼 이름의 마지막 토막이 표에 있나」였는데 그러면 `card_issuer`·`source_url` 같은 평범한 명사까지 위반이 된다 — **타입을 약속하는 접미사 셋**(`_at`·`_date`·`_bp`)만 재는 것으로 좁혔다. **부순 증거**: `_date` 의 기대를 `timestamptz` 로 바꾸니 날짜 컬럼 셋을 이름까지 짚는다 | 완료 |
| Q79 | ~~화면이 권한을 세 가지 방식으로 가른다 — 하나는 안 본다~~ | **`점검 O` 가 찾았다**(2026-09-17). `screen-rules.md:221` 이 「할 수 없는 조작은 화면에 안 그린다. `8a` 가 내려주는 권한 목록으로 판단한다」인데 셋이 서로 다르다: `seller/orders/[sellerOrderNumber]/page.tsx:163` 은 서버가 준 `allowedActions` 로 · `settlements/[settlementNumber]/page.tsx:72-76` 은 `/api/me/permissions` 로 · **`seller/inquiries/page.tsx:96-98` 은 `item.status === "RECEIVED"` 만 보고 권한을 아예 안 본다**. 지금 안 새는 이유는 부여표가 우연히 막아서다 — `V54:57-60` 이 `inquiry:answer` 를 `seller_owner` 에만 허용하고 `:84-87` 이 `auditor` 에 거부다. **부여가 바뀌면 조용히 샌다.** **절차**: (1) 셋 중 하나로 규약을 정한다 — 서버가 줄지(`allowedActions`) 화면이 물을지(`/api/me/permissions`). **사용자 선택지**(응답 계약이 갈린다) (2) `seller/inquiries` 를 그 방식으로 고친다 (3) `screen-rules.md` 에 고른 방식과 **왜 둘을 안 섞나**를 적는다 (4) 게이트를 볼 수 있으면 lint 로 — 「조작 버튼을 그리는 자리는 권한 값을 참조한다」를 `eslint.config.mjs` 에서 잴 수 있는지 보고, 못 재면 왜 못 재는지 적는다. **축**: 규약(`D20`·`D24`) + `D14` 스코프. **강제 지점**: 지금 없다 → 응답 계약(1위)이나 lint(4위)로. **건드리는 자리**: 화면 하나 · `screen-rules.md` · (가능하면) `eslint.config.mjs`. **완료**(2026-09-17). `InquiryQuery`·`seller/inquiries/page.tsx`·`InquiryVisibilityTest`·`screen-rules.md`. **사용자가 (가) 를 골랐다** — 한 자원의 조작은 그 자원 응답의 `allowedActions`, 전역 메뉴만 `/api/me/permissions`. 근거는 **권한만으로 안 갈린다**는 것이다: 답변은 권한·상태 둘이 같이 걸리고 서버가 이미 한자리에서 본다. 화면이 다시 보면 규칙이 두 벌이 된다. **`InquiryQuery.Entry` 에 `allowedActions` 를 더했다**(응답 계약 1위). 셀러 목록은 `evaluator.decide(viewerId, "inquiry", "answer", ofSeller)` 를 **한 번** 묻고(스코프가 셀러라 행마다 물어도 결과가 같다) 행마다 상태만 본다. 화면은 `item.allowedActions.includes("ANSWER")` 로 바뀌었다. **`findAll`(관리자·감사자)은 조작을 안 싣는다** — 답변 입구가 아니라서고, 그것도 테스트로 고정했다. **게이트는 화면 쪽에 못 걸었다**: 「조작 버튼이 권한 값을 참조하나」는 버튼인지가 JSX 모양으로 안 갈려서 lint 로 못 잰다. 대신 서버 쪽에 「권한이 없으면 빈 목록」을 고정했고 **왜 못 재는지를 `D20` 에 적었다**. **부순 증거**: 권한 상수를 없는 이름으로 바꾸니 `ANSWER` 가 안 실려 빨갛다. **첫 부순 증거는 증거가 아니었다** — `canAnswer` 를 빼도 그 시나리오의 셀러는 권한이 있어서 결과가 같았다 | 완료 |
| Q80 | ~~마스킹과 권한을 **재는 자리**가 비어 있다 — record 접근자만 본다~~ | **마무리 21차 독립 리뷰가 둘을 짚었다**(2026-09-17). ① `Q75` 가 말한 강제 지점이 「애노테이션 + 테스트」인데 새 테스트 넷이 전부 **record 접근자**만 본다 — `@JsonInclude(NON_NULL)` 을 떼도 넷이 초록이라 **애노테이션을 재는 자리가 없다**. 분할표 절차 (3)이 「못 보는 칸은 키가 없다를 재는 테스트」였는데 안 섰다 ② `Q79` 가 `screen-rules.md` 에 「권한이 없으면 빈 목록을 테스트가 고정한다」고 적었지만, 그 테스트가 재는 `findAll` 은 `canAnswer` 에 **상수 `false`** 를 넘기는 경로다 — **권한만 없는 셀러**로 재는 자리가 없다(문장은 마무리에서 사실대로 고쳤다). **절차**: (1) `InquiryVisibilityTest` 에 직렬화 대조 — `ObjectMapper` 로 `Entry` 를 찍어 **감사자 응답에 `question` 키가 없고** `_visible_field_groups` 에 `body` 가 없다, 셀러 응답에는 키가 있다. 그래야 애노테이션이 걸린다 (2) **권한만 없는 셀러 fixture** — `inquiry:read` 는 있고 `inquiry:answer` 는 없는 역할을 만들어 `findForSeller` 가 `allowedActions` 를 빈 목록으로 주는지 잰다. `V54` 의 부여를 안 고치고 테스트 안에서 역할을 만든다 (3) `screen-rules.md` 의 그 절에서 `Q80` 참조를 지우고 재는 것을 적는다. **축**: 규약(`D20`·`D23` 나가는 사다리 · `D15`). **강제 지점**: 테스트(4위) — 애노테이션은 붙어 있으나 **붙었는지 재는 것**이 없다. **건드리는 자리**: `InquiryVisibilityTest` · `screen-rules.md`. **완료**(2026-09-17). `InquiryVisibilityTest`(테스트 셋 신설) · `screen-rules.md` · `quality-gates.md`. **직렬화로 재야 애노테이션이 걸린다** — 접근자가 `null` 인 것과 응답에 키가 없는 것은 다른 사실이다. 웹 컨텍스트를 안 띄우는 테스트라 앱의 `ObjectMapper` 빈이 없어서 모듈만 찾아 붙인 것을 쓴다(키 유무만 보므로 표기 설정이 결과를 안 바꾼다). **권한만 없는 상태를 만들어 쟀다** — `role_permission` 에 `inquiry:answer` 의 `seller`/`deny` 행을 넣는다. 기본키가 효과까지 포함이라 허용과 거부가 같이 살고 거부가 이긴다. 그전에는 전체 목록으로 쟀는데 그쪽은 `canAnswer` 에 상수를 넘기는 경로였다. **부순 증거**: `@JsonInclude(NON_NULL)` 을 떼니 빨갛다 — **그전에는 떼도 전부 초록이었다** | 완료 |
| Q81 | ~~정산 지급 버튼이 규약과 다르게 권한을 묻는다~~ | **마무리 21차 독립 리뷰가 짚었다**(2026-09-17). `Q79` 가 `screen-rules.md` 에 「**한 자원의 조작**은 그 자원 응답의 `allowedActions`, 전역 메뉴만 `/api/me/permissions`」로 못 박았는데, `seller/settlements/[settlementNumber]/page.tsx:72` 의 지급 버튼은 **자원 조작인데 `/api/me/permissions`** 로 그린다. `payout-actions.tsx` 도 같은 자리다. **규약을 쓴 그 묶음 안에 위반이 남았다** — `Q79` 는 문의 하나만 고쳤다. **절차**: (1) 정산 상세 응답(`SettlementQuery`)에 `allowedActions` 를 더한다 — 문의와 같은 꼴로 `evaluator.decide(viewerId, "settlement", <동작>, Target.ofSeller(...))` 와 **지급 상태**를 같이 본다(`D7` 「정산 지급 상태」가 전이를 정했다) (2) 화면 둘을 그 값으로 바꾸고 `/api/me/permissions` 호출을 뺀다 (3) 남는 `/api/me/permissions` 사용처가 헤더·메뉴뿐인지 확인한다 — 아니면 그 자리도 같이 센다 (4) `InquiryVisibilityTest` 와 같은 꼴의 테스트. **축**: 규약(`D20`·`D24`) + `D14` 스코프 + `D7`. **강제 지점**: 응답 계약(1위). **건드리는 자리**: `SettlementQuery` · 화면 둘 · 테스트. **완료**(2026-09-17). `SettlementQuery`·`payout-actions.tsx`·`seller/settlements/[settlementNumber]/page.tsx`·`payout-actions.test.tsx`·`SettlementPayoutTest`·`screen-rules.md`·`quality-gates.md`. **사용자가 (가) 를 골랐다** — 서버가 권한·상태·**요청자**를 다 본다. **셋째가 이 청크의 값이다**: 화면 주석이 「자기가 올린 지급은 자기가 승인 못 하는데 누가 올렸는지가 응답에 없어서 버튼이 뜨고 누르면 403 이 온다」를 **이미 적어 두고 있었다** — 서버로 옮기니 그 결함이 같이 사라졌다. `Detail` 에만 싣는다(`Summary` 는 목록이 같이 쓰고 버튼은 상세에만 있다). **화면의 상태 표(`stateAllows`)와 권한 대조를 걷어냈고** 그 테스트 여덟도 서버로 옮겼다 — 화면에 남은 것은 이름을 버튼으로 바꾸는 것과 확인·알림 흐름뿐이다. **함정 하나**: `PayoutStatus.of` 는 저장값(소문자)을 받는데 응답값은 `EnumValue` 가 이미 대문자로 바꾼 뒤라 `valueOf` 를 쓴다. **부순 증거**: 자기 승인 금지 조건을 빼니 「올린 사람에게는 결정이 안 난다」가 빨갛다 | 완료 |
| Q83 | ~~문서·주석이 부르는 이름이 실재하는지 기계가 잰다~~ | **하루에 세 번 났다**(2026-09-17): 응답에 애노테이션을 걸고 같은 record 를 쓰는 다른 화면을 안 고쳤고(마무리 21차), 컬럼을 `read_only` → `is_read_only` 로 바꾸고 그 이름을 부르는 문서 둘을 안 고쳤고(마무리 22차), 없는 테스트 클래스를 주석이 가리켰다(PR #50 리뷰). **코드와 테스트는 이미 걸린다** — 컴파일과 빨간 테스트가 잡는다. **안 걸리는 층이 문서와 주석**이다. **완료**(2026-09-17). `IdentifierReferenceTest`(신설, 느린 레인) · `build.gradle.kts` · 문서 다섯 · `quality-gates.md` · `testing-strategy.md` · 마무리 스킬의 대조 표. **표 이름으로 닻을 내렸다** — `x.y` 를 다 잡으면 128개가 걸리는데(`api.ts`·`github.ref`·`axe.run`) `x` 가 실재하는 표일 때만 보면 97개로 줄고 남는 잡음은 감사 사건 이름 둘(`role.granted`·`permission.denied`)뿐이라 근거와 함께 통과 목록에 뒀다. **세울 때 실물로 여섯이 걸렸다**: `consent_item.depends_on`(→`_id`) · `order_status_history.reason`(→`_note` 표로 갈렸다) · `product.commission_rate`·`seller.commission_rate`(→`commission_bp`) · `refund.request_reason`(→`refund_note`) · **`settlement.payout_date`(그날 `Q78` 이 접미사 표에 넣은 것 — 그 컬럼은 `settlement_cycle` 에 있다)**. `data-lifecycle.md` 의 「자유 텍스트 넷은 아직 판단이 안 끝났다」 문단도 낡아서 다시 썼다 — `5i-2`·`5i-3` 이 이미 갈랐다. **게이트가 처음엔 안 돌았다** — 문서·javadoc 만 고치면 클래스 파일이 같아서 `integrationTest` 가 `UP-TO-DATE` 로 건너뛴다(`Q25` 가 세 번 밟은 자리). `comparedInSlowLane` 에 `doc/reference` 폴더와 `src/main/java` 를 걸어서 풀었다. **부순 증거**: `settlement.payout_date` 와 없는 테스트 이름을 도로 넣으니 둘 다 이름을 짚고 빨갛다 | 완료 |
| Q84 | ~~본문 없이 상태만 내보내는 오류 셋~~ | **마무리 24차 독립 리뷰가 찾았다**(2026-09-17). 필터가 `response.setStatus` 로 끝내는 자리 셋이 **RFC 9457 본문을 안 내보낸다**(`D5` 가 오류에 그것을 요구한다): 죽은 계정 401(`AccountLivenessFilter`) · 동시접속으로 끊긴 세션 401(`ConcurrentSessionFilter`, `Q63` 이 그 모양으로 뒀다) · **인가 거부 403**(`SecurityConfig` 가 `authenticationEntryPoint` 만 걸어서 스프링 기본 `AccessDeniedHandler` 의 `sendError` 로 나간다). **지표보다 본문이 먼저 문제다** — 받는 쪽이 `trace_id` 도 `type` 도 못 받고, 덤으로 `shop.error.raised`(청크 62)에도 안 잡힌다. **절차**: (1) 셋이 `ProblemFactory` 를 지나게 한다 — 필터 둘은 `ProblemEntryPoint` 와 같은 꼴로 본문을 쓰고, 403 은 `accessDeniedHandler` 를 걸어 준다 (2) `ErrorCode` 에 없는 것이 있으면 세운다(죽은 계정·세션 만료가 `UNAUTHENTICATED` 와 같은 코드여도 되나를 정한다 — **갈림길**) (3) `MetricsExposureTest` 나 새 테스트가 **그 셋의 본문과 코드**를 잰다 — 지금은 상태 코드만 보고 지나간다(`:59` 의 403 이 그 자리다). **축**: 규약(`D5` 오류 형식 · `D16`). **강제 지점**: 응답 계약 + 테스트. **건드리는 자리**: `AccountLivenessFilter` · `SecurityConfig` · `ErrorCode` · 테스트. **닫힘**: 오류 응답 전부가 RFC 9457 본문을 갖고 `shop.error.raised` 에 잡힌다. **완료**(2026-09-18) — `ProblemWriter` 를 세워 네 자리가 같은 본문을 쓴다(진입점·죽은 계정·밀려난 세션·인가 거부). 코드 셋을 새로 뒀다(`account-inactive`·`session-superseded`·`access-denied`) — **받는 쪽이 갈라 대응해서다**: 죽은 계정은 다시 로그인해도 소용없고, 밀려난 세션은 다시 로그인하면 되며, 인가 거부는 로그인이 맞는데 권한이 없다. **부순 증거**: `accessDeniedHandler` 를 떼니 `ProblemResponseTest.FilterCut` 이 403 대신 **본문 없는 응답**을 보고 빨개졌다 | 없음 |
| Q85 | ~~청크 순서를 배포 결승선에 맞춰 재조정한다~~ | **완료 — 「구간」 절을 세웠다**(분할표 바로 앞). 사용자가 「지금 좀 멋대로일 수 있으니 순서부터 재조정할 방향성」을 물었고, 재 보니 **규칙 위반이 아니라 규칙의 전제가 바뀐 것**이었다 — 분할표는 끝 없는 프로젝트 전제로 정렬돼 있는데 **포폴로 쓸 배포가 결승선으로 들어왔다.** 미착수 49를 성격으로 가르니 **기능 줄 26 · 운영 9 · 공부 9 · 잔챙이 4** 인데 그때까지 친 것이 거의 다 아래 셋이었다. **선택지 넷을 장단점·공통점 표로 폈고 사용자가 「다」(최소 얼굴만 치고 배포)를 골랐다.** 구간은 다섯이고 **축 넷을 대체하지 않는다** — 구간 사이의 차례일 뿐 구간 안에서는 기존 넷이 그대로다. **그 자리에서 배포의 진짜 구멍을 찾았다**: 기본 프로필이 `db/seed` 를 안 읽고(`application.yml:9`) `DemoOrderSeeder` 가 `@Profile("local")`(`:50`) 이라 **올리면 빈 쇼핑몰이 뜬다** — `Q39` 절차 일곱 단계에 그 얘기가 없었다. `64` 의 덤프가 그 도구를 겸하게 두어 ②로 넣었다. **강제 지점은 문서(5위)다** — 순서는 사람이 고르는 것이라 기계가 「틀린 순서」를 판정 못 한다. **검증**: `doc-lint.sh` 통과 · `verify.sh` 초록 | 완료 |
| Q86 | ~~Dependabot 경보 #6 — `lz4-java` 를 고친 판에 고정한다~~ | **완료** — `backend/build.gradle.kts` 에 `constraints` 블록 신설(1.10.1 → **1.11.1**). `at.yawk.lz4:lz4-java` 는 **전이 의존**이다(`spring-boot-starter-kafka:4.1.1` → **`spring-boot-kafka:4.1.1`** → `spring-kafka:4.1.1` → `kafka-clients:4.2.1`. 2026-09-18 에 `dependencyInsight` 로 다시 읽었다 — 한 홉이 빠져 있었다). 네이티브 XXHash 가 **잘못된 배열 범위를 받으면 JVM 을 죽인다**(<= 1.11.0, medium). **우리가 그 길을 밟나 — 지금은 아니다**: `compression.type` 을 아무 데도 안 정해서 카프카 기본값 `none` 이고 LZ4 코덱이 안 불린다. **그래도 고정했다** — 압축을 켜는 날 이 경보를 다시 만나는 것보다 한 줄이 싸고, 스프링 BOM 이 올라가면 **이 제약이 저절로 무의미해진다**(낮은 쪽이 진다). **축**: 관례(의존 관리) + `D14`(알려진 취약점을 켜 둔 채로 안 둔다). **강제 지점**: 빌드다 — 다만 `constraints` 는 **고정이 아니라 하한**이다(2026-09-18 정정). `dependencyInsight` 의 선택 사유가 「By constraint」 + 「By conflict resolution: between versions 1.11.1 and 1.10.1」이라, **1.10.1 로 내려가는 길은 막히고 더 높은 판이 오면 그쪽이 이긴다.** 보안 고침이라 그 방향은 문제가 아니다. **닫힘**: `dependencyInsight --dependency lz4-java` 가 1.11.1 을 낸다. **`verify.sh --full` 초록은 근거가 아니다**(2026-09-18 정정) — `compression.type` 을 안 정해서 카프카 기본이 `none` 이고, 그래서 **시험이 lz4 코드 경로를 아예 안 밟는다.** 1.10.1 이어도 초록이었다 | 완료 |
| Q87 | ~~구간 표가 미착수 청크를 다 담나를 기계가 본다~~ | **완료 — 마무리 25차 독립 리뷰의 셋째·넷째 지적이다**(2026-09-17). 「구간」 절을 세운 날 **49 중 39만 담겼고**, 「현재 상태」가 순서를 그 표에 넘긴 뒤라 남은 열은 **아무 문서도 차례를 안 드는 상태**였다. 그중 `Q84` 는 바로 앞 묶음이 낸 행이다. 리뷰가 「기계가 이미 잴 수 있다」고 짚었고 실제로 그랬다. **축**: 프로젝트 규약(계획 체계의 일관성). 법·표준이 아니다. **강제 지점**: 테스트(4위) — `doc-lint.sh` 에 집합 차 검사. 문서라 타입·제약으로는 못 내린다. 행 판정은 `plan_open_rows()` 로 묶어 칸 누락 검사와 **한 벌을 쓴다**. **부순 증거 둘**: 넣자마자 열여섯을 찍었고(리뷰가 센 열 + 범위 표기 뒤에 숨은 `16a`·`16b`·`16c`·`30`·`47`·`50`), 표를 고치다 ① 행을 흘렸을 때 **훅이 그 자리에서 잡았다**. **범위 표기를 낱개로 폈다** — 사람이 읽는 표와 기계가 읽는 표가 갈리면 기계가 헛것을 센다. **건드리는 자리**: `scripts/doc-lint.sh` · `PLAN.md` 「구간」 표(열여섯 배치 + 「나」를 뺀 근거 정정). **닫힘**: `doc-lint.sh` 가 초록이고, 구간 표에서 아무 번호나 지우면 그 번호를 찍고 빨개진다 | 완료 |
| Q88 | 문서만 고친 청크에서도 표 대조 시험이 돈다 | **`Q87` 이 치다 드러났다**(2026-09-18). `PlanProgressConsistencyTest` 는 `PLAN.md`·`PROGRESS.md` 를 읽는데 **`verify.sh` 의 레인 지문은 코드·빌드 파일만 본다**(`2z-1`, 의도된 것이다). 그래서 문서만 고친 청크는 「돌릴 것이 없다」로 초록 도장을 받고, 표가 어긋난 것은 **push 뒤 CI 에서야** 빨개진다. 지문에 두 파일을 더하면 **모든 청크가 backend 레인을 끌고 온다** — 청크마다 딸려 오는 파일이라 레인 판정이 무의미해진다. **축**: 프로젝트 규약(검증 배관). **강제 지점**: 테스트(4위) — `migration-immutable.sh` 처럼 **레인 판정 밖** 자리에 두고, 두 파일이 `origin/main` 과 다를 때만 그 시험 하나를 돌린다. **건드리는 자리**: `scripts/verify.sh` 의 레인 판정 앞 구간. 시험은 안 고친다. **닫힘**: `PROGRESS` 이력만 완료로 적고 `PLAN` 행을 안 닫은 채 `verify.sh` 를 돌리면 빨갛고, 코드만 고친 청크에서는 그 시험이 안 돈다 | 0 |
| Q89 | ~~차례를 구간 표 하나가 들게 한다~~ | **완료 — 마무리 25차 독립 리뷰의 둘째 덩어리다**(2026-09-17). `Q39` 를 맨 끝에서 구간 ②로 옮겼는데 **그 말을 베껴 둔 자리들이 안 따라왔다.** 사본은 원본이 바뀌어도 안 따라오므로 **차례의 주인을 「구간」 표 하나로 못박았다.** **축**: 프로젝트 규약(「글 작성 규칙」 1번 — 중복 금지). **강제 지점**: 테스트(4위) — `doc-lint.sh` 에 「순서 단어 + 청크 번호가 한 줄에 있나」. 스캔 밖은 둘뿐이다 — 구간 표(**주인**)와 `PROGRESS.md` 「이력」(**그때는 맞았던 기록**이라 고치면 서사가 거짓이 된다). **예외 구멍은 안 팠다** — 끝난 일은 과거형으로 고치면 순서 단어가 안 남는다. **부순 증거**: 넣자마자 다섯을 찍었고 그중 `coding-rules.md:731`(끝난 `Q36` 을 아직 미룬 것처럼 적은 자리)은 **리뷰가 못 본 것**이다. `「」` 안을 안 걷으면 이 저장소의 기록 문장 자체가 걸려서 걷어낸다(존댓말 검사와 같다). **건드리는 자리**: `scripts/doc-lint.sh` · `README.md:45`(포폴 독자가 읽는 유일한 입구) · `coding-rules.md:731` · `PLAN.md` `Q39` 행 첫 문장과 **선행 칸**(`마지막` → `64`. 본문만 「`64` 가 실질 선행」이라 하고 선행 칸엔 없었는데, **재개 절차는 선행 칸만 읽는다**) · `PROGRESS.md` 호스팅 칸·「그다음 코드 청크」 칸(`Q36` 은 2026-09-14 에 닫혔다). **닫힘**: `doc-lint.sh` 가 초록이고, 아무 문서에 「맨 마지막」과 청크 번호를 한 줄에 적으면 빨개진다 | 완료 |
| Q90 | ~~낡은 「멀다·조건 미달」 표기 둘을 고친다~~ | **완료 — 마무리 25차 리뷰가 남긴 자리다**(2026-09-18에 쳤다). `document-map.md:176` 이 「남은 것은 `D17`·`D21` 둘이고 전부 조건 미달」이라 하는데 **`D17` 은 구간 ①의 첫 손이 됐다**(`Q85` 가 순환으로 판정했다). `security-baseline.md:336` 은 「청크 `26` 이 멀어서 지금 정하면 무엇을 정하는지 모른다」는데 **`26` 은 ①의 둘째다.** 두 문장이 **`D17` 을 못 쓰게 막는 근거로 읽힌다** — 그래서 `D17` 보다 먼저 친다. **축**: 프로젝트 규약(기준 문서의 조건 표기). **강제 지점**: 없다 — 문장이 낡은 것이라 기계가 뜻을 못 읽는다. 대신 `D17` 을 쓰는 청크가 이 두 줄을 입력으로 삼는다. **건드리는 자리**: `doc/reference/document-map.md` · `doc/reference/security-baseline.md`. **닫힘**: 두 줄이 「구간 ①이 순환을 깬다」를 가리키고, `D17` 청크가 그 문장을 근거로 열린다 | 완료 |
| Q91 | ~~호스팅 문서에 시드 단계를 적는다~~ | **마무리 25차 리뷰가 찾았다**(2026-09-17). `Q39` 절차 (4-1)에만 있고 `backend/README.md` 「호스팅에 올릴 때」에는 **데모 데이터를 붓는 단계가 없다.** 기본 프로필은 `db/seed` 를 안 읽고(`application.yml`) `DemoOrderSeeder` 가 `@Profile("local")` 이라, 그 문서만 보고 올리면 **빈 쇼핑몰이 뜬다.** **축**: 프로젝트 규약(실행 문서는 실행되는 순서를 든다). **강제 지점**: 없다 — 배포 절차라 저장소에 값이 없다. `64` 의 덤프·복구 스크립트가 그 단계의 도구고, 그것이 생긴 뒤에 적어야 이름이 맞는다. **건드리는 자리**: `backend/README.md` 「호스팅에 올릴 때」. **닫힘**: 그 절만 읽고 올려도 상품·셀러·주문이 0이 아니다. **완료**(2026-09-18) — `backend/README.md` 「올리고 나면 빈 쇼핑몰이 뜬다」. 세 줄짜리 순서 표고, 부을 도구는 `64` 의 스크립트 둘이다. **「올리기 직전에 뜬다」를 같이 적었다** — 뜬 뒤에 마이그레이션 파일을 고치면 같은 덤프가 더는 안 올라간다(`64` 가 실측으로 밟았다) | 64 |
| Q92 | ~~곧 칠 행은 칸 넷이 다 차 있어야 한다~~ | **완료 — 마무리 25차 잔지적 첫째다**(2026-09-17). `64` 가 `Q39` 몫을 흡수하며 범위가 늘었는데 **닫힘 칸이 빈 채로 지나갔다** — 「분할표 칸 누락」이 **개수 래칫**이라 기준선 안에 이미 든 행은 더 망가져도 안 걸린다. **축**: 프로젝트 규약(분할표 행은 칸 넷을 든다, `2t`). **강제 지점**: 테스트(4위) — `doc-lint.sh` 에 **구간 ①·② 한정 0 기준** 검사. 49행 전부에 걸면 44를 0으로 내리는 큰 일이 되고 **안 지켜지는 규칙은 없는 규칙**이라, 차례가 잡힌 것부터 채운다. **행이 ③으로 물러나면 검사에서도 빠진다** — 곧 안 칠 것에 닫힘을 미리 쓰면 무엇을 닫는지 모르는 채로 쓰게 된다. **부순 증거**: 넣자마자 **일곱**(`D17`·`26`·`27`·`28`·`71`·`64`·`65`) — 리뷰가 손으로 찾은 `64` 는 그중 하나였다. 그 일곱의 닫힘을 이 청크가 썼고 **래칫 기준선이 44 → 37 로 내려갔다**. **함정**: `awk` 의 `[①②]` 문자 클래스가 멀티바이트라 안 먹어서 49행이 다 걸렸다 — 리터럴로 바꿨다(`stack.md` 의 `perl -CSD` 항목과 같은 갈래다). **건드리는 자리**: `scripts/doc-lint.sh` · `PLAN.md` 일곱 행의 닫힘 칸. **닫힘**: `doc-lint.sh` 가 초록이고, 구간 ①·② 행에서 닫힘 칸을 지우면 그 번호를 찍고 빨개진다 | 완료 |
| Q93 | ~~마무리 25차 잔지적 넷과 낮은 확신 둘을 처분한다~~ | **완료 — 마무리 25차를 닫는 청크다**(2026-09-17 지적, 2026-09-18 처분). **넷을 고쳤다**: ① `Q86` 의 전이 경로에 **`spring-boot-kafka` 한 홉이 빠졌다** — `dependencyInsight` 로 다시 읽어 네 홉을 다 적었다 ② **`constraints` 는 고정이 아니라 하한이다** — 선택 사유가 「By constraint」 + 「By conflict resolution」이라 1.10.1 로 내려가는 길만 막힌다. 「안 지킬 방법이 없다」를 그렇게 고쳤다 ③ **`Q86` 닫힘의 둘째 근거를 지웠다** — `compression.type` 을 안 정해서 카프카 기본이 `none` 이고, 시험이 lz4 경로를 안 밟아서 **1.10.1 이어도 초록이었다** ④ **성격별 합이 48이었다** — 다시 세니 잔챙이가 4가 아니라 5였고(26+9+9+5=49), 이 수가 청크마다 움직이는 **스냅숏**이라는 것을 같이 적었다. **낮은 확신 둘도 실물이었다**: 배포 기준점 뒤로 **`db/seed` 가 같이 얼어붙는 것**(`migration-immutable.sh:18`)을 `Q39` 행에 흡수했고 — 뒤 구간은 새 파일을 더해야 한다 — `CLAUDE.md` 에 **순환이면 깬다** 예외와 「무엇을 먼저 칠지 갈릴 때 → 구간 표」 길을 더했다. **축**: 프로젝트 규약. **강제 지점**: 없다 — 산문의 사실 오류라 기계가 뜻을 못 읽는다. 대신 근거를 **실제 명령으로 다시 재서** 적었다(`dependencyInsight`·`migration-immutable.sh:18`). **건드리는 자리**: `PLAN.md`(`Q86`·`Q39`·구간 절) · `CLAUDE.md`. **닫힘**: 마무리 25차 지적 17이 전부 처분됐다 | 완료 |
| Q94 | ~~남의 저작물 신고·삭제 절차~~ | **`26` 이 승격시킨 `R42` 다**(2026-09-18). 저작권법 제102·103조 — 절차가 있어야 **온라인서비스제공자 책임 제한**을 받는다. 지금은 요건표에 번호만 서 있고 코드에 흔적이 0이다. **축**: **법**(저작권법 제102조·제103조, `D2` `R42`) + `D17`. **강제 지점**: **삭제가 저장소까지 간다는 것을 테스트로 건다** — 플래그만 세우고 파일을 두면 서명 URL 을 아는 사람에게 계속 열린다(`media-rules.md` 「남의 것이 올라오면」). 신고 접수는 표와 상태로 받는다. **건드리는 자리**: 신설 신고 표·접수 입구·삭제 경로. `27` 의 이미지 표와 `28` 의 접근 판정을 읽는다. **닫힘**: 신고를 받아 삭제하면 **저장소에 객체가 없고** 그것을 통합 시험이 잰다. `R42` 의 강제 지점 칸에 검사할 수 있는 이름이 서서 `req-coverage.sh` 의 「미착수」에서 빠진다. **완료**(2026-09-18) — `V79` · `CopyrightReportService` · 입구 둘. **제외가 6에서 5로 줄고 37/37 이 됐다**. **접수는 로그인을 안 받는다**(저작권자가 우리 회원일 이유가 없다) — `permission-rules.md` 의 예외 표에 갈래를 하나 세워 적었다. **판정 동작을 새로 팠다**: `product:delete` 를 쓰면 `seller_owner` 가 `seller` 스코프로 갖고 있어서 **셀러가 자기 신고를 스스로 판정한다.** `product:moderate` 는 관리자에게만 준다. **신고가 사진보다 오래 산다** — `product_image_id` 를 `set null` 로 두고 `product_id` 를 따로 든다(restrict 면 「지우라는 신고」가 「지우면 안 되는 제약」이 되고, cascade 면 지우는 순간 증거가 사라진다). **부순 증거**: 저장소 삭제 두 줄을 지우니 시험이 빨개졌다 | 27, 28 |
| Q95 | ~~사진을 지우는 입구를 연다 — 저장소까지~~ | **`64`·`Q94` 가 드러낸 구멍이다**(2026-09-18, `D13` 수명 표를 채우다 나왔다). `product_image` 는 상품에 `cascade` 라 **행은 따라 사라지는데 저장소의 객체는 안 따라간다** — 남는 것은 **주인 없는 파일**이고, 그 열쇠를 아는 사람에게는 서명 URL 이 계속 나온다. `Q94` 의 게시 중단만 저장소까지 가고 상품 파기·삭제 경로는 안 간다. **축**: `D13` 데이터 수명 + `media-rules.md`(삭제는 저장소에서 실제로 지운다). **강제 지점**: **cascade 로는 못 막는다** — DB 가 바깥 저장소를 모른다. 지우는 코드를 앱에 두고, **상품을 지운 뒤 그 열쇠로 `headObject` 가 404 인 것**을 통합 시험이 잰다. **건드리는 자리**: `ProductService.delete` 와 파기 배치(`5i` 계열). `ObjectStorage.delete` 를 쓴다. **완료**(2026-09-18) — **전제가 틀려서 범위를 고쳤다.** 세우면서 「cascade 가 행을 지우는데 객체가 안 따라간다」고 적었는데, **`ProductService.delete` 가 소프트 삭제**(`deleted_at`)라 상품 행이 안 사라지고 따라서 **cascade 도 안 돈다** — `delete from product` 가 코드에 0개다. 주인 없는 파일은 지금 안 생긴다. **대신 진짜 구멍이 따로 있었다**: **사진을 지우는 입구가 0개**라 셀러가 잘못 올린 사진을 못 지운다(`Q94` 의 게시 중단만 지운다). 그것을 열었다 — `ProductImageService.delete` 와 `DELETE /api/seller/products/images/{id}`. **저장소를 먼저 지운다**(행을 먼저 지우면 열쇠를 잃는다). **경로에 상품 번호를 안 받는다** — 사진이 이미 자기 행에 들고 있어서 또 받으면 **둘이 어긋난 요청**이 성립한다. **원래 전제는 파기 배치가 상품을 물리적으로 지우게 되는 날 살아난다** — 그 자리에 주석으로 남겼다. **닫힘**: 지우면 원본·썸네일이 저장소에서 사라지고 남의 상품 사진은 403 이다. **부순 증거**: 저장소 삭제 두 줄을 지우니 빨갛다 | 27 |
| Q96 | ~~확장자와 내용이 짝인지 본다~~ | **마무리 26차 독립 리뷰가 찾았다**(2026-09-18). `media-rules.md` 가 검사 셋을 박았는데 **둘째가 코드에 없다** — `ProductImageService.detect` 는 매직 바이트만 보고 `originalName` 의 확장자를 한 번도 안 읽는다. **왜 값이 있나**: 내용이 PNG 인데 이름이 `.jpg` 면 우리는 PNG 로 저장하고 `original_name` 에는 `.jpg` 가 남는다 — 내려받은 사람이 연 파일과 이름이 어긋난다. 그리고 **이름을 믿는 다음 코드**(내려받기 헤더·이관 스크립트)가 그 어긋남을 물려받는다. **축**: 표준(MIME↔확장자 짝) + `D17`. **강제 지점**: 앱 검증 — 짝이 아니면 `IMAGE_TYPE_NOT_ALLOWED`. **DB 로 못 내린다**(`original_name` 과 `content_type` 의 관계라 `check` 로 쓰면 목록을 SQL 에 박게 된다). **건드리는 자리**: `ProductImageService.detect` 와 그 시험. **닫힘**: 내용이 PNG 인데 이름이 `.jpg` 인 요청이 415 고, 확장자가 아예 없는 이름도 415 다. **완료**(2026-09-18) — `requireMatchingExtension`. **이름을 검증의 입력으로 쓰는 것이 아니다** — 형식은 이미 내용이 정했고, 여기서는 **이름이 그것과 다른지**만 본다. **부순 증거**: 그 호출 한 줄을 지우니 시험 둘이 빨개졌다 | 27 |
| Q97 | ~~업로드 입구를 HTTP 로 재는 시험~~ | **마무리 26차 독립 리뷰가 찾았다**(2026-09-18). `POST /api/seller/products/{id}/images` 를 **HTTP 로 부르는 시험이 0개**다 — `27` 의 닫힘이 「413·415·422 를 통합 시험이 고정한다」인데 실제로 재는 것은 **서비스 직접 호출의 `ErrorCode` 값**뿐이다. **그래서 멀티파트 상한 1 MB 를 못 봤다**(같은 리뷰가 따로 찾았고 `application.yml` 에 6MB 로 고쳤다). **축**: `D5` 응답 계약 + `D15`(무엇을 어느 층에서). **강제 지점**: 테스트 — `MockMultipartFile` 로 입구를 지나 상태 코드와 본문 슬러그를 잰다. **건드리는 자리**: 신설 `SellerProductImageApiTest`. **닫힘**: 1 MB 를 넘는 사진이 봉투를 지나 201 이고, 이미지가 아닌 내용이 415 며, 로그인 없이는 401 이다. **완료**(2026-09-18) — 신설 `SellerProductImageApiTest` 와 `HttpTestBase.postFile`. **MockMvc 로는 못 잰다** — 봉투를 테스트가 직접 만들어서 크기 상한을 아예 안 지난다(`stack.md` 에 절을 세웠다). **저장소를 이 클래스가 띄운다** — `HttpTestBase` 는 저장소가 없고 `StorageTestBase` 는 실제 HTTP 가 없다. **단색 JPEG 은 안 쓴다** — 거의 0 바이트로 접혀서 1 MB 를 못 넘는다. **함정 하나 더**: 하위 클래스에 `@SpringBootTest` 를 다시 달면 바탕의 `webEnvironment` 가 사라져 `local.server.port` 가 없다고 죽는다 — `@TestPropertySource` 로 바꿨다(`stack.md`). **부순 증거**: 봉투 상한을 1MB 로 되돌리니 정확히 그 시험 하나가 빨갛다 | 27 |
| Q98 | ~~필터 401 둘의 본문을 잰다~~ | **마무리 26차 독립 리뷰가 찾았다.** `Q84` 가 네 자리를 고쳤는데 **재는 것은 403 하나뿐**이다 — `account-inactive`·`session-superseded` 는 본문·`type`·`trace_id` 를 아무도 안 재서 **`setStatus` 로 되돌려도 전부 초록**이다. **축**: 규약(`D5`·`D16`). **강제 지점**: 테스트 — 죽은 계정과 밀려난 세션을 실제로 만들고 응답 본문을 본다. **건드리는 자리**: `ProblemResponseTest.FilterCut` 에 둘을 더한다. 죽은 계정은 `app_user.status` 를 바꾸고, 밀려난 세션은 `HttpTestBase` 로 둘째 로그인을 한다. **닫힘**: 둘 다 401 이고 본문의 `type` 이 각자 슬러그며, `ProblemWriter` 호출을 `setStatus` 로 되돌리면 빨갛다. **완료**(2026-09-18) — 새 시험 클래스를 안 만들고 **그 자리를 이미 재던 시험에 붙였다**(`AccountLivenessTest.Deleted`·`AuthLoginTest.ConcurrentSessions`). 둘 다 상태 코드만 보고 있었고, 본문 단언을 더하니 같은 설정을 다시 세울 일이 없다. **부순 증거**: 둘을 `setStatus(401)` 로 되돌리니 **정확히 새 시험 둘만** 빨갛다 — 기존 상태 코드 단언은 그대로 초록이라, 그것이 못 보던 자리였다는 증거다 | Q84 |
| Q99 | ~~신고 입구를 비로그인으로 재는 시험~~ | **마무리 26차 독립 리뷰가 찾았다.** `R42`(저작권법 제102조)가 걸린 「접수는 로그인을 안 받는다」를 재는 것이 `ArchitectureTest` 의 **정적 이름 목록**과 문서뿐이라, **`permitAll` 줄을 지워도 전부 초록이다.** 법 요건인데 강제 지점이 실질 5위다. **축**: **법**(저작권법 제102조, `D2` `R42`) + `D14`. **강제 지점**: 테스트 — 세션 없이 HTTP 로 신고를 넣어 201 인 것을 잰다. **건드리는 자리**: 신설 시험(`HttpTestBase` 로 비로그인 요청). **닫힘**: 비로그인 신고가 201 이고, `PUBLIC_PATHS` 에서 그 줄을 지우면 빨갛다. 판정 경로는 반대로 **비로그인이 401** 인 것도 같이 잰다. **완료**(2026-09-18) — 신설 `CopyrightReportApiTest`(`HttpTestBase`). **`R42` 의 강제 지점이 5위에서 4위로 내려왔다.** **사진 행을 직접 넣는다** — 재는 것이 보안 경계지 저장소가 아니라, 업로드를 부르면 이 바탕이 MinIO 까지 띄워야 한다. **정리를 직접 쓴다**(이 바탕은 롤백이 없다) — 상품이 계정을 `restrict` 로 가리켜서 안 지우면 바탕의 계정 정리가 외래키에서 막힌다. **부순 증거**: `permitAll` 줄을 지우니 「로그인 없이 신고를 넣을 수 있다」가 빨갛다 | Q94 |
| Q100 | ~~요청 제한 창을 시험끼리 안 밟게 한다~~ | **마무리 26차 독립 리뷰가 찾았다**(낮은 확신으로 왔고 실물이다). 창 열쇠가 `rate:{상한}:{주소}` 뿐이라 **같은 fork 의 모든 시험이 열쇠 하나를 나눠 쓴다** — `RateLimitFilterTest` 가 앞만 지우고 뒤는 안 지워서 카운터를 넘긴 채 끝나고, 창 1분 안에 `/api/auth/*` 를 치는 다음 클래스가 429 를 받는다. **지금은 안 터진다** — 마무리 26차가 바탕 둘에서 제한을 꺼서 그 시험만 켜져 있기 때문이고, **끈 것이 가린 것이지 푼 것이 아니다**. **축**: 프로젝트 규약(시험끼리 안 밟는다, `2i-2` 와 같은 갈래). **강제 지점**: 테스트 — 열쇠에 fork 를 넣고, 시험이 **끝날 때도 지운다**. **건드리는 자리**: `RateLimitFilter` 의 열쇠 접두어를 설정으로 빼거나 `@AfterEach` 로 치운다. **닫힘**: `RateLimitFilterTest` 를 켠 채로 느린 레인 전체를 돌려도 다른 클래스가 429 를 안 받는다. **완료**(2026-09-18) — 열쇠 접두어를 설정(`shop.rate-limit.key-prefix`)으로 빼고 시험이 `@DynamicPropertySource` 로 **fork 마다 갈라 준다**(`KafkaTestBase` 의 토픽 이름과 같은 수). **`@AfterEach` 로 뒤도 치운다** — 앞만 지우면 카운터를 넘긴 채 끝난다. **실측**: `RateLimitFilterTest`·`AuthLoginTest`·`HttpFlowTest` 를 한 회차에 같이 돌려 초록. **부순 증거**: 접두어를 기본값으로 되돌리니 「기본 접두어 열쇠가 남지 않는다」가 빨개졌다 | 71 |
| Q101 | ~~복구 리허설을 기계가 돌린다~~ | **PR #53 리뷰 2회차가 찾았다**(2026-09-18). `64` 의 닫힘이 「되살린 DB 로 앱을 띄우는 것」인데 **그것을 다시 돌리는 자리가 어디에도 없다** — `.github`·`scripts/verify.sh` 에 `db-dump`·`db-restore` 호출이 0이라 **다음 마이그레이션이 복구를 깨도 아무것도 안 빨개진다**. 짝인 `65` 는 같은 회차에 `MigrationUndoTest` 로 4위까지 갔는데 `64` 만 1회성 수기로 남았다. **축**: `D13` + 프로젝트 규약(축 2 — 기억에 맡길 것을 강제 지점으로 내린다). **강제 지점**: 테스트(4위) — Testcontainers 로 띄운 Postgres 에 **뜨고 날리고 되부어** 행 수가 같은 것을 잰다. **스크립트를 그대로 못 쓴다** — 지금은 `docker compose exec db` 에 박혀 있어서 대상 컨테이너를 인자로 빼는 것이 이 청크의 설계 결정이다. **건드리는 자리**: `scripts/db-dump.sh`·`db-restore.sh`(컨테이너를 인자로) · 신설 `DatabaseRestoreTest`. **닫힘**: 복구 시험이 느린 레인에 있고, 되붓기를 건너뛰면 빨개진다. **완료**(2026-09-18) — 신설 `DatabaseRestoreTest`. **스크립트를 안 부른다** — `db-dump.sh` 는 `docker compose exec` 로 **컴포즈의 서비스**를 부르는데 여기 도는 것은 Testcontainers 컨테이너라 컴포즈에 없다. **같은 명령을 같은 방식으로** 돌린다(`pg_dump -Fc` ↔ `pg_restore --clean --if-exists`). **이 fork 의 DB 만 건드린다**(`2i-2`). **함정 하나**: 바탕이 `@Transactional` 이라 그대로 두면 **시험의 트랜잭션이 표를 잠근 채 `drop schema` 를 불러서 회차가 통째로 멈춘다** — 10분을 넘겨도 안 끝났다. `NOT_SUPPORTED` 로 트랜잭션을 안 열게 하니 31초다. **부순 증거**: 되붓기를 건너뛰니 빨갛다 | 64 |
| Q102 | ~~상품당 사진 10장 상한을 트리거로 내린다~~ | **리뷰 둘이 같은 자리를 짚었다**(2026-09-18 — 마무리 26차 독립 리뷰와 PR #53 리뷰 3회차). 크기·형식은 `check` 로 2위에 내렸는데 **장수만 앱 검증(3위)에 남았다** — `countOf` 로 세고 비교하는 코드 한 곳이고, **새 입구가 생기면 빠뜨리는 것이 3위의 성질**이다. `coding-rules.md` 「불변식」이 **다른 행을 봐야 하는 값은 트리거**라고 정했고(「주문 금액 = 항목 합」과 같은 자리), 이 값이 정확히 그 모양이다. **축**: 프로젝트 규약(`D23` 축 2 — 내릴 수 있는 데까지 내린다). **강제 지점**: **트리거(2위)** — `product_image` 의 `before insert` 가 그 상품의 행 수를 세고 열한 장째를 거절한다. **`check` 로는 못 한다** — 자기 행만 보는 제약이라 다른 행을 못 센다. **건드리는 자리**: 신설 `V` 와 그 시험. 앱 검증은 **남긴다** — 사용자에게 422 를 주는 것은 앱이고, 트리거는 그것을 빠뜨린 입구를 막는 그물이다. **닫힘**: 앱 검증을 지우고 열한 장째를 넣으면 **DB 가 거절한다**. **완료**(2026-09-18) — `V80__product_image_limit.sql` 의 `before insert` 트리거와 `U80`. 시험이 **`insert` 를 직접 넣어** 앱을 건너뛴 입구를 흉내 낸다. **행 잠금을 안 걸었다** — 동시에 둘이 들어오면 둘 다 통과할 수 있는데, 막으려면 상품 행을 잠가야 하고 그러면 사진 업로드가 상품 수정과 줄을 선다. **여기서 막는 것은 「빠뜨린 입구」지 경합이 아니다**(경합은 앱이 이미 한 트랜잭션에 있다). **부순 증거**: 상한을 9999 로 올리니 시험이 빨개졌다 | 27 |
| 점검 P | ~~가로 · 관례 줄~~ | **완료**(2026-09-18) — 29일 만이고 그 사이 커밋 657·마이그레이션 76 이다. **버린 넷의 근거가 아직 유효하다**(`stack.md` 「관례로 깔리는 넷을 안 쓴다」): 폼은 가장 큰 것이 칸 둘이라 `react-hook-form` 이 여전히 불필요하고, `api.ts` 입구도 안 늘었다. **찾은 것 둘은 전부 오늘 친 것이다** — 점검이 늦어서가 아니라 **방금 들어온 것이라 아무도 안 봤다**: ① `26` 행이 「`stack.md` 에 그 선택의 근거」를 시켰는데 거기 있는 것은 **함정 둘과 버전 표 행뿐**이고 **왜 MinIO 인가가 없었다** — 절을 세우고 후보 셋(LocalStack·파일 시스템·R2 직결)을 왜 안 골랐는지 적었다 ② 5 MiB·10장이 **「관례」로만** 적혔다. `coding-rules.md` 가 **남들이 얼마인지를 같이 적는 선례**를 뒀는데(「스마트스토어 100 · Shopify 255」) 그것을 안 따랐다 — **숫자를 지어내지 않고 「안 셌다」를 밝혔다**(안 본 채로 바꾸면 근거 없는 값이 근거 있는 것처럼 보인다). **축**: 관례(4순위). **강제 지점**: 없다 — 「근거가 적혀 있나」는 뜻을 읽어야 판정된다. **닫힘**: 장부의 관례 줄이 오늘 날짜다 | 완료 |
| Q103 | ~~복구 시험이 업무 데이터를 잰다~~ | **마무리 27차 독립 리뷰가 찾았다**(2026-09-18). `DatabaseRestoreTest` 가 세는 유일한 행 수가 **`flyway_schema_history`**(스키마 메타)고 나머지는 「표가 0개가 아니다」다 — **업무 데이터가 하나도 안 돌아와도 초록이다.** `64` 가 존재하는 이유(「마이그레이션은 데이터를 안 되살린다」)를 정작 못 잰다. **`exit < 2` 를 통과로 봐서 부분 실패한 `pg_restore` 도 초록**이라 둘이 겹친다. **축**: `D13` + 규약. **강제 지점**: 테스트 — 뜨기 전에 **행을 심고** 되부은 뒤 그 행이 있는지 본다. `pg_restore` 종료 코드는 0만 통과로 본다(경고가 나면 그것도 봐야 한다). **건드리는 자리**: `DatabaseRestoreTest`. **닫힘**: 심은 행이 복구 뒤에 있고, 되붓기를 건너뛰면 **행이 없어서** 빨갛다. **완료**(2026-09-18) — 뜨기 전에 `seller` 한 행을 심고 되부은 뒤 그 행을 센다. **부순 증거는 두 번째 것이 맞다**(2026-09-18 마무리 28차 정정). 처음에 적은 `--schema-only` 는 **격리가 안 된다** — 그것은 `flyway_schema_history` 행도 안 되살려서 **기존 단언이 먼저 걸리고 새 단언에 닿지도 않는다**(독립 리뷰가 실측으로 잡았다). 격리되는 것은 **덤프 쪽에서 `--exclude-table-data=seller`** 다 — 표도 이력도 다 돌아오고 **심은 행만 없어서** 새 단언 하나만 빨갛다. **종료 코드를 `< 2` 에서 `0` 으로 좁혔다** — 되돌릴 것이 없는 `drop` 경고로 1 이 나올 수 있는데, 그러면 **부분 실패한 복구도 초록**이었다. 빈 DB 에 붓는 이 자리에서는 그 경고가 안 나고, 나면 그것이야말로 봐야 할 것이다. **일요일 배포의 3단계가 이 스크립트다** — 복구가 스키마만 살리면 빈 쇼핑몰이 뜬다 | Q101 |
| Q104 | ~~요청 제한 창 격리의 전제를 고친다~~ | **마무리 27차 독립 리뷰가 찾았다.** `Q100` 이 「fork 마다 Redis 를 나눠 쓴다」고 전제했는데 **`PostgresTestBase.forkRedis` 가 이미 논리 DB 를 가르고 있었다** — 내가 넣은 pid 접두어는 **같은 fork 안의 다음 클래스와는 같은 값**이라 실제 실패 모드(같은 fork 의 다음 클래스가 429)를 **못 막는다.** 막는 것은 `@AfterEach` 하나인데 **그것을 재는 시험이 없다**(단언이 `@AfterEach` 보다 먼저 돌아서 지워도 초록이다). **축**: 규약(`2i-2`). **강제 지점**: 테스트 — 같은 클래스에 **시험 둘**을 두고 앞이 상한을 넘긴 뒤 뒤가 통과하는지 본다(`@AfterEach` 가 그 사이에 돈다). **건드리는 자리**: `RateLimitFilterTest`. 접두어는 남길지 지울지 그때 정한다 — **전제가 틀렸으니 그 코드가 필요한지부터 묻는다**. **닫힘**: `@AfterEach` 의 삭제를 지우면 빨갛다. **완료**(2026-09-18) — **pid 접두어를 걷었다.** `forkRedis` 가 이미 fork 를 가르므로 같은 일을 두 번 하는 것이었고, 정작 같은 fork 안은 못 막았다. **시험 둘을 순서 고정으로 뒀다**(`@Order`) — 앞이 상한을 넘기고 뒤가 그것을 물려받는지 본다. `@AfterEach` 가 그 사이에 돈다. **`Q100` 때와 무엇이 달라졌나**: 그때는 단언이 `@AfterEach` 보다 먼저 돌아서 **지워도 초록**이었다. 지금은 **다음 시험이 그 역할을 해서** 지우면 빨갛다. **접두어 설정은 남긴다** — 배포에서 Redis 를 남과 나눠 쓸 때 쓸 자리고, 시험이 그것을 안 건드릴 뿐이다 | Q100 |
| Q105 | ~~업로드 입구 시험이 응답 계약을 잰다~~ | **마무리 27차 독립 리뷰가 찾았다.** `Q97` 의 축이 `D5`(응답 계약)인데 **네 시험이 전부 상태 코드만 본다** — 본문 단언이 0개고, 413(봉투 초과)·422(열한 장째)를 지나는 시험도 없다. 원래 닫힘에 있던 「본문에 `type` 과 `trace_id` 가 있다」가 옮겨 적으면서 사라졌다. **`Q95` 의 삭제 입구도 HTTP 시험이 0개다** — 같은 묶음의 `Q97` 이 「서비스 직접 호출로는 입구를 못 잰다」고 결론 낸 바로 옆에서 새 입구가 그것을 안 지켰다. **축**: `D5` + `D15`. **강제 지점**: 테스트. **건드리는 자리**: `SellerProductImageApiTest` 에 본문 단언과 413·422 · `HttpTestBase.Session` 에 `delete`. **닫힘**: 네 상태 코드마다 `type` 슬러그를 재고, 삭제 입구가 204·403·401 을 HTTP 로 지난다 **완료**(2026-09-19) — `HttpTestBase.Session.delete` 신설, `SellerProductImageApiTest` 에 시험 다섯과 `assertProblem`. **슬러그를 글자 그대로 박는다** — `ErrorCode` 를 참조하면 그 값을 바꿔도 시험이 같이 따라가서 **계약이 바뀐 것을 아무도 못 본다**. 413 은 **무작위 바이트 5 MiB + 1** 이다 — 크기 검사가 형식 검사보다 먼저 돌아서 사진일 필요가 없고, 사진은 압축 결과를 미리 못 정해 봉투(6MB)와 앱(5 MiB) 사이를 맞히는 데 운이 낀다. 422 는 **행 열 개를 직접 넣어** 채운다(사용자가 「어느 쪽이든 상관없다」고 해서 고른 쪽이다 — 재는 것은 장수를 세는 자리와 그 응답이고, 저장 경로는 201 시험 둘이 이미 지난다). **부순 증거 둘이 다 격리됐다**: `image-limit-reached` 슬러그를 바꾸니 422 시험 **하나만** 빨갛고, 삭제의 권한 검사를 무력화하니 「남의 사진」 **하나만** 빨갛다 | Q97, Q95 |
| 점검 Q | ~~세로 · 제약 줄~~ | **완료**(2026-09-19) — 30일 만이고 그 사이 **이름 붙은 제약이 154→233**, 마이그레이션이 `V63`→`V80` 으로 열일곱 늘었다. **방향은 코드→출처**(「이건 왜 여기 걸렸나」)에 **누락 축**을 섞었다 — 「무엇이 틀렸나」가 아니라 **「회계가 무엇을 안 덮나」**를 물었다. 찾은 것 셋이 **전부 어제(2026-09-18) 들어온 `V78`·`V79`** 다: ① **`Q28` 의 회계가 여섯을 못 본다** — `LengthConstraintTest` 의 쓸기가 `conname like '%length!_check'` 인데 `V78`·`V79` 가 접미사를 `_length` 로 달았다(39개가 `_length_check` 고 이 여섯만 다르다). 「빠뜨릴 자리가 없다」가 **이름 하나로 뚫렸다** ② **`not null` 인 여섯이 빈 문자열을 받는다** — 규약은 「`@NotBlank` 면 `between 1 and N`」인데 여섯이 `<= N` 이다(`<=` 를 쓰는 나머지 넷은 nullable 이라 규약대로다) ③ **트리거에는 회계가 아예 없다** — 제약은 `EnumConstraintTest`·`LengthConstraintTest` 둘이 `pg_constraint` 를 전부 걷는데 **`pg_trigger` 를 읽는 자리가 저장소에 0개**고 트리거는 34개다. 처분: ①②는 같은 여섯을 건드려서 `Q106` 하나로 묶고 ③은 `Q107`. **노트를 안 쓴다** — 셋 다 축 문서와 청크로 바로 내려갔다 | 점검 F |
| Q106 | ~~길이 제약 여섯을 회계 안으로 들인다~~ | **`점검 Q` 가 찾았다**(2026-09-19). `V78`(상품 사진 셋)·`V79`(저작권 신고 셋)의 길이 제약이 **`_length` 로 끝나서** `LengthConstraintTest` 의 쓸기(`conname like '%length!_check'`)에 안 걸린다 — `Q28` 이 세운 「제약을 전부 걷어 대조하거나 이유를 적는다」가 **이 여섯만 비껴간다**. 같은 여섯이 **`not null` 인데 `<= N`** 이라 빈 문자열이 들어간다(규약은 `@NotBlank` 면 `between 1 and N`). **지금 값은 안 갈렸다** — 신고 셋은 앱도 DB 도 100·320·2000 이다. 막는 것은 **다음에 한쪽만 고치는 날**이다. **갈림길**: (가) 마이그레이션 하나로 **이름을 `_length_check` 로 바꾸고** 정의도 `between 1 and N` 으로 고친다 — 규약과 게이트가 같이 맞는다 (나) 쓸기의 `like` 를 넓힌다 — 마이그레이션이 없지만 **이름이 두 벌로 갈린 채 남는다**. **(가)를 권한다**(`D22` 의 접미사 표가 이미 한 벌이다). **축**: 규약 + `D22`. **강제 지점**: 제약(2위) — 회계 테스트가 4위에서 그것을 센다. **건드리는 자리**: 새 `V81`, `LengthConstraintTest` 의 `PAIRS`·`EXEMPT`. **닫힘**: 여섯이 회계에 잡히고, 하나를 빼면 빌드가 선다 **완료**(2026-09-19) — **(가)로 갔다**(사용자 결정). 새 `V81` 이 여섯을 `drop` 하고 이름을 `_length_check` 로, 정의를 `between 1 and N` 으로 다시 건다. **값은 하나도 안 바꿨다** — 수를 같이 건드리면 어느 쪽이 무엇을 고친 것인지 다음 사람이 못 가른다. 회계에는 **신고 셋이 대조(`pairs`)**, **사진 셋이 면제(`NOT_COMPARED`)** 로 들어갔다 — 저장소 열쇠 둘은 앱이 만들고 올린 파일 이름은 멀티파트 봉투에서 와서 **요청 record 의 칸이 아니다**. **부순 증거**: 면제에서 `product_image_object_key_length_check` 한 줄을 지우니 **27개 중 회계 단언 하나만** 빨갛다 — 그 이름이 쓸기에 잡힌다는 뜻이고, `V81` 전이면 안 잡혀서 **지워도 초록이었다**. **드러난 것 하나 — 처분: 신규 `Q108`**: 이 회계가 보는 것은 **상한 N 하나**고 빈 문자열 허용 여부는 **대조하지 않는다**(`LengthConstraintTest:47`). 그래서 ②는 이번에 손으로 맞춘 것이고 **다음에 같은 실수를 막을 그물이 없다** | 점검 Q |
| Q107 | 트리거도 회계를 받는다 | **`점검 Q` 가 찾았다**(2026-09-19). 제약은 `pg_constraint` 를 전부 걷는 회계가 둘(`EnumConstraintTest`·`LengthConstraintTest`)인데 **`pg_trigger` 를 읽는 자리가 저장소에 하나도 없다.** 트리거는 **34개**고 전부 강제 지점 2위다 — 새 트리거가 **아무 시험도 없이** 들어와도 걸릴 자리가 없고, `점검 F` 가 찾은 **「걸려 있는데 한 번도 안 돈다」**(`V63` 의 지연 트리거 둘)가 바로 그 구멍에서 나왔다. **이름으로는 못 센다** — 제약은 시험이 이름을 단언하는 관례가 있는데(94개가 그렇다) **트리거는 34개 중 이름이 시험에 나오는 것이 0개**다. 효과로 재기 때문이라 **회계는 「어느 시험이 이 트리거를 지나나」를 사람이 적는 표**가 된다(`EnumConstraintTest` 의 `EXEMPT` 와 같은 틀). **축**: 규약 + `D15`. **강제 지점**: 테스트(4위) — 트리거 자체는 2위지만 **빠진 트리거**를 세는 것은 테스트만 할 수 있다. **건드리는 자리**: 새 `TriggerCoverageTest`, `testing-strategy.md`. **닫힘**: 34개가 전부 표에 있고, 하나를 빼거나 새로 더하면 빌드가 선다 | 점검 Q |
| Q108 | ~~`@NotBlank` 와 `between 1 and N` 을 회계가 맞춰 본다~~ | **`Q106` 이 드러냈다**(2026-09-19). `LengthConstraintTest` 는 **상한 N 하나만** 대조하고 빈 문자열 허용 여부는 안 본다 — javadoc 이 「`@NotBlank` 와 짝지으려면 검증 조합 전체를 알아야 한다」고 적어 두고 비워 둔 자리다. 그래서 `V78`·`V79` 가 `not null` 컬럼에 `<= N` 을 달았을 때 **아무것도 안 걸렸고**, `Q106` 이 손으로 맞췄다. **좁히면 잴 수 있다**: `pairs()` 가 이미 제약과 record 칸을 이어 놨으므로, **그 칸에 `@NotBlank` 가 있으면 제약 정의에 `between 1 and` 가 있는지**만 본다 — 검증 조합 전체를 알 필요가 없다. 면제 쪽은 요청 칸이 없어서 이 물음이 성립하지 않는다. **축**: 규약 + `D15`. **강제 지점**: 테스트(4위) — 제약 정의를 읽는 자리가 거기뿐이다. **건드리는 자리**: `LengthConstraintTest`, `coding-rules.md` 「길이 상한은 앱과 DB 양쪽에 둔다」. **닫힘**: 대조 스물넷이 전부 지나고, 하나를 `<= N` 으로 되돌리면 빨갛다 **완료**(2026-09-19). `blankHandlingMatchesRequestRecords` 가 제약의 **아래쪽 경계**와 요청 칸의 빈 값 거절을 맞춰 본다. **판정을 글자로 안 한다** — `pg_get_constraintdef` 가 `between 1 and N` 을 `(length(x) >= 1) AND (length(x) <= N)` 으로 풀어서 돌려주고, **경계가 1 이 아닌 자리도 있다**(`email_change_request_new_email_length_check` 는 3 이다). 그래서 경계 **수**를 읽는다. 앱 쪽도 둘을 센다 — `@NotBlank`(`null` 까지 막는다)와 **`@Size(min = 1)`**(안 보내는 것은 두고 빈 값만 막는다). **첫 회차가 넷을 잡았고 그 자리에서 고쳤다** — 사유 칸 여덟이 `@Size(max = 500)` 이라 빈 문자열을 통과시켰고 DB 가 그것을 거절해서 **빈 사유를 보내면 400 이 아니라 500 이 나갔다**(`ShipmentController` 다섯 · `RefundController` 둘 · `ReturnController` 하나). **부순 증거**: `ReceiveRequest.reason` 의 `min` 을 걷으니 **51개 중 새 시험 하나만** 빨갛다 | Q106 |
| Q82 | ~~지표 이름의 자원 토막이 사건과 갈렸다 — `shop.batch.run`~~ | **마무리 22차 독립 리뷰가 짚었다**(2026-09-17). `Q66` 이 사건의 자원 토막을 **표 이름(단수)** 으로 못 박았는데(`shop.batch.run_finished` → `shop.batch_run.finished`) 지표는 `shop.batch.run` 그대로다 — `observability-rules.md` 「이름과 태그」의 예시와 `BatchRuns` 의 실물 둘 다. `Q66` 행이 「둘 중 하나로 맞춘다」고 적어 둔 자리고, **맞추는 쪽을 정하는 것이 이 청크다.** **갈림길**: (가) 지표도 표 이름으로 — `shop.batch_run.finished`. 사건과 한 규칙이 되고 지금은 **소비자가 0이다**(대시보드는 `63`, 노출은 `62` 가 미착수) (나) 지표는 다른 이름 공간이라고 적는다 — Prometheus 표기에서 점이 밑줄이 되므로 `shop_batch_run_finished` 가 되어 표 이름과 시각적으로 안 갈린다는 반론이 있다. **되돌리기 비용이 비대칭이다**(`D23`) — 대시보드가 서면 쿼리가 그 이름에 묶인다. **절차**: (1) 고른다 (2) `observability-rules.md` 「이름과 태그」의 예시와 규칙을 그 결론으로 (3) 고치기로 하면 `BatchRuns`(와 다른 지표 자리)를 따라가고, 이름을 재는 테스트가 있으면 같이 (4) `D16` 에 **자원 토막의 출처**를 한 줄로 못 박는다 — 지금은 예시만 있고 규칙이 없다. **축**: 규약(`D16`·`D22`·`D12`). **강제 지점**: 문서(5위) → 가능하면 테스트. **건드리는 자리**: `observability-rules.md` · `BatchRuns` · 지표 이름 테스트. **닫힘**: 지표 이름의 자원 토막이 어디서 오는지가 `D16` 에 한 줄로 있고 실물이 그것을 따른다  **완료**(2026-09-17, `Q64` 와 한 묶음). **사용자가 (가) 를 골랐다** — 지표도 표 이름으로. `shop.batch.run` → `shop.batch_run.finished`(`BatchRuns`·`observability-rules.md`). **`D16` 에 규칙을 못 박았다**: 자원 토막은 표 이름(단수)이고 **사건 이름과 안 가른다** — 가르면 새 이름을 지을 때마다 어느 쪽인지 다시 묻게 된다. **지금이 마지막 자리였다** — 대시보드(`63`)와 노출(`62`)이 미착수라 소비자가 0이고, 서면 쿼리가 그 이름에 묶인다| 완료 |
| 2c-2 | ~~기준 문서 제목의 날짜를 doc-lint 가 막는다~~ | **완료.** `scripts/doc-lint.sh`·`CLAUDE.md`·`doc/reference/commerce-compliance.md`. **`점검 J` 가 걷은 것의 재발을 막는다** — 제목에 날짜가 있으면 그 절은 규칙이 아니라 그날의 기록이고, `doc/README.md` 가 「지나간 작업 이력은 문서에 안 쓴다」고 정했다. `doc/reference/*.md` 의 `#` 제목에 `YYYY-MM-DD` 가 있으면 실패한다. **`external-references.md` 는 뺐다** — **거기는 날짜가 내용이다.** 인용 자료를 언제 원문으로 확인했는지가 그 문서의 값이고(`document-map.md`), 낡았는지를 그 날짜로만 판정한다. **세우자마자 셋을 잡았다** — `점검 J` 가 「처분 완료」로만 찾아서 놓친 것들이다: `commerce-compliance.md` 의 「2026-08-22 … 통독」(제목만 갈아 「정보통신망법·표시광고법·물가안정법에서 온 것」으로. 내용은 살아 있는 요건이다)과 `external-references.md` 둘(예외로 뺐다). **사람이 훑은 것을 기계가 다시 훑어 더 찾은 자리다.** **부쉈다**: 날짜 제목을 하나 넣으니 그 줄을 짚으며 빨개졌고, 지우니 초록. `stack.md` 의 절 제목들은 날짜가 없어서 안 걸린다 — 「기억으로 쓰면 틀리는 자리」는 언제 알았든 지금도 유효한 사실이라 날짜를 안 단다. **축**: 규약(`doc/README.md`). **강제 지점**: **4위** — 훅과 CI 가 이미 이 스크립트를 돈다. 새 자리를 안 만들고 있는 것의 눈을 넓혔다 | 완료 |
| 2c-3 | ~~같은 커밋에 CI 가 두 벌 도는 것~~ | **완료 — 절차 ①을 재고 ②로 갔다.** `.github/workflows/ci.yml`·`codeql.yml`·`stack.md`. **실측**: PR #37 의 커밋 하나에 `ci.yml` **2건**·`codeql.yml` **2건**(`e2e.yml` 은 `pull_request` 뿐이라 1건). **절차 ①의 답**: 가지 보호가 `{context, app_id}` 로 맞추고 **이벤트를 안 가린다**(`gh api …/protection` 이 `app_id: 15368` 으로 낸다) — push 실행이 같은 커밋에 같은 이름을 붙이므로 **필수 검사는 그대로 찬다.** 그래서 ③(concurrency 키 바꾸기)으로 안 갔다: 그쪽은 **이미 돌기 시작한 것을 죽이는 것**이라 절반만 아끼고, 취소된 run 이 목록에 남는다. **`pull_request:` 를 뺐다**(사용자 선택) — 실행이 반으로 준다. **되돌리기가 한 줄이라 위험이 작다.** **남긴 경고**: fork 에서 온 PR 은 base 저장소에 push 가 안 돈다 — 바깥 PR 을 받기 시작하면 이 줄을 되돌린다. 지금은 fork 가 없다. **필수 검사 넷이 전부 `ci.yml` 에 있다**는 것도 같이 적었다(`stack.md`) — 그 파일 트리거를 건드릴 때 볼 자리다. **축**: 관례(GitHub Actions 기본값). **강제 지점**: 없다 — 낭비를 줄이는 것이다 **닫힘**: 다음 마무리 PR 에서 `gh run list --commit <sha> --workflow ci.yml` 이 **1건**이고 필수 검사 넷이 그 run 을 가리킨다 | 완료 |
| 2i | ~~테스트 되먹임을 초 단위로 가른다~~ | **완료.** `backend/build.gradle.kts`·`PostgresTestBase`·`HttpTestBase` 수정, `CLAUDE.md` 「검증」 표와 `testing-strategy.md` 갱신. **레인이 둘로 갈렸다** — `test` 69개 7.3초, `integrationTest` 931개 3분대. `build` 는 둘 다 돈다. **예보를 바꿨다**: 분할표는 별도 소스셋이라고 적었는데 실물을 보니 **DB 를 타는 78개 클래스가 전부 `PostgresTestBase`·`HttpTestBase` 를 상속하고 있어서** 경계가 이미 있었다. 소스셋은 빠른 쪽에 testcontainers 의존성을 안 줘서 컴파일로 막는 1위지만 **파일 78개를 옮겨야 해서 diff 를 아무도 못 읽는다.** 태그를 골랐고 **강제 지점이 4위로 내려간 것을 인정한다** — 다만 **표식을 빠뜨릴 자리는 없다**: 컨테이너가 그 두 바탕에만 있어서 DB 를 쓰려면 상속해야 하고, 상속하면 `@Tag("db")` 가 따라온다. 상속 안 하고 DB 를 쓰면 빠른 레인에서 바로 빨개진다. **`finalizedBy(jacocoTestReport)` 를 걷었다** — 안 걷으면 리포트가 느린 레인에 매달려서 **`test` 하나가 결국 컨테이너를 띄운다.** 대신 `check` 가 리포트를 부른다. **빠른 레인이 전체의 7% 라는 것이 이 청크의 산출물이다** — 층 표의 「단위」 줄이 얇다는 뜻이고, 채우는 것은 도메인마다 판단이 달라 한 청크에 안 들어간다 | 완료 |
| 2i-1 | ~~느린 레인 3분을 재고 싼 것만 켠다~~ | **완료.** `backend/src/test/java/com/projectshop/shop/PostgresTestBase.java`·`testing-strategy.md`·`stack.md`·`CLAUDE.md`·`README.md`. **103초 → 72~78초.** 단계마다 재고 전후를 적었다. **`.withReuse(true)` 는 코드에 이미 있었다** — 안 걸린 것은 로컬 파일의 `testcontainers.reuse.enable=true` 였고, 로그가 「Reuse was requested but the environment does not support」로 말하고 있었다. **켜져 있는 것과 도는 것이 다른 자리가 또 하나 늘었다.** −14초. **테스트 bcrypt 를 4 로**(`@Primary PasswordEncoder`) −13초. 운영은 10 그대로고(`D14`) 시드 해시는 비용이 해시 안에 있어 여전히 10 으로 검증된다. **컨텍스트 합치기는 할 것이 없었다** — `BackendApplicationTests` 가 이미 `PostgresTestBase` 를 상속한다. 예보가 틀린 자리다. **`maxParallelForks = 2` 는 되돌렸다** — 세 번 중 두 번 빨갛고 깨진 것이 `AuditLogTest` 였다. 시도 기록이 `REQUIRES_NEW` 라 롤백에 안 쓸리는데(`4b-2`), fork 둘이 재사용 컨테이너 하나를 나눠 쓰면 **한쪽의 `purgeCommittedAuditLogs` 가 다른 쪽이 방금 쓴 행을 지운다.** 컨테이너를 fork 마다 띄우면 격리는 되지만 −14초를 도로 내놓는다. **스키마를 fork 마다 가르는 것은 이 청크 범위가 아니다.** **검증**: `./gradlew build` 초록, `doc-lint.sh` 통과. **축**: 규약(`D15` 레인·`D14` bcrypt) + 관례(Testcontainers reuse). **강제 지점**: 없다 — 측정이다. 켠 것은 되먹임 속도를 바꾸지 무엇을 막나를 안 바꾼다 | 완료 |
| 2j | ~~세션 규칙을 hook 으로 내린다~~ | **완료.** `.claude/settings.json`(신설)·`.gitignore`·`CLAUDE.md` 수정. **`doc-lint.sh` 가 편집 직후에 스스로 돈다** — 깨지면 그 자리에서 막히고 통과하면 아무 말이 없다. **훅이 둘이다**: `Edit|Write` 는 `file_path` 를 보고, **`Bash` 는 명령 문자열을 본다.** 둘째가 이 청크의 핵심이다 — 이 저장소에서 문서를 고치는 실제 수단이 `sed -i` 라서, `Edit` 에만 걸면 **훅이 장식이 된다.** 읽기만 하는 명령(`grep PLAN.md`)은 건너뛰도록 **바꾸는 동사와 문서 이름을 둘 다** 요구한다(5.1초짜리라 아무 때나 돌리면 안 된다). **`.gitignore` 를 열었다** — `.claude/` 통째 무시에서 `.claude/*` + `!.claude/settings.json` 으로 바꿔 **그 파일 하나만** 커밋한다. 심볼릭 링크와 `settings.local.json` 은 그대로 막힌다. 훅은 로컬 취향이 아니라 저장소 규칙이라 빠지면 새 클론에서 다시 5위가 된다. **축**: 규약. **강제 지점**: 설정(5위)인데 **사람의 기억을 안 탄다** — 문서와 갈리는 자리가 거기다. **실제로 도는 것을 셋 다 확인했다**(`CLAUDE.md` 「검증」이 요구하는 그 자리): `Edit` 로 `PLAN.md` 제목을 부수니 막혔고, `sed -i` 로 `PROGRESS.md` 를 부수니 막혔고, 정상 편집은 조용했다. **안 막는 것이 하나 있다** — `git checkout`·`git stash` 처럼 git 이 파일을 되돌리는 경로는 명령에 바꾸는 동사가 없어서 안 걸린다. 그 자리는 `2c-1` 이 CI 로 이미 막고 있어서 안 늘렸다 | 완료 |
| 2k | ~~컨텍스트를 가지친다~~ | **완료.** `doc/process/inspection.md`·`doc/process/wrapup.md`(신설), `CLAUDE.md`·`doc/README.md`·`scripts/doc-lint.sh`·`.claude/settings.json`·`doc/reference/quality-gates.md` 수정. **`CLAUDE.md` 가 43,432자에서 28,487자로 줄었다(−34%).** 지운 것이 아니라 옮긴 것이다 — 「점검」(9,923)과 「마무리」(5,506)를 `doc/process/` 로 빼고 라우터에 포인터만 남겼다. **왜 이것이 가지치기인가**: 「어디를 읽나」 표는 **읽는 수고**를 줄이지만 `CLAUDE.md` 는 **매 세션 전문이 컨텍스트에 올라간다.** 트리거 단어가 있는 절은 파일 밖에 둬도 동작이 같고, 그만큼이 매 세션에서 빠진다. **`doc/process/` 를 새 갈래로 세웠다**(사용자 선택) — 점검·마무리는 **코드가 지킬 기준이 아니라 세션이 따르는 절차**라 `doc/reference/` 의 D 목록에 섞으면 두 종류가 한 바구니에 들어간다. **옮기면서 구멍이 둘 생겼고 둘 다 그 자리에서 막았다**: `doc-lint.sh` 의 대상 목록에 `doc/process/*.md` 가 없었고, `2j` 훅의 경로 패턴에도 없었다 — **새 폴더는 기존 검사망 밖에서 태어난다.** 부숴서 둘 다 걸리는 것을 확인했다. **중복도 하나 만들었다가 지웠다** — 포인터와 옮긴 본문이 같은 문장을 두 번 말해서, 본문 쪽을 자기 설명으로 바꿨다. **축**: 규약. **강제 지점**: 없다 — 줄이는 일이라 새로 걸 것이 없고, `doc-lint.sh` 와 `2j` 훅이 새 폴더까지 덮게 넓힌 것이 이 청크가 남긴 전부다. **안 친 것**: `doc/reference/*` 의 「처분 완료」 블록 열(18,041자). **매 세션 컨텍스트에는 안 올라가서** 레버가 작고, 살아 있는 문장이 섞여 있어 문장 단위 판단이 필요하다 | 완료 |
| 2k-1 | ~~존댓말 금지를 기계로 내린다~~ | **완료.** `scripts/doc-lint.sh`·`CLAUDE.md` 수정. 「글 작성 규칙」 일곱 중 **4번만 4위로 내렸다** — 금지어가 문자열로 정해져 있어 grep 이 100% 잡는다. **나머지 여섯은 안 내렸다**: 사견·늘어짐·6하원칙 따위는 뜻을 읽어야 판정돼서 기계로 못 가고, `coding-rules.md` 가 「못 내린 것만 문서에 남긴다」고 이미 정했으니 **5위가 정답이지 실패가 아니다**(사용자 지적). **범위와 등급은 다른 축이다** — 이 규칙은 범위가 최대(모든 글)인데 출처는 3순위(규약)라 어겨도 아무 일이 안 난다. 그래서 「위험이 커서」가 아니라 **「비용이 0에 가까워서」** 내렸다. **인용을 걸러내는 것이 이 청크의 실제 일이었다.** 저장소에 이미 스무 건이 있었는데 **열여섯이 인용**이었다 — 화면 문구·법조문·남의 오류 메시지. 백틱·`「」`·큰따옴표 안을 걷어내고 본다. **`sed` 로는 안 됐다**: `s/「[^」]*」//g` 가 멀티바이트를 바이트로 갈라서 **걸러진 척하고 통과했다** — `perl -CSD` 로 바꿨다. 조용히 실패하는 필터가 제일 나쁘다. **코드펜스는 지우지 않고 빈 줄로 바꾼다** — 줄을 없애면 줄 번호가 밀려 어디를 고칠지 못 짚는다. **`screen-rules.md` 는 제외했다**: 그 문서는 화면 문구를 정의하는 자리라 존댓말이 인용이 아니라 **본문**이다(좋은 예/나쁜 예 표). **축**: 규약. **강제 지점**: 4위(`doc-lint.sh`), 그리고 `2j` 훅이 이미 걸려 있어서 **편집 직후 자동으로 돈다.** **잡는 것과 안 잡는 것을 둘 다 부숴서 확인했다** | 완료 |
| 3 | 권한 스키마 | `app_user`·`role`·`permission`·`user_role`·`role_permission` 테이블과 초기 데이터 | 완료 |
| 3a | 셀러 조직 스키마 | `seller`·`seller_member` 테이블, `user_role` 에 셀러 참조 추가. 역할 부여에 대상이 생긴다 | 완료 |
| 3b | 거부 규칙 스키마 | `role_permission` 에 `effect`(allow/deny) 추가, 거부가 필요한 초기 데이터 | 완료 |
| 3c | ~~셀러 사업자 정보~~ | **완료.** `V4` 재작성, `V8`·`V13` 수정, `SellerSchemaTest` 10개. 상호·대표자·사업자등록번호·통신판매업 신고번호·사업장 주소·연락처(`D2` R1). **`status` 에 `pending` 을 넣고 `active` 로 올라가려면 신원정보가 채워져 있게 `check` 로 막는다** — 전자상거래법 제20조②가 중개자에게 **판매 개시 전 확인**을 요구하고 제20조의2 가 연대책임을 붙인다. 사용자 선택. **신고번호는 `not null` 이 아니다** — 면제 대상이 있어서(거래 50회 미만·간이과세자) 없는 셀러가 합법이다. 면제 사유를 같이 받아야 "안 넣은 것" 과 "면제라 없는 것" 이 갈린다. `D7` 에 셀러 상태 절을 같이 추가한다. **`V4` 의 `create table seller` 를 다시 쓴다** — `alter add column` 을 일곱 번 붙이면 셀러 테이블의 모양이 두 파일에 흩어진다. **`V13` 이 `alter` 로 붙인 `commission_bp` 도 이때 `V4` 로 옮긴다**(`D3`) — 그 한 줄이 상품 마이그레이션에 셀러 컬럼이 끼어 있는 유일한 자리다. **`6b` 의 선행이라 상품 축보다 먼저 온다** | 완료 |
| 3e | 기본키 이름 정리 | 기존 13개 테이블의 `id` 를 `<테이블>_id` 로 바꾼다(`D22`). **적용된 마이그레이션을 직접 고치고 DB 를 재생성한다** — 로컬 전용이고 배포한 적이 없어서 가능하다. 배포가 한 번이라도 나가면 `alter table rename` 을 새 마이그레이션으로 해야 한다 | 완료 |
| 3d | 역할 코드 이름 정리 | `role.code = 'seller'` 를 `seller_owner` 로 바꾼다. 조직 테이블과 역할이 같은 이름이라 "seller 를 지운다" 가 폐업인지 역할 회수인지 안 갈린다(`D1`) | 완료 |
| 4 | 권한 판정 엔진 | 스코프·조직·거부를 한 군데서 판정. **화면 없이 테스트로만 검증** | 완료 |
| 4d | 응답 필드 마스킹 | 판정 결과를 허용·거부가 아니라 볼 수 있는 필드 목록으로 바꾼다. 셀러는 주문의 배송 정보만 보고 결제 수단은 못 본다(`D2` R8) | 완료 |
| 4c | 권한 매트릭스 회귀 테스트 | 부여방식×스코프×효과×대상의 판정 결과를 표로 떠서 파일로 고정한다. 판정이 바뀌면 diff 가 뜬다. **4a 보다 먼저 한다** | 완료 |
| 4a | 판정 캐시와 무효화 | 사용자 단위 캐시, 역할 변경 시 무효화. 회수가 즉시 먹는지 테스트로 못박는다 | 완료 |
| 4e | ~~허용 범위 타입~~ | **완료.** `Allowed<T>`(sealed). 권한에서 "전부" 는 값이 없어서 빈 집합에 그 뜻을 실었고, 그게 "아무것도 없음" 과 겹쳤다. 두 자리(필드 그룹·볼 수 있는 셀러)가 주석으로 막고 있던 것을 타입으로 내렸다 | 완료 |
| 4f | ~~판정 축 확장 규약~~ | **완료.** `permission-rules.md` 에 「축을 늘릴 때」. 새 조건이 들어올 때 **권한 축인가 도메인 규칙인가**를 먼저 가른다 — 안 가르면 시간·금액·이력이 전부 판정으로 몰려서 조합을 사람이 못 읽는다. 축으로 올리면 캐시(`4a`)·권한 목록(`8a`)·매트릭스(`4c`)·`Decision` 모양 넷을 같이 본다. **시점 의존 축은 캐시 전제와 충돌한다** — 회수 없이 답이 바뀌어서 무효화할 계기가 없다. 정책 엔진으로 넘어갈 임계도 숫자로 적었다. **`11a` 앞에 온다** — 여섯 번째 축이 들어오기 전에 규약이 있어야 그 축이 규약을 따른다 | 완료 |
| 4a-1 | 테스트 fixture 통합 | `PermissionEvaluatorTest`·`FieldVisibilityTest` 의 복제된 헬퍼를 `AuthFixture` 로 옮긴다. 4a 가 세 번째 복사를 막으려고 클래스만 만들어 뒀다 | 완료 |
| 4b | 감사 로그 | 권한 거부와 역할 변경을 기록하는 테이블, 기록 지점 배치 | 완료 |
| 4b-2 | ~~거부 기록이 롤백에 안 쓸리게~~ | **완료.** 사건 종류로 갈랐다 — `AuditLog.Kind` 를 호출자가 고르고 그 값이 전파를 정한다. `ATTEMPT`(거부)는 `REQUIRES_NEW`, `OUTCOME`(역할 부여·상품 생성)은 `REQUIRED`. 한쪽으로 몰면 양쪽 다 깨져서(롤백된 일이 "했다" 로 남거나, 실패한 시도가 한 줄도 안 남거나) 사건마다 고른다. 종류를 안 고르면 컴파일이 안 되므로 강제 지점이 타입까지 올라간다(`D23` 축 2). 전파를 실제로 걸려면 프록시를 지나야 해서 삽입을 `AuditLogWriter` 로 뺐다. **테스트 바탕에 정리를 하나 붙였다** — 따로 커밋된 기록은 테스트 롤백에 안 쓸려서 남의 테스트가 그걸 센다. `D16` 에 판단을 적었다 | 완료 |
| 4b-1 | 감사 로그 조회 | 행위자·대상·기간으로 감사 로그를 찾는 API. 쌓아만 두고 볼 방법이 없으면 감사가 성립 안 한다 | 완료 |
| 5-0 | 동의 이력 스키마 | `consent_item`(항목·개정판)·`user_consent`(동의·철회 사건)·`current_consent` 뷰. 상태가 아니라 사건을 적어야 동의 시점과 철회 시점이 둘 다 남는다(`D2` R7·R14) | 완료 |
| 5-1 | 인증 기반 | Spring Security 스타터, `SecurityFilterChain`(기본 `authenticated`, 공개 경로만 목록), `PasswordEncoder`, 세션 쿠키 속성. 엔드포인트는 안 만든다 | 완료 |
| 5-2 | 회원가입 | `POST /api/auth/signup`. 계정·기본 역할·동의 사건을 한 트랜잭션에 남긴다. 필수 항목 미동의와 야간 수신 종속을 앱이 막는다. JSON snake_case 와 Problem Details 설정도 여기서 켠다(`D5`) | 완료 |
| 5 | 로그인·로그아웃 | `POST /api/auth/login`·`/logout`, DB 기반 `UserDetailsService`, 세션 발급과 ID 재발급. **`formLogin` 을 껐으므로 세션 고정 방어를 직접 부른다** — 안 부르면 `D14` 요구가 조용히 빠진다. **계정 생존 확인을 로그인이 아니라 인증 필터에 둔다**(`ADR 0010`) — 로그인 때만 보면 이미 로그인한 다른 기기가 안 막힌다. `4a` 캐시에 얹는다. `SessionRegistry` 와 `HttpSessionEventPublisher` 도 여기서 등록한다 — 세션 만료(`5g`)가 그걸 쓴다 | 완료 |
| 5b-0 | CSRF 토큰 발급 | 토큰을 `XSRF-TOKEN` 쿠키로 내려서 클라이언트가 `X-XSRF-TOKEN` 헤더에 실을 수 있게 한다. 켜 두기만 하면 토큰을 얻을 방법이 없어 POST 를 아무도 못 부른다 | 완료 |
| 5b | ~~CSRF 프론트 연동~~ | **완료 — `13-2` 가 흡수했다.** 쿠키의 토큰을 읽어 헤더에 싣는 것이 `src/lib/api.ts` 안에 있다. **떼어 놓을 수 없었다** — 안 실으면 로그인이 401 이라, 화면만 만들고 이걸 미루면 `13-2` 가 안 돌아가는 채로 끝난다. 토큰이 없을 때 한 번 두드려 쿠키를 받아 오는 것도 거기 있다. **서버 쪽은 5-1·5b-0 에서 끝났다** | 완료 |
| 5c | ~~인증 실패 경로~~ | **완료.** `LoginAttemptService`·`AbsoluteSessionTimeoutFilter`(신설), `AuthController`·`SecurityConfig` 연결, 테스트 13개, `D14`·`stack.md` 갱신. (계정, IP) 5회 실패에 15분 차단을 **Redis 로** 넣었다(`38`) — 카운터 하나로 완결되고 날아가도 차단이 풀릴 뿐이라 손해가 작다. **차단 검사가 비밀번호 대조보다 앞이다** — 뒤에 두면 차단 중에도 해시 계산이 돌아서 응답 시간이 갈리고 그 차이가 계정 존재 여부를 흘린다. **세션 절대 만료 12시간도 넣었다** — 서블릿에 설정 자리가 없어 코드로만 되고, `session.timeout` 은 무활동 만료라 다른 축이다. **1회용 재설정 토큰은 `5c-1` 로 뗐다** — 발송 수단이 없다(청크 54) | 완료 |
| 5c-1 | ~~비밀번호 재설정 토큰~~ | **완료** — `V67__password_reset_token.sql`(신설, 판 포함) · `PasswordResetService`·`PasswordResetMailer`(신설) · `notification/PasswordResetMail`(신설) · `PasswordResetTest`(신설, 여덟) · `AuthController`(입구 둘) · `SecurityConfig`·`ErrorCode`·`NotificationService`·`data-lifecycle.md`·`LengthConstraintTest`. **정할 것이 사라져 있었다** — 행은 「전달 수단을 먼저 정해야 한다」고 적었는데 `54b` 가 닫히면서 발송기와 `PASSWORD_RESET` 판이 이미 서 있었다(**아무도 안 부르는 채로**). **강제 지점을 2위에 뒀다**: 부분 유니크 인덱스(`used_at is null`)가 **사람마다 살아 있는 토큰을 하나로** 묶는다 — 앱을 거치지 않고 SQL 로 둘째를 넣어 봤더니 DB 가 거부했다(부순 증거). 재사용은 `update … where used_at is null` 의 조건이 막는다. **원문을 저장 안 한다** — 표에는 해시만 넣고 대조도 해시로 한다(`D14`), 로그에도 안 남긴다(`D16` `A09`). **응답이 가입 여부와 무관하게 같다** — 없는 주소면 조용히 끝난다. **규칙 둘이 설계를 고쳤다**: `ArchitectureTest` 가 (1) `auth → notification → consent → auth` 순환을 잡아서 **포트를 `auth` 에 두고 `notification` 이 구현**하게 뒤집었고, (2) 「트랜잭션 안에서 메일 안 부른다」를 잡아서 **표 고치는 부분만 트랜잭션에 넣고 메일은 밖에서** 보내게 갈랐다 — 안에서 보내면 롤백된 뒤에도 **없는 토큰의 링크가 나간다**. **게이트 둘이 새 표를 잡았다**: 수명 정책 대조와 길이 회계가 각각 빨개져서 `D13` 에 행을, 예외 목록에 이유를 적었다. **축**: `D14`(1회용·수명·존재 여부 비노출) · `D16`(`A09`). **강제 지점**: **DB 제약(2위)**. **닫힘**: 테스트 여덟 초록 · `verify.sh --full` 초록 · 빈 DB 기동 `applied_migrations: 67` | 5c, D14 |
| 5c-2 | ~~Redis 장애 시 로그인~~ | **완료.** `LoginAttemptService.LocalCounter`(신설), 테스트 4개, `D14` 갱신. **순수 fail-open 도 fail-close 도 아니고 강등이다** — Redis 예외를 잡고 프로세스 로컬 카운터로 옮겨서 5회 / 15분을 그대로 센다. 인스턴스가 하나라 덮는 범위가 같다. 막으면 캐시 한 대에 서비스가 멈추고, 그냥 통과시키면 「개인정보의 안전성 확보조치 기준」이 요구하는 인증 실패 횟수 제한이 장애 시간 동안 사라진다. **캡차 fail-open 논거(*"남의 장애"*)는 안 걸린다** — Redis 는 같은 compose 안에 있고, 캡차가 죽어도 살아 있다고 적어 둔 방어가 이 카운터다. **복구 뒤에 두 카운터를 안 합친다**(합치면 정상 사용자가 복구 직후 잠긴다). **강등 `WARN` 로그는 `2b` 몫이다** | 완료 |
| 5d | 캡차 | reCAPTCHA v3 를 로그인·회원가입·비밀번호 재설정에 붙인다. `CaptchaVerifier` 인터페이스 뒤에 두고 테스트는 항상 통과하는 구현을 쓴다. 점수 0.5, `action` 검증, 검증 실패는 통과시킨다(`D14`). **이 프로젝트의 첫 외부 호출이고 첫 국외이전이다** — 개인정보법 제28조의8 은 제3자 제공(제17조)과 별개 조문이라 고지 항목이 다르다(`D2`). `13a-1` 처리방침에 둘을 갈라서 적는다. 검증 실패를 `WARN` 으로 남기는데 **이 프로젝트의 첫 애플리케이션 로그가 된다** — `2b` 가 먼저 와야 형식이 정해진 채로 찍힌다. **개인정보법 제28조의8(국외이전)을 `D2` 에 승격시킨다**(점검 A) — 제3자 제공과 별개 조문이라 고지 항목이 다르다. **축**: **법**(개인정보법 제28조의8 — 국외이전 고지·동의), `D14`, 업계 관례(reCAPTCHA v3 점수 0.5). **강제 지점**: `CaptchaVerifier` 인터페이스 뒤에 두고 **검증 실패는 통과시키는 것**을 테스트로 고정한다 — 캡차가 죽으면 로그인이 죽는 구조를 안 만든다. **건드리는 자리**: 신설 verifier 둘(실물·항상 통과). 수정은 `AuthController` 세 경로와 처리방침 새 판(국외이전 항목) | 5c, 13-2, 2b, D14, D2 |
| 5h | ~~계정 생존 확인~~ | **완료.** `AccountLivenessFilter`, `PermissionRuleLoader.isAlive`, 캐시 `accountLiveness` | 완료 |
| 5e | ~~내 계정 조회·수정~~ | **완료.** `AccountService`·`MeController`. **`Location` 문제는 안 풀렸다** — `/api/me` 는 id 로 가리키는 경로가 아니라 201 이 지목할 자원이 못 된다. 관리자용 계정 조회(16)가 그 자리다 | 완료 |
| 5f | ~~동의 조회·철회~~ | **완료.** `ConsentService`. 철회·재동의 둘 다 있고 채널 철회가 야간까지 거둔다 | 완료 |
| 5g | ~~탈퇴~~ | **완료.** `WithdrawalService`, `POST /api/me/withdraw`. `ConcurrentSessionFilter` 를 같이 걸어야 세션 만료가 실제로 먹는다(`ADR 0010`) | 완료 |
| 5i | ~~탈퇴 계정 파기~~ | **완료.** `AccountPurgeService`, `V2`·`V8` 수정. 유예가 지난 탈퇴 계정의 이메일·이름·비밀번호 해시와 `user_consent.acted_ip` 를 비운다(`D2` R9). **`10a` 에서 떼어 왔다** — 거기는 선행이 주문(`10-1`)이라 한참 뒤인데 **파기 대상은 `5g` 가 이미 만들고 있다**. 이메일은 `null` + 부분 유니크 인덱스(`ADR 0007`). 탈퇴는 `update` 라 `user_consent` 의 cascade 가 안 걸린다 — 여기서 직접 지운다. `D13` 에 동의 이력 3년·IP 30일을 적었다 | 완료 |
| 5k | ~~동의 고지 조회~~ | **완료.** `ConsentItemController`(공개)·`MeController` 확장, `ConsentService.readCurrent`·`readMine`. 동의 항목의 고지 내용과 약관 본문을 내려준다. **약관규제법 제3조② 가 고객 요구 시 사본을 내주라고 한다** — 지금 `5f` 는 항목 이름과 상태만 줘서 사본을 요구할 경로가 없다. **가입 전에도 봐야 하므로 공개 경로다**(`SecurityConfig.PUBLIC_PATHS`). 목록에 본문을 통째로 싣지 않는다 — 약관 본문이 커서 동의 화면이 무거워진다. `5-0` 이 스키마로 표현한 것을 앱이 못 하면 그 설계는 쓰인 적이 없는 것이다 | 완료 |
| 5j | ~~동의 고지 항목~~ | **완료.** `V11` 재작성, `ConsentSchemaTest` +5. 정형 넷(목적·항목·기간·거부 시 불이익) + 본문 1칸. `consent_item` 에 수집·이용 목적, 수집 항목, 보유·이용 기간, 거부 시 불이익을 담는다(개인정보법 제15조제2항). **지금은 `title` 뿐이라 "3판에 동의했다" 의 3판 내용이 시스템에 없다** — 개정판을 남기는 설계가 입증에 못 쓰인다. 정형 컬럼으로 나눌지 본문 한 덩어리로 둘지는 이 청크에서 정한다. `acted_ip` 수집도 항목에 적는다. **약관 판의 본문도 같이 담는다**(`D2` R16 약관규제법 제3조) — 명시·설명을 못 하면 그 조항을 계약 내용으로 주장할 수 없다. 근거법이 둘(개인정보 고지 / 약관 명시)이라 요구가 겹치지만 대상 범위가 다르다. **`V11` 의 `create table consent_item` 을 다시 쓴다** — 시드도 같은 파일에 있어서 컬럼과 값을 한 자리에서 맞춘다 | 완료 |
| 5a | 조직 초대·멤버십 | 셀러에 계정을 붙이고 그 셀러 안에서만 먹는 역할을 준다. 멤버 구성을 못 바꾸는 `seller_staff` 역할이 여기서 생긴다(`D1`). **축**: `D6` 권한 모델(조직 역할), `D14`. **강제 지점**: 초대 토큰 유니크 + 역할 부여를 판정 엔진 경로로만 — 표를 직접 만지면 조직 경계가 새는 자리가 하나 더 생긴다. **건드리는 자리**: 신설 `seller_invitation` 표와 `SellerMemberService`. 수정은 `permission_rule` 시드(`seller_staff`) | 3a, 5, D1, D6 |
| 6 | ~~상품 스키마~~ | **완료.** `V13__product_schema.sql`. 테이블 5개(`product`·`product_option`·`product_option_value`·`sku`·`sku_option_value`)와 `seller.commission_bp`. 요율은 만분율 정수, R4 는 `check` 제약, 가격·재고는 `sku` 에 | 완료 |
| 6b | ~~데모 시드 데이터~~ | **완료.** `db/seed/V900__demo_seed.sql`, `application-local.yml`. **`db/migration` 이 아니다** — 참조 데이터와 성격이 달라서 `local` 프로필에서만 Flyway 대상에 더한다. 셀러 2(신고번호/면제), 계정 6, 소속 4. `both-member` 가 두 셀러에 속하는데 역할은 한쪽만 받는다 | 완료 |
| 7b | ~~인증·권한 실패 응답 규약~~ | **완료.** `ErrorCode`·`ShopException`·`ApiExceptionHandler`·`ProblemFactory`·`ProblemEntryPoint`. `D5` 가 정한 자원별 403·404 와 Problem Details 를 실제 코드에 붙인다. 판정 실패는 거부로 떨어뜨리고 500 을 준다. 오류 본문에 `trace_id` 를 넣는다(`D16`). **401 도 여기서 본다** — `5-1` 의 `HttpStatusEntryPoint` 는 본문 없이 상태만 준다. **도메인 예외와 `@RestControllerAdvice` 번역기를 만들고 서비스 6곳의 `ResponseStatusException` 을 옮긴다**(`D23`). **`7` 앞에 온다** — 뒤에 두면 상품·주문 축이 잘못된 패턴을 계속 늘려서 옮길 곳이 6곳에서 12곳이 된다. 403·404 사례는 감사 로그 조회와 계정 API 에 이미 있어서 상품 API 를 안 기다려도 됐다 | 완료 |
| 6b-1 | ~~데모 시드에 상품~~ | **완료.** `V902__demo_products.sql` 신설, `stack.md` 두 절. 상품 다섯 — 옵션 있음·없음, **상품 전체 품절과 조합만 품절**, 배송비와 무료배송, 약정 7영업일과 법정 기한, **동의를 받는 제한(`made_to_order`)과 안 받는 제한(`digital_content`)**. 고른 기준은 **화면에서 갈리는 것을 데이터로 밟는 것**이고, 하나라도 빠지면 그 갈래는 다시 손으로 넣게 된다. **조합은 하나씩 넣고 그 자리에서 선택지를 묶는다** — 한 번에 넣으면 어느 `sku` 가 어느 조합인지 이어 줄 값이 없다. **시드가 시행 전인 동의 판까지 넣던 것을 걷어냈다**(`V900` 이 판을 안 가린다). **`out-of-order` 를 켰다가 되돌렸다** — `stack.md` 가 이미 그 선택을 버려 뒀는데 어겼고, 켜 보니 스위치가 둘 필요해서 그 판단이 오히려 맞았다. `6b-1` 로 **다시 만드는 값이 싸졌다** | 완료 |
| 6b-2 | ~~데모 시드에 주문~~ | **완료.** `DemoOrderSeeder` 신설(`demo/` 패키지), `D23` 「패키지」 한 절, `stack.md` 한 절. **SQL 로 안 넣었다** — 주문 하나가 서려면 통과할 자리가 여덟이고(번호 발급·영업일 계산·서면 네 조항·제한 박제·재고 차감·셀러별 배송비·상태 이력·결제 행) SQL 로 다시 쓰면 **같은 규칙의 사본이 둘**이 된다. `local` 프로필 `ApplicationRunner` 가 **운영 서비스를 그대로 부른다** — `CartService`→`OrderService`→`PaymentService`→`OrderActionService`. **강제 지점이 타입이다**(축 2의 1위): 서비스 시그니처가 바뀌면 시더가 안 컴파일된다. 주문 다섯 — 셀러 둘로 갈린 주문(배송비 3,000원과 무료, 법정 3영업일과 약정 7영업일), 배송완료, 단순 변심 반품, **하자 반품**(사유만 다른데 기한도 배송비 부담도 갈린다), **청약철회 제한이 성립한 주문**(동의받은 `made_to_order` 와 `digital_content` 를 한 묶음에). `support/` 에 못 둔다 — `order → support` 가 이미 있어서 **순환**이라 `demo/` 를 팠고 그 근거를 `D23` 에 적었다. **주문이 하나라도 있으면 건너뛴다** — Flyway 와 달리 기동할 때마다 돌아서 안 막으면 띄운 횟수만큼 쌓인다 | 완료 |
| 7 | ~~상품 등록·수정 API~~ | **완료.** `ProductService`·`ProductController`, 테스트 20개. `scope=seller` 가 실제 자원에 처음 걸렸다. **시드가 `own` 이라 판매자가 상품을 하나도 못 만들던 것을 찾아 고쳤다** — 상품엔 주인 계정이 없다(`ADR 0004`). `product.created_by_user_id` 를 담당자 축으로 새로 뒀고 `own` 은 `5a` 의 `seller_staff` 가 쓴다 | 완료 |
| 7d | ~~판매 중지·제재~~ | **완료.** `ProductTransitions`(전이표), `suspend`·`resume`·`block`·`unblock`, `product.block_reason`, 상태에 `blocked` 추가. **승인이 끝이 아니다** — 위법 표시·위조품 신고·리콜은 팔기 시작한 뒤에 드러나고 알고도 방치하면 연대책임이다(제20조의2). 셀러가 쉬는 것(`suspended`)과 관리자가 막은 것(`blocked`)을 상태로 갈라서 **셀러가 제재를 못 푼다** | 완료 |
| 7c | ~~상품 검수~~ | **완료.** `ProductReviewService`, `product:review` 권한, `product.review_note`, `check_product_sale_allowed` 트리거, 테스트 11개. 셀러가 쓴 상품을 관리자가 승인해야 노출된다. **셀러가 `active` 가 아니면 `on_sale` 전이를 막는다**(`D2` R1, 전자상거래법 제20조②) — `pending` 셀러도 `draft` 는 만들 수 있게 열어 뒀으므로 막는 자리가 여기다. `check` 로는 다른 테이블을 못 봐서 **앱과 트리거 두 겹**으로 막았다 — 앱이 422 로 이유를 주고 트리거가 다른 입구를 막는다 관리자 역할에 실제로 할 일이 생기고 상품에 상태가 붙는다(`D2` R15) | 완료 |
| 7e | ~~상품 상태를 enum 으로~~ | **완료.** `ProductStatus`·`SkuStatus`(신설), `ProductTransitions`·`ProductReviewService`·`ProductQuery`·`ProductService` 수정, `ProductStatusTest`(신설) 4개. 리터럴 13곳이 타입으로 바뀌었고 전이표도 enum 을 든다. **enum 이 둘이다** — 두 목록이 겹치지만 같지 않아서(조합은 검수도 제재도 없다) 하나로 두면 `sku` 자리에 `draft` 를 넣어도 컴파일이 통과한다. **제약 정의를 읽어 대조하는 테스트를 뒀다** — `pg_get_constraintdef` 에서 값을 뽑아 enum 과 맞춘다. 목록을 테스트에 또 적으면 세 번째 사본이 된다. **응답 변환도 enum 을 지나간다** — 대문자로만 올리면 DB 의 모르는 값이 그대로 화면에 나간다. **SQL 텍스트 안의 리터럴은 안 바꿨다**(근거를 `D23` 에 남겼다) — 자바 식별자가 아니라 그 쿼리의 조건 자체고, 파라미터로 빼면 쿼리만 봐서 무엇을 거르는지 안 보인다 | 완료 |
| ~~7e 원안~~ | 상품 상태를 enum 으로 | 상품 축이 상태를 생 문자열로 쓴다 — `ProductReviewService` 에 리터럴 13곳, `ProductTransitions` 의 전이표도 `String` 이다. **`D23` 「열거값을 어디에 두나」가 enum 을 요구한다**(강제 지점 1위). 문자열이면 오타를 컴파일러가 못 잡고, 이름을 바꿀 때 IDE 가 못 따라가고, 「이 값이 어디서 쓰이나」를 못 묻는다. **`product.status` 와 `sku.status` 가 둘 다 `on_sale` 을 쓰므로** 검색으로도 안 갈린다 — enum 이 둘이어야 타입으로 갈린다. 주문 축(`OrderTransitions.Shipment`)에 선례가 있어서 모양을 베낀다. **`check` 목록과 enum 을 대조하는 테스트를 같이 둔다** — DB 에 값을 추가하고 enum 을 안 고치면 `of()` 가 런타임에야 터진다 | 7d, 8, D23 |
| 8 | ~~상품 조회 API~~ | **완료.** `ProductQuery`·`SellerProductController`·`ListQuery`, 테스트 16개. **경로를 갈랐다** — `/api/products`(공개, 판정 없음)와 `/api/seller/products`(스코프를 섞는다). 한 쿼리로 합치면 `or` 하나가 틀렸을 때 비로그인에게 `draft` 가 새고 그 실수는 조용하다. 정렬은 허용 목록(`created_at`·`name`). **`AuditLogQuery` 의 문자열 결합도 여기서 정리했다** | 완료 |
| 8a | 내 권한 목록 API | 로그인한 사용자가 지금 무엇을 할 수 있는지를 서버가 계산해 내려준다. 화면이 역할 이름으로 판단하면 판정이 두 벌이 된다 | 완료 |
| 9 | ~~장바구니~~ | **완료.** `V15__cart.sql`, `CartService`·`CartController`, 로그인 병합, 파기 배치에 흡수. **비로그인도 담는다** — 동의 없이 되는 근거는 개인정보법 제15조①4호(계약 체결 과정의 요청)고, 대신 처리방침 고지(13a)와 보유기간 30일(D13)을 채운다. **세션 ID 를 안 쓴다** — 로그인할 때 바뀌어서 병합할 대상을 못 찾는다 | 완료 |
| 17-1 | ~~불변식 카탈로그~~ | **완료.** `money-invariants.md`. 주문 6·정산 5·환불 3 등식과 강제 지점. **합계를 저장하기로 정했다** — `D8` 이 청크 10 으로 미뤄 둔 것이 여기로 당겨졌다. 여러 행에 걸친 합계는 **지연 트리거**로 건다(`order` 가 `order_item` 보다 먼저 들어와서 즉시 트리거는 언제나 깨진다). 환불·정산 축은 스키마가 없어서 등식만 적고 강제는 `12-1`·`12a-1`·`17` 이 건다 | 완료 |
| 10-1 | ~~주문 스키마~~ | **완료.** `V16__order_schema.sql`, `OrderSchemaTest` 21개. 테이블 4개(`shop_order`·`order_shipping`·`seller_order`·`order_item`), 한 행 등식 7개는 `check`, 합계 3개와 존재 조건 2개는 **지연 트리거**. **`order` 가 SQL 예약어라 `shop_order` 고 기본키는 `order_id`**(`D22` 의 `app_user` 예외 규칙). **배송지를 주문에서 분리했다**(`D13`) — 주문은 5년 보존인데 배송지는 파기 대상이라 한 행에 두면 남길 것과 지울 것이 엉킨다. **`D9` 의 예시 주문번호가 자기 규칙을 어기고 있었다** — 형식을 `check` 로 내리자 거부됐다 | 완료 |
| 10-0 | ~~멱등키~~ | **완료.** `V17__idempotency.sql`, `IdempotencyService`, 테스트 9개, `AccountPurgeService` 에 24시간 정리 흡수, `D11` 갱신. **선점·처리·저장이 한 트랜잭션이다**(사용자 선택) — 필터에 두면 셋이 갈라져 "주문은 커밋됐는데 기록은 진행중" 구간이 생기고, 그때 죽으면 재전송이 영원히 막힌다. **필터는 안 만들었다** — `run()` 호출 여부를 모르니 "헤더는 요구하는데 멱등은 안 걸린" 상태를 가린다. 커버리지는 `10-2` 의 테스트가 지킨다. **409 의 뜻이 바뀌었고 실패는 저장 안 한다** — `D11` 두 문장을 코드에 맞춰 고쳤다 | 완료 |
| 10-2 | ~~주문 생성 API~~ | **완료.** `OrderService`·`OrderController`, `V4` 에 `default_shipping_fee`, `ProductService` 수정, 테스트 21개. 장바구니에서 **고른 항목**만 주문한다(사용자 선택). 가격·수수료율 박제, 재고는 조건부 UPDATE 를 `sku_id` 오름차순으로, 셀러별 `seller_order` 분할, 배송지는 `order_shipping` 에. **주문에 쓰인 SKU 는 `suspended` 로 내리고 옵션 축 변경은 422**(사용자 선택) — 바꾸면 지나간 주문의 옵션 라벨이 뜻을 잃는다. **배송비 출처를 `seller` 에 만들었다**(사용자 선택) — `D3` 가 셀러 몫이라고만 정하고 금액 출처가 없었다 | 완료 |
| 10a | ~~거래기록 보존·파기~~ | **완료.** `TransactionPurgeService`, `V16` 에 `closed_at`, 테스트 9개, `D2`·`D13` 갱신. 배송지 6개월 → 주문 5년 → 감사 로그 3년. **전자상거래법 제6조 제2항을 조문으로 확인했다** — 거래기록은 "보존하여야 한다"(의무)인데 그와 관련된 개인정보는 **"보존할 수 있다"**(재량)라, 5년 보관도 6개월 파기도 둘 다 적법하다. 보존이 권리지 의무가 아니므로 쓸 일이 끝나면 버리는 쪽을 골랐다(사용자 선택). 기산점은 `seller_order.closed_at` 이고 **채우는 것은 청크 11-2** 다 | 완료 |
| 10a-2 | ~~안 닫히는 셀러 주문의 강제 마감~~ | **완료.** `StaleBundleBatch`·`StaleBundleBatchTest`(둘 다 신설), `application.yml`(풀 9)·`batch-catalog.md`. **막히는 상태가 둘이 아니라 셋이었다** — 분할표는 `preparing`·`return_requested` 를 적었는데 전이표를 보니 **`shipping` → `delivered` 를 밀어 주는 것도 없다.** 자동 구매확정은 `delivered` 에서만 시작한다. **`preparing`·`shipping` 둘만 민다**(사용자 선택). 안 보낸 것은 공급 곤란이라 `cancelled` 로 닫고(제15조제2항), 닫히는 순간 `RefundSweeper` 가 환불 요청을 만든다 — **여기서 환불을 직접 만들면 그 스위퍼와 사본이 둘이 된다.** `shipping` 은 `delivered` 로만 민다 — **확정까지 밀면 청약철회할 틈이 사라진다.** **`return_requested` 는 안 민다** — 제18조제2항 1호가 기산점을 「재화를 **반환받은 날**」로 잡아서, 물건이 안 왔는데 `returned` 로 닫으면 **「반환받았다」가 거짓**이 되고 분쟁에서 우리 기록이 우리에게 불리한 증거가 된다. 대신 **세어서 `WARN` 으로 드러낸다** — 안 세면 그 구멍이 아무 데도 안 남는다. **시한은 상태마다 다른 시계를 쓴다**(사용자 선택) — `preparing` 은 **법이 준 `ship_due_at`** 에서 이레(우리 여유), `shipping` 은 `shipped_at` 에서 서른 날. **강제 지점은 「배치가 도나」가 아니라 「결과가 비나」다**(`36a` 함정) — 닫힌 뒤 `closed_at` 이 채워지는 것까지 밟는다. 배치가 아홉이 돼서 `36b` 가 또 걸렸고 풀을 9로 올렸다 | 완료 |
| 2d | ~~컴파일 경고 정리~~ | **완료.** `build.gradle.kts` 에 `-Xlint:deprecation`·`-Xlint:unchecked`, `ErrorCode` 와 테스트 4파일 수정, `stack.md` 갱신. 정체는 **이름이 바뀐 것 둘**이었다 — RFC 9110 이 422 를 `UNPROCESSABLE_ENTITY` 에서 `UNPROCESSABLE_CONTENT` 로 고쳤고(테스트의 `isUnprocessableEntity()` 도 같이), Jackson 3 이 `JsonNode.asText()` 를 `asString()` 으로 바꿨다. 기능이 없어진 게 아니라 경고를 켜기 전엔 어느 줄인지도 안 나왔다. **`-Werror` 는 안 걸었다** — 라이브러리를 올릴 때 남의 코드에서 오는 경고로 빌드가 막히면 급할 때 옵션을 통째로 끄게 된다 | 완료 |
| 10b | ~~재고 경쟁 테스트~~ | **완료.** `OrderConcurrencyTest`(신설) 5개, `D15`·`stack.md` 갱신. 재고 10개에 20명이 동시에 사서 정확히 10개만 팔리고 나머지가 `OUT_OF_STOCK` 을 받는 것, 엇갈리게 담아도 데드락이 안 나는 것을 고정했다. **롤백을 껐다**(`NOT_SUPPORTED`) — 테스트가 트랜잭션을 쥐고 있으면 다른 스레드가 차감을 못 보고 잠금도 안 걸려서 경쟁 자체가 안 난다. **정렬을 지워도 통과할 수 있는 테스트라 대조 테스트를 같이 뒀다** — 조회 쿼리에 `order by` 가 없어서 담은 순서를 뒤집어도 플래너가 같은 순서를 줄 수 있다. 잠그는 순서를 손으로 엇갈리게 만들어 `40P01` 을 실제로 냈다. **그 `40P01` 이 `PessimisticLockingFailureException` 으로 온다** — `10c` 가 이걸 안다 | 완료 |
| 10c | ~~충돌 재시도~~ | **완료.** `Retries.onConflict`(`support`). SQLSTATE `40001`·`40P01` 만 50·100·200ms 로 다시 돌린다 — **예외 타입으로 안 가른다**(`stack.md`). 건 자리 둘: `OrderController.create`(멱등 **바깥**), `OrderStatusBatch.move`. **멱등 안쪽은 애초에 못 건다** — Postgres 가 그 트랜잭션을 abort 시켜서 다음 문장이 `25P02` 로 죽는다. 바깥이면 선점 기록도 같이 롤백돼 있어서 같은 키로 다시 들어오는 것이 맞다. `Runnable` 짝을 안 만들었다 — 값을 돌려주는 람다가 둘 다에 맞아 컴파일이 모호해진다. **이전 설명**: `D11` 이 `40001`·`40P01` 을 50·100·200ms 로 3회 재시도하라고 정했는데 **거는 곳이 아직 없다.** 트랜잭션 바깥에 걸어야 한다 — 안쪽이면 이미 깨진 트랜잭션을 붙들고 다시 돈다. **예외 이름으로 잡으면 안 된다**(`stack.md`) — Postgres 는 SQLSTATE 앞 두 자리로 번역돼서 데드락이 `DeadlockLoserDataAccessException` 이 아니라 상위 타입으로 온다. `OrderConcurrencyTest` 가 그 사실을 고정해 뒀다. **재시도가 멱등키 바깥인지 안쪽인지 정한다** — 안쪽이면 선점 기록까지 되돌아가고, 바깥이면 같은 키로 두 번 들어온다. **거는 곳이 하나 생겼다**: `OrderStatusBatch` 의 건 루프다(`D19` 1층) — 건마다 트랜잭션이 갈려 있어서 바깥 조건이 이미 맞다 | 완료 |
| 11-1 | ~~상태 이력 스키마~~ | **완료.** `V18__order_status_history.sql`, `OrderStatusHistorySchemaTest` 16개, `D2` R6·`D7` 갱신. `ADR 0007` 이 설계만 해 두고 테이블이 없던 자리다 — 전이 코드를 먼저 쓰면 이력을 남길 곳이 없어 두 번 고친다. **두 층을 한 테이블에 받았다**(사용자 선택) — `order_id`·`seller_order_id` 중 정확히 하나를 채우고 상태 값 목록도 층을 보고 가른다. 소비자가 보는 것이 주문 하나의 생애라 나누면 화면마다 이어 붙여야 하고 한쪽을 빠뜨리면 이력이 반만 보인다. **법이 두 갈래로 걸린다**(`D2` R6) — 제1항의 보존 5년, 그리고 **제3항의 열람 제공**(R6 에 빠져 있어서 같이 채웠다). 관리자 전이에 사유 필수, 남은 행은 트리거로 수정 불가 | 완료 |
| 11-1a | ~~공휴일 표~~ | **완료.** `V19__holiday.sql`, `D22` 에 자연키 예외. `D10` 이 영업일 계산을 `holiday` 표에 걸어 뒀는데 표가 없었다 — 기한 박제(`11-2`)가 그것을 쓴다. **2026년을 시드했다**(사용자 선택 — 비워 두면 실제 시스템을 만지는 느낌이 안 난다). **음력에서 나오는 날(설·부처님오신날·추석)과 대체공휴일은 미검증**이라 SQL 주석에 밝혔다. 토·일은 안 담는다 — 요일은 날짜에서 나온다. 기본키가 `holiday_date` 다: 값 자체가 식별자인 표는 대리키를 안 얹는다 | 완료 |
| 11-2 | ~~전이표·전이 서비스~~ | **완료.** `OrderTransitions`·`OrderStatusService`·`BusinessCalendar`(신설), `ErrorCode` 3개 추가, `TransactionPurgeService` 수정, 테스트 17개, `D7`·`D10`·`D22` 갱신. 전이표를 코드에 선언하고(`ADR 0009`) 전이마다 이력 한 줄을 남긴다. **곁가지를 서비스가 데리고 다닌다** — 배송완료는 기한 둘을 박제하고, 확정·취소·반품완료는 `closed_at` 을 채우고, 취소는 재고를 되돌린다. **`10a` 파기에 이력 삭제를 넣었다**(흡수 완료). **관리자는 사유를 적으면 표 밖으로도 옮긴다**(`D7`). **날수 계산만으로는 자동확정이 청약철회와 같은 날이 될 수 있다** — 말일 보정이 붙이는 자리라 `max(8일째, 청약철회 만료 다음날)` 로 고쳤다. **HTTP 경로는 안 만들었다** — 누가 옮길 수 있나는 `11a` 권한 축이고, 그 전에 API 를 열면 인증만 통과하면 남의 주문을 옮긴다 | 완료 |
| 11-3 | ~~상태 자동 전이 배치~~ | **완료.** `OrderStatusBatch`·`SchedulingConfig`(신설), 테스트 10개, `stack.md` 갱신. 결제 만료는 `fixedDelay` 5분(앞 회차가 끝난 뒤부터 세서 느려져도 안 겹친다), 자동확정은 `cron` 04:00 KST. **배치에 규칙이 없다** — 대상을 고르고 `OrderStatusService` 를 부를 뿐이라 재고 복구·이력·`closed_at` 이 한 군데에 남는다. **건마다 트랜잭션이 갈린다** — 배치에 `@Transactional` 을 붙이면 하나가 터질 때 그 회차가 전부 롤백된다. **한산한 회차는 `DEBUG`** — INFO 로 남기면 하루 288줄이 쌓여 진짜 처리가 묻히고, 아무것도 안 남기면 도는지를 못 본다. 실서버에서 스케줄이 실제로 도는 것을 로그로 확인했고 **배치 스레드에도 추적 ID 가 붙는다** | 완료 |
| 11c-1 | ~~구매자 주문 조회~~ | **완료** — `OrderQuery` 신설, `GET /api/orders`·`/{orderNumber}`. 상세가 셀러 묶음·항목·**이력을 한 줄로** 내린다(`D2` R6). 못 보는 주문은 **없는 주문과 같은 404**(`D5`). 배송지는 `shipping` 그룹이라 `_visible_field_groups` 가 따라 나간다. **열거값을 대문자로 내린다**(`D5` 「형식」, 사용자 선택) — 상품 응답이 소문자로 나가는 것은 `11c-3a` 이 맞춘다 | 완료 |
| 11c-2 | ~~셀러 주문 식별자와 노출 경계~~ | **완료** — `seller_order_number`(`S-20260811-K3M9P7`)와 `seller_order_visible` 뷰를 `V16` 에 넣었다. **독립 번호를 골랐다**(사용자 선택) — 이 단위를 부르는 곳이 넷(부분 취소·환불, 정산 명세, 반품 접수, 송장)이고 「주문번호+셀러코드」면 그 넷에 컬럼 둘로 나간다. 뷰는 **`paid` 만** 든다. 청크 12 가 `paid_at` 을 만들면 조건을 그쪽으로 옮긴다 | 완료 |
| 11c-2b | ~~셀러 주문 조회 API~~ | **완료** — `SellerOrderQuery`·`SellerOrderController`(`/api/seller/orders`), `ORDER_FORBIDDEN` 추가. 뷰만 읽어서 미결제 조건을 다시 안 적는다. 스코프는 `ProductQuery` 와 같은 방식으로 조건에 섞는다. **목록 거부는 403, 상세는 404** — 목록에는 가리키는 자원이 없어서 403 이 존재를 안 흘린다. **미결제 목록은 안 만들었다** — 마켓플레이스들이 「미결제확인」을 별도 탭으로 두므로 자리는 있지만 셀러가 그때 할 일이 없다. 만들면 뷰를 더 두지 말고 이 뷰의 조건을 파라미터로 받는 쪽을 먼저 본다 | 완료 |
| 11c-2c | ~~셀러 주문 계약에 발송 기한·반품 사유~~ | **완료.** `V37__seller_order_ship_overdue.sql` 신설, `SellerOrderQuery`, `SellerOrderVisibilityTest`. **`13g` 를 열다가 계약에 칸이 없는 것이 드러났다** — 목록은 `ship_due_at` 을 안 뽑고 `Summary` 에 칸도 없었으며, `V30` 이 뷰에 실어 둔 `return_reason` 은 `Detail` 이 안 받았다. **화면보다 먼저 친 이유는 비용이다**(순위 2번) — 응답 계약은 소비자 수에 비례해 값이 오르고 프론트 소비자가 아직 0이다. **「발송이 늦었나」 판정식을 뷰로 내렸다**(`is_ship_overdue`, `D22` 라 `is_` 로 시작하고 Java 필드는 `shipOverdue` 라 **JSON 은 안 바뀐다**) — 앱 SQL 두 곳에 같은 글자로 있던 것이 목록을 더하면 셋이 되는 자리였고, 기한 초과는 지연배상금 연 15% 가 걸린 도메인 규칙이라 화면마다 답이 갈리면 안 된다. **강제 지점은 뷰→계약 대조 테스트다** — 기존 `ViewColumns` 는 표→뷰만 봐서 이 구멍을 못 잡았다. 새 테스트는 뷰 컬럼마다 **계약에 싣거나 `NOT_IN_CONTRACT` 에 근거를 적거나** 둘 중 하나를 고르게 만든다. `returnReason` 을 빼고 돌려서 **실제로 빨간불이 뜨는 것까지 확인했다** | 11c-2b |
| ~~11c-2 원안~~ | 셀러 주문 조회 | 셀러 처리 화면이 부를 목록·상세. **`shop_order.status` 로 미결제 건을 거른다**(사용자 선택) — 결제 사실을 `shop_order` 한 군데에 두는 대신 셀러 조회가 주문 층을 같이 봐야 한다. **빠뜨려도 오류가 안 나서 뷰로 강제하고** 미결제 건이 안 뜨는 것을 테스트로 고정한다. **셀러 묶음의 노출 번호를 여기서 정한다** — `11c-3a` 이 전이 대상을 가리키려면 필요한데 `seller_order` 에는 내부 id 뿐이다(`D9`) | 11c-1, D9 |
| 11c-3a | ~~주문 처리 API~~ | **완료.** `OrderActionService`·`ShipmentController`(신설), `V20__order_action_permission.sql`, `OrderStatusPolicy`·`OrderStatusService`·`ErrorCode` 수정, 테스트 16개. **동작을 넷으로 갈랐다**(사용자 선택) — 상태 축의 단위가 `resource:action` 이라 `allowedStatuses` 가 역할을 못 본다. 하나로 두면 `delivered` 를 여는 순간 셀러가 대신 구매확정을 눌러 정산을 당기고, 고객에게 주면 고객이 `preparing → shipping` 을 밀어 셀러가 안 보낸 주문이 확정된다. **경로는 최상위다** — `seller_order_number` 가 전역 유일이라 주문번호를 앞에 붙여도 대상이 안 좁아진다(Stripe·Shopify 와 같은 조건). **상태는 조회한 행의 값 그대로 `Target.inStatus(...)` 로 간다**(사용자 지시). **반품에 법 조건 둘을 걸었다** — 청약철회 기한(`D2` R3)과 제한 상품(R4). 전이 서비스에 둬서 배치·관리자도 같은 검사를 지난다. **`ORDER_TRANSITION_NOT_ALLOWED` 를 422 → 409 로 고쳤다** — `D5` 「동작」과 RFC 9110 이 409 인데 `11-2` 가 422 로 넣었고, HTTP 경로가 없어서 밖으로 나간 적이 없다 | 완료 |
| 11c-3b | ~~상세의 `allowed_actions`·응답 형식 정리~~ | **완료.** `PermissionEvaluator.allowedActions` 신설, `OrderActionService.allowedActions`, `OrderQuery`·`SellerOrderQuery`·`ProductQuery`·`ErrorCode` 수정, `D5` 갱신, 테스트 8개. **조회용 판정 경로를 새로 냈다**(사용자 선택) — `decide` 로 동작 여섯을 물으면 상세 한 번에 거부가 넷씩 감사에 쌓이고 그건 침입 시도가 아니라 화면이 버튼 모양을 물어본 것이다. 감사는 3년 보존이라 진짜 시도가 묻힌다. **`decide` 에 플래그를 안 붙였다** — `4b` 가 기록을 판정 안에 넣은 이유(호출자가 빠뜨린다)를 무른다. **거르는 것이 둘이다** — 전이표와 권한·상태 축. 앞을 빼면 셀러가 `update_status` 하나로 셋을 쓰므로 `preparing` 에 「배송완료」가 뜬다. **청약철회 제한은 목록에서 안 본다** — 상품 속성이라 축이 아니고, 미리 알리는 것이 제한의 성립 요건이라(제17조제2항 단서) 화면이 상품에서 이미 받는다. **경로 이름을 동작 이름에 맞췄다**(`request-return`) — 소문자·하이픈이 곧 경로라 화면이 표를 안 든다. **구매자 상세에 `seller_order_number` 를 내린다** — 확정·반품의 단위가 묶음이라 없으면 동작을 못 부른다. **`OUT_OF_STOCK` 을 422 → 409 로**(사용자 선택) | 완료 |
| ~~11c~~ | 주문 조회·처리 API | **셋으로 쪼갰다**(위). 한 청크로는 파일이 6개라 안 떨어졌다. 원래 설명: 구매자의 주문 목록·상세와 셀러의 처리 화면이 부를 경로. 상세는 `order_status_history` 를 시간순으로 같이 내린다 — **전자상거래법 제6조 제3항이 소비자에게 거래기록 열람 방법을 요구한다**(`D2` R6). **셀러 조회는 `shop_order.status` 로 미결제 건을 거른다**(사용자 선택) — 결제 사실을 `shop_order` 한 군데에만 두는 대신, 셀러 쪽 조회가 전부 주문 층을 같이 봐야 한다. **빠뜨려도 오류가 안 나서 뷰로 강제하고** 미결제 건이 셀러 목록에 안 뜨는 것을 테스트로 고정한다. 전이를 부르는 것은 `11a` 가 권한을 붙인 뒤다. **전이 경로는 `Target.inStatus(...)` 로 상태를 실어 보낸다** — 안 실으면 상태 축이 거부한다(`11a`). **`allowed_actions` 를 상세에 같이 내린다**(`11a` 선택) — 권한 목록(`8a`)은 상태를 안 보므로 상태별로 지금 무엇이 되는지는 여기서 답한다. **청약철회 제한 상품의 반품 차단은 여기서 도메인 규칙으로 건다**(`D2` R4, `11a` 에서 옮겨 왔다) — 근거가 상품 속성이라 상태 축으로 표현이 안 된다 | 11-2, 11a, D5 |
| 11a | 상태 조건부 권한 | **완료** — `StatusPolicy`(auth)·`OrderStatusPolicy`(order) 신설, `Target` 에 `status` 추가. 축이 여섯이 됐다(`D6`). **표는 자원 패키지에 두고 `auth` 는 인터페이스만 안다**(사용자 선택) — 반대면 `auth → order` 순환이 는다. **표에 있는 동작인데 상태를 안 실으면 거부**한다. **`delivered` 를 지나면 셀러의 전이 권한이 닫힌다** — 안 닫으면 셀러가 청약철회 기산점을 조작한다(`D7`). **`8a` 권한 목록은 상태를 안 본다**(사용자 선택) — 상태별은 `11c-2` 상세가 내린다. **청약철회 제한 상품의 반품 차단은 `11c-2` 의 도메인 규칙으로 옮겼다** — 근거가 상품 속성이라 축이 아니다 | 완료 |
| 11-4 | ~~기한을 날짜로 되돌리는 자리~~ | **완료.** `OrderStatusService` 에 `END_OF_DAY` 상수(`999_999_000`)를 세우고 테스트를 왕복 검증으로 바꿨다(`keepsDeadlineDateAcrossRoundTrip`). **되돌려 보고 그 테스트만 깨지는 것을 확인**했다. 「말일이 쉬는 날이면 다음 영업일」 자체는 고정 날짜 테스트가 이미 보고 있어서, 새 테스트는 **그 계산이 DB 를 왕복해도 살아남나**를 본다 — 하루가 밀린 구간이 거기였다. 테스트 516개 통과. **이전 설명**: `OrderStatusServiceTest.movesDeadlineOffHolidays` 가 **말일이 금요일인 날에만 깨진다.** `endOfDay` 가 `LocalTime.MAX`(`23:59:59.999999999`)를 넣는데 Postgres `timestamptz` 는 마이크로초까지라 **올림해서 다음날 `00:00:00` 으로 저장한다** — 저장값은 「말일 24시」라 맞고, 그것을 `toLocalDate()` 로 되돌린 테스트가 말일+1 을 본다. **답이 정해졌다**(`D10` 「저장 정밀도가 마이크로초다」) — 문서가 처음부터 `.999999` 여섯 자리로 적어 뒀고 코드가 그것을 안 지켰다. `endOfDay` 를 `999_999_000` 으로 맞추고, **달력을 타는 단언을 고정 날짜 단위 테스트로 내린다**(`D15` 「오늘 날짜에 기대지 않는다」). `requireWithdrawable` 의 비교(`isBefore`)는 지금도 맞아서 안 건드린다 | 11-3, D10, D15 |
| 11b | 대리 관계와 미성년자 취소 | **개인정보법 제22조의2(아동 동의)를 흡수한다**(`D2-5`, `R29`) — 만 14세 미만은 법정대리인 동의를 받고 확인해야 하는데 지금은 생년월일이 없어 아동인지도 모른다. 이 청크가 만드는 두 칸(생년월일·법정대리인 관계)을 그대로 쓴다. 계정에 생년월일과 법정대리인 관계를 두고, 법정대리인이 남의 주문을 취소하는 경로를 연다. `scope=own` 의 "내 것" 정의가 하나 더 생긴다(`D2` R13)<br>**개인정보법 제38조제1항·제2항(대리인의 열람등요구)도 흡수한다**(`Q13`, `R28`) — 지금은 본인 세션만 받아서 대리인이 요구할 자리가 없다. 만 14세 미만 법정대리인과 **같은 두 칸**을 쓰므로 따로 세우면 그 스키마가 두 번 논의된다. **축**: **법**(개인정보법 제22조의2 아동 동의, 민법 제5조 미성년자 취소), `D2` R29. **강제 지점**: 「만 14세 미만이면 법정대리인 동의 행이 있다」를 트리거로 — 생년월일만 받으면 확인 없이 가입이 된다. **건드리는 자리**: `app_user` 에 생년월일·법정대리인 칸, `AuthSignupService`, 동의 항목 시드. 취소권은 주문 축(`OrderActionService`)에 붙는다 | 4, 5, 11-2 |
| 12-1 | ~~모의 결제 — 승인까지~~ | **완료.** `V22__payment.sql`·`MockPaymentGateway`·`PaymentService`. **`D11` 의 두 규칙이 부딪히는 자리였다**(사용자 선택) — 「선점·처리·저장을 한 트랜잭션」과 「트랜잭션 안에서 외부 호출 금지」가 결제에서 같이 걸린다. **PG 호출만 밖으로 빼고 우리 멱등키를 PG 에 그대로 넘겼다** — 재전송이 두 번 승인받는 구간을 우리 트랜잭션이 못 닫아서 PG 가 닫는다(실물 PG 와 같은 구조). **그 대가로 재생 확인도 밖으로 나왔다**(`IdempotencyService.replayIfPresent`) — 재전송 시점에는 주문이 이미 `paid` 라 상태 검사가 **재생에 닿기 전에 막았다.** 테스트가 잡았고 안 잡혔으면 재전송이 앞의 성공 때문에 실패를 받는다. **거절은 예외가 아니라 결과다**(사용자 선택) — 던지면 `payment_failed`·셀러 주문 자동 취소·재고 복구가 같이 롤백돼서 `D7` 이 정한 상태가 배치 말고는 안 쓰인다. **실패 유도는 테스트 카드번호**(사용자 선택, Stripe 관례) — 뒷 4자리 `0000` 거절·`0002` 무응답. `Retries` 에 판정을 받는 `on` 을 열어 결제사 무응답 재시도가 같은 간격표를 쓴다. 원래 설명: PG 흉내 모듈, 실패·재시도. **카드번호·유효기간·CVC 를 애초에 안 담는다**(`D2` R18 여신전문금융업법 제19조) — 모의 결제라 면제되는 게 아니라, 나중에 진짜 PG 를 붙일 때 스키마를 다시 파는 일이 된다 | 11-2, D11, D2, D14 |
| 12-2 | ~~결제 입구~~ | **완료.** `PaymentController`(`POST /api/payments`, 멱등키 필수·카드번호 형식·`Location`), `OrderQuery` 에 `payment` 필드, `HttpFlowTest` 둘. **결제 조회를 따로 안 냈다** — `V6` 의 `payment` 필드 그룹이 이미 그 자리고(`4d`), 경로를 새로 내면 주문 상세와 같은 것을 두 군데서 그린다. `V3` 의 `payment:read` 는 **남이 낸 결제를 보는 자리**(환불 `12a-2`·관리자 `16`)에 걸린다. **응답 표기를 고쳤다** — `Result` 가 그대로 응답이 되면서 `status`·`method` 가 `D5` 의 대문자 스네이크에 걸렸고, 저장값(`approved`)과 표기(`APPROVED`)를 상수로 갈랐다. **거절이 201 인 이유를 `D5` 에 적었다** — 4xx 로 던지면 기록이 같이 롤백된다. **`HttpFlowTest` 의 `payAll()` 을 걷어냈다** — SQL 로 상태를 밀던 자리라 전이표도 결제 기록도 안 거쳤다. 멱등 커버리지 목록(`OrderIdempotencyTest`)에 `/api/payments` 가 들어갔고, 경로마다 본문을 가른다 — 아무 본문이나 보내면 400 이 나긴 하는데 그건 키가 없어서가 아니다 | 12-1, D5 |
| 12a-1 | ~~환불 스키마·서비스~~ | **완료.** `V23__refund.sql`(`refund`·`refund_item`, 지연 트리거 3개), `RefundService`·`ExposedNumber`(신설), `MockPaymentGateway`·`BusinessCalendar`·`OrderStatusService`·`OrderService`·`ErrorCode` 수정, 테스트 18개. **요청 출처를 세 갈래로 열고 자기승인을 막았다**(사용자 선택) — 승인자와 요청자가 다르다는 것을 `refund_self_approval_check` 로 내렸고(강제 지점 2위) 앱은 403 으로 이유를 준다. **`R5` 가 코드에 닿았다** — 기한을 `due_at` 에 박제하고 **기산점을 요청 시각이 아니라 `closed_at` 으로 잡았다**: 요청 시각이면 늦게 요청할수록 기한이 밀려서 법보다 늦게 줘도 안 늦은 것이 된다. 「기한 안에 승인」은 `check` 로 못 건다 — 막으면 늦은 돈이 영영 안 나가서, 강제는 「넘긴 것이 조회로 드러난다」가 천장이다. **부분 환불은 항목·수량 단위고 수수료 절사 잔액을 마지막 수량에 몰아 준다**(사용자 선택) — 안 몰면 통째 환불인데 1원이 정산에 우리 몫으로 남는다. 배송비는 묶음이 비워질 때만 전액이다(항목별로 나눌 근거가 없다). **`12-1` 이 주석으로 남긴 구멍을 닫았다** — 조회와 기록 사이에 만료 배치가 끼면 PG 에는 승인이 남고 주문은 취소되는데, 그래서 **환불 가능 판정을 주문 상태가 아니라 결제 승인 유무로 본다**(`reason_code = payment_error`). **번호 생성을 `ExposedNumber` 로 뽑았다**(흡수) — 환불번호가 세 번째 사본이 될 자리였다. **`endOfDay` 도 `BusinessCalendar` 로 옮겼다** — 마이크로초 올림 함정(`11-4`)을 두 벌 들고 있게 된다 | 완료 |
| 12a-2 | ~~환불 입구~~ | **완료.** `V24__refund_permission.sql`·`RefundController`·`RefundQuery`(신설), `RefundService`·`OrderQuery`·`OrderFields` 수정, 테스트 19개. **권한을 둘로 갈랐다**(사용자 선택) — `payment:request_refund` 와 승인을 한 권한에 두면 요청을 여는 순간 승인이 같이 열려서 고객이 남의 환불을 승인한다(`V20` 이 `order:update_status` 를 넷으로 가른 것과 같은 자리). **승인이 관리자만인 근거는 법이다** — 제18조제2항 괄호가 「대금을 받은 자」를 통신판매업자에 넣어 환급 의무자가 우리고, 제20조의2제3항이 중개자 고지로 그 책임을 못 면한다고 한다. **목록을 기한 임박순으로 냈다** — `12a-1` 이 강제의 천장으로 둔 「넘긴 것이 조회로 드러난다」(`D2` R5)가 그 정렬로 선다. **`refund` 필드 그룹을 세워 셀러도 본다**(사용자 선택) — 정산에서 차감되는 돈이라 못 보면 명세를 확인할 자리가 없다. 요청 사유는 소비자가 쓴 자유 텍스트라 뺐다. **회귀를 하나 냈고 테스트가 잡았다** — 그룹을 조건 없이 붙이자 관리자가 배송지·결제를 잃었다(`V6`: 연결이 없는 규칙은 제한이 없는 것). 이미 연결이 있는 규칙에만 붙게 고치고 방벽을 뒀다 | 완료 |
| 12a-3 | ~~환불 요청 스위퍼와 기산점 갈래~~ | **완료.** `V25__refund_origin.sql`·`RefundSweeper`·`RefundSweeperTest`(신설), `RefundService`·`RefundController` 수정, 테스트 21개. 사유를 다섯으로 갈라(`cancelled`·`supply_failed`·`admin_cancelled`·`withdrawal`·`payment_error`) 기산점을 조문별로 맞췄다. **기산점 목록을 「닫힌 날에서 세는 것」쪽으로 만들었다** — 새 사유가 늘 때 빠뜨리면 이른 쪽으로 떨어져서, 실수가 위반이 아니라 우리 손해로 끝난다. 스위퍼는 이력의 `actor_type` 으로 사유를 고르고 **반품은 누가 눌러도 청약철회**로 본다(셀러의 반품완료는 반환 수령 사실이지 셀러가 일으킨 사건이 아니다). **`query(String.class)` 가 null 을 못 받는다** — 비었는지는 SQL 로 물었다. 원래 설명: 닫힌 묶음 중 | 완료 |
| ~~12a-3 원안~~ | 환불 요청 스위퍼 | 닫힌 묶음 중 환불 요청이 없는 것을 찾아 요청을 만든다. **법이 청약철회만으로 환급 의무를 발생시킨다**(`D2` R5) — 사람이 요청을 안 내면 3영업일이 그냥 흐르고 지연배상금이 연 15%로 붙는다(시행령 제21조의3). **전이 쪽이 아니라 배치다**(사용자 선택) — `OrderStatusService` 에 넣으면 `order → payment` 가 생겨 순환이 되고, `D23` 「의존」이 허용해 둔 순환은 `auth ↔ audit` 하나뿐이다. 몇 분 늦는 것은 3영업일 기한에 무의미하다. `OrderStatusBatch` 의 모양을 베낀다. **기산점 갈래를 같이 고친다**(점검 B) — `12a-1` 이 `reason_code = 'cancelled'` 하나로 소비자 취소와 셀러 공급 불능을 뭉쳤는데 **법의 기산점이 다르다**: 소비자 취소는 제18조제2항 3호로 청약철회한 날(`closed_at`)이고, **셀러 공급 불능은 제15조제2항이라 대금을 지급한 날**(`payment.created_at`)이다. 지금은 뒤엣것을 취소 시각에서 세서 **법보다 늦게 잡는다** | 12a-2, D19 |
| 11-7 | ~~하자 반품 접수~~ | **완료.** `V29__return_reason.sql`·`DefectReturnTest`(신설), `OrderStatusService`·`OrderActionService`·`ShipmentController` 수정, 테스트 9개(전체 656, 실패 0). **제17조제3항의 「제1항 및 제2항에도 불구하고」가 둘을 동시에 연다** — 하자 반품은 7일도 청약철회 제한도 안 걸리고 3개월이다. 그전까지 들어오는 반품을 전부 단순 변심으로 봐서 **8일째 하자 신고와 주문제작 상품의 하자를 거부**하고 있었다. **3개월은 박제 안 한다**(사용자 선택) — 역일이라 `delivered_at` 에서 세면 언제 계산해도 같고, 박제할 이유가 없는 값을 박제하면 같은 사실이 두 곳에 생긴다. **「안 날부터 30일」은 안 건다**(사용자 선택) — 소비자의 인식이라 관찰할 수 없고 제17조제5항이 입증을 우리에게 지웠다. 짧게 지는 쪽이 안전하다. **하자 주장을 안 거른다** — 같은 조항이 접수를 막을 근거를 없앤다. **제약을 `return_requested` 에만 걸었다** — 관리자가 접수를 건너뛰고 반품완료로 직행하는 경로가 있고(`D7`) 그 경우는 조항이 없는 것이 사실이다. 사실 아닌 값을 채우게 만드는 제약은 거짓말을 강요한다 | 완료 |
| 11-6 | ~~공급 기한~~ | **완료.** `V26__supply_deadline.sql`·`ShipDeadlineTest`(신설), `OrderService`·`OrderStatusService`·`OrderQuery`·`SellerOrderQuery`·`ProductService`·`ProductController` 수정, 테스트 9개(전체 631, 실패 0). **결제 승인 때 박제한다** — 선지급식이라 기산점이 대금 지급일이다. **공급시기 약정을 `product.supply_lead_days` 로 받는다**(사용자 선택) — 그 `null` 은 기본값이 아니라 「약정이 없다」는 사실이고, 없으면 법정 3영업일이 걸린다. 묶음은 그 안에서 **가장 긴 것**을 쓴다(한 번에 나가므로 가장 늦게 준비되는 항목이 발송 시점을 정한다). **`shipped_at` 을 같이 박제했다**(사용자 선택) — 없으면 늦게라도 보내는 순간 지연의 흔적이 사라져서 상습 지연과 한 번 지연이 같아 보인다. **뷰를 다시 만들어야 했다** — `seller_order_visible` 이 컬럼 목록을 굳혀서 표에 컬럼이 늘어도 안 따라오고, 셀러 조회가 그 뷰만 읽는다. **`seller_order` 쪽 기본값은 남겼다** — 빠뜨리면 법정 3영업일로 떨어지는 것이 안전한 방향이다. **약정 고지는 `14c` 다** — 약정은 고지가 성립 요건이라 상품 상세가 그려야 법적으로 선다. 원래 설명: 결제 시점에 | 완료 |
| D2-5 | ~~개인정보 보호법 조문 통독~~ | **완료.** `commerce-compliance.md` 에 `R28`~`R30` 신설, 「안 걸린다고 확인한 것」 표 아홉 줄, 「아직 안 적은 법」에 조문 넷. 코드가 안 생긴다. **`R28`(열람등요구)이 지금 어긋난다** — 제38조제4항이 「열람등요구의 방법과 절차는 **수집 방법과 절차보다 어렵지 아니하도록**」인데 가입은 화면이고 열람·정정은 API 뿐이다. **전자상거래법 제21조의2 4호와 같은 자리를 다른 법이 두 번 때린다** — `13e` 하나가 둘을 닫는다. **처리방침이 없는 경로를 가리킨다** — 「마이페이지에서 직접 하실 수 있습니다」라고 적어 뒀다. **지금 안 고친다**(사용자 선택) — 불변 트리거 때문에 개정이 곧 새 판이라 고쳤다 되돌리면 왕복이 이력에 남는다. `R29`(아동)는 `11b` 에 흡수했다(사용자 선택) — 생년월일과 법정대리인 관계 두 칸을 같이 쓴다. `R30`(유출 통지)은 통지 인프라도 **감지할 자리도** 없다 | 완료 |
| D2-3 | ~~전자상거래법 조문 통독~~ | **완료.** `commerce-compliance.md` 에 `R23`~`R27` 신설, 통독 절과 「안 걸린다고 확인한 것」 표, 「아직 안 적은 법」에 조문 일곱. **이 표가 그전까지 조문이 아니라 청크에서 왔다** — 코드를 만들다 걸린 것만 추가돼서 **코드가 없는 요건은 표에도 없었다.** 제1조부터 제46조까지 목차를 훑고 걸릴 만한 것의 본문을 확인했다. **둘이 결정적이다.** `R23`(제20조의3) — 청약 접수와 대금 수령을 다 하는 중개업자는 청약확인과 전자적 대금지급 통지를 **대신 이행하여야 한다**. 「못 면한다」가 아니라 직접 의무라 `R20` 의 급이 올라갔다. `R24`(제21조의2 1호) — **첫 화면 총액 표시**를 어긴다. 지금까지 나온 것 중 **유일한 금지행위**다 | 완료 |
| 14d | ~~첫 화면 총액 표시~~ | **완료.** `ProductQuery`·`CartService`·상품 목록·상품 상세·`purchase-panel`·장바구니 수정, `ProductQueryTest` 에 2개(전체 647, 실패 0). **총액이 큰 글자다**(사용자 선택) — 법이 막는 것이 「일부만 표시해서 **유인**」이라 상품가만 크게 두면 규제 대상이 된 그 관행이 된다. **장바구니까지 값으로 고쳤다**(사용자 선택) — 「배송지를 정해야 나오는 값」이라는 **사실 아닌 문구**를 들고 있었다. **강제 지점이 응답 계약이다** — `shippingFee` 가 빠지면 화면이 상품가만 그리고 오류가 안 나서, 값이 실리는지를 테스트가 고정한다. **3호는 안 걸린다** — 확인 대화상자가 `window.confirm` 이라 버튼 모양을 브라우저가 정한다. 브라우저로 밟아 목록·상세 22,000원(19,000+3,000), 장바구니 41,000원(38,000+3,000)을 확인했다. 원래 설명: 상품 목록과 상세가 배송비를 포함한 총액을 그린다 | 완료 |
| ~~14d 원안~~ | 첫 화면 총액 표시 | 상품 목록과 상세가 배송비를 포함한 총액을 그린다. **제21조의2 1호가 금지행위로 정한 것**이라(`D2` R24) 다른 미착수와 급이 다르다 — 안 한 것이 아니라 하고 있는 것이다. `15-2` 가 배송비를 공개 셀러 응답에 이미 더해 뒀으므로 **값은 있고 첫 화면이 안 쓰는 것**이다. **3호(선택항목의 시각적 차이)도 같이 본다** — 주문 취소·확정의 확인 대화상자가 한쪽 버튼만 눈에 띄게 그려져 있으면 걸린다 | 14, 14b, 15-2, D2 |
| D2-6 | ~~약관규제법 제8~14조 통독~~ | **완료.** `commerce-compliance.md` 에 `R31` 신설과 「안 걸린다고 확인한 것」 표 여섯 줄, 통독 대장 갱신. 코드가 안 생긴다. **`R31`(의사표시의 의제)이 지금 어긋난다** — 제12조1호가 「일정한 작위 또는 **부작위**가 있을 경우 고객의 의사표시가 표명된 것으로 보는 조항」을 무효로 하고 단서가 **「명확하게 따로 고지」**한 경우에만 살려 주는데, **자동 구매확정이 약관에 한 줄도 없고 화면은 날짜만 그린다.** 날짜는 고지가 아니다. **실질 불이익은 작다** — `autoConfirmLastDay` 가 청약철회 만료 다음날보다 앞설 수 없어 그때는 권리가 이미 끝나 있다. 그래도 제12조1호는 불이익의 크기가 아니라 **의사표시로 보는 조항인지**를 묻는다. **안 걸린 여섯 중 넷이 「그런 조항이 없어서」다** — 약관이 짧아서 안전한 것이라 조항을 더할 때마다 그 표를 다시 본다 | 완료 |
| D2-7 | ~~약관 제3판~~ | **완료.** `V36__terms_notice_wording.sql`(신설), 주문 상세 문구 수정, `MeConsentTest` 픽스처 수정. **셋을 한 판에 고쳤다** — `R31` 의 자동 구매확정 고지(제5조 신설), 탈퇴 유예가 **약관에만 30일**로 남아 있던 것(`Q10` 이 5일로 줄이고 `V34` 로 방침은 고쳤는데 약관만 안 따라왔다), 「분리하여 보관」이 **사실이 아니던 것**(제21조제3항 미착수. **하는 것만 적는다**). **시행일이 이레 뒤고 코드를 먼저 안 보냈다** — `V34` 는 덜 보관하는 방향이라 괜찮았는데 여기는 고지라 반대고, 자동확정은 청약철회 기간이 끝난 뒤에만 와서 그 이레에 권리를 잃는 사람이 없다. **개정판이 테스트 하나를 깼는데 그게 진짜였다** — 픽스처가 `order by version desc` 로 **시행 전 판**을 집었다. 운영 경로 둘은 `effective_at` 을 보는데 픽스처만 안 봤다 | 완료 |
| D2-8 | ~~정보통신망법·표시광고법·물가안정법 통독~~ | **완료.** `commerce-compliance.md`(`R32`~`R34` 신설, `R12` 출처 정정, 통독 절), `PLAN.md`(`13g-1` 신설, `58` 에 `R34`). **넷이 나왔다.** ① **`R12` 의 출처가 틀렸다** — 근거로 적혀 있던 **가격표시제 실시요령의 표시의무자가 오프라인 점포**다(매장면적 33㎡ 이상 등). 통신판매를 대상으로 적은 자리가 고시에 없고, 라벨·꼬리표로 개별 상품에 붙이라는 표시 방법도 화면에 안 옮겨진다. 전자상거래법 제13조제2항과 표시광고법 제3조로 옮겼다 — **`price_incl_vat` 는 안 바뀌고 바뀐 것은 그 이름을 정당화하는 조문**이다. ② **표시광고법 제5조 실증책임**(`R32`) — 사실과 관련한 문구는 실증할 수 있어야 하고 요청받으면 15일 이내인데 **받는 자리가 없다**. ③ **제4조 중요정보 고시**(`R33`) — 품목별 필수 항목이 비어도 안 걸린다. ②③은 `13g-1` 을 세워 달았다. ④ **정보통신망법 제50조의7**(`R34`) — 우리는 광고를 **보내는 쪽이 아니라 운영자 쪽**이고, Q&A 가 열리면 그 자리가 광고판이 된다. `58` 이 같이 닫는다. **제50조 제1~8항은 이 통독 직전에 `55`·`55a`·`55b` 가 원문으로 내렸다.** **안 걸리는 넷도 근거를 남겼다**(제50조의4·의5·의8, 물가안정법 제3조) — 빠뜨린 것과 일부러 안 넣은 것이 코드에서 구분이 안 된다. **밀도가 떨어진 것이 아니라 성격이 달라졌다** — 앞의 셋은 없는 요건을 찾았고 이번은 **있는 요건의 출처가 틀린 것**을 찾았다 | 완료 |
| D2-9 | ~~전자상거래법 제20조의3 통독~~ | **완료.** `commerce-compliance.md`(`R35` 신설, 통독 표 정정). **조문 넷을 원문으로 봤다** — 제7조(조작 실수 등의 방지)·제8조(전자적 대금지급의 신뢰 확보)·제13조제2항제5호·제14조제1항. **결론은 「새로 만들 것이 없다」다** — 넷 다 실물이 이미 있다: 제7조는 `/checkout` 이 청약 전에 판매자 정보와 금액을 보이고 장바구니로 돌아가는 길을 낸다(`15-2`), 제8조는 `payment_completed` 통지(`54a`)와 주문 상세 열람, 제13조제2항제5호는 계약내용 서면(`15-4`·`V31`), 제14조제1항은 `order_placed` 통지다. **그래도 요건을 세운 이유가 있다** — **근거가 없으면 다음 사람이 지운다.** 「법 요건은 코드에 없는 것이라 요건표에서 내려간다」의 반대 방향이다: 있는 것에 조문을 붙인다. **`D2-3`(전자상거래법 통독)이 이 조를 빠뜨린 이유도 적었다** — 조문 목차에서 「중개업자의 책임」으로만 읽혀서 **우리 얘기가 아니라고 넘어갔다.** 실제로는 **청약 접수·대금 수령을 하는 중개업자**를 지목하고 우리가 둘 다 한다 | 완료 |
| D18-1 | ~~알림 이력 보존 기간 재판정~~ | **완료.** 법 제6조와 **시행령 제6조제1항 각 호를 원문에서 확인했다** — 1호 표시·광고 6개월, 2호 계약 또는 청약철회 등 5년, 3호 대금결제 및 재화등의 공급 5년, 4호 소비자의 불만 또는 분쟁처리 3년. **거래 통지 넷을 호에 붙이면 전부 5년 칸이다**(청약 접수 제14조제1항→2호, 대금 지급 제8조제3항→3호, 공급 곤란 제15조제2항→3호, 환급 제18조제3항 단서→2호·3호). `D18` 의 3년은 감사 로그에 맞춘 우리 판단이었고 **그건 4호 칸의 값**이다. **3년 → 5년.** 여기서 `data-lifecycle.md` 의 「5년을 사는 표에는 사람 정보가 없다」와 부딪쳤다 — 본문에 이름·주소·금액이 들어간다. **두 층으로 갈랐다**(사용자 선택): 메타 5년 + **본문은 발송 + 6개월**. 법이 막는 것은 아니고(제6조제2항이 「보존할 수 있다」), **재량이라 우리가 골랐다.** 본문은 사본이고 원본이 전부 산다 — 문안은 템플릿 판, 금액은 `shop_order`·`payment`(5년), 시각·받는 사람은 메타. 6개월은 `order_shipping`·`payment_card` 선례를 따른 값이라 파기 배치가 조건 하나로 지운다. **광고성 정보 이력은 1호 6개월**(사용자 선택) — 동의 증명은 이 표가 아니라 `user_consent`(3년)가 한다. **강제 지점 둘을 `54a` 로 넘겼다**: 본문을 별도 표로 떼고 메타가 외래키로 가리킨다(한 표면 `null` 파기가 되고 안 비운 행이 섞여도 안 깨진다), 종류 컬럼 `check` 로 수명을 가른다(배치가 종류를 다시 판정하면 갈라지는 자리가 둘이 된다). 원래 설명: `D18` 이 발송 이력 보존을 **3년**(분쟁 대응 기간)으로 정했는데, 청약 접수 확인·환급 통지는 전자상거래법 제6조의 **「계약 또는 청약철회 등에 관한 기록」(5년)**으로 볼 여지가 있다. 3년이면 4~5년차 분쟁에서 **「보냈다」를 증명할 것이 사라진다**. **`54a` 보다 먼저 해야 한다** — 표를 만들 때 수명이 같이 들어가서, 뒤에 바꾸면 마이그레이션이 하나 더 든다 | 완료 |
| PLAN-1 | ~~미착수 행에 축을 채운다~~ | **완료.** `PLAN.md` 한 파일. 미착수 31행에 **걸리는 축과 강제 지점**을 적고, 표 머리에 그 기준과 완료 표시 두 형식이 같은 뜻이라는 것을 밝혔다. **「청크 하나를 어떻게 치나」를 세운 직후에 잰 것이 계기다** — 미착수 125행 중 45행에 축이 한 글자도 없었고(31행이 진짜 미착수, 14행은 완료인데 취소선만 없던 행), 그대로 두면 새 기준이 **다음 청크부터 바로 안 지켜진다**. 완료 103행은 안 고쳤다 — 이력이다. **처분 순위 2번이라 먼저 쳤다** — 청크가 늘수록 채울 행도 는다 | 완료 |
| PLAN-2 | ~~닫힌 청크가 분할표에서도 닫혔는지 대조한다~~ | **완료.** `PlanProgressConsistencyTest`(신설), `build.gradle.kts`·`PLAN.md` 수정. **`12a-2` 가 이력에는 완료인데 분할표에는 미착수로 이틀 남아 있었다** — 후속 `12a-3` 은 완료로 닫힌 채였고, 한쪽만 고쳐도 아무 명령이 안 깨져서 **다음 사람이 그 청크를 미착수로 읽는다**. 표시를 닫고 **대조를 테스트로 내렸다**(사용자 선택) — 이력에서 완료로 적힌 청크 번호를 분할표에서 찾아 그 행이 닫혔는지 본다. **한 방향만 본다**: 이력에는 점검·마무리처럼 분할표에 행이 없는 줄이 섞여 있어서 반대 방향은 통째로 거짓 경보가 된다. **`test` 의 입력에 두 문서를 신고했다** — 안 하면 문서만 고친 청크에서 Gradle 이 `UP-TO-DATE` 로 건너뛰어 대조가 한 번도 안 돈다 | 완료 |
| PLAN-3 | ~~미착수 행에 건드리는 자리를 채운다~~ | **완료.** `PLAN.md` 한 파일과 `CLAUDE.md` 한 문단. **미착수 74행 전부를 축·강제 지점·건드리는 자리 셋으로 통일했다**(사용자 선택) — 45행은 셋 다 없었고 29행은 앞의 둘만 있었다. **셋째 칸이 이 청크가 더한 것이다** — 「무엇을 신설하고 무엇을 고치나」가 어디에도 없어서, 청크를 잡기 전에 값을 잴 수가 없었다. **`PLAN-1` 이 왜 다시 필요해졌나**: 그때 채운 31행 뒤로 청크가 늘었고, 늘어난 행은 다시 비어 있었다 — **미착수가 늘수록 채울 것도 는다**는 것이 그때 순위 2로 친 이유고 여기서도 같다. 표 머리에 세 칸의 뜻과 **「예보라 치면서 달라진다」**를 적었다 — 달라진 것은 완료 행에 적고 미착수 행은 안 고친다 | 완료 |
| W1 | ~~세션 단위를 정의한다~~ | **완료**(2026-08-20) — `CLAUDE.md`. 「예열」(세션 열기 — 축을 **선언**까지 한다)·「마무리」(세션 닫기 — 대상은 **그날 친 청크 전부**)·「청크 하나를 어떻게 치나」(시작 3줄, 닫힘 5조건)를 세웠다. **`점검 F` 가 근거다** — 읽고도 안 걸린 것이 열하나라, 닫힘 조건에 **「이 청크가 남긴 강제 지점 한 줄」**을 넣었다. `W` 는 저장소를 굴리는 절차 자체를 고치는 청크다(`PLAN-1` 도 같은 종류다) | 완료 |
| W2 | ~~세션 스킬 둘~~ | **`2r` 이 흡수했다**(2026-09-10) — `preheat` 는 `/warmup`, `axis-sweep` 은 `/inspection` 이 됐다. 알맹이 셋 중 「지적 전에 주석·이력을 연다」는 `/inspection` 「코드를 안 읽고 요건표만 보고 판정하지 않는다」에 있고, `law.go.kr` 조회법은 `external-references.md` 에 이날 적었다. **원래 계획**: `.claude/skills/preheat/`(청크 세션 열기 — 축 1·2 두 줄을 **인라인**하고, `PROGRESS` 현재 상태와 **분할표 그 행 하나**만 읽는다. `PLAN-1` 이 행에 축을 적어 둬서 `coding-rules.md` 를 안 연다)와 `.claude/skills/axis-sweep/`(점검 단위 절차 — 축 하나·저장소 전체·**코드는 안 고치고** 축 문서와 `PLAN` 에만 내린다). **알맹이는 스킬이 아니라 그 안에 박는 셋이다** — `law.go.kr` 은 평문 URL 이 제목만 주고 `print=print` 판을 받아야 한다는 것(어제 왕복으로 알아냈는데 **어디에도 안 적혔다**), 「지적 전에 그 자리 주석과 이력을 연다」(`B4` 가 그래서 철회됐다), 「스윕 산출물은 후보다」(`S5` 를 확정으로 적어서 되돌리는 글을 또 썼다). 조회법은 `external-references.md` 에 절로 같이 넣는다 — 스킬은 절차, 문서는 사실. **축**: 프로젝트 규약. **강제 지점**: 스킬 본문이 절차를 고정한다. 다만 **삭는 건 못 막는다** — 그건 여전히 제약·테스트의 몫이다. **건드리는 자리**: 신설 `.claude/skills/preheat/`·`.claude/skills/axis-sweep/` 둘. 저장소 코드와 문서는 안 건드린다 — 읽는 순서만 정한다 | 완료 |
| W3 | ~~이력이 흩어지는 것을 막는다~~ | **완료 — 래칫으로 갔다.** `scripts/doc-lint.sh`·`PROGRESS.md`(「기록 규칙」)·`quality-gates.md`. **행이 적은 「과거 어긋남 둘」이 과소였다** — 재 보니 **일곱**이고, 그중 **299~393줄 95줄이 통째로 역순**이다(08-23→08-18). 한동안 위에 쌓다가(최신 먼저) 아래로 바꾼 흔적이고, 그 섬이 09-06 뒤에 끼어 있다. **날짜 둘이 실제로 갈라져 있다**: `08-12`(171·197줄), `08-18`(224·**394**줄 — 170줄 떨어졌다). 이 청크가 막으려던 증상 그대로다. **되돌리는 안을 버렸다**(사용자 선택): 95줄 이동은 **diff 를 아무도 못 읽고** 얻는 것이 「보기 좋음」인데, 이력은 읽는 순서보다 **찾는 것**이 목적이고 찾기는 `grep` 이 한다. **B→A 는 나중에 되고 A→B 는 안 된다.** **근본 원인을 같이 고쳤다** — 「기록 규칙」에 **순서 규칙이 아예 없었다.** 「이력은 시간순이고 새 줄은 맨 아래」를 절로 세웠다. **주제로 묶는 것은 분할표가 한다**는 문장을 같이 적었다(사용자 지적) — 그쪽이 청크순이고 이력이 시간순이라, 섞으면 「같은 말을 두 번 안 쓴다」가 깨진다. **부쉈다**(`D25`): 과거 날짜 줄을 끝에 심으니 「8곳 (기준선 7)」로 빨갛고, 기준선을 8로 올리니 「내릴 것」 안내가 떴다. **축**: 규약. **강제 지점**: 5위 → 4위(`doc-lint.sh`) | 완료 |
| W4 | ~~CI 액션 버전 올리기~~ | **`2f-1` 이 흡수했다**(2026-09-10 확인) — Dependabot 액션 PR 다섯이 머지돼 `checkout@v7`·`setup-java@v6`·`setup-node@v7`·`setup-gradle@v6` 이다. 릴리스 노트는 그 PR 본문이 들었다. **원래 계획**: **CI 가 처음 돌면서 알려줬다**(2026-08-23). 지금 안 깨지지만 곧 깨진다 — ① `actions/checkout@v4`·`setup-node@v4`·`setup-java@v4`·`gradle/actions/setup-gradle@v4` 가 **Node 20 을 목표로 해서 러너가 24 로 강제 실행**하고 있다(deprecation), ② **`setup-java v4` 는 더 이상 업데이트를 안 받는다** — v5 로 옮기라고 한다. **축**: **업계 관례**(`D23` 축 1 의 4순위 — 따르든 버리든 근거를 적는다). 액션 메이저 판을 올리는 것은 남의 기본값을 받는 일이라 **무엇이 바뀌는지 릴리스 노트를 보고 정한다.** **강제 지점**: 없다 — CI 가 그 자리다. 올린 뒤 **푸시해서 초록을 확인하는 것**이 유일한 검증이고, 그 사실을 이력에 적는다. **건드리는 자리**: `.github/workflows/ci.yml` 하나. 코드는 안 건드린다. **`2c` 를 흡수하지 않는다** — 그 청크는 CI 를 세운 것이고 이건 유지보수다 | 완료 |
| 5e-1 | ~~이메일 변경 확인~~ | **완료** — `V68__email_change_request.sql`(신설, 판 포함) · `EmailChangeService`·`EmailChangeMailer`(신설) · `notification/EmailChangeMail`(신설) · `EmailChangeTest`(신설, 여덟) · `MeController`(입구 둘) · `AccountService`(`verifyPassword` 분리) · `NotificationService`(주소를 밖에서 주는 갈래) · `ErrorCode`·`data-lifecycle.md`·`LengthConstraintTest`·`OpenApiSpecTest`. **강제 지점이 「안 바뀐다」에 있다** — 확인 전에는 `app_user.email` 을 손대지 않고 대기 주소를 다른 표에 둔다. 오타를 쳐도 **받던 주소가 살아 있다**. **`5c-1` 과 같은 뼈대**(해시만 저장 · 살아 있는 것은 하나 · 재사용은 DB 가 막는다)에 다른 점 둘: (1) **이미 로그인한 사람**이라 계정 존재 여부를 숨길 이유가 없어 이미 쓰는 주소면 그대로 거절한다 (2) **확인 메일만 새 주소로 간다** — 통지가 계정 주소로만 보내던 것을 좁게 열었다(`NotificationService` 의 주소 갈래, 패키지 밖에 안 연다). **비밀번호 재확인을 떼어 냈다** — `AccountService.verifyPassword` 가 「본인이 맞나」를 그대로 묻고, 그다음에 「그 주소가 그 사람 것인가」를 묻는다. **게이트 셋이 새 것을 잡았다**: 길이 회계(제약 둘 — 토큰 해시는 예외, **대기 주소는 쌍으로 이었다**), 수명 정책(표 하나), 스펙의 경로 공유(`Account` 를 주는 경로가 `POST /api/me/email` 에서 `…/confirm` 으로 옮겨졌다). **축**: **법**(`D2` R28 정정 요구) · `D14`. **강제 지점**: **DB 제약(2위)**. **닫힘**: 테스트 여덟 초록 · `verify.sh --full` 초록 · 빈 DB 기동 `applied_migrations: 68` | 54b, 5e |
| 5i-1 | ~~보존분 분리 저장~~ | **완료.** `V39__payment_card_split.sql`·`RetainedColumnsTest`(신설), `PaymentService`·`OrderQuery`·`TransactionPurgeService`·`TransactionPurgeBatch`·`PaymentServiceTest`·`TransactionPurgeServiceTest`·`data-lifecycle.md`·`commerce-compliance.md` 수정. **5년을 사는 개인정보는 카드 정보 하나였다** — 주문·항목은 금액·상품명이라 거래 사실이고 사람은 `user_id` 연결뿐인데 그 계정의 식별 정보는 탈퇴 5일 뒤에 비워진다(`5i`). **분리를 표를 가르는 것으로 했다**(사용자 선택) — `order_shipping` 이 이미 검증한 수단이고, 아카이브 스키마는 조회 경로가 둘이 되고 참조 무결성이 끊긴다. 수명을 배송지와 맞췄다(거래 종료 + 6개월). **카드가 사라져도 대금결제 기록은 5년을 채운다** — 금액·승인번호·수단·시각이 `payment` 에 남아서 제6조제1항이 요구하는 것을 안 잃는다. **강제 지점은 컬럼 목록 대조다** — 「카드 결제면 카드 정보가 있다」는 파기가 그 불변식을 정상적으로 깨서 제약으로 못 드는 대신, `RetainedColumnsTest` 가 5년 보존 표 여섯의 컬럼을 못박아 **새 컬럼이 생기면 거래 사실인지 사람 정보인지 고르게** 만든다. **드러난 것**: 자유 텍스트 넷(`order_status_history.reason`·`refund.request_reason`·`decision_reason`·`order_item.withdrawal_restriction_reason`)은 사람이 쓴 글이라 개인정보가 섞일 수 있다 — `5i-2` 로 세웠다 | 완료 |
| 5i-2 | ~~보존 표의 자유 텍스트 — 환불 사유~~ | **완료.** `V48__refund_note.sql`·`RefundNoteSchemaTest`(둘 다 신설), `RefundService`·`RefundQuery`·`TransactionPurgeService`·`TransactionPurgeBatch`·`RetainedColumnsTest`·`RefundSweeperTest`·`data-lifecycle.md`. **원래 넷을 한 청크로 잡았는데 열 파일이 넘어서 둘로 갈랐다**(사용자 선택) — 환불 사유 둘이 여기, 전이 사유가 `5i-3`. **환불이 먼저인 이유**는 `request_reason` 이 **구매자가 직접 쓰는 유일한 칸**이라 연락처가 섞일 위험이 제일 크다. **수명을 갈라 다른 표로 밀었다**(사용자 선택) — `refund_note` 를 만들고 거래 종료 + 6개월에 지운다. `order_shipping`·`payment_card`·`notification_body` 가 쓴 수단 그대로다. **`reason_code` 는 안 옮겼다** — `check` 로 닫힌 열거값이라 사람 글이 안 들어오고 「무슨 사유로 몇 건」은 그 값이 답한다. **옮기면서 잃을 뻔한 것이 있다** — 「반려에는 사유가 필요하다」가 `check` 였는데 컬럼이 표를 넘으면서 한 행 안에서 안 끝나게 됐다. **지연 제약 트리거로 내렸고**, 파기가 여섯 달 뒤 그 행을 지우는 것을 막지 않도록 **`refund_note` 에는 안 걸었다** — 검사할 시점은 반려하는 순간이지 그 뒤가 아니다. **넷 중 하나는 원래부터 자유 텍스트가 아니었다** — `order_item.withdrawal_restriction_reason` 은 `check` 로 닫혀 있다 | 완료 |
| 5i-3 | ~~보존 표의 자유 텍스트 — 전이 사유~~ | **완료.** `V49__order_status_history_note.sql`·`HistoryNoteSchemaTest`(둘 다 신설), `OrderStatusService`·`TransactionPurgeService`·`TransactionPurgeBatch`·`RetainedColumnsTest`·`OrderStatusHistorySchemaTest`·`RefundSweeperTest`·`data-lifecycle.md`. **예보보다 작았다** — 「읽는 자리 셋」을 적었는데 **`reason` 을 읽는 자리가 하나도 없다.** 화면도 조회도 안 쓰고 쓰기만 했다 — **평소에 아무도 안 보는 글이 5년을 살던 것**이라 옮기기에 제일 좋은 모양이었다. `order_status_history_note` 로 떼고 거래 종료 + 6개월에 지운다(`5i-2` 와 같은 수단). **옮기면서 잃을 뻔한 것이 또 있었다** — 「관리자 강제 전이에는 사유가 필수」가 `check` 였는데 표를 넘으면서 한 행 안에서 안 끝나게 됐다. **지연 제약 트리거로 내렸고**, 파기를 막지 않도록 note 쪽에는 안 걸었다. **`5i-2` 가 못 고친 자바독을 여기서 고쳤다** — `RetainedColumnsTest` 의 「자유 텍스트 넷」 주석이 백틱 표기가 달라서 치환이 조용히 실패했었다. **이제 5년 표에 자유 텍스트가 없다** — 남은 `order_item.withdrawal_restriction_reason` 은 `check` 로 닫힌 열거값이라 처음부터 자유 텍스트가 아니었다 | 완료 |
| D2-4 | ~~중개자 책임 문안 수정~~ | **완료.** `V28__liability_wording.sql`(신설 — 약관 제2판·청약철회 안내 제2판), `seller-identity.tsx` 수정, 테스트 6개 고침(전체 647, 실패 0). **고치는 이유가 무효 위험이었다** — 약관 제2조의 「책임은 판매자에게 있습니다」가 약관규제법 제7조1호(법률상 책임을 배제하는 조항은 무효)에 걸린다. 전자상거래법 제20조의2제3항이 **못 면한다**고 못박은 책임을 면하는 것처럼 읽혔다. **「당사자가 아니다」는 안 지웠다** — 제20조제1항이 그 고지를 법으로 요구해서, 지우면 그쪽을 어긴다. **더하는 것이지 무르는 것이 아니다.** 「누가 무엇을 책임지나」를 표로 나눴다(사용자 선택) — 제13조제2항8호가 요구하는 것이라 한 문장으로 뭉개면 반쯤 채운 상태가 된다. **재동의는 안 받는다**(사용자 선택) — 우리 책임을 넓히는 변경이라 불리한 변경이 아니고 `current_consent` 가 코드로 묶어서 옛 판 동의도 잡힌다. **청약철회 안내의 기산점을 `12a-3` 과 맞췄다** — 문안이 둘만 적는데 코드는 셋을 알고 있었다. **`R16` 이 약관규제법 제7조를 안 들고 있었다** — `D2-3` 이 전자상거래법만 통독한 한계가 바로 드러났다 | 완료 |
| 14c | ~~공급시기 약정 고지~~ | **완료.** `V35` 신설, `ProductQuery`·`CartService`·`OrderService` 수정, 상품 상세와 주문서에 고지, `ShipDeadlineTest` 2개. **제15조제1항 단서의 「따로 약정한 것」은 고지가 성립 요건이다** — `11-6` 이 값을 받는 자리를 냈지만 안 그려서 그 날수가 법적으로 안 섰다. **약정이 없어도 그린다** — 법정 기한이 걸린다는 것도 거래조건이다(제13조제2항 4호). **「영업일」이라고 밝힌다** — 우리 계산이 `BusinessCalendar` 라 「5일」로만 쓰면 주말이 끼어 이레가 되고 화면과 실제가 갈린다. **`Q2` 가 넘긴 것을 같이 쳤다** — `seller_order.agreed_lead_days`. 계산 결과인 `supply_lead_days` 로는 「약정 3영업일」과 「약정 없어 법정 3영업일」이 안 갈리고, 그러면 「이 주문에 약정이 있었나」에 답하려고 상품의 **지금** 값을 봐야 한다. **뷰를 또 빠뜨렸는데 `Q2` 의 컬럼 대조 테스트가 잡았다** — 세운 다음 첫 마이그레이션에서 바로 걸렸다 | 완료 |
| 14c-1 | ~~약정 유무를 묶음에 박제한다~~ | **완료 — 이미 되어 있었다.** 코드를 안 건드렸다. `V35__seller_order_agreed_lead_days.sql` 이 그 일을 이미 했다: `seller_order.agreed_lead_days`(null 이 「약정 없음」), 제약 둘(`0~60` 과 `supply_lead_days >= agreed_lead_days`), 뷰 갱신. `OrderService` 가 주문 시점에 상품 값을 그리로 박고, `ShipDeadlineTest` 가 **양쪽을 다 잡는다**(약정 없으면 null, 있으면 그 날수). **`점검 J` 가 `coding-rules.md` 의 처분 문장만 읽고 마이그레이션을 안 열어서 선 행이다** — `/inspection` 「코드를 안 읽고 요건표만 보고 판정하지 않는다」의 두 번째 사례로 그 절에 적었다. **예보한 강제 지점도 틀렸다**: 행은 `not null` 이라 적었는데 `V35` 는 **null 에 뜻을 실었다** — 그래야 「약정 없음」을 표현한다. **검증**: `integrationTest --tests '*ShipDeadlineTest*'` 초록. **축**: 법(제15조제1항 단서, `D2` R21) — 조문은 그대로 걸리고 이미 지켜지고 있다 | 완료 |
| ~~11-6 원안~~ | 공급 기한 | 결제 시점에 `seller_order` 에 공급 기한을 박제하고 넘긴 것을 조회로 드러낸다. **선지급식은 대금 지급일부터 3영업일**이다(`D2` R21, 전자상거래법 제15조제1항). **중개자 고지로 안 빠져나간다** — 제20조의2제3항이 제12조부터 제15조까지를 열거한다. 지금 `seller_order` 는 청약철회·자동확정·종료 셋을 박제하면서 **이것만 없어서** 셀러가 `preparing` 에 무한정 둘 수 있다. `11-2` 의 `freezeDeadlines` 와 같은 모양이고 박제 시점만 배송완료가 아니라 결제다 | 12-2, 11-2, D10, D2 |
| 15-4 | ~~계약내용 서면~~ | **완료.** `V27__order_contract.sql`·`OrderContractTest`(신설), `OrderService`·`OrderQuery`·`ConsentSchemaTest` 수정, 테스트 14개(전체 645, 실패 0). **주문이 그때의 판을 가리킨다**(사용자 선택 — `order_contract_document` 표). 대상이 두 표에 걸쳐 있어(`policy_document`·`consent_item`) `shop_order` 컬럼으로 두면 서로 다른 표를 가리키는 외래키가 나란히 붙고 문서가 늘 때마다 5년 보존 표에 마이그레이션이 들어간다. **호(5·6·8·9)로 담는다** — 법이 정한 것은 호지 우리 문서 구성이 아니라, 문서를 쪼개거나 합쳐도 값이 안 바뀐다. **약관도 다시 박제한다** — `user_consent` 는 가입 시점이라 가입 후 개정되면 주문 시점 약관이 없다. **불변 트리거가 이 설계의 전제였다**(법령 재확인에서 나왔다) — 전자문서법 제4조의2 2호가 「저장된 때의 형태로 보존」을 서면 요건으로 정해서, 본문을 고칠 수 있으면 교부했다는 서면이 서면이 아니다. `V21`·`V11` 이 주석으로만 적어 둔 것을 트리거로 내렸다. **시행 전인 판은 연다**(안 열면 오타 하나에 판이 하나씩 쌓인다). **8호 문서를 세웠다**(사용자 선택) — 법정 사실만 적고 없는 고객센터는 없다고 밝힌다(`13a-2` 의 선). **기존 테스트가 증명하던 외래키를 트리거가 가려서** 시행 전 판으로 그것을 다시 증명하는 테스트를 뒀다. 원래 설명: 주문 시점의 계약 조건을 그 주문에 남기고 | 완료 |
| 15-5 | ~~거래기록 보존·청약 전 표시 고정~~ | **완료.** `OrderRecordText`·`OrderRecordTextTest`·`checkout/summary.tsx`·`summary.test.tsx`(신설), `OrderController`·`orders/[orderNumber]/page.tsx`·`checkout/page.tsx`·`HttpFlowTest`·`api-guidelines.md`·`commerce-compliance.md` 수정. **둘을 닫았다.** 하나 — 제6조제1항 **후단**(보존)이 비어 있었다. `GET /api/orders/{번호}/record` 가 거래기록을 글 파일로 내리고(`Content-Disposition: attachment`) 주문 상세에 내려받기 자리를 냈다. **판정을 다시 안 한다**(사용자 선택: 서버가 파일을 만든다) — 입력이 주문 상세와 같은 조회라 남의 주문을 파일로 받는 두 번째 경로가 안 생긴다. 둘 — `R19` 의 강제 지점이 화면 코드뿐이었다. 청약 전에 보여야 하는 조각을 `checkout/summary.tsx` 로 빼고 **항목·수량·금액·배송비·상품 합계·총액·판매자 신원을 vitest 가 못박는다**. **상태 문구가 두 벌이 됐다** — 화면은 `lib/order-text.ts`, 문서는 서버 표다. 글자는 갈려도 되지만 **덮는 코드 집합**이 갈리면 새 상태가 한쪽에서 이름을 잃어서, 백엔드 테스트가 프론트 파일을 읽어 그 집합을 대조한다. `D5` 에 「파일 응답」 절을 세웠다 | 완료 |
| 12a-4 | ~~지연배상금~~ | **완료.** `V47__refund_delay_interest.sql`·`DelayInterestTest`(둘 다 신설), `RefundService`·`RefundQuery`·`RetainedColumnsTest`·`money-rules.md`. **이율을 원문에서 확인했다** — 시행령 제21조의3, **연 100분의 15**. 기준은 「3영업일을 **초과한** 시점부터 조치 시점까지의 기간」이고 `due_at` 은 `12a-3` 이 사유별 기산점으로 이미 박아 뒀다. **법이 「이자를 더한 금액의 환급 조치」라고 해서 계산이 PG 호출보다 먼저다** — 기록만 하고 안 보내면 조치를 안 한 것이다. **시각을 한 번 잡아 이자와 `decided_at` 이 같은 순간을 쓰게 했다.** **원 미만은 올린다**(사용자 선택) — `D8` 은 버림인데 그것은 **우리가 받는 돈**의 규칙이고 이것은 **물어 주는 돈**이라 방향이 반대다. `D8` 절사 절에 예외를 세웠고 **규칙은 여전히 하나다: 우리에게 불리한 쪽으로 자른다**. **하루가 안 찼어도 1일로 센다**(사용자 선택) — 한 시간 늦은 것에 0원을 물리면 「늦었는데 배상금이 0」이 된다. **`RetainedColumnsTest` 가 그 자리에서 걸렸다** — 5년 사는 표에 컬럼이 늘어서 거래 사실인지 사람 정보인지 고르게 만들었다. 금액이라 목록에 더했다 | 완료 |
| 12a-5 | ~~환불 승인 대기가 기한을 먹는다~~ | **완료.** `V51__refund_system_approval.sql`(신설), `RefundService`·`RefundSweeper`·`RefundSweeperTest`·`RetainedColumnsTest`·`RefundServiceTest`·`NotificationSweeperTest`·`batch-catalog.md`·`commerce-compliance.md` 수정. **셋 중 ①을 골랐다**(사용자 선택) — 시스템이 만든 요청은 시스템이 승인한다. 근거는 **법이 요청·승인 2단계를 요구하지 않는다**는 것이다(제18조제2항은 3영업일만 본다). 대기는 `12a-1` 이 만든 상태고 법은 그것을 기한에서 빼 주지 않는다. 스위퍼가 만든 요청은 **전량 환불**이라 사람이 판단할 여지가 애초에 없다. **강제 지점이 제약으로 내려갔다** — `refund_decision_check` 가 「승인에는 사람이 있어야 한다」를 박아 놔서(`V23`) 시스템 승인이 통과를 못 했다. 사람이 아니라 **승인자 종류**를 보게 바꾸고(`approved_by_type`, `V25` 가 요청자에서 한 것과 같은 모양), `refund_system_approval_scope_check` 로 **시스템은 자기가 만든 요청만** 승인하게 막았다 — 앱 조건으로만 두면 새 입구가 빠뜨린다. **제약이 그 자리에서 셋을 물었다** — `RefundSweeperTest.reject`·`RefundServiceTest`·`NotificationSweeperTest` 의 픽스처가 `approved_by_type` 없이 처리 상태를 만들고 있었다. **오늘 결정끼리 부딪친 자리 하나** — `RefundSweeperTest.waitsForApproval` 이 「돈이 나가는 결정은 사람이 한다」를 근거로 박아 둬서 이번 결정과 정면으로 어긋났다. 테스트는 초록이었을 것이다(만드는 회차만 보므로) — 근거를 고쳐서 만드는 회차와 승인 회차가 갈린 것으로 다시 적었다. **계량을 같이 넣었다** — `reportOverdue` 가 기한 넘긴 대기 수를 회차마다 `WARN` 으로 남긴다. **예보는 「회차에 남긴다」였는데 `batch_run` 이 아니라 로그로 갔다**(1차 마무리가 잡았다) — `RefundSweeper` 는 5분 `fixedDelay` 라 `batch_run` 의 `(배치명, 기준일)` 모델에 안 맞는다. 그 표는 기준일당 성공을 **하루 한 줄**만 담아서, 5분마다 세는 값을 넣을 자리가 없다. 지연배상금은 **승인하는 순간에만** 계산돼서(`RefundService.delayInterest`) 대기 중인 건의 이자는 어느 컬럼에도 안 쌓인다. **그 수가 사람에게 닿는 경로는 여전히 없다**(`A09` 와 같은 자리) | 완료 |
| 13-0 | ~~디자인 취향 스킬 설치~~ | **완료.** `.agents/skills/design-taste-frontend/SKILL.md`(1206줄) + `skills-lock.json`. 실행 스크립트가 없는 지침 문서 하나다. **저장소 안에 깔렸다** — `~/.claude` 가 아니라 프로젝트 `.agents/` 고, 본체와 잠금파일을 커밋했다(사용자 선택). `.claude/` 는 gitignore — 심볼릭 링크가 `core.symlinks=false` 때문에 절대경로 텍스트로 들어간다. **어디까지 쓸지를 `D20` 에 세웠다** — 기본은 쓰는 것이고, 주문 흐름·셀러 화면에서만 `§9`·`§4`·`§6` 로 좁힌다. 다이얼은 `trust-first` 줄(`3-4/2-3/4-5`)이고 값은 청크 13 이 확정한다. 스택 기본값은 우리 결정이 이긴다(`D23` 축 1). 대비비 4.5:1 은 스킬이 아니라 법에서 온 것이라 스킬을 안 써도 남는다. **이전 설명**: `npx skills add https://github.com/Leonxlnx/taste-skill --skill "design-taste-frontend"`. 생성형이 기본으로 뱉는 CSS 를 잡아 준다. **화면을 만들기 전에 깐다** — 다 만들고 깔면 이미 나온 스타일을 되돌리는 일이 된다. **팔레트가 대비비 4.5:1 을 통과하는지 여기서 본다**(`D20` 접근성) — 색을 고르고 나서 대비를 맞추면 팔레트를 다시 고르게 된다. 설치만 하는 청크라 코드가 안 생긴다 | 완료 |
| 13-1 | ~~프론트 기반~~ | **완료.** Next 16(App Router·TypeScript·Tailwind v4), `next.config.ts` 의 `/api/:path*` rewrite 프록시, `eslint-plugin-jsx-a11y` recommended 전체, `<html lang="ko">`, 백엔드 `internal-proxies` 를 루프백으로 좁힘. **화면은 안 만들었다.** 프록시는 실제로 띄워서 확인했다 — 3000 경유가 8080 직접과 같은 JSON 을 주고 401 도 그대로 온다 | 완료 |
| 13-2 | ~~로그인 화면~~ | **완료.** `src/lib/api.ts`(표기 변환·CSRF·오류를 `ApiError` 로), `/login`(서버 컴포넌트 + 클라이언트 폼), 색 토큰과 한글 글꼴(`Noto Sans KR`). **CSRF 를 래퍼가 같이 맡는다** — 안 실으면 로그인이 401 이라 화면만 만들면 안 돌아간다(원래 `5b` 자리였고 흡수했다). **키만 바꾸고 값은 안 바꾼다** — `allowed_actions` 의 값이 열거값이라서다. 프록시 경유로 CSRF→로그인→세션 유지→실패 문구까지 밟아 확인했다 | 완료 |
| 13c | ~~앱 셸과 화면 지도~~ | **완료.** `D20` 에 화면 지도(경로 14개)·자리표시 규칙·셸 절, `SiteHeader`·`SiteFooter`·`ComingSoon`, 자리표시 5개(`/products`·`/cart`·`/signup`·`/terms`·`/privacy`), 건너뛰기 링크. **지도를 그리다 빠진 것 다섯이 나왔다** — 회원가입·마이페이지·셀러 상품·셀러 주문 화면과 상품 상세 API 가 분할표에 아예 없었다(`13d`·`13e`·`13f`·`13g`·`8b` 신설). **로그인 화면의 「회원가입」 링크를 되돌렸다** — 자리표시가 있어서 이제 거짓 링크가 아니다 | 완료 |
| 13c-1 | ~~셀러 화면군의 자리와 공용 쪽 넘김~~ | **완료.** `components/pager.tsx`·`pager.test.tsx` 신설, `app/seller/layout.tsx` 신설, `products`·`orders` 목록이 그것을 쓴다. **`13g` 를 열다가 나온 청크다** — `Pager` 사본이 둘인데 셀러 목록이 셋째를 만들 자리였고, **이미 어긋나 있었다**(「색에만 기대지 않는다」 주석이 한쪽에만 남아 있었다). `pageNumberOf` 도 두 벌이었다. **공용본은 `params` 를 받는다** — 안 받으면 셀러가 셀러 하나로 걸러 놓고 2쪽을 눌렀을 때 거름이 풀린 목록이 나간다. **밀도 경계를 세웠다**: `D20` 이 `VISUAL_DENSITY` 를 구매자 5·셀러 7 로 정하고 「경계를 코드에서 가른다」고 적어 뒀는데 그 경계가 없었다. 경로 접두어가 이미 `/seller` 라 **라우트 그룹을 안 팠다**. **강제 지점은 구조다** — 사본이 하나면 어긋날 자리가 없다. 다만 데모 데이터로는 한 쪽에 다 들어가서 화면에서 `null` 만 나오므로 **검증은 테스트가 진다**(6케이스) | 13c |
| 13c-2 | ~~주문 화면의 공용 어휘를 한 자리로~~ | **완료.** `lib/order-text.ts` 신설, `lib/format.ts` 에 `priceText`, `orders/status.ts` 는 관객별 라벨만 남김, 부르는 곳 아홉 수정. **`13g` 를 열다가 범위가 늘었다** — 처음엔 `priceText` 사본 다섯이었는데 세어 보니 **여덟 자리**였다(이름 있는 사본 여섯 + `checkout-form.tsx` 의 인라인 둘). 상태 문구·`actionPath` 도 `app/orders/` 안에 갇혀 있어서 셀러 화면이 **라우트를 가로질러** 불러야 했다 — `lib/format.ts` 가 `13e` 에 「거기 두고 가져다 쓰면 화면 하나가 다른 화면군의 조각을 부르는 모양이 된다」고 적어 둔 바로 그것이다. **관객이 갈리는 것만 화면군에 남겼다**: 같은 묶음을 두고 사는 사람은 「반품 신청」을, 셀러는 「반품 완료」를 한다. `returnReasonText` 를 새로 넣었다(`11c-2c` 가 계약에 실은 것을 그릴 자리). **강제 지점은 구조다** — 사본이 없으면 어긋날 자리가 없다. 순수 이동이라 테스트를 새로 안 붙였고, `next dev` 로 상품·주문 목록·주문 상세를 띄워 금액·상태·동작 라벨이 그대로인 것을 봤다 | 13c-1 |
| 13d-1 | ~~동의 항목 공개 목록~~ | **완료.** `GET /api/consent-items`(목록), `ConsentService.listCurrent`, `V11` 에 `sort_no` 추가, `MeConsentTest` 5개. **가입 화면이 무엇을 물어야 하는지 알 방법이 없었다** — 있던 것은 `{code}` 하나짜리 조회뿐이라 화면이 코드 다섯을 박게 되고, `V11` 이 「코드가 아니라 데이터로 둔다」고 한 뜻이 사라진다. **본문을 안 내린다** — 약관 전문이 항목 수만큼 딸려 나온다. 펼칠 때 `{code}` 로 따로 받는다. **정형 넷은 내린다** — 동의받는 그 자리에서 목적·항목·기간·거부 시 불이익을 보여야 한다(개인정보법 제15조②). **`D5` 목록 규약(`items`·`page`·`size`)을 안 쓴다** — 쪽으로 자르면 2쪽의 필수 항목을 안 보여준 채 동의를 받는다. **정렬을 서버가 정한다**(사용자 선택) — 필수 먼저, 그 안에서 `sort_no`. **`sort_no` 를 새로 넣은 이유는 개정이다** — 기본키 순으로 두면 개정할 때 새 행의 id 가 커져서 **그 항목이 목록 끝으로 간다.** 이름순도 안 된다(「개인정보 수집」이 「이용약관」 앞). **종속이 부모 뒤에 오는 것은 스키마로 못 건다** — 다른 행을 봐야 해서 `check` 에 안 들어간다. 테스트가 유일한 방벽이고 **`sort_no` 를 25 로 낮춰서 진짜 깨지는 것을 확인했다.** `list(userId)` 정렬도 같이 맞췄다 — 갈리면 같은 항목이 화면마다 다른 자리에 뜬다 | 5-0, 5j, 5k |
| 13d-2 | ~~회원가입 화면~~ | **완료.** `/signup` 서버 페이지 + `signup-form.tsx`(클라이언트), `components/field.tsx` 신설(로그인 폼에서 옮겨 왔다 — 그 파일 주석이 이 자리를 예고해 뒀다), `PolicyBody` 를 `policy-document.tsx` 에서 내보냈다. **항목을 화면에 안 박는다** — `13d-1` 의 목록이 준다. **본문을 서버가 그려서 넘긴다** — 마크다운 렌더러를 클라이언트로 내리면 그만큼 브라우저로 내려가는데 약관은 읽는 것이라 그럴 이유가 없다. **정형 넷은 접지 않는다** — 개인정보법 제15조②가 동의받을 때 알리라고 한 것이라 펼쳐야 보이는 자리에 두면 안 읽고 동의한 것이 된다. 접는 것은 약관 본문뿐이고 길어서다. **종속은 보이되 `disabled` 고 이유를 글로 적는다**(사용자 선택). **부모를 끄면 종속도 같이 꺼진다**(`D2` R14) — 서버가 어차피 거두지만 켜진 채로 두면 보낼 수 없는 동의가 켜져 보인다. **필수 미동의면 버튼을 못 누른다** — 누르게 두고 422 를 받으면 왕복이 헛돈다. 가입은 로그인이 아니라 `/login?signed-up=1` 로 보낸다. 실서버로 관통했다 — 체크 상태 전이 넷, 전문 펼침(강조 살아 있음), 실제 가입 후 `user_consent` 5행(`source=signup`, IP 기록)·역할 `customer`, 중복 가입 문구. 원래 설명: `/signup`. 계정·비밀번호·동의를 한 화면에서 받는다. **항목은 `13d-1` 의 목록이 준다** — 코드를 화면에 박지 않는다. **약관 본문은 `<details>` 로 그 자리에서 펼친다**(사용자 선택) — 브라우저가 키보드·보조기술 동작을 기본으로 주므로 초점 가두기를 직접 안 만든다. 펼칠 때 `{code}` 로 본문을 받아 마크다운 강조를 살려 그린다(`13a-2` 의 `PolicyDocument` 와 같은 렌더러, `D20` 약관규제법 제3조①). **종속 항목은 보이되 누를 수 없게 하고 이유를 글로 적는다**(사용자 선택) — 숨기면 무엇을 더 켤 수 있는지 모르고, 이유 없이 회색이면 고장으로 본다. 필수 미동의와 야간 종속을 화면이 먼저 막고 서버가 다시 막는다(`5-2`). 비밀번호는 **15자**다(`D14-1`). `13c` 가 둔 `/signup` 자리표시를 지운다. **`R16` ①③ 의 마지막 칸이 이 화면이다**(점검 A) | 13d-1, 13a-2, 13c, 5-2, D2, D20, D24 |
| ~~13d 원안~~ | 회원가입 화면 | **둘로 쪼갰다**(위). 목록 API 가 없어서 백엔드 작업이 딸려 왔다. 원래 설명: `/signup`. 계정·비밀번호·동의를 한 화면에서 받는다. **약관 본문은 `13a-1` 가 쓴 것을 `5k` 의 조회 API 로 가져와 그린다** — 화면에 문구를 박으면 개정판이 갈린다. 필수 미동의와 야간 수신 종속을 화면이 먼저 막고, 서버가 다시 막는다(`5-2`). 마크다운 강조를 살려서 그린다(`D20` 약관규제법 제3조). **`R16` ①③ 의 마지막 칸이 이 화면이다**(점검 A) | 13a, 13c, 5-2, 5k, D2, D20 |
| 13e | ~~마이페이지 화면~~ | **완료.** `me/page.tsx`·`me/account-forms.tsx`·`me/consent-list.tsx`·`me/withdraw/page.tsx`·`me/withdraw/withdraw-form.tsx`·`lib/format.ts` 신설, `site-header.tsx`·`login/page.tsx`·`signup-form.tsx`·`orders/status.ts`·주문 화면 둘 수정, `D20` 화면 지도에 `/me/withdraw` 추가. **법 요건 둘을 닫았다** — `R28`(열람등요구를 수집보다 어렵지 않게)과 `R24` 4호 나목(가입과 다른 방법으로만 탈퇴). 처리방침 제7절의 「마이페이지에서 직접」이 이제 사실이다. **결정 넷을 사용자가 골랐다** — 화면은 `/me` 와 `/me/withdraw` 둘(탈퇴만 자기 화면을 갖는다), 머리 링크는 지금 건다, 처리정지·이의제기는 방침 제7·9절을 가리키는 문구로 둔다, 탈퇴 뒤에는 `/login?reason=withdrawn`. **`13b` 의 머리 링크를 흡수했다**(사용자 선택) — 화면만 만들고 링크를 안 걸면 주소를 아는 사람만 열람·탈퇴를 하게 돼서 제38조제4항이 안 닫힌다. 로그인/로그아웃 갈림과 권한 가리기는 `13b` 에 남았다. **드러난 것 둘을 같이 쳤다** — 가입이 `?signed-up=1` 로 보내는데 로그인 화면이 `reason` 만 읽어서 **가입한 사람에게 아무 말도 안 하고 있었다**(`reason=signed-up` 으로 통일), `dateText`·`dateTimeText` 에 두 번째 사용자가 생겨 `lib/format.ts` 로 올렸다(사본이 셋인 구간을 안 만든다). **안 한 것**: 처리정지(제37조)·이의제기(제38조제5항) API, `_visible_field_groups` 의 빈 배열이 「전부 본다」인지(`13b`). 화면은 키의 유무로만 가른다. 검증은 `npm run build`·`npm run lint` 까지고 **HTTP 로는 안 밟았다**(Docker 데몬이 안 떠 있었다) | 완료 |
| 13f | ~~셀러 상품 화면~~ | **완료.** `app/seller/products/page.tsx`·`app/seller/products/new/page.tsx`·`lib/product-text.ts`·`lib/product-text.test.ts`(전부 신설), `components/site-header.tsx`. **분할표가 없는 입구를 전제했다** — 「등록·수정·내림」을 적었는데 `/api/seller/products` 는 **목록만 내린다**. 화면만 그리면 눌러도 아무 일이 안 나는 폼이 되므로 **목록은 진짜로 만들고 등록은 `ComingSoon` 으로 뒀다.** 그것이 `ComingSoon` 의 **첫 사용자**고, `13c` 가 만들어 두고 안 써서 `D20` 의 「아직 없는 화면은 준비 중으로」가 셀러 경로에 한 번도 안 걸려 있던 것을 여기서 걸었다. **밀도 7 이라 표다** — `13g` 의 받은 주문과 같은 모양이고 컨테이너를 다시 안 만든다. **상태는 색으로만 안 가른다**(WCAG 1.4.1) — 막힌 것은 글자로도 적고, 「검수 대기」와 「판매 차단」이 둘 다 안 팔리는데 **할 일이 정반대**라 줄마다 안내를 붙였다. **머리의 링크 판정을 주문과 갈랐다** — 같은 역할에 붙어 있다고 한 검사로 묶으면 역할을 쪼갤 때 안 보이는 링크와 보이는 화면이 어긋난다. **강제 지점**: `npm run lint` 의 접근성 규칙 + 상태 문구를 vitest 로 고정. 데모 데이터의 상품이 전부 판매 중이라 **검수 대기·차단은 눈으로 못 밟는다** | 완료 |
| 13f-1 | ~~셀러 상품 등록·수정 입구~~ | **완료. `13g-1` 을 흡수했다**(사용자 선택) — 같은 폼·같은 컨트롤러라 따로 치면 두 번 연다. `V50__product_substantiation.sql`·`app/seller/products/new/product-form.tsx`·`ProductSubstantiationTest`(전부 신설), `ProductService`·`ProductController`·`SellerQuery`·`SellerController`·`app/seller/products/new/page.tsx`·`data-lifecycle.md`. **`13f` 가 적은 전제가 틀렸다** — 「서버에 만드는 입구가 없다」고 했는데 `/api/products` 에 `POST`·`PUT`·`submit-review` 가 **다 있었다.** `SellerProductController` 만 보고 판단한 결과고, 그래서 이 청크는 화면 + 실증자료가 됐다. **`ComingSoon` 을 지웠다** — 그것이 `13f-1` 의 목적이었다. **실증자료를 등록 시점에 받는다**(`R32`, 표시광고법 제5조) — 요청이 오면 15일 안에 내야 하는데, 등록 때 안 받으면 그 문구를 쓴 사람이 떠난 뒤에 근거를 찾게 된다. **주장 하나에 근거 하나**라 한 칸에 안 섞는다. **없어도 안 막는다** — 제5조는 「요청이 오면 낼 수 있어야」지 「모든 상품에 있어야」가 아니다. **`R33`(중요정보 고시)은 조건으로 미뤘다**(사용자 선택) — 고시가 품목별로 항목을 정하는데 **상품에 카테고리 컬럼이 없어서 강제할 축이 없다.** **어느 셀러로 올리나를 서버가 정한다** — 신설 `GET /api/me/sellers`. 화면이 셀러 번호를 고르게 두면 남의 번호를 넣어 볼 수 있고, 막는 것이 화면이 되면 판정이 두 곳이 된다. **폼은 옵션 없는 상품까지다** — 옵션 조합을 화면에서 만드는 것은 그 자체로 한 청크다. 눌러도 아무 일이 안 나는 폼보다 좁게 도는 폼이 낫다 | 완료 |
| 13g | ~~셀러 주문 화면~~ | **완료.** `seller/orders/page.tsx`(목록)·`[sellerOrderNumber]/page.tsx`(상세)·`actions.ts`(라벨 표) 신설, `site-header.tsx`·`site-header.test.tsx` 수정, `order-actions.tsx` 를 `components/` 로 옮김, `orders/status.ts`·`orders/[orderNumber]/page.tsx` 가 그것을 쓴다. **목록이 답하는 물음은 「무엇부터 보내나」**라 발송 기한이 줄마다 있다(`11c-2c` 가 실은 값). 늦었는지는 뷰가 답하고(`V37`) 화면이 날짜로 다시 안 잰다. **밀도 7 이라 카드가 아니라 표다** — 좁은 화면에서 가로로 밀고 칸을 안 접는다(접으면 기한과 상태가 다른 줄로 간다). **반품 사유를 화면 위쪽에 둔다** — 하자면 반환 비용이 셀러 몫이고 기한이 3개월이라(제17조제3항·제18조제9항) 묻어 두면 못 보고 거절한다. **동작 러너를 공용으로 뺐다** — 셀러 버튼을 새로 쓰면 100줄짜리 사본 둘이 된다. 절차는 하나가 지고 **라벨 표만 관객별**이다(사는 사람의 「반품 신청」과 셀러의 「반품 완료」). **머리는 `order:update_status` 로 가른다** — 역할 이름을 화면에 안 박고(`D24`), `/api/me` 대신 `/api/me/permissions` 하나만 불러 요청이 두 번이 되는 것을 막았다. **드러난 것**: `notFound()` 가 200 으로 나간다 — 기존 화면도 그랬고 주석은 아니라고 적혀 있었다(정보 처분, `stack.md`) | 13c-2, 11c-2b, 11c-2c, 11-2, D20 |
| 13g-1 | ~~상품 문구의 실증과 필수 항목~~ | **완료 — `13f-1` 이 흡수했다**(사용자 선택). 같은 폼·같은 컨트롤러를 두 번 열지 않으려는 것이고, `13f-1` 행에 무엇을 했는지 적었다. **`R32`(실증)는 닫혔고 `R33`(중요정보 고시)은 조건으로 남았다** — 고시가 품목별로 항목을 정하는데 상품에 카테고리 컬럼이 없어 **강제할 축이 없다.** 조건: **카테고리 축이 서면** | 완료 |
| 8b | ~~상품 상세 API~~ | **완료.** `ProductQuery.findPublicDetail`, `GET /api/products/{id}`, `WithdrawalRestrictionReason` enum, 테스트 10개. 조건이 목록과 같다 — 갈리면 목록에 없는 상품이 상세로 열린다. **재고는 수량이 아니라 있고 없고만 내린다.** 청약철회 제한 사유는 법이 고지를 요구해서 공개로 나가고 enum 을 지난다(`D2` R4). **보안 함정 하나를 밟을 뻔했다** — 같은 경로에 `PUT`·`DELETE` 가 있어서 경로만 열면 비로그인이 상품을 고치고 지운다. `GET` 만 열었고 그 조건을 테스트가 지킨다 | 완료 |
| 13b | ~~화면 권한 반영~~ | **완료.** `VisibleFieldGroups` 신설, `AccountService`·`OrderQuery`·`SellerOrderQuery`·`PermissionCatalog` 수정, `site-header.tsx` 재작성, `logout-button.tsx`·`site-header.test.tsx` 신설, `api-session.ts` 에 `apiSessionOptional`. **머리가 누구에게나 「로그인」을 보여줬다** — 주문서까지 온 사람에게도 그랬다(`15-2`). 로그인하면 로그아웃·내 정보·내 주문이 뜬다. **비로그인에게 「내 주문」을 안 그린다** — 누르면 튕기는 링크는 갈 곳이 있는 것처럼 보이게 하는 것이다(`D20`). **세션 쿠키 유무로 안 가른다**(사용자 선택) — 만료된 세션도 쿠키는 남아서 「로그아웃」을 그려 놓고 누르면 튕긴다. 대신 **401 을 `null` 로 돌려주는 입구**를 냈다(머리는 모든 화면에 있어서 보통 입구를 쓰면 공개 화면이 공개가 아니게 된다). **`_visible_field_groups` 의 빈 배열이 「다 보인다」와 「아무것도 안 보인다」 둘을 뜻하던 것을 닫았다**(사용자 선택) — 제한이 없으면 그 자원의 그룹을 **전부** 싣는다. 판정 엔진 안에서는 `Allowed` 가 타입으로 갈라 뒀는데 **직렬화하면서 뭉개진** 자리다. 권한 목록은 자원 이름이 문자열로 돌아서 **표를 읽는다** — `auth` 가 `order` 를 가리키면 의존이 거꾸로 서고, 표와 enum 의 불일치는 `FieldGroupTest` 가 이미 막는다. **셀러·관리자 링크는 아직 없다** — 걸 화면이 미착수다(`13f`·`13g`·`16`) | 완료 |
| 13a-1 | ~~정책 문안과 조회~~ | **완료.** `V21__policy_document.sql`(테이블 + 문안 둘), `policy/PolicyQuery`·`PolicyController`, `PolicyQueryTest` 11개, `POLICY_NOT_FOUND`, `PUBLIC_PATHS` 에 `/api/policies/**`. **`consent_item` 과 표를 가른 이유는 성격이다** — 저쪽은 동의받고 `user_consent` 로 남는 것이고 이쪽은 알리기만 한다. 한 표에 섞으면 「동의 안 받는 항목」을 목록 조회마다 걸러야 하고, **빠뜨리면 가입 화면이 처리방침을 동의 항목으로 띄운다.** **개정판 설계는 같다**(`code`+`version`+`effective_at`) — 처리방침은 시행 7일 전 고지 의무가 있어서 「언제부터 어느 판인가」에 답해야 한다. **`code` 에 `check` 를 안 건다** — 코드가 그 값으로 분기하지 않고 그대로 조회 조건에 넣는다(`D23`), `consent_item.code` 와 같다. **청약철회 안내를 지금 썼다**(사용자 선택) — 반품 축(`43`·`44`)이 없어도 되는 이유는 **적는 것이 우리 정책이 아니라 법 조문**이라서다. 조문은 국가법령정보센터에서 원문을 확인하고 옮겼다(제17조①②③·제18조②⑨⑩). **본문에 법이 요구한 절이 있는지를 테스트가 고정한다** — 조회만 보면 문안에서 절이 빠져도 초록이다. **이용약관은 안 고쳤다** — 제3조가 이미 정확하고, 고치면 `consent_item` 에 v2 가 생겨 동의 흐름이 딸려 온다(`13d-1` 몫) | 9, 13-2, D2, D14, 5j |
| 13a-2 | ~~정책 화면~~ | **완료.** `components/policy-document.tsx` 신설(마크다운 렌더러 + 문서 셸), `/terms`·`/privacy`·`/withdrawal-guide` 셋, `site-footer` 에 청약철회 링크, `external-references.md` 에 MIT 기록. **`react-markdown` + `remark-gfm`** — 표가 CommonMark 가 아니라 GFM 이라 둘이 같이 온다. **원시 HTML 은 안 살린다**(`rehype-raw` 미사용) — 지금 본문은 우리가 쓰지만 셀러 글을 그리게 되는 날 그 결정이 방벽이 된다. **받은 props 를 펼치지 않는다** — react-markdown 이 AST 노드를 `node` 로 같이 넘겨서 그대로 펼치면 DOM 에 `node="[object Object]"` 가 요소마다 붙는다(실제로 붙었고 고쳤다). `th`·`td` 만 `style` 을 받는다 — GFM 이 표 정렬을 그것으로 넘긴다. **접근성 린트가 `{...props}` 안을 못 봐서** 제목·링크에 내용이 없다고 읽는다 — 규칙을 끄지 않고 `children` 을 명시했다. **몰 운영자 사업자 표시는 안 넣기로 정했다** — 실제 운영 사업자가 없어서 지어 넣으면 사실 아닌 고지가 된다(`D23` 「안 넣은 것도 근거를 남긴다」). 원래 설명: `/terms`·`/privacy`·`/withdrawal-guide`. **`react-markdown` 을 깐다**(사용자 선택) — 처리방침이 목록과 표를 쓰고, 직접 쓴 렌더러는 안 다루는 문법을 **오류 없이 원문 글자로 내보낸다.** 서버 컴포넌트라 브라우저로 내려가는 JS 는 0 이고, 원시 HTML 은 `rehype-raw` 를 안 붙여서 안 살아난다. **MIT 라이선스를 `external-references.md` 에 적는다**(`D23`). `/terms` 는 `GET /api/consent-items/terms_of_service`(`5k`), 나머지 둘은 `GET /api/policies/{code}`(`13a-1`) — **출처가 갈리는 이유가 동의받느냐다.** 강조를 살려 그린다(약관규제법 제3조①). `13c` 가 둔 `/terms`·`/privacy` 자리표시를 지우고 `D20` 화면 지도에 `/withdrawal-guide` 를 더한다 | 13a-1, 14b, D20, D24 |
| ~~13a 원안~~ | 정책 문안 | **둘로 쪼갰다**(위). 한 청크로는 파일이 8개라 안 떨어졌다. 원래 설명: 이용약관·개인정보처리방침·청약철회 안내를 화면에 붙일 문서로 쓴다. **이용약관은 처음 쓰는 것이 아니다**(점검 A) — `V11` 시드의 `terms_of_service` 가 이미 초안이고 제2조에 중개자 지위 고지(`R2`)가 들어 있다. **그것을 고쳐 쓴다** — 모르고 새로 쓰면 같은 문안이 두 벌이 되거나 문구가 갈린다. 캡차가 사용자 데이터를 구글로 보내므로 제3자 제공을 처리방침에 적는다(`D14`). **자동수집장치(쿠키) 절을 쓴다** — 비로그인 장바구니가 `CART-TOKEN` 으로 사람을 구분한다(청크 9). 설치·운영 목적과 **거부 방법**까지 적는다. 동의는 안 받아도 되지만(개인정보법 제15조①4호) 고지는 해야 한다. **수집 항목에 IP 를 적는다**(`user_consent.acted_ip`) — 스키마에 있는데 처리방침에 없으면 문서와 코드가 어긋난다(`D2` R11). 항목 목록은 `5j` 가 스키마에 넣은 것과 같아야 한다 | 9, 13-2, D2, D14, 5j, D20 |
| 14a | ~~셀러 신원 공개 조회~~ | **완료.** `SellerQuery`·`SellerController`·`MailOrderExemption`, 테스트 10개, 실서버 확인. 원래 설명: `GET /api/sellers/{셀러번호}`. **`R1` 이 상품 상세에 걸려 있는데 그 값을 내리는 경로가 없었다** — `seller` 에 신원 컬럼이 다 있고(`V4`) 공개로 나가는 자리가 하나도 없다. 상품 상세에 중첩하지 않고 경로를 가른 것은 **부르는 곳이 넷이라서다** — 상품 상세(`14b`), 주문서(`15-2`, 법이 요구하는 시점이 「청약 이전」이다), 소비자 요청 시 열람(R1), 나중의 셀러 상점. 파는 셀러만 열린다(`active`·살아 있음), 아니면 404 | 3c, 8, D2, D5 |
| 14 | ~~상품 목록 화면~~ | **완료.** `apiPublic` 서버 입구 신설(`api.ts`), `/products` 목록, `/products/{번호}` 자리표시, `next.config.ts` 에 `picsum.photos` 이미지 허용. **빈 상태가 둘이다** — 상품이 하나도 없는 것과 이 쪽에만 없는 것을 가른다(뒤쪽은 주소를 직접 고쳐 온 경우라 첫 쪽으로 가는 길을 준다). **쪽 번호를 다 안 늘어놓고 창(window) 5개로 자른다.** **갈 곳 없는 「이전·다음」은 링크가 아니라 `span`** 이다 — 비활성 링크는 보조기술이 만질 수 없다고 말한다(`D20`). **브라우저로 밟다 결함을 하나 고쳤다** — 제목이 두 줄인 카드만 커져서 **가격 줄이 카드마다 어긋났다.** `D20` 이 균등 그리드를 고른 이유가 가격 비교라 `grid-rows-[auto_1fr]` + `content-between` 으로 아래에 붙였다. 실서버로 상품 4건·44건 둘 다 확인했고 초점 순서·다크모드·모바일 단일 컬럼까지 봤다. 원래 설명: `/products`. 번호 페이징과 빈 상태(`D20`). **공개 데이터라 서버 컴포넌트로 그린다** — `api.ts` 에 쿠키 없는 공개 GET 입구를 하나 더 연다(그 파일이 예고해 둔 자리다). 홈의 「상품 보기」가 실제로 닿는 곳이 된다. `13c` 가 둔 `/products` 자리표시를 지운다 | 8, 13c, D20, D24 |
| 14b | ~~상품 상세 화면~~ | **완료.** `page.tsx`(서버)·`purchase-panel.tsx`(클라이언트)·`not-found.tsx` 신설. **법 고지 셋을 다 그린다** — `R1` 신원 일곱 칸을 표로(신고번호가 없으면 면제 사유가 그 자리에), `R2` 중개자 지위, `R4` 제한 사유를 사람이 읽는 말로. **「장바구니에 담기」를 넣었다**(사용자 선택) — 옵션을 고르는 이유가 담기말고 없다. **`R2` 문안은 화면에 짧게 쓰고 약관으로 링크했다**(사용자 선택) — 약관 본문을 잘라 오면 절 제목이 바뀔 때 고지가 조용히 사라지고, 전문을 붙이면 상품마다 약관 한 벌이 딸려 나온다. **문안이 두 벌인 것을 알고 둔다.** **숫자가 아닌 주소를 앞에서 404 로 막는다** — 서버에 물으면 400 이 오는데 그건 「고칠 수 있는 요청」이라는 뜻이라 사람이 볼 답으로 틀렸다. 조합 짝짓기는 **순서를 안 믿는다**(개수가 같고 전부 들어 있으면 같은 조합). 라디오는 `sr-only` 라 키보드로 고를 수 있다. **`을(를)` 을 안 쓴다** — 받침을 보고 조사를 고른다(`D20` 문구 규칙). 실서버로 옵션 고르기·품절 조합·담기(장바구니에 행이 남는 것까지)·404 셋·모바일·다크모드를 다 밟았다. 원래 설명: `/products/{번호}`. 옵션 고르기와 SKU 짝짓기, 품절 표시. **법이 요구하는 고지 셋이 여기 다 걸린다** — 셀러 신원(`R1`, `14a` 가 내려준다), 중개자 지위(`R2` — 통신판매의 당사자가 아니라는 것), 청약철회 제한 사유(`R4`, `8b` 가 이미 내린다) — **이건 표시가 제한의 성립 요건이다**(제17조제2항 단서). 지금은 `requireWithdrawable` 이 반품을 막는데 사람에게 보이는 자리가 없어서 **집행만 있고 성립 근거가 반쪽**이다. 이 화면이 그걸 닫는다(점검 A) | 8b, 14, 14a, D2, D20, D24 |
| 15-1 | ~~장바구니 화면~~ | **완료.** `api-session.ts`(세션 서버 입구), `CartService` 응답 보완, `/cart` 화면 둘(서버 + 클라이언트 잎사귀), `login/page.tsx` 에 만료 안내. **`D24` 가 예고만 해 둔 세 번째 입구를 냈다** — 서버 컴포넌트는 브라우저가 아니라 쿠키가 자동으로 안 붙어서 백엔드가 낯선 손님으로 본다. **파일을 가른 이유는 `next/headers`** 다: `api.ts` 에 들이면 그 파일을 쓰는 클라이언트 컴포넌트가 전부 빌드에서 깨진다. **나르는 쿠키가 둘**(`SHOPSESSION`·`CART-TOKEN`) — 장바구니는 로그인 전에도 사는 자원이라 세션만 나르면 비로그인 장바구니가 빈 채로 그려진다. **401 을 입구가 잡아 `/login?reason=session-expired` 로 보낸다**(`D20`). **백엔드 구멍 둘을 닫았다** — 카트 응답에 조합 라벨과 셀러가 없어서 같은 상품의 다른 조합이 글자가 똑같은 두 줄로 보였고, 주문서가 `R1` 을 채울 재료가 없었다. **브라우저로 밟아서 잡은 것**: 수량이 2인데 `19,000원` 만 있어 단가인지 줄 합계인지 안 갈렸다 — 줄 합계를 크게, 단가를 작게 바꿨다 | 12-2, 14b, 13c, D20, D24 |
| 15-2 | ~~주문서 화면~~ | **완료.** `/checkout` 서버 컴포넌트 + 폼(클라이언트), `components/seller-identity.tsx`(상품 상세와 공유), 백엔드 공개 셀러 응답에 배송비. **`R1`·`R2` 가 여기서 닫혔다** — 셀러별로 접은 신원 일곱 칸과 중개자 고지가 청약 이전에 그려진다. **배송비를 공개로 내렸다**(사용자 선택) — `14a` 가 수수료율과 같이 묶어 뺐는데 배송비는 사는 사람이 내는 돈이라 성격이 다르고, 제13조제2항이 가격에 배송료를 포함해 표시하라고 한다. 그 결정을 방벽으로 잡고 있던 테스트도 같이 뒤집었다. **결제 실패 시 주문번호를 들고 결제만 다시 부른다**(사용자 선택) — 주문을 또 만들면 재고가 두 번 빠진다. **멱등키가 둘**(`D11`)이고 재시도에도 같은 값을 쓴다. **브라우저로 밟아서 잡은 것**: 비로그인이 결제를 누르면 401 이 「결제하지 못했습니다」로 나왔다 — `api()` 도 401 을 로그인으로 보내게 고치고, 주문서는 그리기 전에 `apiSession('/api/me')` 로 막는다. 로그인한 적 없는 사람에게 「만료되었습니다」라고 하던 문구도 `login-required` 로 갈랐다 | 15-1, 12-2, D2 |
| 15-3 | ~~내 주문 화면~~ | **완료.** `/orders`(목록·번호 페이징), `/orders/{주문번호}`(상세), `order-actions.tsx`(동작 버튼), `orders/status.ts`(표기 공유). **`R6` 제3항이 여기서 닫혔다** — 처리 내역을 그려서 「언제 무엇이 있었나」에 답한다. **동작은 `allowed_actions` 만 그린다**(`11c-3b`) — 화면이 상태로 판단하면 규칙이 두 벌이 되고 어긋난 쪽은 눌러야 403 이 난다. **모르는 값은 건너뛴다**(`D5`). 셋 다 확인 대화상자를 받는다(`D20` 되돌릴 수 없는 조작) — 브라우저 기본 대화상자를 쓴다: 초점 가두기를 다시 만들 이유가 없고 물을 것이 예·아니오뿐이다. **브라우저로 밟아서 잡은 것**: 취소 확인 문구가 「환불됩니다」라고 약속했는데 **환급 경로가 아직 없다**(`12a-1`) — 화면이 없는 것을 말하고 있었다. 결제 완료 화면의 「주문 상세 보기」 링크도 여기서 이어졌다 | 15-1, 12-2, D2, D20 |
| 16 | 관리자 화면 | 역할·권한 편집. 권한을 데이터로 둔 보람이 나오는 자리. **역할을 주거나 회수하는 모든 경로에서 `PermissionRuleLoader.evict` 를 부른다** — 안 부르면 TTL 60초 동안 회수가 안 먹는다(청크 4a). **`GET /api/users/{id}` 가 여기서 생긴다** — 가입 응답(`5-2`)의 `Location` 이 그때 붙는다. `/api/me` 는 id 로 가리키는 경로가 아니라 지목할 자원이 못 된다. **축**: `D6` 권한 모델, `D20`. **강제 지점**: 역할을 주거나 회수하는 **모든 경로**에서 `PermissionRuleLoader.evict` 를 부르는 것을 테스트로 건다 — 안 부르면 TTL 60초 동안 회수가 안 먹는다(청크 `4a`). **건드리는 자리**: 신설 `app/admin/roles/*` 와 `GET /api/users/{id}`. 수정은 역할 부여 서비스와 `permission_rule` 조회 | 4, 13-2, 4a, D20 |
| 16a | 조직 역할 편집 화면 | 셀러 안에서 멤버에게 역할을 주고 회수한다. 전역 역할 편집과 화면을 가른다. **축**: `D6`(조직 역할), `D20`. **강제 지점**: 조직 스코프를 판정 엔진이 자르게 둔다 — 화면이 셀러 번호를 넘기는 순간 남의 조직을 편집하는 경로가 생긴다. **건드리는 자리**: 신설 `app/seller/members/*` 와 멤버 편집 입구. 수정은 `5a` 가 만든 멤버십 서비스 | 5a, 16, D20 |
| 16b | 임퍼소네이션 | 관리자가 사용자 시점으로 본다. 감사 로그 필수, 쓰기 조작은 막는다. **축**: `D14`(대행 세션), `D16`(감사 로그). **강제 지점**: 대행 중 쓰기를 필터에서 막고, 감사 로그의 행위자 종류에 대행을 새 값으로 넣는다 — 「누가 시켰나」가 안 남으면 임퍼소네이션은 그냥 권한 우회다. **건드리는 자리**: `SecurityConfig` 와 세션 속성, `audit_log` 의 행위자 값 | 4b, 16, D20 |
| 16c | 관리자 강제 전이 | `order:force_status` 를 만들고 CS 경로를 연다. **상태 축을 비켜 가는 유일한 동작이다** — `OrderStatusPolicy` 가 "표에 예외를 파지 말고 동작을 새로 만든다" 고 정한 자리고, 그래야 그 권한을 누구에게 줬는지가 데이터로 남는다. 사유 필수는 이미 걸려 있다(`order_status_history_admin_reason_check`). **지금 관리자는 `11c-3a` 의 경로로 표 안에서만 옮긴다** — 사유를 적으면 전이표 밖으로도 가지만 상태 축은 못 넘는다. **축**: `D7` 상태기계, `D14` 권한. **강제 지점**: 전이표 밖 동작을 **별도 권한 코드**로 두고 감사 로그(`4b`)에 남긴다 — 표에 예외를 파지 않는다. **건드리는 자리**: 신설 마이그레이션(`order:force_status` 권한)과 강제 전이 입구. 수정은 `OrderStatusService`·`OrderActionService` 와 관리자 화면 | 11c-3a, 16 |

청크 하나가 실제로 해 보니 크거나 작으면 그때 쪼개거나 합친다.
## 재점검 처분 청크

**2026-08-20 축별 재점검이 찾은 스물아홉을 자리별로 묶은 것**이다.
발견 번호는 `F`(법, `D2`)·`S`(표준, `D5`·`D20`)·`B`(강제 지점, `D23`)·`P`(규약, `D7`·`D11`·`D15`·`D20`·`D24`)·
`C`(관례, `stack.md`·`batch-catalog.md`) 다.

**축이 아니라 자리로 묶었다.** 축 1은 **한 자리에서 두 규칙이 부딪칠 때** 쓰는 저울이고,
부딪치지 않는 여러 자리의 차례는 비용이 정한다(`CLAUDE.md` 「무엇부터」).
`F3`(법)과 `B3`(강제 지점)이 같은 자리인데 축 순서로 치면 **0행을 막기 전에 화면을 그리게 된다** —
그 순서로는 빈 서면을 그리는 화면이 먼저 서고, 그때 쓴 테스트가 그 모양을 고정한다.

**「지금 깨져 있는 것」이 없다** — 테스트가 다 초록이라 1순위가 비어 있고, 실질 시작은 2순위다.

**결정 넷은 받았다**(2026-08-20). 미등록 스킴 이름을 넣고(`S1`), 유예를 5일로 줄이고(`F4`),
화면 테스트는 Vitest + Testing Library 로 두고(`P2`), JPA 스타터를 `starter-jdbc` 로 바꾼다(`C1`).

| # | 청크 | 무엇을 하나 | 선행 |
| # | 청크 | 결과 | 커밋 |
|---|---|---|---|
| Q1 | ~~오류 `type` 스킴 교체~~ | **완료.** `urn:shop:` → `tag:projectshop.example,2026:error:`(RFC 4151). **화면은 접두어를 모른다** — `ApiError` 가 슬러그를 떼어 주고 화면 아홉이 그것으로 분기한다. 다음에 또 바꿀 때 고칠 자리가 `api.ts` 하나다. **`S5` 는 방향이 반대였다** — `about:blank` 가 아니라 프레임워크 오류가 전부 `validation-failed` 로 뭉쳐서 **상태 코드보다 `type` 이 더 뭉쳤다.** 상태 코드로 갈랐다 | `b0f4f1c` |
| Q2 | ~~`seller_order` 제약·뷰~~ | **완료.** `V30` — `ship_due_at` 지연 제약 트리거(조건이 다른 표의 상태라 `check` 로는 못 막는다), 뷰 재생성, `refund` FK 인덱스 둘. **뷰와 표의 컬럼 대조 테스트**를 같이 세웠다 — 그것이 없어서 `11-6` 이 적어 둔 함정을 하루 만에 다시 밟았다. **`B4` 는 철회했다**(`V26` 주석이 이미 답을 적어 뒀다) | `40e1438` |
| Q3 | ~~계약내용 서면 완전성~~ | **완료.** `V31` 지연 제약 트리거. 조항 이름이 제약과 트리거 두 곳에 생겨서 **제약 쪽에서 읽어 하나씩 지워 보는 테스트**로 대조한다. 새 제약이 기존 테스트 넷을 깨서 `OrderFixture` 에 붙이는 헬퍼를 모았다 | `57d0444` |
| Q4 | ~~계약내용 서면 교부~~ | **완료.** `GET /api/orders/{번호}/contract-documents/{조항}` 과 주문 상세의 `<details>`. **그 주문이 가리키는 판**을 준다 — 발 아래 약관 링크는 최신판이라 개정 뒤에는 그 사이 고친 것을 들이미는 꼴이 된다 | `db6cd30`·`ae95849` |
| Q5 | ~~청약철회 제한 스키마~~ | **완료.** `V32` — 성립한 제한을 주문 항목에 박제한다. **`copyable_media` 는 값에서 뺐다**(포장 훼손은 물건이 돌아와야 아는 사실). 동의 없는 `made_to_order` 제한은 DB 가 막는다 | `5998b99` |
| Q6 | ~~청약철회 제한 판정·표시~~ | **완료.** 판정이 주문 항목을 읽고 주문서가 동의 칸을 낸다. **테스트가 옛 규칙을 굳혀 놨던 것을 뒤집었다** — 「복제 가능 매체는 반품이 막힌다」가 「안 막는다」가 됐다 | `d50d98e` |
| Q7 | ~~배송지 형식~~ | **완료.** `V33` + `@Pattern` + 화면. 우편번호는 고시가 정한 5자리고 전화번호는 자릿수만 본다 — 형식을 고시가 안 정한다 | `6f6be30` |
| Q8 | ~~프론트 골격~~ | **완료.** `loading.tsx`·`error.tsx`, 체크박스 24×24, 카드번호 `autocomplete` 근거. **규약이 요구한 파일이 없던 것이 가장 조용한 종류였다** | `94e3427` |
| Q9 | ~~화면 테스트 층~~ | **완료.** Vitest + Testing Library. 첫 판은 동의 목록의 판단(필수 항목에 철회 버튼을 안 그린다) | `7c81f48` |
| Q10 | ~~탈퇴 유예 5일~~ | **완료.** `V34` 로 방침 새 판. **시행일이 이레 뒤인데 코드는 즉시 5일**이다 — 덜 보관하는 방향이라 고지가 늦어도 이용자에게 불리하지 않다 | `60689a5` |
| Q11 | ~~`PATCH` 미디어 타입·`Location`~~ | **완료.** `application/merge-patch+json`, 주문 201 에 `Location` | `5358679` |
| Q12 | ~~401 challenge~~ | **완료.** 미등록 스킴 이름. 버린 길 셋을 `D5` 에 적었다 | `e488584` |
| Q13 | ~~`/me` 정보주체 권리~~ | **완료.** `POST /api/me/email`, 처리정지 문구. **`F7`(대리인)은 `11b` 에 흡수** | `0e3559f` |
| Q14 | ~~근거 정정 묶음~~ | **완료.** `D2` R9·R5, `D11` 멱등키 표, `stack.md` 프론트 근거, 배치 카탈로그 행. **`S5` 방향과 `B4` 철회도 같이 기록** | `f9e2bae` |
| Q15 | ~~JPA 스타터 교체~~ | **완료.** `starter-jdbc` 로. `spring.jpa` 설정 블록도 걷어냈다 | `2205650`·`66c8715` |
| Q16 | ~~화면 테스트 층을 채운다~~ | **완료.** `frontend/src/components/order-actions.test.tsx`·`backend/.../ErrorSlugScreenTest.java`(둘 다 신설), `backend/build.gradle.kts`·`testing-strategy.md` 수정. `Q9` 가 층을 세우고 화면이 스물인데 **테스트가 여섯 파일뿐이던 것**을 채우기 시작했다. **두 종류를 넣었다.** ① `OrderActions` 가 주석으로 못 박은 판단 넷을 고정했다 — 서버가 허락한 것만 그린다, **모르는 동작은 건너뛴다**, 되돌릴 수 없는 조작은 확인을 받고 거절하면 안 보낸다, 할 게 없으면 아무것도 안 그린다. **넷 다 빌드·린트·타입 검사를 통과하면서 틀릴 수 있다.** ② **오류 슬러그 층간 대조**(`ErrorSlugScreenTest`) — 화면 아홉 곳이 `switch (error.slug)` 로 갈리는데 백엔드가 슬러그를 바꾸면 **그 가지가 죽고 사용자는 영영 기본 문구만 본다.** 지금 21개가 `ErrorCode` 60개 안에 다 있어서 **초록으로 서고 앞으로의 어긋남만 잡는다**(`StackVersionConsistencyTest` 와 같은 종류). **화면이 아니라 백엔드에 뒀다** — 실물 목록이 거기 있고, 화면 테스트는 **같은 틀린 문자열을 쓰면 초록**이라 못 잡는다. **선례를 따랐다**: `OrderRecordTextTest.StatusLabels` 가 상태 코드 표를 같은 수법으로 대조한다. **의존성을 안 더했다** — 클릭뿐이라 `fireEvent` 로 됐다. `user-event`·`vitest-axe`·MSW·Playwright 는 조건을 적어 두고 안 넣었다. **함정을 하나 더 찾았다**: 슬러그 테스트를 부숴 봤더니 `test` 가 `UP-TO-DATE` 로 **아예 안 돌았다** — Gradle 이 프론트 소스를 입력으로 몰랐다. `comparedScreens` 입력을 걸어 고쳤고, **그 덕에 `OrderRecordTextTest` 도 같은 상태였던 것이 드러났다**(걸려 있는데 안 도는 대조). **축**: 규약(`D15`·`D20`). **강제 지점**: 4위(테스트) 둘 + 빌드 입력 선언. **넷을 부숴서 확인했다** | |
| Q17 | ~~되돌릴 수 없는 조작의 화면을 고정한다~~ | **완료.** `frontend/src/app/me/withdraw/withdraw-form.test.tsx`·`frontend/src/app/me/account-forms.test.tsx`(둘 다 신설). `Q16` 에 이어 화면 테스트를 채웠다 — 9파일 45케이스(전 7파일 34). **탈퇴 화면**: 비밀번호를 다시 받나, **대화상자를 겹치지 않고 곧장 보내나**, 성공하면 `location.replace` 로 나가나(뒤로 가기로 돌아와 없는 계정에 다시 누르는 것을 막는다), 실패 자리가 `role="alert"` 인가. **계정 수정 화면**: **지금 값을 볼 수 없으면 그 폼을 아예 안 그리나**(`D20` — 타입에서는 선택 인자라 빠뜨려도 컴파일이 통과한다), 비밀번호 폼은 늘 있나, 지금 값을 미리 채우나, **계정을 되찾는 통로(이메일·비밀번호)에만 현재 비밀번호를 받나**, 알림 자리가 폼마다 따로 있나. **이 청크의 값은 내가 쓴 테스트가 아무것도 안 보고 있던 것을 잡아낸 것이다.** 「대화상자를 겹치지 않는다」를 처음엔 **그리기만 하고** 검사해서, 컴포넌트에 `window.confirm` 을 일부러 넣었는데 **초록이었다** — 제출을 안 하니 불릴 기회가 없었다. **없는 코드를 고정하는 테스트는 실행 경로까지 가야 한다.** 폼을 제출하도록 고치니 그때부터 잡았다. **조용한 실패를 둘 더 밟았다**: `fireEvent.submit` 을 **버튼에 걸면 아무 일도 안 일어난다**(폼 요소에 걸어야 한다), `location.replace` 는 **재정의가 막혀 있어** `spyOn` 이 `Cannot redefine property` 로 죽는다(`stubGlobal` 로 `location` 을 갈아 끼운다). **축**: 규약(`D15`·`D20`). **강제 지점**: 4위 테스트. **넷을 부숴서 확인했다** — 확인 대화상자 주입, `replace` 를 `href` 로 교체, 조건부 렌더 제거, 그리고 **고치기 전 테스트가 못 잡던 것**까지. **`Q16` 의 `comparedScreens` 입력도 덤으로 확인됐다** — 프론트만 고쳤는데 백엔드 `test` 가 다시 돌았다 | |
| Q18 | ~~E2E 스모크 — 로그인→장바구니. 로컬에서 돈다~~ | **완료.** `frontend/package.json`·`playwright.config.ts`·`e2e/smoke.spec.ts`(둘 다 신설)·`.gitignore`·`testing-strategy.md`·`quality-gates.md`. **조건이 이미 차 있었다** — `D15` 는 「프록시·세션·CSRF 가 얽힌 버그가 한 번이라도 나면」이라고 적었는데 `13-2` 에서 「로그인이 200 인데 화면이 안 넘어가는」 결함이 났고 그때는 손으로 잡았다. **스모크 하나**: 로그인 → 상품 하나 열기 → 장바구니에 담기 → 담긴 것 확인. 시드 계정(`customer@example.com`)을 쓴다. **문구가 아니라 역할·레이블로 잡는다**(`Q9`) — `getByLabel`·`getByRole`. `data-testid` 를 하나도 안 더했다. **백엔드는 Playwright 가 안 띄운다** — Gradle 기동이 느려서 밖에서 올린다. `webServer` 로 띄우는 것은 Next 뿐이고 **`dev` 가 아니라 `start`** 다(개발 서버는 첫 요청에 컴파일해서 타임아웃이 흔들린다). **세운 날 바로 값을 했다**: 담기 버튼을 누르고 **결과를 안 기다린 채** `/cart` 로 갔더니 「담아 두신 상품이 없습니다」가 나왔다 — 요청이 끝나기 전에 화면을 읽은 것이고 **이 층이 아니면 안 보이는 종류**다. 성공 문구를 기다리게 고쳐서 초록. **부쉈다**: `next.config.ts` 의 rewrite 대상을 8081 로 바꾸니 `ECONNREFUSED` 로 죽었다. 되돌리고 다시 초록. **검증**: `npm run e2e` 통과(2.9초), `npm run build`·`lint`·`test`(45개) 그대로 — vitest 가 `e2e/` 를 안 줍는다. **축**: 규약(`D15`·`D20`·`D24`) + 관례(Playwright). **강제 지점**: 4위지만 **이 청크까지는 로컬에서만 돌아 사실상 5위**다. `Q18-1` 이 CI 로 내린다 | 완료 |
| Q18-1 | ~~E2E 를 CI 로 — `e2e.yml`~~ | **워크플로는 섰다.** `.github/workflows/e2e.yml`(신설)·`CLAUDE.md`·`quality-gates.md`. **`pull_request` + `workflow_dispatch` 만이다**(사용자 선택) — `ci.yml` 에 넣으면 청크마다 4~6분이 붙는데 이 층이 잡는 셋(프록시·세션·CSRF)은 청크마다 안 바뀐다. **PR 이 마무리 때 한 번뿐이라 리뷰와 같은 리듬**이 된다. step 열셋: 컴포즈로 DB·Redis 를 `--wait` 로 올리고, **백엔드를 배경으로 띄운 뒤 `curl --retry` 로 2분을 기다린다**(Playwright 의 `webServer` 는 Next 만 띄운다 — Gradle 기동을 거기 맡기면 타임아웃이 실행마다 흔들린다). 실패했을 때만 리포트를 올리고 백엔드 로그 100줄을 찍는다. **필수 상태 검사로는 안 걸었다** — 브라우저를 띄우는 층이라 흔들리면 그 빨강이 진짜 실패를 가린다. 첫 달 뒤 `D25` 가 정한다. **증거가 왔다** — 마무리 PR #30 에서 `smoke` 잡이 **1분 51초에 통과**했다. 백엔드를 배경으로 띄우고 `curl --retry` 로 기다리는 방식이 러너에서 그대로 돌았다. **원래 적어 둔 것**: `gh workflow run e2e.yml --ref work/2026-09-06` 이 **`workflow e2e.yml not found on the default branch`** 로 404 다 — `workflow_dispatch` 는 기본 가지에 그 파일이 있어야 뜬다. `2e-3` 의 제출 워크플로와 같은 자리다. **마무리 PR 이 열리면 `pull_request` 로 돌고 그것이 증거다.** 그때 「부순 날」 칸을 채우고 이 행을 닫는다. **로컬에서는 이미 부쉈다**(`Q18`) — rewrite 대상을 8081 로 바꾸니 `ECONNREFUSED`. **축**: 규약(`D15`·`D25`). **강제 지점**: 5위(로컬) → **4위**(PR 의 CI 잡) | 완료 |
| Q19 | ~~영업일·주문 기한 계산을 순수 함수로 뗀다~~ | **완료.** `BusinessCalendar.java`·`OrderStatusService.java`·`OrderDeadlines.java`(신설)·`OrderDeadlinesTest.java`·`BusinessCalendarTest.java`(둘 다 신설)·`OrderStatusServiceTest.java`·`testing-strategy.md`. **빠른 레인 69 → 87개(전체의 9%).** `BusinessCalendar` 의 셋(`nextBusinessDay`·`plusBusinessDays`·`isBusinessDay`)에 **휴일 집합을 받는 static 짝**을 두고 인스턴스 메서드를 껍데기로 만들었다. `OrderDeadlines` 는 record 하나로 청약철회 말일과 자동확정일을 낸다 — 상수 둘(`WITHDRAWAL_DAYS` 7·`AUTO_CONFIRM_DAYS` 8)과 요건 번호 주석이 서비스에서 여기로 왔다. **값은 하나도 안 바뀌었다**(`./gradlew build` 초록이 그것을 보인다). **테스트가 아홉 늘었다** — 평일·주말 보정·연휴·말일 보정이 두 기한을 붙이는 자리·휴일 표가 빈 연도, 그리고 **한 해를 통째로 훑어 「자동확정은 언제나 청약철회 말일보다 뒤」를 보는 것**. 마지막 것은 365번 도는데 컨테이너를 띄우면 못 돌린다. **통합 테스트에서 하나를 걷었다** — 같은 날짜(2026-08-10)를 보던 케이스라 단위로 옮기고 그 자리에 왜 옮겼는지를 주석으로 남겼다. 나머지 느린 것은 안 지웠다(DB 왕복을 보는 것은 그대로다). **예보와 다른 것 하나**: 휴일을 표에서 통째로 읽지 않고 **계산이 훑을 구간만** 읽는다(`holidaysAround`) — 임시공휴일이 계속 쌓이는 표라 전체를 들면 해가 갈수록 커진다. `plusBusinessDays` 는 세면서 나아가므로 창을 걸음 수만큼 넓힌다. **`D15` 에 규칙과 표를 세웠다** — 뗀 것 둘(`4c-0` 30배·`Q19`)과 **안 뗀 것 하나**(정산 지급액. SQL 이 되읽어 합산하는 설계라 앱 사본이 곧 「두 벌」이다). **축**: 규약(`D15`·`D10`) + **법**(제17조 기한 — 옮겼을 뿐 값은 안 바뀐다). **강제 지점**: 4위 테스트가 **DB 없이** 돈다 — 강도는 같고 되먹임이 빠르다 | 완료 |

**축 6(코딩 규칙)은 오늘 친 코드까지 포함해 돌렸다.** 셋이 나왔고 둘은 코드로 쳤다(`518f276`) —
**두 층에 흩어진 규칙이 갈리는 것**을 막는 테스트 둘이다(우편번호 형식, 오류 이름 접두어).
`K1`·`K2` 는 `D23` 본문에 넣었고 `K3` 는 위반이 아니라 관측이다.

**순서는 위에서 아래로 갔다.** `Q1`~`Q9` 가 2순위(쓰는 곳이 늘면 비싸진다)였고,
`Q10` 은 법이라 그 뒤에 바로 붙였고, `Q11`~`Q15` 는 값이 시간과 무관했다.

**남은 것은 셋이다** — 이메일 변경의 확인 절차(알림 축 `54a`~`56`),
처리정지 접수 창구(Q&A 축 `58`·`59`), 약정 유무의 박제(**`14c-1`** — `14c` 는 고지만 닫았다. `점검 J` 가 갈랐다).

## 기준 문서

여러 청크가 같이 참조하는 결정을 모아 두는 문서들이다. 한 청크에서만 쓰는 결정은 ADR 로 남긴다.
어느 청크가 어느 문서를 기다리는지는 `doc/reference/document-map.md` 에 있다.

| # | 문서 | 무엇을 정하나 | 선행 |
|---|---|---|---|
| D1 | 문서 트리 규약·용어집 | 문서가 어디 가나, 도메인 용어의 한↔영 대조 | 완료 |
| D2 | 법·정책 요건표 | 전자상거래법·개인정보법 요건과 설계 지점의 매핑 | 완료 |
| D3 | 비즈니스 모델 | 누가 누구에게 무엇을 팔고 돈이 어디로 흐르나. 수수료 주체와 요율 | 완료 |
| D4 | 도메인 모델 | 엔티티 관계와 경계. 무엇이 무엇을 참조하고 무엇이 같이 태어나고 죽나 | 완료 |
| D5 | API 설계 규약 | URL·동사·페이징·정렬·필터·오류 형식·버전. 우리가 정할 것만 본문에 쓰고 나머지는 Zalando RESTful API Guidelines 를 따른다 | 완료 |
| D6 | 권한 모델 명세 | 판정 규칙, 스코프·효과의 우선순위, 필드 그룹 | 완료 |
| D7 | 상태머신 카탈로그 | 주문·상품·정산·문의·반품의 상태와 전이표. 전이표는 코드에 선언한다(ADR 0009) | 완료 |
| D8 | 금액·통화 규약 | 저장 단위, 반올림, 부가세, 할인 적용 순서 | 완료 |
| D9 | 식별자 규약 | 내부 ID 와 외부 노출 번호를 어떻게 가르나 | 완료 |
| D10 | 시각·영업일 규약 | 저장 시간대, 영업일 계산, 배치 기준 시각. 공휴일은 `holiday` 테이블에 두고 임시공휴일을 배포 없이 추가한다(ADR 0009) | 완료 |
| D11 | 동시성·트랜잭션 규약 | 락 전략, 격리 수준, 멱등키 형식, 재시도 | 완료 |
| D12 | ~~이벤트 카탈로그~~ | **완료 — `event-catalog.md`**(2026-09-14 설계 세션). `type` 일곱, 원천 표 여섯, CloudEvents 1.0 봉투, 페이로드 규칙 넷, `outbox_event` 표 정의, 버전 규칙. 사용자 결정 셋: 원천은 아웃박스 표 + 트리거 · 봉투는 CloudEvents 이름 · 최소 한 번. 「이벤트 0개라 조건 미달」은 순환이었다 — append-only 표가 이미 사건을 적고 있었다 | 완료 |
| D13 | 데이터 수명 정책 | 보존 기간, 파기 대상, 삭제 방식 | 완료 |
| D14 | 보안 기준 | 세션·시크릿·파일 접근·서명·비밀 관리 | 완료 |
| D15 | 테스트 전략 | 무엇을 어느 층에서 검증하나, 컨테이너 사용, 매트릭스 형식 | 완료 |
| D16 | 관측 규약 | 로그 형식·레벨·추적 ID·지표 이름 | 완료 |
| D17 | ~~파일·미디어 규약~~ | 업로드 제한, 저장 경로, 접근 권한, 변환. **축**: `D14`(업로드 검증), 표준(MIME 판별). **강제 지점**: 없다 — 문서다. 제한값을 `26`~`28` 이 `check` 와 필터로 내린다. **건드리는 자리**: 신설 `media-rules.md`. **닫힘**: 문서가 서고 `26`~`28` 이 인용할 값이 정해진다 — 최대 바이트 수 · 허용 MIME 목록 · 저장 경로 규칙 · 접근 판정 방식(서명 URL 이냐 프록시냐) 넷에 **숫자와 이름이 박혀 있다**. 「나중에 정한다」가 하나라도 남으면 안 닫힌다. **완료**(2026-09-18) — `media-rules.md`. 넷을 다 박았다: **5 MiB** · `image/jpeg`·`image/png`(SVG·GIF·**WebP** 제외 — 근거 적음. WebP 는 JDK 가 읽지도 쓰지도 못한다) · `{버킷}/{자원}/{UUID}/{이름}.{확장자}` · **공개는 만료 5분 서명 URL, 비공개는 프록시**(사용자 결정). 더한 것 셋: **EXIF 를 저장 전에 지운다**(위치·시각이 개인정보다) · **매직 바이트로 판별하고 `Content-Type` 을 안 믿는다** · **썸네일 실패면 업로드가 통째로 실패한다**(「썸네일 없는 이미지」 상태를 안 만든다). **바이러스 검사를 안 넣은 근거**도 적었다 | 완료 |
| D18 | ~~알림 정책~~ | **완료.** `notification-rules.md`. **거래 통지와 광고성 정보를 먼저 갈랐다** — 같은 메일함에 도착해도 동의·야간·거부가 전부 갈린다. 거래 통지는 계약 이행이라 동의가 필요 없고 야간에도 나가며 **수신 거부 자리를 안 만든다**(안 보내면 우리가 위반이고, 사용자가 껐다는 사실이 그 위반을 안 덮는다). **채널은 이메일 하나**(사용자 선택) — 문자를 뺀 이유가 기능이 아니라 데이터다: 전화번호를 받는 자리가 없고 그건 최소수집과 부딪친다. **템플릿을 DB 에 판과 시행일로 둔다**(사용자 선택) — 코드 상수면 개정한 순간 지나간 발송의 본문을 복원 못 한다. **발송 이력에 본문을 그대로 남긴다**(사용자 선택) — 분쟁에서 「보냈다」를 증명하는 것이 이 표라서고, 대가로 개인정보가 한 벌 더 쌓여서 보존 3년에 거래기록 파기가 지운다. 강제 지점 다섯을 표로 적어 `54a`·`54b` 가 채우게 했다 | 완료 |
| D19 | 배치·스케줄 카탈로그 | **완료** — `batch-catalog.md`. 카탈로그 5개, 재실행 안전성 수단 셋, 실패 처리 **3층**(건·회차·체인), 실행 이력 `batch_run`. **체인이 있는 배치만 회차 재시도를 한다**(10분 3회) — 선행이 뒤를 막는 자리라 다음 회차까지 24시간이 밀린다. 체인 없는 것은 다음 회차에 맡긴다. 분산 잠금은 안 만든다(1대 전제) | 완료 |
| D20 | 화면·문구 규약 | **완료** — `screen-rules.md`. 권한 없는 것은 숨기고, 볼 수 없는 필드는 그 줄을 안 그리고, 오류는 자리를 가려서 보여준다. **화면 문구는 존댓말** — `CLAUDE.md` 의 평서형 규칙은 개발자가 읽는 글에만 걸린다 | 완료 |
| D21 | 성능 목표 | 응답 시간 기준, 데이터 규모 가정, 측정 방법. **측정값을 입력으로 쓴다** — 실행 계획을 한 번도 안 읽고 정하면 수치가 감이다. **축**: **측정값**이 입력이다(청크 `42` 의 실행 계획). 감으로 정하면 그 수치를 아무도 안 믿는다. **강제 지점**: 없다 — 문서다. 수치를 `70`(부하)이 검증한다. **건드리는 자리**: 신설 `performance-goals.md` | 42 |
| D23 | 코딩 규약 | 계층·예외·트랜잭션·SQL·주석·테스트 작성 방식. **바깥 표준이 아니라 코드에 이미 성립한 관례를 뽑았다.** 어긋난 둘(서비스의 HTTP 예외 → 7b, `AuditLogQuery` 문자열 결합 → 8)을 문서에 적어 뒀다 | 완료 |
| D24 | 프론트 계층 규약 | **완료** — `frontend-rules.md`. `D23` 이 백엔드 계층을 정했는데 프론트 쪽이 통째로 비어 있었다. 서버 컴포넌트가 기본이고 `"use client"` 쪽이 근거를 댄다. **`api.ts` 입구가 둘**(브라우저·서버)이고 **인증 화면도 서버 컴포넌트**다(사용자 선택) — 세션을 서버 입구가 손으로 나른다. **캐시 절은 번들된 Next 문서를 읽고 썼다** — `force-static` 이 `cookies()` 를 빈 값으로 만드는 것이 조용히 뚫리는 경로다 | 완료 |
| D22 | 명명 규칙 | DB 컬럼·테이블·기본키, Java 식별자. 기준은 `sqlstyle.guide` 와 Google Java Style. **새 테이블부터 기본키가 `<테이블>_id` 다** — 기존 13개는 안 바꾸고 벗어난 이유를 문서 맨 위에 적었다 | 완료 |
| D25 | ~~품질 게이트 기준~~ | **완료** — `quality-gates.md`. **게이트를 켜는 것과 그 게이트가 무엇을 막는지 아는 것은 다른 일이다** — `2e` 가 그것을 보였다(100건 중 실물 0). **층을 세었더니 둘이 한쪽에만 있었다**: gitleaks 는 CI 만, **`doc-lint.sh` 는 로컬만**이라 사람이 기억해야 돈다 — `2c-1` 로 세웠다. **문턱을 둘로 갈랐다**: 측정된 것(SpotBugs 새 검출 0·테스트 실패 0·CI 초록)과 **아직 못 정하는 것**(dependabot 자동 머지 여부·리뷰 반복 지적 목록). 뒤쪽에 잠정치를 안 박았다 — `D21` 이 막혀 있는 것과 같은 자리고, **박으면 그 값이 근거 없이 관례가 된다.** 대신 **무엇을 재면 정해지나**를 적었다. **새 처분 표를 안 만들었다** — 게이트가 낸 것은 「드러난 것의 처분」 셋으로 간다. **AI 리뷰 지적은 반드시 처분한다**(사용자 선택) — 「오탐이다」도 처분이고 근거를 적는다. 안 그러면 지적이 PR 과 함께 묻히고 **그것이 이 문서가 막으려는 세 번째 더미**다. **제외 근거를 셋으로 닫았다**(오탐·구조적·도달 불가) — 셋 다 아니면 코드를 고친다. **격자에 새 세로줄을 안 만들었다** — 세로 다섯은 `D23` 이 정한 목록이라 품질 게이트는 「축이 아닌 것도 훑는다」 쪽이고, 「어디까지 봤나」에 줄 하나로 앉았다. **주간 리포트(`2h`)와 묻는 것이 다르다** — 리포트는 「지금 무엇이 쌓여 있나」, 점검은 **「이 게이트가 아직 제 역할을 하나」**다 | 완료 |

## 확장 청크

16b 이후다. 순서는 의존이 허락하는 한 자유롭게 잡는다.
각 그룹이 기다리는 기준 문서는 `document-map.md` 에 적혀 있다.

| # | 청크 | 무엇을 하나 | 선행 |
|---|---|---|---|
| 17 | ~~정산 스키마~~ | **완료.** `V52__settlement.sql`·`SettlementSchemaTest`(둘 다 신설), `money-invariants.md`·`data-lifecycle.md` 수정. 표 셋을 세웠다 — `settlement_cycle`(월 하나에 한 행, 셀러와 무관)·`settlement`(셀러 하나의 그 주기 정산서)·`settlement_item`(정산서 한 줄). **알갱이는 주문 항목 건별이다**(사용자 선택) — 종류별 합계 네 줄로 두면 정산서 안에 근거가 없어서 「이 수수료 합이 어느 주문에서 나왔나」에 답을 못 하고, 그때 주문 표를 다시 더하면 그 계산이 마감 때와 같다는 보장이 없다. **`17-1` 의 등식 다섯 중 넷이 내려갔다**: 지연 트리거(`payout_amount = sum(items)`), **`check`(`carried_over = least(0, payout_amount)`)** — 문서는 테스트라고 적었는데 한 행 안에서 결정되는 값이라 한 층 아래로 내렸고 문서를 고쳤다, `unique(주기, 셀러)`, 그리고 **부분 유니크 넷**. 남은 둘(합계 대조)은 마감 배치가 값을 만들기 시작하는 `19` 가 건다. **드러나서 등식이 하나 늘었다** — `(셀러, 주기)` 유니크는 **같은 주기를 두 번 마감하는 것만** 막는다. 같은 주문 항목이 **다른 주기의 정산서에** 실리면 두 정산서 각각은 합이 맞고 셀러가 돈을 두 번 받는다. 그래서 근거 참조 넷에 **정산서를 넘어선 전역 유니크**를 걸고 `money-invariants.md` 에 여섯째 줄로 적었다. **공급자를 받지 않고 만든다** — 부가가치세법(`D2` R17)이 상품대금(셀러→고객)과 중개수수료(플랫폼→셀러)를 갈라야 한다고 하는데, 문서가 「공급자 컬럼을 따로 받아서 채우면 종류와 어긋난 행이 생긴다」고 적어 뒀다. **생성 열**로 두면 어긋날 방법 자체가 없다(`D23` 축 2 의 1위, 구조). **부호도 종류가 정한다** — 수수료를 양수로 담고 뺄셈을 앱이 하면 「합이 곧 지급액」이 성립을 안 해서 지연 트리거가 아무것도 못 막는다. **테스트가 그 자리에서 둘을 물었다** — 이월 픽스처가 같은 주기에 정산서를 두 장 만들고 있었고(`(셀러,주기)` 유니크), 생성 열의 null 을 `query(String.class).single()` 로 읽고 있었다 | D3, D8, D10, 12, D2, 17-1 |
| 18 | ~~수수료 계산~~ | **완료.** `V55__settlement_commission_basis.sql`(신설), `SettlementService`·`SettlementSchemaTest`·`money-invariants.md` 수정. **`17` 의 정산 항목이 금액만 들고 있었다** — 셀러가 「이 수수료가 무엇에 몇 퍼센트냐」를 물으면 정산서가 답을 못 하고 주문 표를 다시 뒤져야 하는데, **그때 하는 계산이 마감 때와 같다는 보장이 없다.** 요율(`commission_bp`)과 기준 금액(`commission_base_amount`)을 정산 행에 굳혔다. **강제 지점이 한 행 안의 `check` 로 내려갔다** — `settlement_item_commission_amount_check` 가 「기준액 × 요율 = 금액」을 검사해서 **앱이 정산 시점에 다시 계산하는 경로를 안 만든다.** `order_item_commission_amount_check` 와 같은 모양이고 절사 규칙(버림, `D8`)도 같다. **환급 줄에는 안 박았다** — `refund_item.commission_refund` 는 원 수수료를 수량으로 나눈 값인데 **마지막 수량에 절사 잔액을 몰아 줘서** 「기준액 × 요율」을 넘을 수 있다(단가 10,005 × 3개 10% → 마지막 1개 환급 1,001 > 1,000). 등식은 물론 **상한으로도 못 쓴다.** 환급의 근거는 요율 재계산이 아니라 **원 수수료 금액**이고 그 등식은 환불 축이 이미 들고 있다. **`17` 의 스키마 테스트 여섯이 그 자리에서 물렸다** — 수수료 줄에 근거 없이 넣고 있었다. **비용 순위 2번으로 `59-1`·`58-1` 보다 먼저 쳤다** — 정산 항목 계약을 건드리는데 지금 소비자가 `SettlementService` 하나고, `20` 이 그 값을 내리기 시작하면 화면까지 같이 고친다 | 17 |
| 19 | ~~정산 마감 배치~~ | **완료.** `SettlementService`·`SettlementCloseBatch`·`SettlementCloseBatchTest`(셋 다 신설), `BatchRuns`·`application.yml`·`batch-catalog.md` 수정. **`D19` 3층 첫 체인이 섰다** — `auto_confirm` 이 그날 성공해야 `settlement_close` 가 돈다. 안 돌면 `WARN` + `batch_run` 에 `skipped`. **기준일을 실행일로 안 잡았다** — 판정이 「같은 기준일에 선행이 성공했나」라, 1일로 잡으면 **말일 자동 구매확정이 안 돈 것을 못 본다.** 그 말일이 이번 정산의 마지막 확정분이 나온 날이라 **기준일 = 전달 말일**이고 실행만 1일 05:00 이다(04:00 은 선행 자리라 같은 시각이면 순서가 안 보장된다). **드러난 것 하나 — `shouldRetry` 가 스킵을 못 봤다.** 카탈로그가 「스킵된 후행은 재시도 창 안에서 다시 시도한다」고 정했는데 코드는 `transient` **실패만** 봤고, 스킵은 실패가 아니라 그 조건에 안 걸렸다. **체인을 세우는 순간 그 규칙이 처음으로 실행됐고 그때 빈 것이 드러났다.** 마지막 회차가 `skipped` 여도 재시도하게 고쳤다. **줄은 주문 항목 건별이고 종류가 여섯이다**(`17` 이 깐 모양) — 판매·수수료·배송비·회수·수수료환급·이월. 대상 조건이 전부 **「아직 어느 정산서에도 안 실린 것」**이고 그것을 `V52` 의 전역 부분 유니크가 한 층 아래에서 지킨다. **지연배상금을 셀러 몫에서 안 뺐다**(`12a-4`) — 읽는 것이 `refund_item` 뿐이라 `refund.delay_interest` 가 정산에 안 닿는다. **거래가 없고 이월만 있는 셀러도 정산서를 만든다** — 빼면 그 채권이 다음 달에도 같은 이유로 빠져서 영영 회수가 안 된다. **줄이 하나도 안 붙은 정산서는 지운다** — 그 달 환불이 전부 정산 안 된 건이었던 셀러가 그렇게 되고, 빈 정산서가 서면 「거래가 없었다」와 「마감이 덜 돌았다」가 안 갈린다. **`17-1` 의 남은 등식 둘을 걸었다** — 종류별 합을 **정산과 다른 경로로 다시 더해** 원본과 대조한다. 같은 쿼리를 쓰면 그것이 틀렸을 때 양쪽이 똑같이 틀리고, 지급액 하나만 보면 두 종류가 서로의 오차를 상쇄해도 초록이 된다 | 17, D19, 36 |
| 20 | ~~정산서 조회~~ | **완료.** `V56__settlement_number.sql`·`SettlementQuery`·`SettlementController`·`SettlementQueryTest`(넷 다 신설), `SettlementService`·`SettlementSchemaTest`·`ErrorCode`·`identifier-rules.md` 수정. **`D9` 가 이미 `settlement_number` 를 요구하고 있었다** — `17` 이 표를 세우면서 빠뜨렸고 그때는 부르는 곳이 없어서 안 드러났다. **조회 입구를 여는 이 청크가 그것을 걸었다.** 접두어는 `T-` 다 — **`P-` 는 `D9` 가 결제에 예약**해 뒀고 두 글자는 나머지 넷(빈 문자열·`S-`·`R-`·`Q-`)과 형식이 어긋난다. **범위를 판정 엔진에게 물어서 조건으로 옮겼다**(`RefundQuery.visibleFor` 와 같은 모양) — 쿼리에 셀러 번호를 손으로 끼우면 스코프 규칙이 바뀔 때 고칠 곳이 판정 엔진 밖으로 흩어진다. **못 봄이 0건이 아니다** — 정산 건수는 곧 그 셀러가 거래한 달 수고 정산액은 월 거래액이라 빈 목록을 주면 개수로 샌다. 남의 정산서 단건 조회도 **404** 다. **경로를 관객별로 안 갈랐다** — `59` 는 같은 자원인데 **행 자체**가 달라야 했고(비공개 문의) 여기는 **범위만** 달라진다. 갈리지 않는데 입구를 늘리면 목록 규약을 두 벌 지킨다. **줄은 상세에서만 내린다** — 주문 항목 건별이라 목록에 실으면 한 셀러의 한 달치가 수백 줄이고 긁으면 거래 내역 전체가 된다. **고객에게 안 열고 감사자에게는 열었다**(돈이 어디로 갔나를 못 보면 감사가 성립을 안 한다). 마이그레이션에 **고객에게 안 열렸나**와 **셀러가 `seller` 범위로만 열렸나** 검증을 `do` 블록으로 넣었다. **`17` 의 스키마 테스트 스물이 그 자리에서 물렸다** — `settlement_number` 가 `not null` 이 되면서. **화면은 안 했다** — `59` 와 같이 API 까지다. **드러난 것 하나**: `payment_number` 도 `D9` 표에만 있고 실물이 없다. 다만 **결제를 URL 로 가리키는 입구가 하나도 없어서 순번이 샐 자리가 없다** — 청크를 세우지 않고 그 표 행에 사실을 적었다(조회 입구가 서는 청크가 같이 만든다) | 18, 4 |
| 21 | ~~지급 처리~~ | **완료.** `V57__settlement_payout.sql`·`SettlementPayoutService`·`SettlementPayoutTest`(셋 다 신설), `SettlementController`·`ErrorCode`·`state-machines.md` 수정. **`19` 뒤가 비어 있었다** — 마감된 정산서와 실제로 돈이 나간 정산서가 **데이터로 안 갈렸고**, 두 번 지급해도 표가 아무 말을 안 했다. 요청·승인을 **다른 권한**으로 갈랐다(`request_payout`·`payout`) — 하나로 두면 올리는 순간 승인이 같이 열리고 그때 남는 것은 자기승인 제약뿐인데 **그것은 자기 것만 막는다**(`V24` 가 환불에서 적은 문장 그대로). **둘 다 관리자다** — 셀러가 올리게 하면 **셀러가 안 눌러서 지급이 밀리는 구조**가 되는데 그건 `12a-5` 가 오늘 환불에서 고친 함정과 같은 모양이다. **강제 지점 셋**: `settlement_payout_self_approval_check`(psql 로 넣어도 걸린다), `settlement_payout_amount_check`(지급액 0 이하면 못 올린다 — 이월로 넘어가지 지급 대상이 아니다), 그리고 **상태·시각·행위자를 사슬로** 묶은 것. **오늘 밟은 구멍을 안 다시 밟았다** — 「상태 = (사람과 시각이 둘 다 있나)」로 쓰면 한쪽만 채운 행이 통과한다(`V53` 의 `inquiry_block_check` 가 그 자리였다). **반려가 종점이 아니다** — 정산서는 `(셀러, 주기)` 당 하나라 환불처럼 새 행을 못 만든다. 다시 올리면 같은 행이 `requested` 로 돌아가고 처리 칸이 비워지며, 반려 사실은 감사 로그가 든다. **`D7` 에 정산 지급과 문의 상태머신을 적었다** — 「정산은 청크 17, 문의는 `58` 에서 그때 정한다」로 남아 있던 자리다. **실제 이체 연동은 안 한다** — 셀러 계좌 송금은 은행 이체라 모의 결제의 일이 아니고, 여기서 하는 것은 「보냈다」를 기록하는 데까지다 | 19, 12a-2 |
| 20-1 | ~~정산 화면~~ | **완료.** `lib/permissions.ts`·`lib/settlement-text.ts`·`components/payout-actions.tsx`·`components/payout-actions.test.tsx`·`app/seller/settlements/page.tsx`·`app/seller/settlements/[settlementNumber]/page.tsx`(여섯 다 신설), `site-header.tsx`·`site-header.test.tsx`·`test/screen-text.test.ts` 수정. **API 를 안 건드렸다** — 이 행이 그렇게 그어져 있었다. **강제 지점을 권한 목록에 뒀다**: `payoutActionsFor(권한, 상태)` 하나가 버튼을 고르고, 고른 것이 없으면 컴포넌트가 **빈 DOM 을 낸다** — 그려 놓고 감추면 그 자리가 DOM 에 남아서 화면낭독기가 읽고 개발자 도구로 되살리면 눌러진다(`59` 와 같은 판단). **상태와 권한을 둘 다 본다** — 권한만 보면 이미 지급한 정산서에 승인 버튼이 뜨고, 상태만 보면 볼 권한뿐인 셀러에게 지급 버튼이 뜬다(`V57` 이 막으려던 자리가 그것이다). **부쉈다**(`D25`) 둘: 권한 거르개를 걷으니 열 중 **그 셋만** 빨갛고, 머리의 `canReadSettlements` 를 `true` 로 바꾸니 일곱 중 **그 하나만** 빨갰다. **화면이 못 아는 것이 하나 있다** — 자기가 올린 지급은 자기가 승인 못 하는데 누가 올렸는지가 응답에 없다. 버튼은 뜨고 누르면 403 이며 그 답을 `messageFor` 가 말로 바꾼다. **경로를 관객별로 안 갈랐다** — `SettlementController` 와 같은 판단이고, 셀러 틀(밀도 7) 안에 둔 것은 정산서를 처리하는 것이 파는 쪽의 일이라서다. **권한 읽기를 `lib/permissions.ts` 로 모았다** — 머리가 혼자 들던 모양이 두 번째 사용자를 만났다. **드러난 것**: 표 조각(`Th`·`Td`) 사본이 둘이 됐다(처분: 흡수 — 셋째 표 화면이 `43a-5` 에서 난다). **축**: `D20` 화면 규약 + `D24` + WCAG 1.4.1 + `D14`. **강제 지점**: 테스트(4위) — 버튼을 고르는 입력이 서버 판정이라 타입으로는 못 내린다 | 20, 21 |
| 22 | ~~문의 스키마~~ | **`58` 이 대신했다**(2026-08-23 점검 G 가 중복을 찾았다). 이 행이 미착수로 남아 있으면 **다음 사람이 이미 있는 표를 다시 만든다.** `58` 이 `inquiry` 를 세웠고 상태·답변·차단 칸이 다 들어갔다. **하나가 안 옮겨졌다** — 이 행의 「대상 자원(**주문**·상품)」에서 주문 쪽이다. `58` 은 상품과 계정(처리정지·이의제기·분쟁)만 담았고 **「이 주문 언제 와요?」를 담을 값이 없다.** `58-2` 로 세웠다(사용자 선택) | 완료 |
| 23 | ~~문의 API~~ | **`59` 가 대신했다**(점검 G). 관객별 입구 셋과 답변까지 섰다. **근거 조문이 어긋난 채로 남아 있었다** — 이 행은 전자상거래법 **제20조의3**(중개자의 불만·분쟁 처리)을 적었는데 `58` 이 세운 `R25` 는 **제20조제3항**이다. 둘 중 하나가 틀렸거나 다른 조문이고 **원문을 안 봤다** — `58-1` 이 그 확인을 같이 한다 | 완료 |
| 24 | ~~문의 화면~~ | **`59-1` 이 대신한다**(점검 G). 아직 미착수지만 **행을 둘로 두면 다음 사람이 어느 쪽을 잡을지 고르는 자리가 생긴다** — `11-5` 가 `35e` 를 흡수할 때와 같은 판단이다. 이 행이 적어 둔 것(역할별로 갈리는 목록과 상세, `D20`·`D24`, `npm run lint` 의 접근성 규칙)은 `59-1` 행이 들고 있다 | 완료 |
| 25 | ~~문의 개인정보 가림~~ | **완료.** `V62__inquiry_field_groups.sql`·`InquiryFields`(둘 다 신설), `InquiryQuery`·`InquiryVisibilityTest` 수정. **`59` 가 축에 `D16` 을 걸고도 본문이 누구에게 나가는지를 안 봤다**(점검 G). `inquiry.question` 은 자유 텍스트 2000자고 사람이 직접 쓴다 — **글 안에 연락처가 섞여 들어온다**(`5i-2` 가 사유 글을 5년 표에서 뗀 이유, `V24` 가 환불 요청 사유를 셀러에게 안 준 이유와 같다). **감사자에게서 닫았다** — 감사는 「누가 언제 무엇을 처리했나」지 고객이 쓴 글이 아니다(개인정보보호법 제3조제1항 최소처리). `V6` 가 감사자에게서 `payment` 를 닫은 것과 같은 논리고, 거기 근거가 「결제 수단까지 볼 이유는 없다」였다. **연결이 없으면 제한이 없다**(`V6`)라 **감사자에게 `basic` 을 연결하는 것 자체가 `body` 를 닫는 행위**다. `do` 블록 둘이 검사한다 — 본문이 열렸나, 그리고 **연결이 실제로 생겼나**(후자가 0이면 「제한 없음」과 구분이 안 된다). **공개 Q&A 는 그대로다** — 공개로 낸 글이라 가릴 것이 없다 **마스킹이 실제로 걸리는 것은 `25-1` 이다** — 감사자만으로 남의 문의를 보는 경로가 없어서, 여기서 고정한 것은 **판정이 그 그룹을 안 열어 준다**는 사실까지다 | 59, 4d |
| 25-1 | ~~관리자·감사자용 문의 목록~~ | **완료** — `InquiryQuery.findAll`(신설) · `InquiryController` 에 `GET /api/inquiries` · `InquiryVisibilityTest`(단언 여섯) · `OpenApiSpecTest`(공유 목록). **마스킹이 응답에서 비는 것을 처음 확인했다** — `V62` 가 「감사자에게 본문이 안 열렸나」를 마이그레이션 안에서 보는데, **훑는 목록이 없어서 응답 쪽은 아무도 못 봤다.** 이제 `findAll` 이 그것을 태운다. **내 기대가 한 번 틀렸다**: 처음에 「관리자도 본문을 못 본다」로 적었더니 **실제 본문이 떠서 빨갰다** — `V62` 는 관리자에게 본문을 연다(법정 요구에 답하는 자리, `R25`·`R28`). 가려지는 것은 **감사자**고, 둘을 같이 재야 「누구에게 가려지나」가 목록에서 갈리는 것이 보인다. **그 빨감이 곧 부순 증거다** — 목록이 진짜 값을 싣고 있다. **마스킹 조건을 새로 안 썼다** — `bodyVisibleTo` 를 그대로 태운다. **대상은 `Target.of(-1L, -1L)`**(`AuditLogQuery` 와 같은 꼴) — 남의 것이 섞여 오는 요청이라 `own` 으로도 `seller` 로도 못 덮는다. **경로는 `/api/inquiries` 다** — 관객을 경로에 안 박는다. **드러난 것을 같이 봤다**: `findForSeller` 의 `sellers.iterator().next()` 가 기대는 전제(어느 셀러를 대든 답이 같다)를 **검사로 내렸다** — 주석에만 있던 근거가 이제 갈리는 날 빨개진다. **축**: `D14` 권한 스코프(`all`) · `D5` 목록 규약. **강제 지점**: 테스트(4위). **닫힘**: `InquiryVisibilityTest` 초록 · `verify.sh --full` 초록 | 25, 59-2 |
| 26 | ~~파일 저장소~~ | MinIO 컨테이너, 업로드 경로 규칙, 용량·형식 제한. **저작권법 제102~103조를 `D2` 에 승격시킨다**(점검 A) — 남의 이미지가 올라올 때 신고·삭제 절차가 있어야 온라인서비스제공자 책임 제한을 받는다. **축**: `D17` 파일 규약, **법**(저작권법 제102~103조 — 신고·삭제 절차가 있어야 책임 제한을 받는다, `D2`). **강제 지점**: 컨테이너와 버킷 정책 자체 — 공개 버킷을 안 만든다. **건드리는 자리**: 신설 MinIO 컨테이너(`docker-compose.yml`)와 저장소 설정. `stack.md` 에 그 선택의 근거. **닫힘**: 컨테이너가 뜨고 **공개 버킷이 0개**인 것을 정책으로 확인하고, 저작권법 신고·삭제 절차가 `D2` 에 요건 번호로 서고 그 행의 강제 지점 칸이 빈칸이 아니다 **묶음 검토(2026-08-23)**: `27`·`28` 과 **안 묶는다.** 선행 `D17`(파일·미디어 규약)이 미착수인데 그 문서의 선행(`D14`)은 차 있어서 **지금 쓸 수 있어 보이지만 아니다** — 「문서가 코드보다 먼저다」의 두 번째 조건인 **「정할 근거가 실제로 생겼다」**가 안 찼다. 파일이 코드에 하나도 없어서 지금 `D17` 을 쓰면 **무엇을 정하는지 모르는 채로 정한다**(`D21` 이 막힌 것과 같은 함정). **닭·달걀이라 순서로 푼다**: `26` 을 최소로 쳐서 저장 자리를 만들고 → `D17` 을 쓰고 → `27`·`28` 을 친다. 셋을 묶으면 그 중간에 문서를 낄 자리가 없어진다. **완료**(2026-09-18) — 컨테이너가 `quay.io/minio/minio` 로 서고(Docker Hub 의 `minio/minio` 는 없어졌다) 버킷 둘(`shop-public`·`shop-private`)을 **앱이 기동 뒤에 만든다**. 컴포즈에 `mc` 를 안 둔 이유는 **절차가 두 벌이면 한쪽이 낡아서**다. **기본이 꺼짐이다**(`STORAGE_BOOTSTRAP=false`) — `shop.events.sink` 와 같은 자리다. **배포는 Cloudflare R2**(사용자 결정) 라 S3 클라이언트가 하나다. **`R42` 가 섰다**(저작권 신고·삭제, 미착수 → `Q94`). **부순 증거**: 버킷 만드는 줄을 지우고 돌렸더니 **처음엔 통과했다** — 재사용 컨테이너에 지난 실행의 버킷이 남아서다. 이름에 pid 를 넣고 다시 부수니 둘이 빨개졌다. **게이트 둘이 나를 잡았다**: `ArchitectureTest`(support 허용 목록에 SDK 없음) · `TestConventionTest`(`` 없음) | D17, D2 |
| 27 | ~~상품 이미지 업로드~~ | 셀러가 자기 상품에만 올린다. **축**: `D14` 보안 기준선(업로드 — 크기·MIME·확장자), `D3` 스키마. **강제 지점**: 원본 이름·MIME·바이트 수를 컬럼으로 받고 `check` 로 좁힌다. 저장 경로 이름은 앱이 만든 것만 쓴다. **건드리는 자리**: 신설 업로드 입구와 상품 이미지 표. 수정은 셀러 상품 화면(`13f`)의 등록 폼. **닫힘**: 남의 상품에 올리면 403 이고, 크기·형식·장수를 어긴 요청이 각각 413·415·422 이며, **그 넷이 통합 시험으로 고정된다**. `check` 제약이 도는 스키마에 있다. **완료**(2026-09-18) — `V78__product_image.sql` · `ProductImageService` · `ObjectStorage`(저장소 파사드) · `POST /api/seller/products/{id}/images`. **받은 바이트를 그대로 안 저장한다** — 읽어서 다시 인코딩하고, 그것이 **EXIF 제거와 형식 판별을 한 번에 한다**(지울 항목을 열거 안 해서 새 항목이 생겨도 안 샌다). **게이트 셋이 나를 잡았다**: `ArchitectureTest`(서비스가 `MultipartFile` 을 받으면 계층 위반 — 컨트롤러가 `Incoming` 으로 바꾼다) · `DocumentMapConsistencyTest`(새 표를 `domain-model.md` 가 안 부른다) · 통합 시험(서명 URL 이 `버킷.호스트` 로 나와 `UnknownHostException` — presigner 에 경로 방식을 안 걸었다) | 26, 7 |
| 28 | ~~파일 접근 판정~~ | URL 을 알면 보이는 기본값을 막는다. 서명된 URL 또는 프록시. **축**: `D14`, `D5`. **강제 지점**: URL 을 알면 열리는 경로를 하나도 안 남기는 것이 통과 기준이다. **판정 엔진은 갈래마다 다르게 걸린다**(2026-09-18 정정) — 공개 갈래는 목록·상세가 **판정이 없는 경로**라(청크 `8`) 서명 URL 발급에도 판정이 없고, 막는 것은 **만료 5분**이다. 비공개 갈래가 요청마다 판정을 지나고 그것은 `43a-5` 가 연다. **건드리는 자리**: 신설 파일 접근 판정 경로(서명 URL 또는 프록시). `26` 의 저장소 설정과 `27` 의 이미지 표를 읽는다. **닫힘**: **URL 을 알면 열리는 경로가 0개**인 것이 통과 기준이다 — 저장소로 바로 가는 주소가 밖에서 안 열린다. **완료**(2026-09-18) — 응답이 **만료 5분 서명 URL** 을 싣는다(목록은 썸네일, 상세는 원본). `ProductImageAccessTest` 가 **서명을 떼면 같은 주소가 401·403** 인 것을 잰다. 화면도 자리표시 대신 진짜를 그리고 **사진이 있을 때만 `alt` 에 상품명이 들어간다**(`D20` — 자리표시는 상품과 무관해서 읽어 주면 없는 정보를 있는 것처럼 말한다). **비공개 프록시는 여기서 안 만들었다** — 태울 자원이 아직 없어서(반품 검수 사진은 `43a-5`) 지금 만들면 죽은 코드다. `ObjectStorage.open` 이 비공개 전용으로 이미 서 있고 `43a-5` 가 그 위에 입구를 올린다 | 27, 4 |
| 29 | 웹훅 등록 | 셀러가 엔드포인트와 시크릿을 등록한다. **축**: `D12` 이벤트 이름, `D14`(시크릿 저장). **강제 지점**: 시크릿을 평문으로 못 넣게 — 저장 전 해시·암호화 여부를 스키마가 강제한다. **건드리는 자리**: 신설 웹훅 등록 표와 셀러 입구. 수정은 `permission_rule` 시드 | D12, D14, 5a |
| 30 | 웹훅 발송 | 서명 헤더, 타임아웃, 실패 기록. **축**: 표준 — HMAC 서명 헤더와 타임스탬프(재전송 차단), `D16`. **강제 지점**: 발송 기록 표 + 타임아웃 값을 한 곳에서만 정한다. **건드리는 자리**: 신설 발송기와 발송 기록 표. `32` 의 아웃박스를 읽는다 **묶음 검토(2026-08-23)**: 아웃박스와 **묶을 필요가 없다** — 이 행의 선행에 이미 `32` 가 있어서 **순서가 분할표에 박혀 있다.** 묶으면 파일이 여섯을 넘고, 아웃박스가 미완인 채로 웹훅을 얹으면 나중에 발송 경로를 갈아엎는다. **먼저 칠 것은 `32`·`33`** 이고 둘 다 `D12`(이벤트 카탈로그)를 기다린다 — `D12` 의 선행 `D5` 는 차 있다 | 29, 32 |
| 31 | 웹훅 재시도 | 지수 백오프, 최대 횟수, 수동 재발송. **축**: 표준 — 지수 백오프, `D7`. **강제 지점**: 시도 횟수와 다음 시각을 **DB 컬럼**으로 둔다. 앱 메모리에 두면 재기동에 사라진다. **건드리는 자리**: `30` 의 발송 기록 표에 시도 횟수·다음 시각 칸과 재발송 입구. `36a` 의 재시도 분류를 그대로 쓴다 | 30 |
| 32 | ~~아웃박스 테이블~~ | **`Q57` 이 대신한다**(2026-09-14 설계 세션). 모양이 바뀌었다 — 앱이 넣는 것이 아니라 원천 표의 트리거가 채운다(`D12`). 원안: 도메인 변경과 이벤트 기록을 한 트랜잭션에 넣는다. **축**: `D12`, `D11`(한 트랜잭션). **강제 지점**: 도메인 변경과 이벤트 기록을 **같은 트랜잭션**에 넣는 것을 테스트로 고정 — 갈라지면 「일어났는데 안 알린」 사건이 남는다. **건드리는 자리**: 신설 아웃박스 표. 수정은 발행 시점을 부르는 서비스들(주문·결제·환불) | D12, 10-2 |
| 33 | ~~Kafka 컨테이너 — 브로커가 뜬다~~ | **결정(사용자, 2026-09-14)**: 전송 수단은 Kafka, 첫 소비자는 거래 통지(`33a`), **배포엔 안 올린다**(`shop.events.sink=none`). 모양은 `event-catalog.md` 「전송 — Kafka」가 다 정했다 — 토픽 `shop.events` 하나 · 파티션 키 `subject` · 파티션 3 · 봉투 JSON 그대로 · 최소 한 번. 여기서 다시 정하지 않는다. **절차**: (1) `docker-compose.yml` 에 `kafka` 서비스 — `apache/kafka:<그날 최신 안정판>` **KRaft 단일 노드**(ZooKeeper 없음), 9092, healthcheck, 볼륨. 태그를 `stack.md` 버전 표에 행으로 — `StackVersionConsistencyTest` 가 컴포즈 태그를 읽는다. 「아직 안 정한 것」의 Kafka 행을 지운다 (2) `build.gradle.kts` — `org.springframework.kafka:spring-kafka`(Boot 4 BOM 이 관리하나 확인) + `testImplementation("org.testcontainers:testcontainers-kafka")`(2.x 좌표 규칙 — `stack.md` 「Testcontainers 2.x 는 좌표와 클래스가 같이 움직였다」) (3) `application.yml` — `spring.kafka.bootstrap-servers: ${KAFKA_BOOTSTRAP:localhost:9092}` · `shop.events.sink: ${EVENTS_SINK:none}` · `shop.events.topic: shop.events`. **`none` 이면 Kafka 빈을 아예 안 만든다**(`@ConditionalOnProperty(name="shop.events.sink", havingValue="kafka")`) — 배포와 빠른 레인이 브로커를 안 찾게. `README.md` env 표에 둘을 더한다 (4) `support/OutboxPublisher` — `@Scheduled(fixedDelayString="PT2S")`, `batch-catalog.md` 에 행. `published_at is null` 을 `outbox_event_id` 순으로 200개씩 읽어 `KafkaTemplate.send(topic, subject, 봉투JSON)` + 헤더 `type`·`id`. **`send().get()` 으로 ack 를 기다린 뒤에만 `published_at` 을 채운다** — 비동기로 두면 「보냈다」가 거짓일 수 있다. 읽기와 표시 갱신은 각각 짧은 트랜잭션, **Kafka 호출은 트랜잭션 밖**(`D11`). `ArchitectureTest` 의 `바깥` 목록에 `KafkaTemplate` 을 더한다 — 그래야 규칙이 이 자리를 본다 (5) `BatchRuns` 는 안 탄다 — 2초 주기라 회차 개념이 없다(`D19` 「5분마다 도는 배치는 다음 회차가 곧 재시도」). 실패한 편지는 `published_at` 이 비어 다음 회차가 다시 집는다 — **여기서 중복 발행이 생기고 그것이 「최소 한 번」의 근원**이다. 주석에 적는다 (6) 토픽 생성 — `KafkaAdmin` 에 `NewTopic("shop.events", 3, (short) 1)`. 파티션 3 인 이유는 키별 순서가 실제로 갈리는 것을 볼 수 있게 (7) 발행된 행 파기 — `TransactionPurgeService` 에 `published_at < 7일 전` 삭제(`D13` 행은 `Q57` 이 이미 세웠다) (8) `PostgresTestBase.Containers` 에 셋째 빈 `KafkaContainer`(이미지 컴포즈와 같게, `withReuse(true)`). `sink=kafka` 는 그 테스트 클래스에서만 `@TestPropertySource` 로 켠다 — 나머지 느린 레인은 브로커를 안 찾는다 (9) 테스트 `OutboxPublisherTest`(`db`) 셋 — `주문_전이_하나가_토픽에_한_건으로_간다`(전이 → 발행기 한 번 → `KafkaConsumer` 로 직접 읽어 1건 + `published_at` 참) · `같은_subject_는_같은_파티션에_간다`(한 묶음 전이 셋 → 파티션 번호 같음) · `브로커가_ack_를_안_주면_published_at_이_안_찬다`(존재하지 않는 bootstrap 으로 발행기 한 번 → 행 그대로) (10) `README.md` 「요구 사항」에 「Kafka 는 로컬 전용 — `EVENTS_SINK=kafka` 일 때만」 한 줄. **축**: 규약(`D12` · `D11` 트랜잭션 밖 · `D19`) + 관례(Kafka 파티션 키 · KRaft). **강제 지점**: 테스트(4위). **건드리는 자리**: 컴포즈 · `build.gradle.kts` · `application.yml` · `OutboxPublisher`(신설) · `PostgresTestBase` · `ArchitectureTest` · `TransactionPurgeService` · 문서 넷(`stack`·`batch-catalog`·`README` 둘). **닫힘**: `OutboxPublisherTest` 셋 · `verify.sh --full` — **느린 레인이 몇 초 늘었는지 이력에 적는다.** **부순 증거**: `send().get()` 을 빼면 셋째가 빨갛다. **안 되면**: 재사용 컨테이너에서 Kafka 포트가 충돌하면 그 클래스만 `withReuse(false)` 로 두고 시간을 적는다 **완료**(2026-09-16). **앞 절반만 쳤다** — 사용자 선택으로 청크를 둘로 갈랐다(설명 모드 선택지 (나)). 여기는 **브로커가 뜨는 데까지**고 편지를 보내는 것은 `33b` 다. 가른 이유는 앞 절반이 **동작을 하나도 안 바꾸는 순수 추가**라 따로 닫을 수 있어서다 — 브로커가 안 뜨는 것과 발행이 틀린 것은 원인이 겹치지 않는다. `docker-compose.yml`(`kafka` 서비스·`kafka-data` 볼륨) · `build.gradle.kts`(스타터·테스트 좌표) · `application.yml`(`spring.kafka` · `shop:` 절 신설) · `stack.md`(버전 행 둘, 「아직 안 정한 것」의 Kafka 행 삭제) · `event-catalog.md` · `README.md` · `backend/README.md` · `.env.example`. **실측 셋**: ① **`spring-kafka` 만 넣으면 자동 설정이 안 따라온다** — Boot 4 는 자동 설정이 모듈별로 쪼개져서 `spring-boot-kafka` 가 따로 있다. `spring-boot-starter-kafka` 로 들였다 ② **분할표가 적은 「`none` 이면 Kafka 빈을 아예 안 만든다」는 그대로 못 한다** — 자동 설정을 끄려면 `spring.autoconfigure.exclude` 에 적어야 하는데 그 값이 `EVENTS_SINK` 하나로 같이 안 움직인다(켜는 사람이 제외 목록도 비워야 한다). **막으려던 것은 브로커로 나가는 연결**이고 그것은 우리 빈(`33b` 의 발행기·토픽)에서 막힌다 — 프로듀서는 첫 발송 때 붙는다. `event-catalog.md` 「전송」의 그 칸을 고치고 근거를 그 아래 적었다(코드보다 문서를 먼저 고쳤다) ③ 이미지는 **`apache/kafka:4.3.1`** — Docker Hub 태그 목록에서 rc 가 아닌 최신을 골랐다. `PostgresTestBase` 의 Kafka 컨테이너는 **여기서 안 세웠다** — 빈으로 세우면 그것을 안 쓰는 느린 레인 전부가 브로커를 같이 띄운다. 쓰는 테스트가 생기는 `33b` 가 세운다. **검증**: `verify.sh --full` 초록(느린 레인이 `spring-kafka` 를 얹고도 그대로 뜬다) · `docker compose up -d kafka` 가 24초에 healthy(브로커 4.3.1 `STARTED`) · 호스트 9092 열림 · `doc-lint.sh` · `StackVersionConsistencyTest` 가 새 행 둘을 대조 | Q57 · D12 |
| 33b | ~~아웃박스 발행기 — 표의 사건이 브로커로 나간다~~ | **`33` 의 뒷 절반이다**(2026-09-16 에 갈랐다). 브로커·의존성·설정 스위치는 `33` 이 이미 세웠다 — 여기서 다시 안 정한다. 모양은 `event-catalog.md` 「전송 — Kafka」다. **절차**: (1) `support/OutboxPublisher` 신설 — `@Scheduled(fixedDelayString="PT2S")`, `@ConditionalOnProperty(name="shop.events.sink", havingValue="kafka")`. `published_at is null` 을 `outbox_event_id` 순으로 200개씩 읽어 `KafkaTemplate.send(topic, subject, 봉투JSON)` + 헤더 `type`·`id`. **`send().get()` 으로 ack 를 기다린 뒤에만 `published_at` 을 채운다** — 비동기로 두면 「보냈다」가 거짓일 수 있다. 읽기와 표시 갱신은 각각 짧은 트랜잭션, **Kafka 호출은 트랜잭션 밖**(`D11`) (2) `ArchitectureTest` 의 `바깥` 목록에 `KafkaTemplate` 을 더한다 — 그래야 규칙이 이 자리를 본다 (3) `BatchRuns` 는 안 탄다 — 2초 주기라 회차 개념이 없다(`D19`). 실패한 편지는 `published_at` 이 비어 다음 회차가 다시 집는다 — **여기서 중복 발행이 생기고 그것이 「최소 한 번」의 근원**이다. 주석에 적는다 (4) 토픽 생성 — `KafkaAdmin` 에 `NewTopic("shop.events", 3, (short) 1)`, 같은 조건부. 파티션 3 인 이유는 키별 순서가 실제로 갈리는 것을 볼 수 있게 (5) 발행된 행 파기 — `TransactionPurgeService` 에 `published_at < 7일 전` 삭제(`D13` 행은 `Q57` 이 세웠다). `TransactionPurgeBatch` 의 합계·로그에 그 칸을 더한다 — **마무리 19차가 같은 자리를 빠뜨렸다** (6) `PostgresTestBase.Containers` 에 셋째 빈 `KafkaContainer`(이미지는 컴포즈와 같게, `withReuse(true)`). **`sink=kafka` 는 그 테스트 클래스에서만** `@TestPropertySource` 로 켠다 (7) 테스트 `OutboxPublisherTest`(`db`) 셋 — `주문_전이_하나가_토픽에_한_건으로_간다` · `같은_subject_는_같은_파티션에_간다` · `브로커가_ack_를_안_주면_published_at_이_안_찬다` (8) `batch-catalog.md` 에 발행기 행. **축**: 규약(`D12` · `D11` 트랜잭션 밖 · `D19`) + 관례(파티션 키). **강제 지점**: 테스트(4위) — ack 순서는 타입으로도 제약으로도 못 막는다. **건드리는 자리**: `OutboxPublisher`(신설) · `PostgresTestBase` · `ArchitectureTest` · `TransactionPurgeService`·`TransactionPurgeBatch` · `batch-catalog.md` · 테스트 신설. **닫힘**: `OutboxPublisherTest` 셋 · `verify.sh --full` — **느린 레인이 몇 초 늘었는지 이력에 적는다**(Kafka 컨테이너가 이때 처음 뜬다). **부순 증거**: `send().get()` 을 빼면 셋째가 빨갛다. **안 되면**: 재사용 컨테이너에서 포트가 충돌하면 그 클래스만 `withReuse(false)` 로 두고 시간을 적는다 **완료**(2026-09-16). `V73__outbox_claim.sql` 신설 · `support/OutboxPublisher`·`OutboxClaims`·`EventTopicConfig` 신설 · `TransactionPurgeService`·`TransactionPurgeBatch` · `ArchitectureTest` · `OutboxPublisherTest` 신설 · `batch-catalog.md`. **행이 없던 마이그레이션이 하나 늘었다**(`V73`, `V72` 는 `Q59` 예약 — **그 예약은 `점검 O` 가 `V74` 로 옮겼다.** 빈 번호를 뒤에 채우면 `V73` 을 받은 DB 가 죽는다). 사용자가 「두 대가 떠도 같은 편지를 안 보낸다」(선택지 (나), `skip locked`)를 골랐는데 **그대로 쓰면 `D11` 과 부딪친다** — `for update` 잠금은 커밋까지 살아서 브로커 왕복이 트랜잭션 안에 들어간다. **잠금을 `claimed_at` 컬럼으로 내렸다**: 집는 트랜잭션이 `skip locked` 로 짧게 끝내고 칸만 찍고, 보내는 것은 그 밖, 도장은 그다음 짧은 트랜잭션이다. 집고 죽은 편지는 1분 뒤 다시 집힌다. **실측 넷**: ① **자기 호출이면 `@Transactional` 이 안 먹는다** — 프록시를 안 지난다. 여기서는 조용히 안 끝나고 **트랜잭션이 없으면 `for update` 잠금이 그 select 문에서 풀려 `skip locked` 가 아무것도 안 막는다.** DB 쪽을 `OutboxClaims` 빈으로 갈랐다 ② **`support` 허용 목록에 `org.apache.kafka` 를 더했다** — `Q53` 이 `io.micrometer` 로 밟은 자리와 같다(자원이 아니라 라이브러리) ③ **첫 셋째 테스트가 ack 를 안 재고 있었다.** 없는 주소를 물리면 `send()` 가 **동기로** 던져서 `get()` 을 통째로 지워도 초록이었다 — **부순 증거를 해 보고서야 드러났다.** 브로커가 **크기로 거절하는** 토픽(`max.message.bytes=1`)으로 바꿔서 실패가 future 로만 오게 했다. `min.insync.replicas=2` 도 해 봤는데 **Kafka 4.3 은 그 값을 복제본 수에 묶어서** 단일 노드에서 안 걸린다(CLI 로 확인) ④ 주문 층 이력은 결제 상태만 받는다(`order_status_history_status_check`) — 분할표 예시의 `paid→preparing` 은 셀러 주문 층이다. **남긴 강제 지점: 테스트(4위) 둘** — `OutboxPublisherTest` 셋과 `ArchitectureTest` 의 「바깥」 목록에 들어간 `KafkaTemplate`(사슬을 걷는 정적 규칙이라 새 입구도 잡는다). **부순 증거**: `get()` 을 빼니 셋째가 빨강, 되돌리니 초록. **검증**: `verify.sh --full` 초록 · 빈 DB 기동 `applied_migrations` 72(파일 69 + 시드 3) · `OutboxPublisherTest` 20.1초(브로커 컨테이너 기동 포함 — 느린 레인이 그만큼 늘었다) · `doc-lint.sh`. **8080 을 남의 프로세스가 또 쥐고 있어서 기동 확인은 8081 로 했다**(`Q57` 과 같은 자리) | 33 |
| 33a | ~~거래 통지 소비자 — 5분 스위퍼를 즉시로~~ | **결정(설계 세션)**: `NotificationSweeper` 를 없애지 않는다. 소비자가 즉시 처리하고 스위퍼는 **안전망**(5분)으로 남는다 — 소비자가 죽어 있는 동안의 사건을 집는다. 둘이 같은 사건을 잡아도 `notification` 부분 유니크가 하나만 남긴다(`54a`). 데드레터 큐를 안 두는 이유가 이것이다(`event-catalog.md` 「전송」). **먼저 확인할 것**: 주문 **생성**이 `order_status_history` 에 `from_status null → payment_pending` 행을 남기나. 남기면 그것이 `shop.order.status_changed` 의 생성 사건이고, 안 남기면 `order_placed` 통지는 스위퍼 몫으로 남고 그 사실을 `D12` 「안 만드는 것」에 적는다. **절차**: (1) `notification/NotificationConsumer` — `@KafkaListener(topics = "${shop.events.topic}", groupId = "shop-notification")`, `sink=kafka` 일 때만 빈이 된다. 헤더 `type` 으로 가른다: `shop.order.status_changed` 의 `to_status = paid` → `PAYMENT_COMPLETED` · `shop.refund.status_changed` 의 완료 → `REFUND_COMPLETED` · 공급 지연은 시각 조건이라 사건이 아니다 — 스위퍼 몫으로 남긴다. 스위퍼 넷 중 무엇이 사건으로 오고 무엇이 안 오는지 **표로 javadoc 에** (2) 처리는 `NotificationService.send(...)` 그대로 — 중복이면 「할 일 없음」. `AckMode.RECORD` — 오프셋은 처리 뒤 커밋 (3) 실패 — 예외를 그대로 올리면 무한 재시도다. `DefaultErrorHandler` 로 3회 뒤 `ERROR` 로그 + 건너뜀. 건너뛴 사건은 스위퍼가 5분 뒤 집는다. 근거를 `notification-rules.md` 「실패와 재시도」에 (4) 테스트 `NotificationConsumerTest`(`db`, `sink=kafka`) 둘 — `발행기가_보낸_결제완료를_소비자가_통지로_남긴다`(전이 → 발행기 → 소비자 → `notification` 1행. 그 뒤 스위퍼를 돌려도 여전히 1행) · `소비자가_없어도_스위퍼가_같은_결과를_낸다`(`sink=none` 컨텍스트 — 스위퍼만으로 같은 1행. **두 경로가 같은 결과**라는 것이 이 테스트의 값이다) (5) `batch-catalog.md` 거래 통지 스위퍼 행에 「안전망 — 소비자(`33a`)가 즉시 처리하고 이것은 뒤를 집는다」. **축**: 법(전자상거래법 제14조① 「신속하게」 — 5분에서 즉시로) + 규약(`D18` · `D11`). **강제 지점**: DB 제약(2위 — 부분 유니크, 이미 있다) + 테스트(4위). **건드리는 자리**: `NotificationConsumer`(신설) · `NotificationConsumerTest`(신설) · `batch-catalog.md` · `notification-rules.md` · `event-catalog.md`. **닫힘**: 둘 초록 · `verify.sh --full`. **안 되면**: `@KafkaListener` 가 컨텍스트 캐시 가짓수를 늘린다(`D15`) — `sink` 가 다른 컨텍스트가 하나 늘어나는 것은 감수하고 수를 적는다 **완료**(2026-09-16). `NotificationConsumer` 신설 · `NotificationConsumerTest` 신설 · `KafkaTestBase` 신설(테스트) · `OrderService` · `NotificationSweeper` · `EventTopicConfig` · `application.yml` · `OutboxPublisher`(주기를 설정으로) · `OrderConcurrencyTest`·`OrderStatusServiceTest` · 문서 셋(`event-catalog`·`batch-catalog`·`notification-rules`). **「먼저 확인할 것」의 답: 주문 생성은 이력 행을 안 남기고 있었다.** 그래서 사용자 선택으로 **남기게 했다**(선택지 (나)) — 표는 처음부터 이 행을 예상하고 있었다(`from_status` 가 비어도 되고 전이 검사도 「비어 있음 → 결제대기」를 받는다). 넣는 코드만 없었다. 그 덕에 **법이 「신속하게」를 요구하는 청약 접수 확인이 즉시 경로를 탄다** — 안 했으면 넷 중 빨라지는 것이 지급·환급 둘뿐이고 정작 제14조제1항은 5분에 남는다. **공급 지연은 그대로 스위퍼 몫이다**(시각 조건이라 전이가 아니다). **소비자가 스위퍼의 쿼리를 그대로 쓴다** — 대상 조건과 문안 값을 소비자가 따로 들면 같은 규칙이 두 벌이 되고 한쪽만 고치는 날 두 경로가 다른 통지를 보낸다. 번호 하나로 좁히는 입구 셋(`sendOrderPlaced`·`sendPaymentCompleted`·`sendRefundCompleted`)을 스위퍼에 냈다. **실측 넷**: ① **스케줄러가 테스트 컨텍스트에서도 돈다** — 2초 회차가 테스트가 부르는 회차와 같은 행을 두고 경쟁한다. 주기를 설정값으로 빼고 `KafkaTestBase` 가 한 시간으로 민다 ② **롤백을 끄면 지연 트리거가 처음으로 돈다** — 「셀러 주문이 없는 주문」과 금액 등식이 커밋 때 보는 것이라, 롤백하는 시험에서는 한 번도 안 도는 자리다. 픽스처를 한 트랜잭션으로 묶고 셀러 주문·항목·재고까지 채웠다 ③ **`OrderConcurrencyTest` 의 정리가 조용히 실패하고 있었다** — 생성 이력 행이 `restrict` 로 주문 삭제를 막아서 행이 남고, **그것을 전체를 세는 뒤 시험들이 같이 세서 그쪽이 대신 빨개졌다**(주문 11개, 재고 0). 정리에 이력 행을 더했다 ④ `OrderStatusServiceTest` 의 이력 수 둘이 하나씩 늘었다(3→4, 2→3) — 생성도 이력의 첫 줄이다. **남긴 강제 지점: DB 제약(2위)은 이미 있었고**(`notification` 부분 유니크가 두 경로의 중복을 막는다) 새로 내린 것은 **테스트(4위)** — `NotificationConsumerTest` 둘이 「즉시 경로」와 「안전망만」이 같은 한 행을 낸다는 것을 잰다. **부순 증거**: 소비자의 주문 전이 분기를 `0` 으로 막으니 첫째가 빨강(둘째는 스위퍼 경로라 초록 — 갈라 둔 것이 그대로 보인다). **검증**: `verify.sh --full` 초록 · `doc-lint.sh` | 33b |
| 34 | ~~이벤트 소비 검증 — 같은 편지가 두 번 와도 결과가 같다~~ | **테스트만이다.** 코드는 `33`·`33a` 가 만든 것을 쓴다. `EventRedeliveryTest`(`db`, `sink=kafka`) 셋 — `같은_사건을_두_번_발행해도_통지는_하나다`(`published_at` 을 비워 발행기를 다시 돌린다 → 토픽에 2건 → 소비 뒤 `notification` 1행) · `한_subject_의_전이_셋은_발행_순서대로_소비된다`(파티션 키 보장의 실측) · `다른_subject_는_순서를_약속하지_않는다`(섞여도 통과 — 약속 안 한 것을 테스트가 고정하지 않게 순서를 단언 안 한다). **축**: 규약(`D11` 멱등 · `D12` 「순서는 subject 단위로만」 · `D15`). **강제 지점**: 테스트(4위). **건드리는 자리**: 테스트 하나 신설. **닫힘**: 셋 초록 **완료**(2026-09-16). `EventRedeliveryTest` 신설 · `CommittedOrderFixture` 신설(테스트 픽스처) · `NotificationConsumerTest`(그 픽스처를 쓰게) · `quality-gates.md`(행 셋). **셋째를 안 썼다**(사용자 선택): 「다른 `subject` 는 순서를 약속하지 않는다」는 **약속 안 한 것**이라 섞여도 통과하고, 깨지려야 깨질 수가 없는 줄이 게이트 표에 서면 **막는 것처럼 보이는 빈 줄**이 하나 는다(`D25`). 그 사실은 테스트 javadoc 과 `event-catalog.md` 가 든다. **실측 셋**: ① **중복을 DB 로 못 만든다** — `published_at` 을 비우려 했더니 `V70` 의 불변 트리거가 「발행 표시를 되돌리면 같은 사건이 다시 나간다」로 막았다. **중복은 원래 전송 계층에서 나는 것**이라(브로커는 받았는데 ack 가 못 돌아온다) 토픽에 같은 봉투를 한 번 더 실어서 만든다 — 분할표가 적은 방법보다 실제에 가깝다 ② **토픽을 앞에서부터 읽으면 안 된다** — 발행된 행이 7일 남고 **주문 번호는 빌드마다 되풀이돼서**, 지난 빌드가 같은 번호로 남긴 사건까지 세어 셋 자리에 일곱이 잡혔다. `TopicTail` 이 **지금 끝**에 붙는다 ③ **첫째는 부술 수가 없다** — 앱 조건(`n.notification_id is null`)을 걷어내도 초록이다. 막는 것이 `notification` 의 부분 유니크(2위)라 부수려면 마이그레이션을 고쳐야 한다. **그래서 이 줄은 제약이 이 경로에서 실제로 걸리는지를 재는 자리**고, 그 사실을 게이트 표에 그대로 적었다. **남긴 강제 지점: 테스트(4위) 하나 + 게이트 표 행 셋** — `33b`·`33a` 몫까지 같이 세웠다(18·19차 리뷰가 두 번 지적한 자리다). **부순 증거**: 발행 순서를 뒤집으니(`order by … desc`) 둘째가 빨강. **검증**: `verify.sh --full` 초록 · `doc-lint.sh` | 33a |
| 35 | ~~Testcontainers 전환~~ | **D15 를 쓰면서 앞당겨 끝냈다.** `PostgresTestBase` 참조 | 완료 |
| 35a | ~~HTTP 계층 테스트~~ | **완료.** `HttpTestBase`·`HttpFlowTest` 참조. `D15` 가 세 층이 됐다 | 완료 |
| 35b | ~~계정 관리 축 HTTP 검증~~ | **완료.** 탈퇴의 두 기기 시나리오가 `HttpFlowTest` 에 들어갔다 | 완료 |
| 35c | ~~HTTP 층 상품 픽스처~~ | **완료.** `HttpTestBase` 에 `givenSellableProduct`·`givenSellerOwner`·`postWithIdempotencyKey` 와 주문 축 정리, `HttpFlowTest` 에 관통 4개. **버그 둘을 잡았다.** (1) **`V17` 지연 트리거가 `NEW` 를 믿어서 `POST /api/orders` 가 실서버에서 언제나 500 이었다** — `NEW` 는 트리거를 걸어 준 문장 시점의 행이라 `insert` 로 걸린 예약분이 커밋 때 `response_body = null` 을 본다. 행을 다시 읽도록 고쳤다(`V16` 의 `assert_order_amounts` 와 같은 모양). 통합 테스트 30개가 전부 롤백해서 그 트리거가 한 번도 안 돌았다 — **이 청크가 첫 커밋을 일으켜 드러났다.** (2) **`ApiExceptionHandler` 가 500 원인을 로그에 안 남겼다** — 주석엔 남긴다고 적혀 있었다. 안 고쳤으면 (1)을 못 찾았다. **정리를 한 트랜잭션으로 묶었다** — 나눠 지우면 `order_item` 만 지운 커밋에서 금액 등식이 깨진다 | 완료 |
| ~~35c 원안~~ | HTTP 층 상품 픽스처 | `HttpTestBase` 에 "팔 수 있는 상품" 을 만드는 헬퍼를 둔다. **지금 HTTP 층은 상품이 필요한 흐름을 못 덮는다** — 셀러 등록·신원 확인·검수까지 세워야 해서 청크 9 가 장바구니 병합을 서비스 층에만 남겼다. 주문(10)·결제(12)가 같은 벽을 만난다. `db/seed` 는 `local` 프로필이라 테스트가 안 태운다 | 9, 10-2 |
| 점검 A | ~~법 축 점검~~ | **완료** — `doc/notes/checkpoint-4-law.md`. 내려간 것 8·부분 5·미착수 4·주석뿐 1. 대장 18행을 채웠고 `D2-2`·`14b`·`13a-1` 로 처분했다. 원래 설명: `D2` 의 R1~R18 을 **요건표에서 코드로 내려가며** 훑고 `D2` 의 「강제 지점」 칸을 채운다. **코드에서 올라가면 안 보인다** — 없는 것이 위반이라서다(`D23`). 대장을 채우는 것이 곧 이 점검이라 둘을 안 가른다. 결과는 `doc/notes/checkpoint-4-law.md` | D2, D23 |
| D2-2 | ~~못 내린 요건에 근거 달기~~ | **완료.** `sku.price` → **`price_incl_vat`**, `order_item.unit_price` → **`unit_price_incl_vat`**(`V13`·`V16` 의 `create` 문 직접 수정, `docker compose down -v`). 제약 이름도 따라갔다. **`order_item` 까지 같이 옮긴 이유**는 그쪽이 더 나빴기 때문이다 — 부가세 언급이 주석에도 없었다. 응답 필드가 `price_incl_vat`·`min_price_incl_vat` 로 바뀌므로 **화면이 쓰기 전인 지금이 최저가다.** `R13` 근거는 `OrderActionService.Action.CANCEL` 에 남겼다 — 취소 주체가 제3자라 `scope=own` 으로 표현이 안 된다는 것까지. `D2` 대장·`D8`·`money-invariants` 갱신. 테스트 516개 통과, 실서버로 관통(옛 이름으로 보내면 400, 새 이름으로 201, DB·응답 모두 새 이름). **밟다 결함 하나가 나왔다 → `8c`.** 원래 설명: 점검 A 가 찾은 둘을 닫는다. **`R12`(부가세 포함가)의 강제 지점이 주석뿐이다** — 값만 봐서는 세금 포함 여부를 알 수 없어 `check` 로 못 막는 것으로 보이는데, **못 내린 것인지 안 내린 것인지가 코드에서 안 갈린다**(`D23` 축 2). **답이 정해졌다 — 이름으로 올린다**(사용자 선택). `sku.price` → `price_incl_vat`. 축 2 의 1순위(타입·스키마 구조)로 올라간다: 공급가액을 넣으려던 사람이 **이름을 보고 멈춘다.** 응답 필드도 같이 바뀌므로 **화면이 아직 안 쓰는 지금이 제일 싸다** — `14` 가 오기 전에 한다. **`R13`(미성년자 취소권)은 흔적이 0건이다** — `D2` 가 「안 다루기로 해도 왜 안 다루는지 적는다」고 요구했는데 없다. **근거를 취소 판정 자리에 남긴다**(사용자 선택, `OrderActionService` 의 취소 경로) — `D2` 에만 적으면 그 코드를 고치는 사람이 문서를 안 열고 지난다. `11b` 가 멀어서 그때까지 비워 두면 안 된다 | 점검 A, D2, D23 |
| 13h | ~~어느 칸인지 모르는 오류의 자리~~ | **완료.** `D20` 에 「모르는 칸을 지목하지 않는다」 절과 오류 자리 표의 새 행, 「셸」에 **`main` 표지는 셸이 한 번만 그린다**를 넣고 화면을 맞췄다. 로그인 폼이 `aria-invalid` 를 안 건다 — 그 속성의 뜻이 「이 칸의 값이 유효하지 않다」인데 **맞은 이메일까지 그렇게 낭독됐다.** `Field` 의 `invalid` 는 남겼고 기본값을 거짓으로 뒀다 — 가입 화면(`13d-1`)처럼 칸마다 검사하는 곳이 쓴다. `layout.tsx` 가 `<main id="main">` 을 그리고 화면 셋(`page`·`login/page`·`ComingSoon`)은 `div` 로 내렸다. **렌더된 마크업으로 확인**: `main` 1개·`id=main` 1개·건너뛰기 링크 1개가 같은 곳을 가리키고 `aria-invalid` 가 전부 거짓. 원래 설명: 점검 D 가 찾았다. 로그인 실패가 **두 입력칸에 모두 `aria-invalid`** 를 건다 — 서버가 어느 칸인지 일부러 안 가르는데(`D14`) 화면낭독기는 **맞은 이메일까지 「잘못된 입력」이라고 읽는다.** `D20` 의 오류 자리 표에 **「어느 칸인지 서버가 안 알려주는 오류」 줄이 없다** — 규칙을 먼저 고치고 화면을 맞춘다(`D23` 「이 문서를 고칠 때」). 같이 **건너뛰기 링크가 `<main>` 이 아니라 감싸는 `div#main` 을 가리키는 것**도 본다. 고치는 방법이 둘이고 `ComingSoon` 이 `main` 을 그리는 구조와 얽혀 있다 | 점검 D, 13-2, D20 |
| 4g | ~~필드 그룹 코드를 타입으로~~ | **완료.** `auth/FieldGroup` 인터페이스와 `order/OrderFields`·`me/UserFields` enum 신설, `Decision.canSee` 가 타입만 받는다, `FieldGroupTest` 3개. **`11a` 의 선례를 그대로 썼다** — 인터페이스만 `auth` 에 두고 목록은 자원 패키지에 둬서 `auth → order` 순환을 안 만든다. **목록이 둘인 이유는 `basic` 이 양쪽에 있는데 뜻이 달라서다** — 하나로 두면 주문 판정에 사용자 그룹을 물어도 컴파일이 통과하고 조용히 거짓이 온다. **바꾸자마자 컴파일러가 테스트 6곳의 생 문자열을 잡았다.** 대조 테스트는 `check` 가 아니라 **표**가 출처인 첫 사례고, 코드에만 있는 값을 넣어 깨지는 것을 확인했다. **`UserFields` 는 관객 패키지에 있다** — `5l` 이 `me` 를 가를 때 같이 옮긴다. 원래 설명: 점검 C 가 찾았다. `decision.canSee("basic")`·`"contact"`·`"shipping"` 다섯 곳이 **생 문자열**이다. 값 목록이 `permission_field_group.code` 에 있고 코드가 분기하므로 `D23` 이 enum 을 요구한다. **실패가 조용하다** — 오타를 내면 `canSee` 가 `false` 를 돌려주고 그 필드가 응답에서 빠지는데, 막히는 쪽이라 **오류도 로그도 안 남고** 화면에서는 「볼 수 없는 필드」와 구분이 안 된다(`D20` 이 갈라 둔 둘이 여기서 뭉개진다). 제약 대조 테스트도 같이 붙인다 | 점검 C, 4d, D23 |
| 5l | ~~`me` 패키지를 자원 단위로~~ | **완료.** `me/` 를 **`account/`**(`app_user` — `AccountService`·`WithdrawalService`·`AccountPurgeService`·`UserFields`·`MeController`)와 **`consent/`**(`ConsentService`·`ConsentItemController`)로 갈랐다. 테스트도 같이 옮겼다. **예외를 안 적고 코드를 옮긴 이유는 관례를 세어 봐서다**(사용자 선택) — Magento·Shopware·Sylius·Medusa·Spree·Saleor 가 전부 모듈은 자원 이름이고 관객은 그 안에서 가른다. **`me` 를 모듈로 판 곳이 하나도 없다**(축 1의 4위라 근거는 아니고 셈이다). **`MeController` 는 안 갈랐다** — 입구는 경로를 따라가고, 가르면 `/api/me` 를 그리는 코드가 두 패키지로 흩어진다. `D23` 「패키지」에 절을 하나 더하고 「지금 어긋난 곳」에서 이 행을 닫았다. 원래 설명: 점검 C 가 찾았다. `me` 는 자원이 아니라 **관객**("나")이라 패키지 경계 규칙(`D23`)과 어긋난다. 실제로 다루는 자원은 `app_user`(`AccountService`·`WithdrawalService`)와 `consent_item`·`user_consent`(`ConsentService`)로 둘이고, **`ConsentItemController` 는 로그인 전에 부르는 공개 조회**라 「나」와 맞지도 않는다. **그 규칙은 `product` 하나를 보고 세운 것**이라(점검 C 관찰) 규칙에 예외를 적는 쪽도 선택지다 — **옮길지 예외로 둘지를 먼저 정한다** | 점검 C, D23 |
| D14-1 | ~~보안 기준을 개정판에 맞춘다~~ | **완료.** `Password.java` 를 **15자**로(`@Size(min = 15)`), `D14` 의 비밀번호 절을 표준 등급(`SHALL`/`SHOULD`)과 준수 여부까지 적는 표로 다시 쓰고, **OWASP 매핑을 2025 판으로 다시 그렸다.** 신규 `A10`(예외 상황 처리 미흡)에 절을 하나 세웠다 — `ApiExceptionHandler`·`Retries`·**Redis 강등**·**캡차 fail-open** 이 그 항목이고 뒤의 둘이 핵심 물음(**무엇이 죽었느냐로 갈린다**)이다. `A03` 공급망 승격은 `2c` 의 요구사항을 늘렸고, `A09` 의 경보는 **사람에게 닿는 경로가 없다**(`54b`). SSRF 는 `A01` 로 흡수 — 없어진 요구가 아니라 자리가 옮았다. **테스트를 경계로 바꿨다**(14자 거부·15자 수락) — 7자로 두면 한 칸 밀려도 안 잡힌다. 되돌려서 잡히는 것을 확인했다. 한글 테스트도 15자를 넘겨서 **문자 집합만 걸리게** 고쳤다. 개발용 계정(`V901`)과 자동 채움도 정책에 맞췄다. 원래 설명: 점검 E 가 찾았다. **근거 둘이 개정됐다.** (1) **NIST SP 800-63B Rev 4** — 비밀번호가 단독 인증수단이면 **최소 15자 `SHALL`**(우리 8자), **블록리스트 대조 `SHALL`**(우리 없음). 나머지는 지킨다(최대 64·인쇄 ASCII+공백·조합 강제 안 함). **15자로 올린다**(사용자 선택) — `SHALL` 이고 출처 2순위라 규약보다 위다. `Password.java` 한 곳만 고치면 되는 것이 규칙을 유일한 출처로 모아 둔 보람이다. **같이 닿는 곳**: `D14` 문안, `V901` 개발용 테스트 계정(`1` 이라 통과 못 한다), 테스트 픽스처. 데모 시드(`demo-password-1234`, 19자)는 그대로 통과한다. **블록리스트는 여기서 안 한다** — `D14-2` 로 뺐다. (2) **OWASP Top 10 2025** — `D14` 가 2021 에 매핑돼 있다. **`A10 Mishandling of Exceptional Conditions` 가 신규**고 `ApiExceptionHandler`·`Retries`·**Redis 와 캡차의 fail-open** 이 정면으로 걸린다. **`A03 Software Supply Chain Failures` 가 3위**라 의존성 검증 절차가 필요하다. **`A09` 가 「Logging and Alerting」**으로 바뀌어 알림이 이름에 들어왔는데 우리는 없다(`D18`). 매핑을 2025 로 다시 그린다 | 점검 E, D14 |
| D13-1 | ~~표와 수명 대조~~ | **완료.** `DataLifecycleCoverageTest`(신설), `data-lifecycle.md`, `AccountPurgeService` 주석. **`information_schema` 의 표 목록과 문서를 이름으로 대조한다** — 정책 표에 「표」 칸을 더하고, 수명을 정할 것이 없는 표는 「수명을 안 정하는 표」 절에 **근거와 함께** 적게 했다. 파서는 칸 순서가 아니라 **머리글이 「표」인 칸**을 찾아서, 칸을 더해도 안 깨진다. **대조는 양방향이다** — 정책 없는 표(수명이 안 정해진 것)와 표 없는 정책 행(지워진 표를 가리키는 것)을 따로 본다. **드러난 것 셋**: ① `sku_stock` 이 빠져 있었다(이 청크가 세워진 이유), ② **`idempotency_key` 는 반대로 갔다** — 코드가 24시간마다 지우고 있는데 문서에 행이 없었다. 수명이 코드에만 있으면 문서를 읽는 사람이 그 표를 무기한으로 본다, ③ `order_contract_document` 도 없었다. **주석 하나가 코드와 어긋났다** — `AccountPurgeService` 가 `acted_ip` 유예를 30일로 적었는데 `Q10` 이 5일로 고칠 때 안 따라왔다. 날수를 지우고 `{@link #GRACE_DAYS}` 를 가리키게 해서 사본을 없앴다. **`—` 칸에는 어느 청크가 표를 세우는지 적게 했다**(알림 셋은 `54a`, 문의는 `58`) — 「나중에」로 끝나면 처분이 아니다. 35표가 양방향으로 맞는 것과, `sku_stock` 을 빼고 없는 이름을 넣어 **둘 다 빨개지는 것**을 확인했다. 원래 설명: **새 표를 만들면서 수명을 안 정해도 아무것도 안 깨진다.** 2026-08-21 에 표를 넷 만들었는데(`batch_run`·`payment_card`·`sku_stock`·`sku_stock_movement`) `data-lifecycle.md` 의 자원별 정책에 셋만 들어갔다 — 빠진 것을 잡은 것은 **사람이 센 것뿐**이다 | 완료 |
| D14-2 | 비밀번호 블록리스트 | **`D14-1` 이 남긴 나머지 `SHALL`** 이다(NIST SP 800-63B Rev 4) — 유출·사전·문맥 단어와 대조하라고 요구하는데 우리는 안 한다. **`D14-1` 에서 뺀 이유는 새 일이라서다** — 목록 출처(HaveIBeenPwned k-익명 API 냐 내려받은 목록이냐), 저장 방식, 검사 시점(가입·변경 둘 다), 그리고 **바깥을 부르면 그 장애가 우리 장애가 되는가**(캡차 fail-open 논거와 같은 물음, `D14`)를 먼저 정해야 한다. **정할 때까지 `SHALL` 하나가 열려 있다는 사실을 `D14` 에 적어 둔다**. **축**: 표준 — NIST SP 800-63B Rev 4 의 남은 `SHALL`(유출·사전·문맥 단어 대조). **강제 지점**: 검사 시점을 가입·변경 **둘 다**에 걸고 그것을 테스트로 — 한쪽만 걸면 바꿔서 우회한다. **건드리는 자리**: 신설 목록 저장소 또는 외부 조회기(`D14-2` 가 고른다). 수정은 `AuthSignupService`·`AccountService` | D14-1, D14 |
| 5m | ~~동의 쓰기를 한 입구로~~ | **완료.** `5l` 이 드러냈다 — 패키지를 가르고 나니 `account/WithdrawalService` 가 `user_consent` 에 직접 `insert` 하는 것이 경계를 넘는 쓰기로 보였다. **전에는 같은 패키지라 안 보였다.** `ConsentService.revokeAll(userId, actorIp)` 로 옮겼다. **위험은 규칙이 두 벌인 것**이었다 — append-only·종속 항목·필수 처리를 `ConsentService` 가 정하는데 탈퇴 경로가 그것을 안 지난다. 지금은 결과가 우연히 같지만 규칙을 고치는 사람이 탈퇴 경로를 못 본다. **`AccountPurgeService` 는 안 옮겼다** — 조건이 전부 `app_user.deleted_at` 이라 옮기면 `consent` 가 `app_user` 를 읽게 돼서 방향만 뒤집힌다. 그 선을 `D23` 「남의 자원 표를 언제 직접 만지나」에 적었다. 방향은 `account → consent` 단방향 그대로다 | 5l, D23 |
| 8c | ~~옵션 없는 상품의 조합이 상세에서 사라진다~~ | **완료.** `findPublicSkus` 를 `left join` 으로 바꾸고 `option_value_ids` 를 빈 배열로 내린다. **`getLong` 이 null 을 0 으로 주는 자리라 `getObject` 로 받아 null 을 거른다** — 안 거르면 옵션 없는 SKU 가 **없는 선택지 0 번을 가리킨다.** 옵션 축이 없는 상품을 만드는 픽스처와 상세 검증을 `ProductQueryTest` 에 뒀다. 원래 설명: `D2-2` 를 실서버로 밟다 나왔다. `ProductQuery.findPublicSkus` 가 `sku_option_value` 를 **inner join** 해서, **옵션이 없는 상품의 SKU 는 행이 하나도 안 나온다** — `skus: []` 로 내려간다. `ProductController` 가 「옵션이 없는 상품은 빈 배열이고 SKU 가 하나다」로 명시한 정상 상품인데 **상세에 살 수 있는 조합이 안 보인다.** 담기·주문은 `sku_id` 로 하므로 **화면만 못 그린다** — `14b` 가 이걸 모르고 만들면 그 상품이 통째로 안 팔린다. `left join` 으로 바꾸고 `option_value_ids` 를 빈 배열로 내린다. **테스트가 전부 초록인 채로 있었다** — `ProductQueryTest` 가 옵션 있는 상품만 본다 | 8b, 14b |
| 점검 D-2 | ~~프론트 재점검 — 실제로 밟는다~~ | **완료 — 화면 열일곱을 진짜 브라우저로 밟았고 지적 0건이다. **행의 「열넷」은 예보였다**(`15-5` 까지 끝나면) — 실제 `page.tsx` 는 **스물**이고, 그중 동적 경로 셋(`[productId]`·`[orderNumber]`·`[sellerOrderNumber]`)은 **실물 id 가 있어야 열려서** 안 밟았다.** `stack.md`·`PROGRESS.md`. **노트를 안 썼다** — 찾은 것이 축 문서로 바로 내려갔다(`F`·`G`·`H`·`K` 와 같은 판단). **잰 것 넷**: 이름 없는 조작 · 초점 역행 · 제목 단계 건너뜀(h1→h3) · 390px 가로 스크롤. **전부 0.** 로그인해서 `/me`·`/cart`·`/orders`·`/checkout`·셀러 화면 넷까지 다 봤다(조작 수가 화면마다 달라 실제로 그 화면을 연 것이 확인된다 — `/me` 27개, `/cart` 12개). **이 점검의 진짜 산출물은 재는 법이다.** **1차가 지적 50건을 냈는데 전부 오탐이었다** — ① `aria-label`·`textContent` 만 읽어 **`<label for>` 로 이어진 이름을 못 봤고**(접근성 트리로 물으니 이름 없는 것 0개), ② **`<nextjs-portal>`**(Next.js 개발 오버레이, shadow root)을 초점 대상으로 세서 「크기 0」·「초점 역행」이 화면마다 떴고, ③ 2차에서는 **로그인이 아예 안 됐다** — 개발 빌드가 `TEST_ACCOUNT` 로 칸을 미리 채우는데(`login-form.tsx`) 그 위에 쳐서 값이 두 벌이 됐다. **셋 다 「화면이 틀린 것」이 아니라 「재는 쪽이 틀린 것」이다.** `/inspection` 의 「코드를 안 읽고 판정하지 않는다」가 브라우저 쪽에서도 걸린다 — **판정을 뒤집기 전에 도구부터 의심한다.** 그 셋을 `stack.md` 에 절로 세웠다. **축**: `D20` 화면 규약 + `D24` + WCAG. **강제 지점**: 없다 — 점검이다 | 완료 |
| D23-1 | ~~열거값을 어디에 두나~~ | **완료**(2026-08-12) — `coding-rules.md` 에 절 신설, `api-guidelines.md` 에 「모르는 열거값은 무시한다」, 청크 `7e` 신설. 코드는 안 건드렸다. **공통코드 테이블을 쓸지 묻는 데서 나왔고**, 국내 SI 관례와 대조해 **도메인 상수는 코드에 두고 운영이 늘리는 것만 표로 뺀다**로 갈랐다. **분할표에 늦게 올렸다**(`13b` 마무리) — 이력에는 있는데 여기 없어서 「끝난 청크」를 세는 표가 하나 모자랐다 | D23 |
| ~~D22-1~~ | 라이선스를 기록한다 | **점검 E 에 합쳤다**(사용자 선택) — 둘 다 `external-references.md` 를 만지므로 링크를 여는 김에 저작자·라이선스를 같이 적는다. 따로 하면 같은 45개 링크를 두 번 연다. 원래 설명: 점검 C 가 찾았다. `D23` 이 「인용 자료의 저작자·라이선스를 `external-references.md` 에 적는다」고 정했는데 **그 파일에 라이선스가 한 줄도 없다.** `D23` 이 직접 든 예(SQL Style Guide = CC BY-SA 4.0)조차 `coding-rules.md` 안에만 있다. 의존성은 훑어서 GPL 계열이 없는 것을 봤지만 **확인한 기록이 없다** — `D23` 이 말한 「판단한 적 없는 것과 판단해서 괜찮은 것은 다르다」가 지금 상태다. 공개로 돌리기 전에 닫아야 하는 자리다 | 점검 C, D23 |
| 11-5 | ~~주문 축 상태 목록에 방벽 걸기~~ | **완료.** `OrderStatusListTest` 신설 — 층마다의 제약 둘, **이력 제약은 `case` 갈래별로 갈라서** 대조한다(사용자 선택). 합집합만 보면 **배송 값이 결제 갈래에 섞인 것을 못 잡는다.** `app_user.status` 는 `auth/UserStatus` enum 으로 올리고 `UserStatusTest` 로 묶었다 — **`account` 가 아니라 `auth` 에 둔 이유는 순환이다**(`account → auth` 가 이미 있어서 반대 방향을 새로 내면 하나 는다. `StatusPolicy`·`FieldGroup` 과 같은 모양). **`seller.status` 는 안 고쳤다** — 자바 비교가 없고 SQL 리터럴뿐이라 `D23` 「SQL 텍스트 안의 리터럴은 안 바꿨다」가 이미 정한 자리다. **`35e` 를 흡수했다**(사용자 선택). **진짜 잡는지 확인했다** — `Payment` 에 값을 하나 더하니 층 제약과 이력 갈래 둘이 같이 깨졌고 원복했다. 원래 설명: 점검 B 가 찾았다. **`shop_order_status_check`·`seller_order_status_check`·`order_status_history_status_check` 셋에 대조 테스트가 없다** — `Payment`·`Shipment` enum 과 두 사본인데 `D23` 이 요구한 방벽이 상품 축(`7e`)에만 있다. **지금은 갈리지 않았다**(4/4·7/7 확인) — 방벽만 없다. **사본이 여섯이다**(enum 2 + 제약 4곳, 이력 제약이 `case` 로 목록을 한 번 더 적는다). 같이 **`app_user.status`·`seller.status` 의 생 문자열 비교를 enum 으로 올린다**(`"active".equals(...)`) — `7e` 가 상품 축에서 치운 것과 같은 모양이고 `D23` 의 「지금 어긋난 곳」이 비었다는 서술도 고친다 | 점검 B, D23 |
| 점검 B | ~~DB 제약 축 점검~~ | **완료** — `doc/notes/checkpoint-5-db.md`. 제약 55/55·트리거 16/16·금액 컬럼 16/16·`updated_at` 9/9, **사라진 제약 0**. `11-5` 로 처분했다. 원래 설명: 스키마에 실제로 걸린 제약과 문서가 말하는 제약을 대조한다. **`create` 문을 직접 고치는 관행 때문에 제약이 조용히 빠질 수 있다**(`D23` 「마이그레이션」) — 빠져도 테스트가 안 잡는다. 점검 A 가 채운 대장이 입력이다 | 점검 A |
| 점검 C | ~~규약·코딩 축 점검~~ | **완료** — `doc/notes/checkpoint-6-rules.md`. 3.9배 커지는 동안 새 위반 넷. `4g`·`5l`·`D22-1` 로 처분하고 `D22` 를 고쳤다. 원래 설명: 코드에서 규칙으로 **올라가며** 훑는다. `checkpoint-3-rules`(08-07, main 2,246줄) 이후 **8,689줄로 3.9배**가 됐고 그 사이 규칙도 늘었다(축 1 플랫폼 사실, 축 2 안 나가게 하는 것, 패키지 경계). 새로 생긴 절부터 본다 | D22, D23 |
| 점검 D | ~~프론트 점검~~ | **완료** — `doc/notes/checkpoint-7-frontend.md`. 접근성 항목 전부 초록이고 **대비비를 직접 계산해 통과**를 확인했다. `13h`·`점검 D-2` 로 처분했다. 원래 설명: `D24` 기준으로 `frontend/src` 751줄을 훑는다. **화면 축이 이제 막 열려서 대상이 짧다** — 어긋난 것을 지금 고치는 값이 제일 싸다 | D24 |
| 점검 E | ~~기준 자료가 아직 유효한가~~ | **완료** — `doc/notes/checkpoint-8-references.md`. **핵심 근거 여섯 중 둘이 개정됐다**(NIST SP 800-63B Rev 4·OWASP Top 10 2025) → `D14-1`. `D22-1` 을 합쳐 라이선스도 기록했다. **45개 중 여섯만 봤고** 나머지는 다음 회차다. 원래 설명: `external-references.md` 의 링크 확인이 **2026-08-05** 다. 표준은 개정된다 — RFC 9110 이 422 의 이름을 바꿔서 청크 `2d` 가 그걸 치웠다. **웹 조회가 필요하다.** 다른 점검과 섞지 않는다 — 섞으면 한 점검이 끝없이 불어난다. **`D22-1`(라이선스 기록)을 여기 합쳤다**(사용자 선택) — 같은 문서를 만지므로 링크를 여는 김에 저작자·라이선스를 같이 적는다 | 점검 C |
| 점검 F | ~~축 여섯 재점검~~ | **완료**(2026-08-20) — 노트 파일을 따로 안 썼다. 찾은 것이 **축 문서에 바로 내려갔다**(`D2` 의 2026-08-20 절, `api-guidelines.md`·`screen-rules.md`·`coding-rules.md`). 축 여섯(법·표준·DB 제약·규약·관례·코딩)으로 **직전 3일치**를 훑어 **서른둘**(`F1`~`F9`·`S1`~`S8`·`B1`~`B5`·`P1`~`P4`·`C1`~`C3`·`K1`~`K3`)을 찾고 자리별 처분 청크 `Q1`~`Q15` 로 잘랐다. **그중 열하나는 그 문서를 읽고도 안 걸린 것**이다(표준 여덟·관례 셋) — 「읽기는 재발을 못 막는다」의 근거가 여기서 나왔다. **둘을 되돌렸다** — `S5` 는 방향이 반대였고 `B4` 는 `V26` 주석을 안 읽고 판정한 것이라 철회했다. 오판 6% 가 이 단위의 비용이다 | 완료 |
| 점검 G | ~~묶을 청크 검토~~ | **완료**(2026-08-23) — 노트 파일을 안 썼다. 찾은 것이 **분할표 행에 바로 내려갔다**(`점검 F` 와 같은 판단). 미착수 예순셋에서 「합쳐서 칠 것」을 골랐더니 **`43`+`44` 하나만** 남았고, 나머지 셋은 **묶는 문제가 아니라 순서·분해 문제**였다 — `60`+`61` 은 사슬 넷이 막고(`42` 를 갈라야 풀린다), 파일 축은 **문서를 낄 자리**가 필요하고, 웹훅+아웃박스는 **선행 칸에 이미 순서가 박혀 있었다.** 덤으로 `43` 의 선행이 **없는 청크(`11`)를 가리키던 것**을 고쳤다 | 완료 |
| 점검 H | ~~세로 축 · 타입 줄~~ | **완료**(2026-08-29) — 노트 파일을 안 썼다. 찾은 것이 분할표 행 둘로 바로 내려가서다(`F`·`G` 와 같은 판단). **훑는 방향을 바꿔야 했다** — 타입 175개(`record` 149·`enum` 26)를 훑으면 「이 타입이 왜 있나」를 묻게 되는데, 알고 싶은 것은 **「타입으로 막을 수 있는데 안 막은 자리」**고 그건 타입 쪽에 흔적이 없다. **DB 가 값 목록을 닫아 둔 자리(`check (X in (...))`, 27개)에서 「Java 가 이 값을 무슨 타입으로 드나」를 물었다.** `CLAUDE.md` 의 세는 단위를 그렇게 고쳤다. **찾은 것 둘**: ① **열거형이 있는 열아홉 중 DB 제약과 대조되는 것이 넷뿐이다**(`app_user_status`·`shop_order_status`·`seller_order_status`·`seller_exempt_reason`). `D23` 이 「제약 정의를 읽어 enum 과 대조하는 테스트를 둔다」고 **규칙으로 적어 뒀는데 적용이 4/19** 고, **이 세션이 세운 열하나가 전부 대조 밖**이다 → `43a-19`. ② **닫힌 목록인데 열거형이 없는 컬럼이 열이다** → `43a-20`. **「걸려 있다」와 「돈다」가 또 갈렸다** — 규칙은 있고 실물은 넷이었다. **`43a-19` 가 이 수를 고쳤다**: 4/19 가 아니라 **7/18** 이다(`ProductStatusTest` 가 셋을 한 파일에서 대조하는데 파일 수로 셌다). 안 걸린 열하나는 맞았다 | 완료 |
| 점검 I | ~~가로 축 · 법 줄~~ | **완료**(2026-08-29) — 노트를 안 썼다(고친 것이 요건표 자체로 내려갔다). **요건표에서 코드로 내려갔다** — 가로는 반대로 못 간다. **셋을 물었다.** ① **요건표가 부르는 이름이 아직 실재하나** — 강제 지점 칸의 식별자 **206개를 뽑아 저장소와 대조했고 전부 실재한다**(`A09` 하나는 OWASP 항목 번호라 오탐). 이 세션이 `Actor.person`·`enumValue`·상수 여럿을 없앴는데 요건표가 부르던 것은 없었다. ② **`점검 A` 뒤에 늘어난 `R34`~`R37` 이 강제 지점을 채웠나** — 넷 다 찼다. `점검 F` 가 지적한 둘(`R34` 는 게시 중단이 `psql` 로만, `R36` 은 약관 문구에만)이 `59-2`·`43`·`43a-2` 로 닫혔다. ③ **요건이 가리키는 청크가 분할표에 있나** — **여덟 행이 없는 청크를 가리켰다.** `11`·`12`·`13a`·`54` 가 나중에 잘게 갈렸는데(`11-2`·`12-1`·`13a-1`·`54a`) **요건표가 옛 이름을 그대로 들고 있었다.** 글 참조라 **지금 고쳤고**, 왜 고쳤는지를 표 머리말에 적었다 — 가리키는 청크가 없으면 「이 요건이 누구 소관인가」에 답이 안 나오고, **「승격을 빠뜨리지 않는 방법은 선행이다」 장치도 청크 이름이 맞아야 돈다**. **「아직 안 적은 법」 열넷은 전부 「근거가 생기는 자리」를 가리킨다** — 처분이 다 돼 있다. **「미착수」 넷**(`R17`·`R27`·`R29`·`R30`)은 기능 자체가 없는 것이라 청크를 기다린다 | 완료 |
| 점검 J | ~~세로 축 · 문서 줄 — 「처분 완료」 블록을 걷는다~~ | **완료** — `doc/reference/` 열 파일에서 **260줄**을 걷고 `doc/process/inspection.md` 를 갱신했다. 노트는 안 썼다(`F`·`G` 와 같은 판단 — 축 문서로 바로 내려갔다). **살린 것이 셋이다.** ① `commerce-compliance.md` 의 「같은 실수가 세 모양으로 나온다」와 「조문을 대조해서 맞았던 것」 — **이력이 아니라 지금 유효한 법 근거**다. 틀린 것만 남기면 다음 사람이 같은 조문을 다시 연다. ② `state-machines.md` 의 「전이 조건의 단일 출처가 이 문서다」. ③ `api-guidelines.md`·`coding-rules.md` 의 오판 둘을 **`inspection.md` 로 옮겼다** — 점검이 낸 것도 틀릴 수 있고(`about:blank` 오판), **주석까지 안 읽고 스키마만 보고 판정한 것**(`B4`)이 그 원인이었다. 둘 다 「점검을 어떻게 하나」라 기준 문서가 아니라 절차 문서 자리다. **나머지는 전부 「그때 무엇을 고쳤다」라 지웠다** — `doc/README.md` 가 「지나간 작업 이력은 문서에 안 쓴다」고 정했고 git 이 든다. **실물 하나를 찾았다**: `coding-rules.md` 의 `B4` 처분이 **이미 닫힌 `14c` 를 가리키고 있었다** — 「가리키는 청크가 있나」 대조에 걸리는 자리다. **약정 유무가 묶음에 안 박제되는 것**이 남아 있어 `14c-1` 을 세웠다(법 축, 강제 지점 2위). `PLAN.md` 의 「남은 것은 셋」 줄도 같이 고쳤다. **재발은 `2c-2` 가 막는다** — 기준 문서 제목의 날짜를 `doc-lint.sh` 가 잡는다. **검증**: `doc-lint.sh` 통과, `./gradlew test` 초록(`StackVersionConsistencyTest` 가 `stack.md` 를 읽는다) | 완료 |
| ~~35e~~ | 제약 대조 헬퍼 합치기 | **`11-5` 에 흡수했다**(사용자 선택) — 그 청크가 세 번째 사본을 만드는 자리라 따로 두면 사본 셋인 구간이 생긴다. `test` 쪽 `support/ConstraintValues` 한 벌로 합쳤고 넓은 쪽 정규식(`[a-z0-9_]+`)을 남겼다. 원래 설명: `check` 정의를 읽어 enum 과 대조하는 헬퍼가 `ProductStatusTest`·`SellerQueryTest` 두 벌이고 **정규식이 다르다** — 앞은 소문자만 훑아서 `under_50_transactions` 같은 값을 놓친다. 공용 테스트 헬퍼로 뺀다. **`D23` 이 「세 번째 사본이 생긴다」고 경계한 것과 같은 모양의 사본**이라 그 절이 자기 도구에 안 걸려 있었다 | 14a |
| 36 | ~~스케줄러 도입~~ | **완료.** `V38__batch_run.sql`·`BatchRuns`·`AccountPurgeBatch`·`TransactionPurgeBatch`·`BatchRunsTest`(신설), `TransactionPurgeService`·`AccountPurgeService`·`batch-catalog.md`·`data-lifecycle.md` 수정. **파기 배치 둘이 운영에서 한 번도 안 돌고 있었다** — 로직은 `5i`·`10a` 에 다 서 있는데 부르는 곳이 테스트뿐이라 개인정보 파기가 실행 자체가 없었고, **아무 테스트도 안 깨져서** 두 청크를 그대로 지났다. **배치 클래스를 서비스와 갈랐다** — 파기 서비스가 `@Transactional` 이라 거기에 `@Scheduled` 를 붙이면 회차 기록이 같은 트랜잭션이 되고 **실패 행이 롤백돼서 안 남는다**(카탈로그의 클래스 칸을 그래서 고쳤다). **강제 지점 둘**: `(batch_name, baseline_date)` 부분 유니크가 성공 회차를 하나로 묶고(정산 마감이 두 번 지급하는 것을 스키마가 거부한다), **`@Scheduled` 가 붙어 있는지를 반사로 보는 테스트**가 「로직만 있고 안 도는」 상태의 재발을 막는다. `batch_run` 보존 1년은 거래기록 파기에 얹었다. **2층 회차 재시도와 자동 구매확정 이력은 `36a` 로 뺐다** | 완료 |
| 36a | ~~회차 재시도와 자동 구매확정 이력~~ | **완료.** `V42__batch_run_failure_kind.sql`·`RetryableBatch`·`BatchRetrySweeper`·`BatchRetrySweeperTest`(신설), `BatchRuns`·`OrderStatusBatch`·`AccountPurgeBatch`·`TransactionPurgeBatch`·`BatchRunsTest`·`OrderStatusBatchTest`·`batch-catalog.md` 수정. **`D19` 2층에 코드가 없던 것을 세웠다** — 스위퍼가 10분마다 오늘 회차를 훑어 **마지막 실패가 일시적이고 시도가 셋 미만인 것**만 다시 돌린다. **판단을 실패한 순간에 내린다** — SQLSTATE 로 갈라(`08*`·`40001`·`40P01` 만 일시적) `failure_kind` 에 적는다. 이력만 보고 나중에 가르면 예외 이름 문자열을 파싱하게 되고 판단이 두 벌이 된다. **모르는 실패는 결정적으로 본다** — 넓게 잡으면 같은 자리에서 세 번 죽는다. **시도 횟수를 메모리에 안 둔다** — 이력의 행 수가 곧 시도 수라 기동해도 안 사라진다. **자동 구매확정이 이력을 남기기 시작했다** — 기준일이 있는 배치라 `D19` 기준으로 대상이었고, 그 이력을 처음 쓰는 것이 `19`(정산 마감)의 체인 판정이다. **강제 지점**: `failure_kind` 의 `check` 두 개(실패 행에만·값 둘만)와 **결정적 실패를 안 돌리는 것을 고정한 테스트** | 완료 |
| 36b | ~~배치 스케줄러 스레드 풀~~ | **완료.** `application.yml`·`BatchSchedulingTest`(신설)·`batch-catalog.md`. `spring.task.scheduling.pool.size` 를 **6**(배치 수)으로 정했다. **찾은 것**: 밀리는 자리가 상시가 아니라 **매월 1일 04:00** 이고, 그 시각에 크론 셋이 같이 예약된다(개인정보 파기·자동 구매확정·거래기록 파기). 그동안 5분짜리 둘과 10분짜리 하나가 전부 멈추는데, **멈추는 쪽이 더 급한 것을 들고 있다** — 거래기록 파기는 하루 늦어도 아무 일이 없고 환불 스위퍼가 밀리면 3영업일 기한이 흐른다. **풀 하나를 배치 수만큼 잡았다**(사용자 선택) — 스케줄러를 둘로 가르는 안도 저울질했는데 `@Scheduled(scheduler = ...)` 를 배치 여섯에 다 붙여야 해서, 분할표가 「배치 코드는 안 건드린다」로 예보한 모양과 멀어진다. **`fixedDelay` 는 앞 회차가 끝나야 다음이 잡혀서 배치 하나가 스레드 둘을 안 쓴다** — 그래서 배치 수면 충분하다. **강제 지점은 설정값이 아니라 테스트 둘이다**: ① `ScheduledTaskHolder` 로 등록된 배치 수를 세서 풀과 비교(배치를 더하면 여기가 빨개지고 고칠 곳을 말해 준다), ② 그 수만큼 붙잡아 두고 **전부 시작되는지** 확인(스레드가 하나면 큐에 쌓여 시작조차 못 한다). 「새 배치를 더할 때」 절에 풀을 같이 올리는 항목을 더했다. 원래 설명: `@Scheduled` 가 여섯인데 스레드가 하나다 — `spring.task.scheduling.pool.size` 를 안 정해서 Spring 기본값 1 로 돈다 | 완료 |
| 37 | ~~미결제 주문 정리~~ | **완료 — `11-3` 이 이미 했다.** `OrderStatusBatch.expireUnpaidOrders` 가 결제대기 30분 초과 주문을 `payment_expired` 로 옮기고 재고를 되돌린다(5분 `fixedDelay`, `D19` 카탈로그 첫 줄). 이 행이 적어 둔 강제 지점(「행을 지우지 않고 상태만 옮긴다」)도 전이 제약이 이미 지고 있다. **분할표에만 미착수로 남아 있었다** — `PLAN-2` 의 대조는 방향이 하나라(이력 → 분할표) 이력에 없는 행은 못 잡는다. 원래 설명: 일정 시간이 지난 결제대기 주문을 취소한다 | 완료 |
| 38 | ~~Redis 컨테이너~~ | **완료.** `docker-compose.yml` 에 `redis` 서비스, `starter-data-redis`, `application.yml` 연결 설정, `PostgresTestBase` 에 Redis 컨테이너, `RedisConnectionTest` 4개. **선행에서 `D21` 을 뺐다** — 컨테이너를 띄워 붙이는 데 성능 목표가 필요 없다(`42` 에서 뺀 것과 같은 논리). **컨테이너를 나눈다** — 한 컨테이너에 프로세스가 둘이면 하나가 죽어도 healthy 로 보인다. **의존성만 넣어도 캐시가 Redis 로 넘어갈 뻔했다** — 명시적 `CacheManager` 빈이 막고 있고 그걸 테스트로 고정했다 | 완료 |
| 39 | 판정 캐시 이관 | 로컬 캐시를 Redis 로 옮긴다. 무효화가 여러 인스턴스에 퍼지나. **축**: `concurrency-rules.md`(무효화 전파), `D16`(강등 시 `WARN` — `5c-2` 가 남긴 자리). **강제 지점**: Redis 가 죽어도 판정이 되는 경로 + 그때 `WARN` 한 줄. **건드리는 자리**: `4a` 의 판정 캐시 구현. Redis 설정은 `docker-compose.yml` 에 이미 있다 — 컨테이너를 새로 안 만든다 | 38 |
| 40 | 집계 스키마 | 통계용 테이블 또는 뷰. 실시간 집계와 사전 집계의 갈림길. **축**: `D8` 금액, `D21`(집계 비용). **강제 지점**: 집계 결과와 원장의 합을 대조하는 테스트 — 사전 집계를 고르면 그 값이 언제 낡는지가 문제고, 대조가 그것을 드러낸다. **건드리는 자리**: 신설 집계 표 또는 뷰. 기존은 읽기만 한다 | D8, 12 |
| 41 | 통계 API·화면 | 셀러는 자기 것만. 스코프가 집계에 섞이는 자리. **축**: `D14` 스코프, `D5` 목록 규약, `D20`. **강제 지점**: 집계 쿼리도 판정 스코프를 지나게 한다 — **집계는 권한이 새기 제일 쉬운 자리다**. **건드리는 자리**: 신설 통계 입구와 셀러·관리자 화면. `40` 의 집계 표를 읽는다 | 40, 4 |
| 42-0 | ~~측정을 미리 쌓는다 — `pg_stat_statements`~~ | **완료.** `docker-compose.yml`·`docker/postgres/init/01-stat.sql`(신설)·`stack.md`·`README.md`·`PLAN.md`(`42` 행). **`42 → D21 → 60 → 61 → 70` 사슬의 머리에 표본을 먼저 깐다** — 그날부터 재기 시작하면 하루치로 인덱스를 고르게 된다. **컴포즈에서만 켠다.** 마이그레이션(`create extension`)으로 안 하는 이유는 **Testcontainers 가 그 옵션 없이 떠서** 스키마에 넣으면 테스트 DB 와 로컬 DB 가 갈리기 때문이다. `command` 로 `shared_preload_libraries=pg_stat_statements`·`track=all` 을 주고, `initdb.d` 스크립트가 확장을 만든다. **`auto_explain` 은 안 켰다** — 지금 필요한 것은 「무엇이 느린가」의 목록이지 계획이 아니다. **확인**: `down -v` 후 `up -d --wait` → `select count(*) from pg_stat_statements` 가 28행. `docker compose config --quiet` 종료 0, 컨테이너 둘 `healthy`, `./gradlew integrationTest` 초록(Testcontainers 는 이 경로를 안 타서 안 흔들렸다). **`stack.md` 에 절 둘**: `initdb.d` 는 **볼륨이 비었을 때만** 돌고 안 돌아도 오류가 안 난다는 것, `shared_preload_libraries` 는 기동 인자라야 먹고 `create extension` 만으로는 **뷰를 읽는 순간** 터진다는 것. **축**: 관례(Postgres 표준 확장) + 규약(`D21` 「측정값이 입력」). **강제 지점**: 없다 — 측정 장치다 | 완료 |
| 42 | 성능 측정·튜닝 | 실행 계획을 읽고 인덱스를 고친다. **목표 수치 없이 돈다** — 느린 쿼리를 찾는 데 기준선이 필요 없고, 여기서 나온 수치가 `D21` 의 입력이 된다. **축**: 측정 — 실행 계획을 읽는다. 목표 수치 없이 돈다(`D21` 이 이 결과를 입력으로 쓴다). **강제 지점**: 없다 — 여기서 나온 인덱스가 다음 청크의 근거가 된다. **`42-0` 이 `pg_stat_statements` 를 켜 둬서 여기 올 때 읽을 것이 이미 쌓여 있다.** 시작은 `select calls, mean_exec_time, query from pg_stat_statements order by total_exec_time desc limit 20` 이다. **건드리는 자리**: 인덱스 마이그레이션과 느린 쿼리(`OrderQuery`·`ProductQuery`·`SellerOrderQuery`) | 8 |
| 43 | ~~반품 스키마~~ | **완료**(`44` 흡수). `V63__return_request.sql`·`ReturnRequestSchemaTest`(둘 다 신설), `OrderTransitions`·`TransactionPurgeService`·`TransactionPurgeBatch`·`state-machines.md`·`commerce-compliance.md`(`R36`·`R37` 신설)·`data-lifecycle.md`·`identifier-rules.md` 수정. 표 넷 — `return_request`(진행)·`return_request_item`(부분 반품)·`return_pickup`(수거지, 6개월)·`return_note`(사람 글, 6개월). **묶음은 요약, 반품 표가 진행**이고 어긋남은 지연 트리거 둘이 양방향으로 막되 **상태를 안 옮긴다**. 전이 둘이 늘었다 — `return_requested → delivered`(거절 복귀), `confirmed → return_requested`(제17조제3항 3개월). 파기 둘을 **접수 입구(`43a`)보다 먼저** 넣었다. **`R36`(제18조제9·10항 부담 주체)·`R37`(제17조제5항 입증책임)이 섰다** — 그전에는 약관 문구에만 있어서 강제 지점 5순위였다 | 완료 |
| 44 | ~~반품 상태머신~~ | **`43` 이 흡수해서 같이 쳤다**(점검 G 가 묶었다). | 청약철회 기간과 제한 사유가 전이 조건이 된다. **`copyable_media` 의 판정이 여기서 선다**(`Q6`, `R4`) — 제17조제2항4호는 「복제가 가능한 재화등의 **포장을 훼손한 경우**」라 물건이 돌아와야 아는 사실이고, 제17조제5항이 그 입증을 우리에게 지웠다. **그래서 접수는 안 막는다** — 검수에서 훼손이 확인됐을 때 거절하는 전이가 이 청크의 일이다. **축**: **법**(제17조제2항 제한 사유, 제5항 입증 책임), `D7` 상태기계. **강제 지점**: 전이표에 넣어 상태 제약이 막게 한다 — 「접수는 막지 않고 판정에서 가른다」가 `Q6` 의 결론이라 접수 경로에 조건을 안 건다. **건드리는 자리**: `OrderTransitions`·`OrderStatusPolicy` 와 반품 표 **묶음**: `43` 이 이 청크를 흡수해서 같이 친다(2026-08-23 검토) — 위 행에 근거와 걸릴 것을 적었다. **닫을 때 여기도 같이 닫는다** | 43, 11a |
| 43a-1 | ~~반품 접수 행~~ | **완료**(2026-08-26). `ReturnRequestService`(신설)·`OrderStatusService`·`OrderActionTest` 수정. **`43` 이 표를 세우고 넣는 코드를 안 만들어서 반품이 통째로 막혀 있었다** — 묶음만 `return_requested` 로 가면 `V63` 의 지연 트리거가 커밋에서 거부한다. **데모 시드 기동에서 처음 드러났다**: 그 트리거는 걸린 뒤 한 번도 안 돌았고(테스트는 롤백한다) 빌드도 CI 도 초록이었다. 접수가 같은 트랜잭션에서 `return_request` + `return_request_item`(묶음 통째, `D7`)을 연다. **전이 서비스 안에서 부른다** — 입구마다 부르면 새 입구가 빠뜨린다(`D23` 축 2). **강제 지점을 새로 안 만들었다** — `V63` 의 지연 트리거가 이미 그 자리고 이 청크는 그것을 통과시키는 쪽이다. 테스트가 `set constraints all immediate` 로 커밋 검사를 그 자리에서 돌린다 | 43 |
| 43a-2 | ~~반품 입고·판정~~ | **완료**(2026-08-28). `V64__return_decision.sql`·`ReturnController`·`ReturnDecisionTest`(셋 다 신설), `OrderActionService`·`OrderStatusPolicy`·`ReturnRequestService`·`OrderStatusService`·`ErrorCode`·`ShipmentController`·`actions.ts`·`OrderActionTest`·`OrderStatusPolicyTest`·`state-machines.md`·`commerce-compliance.md`·`permission-rules.md` 수정. **동작을 셋으로 갈랐다**(`receive_return`·`approve_return`·`reject_return`)**고 `update_status` 에서 `return_requested` 를 뺐다** — 그전에는 셀러가 `DELIVER` 로 **반품 거절 복귀**를 밀 수 있었고 `D7` 은 「관리자만」이라고 문서로만 말했다. **판정 둘은 관리자다**(사용자 선택) — 제17조제5항이 훼손 책임의 입증을 우리에게 지워서(`R37`) `state-machines.md` 가 승인을 「셀러」라 적은 것을 **문서부터 고쳤다**. **부담 주체는 요청 칸을 안 만들었다** — 셋이 전부 결정적이라 계산이고, 칸을 두면 `check` 가 막아도 고를 수는 있게 된다(`R36`). **접수 취소를 조문으로 닫았다** — 제17조제4항 발신주의 + 민법 제543조제2항이라 소비자의 일방적 취소권이 없다. **드러난 것 둘**: 거절 복귀가 `freezeDeadlines` 를 다시 불러 **청약철회 기산점이 오늘로 밀렸을 것**이고(`from == SHIPPING` 으로 막았다), 재고 사유 목록(`V41`)에 반품이 없어서 복구가 `order_cancelled` 로 들어갔을 것이다(`return_restocked` 를 더했다) | 43 |
| 43a-3 | ~~확정 뒤 반품~~ | **완료**(2026-08-29). `OrderStatusPolicy`·`OrderStatusPolicyTest`·`OrderActionTest` 수정. **`43` 이 전이표에 `confirmed → return_requested` 를 냈는데 `request_return` 의 허용 상태가 `delivered` 뿐이라 길은 났고 문이 잠겨 있었다** — 확정한 사람의 하자 신고가 통째로 거부됐다. 제17조제3항의 3개월은 구매확정으로 안 끝난다(`D2` R3). **동작을 안 갈랐다**(사용자 선택) — `request_return_defect` 를 따로 만들어 상태 축으로 막는 안이 더 세지만, **사는 사람이 7일 안에 손으로 확정할 수 있어서** 그때 제17조제1항의 권리를 우리 장치가 자른다. 사유 판정은 이미 있는 `requireWithdrawable` 이 하고 강제 지점은 앱 검증(3위)으로 남는다. **경계 넷을 관통 테스트로 못박았다** — 손확정 뒤 기한 안 변심 통과 / 기한 지난 변심 409 / 확정 두 달 뒤 하자 통과 / 넉 달 뒤 하자 409. **빈 DB 기동 + curl 로 넷을 다시 밟았다** — `allowed_actions` 가 확정 뒤 `REQUEST_RETURN` 하나를 내리고, 하자 접수가 커밋돼서 `V63` 의 지연 트리거를 지났다. **드러난 것 둘**: `terminalIsEmpty` 가 구매확정을 종착으로 보고 있었고(취소로 바꿨다), `return_reason` 이 JSON 에서 `DEFECT` 만 받는다(`43a-5` 로 흡수) | 43a-2 |
| 43a-4 | ~~반품 처리 기한~~ → ~~고시 분쟁유형 여섯을 요건표에 세운다~~ | **완료 — 제목이 틀렸던 것이 이 청크의 결론이다.** `commerce-compliance.md`(요건 셋·출처·「아직 안 적은 법」)·`DefectReturnTest`·`RefundApiTest`·`StaleBundleBatch`. **원문을 열었다**(사용자가 PDF 를 줬다 — 별표2 「품목별 해결기준」). **우리 업종은 「2. 온라인서비스 관련 > ①인터넷쇼핑몰업」** 이고 분쟁유형이 여섯이다. **행이 적은 「반품 처리 기한」이 고시에 없다.** 거기 있는 기한은 **「환급은 해제일로부터 3일 이내」 하나뿐**이고 그건 `R5`(제18조제2항 3영업일)가 이미 든다. **여섯을 코드와 대조해 셋으로 갈랐다**: `R38`(미인도 — 해제로 가는 길이 없다) · `R39`(지연인도 — **구매목적 달성 여부로 갈려서 기계가 혼자 못 판정한다**) · `R40`(훼손·오배송·부당청구 — **이미 다루는데 요건 번호가 없어서 `req-coverage` 가 못 세고 있었다**). **`R40` 은 테스트에 번호를 달아 바로 닫았다** — 미호출이 15 → 14 로 줄었다. **코드가 먼저 있고 요건이 없던 자리**다. **`RETURN_STALE_DAYS = 30` 의 근거 없음을 코드에 박았다** — 「찾다가 못 찾은 것이 아니라 **찾아보고 없는 것을 확인했다**」. 다음 사람이 같은 자리를 다시 판다. **「아직 안 적은 법」에서 그 줄을 걷었다** — 승격이 끝났다. **축**: 법(소비자기본법 + 공정위 고시). **강제 지점**: 유형마다 다르다 — `R40` 은 테스트(4위), `R38`·`R39` 는 담당 청크가 설 때 정한다 | 완료 |
| 43a-4a | ~~미인도·지연인도를 요건에서 코드로~~ | **완료 — 없던 것은 화면이었다.** `components/ship-delay-notice.tsx`·`app/orders/[orderNumber]/ship-delay.test.tsx`(둘 다 신설), `app/orders/[orderNumber]/page.tsx`·`commerce-compliance.md` 수정. **행이 예보한 것과 실제가 달랐다**: 「해제로 가는 길이 없다」고 적혀 있었는데 **길은 둘이 이미 있었다** — 사는 사람의 `cancel`(`preparing` 에서 열린다)과 `StaleBundleBatch.cancelUnshipped`(기한 + 7일)이고, 닫히면 `RefundSweeper` 가 환급을 만든다. **요건표가 30일 `WARN` 을 미발송 쪽이라고 적었는데 그 `WARN` 은 방치된 반품 요청 쪽**이었다 — 요건이 코드보다 늦게 서면 이렇게 어긋나고, 그 자리를 고쳤다. **진짜 구멍은 화면이다**: 서버가 `ship_overdue` 를 응답에 싣고 공급 곤란 통지도 나가는데(`56`) **주문 상세가 그 칸을 안 읽었다** — 메일을 못 본 사람에게는 아무 일도 없는 것처럼 보였다. **안 보낸 것과 늦게 보낸 것에 다른 말을 한다** — 떠난 물건은 취소가 아니라 반품이라(`glossary.md`) 늦게 보낸 건에 취소를 말하면 거짓이다. **접수 자리를 새로 안 팠다**(사용자 결정) — 묶음마다 주문 문의가 이미 있고(`58-2`) 거기가 「불편에 그쳤다」를 말할 자리다. **손해배상은 다루기로 정했다**(사용자 결정) — 다만 고시도 소비자기본법 시행령 별표1 도 **배상액을 안 정해서**(2026-09-14 원문 재확인) 자동 계산이 성립을 안 한다. `43a-4b` 로 갈랐다. **강제 지점은 쪽 단위 테스트다** — 조각만 시험하면 **쪽이 값을 안 넘기는 결함**이 안 걸리고 그것이 바로 이 청크가 고친 결함이다. **비동기 서버 조각 때문에 브라우저 렌더러를 못 쓴다** — `react-dom/static` 의 `prerender` 로 문자열을 뽑아 문서에 넣고 `getByRole` 로 묻는다(`stack.md` 감). **부쉈다**(`D25`): 쪽에서 `<ShipDelayNotice>` 를 걷으니 넷 중 **그 둘만** 빨갰다. **축**: 법(소비자분쟁해결기준 별표2 2)·3)·6)) + `D20`. **강제 지점**: 테스트(4위) | 43a-4 |
| 43a-4b | ~~미인도·지연인도 손해배상 — 판정 표와 정산 반영~~ | **완료.** `V69__compensation.sql`(신설), `SettlementItemKind`·`SettlementService`·`SettlementSchemaTest`·`settlement-text.ts`·`money-invariants.md`·`commerce-compliance.md` 수정. **계산기를 안 만들었다** — 고시도 소비자기본법 시행령 별표1 도 **배상액을 안 주고**(2026-09-14 원문 확인) 지연인도는 「구매목적을 달성했나」로 갈리는데 그것이 소비자의 사정이다. 그래서 표가 담는 것은 계산 결과가 아니라 **사람이 내린 판정**이고, `money-invariants.md` 에 「이 축에는 등식이 안 선다」를 절로 적었다 — 대조할 원본이 없는 값이라 **행이 갖춰야 할 것**을 대신 건다. **부담 주체를 사유로 갈랐다**(사용자 결정) — 셀러로 고정하면 **우리 결제나 시스템이 멈춰서 늦은 건까지 셀러가 무는데** 그 정산서는 거짓이다. `bearer = 'seller'` 인 것만 실린다. **지급은 안 한다**(사용자 결정) — 「환불이 아닌 지급」을 모의 게이트웨이에 새로 태우지 않고, 배상액이 사람이 정하는 값이라 지급도 사람이 하는 것이 앞뒤가 맞는다. **기존 제약 셋을 다시 썼다** — 종류·부호·근거 검사에 `compensation` 갈래를 더하면서 **기존 갈래마다 `compensation_id is null` 을 같이 더했다**: 안 더하면 판매 줄에 배상 근거가 붙어도 아무것도 안 막는다. **공급자 칸은 안 고쳤다** — 손해배상금은 재화·용역의 공급이 아니라 부가가치세 과세 대상이 아니고(`R17` 의 세금계산서 대상이 아니다) 이월이 같은 이유로 이미 비어 있다. **부쉈다**(`D25`): `settlement_item_compensation_unique` 를 걷고 컨테이너를 비워 다시 올리니 **그 하나만** 빨갛고 되돌리니 초록. **빈 DB 기동도 봤다** — `shop_check` 에서 `applied_migrations` 가 **69**(파일 66 + 시드 3)고 기동 로그에 오류가 없다. **마무리가 이 청크를 두 자리 고쳤다.** ① 독립 리뷰가 「`bearer = 'platform'` 을 아무것도 안 막는다」를 짚었다 — 앱의 `where` 하나뿐이라 `psql` 도 새 입구도 그 조건을 안 지난다. **외래키로 내렸다**: 판정의 `(번호, 부담 주체)` 쌍을 통째로 가리키고 정산 쪽 값을 `seller` 로 못박아서, `platform` 판정은 **가리킬 쌍이 없어 줄이 안 선다**(검사로는 못 막는다 — 값이 다른 표에 있다). ② 그것을 파다가 **사유 글을 판정 행에 둔 것**이 드러났다 — `data-lifecycle.md` 의 정산 줄이 「사람 글이 안 들어온다」고 적어 뒀는데 정산이 외래키로 그 글을 가리키게 됐고, **판정은 장부라 5년이고 사유는 분쟁 기록이라 3년**이라 한 행에 두면 둘 중 하나가 틀린다. `compensation_note` 로 뗐다(`refund_note`·`order_status_history_note` 와 같은 모양, `5i-2`). **축**: 법(소비자분쟁해결기준 별표2 2)·3)·6)) + `18`·`19` 정산 규약. **마무리가 빠뜨린 호출 하나를 더 찾았다** —  에 3년 물리 삭제로 적고 **지우는 코드를 안 넣었다.**  가 restrict 라 판정이 쌓인 뒤 **5년째 주문 파기가 통째로 멈춘다.**  를 주문보다 **앞에** 두고  에 셋을 세웠다. **부쉈다**: 그 호출을 0 으로 바꾸니 셋 중 둘이 빨갰다. **마무리가 빠뜨린 호출 하나를 더 찾았다** — `data-lifecycle.md` 에 3년 물리 삭제로 적고 **지우는 코드를 안 넣었다.** `compensation.seller_order_id` 가 restrict 라 판정이 쌓인 뒤 **5년째 주문 파기가 통째로 멈춘다.** `TransactionPurgeService.deleteExpiredCompensations` 를 주문보다 **앞에** 두고 `TransactionPurgeServiceTest` 에 셋을 세웠다. **부쉈다**: 그 호출을 0 으로 바꾸니 셋 중 둘이 빨갰다. **강제 지점**: **DB 제약(2위)** — 금액·사유·판정자가 한 행에서 묶이고 부담 주체가 값으로 닫힌다 | 43a-4a, 19 |
| 43a-4c | 손해배상 접수·판정 화면 | **`43a-4b` 가 표까지 세우고 갈랐다.** 지금 `compensation` 에 행을 넣는 길이 `psql` 뿐이다 — `59-2` 가 게시 중단에서 지난 자리와 같고(자리는 있는데 옮기는 코드가 없었다), **그 상태로 두면 배상이 데이터로 안 남는다.** 입구 하나와 관리자 화면 하나다. **셀러 쪽은 이미 보인다** — `20-1` 이 세운 정산서 상세가 배상 줄을 그린다(`settlement-text.ts` 가 문구를 든다). **먼저 정할 것**: 주문 문의(`58-2`)에서 승격시키나 주문 상세에서 바로 여나. **축**: `D14`(새 권한 — `compensation:decide` 를 관리자 밖으로 안 낸다), `D5` 응답 계약, `D20`. **강제 지점**: 마이그레이션의 `do` 블록이 **관리자 밖으로 나갔는지 검사한다**(`V57`·`V58` 과 같은 모양) + 화면은 **권한 목록으로 그린다**(`20-1` 의 `payoutActionsFor` 와 같은 자리). **건드리는 자리**: `V70`(예약), `CompensationService`·`CompensationController`, 관리자 화면. **닫힘**: 셀러에게 `compensation:decide` 를 주면 마이그레이션이 터지고, 배상이 화면에서 서서 정산서에 실리는 것을 테스트가 밟는다 | 43a-4b |
| 43a-28 | ~~세법 보존 조문 대조 — `R41` 과 보존 만료일 계산~~ | **원래 `43a-6` 으로 섰는데 그 번호는 2026-08-29 에 닫힌 「응답 열거값 표기」가 이미 써서 바꿨다**(2026-09-14 설계 세션). **사실**: `TransactionPurgeService.deleteExpiredOrders` 가 거래 종료 5년 뒤 `order_item` → `order_status_history` → `seller_order` → `shop_order` 를 지우는데, `settlement_item`(`order_item`·`seller_order`·`refund_item`·`compensation` 을 restrict)과 `compensation.seller_order_id`(restrict)가 잡아서 **정산된 주문은 파기가 통째로 실패한다.** 5년 지난 정산이 없어 안 터졌고 테스트도 그 조합이 없다. **결정(사용자, 2026-09-14) — (A) 정산을 먼저 파기하고 주문은 그 뒤**: 정산·배상은 세법 보존(과세기간 확정신고기한 다음날 + 5년)까지 살고 **주기 단위로 통째** 사라진다. 주문 파기는 「어느 정산·배상 줄도 이 주문을 안 가리킨다」가 될 때까지 미룬다 — 개인정보법 제21조① 단서 「다른 법령에 따라 보존」이 근거고, 그동안 주문 항목은 장부의 증빙이다. 버린 안은 참조를 `set null` 로 바꾸는 것 — 장부가 근거 잃은 줄을 품고 `compensation` 은 무엇에 대한 배상인지 모르게 된다. **절차**: (1) **조문 대조가 선행이다** — 국세기본법 제85조의3(장부·증거서류 5년, 기산은 신고기한 다음날)과 부가가치세법 제71조를 국가법령정보센터에서 열어 확인하고 `D2` 에 **`R41` 「장부·증빙 보존」** 행을 세운다(조문·기간·기산·강제 지점 = 이 청크의 테스트). 기억과 다르면 멈추고 `D13` 을 다시 본다 (2) `support/TaxRetention.java`(final, 순수) — `LocalDate retainUntil(LocalDate payoutDate)`: 1~6월이면 그해 7월 25일, 7~12월이면 다음 해 1월 25일이 확정신고기한이고 그 **다음날 + 5년**을 돌려준다. `TaxRetentionTest`(빠른 레인) 셋 — 상반기·하반기·연말 경계 (3) `TransactionPurgeService.deleteExpiredSettlements(OffsetDateTime baseline)` — 대상은 `settlement_cycle` 중 `retainUntil(payout_date) < baseline` 이고 **다른 주기의 이월 줄(`carried_from_settlement_id`)이 이 주기의 정산서를 안 가리키는 것**. 이월 사슬이면 뒤 주기가 먼저 만료돼야 하고 그것은 한 달 뒤다. **주기 통째**로 `settlement_item` → `settlement` → `settlement_cycle` 순으로 지운다(cascade 를 수단으로 안 쓴다 — `D23`). 지연 트리거 `settlement_amounts_check` 는 커밋 시점에 정산 행이 없어 `not found` 로 빠진다 — 주석에 적는다 (4) `deleteExpiredCompensations(baseline)` — `compensation` 중 `retainUntil(decided_at 의 날짜) < baseline` 이고 **어느 `settlement_item` 도 안 가리키는 것**. 정산이 (3)에서 먼저 사라지므로 같은 회차에 이어진다. `compensation_note` 는 지금처럼 3년이다 (5) `closedOrderIds` 에 조건 둘 — 그 주문의 `order_item`·`seller_order`·`refund_item` 을 가리키는 `settlement_item` 이 없고, 그 주문의 `seller_order` 를 가리키는 `compensation` 이 없다. `purge()` 의 순서는 정산 → 배상 → 주문 (6) `data-lifecycle.md` 는 설계 세션이 정산·배상·주문 행을 이미 고쳤다(「조문 대조 전」 표시). (1)이 끝나면 그 표시를 지우고 `R41` 을 적는다 (7) `TransactionPurgeServiceTest` 넷 — `정산서가_딸린_주문은_정산이_지워지기_전엔_안_지운다`(**이것이 먼저 서고 빨간 것을 본다** — 지금 코드는 restrict 예외로 죽는다) · `정산_보존이_끝나면_주기를_통째로_지우고_그_뒤_주문도_지운다` · `배상_판정이_딸린_묶음도_같다` · `정산_없는_주문은_지금처럼_5년에_지운다`(회귀). **축**: 법(개인정보법 제21조① 단서 · 국세기본법 제85조의3 · 부가가치세법 제71조 — **대조 선행**) + 규약(`D13` · `D23` cascade). **강제 지점**: 테스트(4위) + 조회 조건(앱 3위). 제약으로는 못 내린다 — 시간 조건이다. **건드리는 자리**: `TransactionPurgeService` · `TransactionPurgeServiceTest` · `support/TaxRetention`·`TaxRetentionTest`(신설 둘) · `commerce-compliance.md`(`R41`) · `data-lifecycle.md` · `batch-catalog.md`(거래기록 파기 행에 정산·배상). **닫힘**: 위 테스트 넷 + `TaxRetentionTest` 셋 · `DataLifecycleCoverageTest` · `RequirementEnforcementTest`(`R41` 이 테스트 이름을 가리킨다) · `verify.sh --full`. **안 되면**: 조문이 기억과 다르면(기간이 5년이 아니거나 기산이 다르면) `TaxRetention` 의 숫자만 바뀐다 — 구조는 같다. **둘로 갈랐다**(사용자 결정 2026-09-16): **이 행은 (1)·(2)와 문서 — 법에서 온 숫자를 확정하는 일**이고, **(3)~(7) 파기 순서·조회 조건·테스트 넷은 `43a-28b`** 다. 가른 이유는 중간에 끊겼을 때다 — 「정산은 지우는데 주문 조건은 아직」이 지금보다 나쁜 유일한 상태고, 앞만 끝난 상태는 파기 동작을 안 바꿔서 나빠지지 않는다. **완료**(2026-09-16). `commerce-compliance.md`(`R41` 신설) · `support/TaxRetention`(신설) · `TaxRetentionTest`(신설, 빠른 레인 4) · `data-lifecycle.md`(「조문 대조 전」 표시 제거). **조문 셋을 원문으로 확인했다**: 국세기본법 제85조의3제2항 「법정신고기한이 지난 날부터 5년(역외 7년)」 · 부가가치세법 제71조제3항 「확정신고 기한 후 5년」 · 같은 법 제49조제1항 「과세기간이 끝난 후 25일」. 계획표의 전제 셋이 다 맞았다. **한 군데를 고쳤다**: 계획표는 `payout_date` 로 과세기간을 정하라는데 조문은 「거래사실이 속하는 과세기간」이라 **주기의 거래일(`period_end`)로 잰다** — 지급일로 재면 12월 주기가 다음 해 1기로 잡혀 반년을 더 들고 있고, 그것은 개인정보법 제21조제1항이 막는다. 넷째 테스트가 그 갈림을 값으로 박았다. **`PublicSurfaceTest` 가 그 자리에서 잡았다** — `TaxRetention` 을 `public` 으로 냈더니 「패키지 밖에서 아무도 안 쓴다」로 빨갰다. package-private 로 좁혔고 `43a-28b` 가 `order` 에서 부를 때 넓힌다 | 17, 43a-4b |
| 43a-28b | ~~5년 주문 파기가 정산·배상·문의를 밟는다 — 순서와 조건~~ | **`43a-28` 에서 갈라 나왔다**(2026-09-16). 앞쪽이 확정한 `R41`·`TaxRetention` 을 쓴다. **사실**: `TransactionPurgeService.deleteExpiredOrders` 가 주문을 지울 때 `restrict` 외래키 셋이 잡는다 — `settlement_item`(`order_item`·`seller_order`·`refund_item`) · `compensation.seller_order_id` · **`inquiry.seller_order_id`**. 셋째는 설계 세션이 못 본 것이고 2026-09-16 에 세어서 나왔다: 문의는 3년인데 **기산이 작성일**이라 주문이 닫히고 2년 넘게 지나 달린 문의는 아직 살아 있고, `purge()` 가 **주문을 문의보다 먼저 지운다.** 지금 조합이 실제로 생기면 그 회차가 통째로 롤백돼서 **그날 다른 사람들의 개인정보도 안 지워진다. 절차**: (1) `deleteExpiredSettlements(baseline)` — `settlement_cycle` 중 `TaxRetention.retainUntil(period_end) < baseline` 이고 다른 주기의 이월 줄(`carried_from_settlement_id`)이 이 주기의 정산서를 안 가리키는 것. **주기 통째로** `settlement_item` → `settlement` → `settlement_cycle` 순(cascade 를 파기 수단으로 안 쓴다 — `D23`). 지연 트리거 `settlement_amounts_check` 는 커밋 시점에 정산 행이 없어 빠진다 — 주석에 적는다 (2) `deleteExpiredCompensations(baseline)` — `compensation` 중 `retainUntil(decided_at 의 날짜) < baseline` 이고 어느 `settlement_item` 도 안 가리키는 것 (3) **`purge()` 순서를 고친다** — 정산 → 배상 → **문의** → 주문. 문의를 주문 앞으로 옮기는 것이 셋째 발견의 처분이다 (4) `closedOrderIds` 에 조건 셋 — 그 주문의 `order_item`·`seller_order`·`refund_item` 을 가리키는 `settlement_item` 이 없고, `seller_order` 를 가리키는 `compensation` 이 없고, `seller_order` 를 가리키는 `inquiry` 가 없다 (5) **`PurgeBlockerTest` 신설**(느린 레인, 사용자 결정 2026-09-16) — `information_schema` 에서 `on delete restrict` 외래키를 읽어 **대상이 파기가 지우는 표인 것**을 고르고, 그 원본 표마다 「파기가 주문보다 먼저 지우나, 아니면 주문 고르는 조건이 이 표를 보나」를 묻는다. 둘 다 아니면 빨갛다. **세는 방법이 곧 규칙이다**(`Q56` 과 같은 꼴) — 새 표가 주문을 `restrict` 로 가리키는 날 그 자리가 빨개진다 (6) `TransactionPurgeServiceTest` 넷 — `정산서가_딸린_주문은_정산이_지워지기_전엔_안_지운다`(**먼저 세우고 빨간 것을 본다**) · `정산_보존이_끝나면_주기를_통째로_지우고_그_뒤_주문도_지운다` · `배상_판정이_딸린_묶음도_같다` · `문의가_달린_주문은_문의가_지워진_뒤에_지운다` (7) `batch-catalog.md` 거래기록 파기 행에 정산·배상·순서. **축**: 법(개인정보법 제21조① 단서 · `R41`) + 규약(`D13`·`D23`). **강제 지점**: 테스트(4위, `PurgeBlockerTest` 가 **종류를 막는다**) + 조회 조건(앱 3위). **건드리는 자리**: `TransactionPurgeService` · `TransactionPurgeServiceTest` · `PurgeBlockerTest`(신설) · `TaxRetention`(가시성) · `batch-catalog.md`. **닫힘**: 위 테스트 넷 + `PurgeBlockerTest` · `DataLifecycleCoverageTest` · `verify.sh --full`. **안 되면**: `PurgeBlockerTest` 가 「조회 조건이 이 표를 보나」를 SQL 텍스트로 재야 하는데 그것이 약하면 **파기가 지우는 표 목록을 상수로 두고 그 목록과 외래키 목록을 대조**하는 데까지만 간다 — 그래도 새 표가 생기는 것은 잡는다. **완료**(2026-09-16). `TransactionPurgeService`(정산·배상 파기 신설 · `purgeableOrderIds` 신설 · `purge()` 순서 · `Purged` 칸 둘) · `PurgeBlockerTest`(신설, 느린 레인 2) · `TransactionPurgeServiceTest`(넷 추가) · `TaxRetention`(가시성) · `batch-catalog.md`. **사용자 결정: 상수 목록 대조로 간다**(SQL 텍스트를 안 읽는다) — 글자를 보는 판정은 상수로 빼거나 파일을 옮기면 조용히 비켜나고 주석에 속기도 한다. **`closedOrderIds` 를 그대로 두고 `purgeableOrderIds` 를 따로 세웠다** — 조건을 공용 자리에 붙였더니 **정산이 걸린 주문의 배송지가 6개월에 안 지워지는** 회귀가 됐다. 배송지는 개인정보고 장부가 아니라 미룰 것이 아니다. **묶음 하나라도 걸리면 그 주문 전체를 뺀다** — 걸린 묶음만 빼고 지우면 그 묶음이 남아 `shop_order` 삭제가 다시 거부된다. **부순 증거 둘**: 조회 조건에서 정산 항을 끄니 첫 테스트가 **외래키 위반으로 죽었다**(고치기 전 상태 그대로) · `PurgeBlockerTest` 의 처분 목록에서 `settlement_item` 한 줄을 빼니 그 표의 외래키 **셋을 정확히 짚어** 빨갛다 | 43a-28 |
| 43a-5 | 반품 수거·검수·화면 | `43a-3` 이 확정 뒤 반품을 열었다. 남은 것은 **수거지**(`return_pickup`)·**검수 소견**(`inspected`·`return_note.inspection_note`)과 **화면 셋** — 셀러의 입고·검수, 관리자의 판정, 사는 사람의 반품 진행. **수거지는 개인정보라 셋이 다 차야 컬럼을 만든다**(`D23`) — 동의 항목·`D13` 수명·파기 코드. **검수를 상태로 세울지가 갈린다** — `inspected` 를 세우면 전이표·권한·화면이 같이 늘고, 입고 안에서 소견만 받으면 상태가 안 는다. **`43a-6` 이 표기를 닫았다** — 응답도 요청도 `DEFECT` 라 화면은 대문자 하나만 알면 된다. **축**: `D20` 화면 규약, WCAG, **법**(제17조제5항 입증책임 — 검수 소견이 그 근거다). **강제 지점**: 검수 소견은 `inspected_by_user_id` 가 이미 짝을 강제한다. 화면은 `lint` 가 접근성을 받는다. **건드리는 자리**: `ReturnController`(수거·검수), `frontend/src/app/seller/orders`·`admin`, 사는 사람의 주문 상세  **표 조각을 여기서 모은다**(`20-1` 이 흡수시켰다) — `Th`·`Td` 가 받은 주문과 정산서에 사본 둘로 있고, 화면 셋 중 하나가 셋째 표가 되는 순간 `components/` 로 옮긴다(`Pager` 가 `13c-1` 에서 같은 자리를 지났다). | 43a-3 |
| 43a-6 | ~~응답 열거값 표기~~ | **완료**(2026-08-29). `OrderStatusService`·`SellerOrderQuery`·`order-text.ts` 수정, `ResponseEnumCaseTest` 신설. **`43a-3` 이 넘긴 것을 열어 보니 방향이 반대였다** — `D5` 「값의 형식」이 열거값을 대문자 스네이크로 이미 정했고 `enumValue()` 를 지나는 15곳이 그것을 지키는데, `SellerOrderQuery` 의 `return_reason` **한 칸만 그 호출을 빠뜨려서** 저장값 `defect` 가 새 나갔다. 화면 `RETURN_REASON` 표가 그 샌 값에 맞춰 소문자로 쓰여 있었다. 규약이 아니라 코드가 틀린 것이라 **`D5` 는 안 고쳤다**. **강제 지점을 타입(1위)으로 내렸다**(사용자 선택) — `Detail.returnReason` 을 `String` 에서 `ReturnReason` 으로 바꾸면 표기를 고르는 자리가 없어져 빠뜨림이 성립하지 않는다. `ReturnReason.of` 는 `ProductQuery` 의 관례를 따라 **모르는 저장값에 터진다**. **거기에 대조 테스트를 얹었다** — 칸 이름을 안 적고 **저장값 집합을 들고 응답 전체를 훑어서** 새 칸이 생겨도 등록 없이 걸린다. **그 테스트가 자기 자신을 검증한다**(저장값을 일부러 실어 보고 잡히는지 본다). **드러난 것 둘**: 대조가 `_visible_field_groups` 의 `"shipping"` 을 헛짚었다 — 필드 그룹 이름과 배송 상태 코드가 글자만 같아서, `D5` 의 「밑줄은 메타 필드」를 근거로 메타 칸을 건너뛰게 했다. `OrderQuery`·`SellerOrderQuery` 의 `enumValue` 는 **그냥 `toUpperCase()` 라 DB 의 모르는 값을 그대로 올려 보낸다** — `ProductQuery` 는 enum 을 지나 막는다(`43a-7` 로 세웠다) | 43a-3 |
| 43a-7 | ~~주문 상태를 열거형으로 지나게 한다~~ | **완료**(2026-08-29). `OrderTransitions`(`statusName` 신설)·`OrderQuery`·`SellerOrderQuery`·`ResponseEnumCaseTest` 수정. **`enumValue` 사본이 여섯인데 `ProductQuery` 하나만 열거형을 지났다** — 나머지 다섯은 `toUpperCase()` 라 DB 에 모르는 값이 들어와 있으면 그대로 응답에 실려 나간다. **여섯을 한 청크로 못 쳐서 잘랐다**(파일 1~3개) — 여기서는 **열거형이 이미 있는 주문 쪽 상태**만 옮겼다. 결제 층은 `Payment.of`, 배송 층은 `Shipment.of`, **상태 이력은 두 층이 한 목록에 섞여 들어와서** `OrderTransitions.statusName` 이 둘 다 찾아본다(값이 안 겹친다). **강제 지점이 타입(1위)이다** — 어긋난 순간 터진다. 제약을 떼고 모르는 값을 넣어 **실제로 터지는 것을 테스트로 고정**했다(지연 트리거 때문에 `set constraints all immediate` 를 먼저 부른다). **`enumValue` 에 남은 넷을 주석에 못박았다** — `actor_type`·`clause`·`reason_code`·`method`. 넷 다 코드가 값마다 분기하는데(`"admin".equals(actor.type())`, `"card".equals(command.method())`) Java 열거형이 없다 | 43a-6 |
| 43a-8 | ~~주문 쪽 열거형 둘~~ | **완료**(2026-08-29). `ActorType`·`ContractClause`(둘 다 신설), `OrderQuery`·`ResponseEnumCaseTest` 수정. **넷을 자원별로 갈랐다**(사용자 선택) — `actor_type` 은 `Actor.` 쓰는 곳까지 14파일, `reason_code`·`method` 도 13~14파일이라 한 청크에 안 들어간다. **읽는 쪽만 바꾼다**: `enumValue` 자리에서 열거형을 지나게 하고 `Actor.type`·`Command.method` 는 문자열로 남긴다 — 그쪽까지 가는 것(`D23` 「생 문자열을 쓰지 않는다」의 완성)은 이 열거형들을 쓰기만 하면 되므로 **여기가 그 선행이고 후속이 `43a-15`·`43a-16` 이다**(2차 마무리에서 세웠다 — 그전에는 「선행이다」라고만 하고 후속이 계획에 없었다). **`clause` 입력은 안 건드렸다** — 조항 하나를 펼치는 경로는 모르는 값에 조회가 0건이라 404 로 답하는데, 거기서 `of()` 를 부르면 없는 조항이 500 이 된다. **`43a-6` 의 대조 사각지대가 절반 닫혔다** — 값 집합에 둘을 더했고, **그 훑기가 빈 응답을 본 것이 아님을 따로 못박았다**(`detailActuallyCarriesScannedFields`). 「걸려 있다」와 「돈다」는 다르고 이 저장소가 이미 밟은 함정이다 | 43a-7 |
| 43a-9 | ~~결제 쪽 열거형 둘~~ | **완료**(2026-08-29). `PaymentStatus`·`PaymentMethod`(둘 다 신설), `OrderQuery`·`ResponseEnumCaseTest` 수정. **분할표가 남은 수를 틀리게 적고 있었다** — `43a-7`·`43a-8` 이 「남은 넷/둘」이라 적으면서 **환불 상태와 결제 상태를 빼먹었다.** 세어 보니 넷이고(`payment.status`·`payment.method`·`refund.status`·`refund.reason_code`) `payment` 패키지에 열거형이 하나도 없었다. 그래서 **앞서 고른 자원별 가르기를 그대로 적용해 결제/환불로 또 갈랐다** — 여기는 결제 둘. **이름이 부딪쳤다**: `shop_order.status`(주문이 어디까지 왔나)와 `payment.status`(승인 시도 한 건의 결과)가 둘 다 `paymentStatus` 였다. **부딪쳤다는 것 자체가 둘이 헷갈린다는 증거**라 주문 쪽을 `orderPaymentStatus` 로 갈랐다. 분기 근거는 `"card".equals(...)` 두 곳(`PaymentService` 검증·`MockPaymentGateway` 판정)과 **`approved` 상수가 두 클래스에 중복 선언**된 것이다. **열거형은 `payment` 패키지에 뒀다**(`D23` 「남의 자원 표를 언제 직접 만지나」 — 값 목록은 그 자원의 규칙이다). `OrderQuery` 가 이미 그 표를 직접 읽어서 공개했다 | 43a-8 |
| 43a-11 | ~~환불 쪽 열거형 둘~~ | **완료**(2026-08-29). `RefundStatus`·`RefundReason`(둘 다 신설), `OrderQuery`·`RefundQuery`·`RefundService`·`ResponseEnumCaseTest` 수정. **`RefundService` 에 값 목록이 이미 상수 여덟으로 모여 있어서** 열거형을 그냥 세우면 **같은 목록이 두 벌**이 된다. 상수를 없애려면 부르는 자리가 서른이라 한 청크에 안 들어간다. 그래서 **상수를 열거형에 매달았다**(`APPROVED_CODE = RefundStatus.APPROVED.code()`) — 정본이 하나로 남고 호출자를 안 건드린다. **하나씩 없애는 것이 `43a-16` 이다**(2차 마무리에서 「나중에」로 끝나 있던 것을 청크로 세웠다). **`OrderQuery.enumValue` 를 지웠다** — 호출자가 0이 됐는데 남겨 두면 다음에 열거 칸을 더하는 사람이 그것을 베낀다. **`43a-6` 이 잡은 것이 정확히 그 일**이었다. 사유마다 **기산점 조문이 갈리는 것**을 열거형 주석에 실었다(제18조제2항 3호 / 제15조제2항, `V25` 의 근거). **대조 사각지대가 다 닫혔다** — 응답 열거값이 전부 열거형을 지난다 | 43a-9 |
| 43a-12 | ~~문의 쪽 열거형 둘~~ | **완료**(2026-08-29). `InquiryStatus`·`InquiryKind`(둘 다 신설), `InquiryService`·`InquiryQuery` 수정. **`43a-11` 과 다르게 상수를 없앴다** — 거기는 호출자가 서른이라 상수를 열거형에 매달았는데 여기는 **`InquiryService` 한 파일 14곳**이라 갈아탈 수 있다. `D23` 「Java 표현 — 생 문자열을 쓰지 않는다」를 실제로 달성한다. **상수가 이미 불완전했다** — `kind` 가 DB 에 다섯인데 상수는 셋뿐이었다(`access_objection`·`dispute` 없음). 열거형이 정본이면 그 어긋남이 안 생긴다. **`kind` 는 대상·기한·보존을 한꺼번에 정한다** — `product`·`order` 만 대상이 붙고, `processing_stop` 만 10일이며(개인정보 보호법 시행령 제44조제2항, `D2` R28), `dispute` 는 3년 보존이다. 그 셋을 열거형 주석에 실었다. **`ResponseEnumCaseTest` 는 안 넓혔다** — 그것은 주문 자원의 응답을 훑는 테스트고 문의는 다른 자원이다. 강제 지점이 이미 타입(1위)이라 훑기(4위)보다 세다. **문의 응답이 대조 밖에 있다는 사실은 `43a-10` 행에 적었다** | 43a-11 |
| 43a-13 | ~~정산 쪽 열거형 셋~~ | **완료**(2026-08-29). `SettlementItemKind`·`SettlementSupplier`·`PayoutStatus`(셋 다 신설), `SettlementQuery`·`SettlementPayoutService` 수정. **부호·공급자 규칙을 열거형에 안 실었다** — `V52` 가 이미 정했다: 부호는 `settlement_item_amount_sign_check`(2위), 근거는 `settlement_item_source_check`(2위), **공급자는 생성 열이라 어긋날 방법 자체가 없다**(1위, 구조). Java 필드로 옮기면 **규칙이 두 벌이 되고 강제 지점이 앱 검증(3위)으로 내려간다** — `D23` 축 2 가 「가능한 한 아래로」라 정반대다. 열거형은 **값 목록과 표기만** 맡고, 다음 사람이 옮기려 드는 것을 막는 근거를 주석에 적었다. 상수는 `43a-12` 처럼 없앴다(`SettlementPayoutService` 한 파일 10곳). **`enumValue` 사본이 둘로 줄었다** — 남은 `SellerOrderQuery`·`ProductQuery` 는 **둘 다 이미 열거형을 지난다** | 43a-12 |
| 43a-10 | ~~표기 자리를 한 곳으로 모은다~~ | **완료**(2026-08-29). `EnumValue`(신설, `support/`), `OrderQuery`·`SellerOrderQuery` 수정, `coding-rules.md`·`testing-strategy.md` 보강. **세어 보니 판단이 뒤집혔다** — 남은 사본이 둘인 줄 알았는데 **같은 모양의 헬퍼가 여섯 파일에 열여덟 벌**이었다(`code == null ? null : X.of(code).name()`). **진짜 사본은 `enumValue` 라는 이름이 아니라 null 가드 + `name()` 패턴**이고, 사본이 그만큼이면 한 벌이 패턴을 안 따라도 아무도 모른다 — `43a-6` 이 잡은 사고가 그것이다. **공용 자리 먼저, 자원별로 갈아탄다**(사용자 선택) — 여기서는 `EnumValue` 를 세우고 **주문 쪽 열 벌**만 옮겼다. **`support/` 에 뒀다**(`D23` 「자원이 아닌데 패키지를 파는 경우」의 조건 셋 중 「기존 비자원 패키지에 넣으면 순환이 된다」가 안 걸린다) — 부르는 쪽이 열거형을 적으므로 **이 자리는 열거형을 하나도 모른다**. **헬퍼 이름이 하던 문서 역할이 타입 이름으로 옮겨갔다** — `EnumValue.of(x, OrderTransitions.Payment::of)` 와 `EnumValue.of(x, PaymentStatus::of)` 가 그 자리에서 갈린다. **훑기는 안 넓혔다**(사용자 선택) — 근거를 `D15` 에 절로 세웠다: 훑기가 잡는 것은 「헬퍼를 안 부르고 생 문자열을 실은 새 칸」뿐이고, 위험은 자원마다 같지만 **비용도 자원마다 든다**. **훑기는 사고가 난 자리에 세우는 것**이지 미리 까는 것이 아니다 | 43a-13 |
| 43a-14 | ~~남은 넷을 EnumValue 로 갈아탄다~~ | **완료**(2026-08-29). `RefundQuery`·`InquiryQuery`·`SettlementQuery`·`ProductQuery` 수정. **표기 헬퍼가 0이 됐다** — 열여덟 개가 사라지고 호출 스물다섯 곳이 `EnumValue` 하나를 지난다. **섞여 있던 구간이 닫혔다**(`43a-10` 이 대가로 적어 둔 것). **`ProductQuery.reasonValue` 도 같이 갔다** — `WithdrawalRestrictionReason.of` 가 `null` 을 돌려주는 다른 모양이라 안 맞을 줄 알았는데, **`EnumValue` 가 `null` 을 먼저 거르므로** 저장값이 있으면 `of` 가 열거형을 주거나 터진다. **`OrderTransitions.statusName` 만 안 갔다** — 그것은 상태 이력 한 줄이 어느 층인지 모르는 채로 **두 층을 찾아보는** 다른 일이라 공용 자리가 대신할 수 없다 | 43a-10 |
| 43a-15 | ~~전이 주체를 열거형으로 받는다~~ | **완료**(2026-08-29). `OrderStatusService.Actor` 와 호출자 셋 수정. **팩터리를 주체별로 갈랐다** — `person(String, long)` 을 없애고 `customer(long)`·`seller(long)` 을 세웠다. `system`·`admin` 은 이미 그 모양이었고 **나머지 둘만 문자열로 뭉쳐 있었다**. 이제 **문자열 인자가 아예 없어서** 오타도 역할 코드 혼입(`seller_owner` 대 `seller`)도 성립하지 않는다. 필드도 `ActorType` 이라 `"admin".equals(actor.type())` 이 `actor.type() == ActorType.ADMIN` 이 됐고, 저장할 때만 `code()` 를 지난다. **드러난 것**: 테스트가 `Actor.person("admin", userId)` 로 **사유 없는 관리자**를 만들고 있었다 — 「관리자여도 사유가 없으면 막힌다」를 보려고 `person` 을 우회로로 쓴 것이다. `Actor.admin(userId, null)` 로 바꿔 뜻을 그대로 적었다 | 43a-8 |
| 43a-16 | ~~결제·환불 도메인의 생 문자열을 없앤다~~ | **완료**(2026-08-29). `PaymentController`·`PaymentService`·`MockPaymentGateway`·`RefundService`·`RefundSweeper`·`RefundQuery`·`DemoOrderSeeder`·`checkout-form.tsx` 수정, `api-guidelines.md` 보강, 테스트 아홉 파일. **`D5` 와 실물이 어긋난 것을 먼저 처리했다** — `D5` 는 「422 = 형식은 맞는데 값이 규칙에 안 맞음」인데 요청 필드를 enum 으로 받으면 **역직렬화에서 걸려 400** 이라 검증기까지 못 간다. **400 으로 통일하고 `D5` 에 예외를 절로 적었다**(사용자 선택) — 문자열 + `@Pattern` 이면 422 를 낼 수 있지만 값 목록이 `check`·enum·정규식 **셋**이 된다. **사본이 하나 느는 값이 오류 코드 하나보다 크다.** **계약이 바뀌었다**: `method` 요청 값이 `card` → `CARD`. 응답의 `method` 는 이미 대문자였고 요청만 소문자였다 — **화면(`checkout-form.tsx`) 한 줄과 테스트 둘을 고쳤다**(사용자 선택). **멱등키 지문은 `code()` 를 넣는다** — 열거값 이름을 넣으면 보관 중인 지문과 안 맞아 같은 요청이 다른 것으로 보인다. `RefundService` 상수 여덟을 없애고 `Pending.status` 를 `RefundStatus` 로 바꿨다 — 그전에는 `RefundStatus.APPROVED` 와 `PaymentStatus.APPROVED` 가 **글자가 같아서** 한쪽을 다른 쪽 자리에 넣어도 컴파일이 통과했다 | 43a-11 |
| 43a-17 | ~~환불 표의 주체도 `ActorType` 으로~~ | **완료 — 여는 대신 옮겼다**(사용자 선택). `support/ActorType.java`(이동·`public`)·`order/ActorType.java`(삭제)·`RefundService.java`·`EnumConstraintTest.java`·`stack.md`. **`PAIRS` 는 이미 제약 둘을 같이 보고 있었다** — `43a-15` 가 `order_status_history_actor_type_check` 와 `refund_requested_by_type_check` 를 한 줄에 적어 뒀는데 **코드만 한쪽이 문자열이었다.** 대조는 서 있었고 타입이 안 따라간 것이다. **`order` 에서 열지 않았다**: 그러면 이 값을 쓰는 자원이 늘 때마다 `X → order` 의존이 생기고 `ArchitectureTest` 의 순환 예외가 자란다 — 그 규칙이 「넷째가 생기면 멈춘다」고 적어 뒀고 지금 셋이다. **`refund` 를 읽는 패키지가 이미 넷**이라 셋째 소비자가 가깝다. **자원마다 따로 만드는 안도 버렸다** — 쪼개진 서비스에서는 그게 맞지만 이 저장소는 아직 한 덩어리고, `43a-*` 사슬이 없애려던 모양이다. **`support`→자원별 복사는 쪼갤 때 쉽다.** **승인자는 안 건드렸다**: `approved_by_type` 이 `admin`·`system` 둘이라 **값 집합이 달라** 같은 타입으로 못 묶는다(`V51`). `BY_SYSTEM` 상수가 그쪽에 남았고 주석에 이유를 적었다. **부쉈다**(`D25`): `COURIER` 를 더하니 **제약 둘 다** 빨갛고 되돌리니 초록. **그 전에 헛돌았다** — `gradlew test`(빠른 레인)로 돌려서 0개 실행에 `BUILD SUCCESSFUL` 이었다. `EnumConstraintTest` 는 DB 를 타서 `integrationTest` 다. **축**: 규약(`D23`). **강제 지점**: **타입(1위)** + `PAIRS` 대조(4위) | 완료 |
| 43a-18 | ~~반품 표의 열거값 둘~~ | **완료.** `ReturnStatus.java`·`ReturnShippingFeeBearer.java`(둘 다 신설)·`ReturnRequestService.java`·`EnumConstraintTest.java`(`PAIRS` 두 줄)·`commerce-compliance.md`. **행이 적은 상태 넷이 실제로는 여섯이었다** — `picked_up`·`inspected` 가 더 있고 **둘 다 건너뛸 수 있는 칸**이다(택배가 걷어 가지 않으면·검수를 따로 안 적으면). `isDecided()` 로 끝 둘을 갈랐다. **부담 주체가 이 청크의 본체다**(`D2` R36, 제18조제9·10항). **판정 표가 이미 `bearerOf` 의 Javadoc 에 있었는데 값만 문자열이었다** — 그 표를 타입 안으로 옮겼다. **`close` 가 부담 주체를 인자로 안 받게 바꿨다** — 넘기게 두면 그 자리가 **선택지**가 된다. 이제 판정과 사유에서 나오므로 **고를 자리가 없다.** `V63` 의 `check` 셋이 같은 것을 막지만 **막히는 것과 고를 수 없는 것은 다르다** — 제약은 값이 들어갈 때, 타입은 만들 때 막는다. **부쉈다**(`D25`) 셋: `CANCELLED`·`PLATFORM` 을 더하니 `PAIRS` 가 **제약 둘 다** 빨갛게 했고(`null or in (…)` 꼴도 읽는다), **하자 판정을 `CONSUMER` 로 뒤집으니 「하자를 인정하면 판매자가 문다」가 잡았다** — 법 규칙에도 그물이 있다. **축**: 법(`D2` R36) + 규약(`D23`). **강제 지점**: **타입(1위)** + `PAIRS` 대조(4위) | 완료 |
| 43a-19 | ~~열거형과 제약을 대조한다~~ | **완료**(2026-09-03). `EnumConstraintTest` 신설, `UserStatusTest`·`ProductStatusTest`·`OrderStatusListTest`·`SellerQueryTest` 에서 같은 단언을 걷어냈다. **한 파일에 모았다**(사용자 선택) — 결정의 이유는 대조가 아니라 **누락 감시자**다. 열거형마다 옆에 두면 「새 열거형이 대조를 안 받는 것」을 막는 것이 아무것도 없고, `점검 H` 가 찾은 구멍이 정확히 그 모양이었다. `everyEnumIsAccountedFor` 가 `main` 이 컴파일된 자리를 걸어 열거형을 전부 뽑고, `PAIRS`·`GENERATED`·`EXEMPT` 어디에도 없으면 빨개진다. **`점검 H` 의 수를 고쳤다** — 4/19 가 아니라 **7/18** 이다. `ProductStatusTest` 가 상품·조합·청약철회 사유 셋을 이미 대조하는데 파일 수로 셌다. **안 걸린 열하나는 정확했다.** **셋이 실물에서 갈렸다**: ① 감시자가 **중첩 열거형 넷**을 더 찾았다(`AuditLog$Kind`·`AdvertisingGate$Verdict`·`OrderActionService$Action`·`OrderStatusService$ReturnReason`) — 이름으로 훑은 목록에 없던 것이라 **감시자가 첫 실행에서 값을 했다.** ② 그중 `ReturnReason` 은 `seller_order_return_reason_check`·`return_request_reason_code_check` 둘에 걸려 있어 `PAIRS` 로 갔다. ③ `SettlementSupplier` 는 `check` 가 없다 — `settlement_item.supplier` 가 **생성 열**이라 목록이 생성 식 안이고, `information_schema` 의 `generation_expression` 에서 `then` 뒤만 뽑는다. **열거형이 `package-private` 이라 `code()` 를 리플렉션으로 부른다** — 대안이 대조 하나 때문에 열여덟의 접근 범위를 넓히는 것인데 그러면 `43a-15` 가 정한 「`ActorType` 은 `order` 밖에서 안 쓴다」 같은 결정이 테스트 때문에 풀린다. **실제로 도는 것을 봤다** — `main` 에 열거형 하나를 임시로 넣으니 감시자가 그 이름을 대고 빨개졌고, 지우니 초록으로 돌아왔다 | 43a-16 |
| 43a-20 | ~~열거형이 없는 닫힌 목록 열 — 권한~~ | **완료**(2026-09-03). `Scope`·`Effect`(신설), `PermissionEvaluator`·`PermissionRuleLoader`·`PermissionCatalog` 수정. **`점검 H` 가 센 열 개 컬럼을 자원별로 갈랐고**(`CLAUDE.md` 청크 상한) 이 청크는 **권한 축 둘**이다 — 나머지는 `43a-22`~`43a-25`. **두 `switch` 의 `default` 가 이 청크의 이유다** — `covers` 의 `default -> false` 와 `scopeWidth` 의 `default -> 0` 이 짝이라, 범위를 하나 더하면 **권한을 준 사람은 줬다고 믿는데 아무도 안 통과하고 오류도 로그도 안 남았다.** 지금은 `default` 가 없어서 **값이 늘면 컴파일이 안 된다**(1위). **응답 계약이 바뀌었다**(사용자 선택) — `GET /api/me/permissions` 의 `scopes` 가 `["own"]`→`["OWN"]` 이다. `D5` 「값의 형식」을 따른 것이고 `43a-16` 의 `card`→`CARD` 와 같은 자리다. **`Scope` 만 `public` 이다** — `PermissionCatalog.Entry` 가 공개 응답 record 라 그 자리를 `String` 으로 두면 아무 문자열이나 들어간다. **프론트에 합집합 타입을 안 만들었다** — 머리가 `resource`·`action` 으로만 고르고 범위 값을 비교하는 자리가 없어서, 적으면 서버와 안 맞춰지는 사본만 는다. **드러난 것 둘**: `PermissionMatrixTest` 가 `List.of("own","seller","all")` 로 **세 번째 사본**을 들고 있어서 `values()` 로 바꿨다(스냅샷은 그대로 — `.code()` 라 글자가 같다). `@Nested class Effect` 가 새 열거형을 가려서 `EffectPriority` 로 고쳤다. **`gradlew build`·프론트 넷 통과** | 점검 H |
| 43a-22 | ~~닫힌 목록 열 — 셀러 상태~~ | **완료 — 열거형을 안 만들었다.** `SellerSchemaTest.java`(대조 하나)·`coding-rules.md`(「지금 어디에 있나」 행)·`quality-gates.md`. **전제가 실물과 달랐다**: 행이 「화면과 판정이 같이 본다」고 적었는데 **Java 도 화면도 `seller.status` 를 안 읽는다** — `active` 를 묻는 자리가 SQL 안에만 둘이다(`SellerQuery.findPublicIdentity` 의 `where`, `V13` 판매 시작 트리거). `D23` 「가르는 물음」의 답이 「값이 늘어도 코드를 안 고친다」라 **enum 을 만들면 소비자 0인 타입이 선다**(대전제 「세어 보고 하나뿐이면 안 만든다」). 대신 그 두 SQL 이 매달린 값을 `check` 정의에서 뽑아 고정했다 — 이름이 갈리면 공개 신원이 통째로 비고 트리거가 **모두를 막는다.** **부쉈다**: `active` → `live` 로 빨강(단언이 이유인 것을 리포트로 확인 — Flyway 는 61개 검증 통과), 되돌려 초록. **축**: 규약(`D23`). **강제 지점**: 테스트(4위) — 열거형이 없어 타입으로 못 올린다 | 완료 |
| 43a-23 | ~~닫힌 목록 열 — 정산 배치~~ | **완료.** `BatchRunStatus.java`·`FailureKind.java`(둘 다 신설)·`BatchRuns.java`·`EnumConstraintTest.java`(`PAIRS` 두 줄)·`BatchRetrySweeperTest`·`BatchRunsTest`. **`43a-22` 와 답이 갈렸다** — 여기는 Java 가 값을 만든다(`failureKindOf` 가 SQLSTATE 로 갈라 낸다). **SQL 리터럴도 같이 묶었다**(예보에 없던 것): `shouldRetry`·`alreadySucceeded` 의 `succeeded`·`transient`·`skipped` 를 이름 파라미터로 바꿔 열거형이 값을 대게 했다 — 안 그러면 enum 을 고쳐도 그 SQL 이 옛 글자를 물고 **조용히 아무도 안 걸린다**(`43a-22` 가 본 자리와 같은 모양). **부쉈다**(`D25`): `BatchRunStatus` 에 `ABORTED` 를 더하니 `EnumConstraintTest` 가 빨갛고 되돌리니 초록. **축**: 규약(`D23`). **강제 지점**: **타입(1위)** + `PAIRS` 대조(4위) | 완료 |
| 43a-24 | ~~닫힌 목록 열 — 재고 사유~~ | **완료.** `StockReason.java`(신설)·`OrderService.java`·`OrderStatusService.java`·`EnumConstraintTest.java`(`PAIRS` 한 줄). 다섯을 든다 — Java 가 셋을 만들고(`ORDER_PLACED`·`ORDER_CANCELLED`·`RETURN_RESTOCKED`) 둘은 SQL·시드 쪽이다(`INITIAL`·`ADJUSTMENT`). **`public` 이다**: 표는 상품 자원인데 재고를 옮기는 쪽이 `order` 패키지라 근거를 Javadoc 에 적었다(`43a-27` 규칙). **SQL 리터럴 둘을 같이 묶었다**(`43a-23` 과 같은 이유) — `OrderService` 의 인라인 `'order_placed'` 와 `restoreStock` 의 `String reason` 인자. **부쉈다**(`D25`): `SHRINKAGE` 를 더하니 빨갛고 되돌리니 초록. **축**: 규약(`D23`). **강제 지점**: **타입(1위)** + `PAIRS` 대조(4위) | 완료 |
| 43a-25 | ~~닫힌 목록 열 — 알림 이력의 축 셋~~ | **완료**(2026-09-03). `NotificationKind`·`NotificationChannel`·`NotificationStatus`(신설), `NotificationService`·`NotificationTemplates`·`AdvertisingNotifications`·`NotificationSendTest` 수정. **넷을 한 청크로 못 쳤다** — `event_type` 은 발송 자리가 넷이라 파일이 열 개가 된다. 이력의 축 셋만 치고 사건 아홉은 `43a-26` 으로 갈랐다. **`kind` 는 법이 든다**(정보통신망법 제50조) — 광고는 사전 동의와 야간 동의를 보고 거래 통지는 안 본다. **`channel` 은 값이 하나뿐인데도 열거형이다** — 「가르는 물음」이 묻는 것은 값이 몇 개냐가 아니라 값이 늘 때 코드를 고쳐야 하나이고, 채널이 늘면 보내는 코드도 주소 찾는 코드도 갈린다. 그전에는 발송 SQL 안에 `'email'` 이 박혀 있어서 **고칠 자리를 쿼리 본문에서 찾아야 했다.** **`.query(Version.class)` 자동 매핑을 뗐다** — 저장값이 소문자라 `valueOf` 가 못 읽고, 읽더라도 모르는 값에 조용히 실패한다. **강제 지점을 하나 더 세웠다**(이 청크의 값) — 거래 통지 입구로 광고 판이 나가는 것을 막는다. 접근 범위만으로는 안 막히는 자리였다: 광고 입구는 패키지 안이지만 **공개 `send` 는 누구나 부르고** 거기 광고 판 코드가 들어오면 관문을 안 지난 광고가 나간다. **종류가 타입이 되기 전에는 비교할 것 자체가 없었다.** 관통 테스트를 붙였고 판이 없을 때도 같은 예외라 **메시지까지 고정**했다. **드러난 것 하나**: `password_reset`·`email_change` 가 `check` 에 있는데 **Java 도 시드도 그 값을 안 만든다** — `43a-26` 이 열거형을 세울 때 같이 처분한다. **`gradlew build` 통과** | 43a-20 |
| 43a-26 | ~~닫힌 목록 열 — 알림 사건 아홉~~ | **완료.** `NotificationEventType.java`(신설)·`NotificationService.java`(`send` 서명 셋)·`NotificationSweeper.java`·`ConsentReconfirmSweeper.java`·`AdvertisingNotifications.java`·`EnumConstraintTest.java`·알림 테스트 둘. **`PASSWORD_RESET`·`EMAIL_CHANGE` 를 안 지웠다** — `D18` 이 `5c-1`·`5e-1` 에 배정한 값이라 Javadoc 에 「부르는 코드가 아직 없다」와 그 청크 번호를 적었다. 지우면 그 청크가 제약부터 다시 연다. **SQL 리터럴 다섯도 묶었다**(`43a-23`·`43a-24` 와 같은 이유) — 쓸이 `n.event_type = '…'` 로 박아 쓰던 자리를 이름 파라미터로. **사건과 템플릿 코드는 계속 갈라 둔다** — 광고만 다르고(`send(eventType, eventType.code(), …)`) 그 이유가 `43a-25` 의 관문이다. **부쉈다**(`D25`): `SHIPPING_STARTED` 를 더하니 빨갛고 되돌리니 초록. **축**: 규약(`D23`). **강제 지점**: **타입(1위)** + `PAIRS` 대조(4위) | 완료 |
| 43a-27 | ~~근거 없는 `public` 을 좁힌다~~ | **완료.** `PublicSurfaceTest.java`(신설)·좁힌 것 여섯(`AbsoluteSessionTimeoutFilter`·`InquiryFields`·`NotificationEventType`·`OrderDeadlines`·`MailOrderExemption`·`HttpTestBase`)·`coding-rules.md`(절 하나)·`quality-gates.md`. **세는 것을 기계에 맡겼더니 손 세기가 두 번 틀렸다** — `grep` 은 `EnumConstraintTest` 의 `PAIRS` 문자열을 참조로 셌고(오탐), 테스트 참조를 빼면 좁힐 수 없는 것을 좁히라고 시켰다. **파일을 갈랐다**: `ArchitectureTest` 는 `main` 만 보는데 이 규칙은 테스트도 소비자로 세야 해서, 한 파일에서 범위를 바꾸면 저쪽 순환 규칙이 가짜 순환을 문다. **예외 목록을 안 만들었다** — 스프링이 부르는 것은 `@Component` 메타 표시 한 줄로 걸린다. **`43a-26` 이 연 것을 이 청크가 도로 좁혔다**(`NotificationEventType`). **부쉈다**(`D25`): `OrderDeadlines` 를 다시 열어 빨간 것을 보고 되돌렸다. **축**: 규약(`D23`). **강제 지점**: **타입(1위)** | 완료 |
| 43a-21 | ~~열거형이 표의 행과 갈리는 것~~ | **완료.** `ActionPermissionTest.java`(신설)·`EnumConstraintTest.java`(EXEMPT 한 줄)·`quality-gates.md`. `FieldGroupTest` 와 같은 모양이다 — 표를 질의해서 코드의 이름이 거기 있나만 본다. **한쪽만 본다**: 표에만 있는 권한은 정상이라(`create`·`read`·`refund` 는 이 열거형 밖에서 쓴다) `isSubsetOf` 다. **부쉈다**(`D25`): `CANCEL` 의 권한을 `cancel_typo` 로 바꾸니 빨갛고 되돌리니 초록. **축**: 규약(`D23`). **강제 지점**: 테스트(4위) — `check` 가 아니라 데이터라 제약으로 못 내린다 | 완료 |
| ~~45~~ | ~~교환 처리~~ | **안 한다**(2026-08-23 마무리, 사용자 선택). `state-machines.md` 「안 다루는 것」이 이미 「교환은 반품 후 재주문으로 대신한다」고 정해 놨는데 청크만 살아 있었다 — 문서가 이긴다. **왜 안 하나**(`D23` 「안 넣은 것도 근거를 남긴다」): ① **전이가 두 배로 는다** — 반품 상태 여섯에 재출고 갈래가 얹힌다. ② **반품 + 재주문이 같은 결과를 낸다.** 소비자가 얻는 것이 같고 우리가 지는 의무도 같다. ③ **갈래가 넷에 하나씩 붙는다** — 반품(회수), 재고(두 번 움직인다), 정산(수수료를 반환하고 다시 뗀다), 배송비(재출고분을 누가 무나). 넷 다 이미 굴러가는 축이라 각각에 예외가 생긴다. **되살릴 조건**: 동일 상품 사이즈 교환처럼 **재주문이 어색한 경우**가 실제로 세어져서 나올 때 | 안 한다 |
| 46 | 리뷰 스키마 | 평점, 본문, 사진, 대상 주문 참조. **축**: `D22` 이름 규칙, `D3`. 표시광고법(후기) 요건은 `D2-8` 이 통독한 뒤 걸린다. **강제 지점**: 주문 항목 참조를 `not null` 로 — 산 것과 안 이어진 리뷰를 만들 수 없게 한다. **건드리는 자리**: 신설 리뷰 표. 주문 항목을 `not null` 로 가리키므로 `order_item` 을 읽는다 | 6, 11 |
| 47 | 리뷰 작성 자격 | 산 사람만 쓴다. 주문 이력이 권한 조건이 된다. **축**: `D14` — **권한 조건에 데이터가 들어오는 첫 자리다**. **강제 지점**: 주문 항목당 1건 유니크 + 판정. 앱에서만 세면 두 번 눌러 두 건이 된다. **건드리는 자리**: `permission_rule` 에 리뷰 작성 규칙과 판정 조건. 리뷰 입구는 `46` 이 만든 표를 쓴다 | 46, 4 |
| 48 | 셀러 답글·신고 | 셀러는 자기 상품 리뷰에만 답한다. 신고는 관리자가 본다. **축**: `D14` 스코프, `D7`(신고 상태), `D20`. **강제 지점**: 「자기 상품」을 판정 스코프로. 신고 처리는 전이표에 넣는다. **건드리는 자리**: `46` 의 리뷰 표에 답글·신고 칸 또는 신설 표, 그리고 셀러·관리자 입구 | 47 |
| 49 | 쿠폰 스키마 | 발급 조건, 사용 조건, 할인 방식, 부담 주체. **전자금융거래법을 `D2` 에 승격시킨다**(점검 A) — 쿠폰·포인트가 선불전자지급수단이 되면 충전·잔액·환불 의무가 붙는다. **축**: **법**(전자금융거래법 — 선불전자지급수단이 되면 충전·환불 의무가 붙는다, `D2` 승격), `D8`. **강제 지점**: 부담 주체와 할인 방식을 `check` 로 좁힌다 — 자유 문자열이면 정산이 사유별로 못 가른다. **건드리는 자리**: 신설 쿠폰 표 둘(정의·발급). 기존은 안 건드린다 | D8, D3, D2 |
| 50 | 쿠폰 적용 | 적용 순서와 중복 사용 규칙. 금액 계산이 여기서 복잡해진다. **축**: 프로젝트 규약 — 금액은 원 단위 정수(`D22`), 계산 순서를 한 곳에서만 정한다. **강제 지점**: 적용 결과를 주문에 박제한다(`V26` 과 같은 이유). 화면이 다시 계산하지 않는다. **건드리는 자리**: `OrderService` 의 금액 계산과 주문 항목 박제. 쿠폰 적용 결과를 주문에 박는다 — 화면이 다시 계산하지 않는다 | 49, 10-2 |
| 51 | 쿠폰 정산 반영 | 몰 부담과 셀러 부담이 정산에서 갈린다. **축**: `18` 이 세운 정산 규약. **강제 지점**: 부담 주체 컬럼 + `check`. 몰 부담과 셀러 부담을 앱에서 나누면 정산서와 안 맞는 자리가 생긴다. **건드리는 자리**: `17`·`18` 의 정산 항목에 부담 주체 칸. 쿠폰 표는 읽기만 한다 | 50, 18 |
| 52 | ~~재고 스키마~~ | **완료.** `V40__sku_stock.sql`·`SkuStockTest`(신설), `OrderService`·`OrderStatusService`·`CartService`·`ProductQuery`·`ProductService`·`DemoOrderSeeder`·`V902` 시드·테스트 20여 파일 수정, `concurrency-rules.md`·`domain-model.md`·`coding-rules.md` 갱신. **재고를 상품 정의에서 갈랐다**(사용자 선택) — `sku.stock_count` 가 상품 표에 얹혀 있어서 재고가 바뀔 때마다 `sku.updated_at` 이 밀렸고, 이동 이력(`53`)이 붙으면 진실이 두 곳이 될 자리였다. **진실은 여전히 카운터다** — 집계로 판정하면 음수를 막던 조건부 `UPDATE`(`D11`)가 경쟁에 약해진다. **가용 재고를 생성 컬럼으로 뒀다**(`on_hand - safety_stock`) — 앱이 빼면 화면·장바구니·주문이 각자 빼서 셋이 갈린다. 조건부 `UPDATE` 의 조건이 그 컬럼이라 안전 재고가 그냥 걸린다. **재고 행 없는 sku 를 지연 제약 트리거가 막는다** — 만들어 주는 트리거로 하면 수량 채우는 문장이 반드시 뒤따라야 해서 빠뜨릴 자리가 생기고, 확인만 하면 한 문장(CTE)으로 넣을 수 있다. **강제 지점 셋**: 지연 제약, `on_hand >= 0` check, 그리고 **`sku` 에 재고 이름이 다시 생기면 빨개지는 테스트** | 완료 |
| 53 | ~~재고 이동 이력~~ | **완료.** `V41__sku_stock_movement.sql`·`SkuStockMovementTest`(신설), `OrderService`·`OrderStatusService`·`SkuStockTest`·`OrderServiceTest`·`OrderStatusServiceTest`·`OrderConcurrencyTest`·`ProductQueryTest`·`concurrency-rules.md`·`data-lifecycle.md` 수정. **이력은 설명이지 진실이 아니다** — 팔 수 있나는 여전히 `sku_stock` 의 조건부 UPDATE 가 답한다(`52` 의 결정). **이력 없는 변동을 구조에서 없앴다**(사용자 선택) — 재고를 옮기는 일을 `move_stock()` 하나로 모으고 `on_hand` 직접 UPDATE 는 트리거가 거부한다. 함수가 조건부 UPDATE 와 이력 INSERT 를 같이 해서 한쪽만 도는 자리가 없고, **0행의 뜻을 `false` 로 그대로 지킨다**(`D11`) — 예외로 올리면 재고 부족이 진짜 고장과 같은 자리로 온다. **재고 행을 만들 때도 `initial` 한 줄이 남는다** — 안 남기면 합계가 처음부터 어긋나 「얼마부터 셌나」를 따로 알아야 한다. **주문 생성에서 재고 차감을 주문 뒤로 옮겼다** — 이력이 「어느 주문 때문인가」에 답하려면 그 시점에 주문이 있어야 하고, 다투는 행을 늦게 잡는 편이 잠금을 짧게 문다. **강제 지점 넷**: 사유 `check`, `quantity <> 0`, 직접 UPDATE 차단 트리거, 그리고 **이력 합계 = `on_hand` 대조 테스트** | 완료 |
| 53a | ~~재고 이동 함수의 예외 경로~~ | **완료 — 전제가 틀렸다.** `SkuStockMovementTest`(테스트 둘)·`stack.md`. **마이그레이션은 안 만들었다.** 계획은 「플래그가 켜진 채 남아서 세이브포인트로 삼키면 차단이 뚫린다」였는데 **돌려 보니 안 뚫린다** — `set_config(..., is_local := true)` 로 켠 값은 GUC 스택에 서브트랜잭션 단위로 쌓여서 그 구간이 abort 되면 켜기 전으로 돌아가고, **예외를 삼키려면 abort 가 반드시 먼저 온다**(Postgres 는 오류 뒤 세이브포인트로 안 되돌리면 그 트랜잭션에서 아무것도 못 한다). 세이브포인트 롤백과 plpgsql `exception when others` 두 갈래로 확인했고 둘 다 플래그가 빈 값에 직접 `UPDATE` 가 거부됐다. **그래서 `exception` 블록을 안 넣었다**(사용자 선택) — 막는 것이 없는데 plpgsql `exception` 블록은 암묵 세이브포인트라 재고 이동마다 서브트랜잭션이 하나씩 생긴다(`D23` 「안 넣은 것도 근거를 남긴다」). **처분은 정보다** — 플랫폼 사실이라 `stack.md` 「기억으로 쓰면 틀리는 자리」에 절을 세웠다. **근거를 `V41` 주석에 안 적었다** — 적용된 마이그레이션을 고치면 Flyway 체크섬이 어긋난다. 테스트 둘은 `PROPAGATION_NESTED` 로 삼킨 뒤 ① 직접 `UPDATE` 가 여전히 거부되는 것과 ② 정상 이동이 여전히 되는 것을 본다. 터뜨리는 방법은 `on_hand + Integer.MAX_VALUE` 로 int 를 넘기는 것이라 **플래그를 끄기 전에 `UPDATE` 문 자체가 터진다**. 원래 설명: `move_stock()` 이 세션 플래그를 켜고 `UPDATE` 뒤에 끄는데, **그 사이에서 던지면 켜진 채로 남는다** | 완료 |
| 54a | ~~알림 스키마와 파기~~ | **완료.** `V43__notification.sql`(신설)·`NotificationSchemaTest`(신설)·`TransactionPurgeService`·`TransactionPurgeBatch`·`data-lifecycle.md`·`TransactionPurgeServiceTest`. **`54` 를 둘로 갈랐다**(사용자 선택) — 한 청크로 치면 파일 일곱이라 청크 규칙의 두 배고, 끊겼을 때 남는 중간이 스키마다. **표가 셋인 이유는 수명이 셋이라서다**(`D18-1`): 템플릿 판은 안 지우고, 메타는 거래 5년·광고 6개월, 본문은 6개월. **강제 지점을 전부 제약으로 내렸다** — 대상별 **부분 유니크 셋**, `check` 여섯(종류·채널·상태·사건·대상 하나·성공↔시각·실패↔이유), 외래키 넷, 본문은 **기본키 공유로 1:1 강제**(`D22` 「1:1 로 붙는 확장 표」, `order_shipping` 선례). **분할표의 예보를 하나 고쳤다** — 유니크를 `(사건, 대상 셋)` 하나로 두면 **대상이 없는 알림**(광고·비밀번호 재설정)이 사건 하나로 유니크가 돼서 평생 한 번밖에 못 나간다. 대상별 부분 유니크면 `null` 함정도 안 생긴다(`batch_run` 성공 회차 선례). **종류를 판에서 박제한다** — 수명이 그 값에서 나오므로 판이 고쳐져도 이미 나간 것의 보존 기간이 안 움직인다(`order_item` 이 상품명을 박제한 것과 같다). 파기는 거래기록 파기 배치에 얹었다 | 완료 |
| 54b | ~~알림 발송기와 서비스~~ | **완료.** `MockNotificationSender`·`NotificationTemplates`·`NotificationService`·`NotificationSendTest`(전부 신설). **거래 통지만 세웠다**(사용자 선택) — 광고 경로는 `55` 가 맡고 `D18` 강제 지점 둘도 그리로 옮겨 적었다. **발송기는 인터페이스 없이 구체 `@Component` 하나다** — `MockPaymentGateway` 선례고, 갈아끼울 것이 실물 SMTP 하나뿐이라 「세어 보고 하나뿐이면 안 만든다」에 걸린다. `5c-1` 이 선례로 부르던 `CaptchaVerifier` 는 **코드에 없어서**(`D14` 의 설계일 뿐) 그 문장을 고쳤다. **변수는 `{{이름}}` 치환**(사용자 선택). **강제 지점 셋**: ① 안 채워진 자리가 남으면 던진다 — 환급 금액처럼 법이 요구하는 항목이 빈 채로 나가면 제18조제3항 단서를 안 지킨 것이 된다. **이력도 안 남긴다** — 보낼 수 없는 것을 「보내는 중」으로 두면 재시도가 그것을 집는다, ② 판은 **시행일**로 고른다(`D2-7` 함정) — 판 번호로 고르면 개정판을 미리 넣은 순간 시행 전 문안이 나간다, ③ 멱등은 `54a` 의 부분 유니크가 막고 서비스는 그 예외를 **「할 일 없음」으로 받는다**. **트랜잭션 경계에서 하나 배웠다** — `TransactionTemplate` 을 기본 `REQUIRED` 로 두면 중복 예외가 **부르는 쪽 트랜잭션까지 죽여서** 삼킬 수가 없다. `PROPAGATION_NESTED` 로 바꿨고, 그것이 되는 근거가 `53a` 가 `stack.md` 에 적어 둔 사실이다. 주소가 없는 계정(파기됨)은 `no_address` 실패로 **이력을 남긴다** — 왜 안 갔는지에 답할 자리가 있어야 한다 | 완료 |
| 55 | ~~광고 경로와 수신 동의~~ | **완료.** `AdvertisingGate`·`AdvertisingNotifications`·`AdvertisingGateTest`(전부 신설), `ConsentService`·`NotificationService`·`notification-rules.md`. **조문을 원문에서 확인했다** — 제50조 제1항(명시적 사전 동의)·제2항(거부·철회하면 전송 금지)·제3항(**오후 9시부터 그 다음 날 오전 8시까지** 별도 동의)·제4항(본문에 명칭·연락처·수신거부 조치). `D18` 의 21~08 이 맞았다. **`D18` 이 넘긴 강제 지점 둘을 구조로 닫았다** — 분기가 아니라 **입구를 갈라서**, `NotificationService` 가 `AdvertisingGate` 를 안 들어 거래 통지 코드에서 동의를 볼 방법이 없다(`D23` 축 2는 타입·구조가 1순위고 분기는 앱 검증 3순위다). **강제 지점 넷**: ① 입구 분리, ② 동의·야간을 관문 한 곳에, ③ **광고 판에 제50조제4항의 자리(`sender_name`·`sender_contact`·`unsubscribe`)가 없으면 거부** — 값이 아니라 **판**을 본다(값만 보면 판이 그 자리를 안 쓸 때 조용히 빠진다), ④ 거래 통지가 동의 없이도·철회 뒤에도 나가는 것을 테스트가 밟는다. **야간에 막힌 광고는 안 보낸다**(사용자 선택) — 미루려면 예약 칸과 배치가 드는데 광고를 보내는 청크가 아직 없다. **막히면 이력을 안 남긴다** — 그 표는 「보냈다」를 증명하는 자리다. **사본을 만들 뻔했다** — `ConsentService` 에 이미 `isGranted` 가 있고 `current_consent` 뷰가 「최신 행이 이긴다」를 들고 있어서, 새로 짜는 대신 `public` 으로 열었다. **드러난 것 둘은 신규 청크로 세웠다**(`55a`·`55b`) — 제7항(동의·거부·철회 처리 결과 통지)과 제8항(2년마다 수신동의 확인) | 완료 |
| 55a | ~~동의 처리 결과 통지~~ | **완료.** `V45__notification_consent_result.sql`(신설)·`NotificationSweeper`·`NotificationService`·`NotificationSweeperTest`. **시행령을 원문에서 확인했다** — 제62조의2 가 「의사를 표시한 날부터 **14일 이내**」에 ①전송자의 명칭 ②의사표시 사실과 표시한 날짜 ③처리 결과를 알리라고 한다. **광고성 정보를 실으면 안 된다** — 처리 결과에 혜택 안내를 얹으면 그것이 광고 전송이고, 철회한 사람이면 제50조제2항 위반이다. 판이 그것을 막는다. **대상 칸이 넷이 됐다** — 어느 의사표시에 대한 통지인지가 붙어야 14일을 잰다. `notification.user_consent_id` 를 더하고 `num_nonnulls(...) <= 1` 과 부분 유니크를 같이 세웠다. **광고 항목의 의사표시만 대상이다** — 제50조는 영리목적 광고성 정보에 걸린 의무라 필수 약관에는 안 걸린다. 전부 보내면 가입할 때마다 여러 통이 나간다. **사건 목록이 닫혀 있어서 마이그레이션 없이는 못 남긴다** — 그것이 강제 지점이다. 배치를 안 늘렸다 — 거래 통지 스위퍼에 다섯째 시점으로 얹었다 | 완료 |
| 55b | ~~수신동의 정기 확인~~ | **완료.** `V46__consent_reconfirm.sql`·`ConsentReconfirmSweeper`·`ConsentReconfirmSweeperTest`(전부 신설), `AdvertisingGate`·`application.yml`(풀 8)·`batch-catalog.md`. **시행령을 원문에서 확인했다** — 제62조의3 이 「수신동의를 받은 날부터 **2년마다**」 확인하고 ①전송자의 명칭 ②동의 사실과 동의한 날짜 ③유지·철회 의사표시 방법을 알리라고 한다. **함정은 「확인이 반복된다」였다** — `55a` 가 건 `(event_type, user_consent_id)` 유니크가 **두 번째 주기를 영영 막는다**. 처리 결과는 의사표시당 한 번이고 확인은 2년마다라 성격이 달라서, 유니크를 `consent_result` 에만 걸고 확인의 중복은 `user_consent.reconfirmed_at` 이 막게 했다. **그 사실은 2년 뒤에야 드러날 것이라 테스트가 두 주기를 실제로 밟는다.** **강제 지점은 관문이다** — 확인이 밀린 동의를 `AdvertisingGate` 가 거부한다(`NOT_RECONFIRMED`). 배치가 못 돌면 확인 없는 동의가 쌓이는데 관문이 안 보면 그 동안 광고가 그대로 나가고, 그때 걸리는 것은 테스트가 아니라 과태료다. **배치가 멈추면 안 나가는 쪽으로 기운다.** 확인 시각은 **보낸 뒤에** 남긴다 — 먼저 남기면 발송 실패 시 다음 확인이 2년 뒤가 된다. 배치가 여덟이 돼서 풀을 8로 올렸고 7로 되돌려 빨간불을 확인했다 | 완료 |
| 56 | ~~알림 트리거 배치~~ | **완료.** `V44__notification_template_seed.sql`·`NotificationSweeper`·`NotificationSweeperTest`(전부 신설), `application.yml`·`batch-catalog.md`. **`D2` R20 이 닫혔다** — 법이 네 시점에 요구하는 통지가 코드는 다 있는데 하나도 안 나가고 있었다. **스위퍼로 갔다**(사용자 선택) — 상태를 훑어 **통지가 없는 건**을 찾는다. `RefundSweeper` 와 같은 모양이고 **상태 전이 코드를 안 건드린다**. 전이 자리마다 발송을 심으면 그 자리 수만큼 빠뜨릴 곳이 생긴다. **기한 초과 판정을 다시 안 짰다** — `seller_order_visible.is_ship_overdue` 가 그 식을 들고 있다(`11c-2c`). 그 뷰가 `o.status = 'paid'` 로 걸러서 **안 낸 주문에는 공급 곤란 통지가 안 나간다** — 발송 기한을 따질 이유가 없어서 맞는 동작이다. **거슬러 보내는 창을 이레로 뒀다**(우리 판단) — 보내는 자리가 없던 동안 쌓인 주문에 지금 와서 보내면 통지가 아니라 무더기 발송이다. **멱등은 여기서 안 막는다** — `54a` 의 부분 유니크가 막고 스위퍼 조건은 대상을 줄이는 것뿐이다(조회와 발송 사이에 틈이 있다). **배치가 일곱이 됐고 `36b` 의 테스트가 그 자리에서 걸렸다** — 풀을 7로 올렸고, 6으로 되돌려 빨간불을 확인했다. 문안은 `V44` 가 1판으로 시드한다(존댓말. 사용자가 읽는 글이다) | 완료 |
| 56a | ~~스위퍼 창 밖으로 나간 통지 감시~~ | **완료 — 감시 대신 구멍을 없앴다**(사용자 선택). `NotificationSweeper`·`NotificationSweeperTest`·`batch-catalog.md`. **분할표 예보가 둘 다 안 맞았다.** ① 「회차를 `failed` 로 남긴다」고 적었는데 **`NotificationSweeper` 는 `fixedDelay` 라 회차 이력이 없다**(`RefundSweeper` 와 같다). ② 「창 밖 건을 감시한다」도 답이 아니었다 — 감시해 봐야 **그 건은 여전히 안 나가고 손으로 보낼 자리도 없다.** **창을 왜 뒀는지로 돌아갔다** — 막으려던 것은 「이레」가 아니라 **「보내는 자리가 없던 동안 쌓인 주문」**이고, 그것은 **기능이 언제 섰나**로 정확히 표현된다. 바닥을 `notification_template` 첫 판 시각으로 바꿨다. **그 뒤에 생긴 것은 배치가 얼마를 멈췄든 전부 잡히므로 감시할 구멍이 없어진다.** 밀린 것이 한 번에 나가도 각 통지가 서로 다른 주문 건이라 스팸이 아니고, **안 보내는 쪽이 위반이다.** 테스트를 둘로 갈랐다 — 기능이 서기 전 주문은 안 보내고, **아흔 날 멈췄다 돌아도 다 잡는다** | 완료 |
| 57 | 배송 추적 | 송장 번호, 택배사, 상태 조회 흉내. **기사·화물의 현재 위치를 다루기로 하면 위치정보법이 걸린다**(`D2`) — 배송지 주소는 안 걸리지만 실시간 위치는 개인위치정보다. 흉내만 낼지 여기서 정한다. **축**: **법**(위치정보법 — 실시간 위치를 다루면 걸린다, `D2`), `D7`. **강제 지점**: 송장 번호 형식을 `check` 로. 위치를 안 다루는 것 자체가 결정이라 스키마에 그 칸을 안 만든다. **건드리는 자리**: `seller_order` 에 송장 칸 또는 신설 배송 표. 화면은 주문 상세 | 11, D2 |
| 58 | ~~상품 Q&A 스키마~~ | **완료.** `V53__inquiry.sql`·`InquirySchemaTest`(둘 다 신설), `TransactionPurgeService`·`TransactionPurgeBatch`·`data-lifecycle.md` 수정. **법 요건 셋을 닫았다** — `R28`(개인정보법 제37조 처리정지·제38조제5항 이의제기), `R25`(전자상거래법 제20조제3항 불만·분쟁), `R34`(정보통신망법 제50조의7 광고 게시물 차단). 앞의 둘은 **받을 자리**가 없었고 셋째는 **내릴 수단**이 없었다. **표를 안 갈랐다** — 넷 다 「받아서 답한다」는 같은 수명을 살고 같은 상태를 지나서, 가르면 접수·답변·차단이 표마다 사본이 된다. 갈리는 것은 **무엇에 붙느냐**뿐이라 `inquiry_product_check` 가 그것만 가른다: **처리정지는 대상이 사람이라 상품 번호를 요구하면 상품을 안 산 사람이 권리를 못 쓴다.** **차단 사유를 열거로 닫았다** — `advertisement`(제50조의7)와 `abuse`(약관)를 값으로 갈라, 법이 근거인 것과 우리가 정한 것이 개정될 때 안 헷갈리게 했다. **보존 3년**(시행령 제6조 4호 소비자 불만·분쟁처리 기록)이고 **파기 코드를 같이 넣었다** — 「수집하는 코드가 파기하는 코드보다 먼저 나오면 그 사이가 위반 구간이다」. 기준은 **낸 날**이다: 답변일로 잡으면 답이 안 나간 문의가 영영 안 지워지는데, 그 상태가 오래 남는 것이 바로 우리가 답을 안 한 경우다. **제약이 느슨한 것을 테스트가 물었다** — 「상태 = (시각과 사유가 둘 다 있나)」로 쓰면 **사유만 매달아 둔 행이 통과한다**(한쪽이 비어 오른쪽이 거짓, 상태도 거짓이라 등식이 맞는다). 셋을 사슬로 묶었고 답변 쪽도 같은 구멍이 있어서 같이 조였다. **안 한 것 하나** — 처리정지·이의제기의 **법정 처리 기한을 안 박았다.** 제37조·제38조의 기한을 원문으로 확인 못 했고 `D2` 에도 없다. 기억으로 박으면 틀린 날짜가 제약이 된다 — `58-1` 로 세웠다 | 6, D2-8 |
| 58-1 | ~~법정 요구의 처리 기한~~ | **완료.** `V59__inquiry_due.sql`(신설), `InquiryService`·`InquiryQuery`·`InquiryVisibilityTest`·`InquirySchemaTest`·`commerce-compliance.md` 수정. **조문을 원문으로 봤다** — 법 제37조는 「**지체 없이**」라고만 하고 **일수가 조문에 없으며**, **시행령 제44조제2항**이 「처리정지 요구서를 **받은 날부터 10일 이내**」로 정한다(제41조제4항 열람·제43조제3항 정정도 같은 10일). **달력일이다** — 「영업일」이 안 적혀 있어서 `D10` 이 안 걸린다: `refund.due_at`(3영업일)과 갈리는 자리다. **접수하는 순간 박제한다** — 답한 날에서 세면 늦게 답할수록 기한이 밀려 「늦었다」가 성립을 안 한다. **기한을 못 찾은 것도 근거를 남겼다**(`D23`) — 제38조제5항(이의제기)과 전자상거래법 제20조(분쟁)는 일수를 못 찾았고 후자는 「신속히」다. **모르는 것을 지어서 박느니 비워 둔다**: 틀린 날짜가 박히면 그것이 지켜야 할 값이 된다. **「기한 안에 답」은 `check` 로 못 건다** — 막으면 늦은 답이 영영 안 나간다. 강제는 **「넘긴 것이 조회로 드러난다」가 천장**이고(`Entry.overdue`) 그 위는 사람이 본다(`R5` 와 같은 자리). **조문 번호 충돌도 풀었다** — 원문을 보니 **제20조의3 은 「통신판매의 중요한 일부 업무를 수행하는 통신판매중개업자의 책임」**이고 불만·분쟁과 무관하다. 없어진 `23` 행이 틀렸고 `R25`(제20조제3항)가 맞다. **그 조사가 새 요건을 하나 물었다** — 제20조의3 은 **청약 접수를 받거나 대금을 지급받는 중개업자**에게 제7조·제8조·제13조제2항제5호·제14조제1항을 **대신 이행**하게 하는데 **우리가 둘 다 한다**(`R5` 에서 「대금을 받은 자」로 이미 정했다). `D2-9` 로 세웠다 | 58 |
| 58-2 | ~~주문에 붙는 문의~~ | **완료.** `V61__inquiry_order.sql`·`orders/[orderNumber]/order-inquiry-form.tsx`(둘 다 신설), `InquiryService`·`InquiryQuery`·`InquiryController`·`InquiryVisibilityTest`·`me/inquiries/page.tsx`·`orders/[orderNumber]/page.tsx` 수정. **`dispute` 로 대신하면 안 되는 이유가 이 청크의 근거다** — 그 표는 시행령 제6조 4호의 **불만·분쟁처리 기록**(3년)이라 단순 문의까지 쌓이면 「분쟁이 몇 건이었나」에 답을 못 한다. **`inquiry_target_check` 가 종류와 대상을 묶는다** — 상품 문의는 상품에, 주문 문의는 묶음에, 계정에 붙는 셋은 아무 데도. **공개가 성립하지 않는다** — `V54` 의 비공개 강제가 그대로 걸려서 조건을 안 바꿔도 막힌다. **없는 묶음과 남의 묶음이 같은 404 다** — 갈라 주면 묶음 번호를 훑어 실재하는 주문의 지도를 그릴 수 있다. **드러난 것 하나** — 셀러 목록이 **상품의 셀러만** 보고 있었다: 주문 문의는 셀러가 묶음에서 나와서 그대로 두면 **셀러가 자기 주문에 온 질문을 못 본다.** `coalesce` 로 둘 다 보게 했다 | 58, 59 |
| 59 | ~~Q&A 공개·비공개~~ | **완료.** `V54__inquiry_visibility.sql`·`InquiryService`·`InquiryQuery`·`InquiryController`·`InquiryVisibilityTest`(다섯 다 신설), `ErrorCode`·`SecurityConfig` 수정. **`58` 이 표만 세워서 `R25`·`R28` 의 법정 창구가 아직 방침 문구였다** — 입구가 서면서 「어디로 받나」에 코드가 답한다. **목록을 관객별로 갈랐다**(사용자 선택): `GET /api/products/{id}/inquiries`(공개분, 비로그인)·`/api/me/inquiries`(내가 낸 것)·`/api/seller/inquiries`(내 셀러 상품). 하나로 두고 로그인 상태로 행을 거르면 **응답 record 가 하나가 되고 비공개 본문을 담을 칸이 상시 존재한다** — 규칙 한 줄을 빠뜨리면 남의 비공개가 그대로 나간다. 갈라 두면 조건이 **「숨기는 것」이 아니라 「고르는 것」**이 돼서 빠뜨렸을 때 **결과가 더 나오는 게 아니라 빈다.** `PublicEntry` 에는 **낸 사람을 실을 칸이 아예 없고** 테스트가 그것을 리플렉션으로 못박는다. **기본값을 층마다 다르게 뒀다** — `is_public` 의 DB 기본값은 **비공개**고 API 는 상품 Q&A 를 공개로 본다. 처음엔 DB 도 공개로 뒀다가 **`58` 의 스키마 테스트 둘이 물었다**: 계정 요구를 그냥 넣으면 `inquiry_visibility_check` 에 걸리고, 그때 고치는 사람이 「`true` 로 넣으면 되나」를 먼저 시도하게 된다. **아래층은 안전한 쪽으로, 위층은 쓰기 좋은 쪽으로** 두고 근거를 마이그레이션에 적었다. **`R34` 가 목록 조건으로 내려갔다** — 내려간 게시물이 공개 목록에서 빠지고 **작성자에게는 그대로 보인다**(제50조의7 은 게시 중단을 요구하지 작성자에게서 감추라고 하지 않는다). **권한 자원을 새로 팠다** — `product:read` 에 얹으면 계정에 붙는 요구가 상품 권한으로 열리는데, 그 요구는 상품을 안 산 사람도 낸다. 마이그레이션에 **감사자 deny 검증**과 **셀러가 `seller` 아닌 범위로 열렸나 검증**을 `do` 블록으로 넣었다 | 58, 4 |
| 59-1 | ~~문의 화면~~ | **완료.** `V60__inquiry_public_index.sql`·`/me/inquiries`(`page`·`inquiry-form`·`withdraw-button`)·`/seller/inquiries`(`page`·`answer-form`)·`products/[productId]/product-inquiries`·`ask-form`(전부 신설), `InquiryService`·`InquiryController`·`InquiryQuery`·`InquiryVisibilityTest`·`me/page.tsx`·`products/[productId]/page.tsx`·`site-header.tsx`·`state-machines.md`·`commerce-compliance.md` 수정. **`24` 를 흡수했다**(점검 G 가 중복으로 닫아 둔 행). **`R28` 이 여기서 끝났다** — 제38조제4항이 「열람등요구의 방법과 절차는 **수집보다 어렵지 아니하도록**」인데 수집은 `/signup` **화면**이고 이쪽은 **API 뿐**이었다. `/me` 의 「권리 행사와 탈퇴」 절이 그 화면을 가리켜 **셸의 머리에서 두 번 눌러 닿는다**(`13e` 와 같은 판단). **처리 기한을 화면이 말한다**(`58-1`) — 넘긴 건은 붉게 뜬다: 「넘긴 것이 조회로 드러난다」가 천장이라 그 사실이 **사람에게 닿는 자리**가 여기다. **`withdrawn` 전이도 닫았다** — 대상을 **낸 사람으로 좁혔다**: 문의 그대로 판정하면 셀러가 실려 `seller` 스코프가 걸리고 **셀러가 불리한 질문을 지우는 자리**가 된다. **거두기를 만들자 진짜 버그가 드러났다** — 공개 목록이 `blocked` 만 빼고 있어서 **거둔 질문이 남에게 그대로 보였다.** `withdrawn` 이 그때 **도달 불가능해서 안 드러난** 것이다. 조건을 **「뺄 상태」에서 「보일 상태」로 뒤집었다**(`V60`): 「이 상태만 뺀다」면 상태가 늘 때마다 그 자리를 고쳐야 하고 빠뜨리면 **안 보여야 할 것이 보이는 쪽으로 샌다.** **검증**: `./gradlew build`·`npm run build`·`lint`·`test` 전부 통과 | 59, 13e |
| 59-2 | ~~광고 게시물을 내리는 수단~~ | **완료.** `V58__inquiry_block_permission.sql`(신설), `InquiryService`·`InquiryController`·`InquiryVisibilityTest`·`commerce-compliance.md`·`state-machines.md` 수정. **`58` 이 만든 것은 「자리」였고 「수단」이 아니었다** — `blocked` 로 옮기는 코드가 없어 `psql` 로만 내려졌고, 요건표에는 「내릴 수단이 생겼다」고 적혀 있었다(1차 마무리가 잡았다). **관리자만이다**(사용자 선택) — 조문의 의무자가 운영자라 그 판단도 운영자가 한다. 셀러에게 열면 **불리한 질문을 광고로 몰아 내리는 자리**가 같이 생기고, 내린 근거가 `advertisement` 로만 남아 **「광고였다」와 「불리해서 내렸다」가 같은 값**이 된다. 마이그레이션의 `do` 블록이 관리자 밖으로 나갔는지 검사한다. **제약 하나를 고쳤다** — `58` 이 `inquiry_answer_check` 에 「`status='answered'` 인 것과 `answered_at` 이 있는 것은 같다」를 박아 놔서 **답이 나간 글을 못 내렸다.** 광고에 답을 달았다고 그 광고가 남을 이유가 없고, **`answered_at` 은 사건 시각이지 상태 표현이 아니다** — 방향을 하나만 걸게 바꿨다(답변 상태면 답이 있다. 답이 있다고 답변 상태인 것은 아니다). **테스트가 보는 것을 바꿨다** — 「내린 글이 목록에서 빠지는 것」이 아니라 **「내리는 경로가 있는 것」**이다. 앞은 `59` 가 이미 고정했고 그 테스트가 **SQL 로 상태를 밀어 넣고 시작해서** 이 구멍이 초록인 채로 남았다 | 59, D2-8 |
| 60 | 검색 인덱스 | Postgres 전문검색, 형태소 처리의 한계. **축**: 표준 — Postgres 전문검색의 한계(한국어 형태소), `D21`. **강제 지점**: 인덱스와 조회 방식을 한 곳에 두고 화면이 직접 SQL 을 안 짜게 한다. **건드리는 자리**: 신설 인덱스 마이그레이션과 검색 조회기. 기존 상품 조회는 안 건드린다 **묶음 검토(2026-08-23)**: `61`(검색 API)과 **안 묶는다.** 묶여서가 아니라 **지금 못 쳐서**다 — 선행 `D21`(성능 목표)이 미착수고 그 `D21` 의 선행이 `42`(성능 측정·튜닝)라 사슬이 `42 → D21 → 60 → 61` 로 넷이다. **먼저 할 일은 묶는 것이 아니라 `42` 를 가르는 것**이다: 측정과 튜닝은 다른 일이고, `D21` 이 「측정값이 없어서 못 쓴다」로 막혀 있으니 **측정만 떼어 내면 그 사슬이 풀린다** | D21, 6 |
| 61 | 검색 API | 페이징·정렬·필터 규약을 따른다. 스코프가 섞인다. **축**: `D5` 목록 규약(페이징·정렬), `D14` 스코프. **강제 지점**: 목록 규약을 지키는지 응답 계약 테스트로 — 검색만 다른 모양이면 화면이 두 벌이 된다. **건드리는 자리**: 신설 검색 입구. 수정은 상품 목록 화면의 검색창 | 60, D5 |
| 62 | ~~지표 노출~~ | Actuator 지표를 Prometheus 형식으로. **축**: `D16` 관측 규약, 표준(Prometheus 노출 형식). **강제 지점**: 지표 이름을 `D16` 규약으로 고정하고 그것을 테스트로 — 이름이 갈리면 대시보드가 조용히 빈다. **건드리는 자리**: `build.gradle.kts` 의존성과 Actuator 설정. **`Q53` 이 이미 열었다**(2026-09-16) — 노출·권한·지표 셋·이름 규약이 그 청크에 있다. 이 행에 남은 것은 **지표를 더 세우는 것**이고, 그때 `observability-rules.md` 「지표」의 이름·태그 규칙을 따른다  **완료**(2026-09-17). `ProblemFactory`·`MetricsExposureTest`·`observability-rules.md`. **남은 것이 오류율이었다** — `Q53` 이 노출·권한·지표 셋을 열었는데 청크 63 이 그리려는 그래프 둘 중 「오류율」의 축이 없었다. **사용자가 (나) 를 골랐다**(오류 코드 축, `shop.error.raised{code}`). (가)(스프링의 `http.server.requests`)는 **이미 나오고 있는 것을 실측으로 확인했고** 태그도 경로 템플릿이라 안 터진다 — 버린 이유는 **「왜 났나」가 안 보여서**다. 이 저장소가 묻고 싶은 것은 「어느 규칙이 얼마나 걸리나」고 `ErrorCode` 가 닫힌 목록(62종)이라 그 축이 싸다. 둘은 **같이 쓴다** — 63 이 두 패널을 다 그린다. **세는 자리가 `ProblemFactory.create` 하나다**: 본문을 만드는 곳이 하나라 세는 곳도 하나고, **예외 처리기에만 붙이면 401 이 안 세어진다**(보안 필터가 먼저 끊는다). **강제 지점은 테스트**다 — 이름이 갈리면 대시보드가 **조용히 빈다**(빈 패널과 오류 0이 화면에서 같아 보인다). **부순 증거**: 지표 이름을 `shop.errors.raised` 로 바꾸니 빨갛다| 완료 |
| 63 | 대시보드 | Grafana 컨테이너, 판정 지연과 오류율 그래프. **축**: `D16` 관측 규칙, `D21` 성능 목표. **강제 지점**: 없다 — **`D21` 을 쓸 근거(측정값)가 여기서 처음 생긴다**. **건드리는 자리**: `docker-compose.yml` 에 Grafana 와 대시보드 정의 파일. 앱 코드는 안 건드린다 | 62 |
| 64 | ~~백업·복구 리허설~~ | 덤프하고 날리고 되살린다. 마이그레이션만으론 데이터가 안 돌아온다. **축**: `D13` 데이터 수명, `stack.md`(Postgres 판). **강제 지점**: 되살린 DB 로 **앱을 띄우는 것 자체** — 「덤프가 있다」와 「되살아난다」는 다른 말이다(2026-09-18 정정 — 처음엔 「테스트를 돌린다」고 적었는데 느린 레인은 Testcontainers 라 이 DB 를 안 쓴다. 실제로 잰 것은 기동이고 Flyway 검증이 그 안에 있다). **건드리는 자리**: 신설 덤프·복구 스크립트와 `stack.md` 절. 앱 코드는 안 건드린다. **`Q39` 를 흡수한 몫이 하나 있다**(`Q85`, 2026-09-17): 같은 덤프가 **호스팅에 데모 데이터를 붓는 도구를 겸한다** — 기본 프로필이 `db/seed` 를 안 읽어서 그냥 올리면 빈 쇼핑몰이 뜬다. 그래서 스크립트는 **대상 DB 를 인자로 받는다** — 로컬로만 되살리게 짜면 배포에서 다시 쓴다. **닫힘**: 덤프 → 스키마를 날린다 → 복구 → **행 수가 같고 앱이 그 DB 로 뜬다**. 「덤프가 있다」로는 안 닫힌다. **완료**(2026-09-18) — `scripts/db-dump.sh`·`scripts/db-restore.sh`, 둘 다 **대상 DB 를 인자로 받는다**(`Q39` 가 그 인자로 호스팅을 준다). **실측**: 상품 5·계정 7·셀러 2·이력 79 를 뜨고 `drop schema public cascade` 로 날린 뒤 되부으니 넷이 그대로였고, 앱이 **79개를 검증하고 「마이그레이션 불필요」로 떴다**. **그 전에 한 번 실패했다** — 오래된 덤프는 `V70` 체크섬에서 막혔고, 덤프가 깨진 것이 아니라 **배포 전 마이그레이션이 가변이라 코드가 그 사이에 움직인 것**이었다(`stack.md` 에 절을 세웠다) | D13 |
| 65 | ~~마이그레이션 롤백~~ | Flyway 무료판에 undo 가 없다. 되돌리는 마이그레이션을 손으로 쓴다. **축**: `stack.md` 의 Flyway 절(로컬은 `down -v`, 배포는 다르다). **강제 지점**: 되돌리는 파일이 있는지 자체를 테스트로 건다. **건드리는 자리**: 신설 되돌리는 마이그레이션 파일들과 그 존재를 확인하는 테스트. 기존 마이그레이션은 안 고친다 — 적용된 것을 고치면 체크섬이 깨진다. **닫힘**: 되돌리는 파일이 **있어야 할 `V` 마다 있는지**를 시험이 세고(없으면 빨갛다), **완료**(2026-09-18) — `db/undo/U<번호>__*.sql` 와 `MigrationUndoTest`. **경계가 `V78` 이다** — 그 앞 일흔일곱을 소급해 쓰면 하루가 가고 아무도 안 읽는다. 여기서 막는 것은 **새로 더하는 것이 짝 없이 들어오는 일**이고 그래서 0 기준이다. **실제로 되돌려 봤다** — `U79`·`U78` 이 돌아 표 둘이 사라졌지만 **다시 올릴 때 막혔다**: 시드가 `V900+` 라 로컬 DB 에는 늘 더 높은 번호가 적용돼 있어서, 앞 번호를 비우면 Flyway 가 `Detected resolved migration not applied to database: 78` 로 거부한다. **되돌리기는 맨 뒤부터만 된다**(`stack.md` 에 절을 세웠다). **부순 증거**: `U79` 를 치우니 시험이 빨개졌다 | 0 |
| 66 | ERD 자동 생성 | 스키마에서 그림을 뽑는다. 손으로 그리면 어긋난다. **축**: `D4` 도메인 모델, 업계 관례(SchemaSpy·mermaid 중 고른다). **강제 지점**: 그림을 스키마에서 뽑는다 — 손으로 그리면 다음 마이그레이션에서 바로 어긋난다. **건드리는 자리**: 신설 생성 스크립트와 산출물 경로. `doc/` 에 그림이 들어갈 자리 | D4 |
| 67 | ADR 회고 | 지금까지의 ADR 을 다시 읽고 틀린 것에 `상태: 폐기` 를 붙인다. **축**: `D1` 문서 트리 규약. **강제 지점**: 없다 — 문서 청크다. **폐기한 ADR 을 지우지 않는다**(이력은 그때의 사실이다). **건드리는 자리**: `doc/adr/` 의 기존 ADR 들. 폐기 표시만 더하고 본문은 안 고친다 — 그때의 판단이 기록이다 | 0 |
| 68 | 장애 주입 | DB 를 죽이고 앱이 어떻게 죽는지 본다. 헬스체크가 잡나. **축**: `D16`(헬스체크·로그), `stack.md`. **강제 지점**: 없다 — 실험이다. 나온 것은 정보로 `stack.md` 에 적거나 청크로 세운다. **건드리는 자리**: 실행 절차만. 코드는 결과에 따라 | D16 |
| 69 | 변이 테스트 | 코드를 일부러 망가뜨려 테스트가 잡는지 본다. **`2m` 의 규칙을 4위로 내리는 자리다** — 「게이트는 부순 증거가 있어야 닫힌다」를 지금은 사람이 읽어야 걸린다(5위). 변이 테스트는 **부수는 것을 기계가 한다.** **축**: `D15` 테스트 전략. **강제 지점**: 살아남은 변이를 목록으로 남기고 그 자리에 테스트를 더한다 — 커버리지 수치가 아니라 「무엇을 못 잡나」가 산출물이다. **건드리는 자리**: `build.gradle.kts`(PIT 같은 도구)와 약한 자리의 테스트 | D15, 4c |
| 70 | 부하 테스트 | 데이터 10만 건에서 목록 조회가 어떻게 되나. **축**: `D21` 성능 목표(그 수치가 판정 기준), `D15`. **강제 지점**: 목표를 넘기면 실패하는 기준선 — 수치 없이 돌리면 결과를 읽는 사람마다 다르게 읽는다. **건드리는 자리**: 신설 부하 스크립트와 시드 확대. 앱 코드는 결과에 따라 | D21, 42 |
| 71 | ~~Rate limiting~~ | 로그인 시도 제한을 일반화한다. **축**: `D14` 보안 기준선, 표준(RFC 6585 — 429 와 `Retry-After`). **강제 지점**: 제한을 필터 한 곳에 두고 상태 코드·헤더를 응답 계약 테스트로 고정한다. **건드리는 자리**: 신설 제한 필터. **닫힘**: 제한을 넘긴 요청이 **429 와 `Retry-After` 헤더**를 내고(RFC 6585), 그것을 응답 계약 시험이 고정한다. **완료**(2026-09-18) — `RateLimitFilter` 가 **주소 단위**로 센다(인증 경로 분당 20 · 나머지 120). **`5c` 를 흡수 안 했다** — 그쪽은 **한 계정**의 실패를 세고 이쪽은 **한 주소**의 요청 전부를 센다. 계정을 바꿔 가며 두드리는 것은 `5c` 가 못 막고 이쪽이 막으므로, 합치면 둘 중 하나를 잃는다. **Redis 가 죽으면 통과시킨다** — `5c` 와 반대 방향이고, 가르는 것은 「막는 것과 여는 것 중 무엇이 사고인가」다. 로그인 잠금이 풀리면 무차별 대입이 열리지만 요청 제한이 풀리면 좀 붐빌 뿐이고, 여기서 막으면 **Redis 장애가 전면 장애가 된다**. **부순 증거**: 상한을 10만으로 올리니 시험이 빨개졌다 | D14, 5c |
| 72 | API 버전 정책 | v1 과 v2 를 어떻게 공존시키나. **축**: `D5` 의 버전 절이 **이미 정해 둔 것**을 따른다 — 지금은 안 붙이고 붙일 때 `/api/v2` 를 더한다. **강제 지점**: 없다 — 문서다. 호환을 깨는 변경의 기준(Zalando)을 여기서 확정한다. **건드리는 자리**: `api-guidelines.md` 의 버전 절 | D5 |
| 73 | 버전 공존 구현 | 같은 자원에 두 응답 모양을 유지한다. **축**: `D5` API 규약, RFC 9457(타입 URI 는 안 바뀐다). **강제 지점**: 두 모양을 **같은 테스트가 돌게** 한다 — 한쪽만 도는 순간 다른 쪽이 조용히 깨진다. **건드리는 자리**: 컨트롤러의 경로와 응답 조립. `D5` 가 정한 대로 기존 경로를 v1 으로 남기고 v2 를 더한다 | 72 |
| 74 | 판정 엔진 재구현 (1) | Spring Security `AuthorizationManager` 로 같은 명세를 짠다. **축**: `D6` 권한 모델(같은 명세를 다른 수단으로), 업계 관례(Spring Security `AuthorizationManager`). **강제 지점**: `4c` 의 권한 매트릭스를 **두 구현에 같이** 돌린다 — 그것이 「같은 명세」의 정의다. **건드리는 자리**: 신설 두 번째 판정 구현. 기존 판정 엔진은 안 건드린다 | D6, 4 |
| 75 | 판정 엔진 비교 | 두 구현을 같은 매트릭스로 검증하고 차이를 ADR 로 남긴다. **축**: `D15` 테스트 전략. **강제 지점**: `4c` 의 권한 매트릭스 회귀 테스트를 **두 구현에 같이** 돌린다. 그것이 비교의 정의다. **건드리는 자리**: `4c` 의 권한 매트릭스 테스트를 두 구현에 돌리게 고친다. 차이는 ADR 로 남긴다 | 74, 4c |
| 76 | GraphQL 재구현 | 필드 단위 권한(4d)이 GraphQL 에서 어떻게 달라지나. **축**: `D5`(스키마가 응답 계약을 대신한다), `D6` 필드 단위 권한. **강제 지점**: 필드 판정을 리졸버가 아니라 판정 엔진에 두는 것 — 리졸버마다 걸면 새 필드가 조용히 열린다. **건드리는 자리**: 신설 GraphQL 스키마와 리졸버. `4d` 의 필드 그룹을 읽기만 한다 | D5, 4d |

## 이 계획을 고칠 때

이 파일은 확정문서가 아니다. 청크를 해 보고 설계가 틀렸다고 판단되면 여기를 고치고,
왜 바꿨는지를 `doc/adr/` 에 남긴다. 계획을 지키는 것보다 틀린 걸 아는 게 낫다.
