 **`R34` 가 여기서 닫힌다**(`D2-8`) — 정보통신망법 제50조의7 은 광고를 **보내는 쪽**이 아니라 게시판 **운영자 쪽**에도 자리를 만든다: 광고성 게시물을 운영자가 거부하면 게시가 중단돼야 한다. Q&A 가 열리는 순간 그 자리가 광고판이 되고, 지금은 내릴 수단이 없다.# 계획

## 무엇을 만드나

멀티 셀러 쇼핑몰. 판매자가 여럿 입점하고, 고객이 사고, 관리자가 관리한다.

만드는 목적은 물건을 파는 게 아니라 **권한 체계를 정갈하게 짜는 법을 익히는 것**이다.
실무에서 관리자·사용자 구분이 코드 여기저기 흩어져 유지보수가 힘들어지는 걸 겪었고,
그게 어디서부터 잘못되는지를 정면으로 다루려고 만든다.

그래서 판단이 갈릴 때는 **기능을 늘리는 쪽이 아니라 구조가 드러나는 쪽**을 고른다.
할인쿠폰 10종류보다 권한 판정 한 군데를 제대로 만드는 게 이 프로젝트의 목적에 맞다.

## 확정 사항

사용자가 직접 고른 것들이다.

| 항목 | 결정 | 이유 |
|---|---|---|
| 종류 | 쇼핑몰 (가격비교 아님) | 권한·역할 설계 문제는 쇼핑몰 쪽에만 있다. 가격비교는 권한 축이 얕다 |
| 판매 구조 | 멀티 셀러 마켓플레이스 | 판매자가 여럿이어야 "자기 상품만 수정" 같은 행 단위 권한이 필요해진다. 실무에서 지저분해지는 지점이 정확히 여기다 |
| 권한 모델 | RBAC + 리소스 스코프 | 역할과 권한을 테이블로 분리하고, 거기에 `own`/`seller`/`all` 같은 행 단위 조건을 더한다 |
| 스택 | Next.js + Spring Boot | Spring Security가 RBAC와 메서드 단위 권한 판정을 정석으로 지원한다. 실무 Java와 직결된다 |
| 결제·배포 | 가짜 결제, 로컬 실행만 | PG를 흉내내는 모의 모듈을 만든다. 상태 전이·멱등성 설계는 그대로 연습되고 사업자등록·심사를 안 거친다 |

## 내가 대신 정한 것

쇼핑몰 구조를 익히려고 만드는 프로젝트라서, 구조를 알아야 답할 수 있는 질문은 묻지 않고 정했다.
근거를 같이 적어 둔다. **아니라고 판단되면 뒤집어도 된다.** 코드가 아직 없어서 뒤집는 비용이 거의 없다.

### DB는 PostgreSQL, Docker Compose로 띄운다

Oracle이 실무 환경이지만 로컬에 올리는 비용이 크고, 이 프로젝트에서 배울 것은 SQL 방언이 아니라 스키마 설계다.
Postgres는 컨테이너 한 줄로 뜨고 Spring Boot 기본 지원이 두텁다.

Oracle 문법 연습이 목적에 포함된다면 뒤집을 만하다.

### 인증은 세션 쿠키, JWT 아님

JWT가 유행이지만 쇼핑몰에는 안 맞는 구석이 있다. 토큰을 서버가 회수할 수 없어서
"판매자 권한 박탈" 같은 조작이 즉시 반영되지 않는다. 권한 체계가 주제인 프로젝트에서 이건 치명적이다.

`HttpOnly` + `SameSite` 쿠키에 세션을 담고 서버가 상태를 쥔다. 로그아웃과 권한 회수가 즉시 먹는다.
프론트와 백엔드가 포트가 달라 생기는 CORS 문제는 Next.js rewrite 프록시로 같은 출처로 만들어 피한다.

### 상품은 Product / ProductOption / Sku 세 층으로 나눈다

초보 설계가 흔히 틀리는 지점이다. 상품 테이블 하나에 재고 컬럼을 두면
"검정 M은 품절인데 흰색 L은 남음" 을 표현할 수 없다.

- `Product` — 고객에게 하나로 보이는 상품. 이름, 설명, 판매자
- `ProductOption` — 선택 축. 색상, 사이즈
- `Sku` — 옵션 조합 하나. **재고와 가격은 여기에 붙는다**

주문은 `Product`가 아니라 `Sku`를 가리킨다.

### 주문에는 상태머신을 명시하고, 전이는 한 군데서만 한다

`status` 컬럼을 두고 여기저기서 갈아끼우면 반드시 깨진다.
허용된 전이를 코드에 표로 선언하고, 그 표를 거치지 않는 상태 변경은 막는다.

```
결제대기 → 결제완료 → 배송준비 → 배송중 → 배송완료 → 구매확정
   │          │           │
   └ 취소     └ 취소       └ 취소
                                      배송완료 → 반품요청 → 반품완료
```

### 주문 시각의 값은 복사해서 박제한다

주문 항목에 `sku_id`만 두고 가격을 조인해 오면, 판매자가 가격을 바꿨을 때 **과거 주문 금액이 같이 바뀐다.**
돈이 걸린 데이터는 주문 시점의 상품명·가격을 주문 항목에 복사해 넣는다.

### 결정 기록은 `doc/adr/` 에 남긴다

번호 붙인 마크다운 한 장에 결정 하나. 무엇을 정했고, 무엇과 저울질했고, 왜 그걸 골랐는지.
나중의 내가 "왜 이렇게 했지" 를 코드에서 역추적하지 않게 하려는 것이다. 유지보수가 주제인 프로젝트라 이것도 산출물이다.

## 권한 모델

이 프로젝트의 중심이라 따로 적는다.

### 왜 역할 enum으로는 부족한가

`ROLE_ADMIN` / `ROLE_SELLER` 같은 상수를 코드에 박으면 두 가지가 무너진다.

1. **역할이 늘 때마다 코드를 고치고 재배포해야 한다.** CS 담당, 정산 담당, 읽기전용 감사자가 생길 때마다 전부 손댄다
2. **"자기 것만" 을 표현할 수 없다.** 판매자 A와 판매자 B는 역할이 같지만 볼 수 있는 주문이 다르다.
   이걸 역할로 못 푸니 결국 서비스 코드마다 `if (order.sellerId != me.id) throw ...` 가 흩어진다. 실무에서 지저분해지는 원인이 이것이다

### 구조

```
user ─< user_role >─ role ─< role_permission >─ permission
                                                    │
                                          resource + action
                                          (order:read, product:update)

+ 스코프 판정
  order:read  × scope=own    → 자기 주문만
  order:read  × scope=seller → 자기 상품이 들어간 주문
  order:read  × scope=all    → 전체
```

권한은 코드가 아니라 데이터다. 새 역할은 행을 넣어서 만들고, 배포하지 않는다.

스코프는 두 자리에서 쓰인다. 단건 조회는 "이 행에 접근할 수 있나" 를 판정하고,
목록 조회는 애초에 조회 조건에 스코프를 섞어 넣는다. 후자를 빠뜨리면 목록에서 남의 데이터가 새어 나간다.

### 판정을 흔드는 축 네 개

역할 세 개 × 스코프 한 겹으로는 실무에서 권한이 무너지는 지점을 재현하지 못한다.
아래 네 축을 넣어서 판정 엔진이 실제로 어려워지게 만든다. 바깥 모델에서 가져온 근거는 `doc/reference/permission-models.md` 에 있다.

**1. 셀러는 사람이 아니라 조직이다**

한 셀러에 계정이 여럿 붙는다. 대표, 상품 담당, CS 담당, 정산 담당이 같은 셀러 안에서 하는 일이 다르다.
그래서 역할 부여에 대상이 붙는다. "이 사람은 CS 담당" 이 아니라 **"이 사람은 A셀러의 CS 담당"** 이다.
GitHub가 저장소별로 역할을 붙이는 방식과 같다. `scope=seller` 는 이 구조가 생겨야 판정할 것이 생긴다.

**2. 거부가 허용을 이긴다**

허용만 쌓으면 "정산 담당인데 이 셀러만 제외" 를 표현할 수 없다.
`role_permission` 에 allow/deny를 두고, 같은 권한이 양쪽으로 잡히면 deny가 이긴다.
스코프는 넓은 쪽이 이기고(`all` > `seller` > `own`) 효과는 좁은 쪽이 이긴다. 두 규칙의 방향이 반대라는 점이 판정의 핵심이다.

**3. 자원의 상태가 권한을 깎는다**

배송이 시작된 뒤에는 판매자도 주문을 못 고친다. 역할이 그대로여도 자원의 상태가 바뀌면 할 수 있는 일이 줄어든다.
역할·스코프 판정을 통과한 다음에 상태 조건을 한 번 더 본다. 주문 상태머신과 권한이 만나는 자리다.

**4. 권한 회수는 즉시 먹어야 한다**

세션 쿠키를 고른 이유가 즉시 회수인데, 판정 결과를 캐시하는 순간 그 근거가 깨진다.
캐시를 넣고, 역할이 바뀔 때 무효화가 실제로 동작하는지 테스트로 못박는다.

여기에 판정의 흔적을 남기는 감사 로그, 요청자와 승인자를 가르는 환불 승인, 관리자가 사용자 시점으로 보는 임퍼소네이션이 붙는다.
셋 다 "누가 무슨 권한으로 했나" 를 기록으로 남겨야 성립한다.

## 청크 분할표

파일 1~3개, 커밋 1개 단위다. 앞 청크가 미완이어도 뒤를 잡을 수 있게 자르려 했지만,
1~4는 토대라 순서를 지켜야 한다.

번호에 붙은 글자는 나중에 끼워 넣은 청크다. 앞 번호를 밀지 않으려고 이렇게 붙였다.

**미착수 행에는 셋을 적는다**(CLAUDE.md 「청크 하나를 어떻게 치나」).

| 칸 | 무엇 | 왜 |
|---|---|---|
| **축** | 법·표준·규약·관례 중 무엇이 근거인가 | 안 적혀 있으면 그 청크를 잡는 사람이 **기억나는 축만** 편다 |
| **강제 지점** | 제약·트리거·테스트 중 무엇이 막나 | 「다음에 조심한다」로 끝나는 청크는 아무것도 안 막는다 |
| **건드리는 자리** | 무엇을 신설하고 무엇을 고치나 | 범위를 미리 적어야 **잡기 전에** 값을 잴 수 있다 |

**셋 다 예보다 — 치면서 달라질 수 있다.** 달라지면 완료 행에 실제로 한 것을 적는다.
완료 행은 안 고친다 — 그때 그렇게 쳤다는 기록이다.

**완료 행에는 결과만 적는다.** 무엇이 생겼고 어느 제약이 섰나까지고, **왜 그렇게 정했나와
무엇이 드러났나는 `PROGRESS.md` 이력이 든다**(그쪽 「기록 규칙」). 같은 말을 두 벌 유지하면
한쪽이 낡는데 **낡은 것을 아무도 안 잡는다** — `PlanProgressConsistencyTest` 는 「닫혔나」만 보지
내용을 안 본다.

**여기를 읽는 사람은 다음 청크를 고르는 사람**이라, 필요한 것은 「선행이 무엇을 남겼나」다.

완료 표시가 두 형식이다. **`~~취소선~~` 과 선행 칸의 `완료` 는 같은 뜻이다** —
취소선은 상세를 다시 쓴 행에만 붙었다. 형식 차이에 뜻이 없다.

| # | 청크 | 무엇을 하나 | 선행 |
|---|---|---|---|
| 1 | 저장소 구조 + Docker Compose | `backend/`·`frontend/` 골격, Postgres 컨테이너, README | 완료 |
| D1 | 문서 트리 규약 + 용어집 | `doc/README.md` 에 어떤 문서가 어디 가는지, 도메인 용어 한↔영 대조표 | 완료 |
| D2 | 법·정책 요건표 | 전자상거래법·개인정보보호법 요건을 조문 단위로 적고, 각 요건이 이 프로젝트의 어느 스키마·화면에 걸리는지 매핑 | 완료 |
| 2 | Spring Boot 기동 + DB 연결 | 의존성, `application.yml`, 헬스체크 API 1개, 마이그레이션 도구 | 완료 |
| 2a | API 문서 자동화 | springdoc-openapi 로 코드에서 스펙을 뽑고 `/api/docs` 로 연다. 손으로 쓴 문서는 코드와 어긋난다. **축**: 표준 — OpenAPI 3.1 과 `D5` 응답 규약. **강제 지점**: 스펙을 코드에서 뽑는다 — 손으로 쓴 문서는 어긋나도 아무것도 안 깨진다. **건드리는 자리**: 신설은 `build.gradle.kts` 의존성과 `/api/docs` 설정. 컨트롤러 열다섯은 안 고친다 — 고쳐야 나오는 스펙이면 그 자체가 잘못된 것이다 | 2, D5 |
| 2c | ~~CI~~ | **완료.** `.github/workflows/ci.yml`(신설), `CLAUDE.md`·`security-baseline.md`·`testing-strategy.md` 수정. 잡 둘이 병렬로 돈다 — backend 는 `./gradlew build`, frontend 는 `npm ci` 뒤 `build`·`lint`·`test` 다(**프론트를 같이 거는 것은 사용자 선택** — 화면 청크가 앞으로 계속 나오는데 그쪽 검증만 사람 기억에 남는다). **Postgres 를 service 컨테이너로 안 띄운다** — 테스트가 Testcontainers 로 스스로 띄우고(`PostgresTestBase`) 러너에 Docker 가 이미 있다. 두 군데서 띄우면 어느 DB 에 붙었는지가 실행마다 달라진다. `npm install` 이 아니라 `npm ci` 다 — 잠금 파일과 어긋나면 조용히 맞추지 말고 실패해야 한다. 동시성 그룹으로 앞 회차를 접고 권한을 `contents: read` 로 좁혔다(`D14`). **아직 한 번도 안 돌았다** — 원격이 109 커밋 뒤에 있고(`08278b7` 이 마지막 푸시) 사용자가 푸시를 미뤘다. **그때까지 강제 지점은 안 걸린다**: 청크를 닫을 때 돌리는 것은 여전히 CLAUDE.md 「검증」 표다 | 완료 |
| 2b | ~~로그·요청 추적 규약~~ | **완료.** `logback-spring.xml`·`RequestLogFilter`(신설), `build.gradle.kts`·`application.yml`, `ProblemFactory` 수정, `RequestTraceTest` 6개, `D16` 갱신. Micrometer Tracing + Brave 로 W3C `traceparent` 를 이어받고 MDC 로 모든 줄에 찍는다(사용자 선택 — 널리 쓰이는 표준). **Boot 4 는 자동설정이 모듈로 쪼개져서 브리지와 자동설정 모듈을 둘 다 넣어야 한다** — 하나만 넣으면 빈은 뜨는데 `Tracer.NOOP` 이라 조용히 안 찍힌다. **`D16` 의 "앞 6자리" 를 "뒤 6자리" 로 고쳤다** — Brave 128비트 ID 는 앞 8자리가 생성 시각이라 같은 시간대 요청이 전부 같아 보였다. **필터 자리는 보안 필터 바깥·추적 필터 안쪽** — 안쪽이면 401·403 이 한 줄도 안 남는다. **`ProblemFactory` 가 헤더를 직접 파싱하던 것을 걷어냈다** — 같은 사실을 두 곳에서 정하면 사용자가 불러 준 ID 로 로그를 못 찾는다. **미뤄 둔 `WARN` 둘(`5d` 캡차·`5c-2` Redis 강등)은 그 청크에서 붙인다** — 형식이 섰으니 막힌 것이 없다 | 완료 |
| 3 | 권한 스키마 | `app_user`·`role`·`permission`·`user_role`·`role_permission` 테이블과 초기 데이터 | 완료 |
| 3a | 셀러 조직 스키마 | `seller`·`seller_member` 테이블, `user_role` 에 셀러 참조 추가. 역할 부여에 대상이 생긴다 | 완료 |
| 3b | 거부 규칙 스키마 | `role_permission` 에 `effect`(allow/deny) 추가, 거부가 필요한 초기 데이터 | 완료 |
| 3c | ~~셀러 사업자 정보~~ | **완료.** `V4` 재작성, `V8`·`V13` 수정, `SellerSchemaTest` 10개. 상호·대표자·사업자등록번호·통신판매업 신고번호·사업장 주소·연락처(`D2` R1). **`status` 에 `pending` 을 넣고 `active` 로 올라가려면 신원정보가 채워져 있게 `check` 로 막는다** — 전자상거래법 제20조②가 중개자에게 **판매 개시 전 확인**을 요구하고 제20조의2 가 연대책임을 붙인다. 사용자 선택. **신고번호는 `not null` 이 아니다** — 면제 대상이 있어서(거래 50회 미만·간이과세자) 없는 셀러가 합법이다. 면제 사유를 같이 받아야 "안 넣은 것" 과 "면제라 없는 것" 이 갈린다. `D7` 에 셀러 상태 절을 같이 추가한다. **`V4` 의 `create table seller` 를 다시 쓴다** — `alter add column` 을 일곱 번 붙이면 셀러 테이블의 모양이 두 파일에 흩어진다. **`V13` 이 `alter` 로 붙인 `commission_bp` 도 이때 `V4` 로 옮긴다**(`D3`) — 그 한 줄이 상품 마이그레이션에 셀러 컬럼이 끼어 있는 유일한 자리다. **`6b` 의 선행이라 상품 축보다 먼저 온다** | 완료 |
| 3e | 기본키 이름 정리 | 기존 13개 테이블의 `id` 를 `<테이블>_id` 로 바꾼다(`D22`). **적용된 마이그레이션을 직접 고치고 DB 를 재생성한다** — 로컬 전용이고 배포한 적이 없어서 가능하다. 배포가 한 번이라도 나가면 `alter table rename` 을 새 마이그레이션으로 해야 한다 | 완료 |
| 3d | 역할 코드 이름 정리 | `role.code = 'seller'` 를 `seller_owner` 로 바꾼다. 조직 테이블과 역할이 같은 이름이라 "seller 를 지운다" 가 폐업인지 역할 회수인지 안 갈린다(`D1`) | 완료 |
| 4 | 권한 판정 엔진 | 스코프·조직·거부를 한 군데서 판정. **화면 없이 테스트로만 검증** | 완료 |
| 4d | 응답 필드 마스킹 | 판정 결과를 허용·거부가 아니라 볼 수 있는 필드 목록으로 바꾼다. 셀러는 주문의 배송 정보만 보고 결제 수단은 못 본다(`D2` R8) | 완료 |
| 4c | 권한 매트릭스 회귀 테스트 | 부여방식×스코프×효과×대상의 판정 결과를 표로 떠서 파일로 고정한다. 판정이 바뀌면 diff 가 뜬다. **4a 보다 먼저 한다** | 완료 |
| 4a | 판정 캐시와 무효화 | 사용자 단위 캐시, 역할 변경 시 무효화. 회수가 즉시 먹는지 테스트로 못박는다 | 완료 |
| 4e | ~~허용 범위 타입~~ | **완료.** `Allowed<T>`(sealed). 권한에서 "전부" 는 값이 없어서 빈 집합에 그 뜻을 실었고, 그게 "아무것도 없음" 과 겹쳤다. 두 자리(필드 그룹·볼 수 있는 셀러)가 주석으로 막고 있던 것을 타입으로 내렸다 | 완료 |
| 4f | ~~판정 축 확장 규약~~ | **완료.** `permission-rules.md` 에 「축을 늘릴 때」. 새 조건이 들어올 때 **권한 축인가 도메인 규칙인가**를 먼저 가른다 — 안 가르면 시간·금액·이력이 전부 판정으로 몰려서 조합을 사람이 못 읽는다. 축으로 올리면 캐시(`4a`)·권한 목록(`8a`)·매트릭스(`4c`)·`Decision` 모양 넷을 같이 본다. **시점 의존 축은 캐시 전제와 충돌한다** — 회수 없이 답이 바뀌어서 무효화할 계기가 없다. 정책 엔진으로 넘어갈 임계도 숫자로 적었다. **`11a` 앞에 온다** — 여섯 번째 축이 들어오기 전에 규약이 있어야 그 축이 규약을 따른다 | 완료 |
| 4a-1 | 테스트 fixture 통합 | `PermissionEvaluatorTest`·`FieldVisibilityTest` 의 복제된 헬퍼를 `AuthFixture` 로 옮긴다. 4a 가 세 번째 복사를 막으려고 클래스만 만들어 뒀다 | 완료 |
| 4b | 감사 로그 | 권한 거부와 역할 변경을 기록하는 테이블, 기록 지점 배치 | 완료 |
| 4b-2 | ~~거부 기록이 롤백에 안 쓸리게~~ | **완료.** 사건 종류로 갈랐다 — `AuditLog.Kind` 를 호출자가 고르고 그 값이 전파를 정한다. `ATTEMPT`(거부)는 `REQUIRES_NEW`, `OUTCOME`(역할 부여·상품 생성)은 `REQUIRED`. 한쪽으로 몰면 양쪽 다 깨져서(롤백된 일이 "했다" 로 남거나, 실패한 시도가 한 줄도 안 남거나) 사건마다 고른다. 종류를 안 고르면 컴파일이 안 되므로 강제 지점이 타입까지 올라간다(`D23` 축 2). 전파를 실제로 걸려면 프록시를 지나야 해서 삽입을 `AuditLogWriter` 로 뺐다. **테스트 바탕에 정리를 하나 붙였다** — 따로 커밋된 기록은 테스트 롤백에 안 쓸려서 남의 테스트가 그걸 센다. `D16` 에 판단을 적었다 | 완료 |
| 4b-1 | 감사 로그 조회 | 행위자·대상·기간으로 감사 로그를 찾는 API. 쌓아만 두고 볼 방법이 없으면 감사가 성립 안 한다 | 완료 |
| 5-0 | 동의 이력 스키마 | `consent_item`(항목·개정판)·`user_consent`(동의·철회 사건)·`current_consent` 뷰. 상태가 아니라 사건을 적어야 동의 시점과 철회 시점이 둘 다 남는다(`D2` R7·R14) | 완료 |
| 5-1 | 인증 기반 | Spring Security 스타터, `SecurityFilterChain`(기본 `authenticated`, 공개 경로만 목록), `PasswordEncoder`, 세션 쿠키 속성. 엔드포인트는 안 만든다 | 완료 |
| 5-2 | 회원가입 | `POST /api/auth/signup`. 계정·기본 역할·동의 사건을 한 트랜잭션에 남긴다. 필수 항목 미동의와 야간 수신 종속을 앱이 막는다. JSON snake_case 와 Problem Details 설정도 여기서 켠다(`D5`) | 완료 |
| 5 | 로그인·로그아웃 | `POST /api/auth/login`·`/logout`, DB 기반 `UserDetailsService`, 세션 발급과 ID 재발급. **`formLogin` 을 껐으므로 세션 고정 방어를 직접 부른다** — 안 부르면 `D14` 요구가 조용히 빠진다. **계정 생존 확인을 로그인이 아니라 인증 필터에 둔다**(`ADR 0010`) — 로그인 때만 보면 이미 로그인한 다른 기기가 안 막힌다. `4a` 캐시에 얹는다. `SessionRegistry` 와 `HttpSessionEventPublisher` 도 여기서 등록한다 — 세션 만료(`5g`)가 그걸 쓴다 | 완료 |
| 5b-0 | CSRF 토큰 발급 | 토큰을 `XSRF-TOKEN` 쿠키로 내려서 클라이언트가 `X-XSRF-TOKEN` 헤더에 실을 수 있게 한다. 켜 두기만 하면 토큰을 얻을 방법이 없어 POST 를 아무도 못 부른다 | 완료 |
| 5b | ~~CSRF 프론트 연동~~ | **완료 — `13-2` 가 흡수했다.** 쿠키의 토큰을 읽어 헤더에 싣는 것이 `src/lib/api.ts` 안에 있다. **떼어 놓을 수 없었다** — 안 실으면 로그인이 401 이라, 화면만 만들고 이걸 미루면 `13-2` 가 안 돌아가는 채로 끝난다. 토큰이 없을 때 한 번 두드려 쿠키를 받아 오는 것도 거기 있다. **서버 쪽은 5-1·5b-0 에서 끝났다** | 완료 |
| 5c | ~~인증 실패 경로~~ | **완료.** `LoginAttemptService`·`AbsoluteSessionTimeoutFilter`(신설), `AuthController`·`SecurityConfig` 연결, 테스트 13개, `D14`·`stack.md` 갱신. (계정, IP) 5회 실패에 15분 차단을 **Redis 로** 넣었다(`38`) — 카운터 하나로 완결되고 날아가도 차단이 풀릴 뿐이라 손해가 작다. **차단 검사가 비밀번호 대조보다 앞이다** — 뒤에 두면 차단 중에도 해시 계산이 돌아서 응답 시간이 갈리고 그 차이가 계정 존재 여부를 흘린다. **세션 절대 만료 12시간도 넣었다** — 서블릿에 설정 자리가 없어 코드로만 되고, `session.timeout` 은 무활동 만료라 다른 축이다. **1회용 재설정 토큰은 `5c-1` 로 뗐다** — 발송 수단이 없다(청크 54) | 완료 |
| 5c-1 | 비밀번호 재설정 토큰 | 1회용 토큰을 발급하고 그것으로만 비밀번호를 바꾼다. 응답은 **가입 여부와 무관하게 같은 문구**다(`D14`) — 갈리면 가입 여부를 물어보는 도구가 된다. **전달 수단을 먼저 정해야 한다** — 메일 발송이 청크 `54b` 라 지금은 보낼 곳이 없고, `A09` 가 로그에 토큰을 남기지 말라고 해서 로그로 흘릴 수도 없다. **모의 구현을 구체 `@Component` 로 두는 방식**이 선례다(`MockPaymentGateway`). `CaptchaVerifier` 는 `D14` 가 그린 설계일 뿐 코드에 없다. **축**: `D14` 보안 기준선(1회용 토큰·수명), `D16`(`A09` — 토큰을 로그에 안 남긴다). **강제 지점**: 토큰을 해시로 저장하고 사용 시각을 채우는 전이를 `check` 로 — 재사용을 앱이 아니라 DB 가 막는다. **건드리는 자리**: 신설 `password_reset_token` 표와 `PasswordResetService`. 수정은 `AuthController` 와 `54b` 의 발송기 | 5c, D14 |
| 5c-2 | ~~Redis 장애 시 로그인~~ | **완료.** `LoginAttemptService.LocalCounter`(신설), 테스트 4개, `D14` 갱신. **순수 fail-open 도 fail-close 도 아니고 강등이다** — Redis 예외를 잡고 프로세스 로컬 카운터로 옮겨서 5회 / 15분을 그대로 센다. 인스턴스가 하나라 덮는 범위가 같다. 막으면 캐시 한 대에 서비스가 멈추고, 그냥 통과시키면 「개인정보의 안전성 확보조치 기준」이 요구하는 인증 실패 횟수 제한이 장애 시간 동안 사라진다. **캡차 fail-open 논거(*"남의 장애"*)는 안 걸린다** — Redis 는 같은 compose 안에 있고, 캡차가 죽어도 살아 있다고 적어 둔 방어가 이 카운터다. **복구 뒤에 두 카운터를 안 합친다**(합치면 정상 사용자가 복구 직후 잠긴다). **강등 `WARN` 로그는 `2b` 몫이다** | 완료 |
| 5d | 캡차 | reCAPTCHA v3 를 로그인·회원가입·비밀번호 재설정에 붙인다. `CaptchaVerifier` 인터페이스 뒤에 두고 테스트는 항상 통과하는 구현을 쓴다. 점수 0.5, `action` 검증, 검증 실패는 통과시킨다(`D14`). **이 프로젝트의 첫 외부 호출이고 첫 국외이전이다** — 개인정보법 제28조의8 은 제3자 제공(제17조)과 별개 조문이라 고지 항목이 다르다(`D2`). `13a-1` 처리방침에 둘을 갈라서 적는다. 검증 실패를 `WARN` 으로 남기는데 **이 프로젝트의 첫 애플리케이션 로그가 된다** — `2b` 가 먼저 와야 형식이 정해진 채로 찍힌다. **개인정보법 제28조의8(국외이전)을 `D2` 에 승격시킨다**(점검 A) — 제3자 제공과 별개 조문이라 고지 항목이 다르다. **축**: **법**(개인정보법 제28조의8 — 국외이전 고지·동의), `D14`, 업계 관례(reCAPTCHA v3 점수 0.5). **강제 지점**: `CaptchaVerifier` 인터페이스 뒤에 두고 **검증 실패는 통과시키는 것**을 테스트로 고정한다 — 캡차가 죽으면 로그인이 죽는 구조를 안 만든다. **건드리는 자리**: 신설 verifier 둘(실물·항상 통과). 수정은 `AuthController` 세 경로와 처리방침 새 판(국외이전 항목) | 5c, 13-2, 2b, D14, D2 |
| 5h | ~~계정 생존 확인~~ | **완료.** `AccountLivenessFilter`, `PermissionRuleLoader.isAlive`, 캐시 `accountLiveness` | 완료 |
| 5e | ~~내 계정 조회·수정~~ | **완료.** `AccountService`·`MeController`. **`Location` 문제는 안 풀렸다** — `/api/me` 는 id 로 가리키는 경로가 아니라 201 이 지목할 자원이 못 된다. 관리자용 계정 조회(16)가 그 자리다 | 완료 |
| 5f | ~~동의 조회·철회~~ | **완료.** `ConsentService`. 철회·재동의 둘 다 있고 채널 철회가 야간까지 거둔다 | 완료 |
| 5g | ~~탈퇴~~ | **완료.** `WithdrawalService`, `POST /api/me/withdraw`. `ConcurrentSessionFilter` 를 같이 걸어야 세션 만료가 실제로 먹는다(`ADR 0010`) | 완료 |
| 5i | ~~탈퇴 계정 파기~~ | **완료.** `AccountPurgeService`, `V2`·`V8` 수정. 유예가 지난 탈퇴 계정의 이메일·이름·비밀번호 해시와 `user_consent.acted_ip` 를 비운다(`D2` R9). **`10a` 에서 떼어 왔다** — 거기는 선행이 주문(`10-1`)이라 한참 뒤인데 **파기 대상은 `5g` 가 이미 만들고 있다**. 이메일은 `null` + 부분 유니크 인덱스(`ADR 0007`). 탈퇴는 `update` 라 `user_consent` 의 cascade 가 안 걸린다 — 여기서 직접 지운다. `D13` 에 동의 이력 3년·IP 30일을 적었다 | 완료 |
| 5k | ~~동의 고지 조회~~ | **완료.** `ConsentItemController`(공개)·`MeController` 확장, `ConsentService.readCurrent`·`readMine`. 동의 항목의 고지 내용과 약관 본문을 내려준다. **약관규제법 제3조② 가 고객 요구 시 사본을 내주라고 한다** — 지금 `5f` 는 항목 이름과 상태만 줘서 사본을 요구할 경로가 없다. **가입 전에도 봐야 하므로 공개 경로다**(`SecurityConfig.PUBLIC_PATHS`). 목록에 본문을 통째로 싣지 않는다 — 약관 본문이 커서 동의 화면이 무거워진다. `5-0` 이 스키마로 표현한 것을 앱이 못 하면 그 설계는 쓰인 적이 없는 것이다 | 완료 |
| 5j | ~~동의 고지 항목~~ | **완료.** `V11` 재작성, `ConsentSchemaTest` +5. 정형 넷(목적·항목·기간·거부 시 불이익) + 본문 1칸. `consent_item` 에 수집·이용 목적, 수집 항목, 보유·이용 기간, 거부 시 불이익을 담는다(개인정보법 제15조제2항). **지금은 `title` 뿐이라 "3판에 동의했다" 의 3판 내용이 시스템에 없다** — 개정판을 남기는 설계가 입증에 못 쓰인다. 정형 컬럼으로 나눌지 본문 한 덩어리로 둘지는 이 청크에서 정한다. `acted_ip` 수집도 항목에 적는다. **약관 판의 본문도 같이 담는다**(`D2` R16 약관규제법 제3조) — 명시·설명을 못 하면 그 조항을 계약 내용으로 주장할 수 없다. 근거법이 둘(개인정보 고지 / 약관 명시)이라 요구가 겹치지만 대상 범위가 다르다. **`V11` 의 `create table consent_item` 을 다시 쓴다** — 시드도 같은 파일에 있어서 컬럼과 값을 한 자리에서 맞춘다 | 완료 |
| 5a | 조직 초대·멤버십 | 셀러에 계정을 붙이고 그 셀러 안에서만 먹는 역할을 준다. 멤버 구성을 못 바꾸는 `seller_staff` 역할이 여기서 생긴다(`D1`). **축**: `D6` 권한 모델(조직 역할), `D14`. **강제 지점**: 초대 토큰 유니크 + 역할 부여를 판정 엔진 경로로만 — 표를 직접 만지면 조직 경계가 새는 자리가 하나 더 생긴다. **건드리는 자리**: 신설 `seller_invitation` 표와 `SellerMemberService`. 수정은 `permission_rule` 시드(`seller_staff`) | 3a, 5, D1, D6 |
| 6 | ~~상품 스키마~~ | **완료.** `V13__product_schema.sql`. 테이블 5개(`product`·`product_option`·`product_option_value`·`sku`·`sku_option_value`)와 `seller.commission_bp`. 요율은 만분율 정수, R4 는 `check` 제약, 가격·재고는 `sku` 에 | 완료 |
| 6b | ~~데모 시드 데이터~~ | **완료.** `db/seed/V900__demo_seed.sql`, `application-local.yml`. **`db/migration` 이 아니다** — 참조 데이터와 성격이 달라서 `local` 프로필에서만 Flyway 대상에 더한다. 셀러 2(신고번호/면제), 계정 6, 소속 4. `both-member` 가 두 셀러에 속하는데 역할은 한쪽만 받는다 | 완료 |
| 7b | ~~인증·권한 실패 응답 규약~~ | **완료.** `ErrorCode`·`ShopException`·`ApiExceptionHandler`·`ProblemFactory`·`ProblemEntryPoint`. `D5` 가 정한 자원별 403·404 와 Problem Details 를 실제 코드에 붙인다. 판정 실패는 거부로 떨어뜨리고 500 을 준다. 오류 본문에 `trace_id` 를 넣는다(`D16`). **401 도 여기서 본다** — `5-1` 의 `HttpStatusEntryPoint` 는 본문 없이 상태만 준다. **도메인 예외와 `@RestControllerAdvice` 번역기를 만들고 서비스 6곳의 `ResponseStatusException` 을 옮긴다**(`D23`). **`7` 앞에 온다** — 뒤에 두면 상품·주문 축이 잘못된 패턴을 계속 늘려서 옮길 곳이 6곳에서 12곳이 된다. 403·404 사례는 감사 로그 조회와 계정 API 에 이미 있어서 상품 API 를 안 기다려도 됐다 | 완료 |
| 6b-1 | ~~데모 시드에 상품~~ | **완료.** `V902__demo_products.sql` 신설, `stack.md` 두 절. 상품 다섯 — 옵션 있음·없음, **상품 전체 품절과 조합만 품절**, 배송비와 무료배송, 약정 7영업일과 법정 기한, **동의를 받는 제한(`made_to_order`)과 안 받는 제한(`digital_content`)**. 고른 기준은 **화면에서 갈리는 것을 데이터로 밟는 것**이고, 하나라도 빠지면 그 갈래는 다시 손으로 넣게 된다. **조합은 하나씩 넣고 그 자리에서 선택지를 묶는다** — 한 번에 넣으면 어느 `sku` 가 어느 조합인지 이어 줄 값이 없다. **시드가 시행 전인 동의 판까지 넣던 것을 걷어냈다**(`V900` 이 판을 안 가린다). **`out-of-order` 를 켰다가 되돌렸다** — `stack.md` 가 이미 그 선택을 버려 뒀는데 어겼고, 켜 보니 스위치가 둘 필요해서 그 판단이 오히려 맞았다. `6b-1` 로 **다시 만드는 값이 싸졌다** | 완료 |
| 6b-2 | ~~데모 시드에 주문~~ | **완료.** `DemoOrderSeeder` 신설(`demo/` 패키지), `D23` 「패키지」 한 절, `stack.md` 한 절. **SQL 로 안 넣었다** — 주문 하나가 서려면 통과할 자리가 여덟이고(번호 발급·영업일 계산·서면 네 조항·제한 박제·재고 차감·셀러별 배송비·상태 이력·결제 행) SQL 로 다시 쓰면 **같은 규칙의 사본이 둘**이 된다. `local` 프로필 `ApplicationRunner` 가 **운영 서비스를 그대로 부른다** — `CartService`→`OrderService`→`PaymentService`→`OrderActionService`. **강제 지점이 타입이다**(축 2의 1위): 서비스 시그니처가 바뀌면 시더가 안 컴파일된다. 주문 다섯 — 셀러 둘로 갈린 주문(배송비 3,000원과 무료, 법정 3영업일과 약정 7영업일), 배송완료, 단순 변심 반품, **하자 반품**(사유만 다른데 기한도 배송비 부담도 갈린다), **청약철회 제한이 성립한 주문**(동의받은 `made_to_order` 와 `digital_content` 를 한 묶음에). `support/` 에 못 둔다 — `order → support` 가 이미 있어서 **순환**이라 `demo/` 를 팠고 그 근거를 `D23` 에 적었다. **주문이 하나라도 있으면 건너뛴다** — Flyway 와 달리 기동할 때마다 돌아서 안 막으면 띄운 횟수만큼 쌓인다 | 완료 |
| 7 | ~~상품 등록·수정 API~~ | **완료.** `ProductService`·`ProductController`, 테스트 20개. `scope=seller` 가 실제 자원에 처음 걸렸다. **시드가 `own` 이라 판매자가 상품을 하나도 못 만들던 것을 찾아 고쳤다** — 상품엔 주인 계정이 없다(`ADR 0004`). `product.created_by_user_id` 를 담당자 축으로 새로 뒀고 `own` 은 `5a` 의 `seller_staff` 가 쓴다 | 완료 |
| 7d | ~~판매 중지·제재~~ | **완료.** `ProductTransitions`(전이표), `suspend`·`resume`·`block`·`unblock`, `product.block_reason`, 상태에 `blocked` 추가. **승인이 끝이 아니다** — 위법 표시·위조품 신고·리콜은 팔기 시작한 뒤에 드러나고 알고도 방치하면 연대책임이다(제20조의2). 셀러가 쉬는 것(`suspended`)과 관리자가 막은 것(`blocked`)을 상태로 갈라서 **셀러가 제재를 못 푼다** | 완료 |
| 7c | ~~상품 검수~~ | **완료.** `ProductReviewService`, `product:review` 권한, `product.review_note`, `check_product_sale_allowed` 트리거, 테스트 11개. 셀러가 쓴 상품을 관리자가 승인해야 노출된다. **셀러가 `active` 가 아니면 `on_sale` 전이를 막는다**(`D2` R1, 전자상거래법 제20조②) — `pending` 셀러도 `draft` 는 만들 수 있게 열어 뒀으므로 막는 자리가 여기다. `check` 로는 다른 테이블을 못 봐서 **앱과 트리거 두 겹**으로 막았다 — 앱이 422 로 이유를 주고 트리거가 다른 입구를 막는다 관리자 역할에 실제로 할 일이 생기고 상품에 상태가 붙는다(`D2` R15) | 완료 |
| 7e | ~~상품 상태를 enum 으로~~ | **완료.** `ProductStatus`·`SkuStatus`(신설), `ProductTransitions`·`ProductReviewService`·`ProductQuery`·`ProductService` 수정, `ProductStatusTest`(신설) 4개. 리터럴 13곳이 타입으로 바뀌었고 전이표도 enum 을 든다. **enum 이 둘이다** — 두 목록이 겹치지만 같지 않아서(조합은 검수도 제재도 없다) 하나로 두면 `sku` 자리에 `draft` 를 넣어도 컴파일이 통과한다. **제약 정의를 읽어 대조하는 테스트를 뒀다** — `pg_get_constraintdef` 에서 값을 뽑아 enum 과 맞춘다. 목록을 테스트에 또 적으면 세 번째 사본이 된다. **응답 변환도 enum 을 지나간다** — 대문자로만 올리면 DB 의 모르는 값이 그대로 화면에 나간다. **SQL 텍스트 안의 리터럴은 안 바꿨다**(근거를 `D23` 에 남겼다) — 자바 식별자가 아니라 그 쿼리의 조건 자체고, 파라미터로 빼면 쿼리만 봐서 무엇을 거르는지 안 보인다 | 완료 |
| ~~7e 원안~~ | 상품 상태를 enum 으로 | 상품 축이 상태를 생 문자열로 쓴다 — `ProductReviewService` 에 리터럴 13곳, `ProductTransitions` 의 전이표도 `String` 이다. **`D23` 「열거값을 어디에 두나」가 enum 을 요구한다**(강제 지점 1위). 문자열이면 오타를 컴파일러가 못 잡고, 이름을 바꿀 때 IDE 가 못 따라가고, 「이 값이 어디서 쓰이나」를 못 묻는다. **`product.status` 와 `sku.status` 가 둘 다 `on_sale` 을 쓰므로** 검색으로도 안 갈린다 — enum 이 둘이어야 타입으로 갈린다. 주문 축(`OrderTransitions.Shipment`)에 선례가 있어서 모양을 베낀다. **`check` 목록과 enum 을 대조하는 테스트를 같이 둔다** — DB 에 값을 추가하고 enum 을 안 고치면 `of()` 가 런타임에야 터진다 | 7d, 8, D23 |
| 8 | ~~상품 조회 API~~ | **완료.** `ProductQuery`·`SellerProductController`·`ListQuery`, 테스트 16개. **경로를 갈랐다** — `/api/products`(공개, 판정 없음)와 `/api/seller/products`(스코프를 섞는다). 한 쿼리로 합치면 `or` 하나가 틀렸을 때 비로그인에게 `draft` 가 새고 그 실수는 조용하다. 정렬은 허용 목록(`created_at`·`name`). **`AuditLogQuery` 의 문자열 결합도 여기서 정리했다** | 완료 |
| 8a | 내 권한 목록 API | 로그인한 사용자가 지금 무엇을 할 수 있는지를 서버가 계산해 내려준다. 화면이 역할 이름으로 판단하면 판정이 두 벌이 된다 | 완료 |
| 9 | ~~장바구니~~ | **완료.** `V15__cart.sql`, `CartService`·`CartController`, 로그인 병합, 파기 배치에 흡수. **비로그인도 담는다** — 동의 없이 되는 근거는 개인정보법 제15조①4호(계약 체결 과정의 요청)고, 대신 처리방침 고지(13a)와 보유기간 30일(D13)을 채운다. **세션 ID 를 안 쓴다** — 로그인할 때 바뀌어서 병합할 대상을 못 찾는다 | 완료 |
| 17-1 | ~~불변식 카탈로그~~ | **완료.** `money-invariants.md`. 주문 6·정산 5·환불 3 등식과 강제 지점. **합계를 저장하기로 정했다** — `D8` 이 청크 10 으로 미뤄 둔 것이 여기로 당겨졌다. 여러 행에 걸친 합계는 **지연 트리거**로 건다(`order` 가 `order_item` 보다 먼저 들어와서 즉시 트리거는 언제나 깨진다). 환불·정산 축은 스키마가 없어서 등식만 적고 강제는 `12-1`·`12a-1`·`17` 이 건다 | 완료 |
| 10-1 | ~~주문 스키마~~ | **완료.** `V16__order_schema.sql`, `OrderSchemaTest` 21개. 테이블 4개(`shop_order`·`order_shipping`·`seller_order`·`order_item`), 한 행 등식 7개는 `check`, 합계 3개와 존재 조건 2개는 **지연 트리거**. **`order` 가 SQL 예약어라 `shop_order` 고 기본키는 `order_id`**(`D22` 의 `app_user` 예외 규칙). **배송지를 주문에서 분리했다**(`D13`) — 주문은 5년 보존인데 배송지는 파기 대상이라 한 행에 두면 남길 것과 지울 것이 엉킨다. **`D9` 의 예시 주문번호가 자기 규칙을 어기고 있었다** — 형식을 `check` 로 내리자 거부됐다 | 완료 |
| 10-0 | ~~멱등키~~ | **완료.** `V17__idempotency.sql`, `IdempotencyService`, 테스트 9개, `AccountPurgeService` 에 24시간 정리 흡수, `D11` 갱신. **선점·처리·저장이 한 트랜잭션이다**(사용자 선택) — 필터에 두면 셋이 갈라져 "주문은 커밋됐는데 기록은 진행중" 구간이 생기고, 그때 죽으면 재전송이 영원히 막힌다. **필터는 안 만들었다** — `run()` 호출 여부를 모르니 "헤더는 요구하는데 멱등은 안 걸린" 상태를 가린다. 커버리지는 `10-2` 의 테스트가 지킨다. **409 의 뜻이 바뀌었고 실패는 저장 안 한다** — `D11` 두 문장을 코드에 맞춰 고쳤다 | 완료 |
| 10-2 | ~~주문 생성 API~~ | **완료.** `OrderService`·`OrderController`, `V4` 에 `default_shipping_fee`, `ProductService` 수정, 테스트 21개. 장바구니에서 **고른 항목**만 주문한다(사용자 선택). 가격·수수료율 박제, 재고는 조건부 UPDATE 를 `sku_id` 오름차순으로, 셀러별 `seller_order` 분할, 배송지는 `order_shipping` 에. **주문에 쓰인 SKU 는 `suspended` 로 내리고 옵션 축 변경은 422**(사용자 선택) — 바꾸면 지나간 주문의 옵션 라벨이 뜻을 잃는다. **배송비 출처를 `seller` 에 만들었다**(사용자 선택) — `D3` 가 셀러 몫이라고만 정하고 금액 출처가 없었다 | 완료 |
| 10a | ~~거래기록 보존·파기~~ | **완료.** `TransactionPurgeService`, `V16` 에 `closed_at`, 테스트 9개, `D2`·`D13` 갱신. 배송지 6개월 → 주문 5년 → 감사 로그 3년. **전자상거래법 제6조 제2항을 조문으로 확인했다** — 거래기록은 "보존하여야 한다"(의무)인데 그와 관련된 개인정보는 **"보존할 수 있다"**(재량)라, 5년 보관도 6개월 파기도 둘 다 적법하다. 보존이 권리지 의무가 아니므로 쓸 일이 끝나면 버리는 쪽을 골랐다(사용자 선택). 기산점은 `seller_order.closed_at` 이고 **채우는 것은 청크 11-2** 다 | 완료 |
| 10a-2 | ~~안 닫히는 셀러 주문의 강제 마감~~ | **완료.** `StaleBundleBatch`·`StaleBundleBatchTest`(둘 다 신설), `application.yml`(풀 9)·`batch-catalog.md`. **막히는 상태가 둘이 아니라 셋이었다** — 분할표는 `preparing`·`return_requested` 를 적었는데 전이표를 보니 **`shipping` → `delivered` 를 밀어 주는 것도 없다.** 자동 구매확정은 `delivered` 에서만 시작한다. **`preparing`·`shipping` 둘만 민다**(사용자 선택). 안 보낸 것은 공급 곤란이라 `cancelled` 로 닫고(제15조제2항), 닫히는 순간 `RefundSweeper` 가 환불 요청을 만든다 — **여기서 환불을 직접 만들면 그 스위퍼와 사본이 둘이 된다.** `shipping` 은 `delivered` 로만 민다 — **확정까지 밀면 청약철회할 틈이 사라진다.** **`return_requested` 는 안 민다** — 제18조제2항 1호가 기산점을 「재화를 **반환받은 날**」로 잡아서, 물건이 안 왔는데 `returned` 로 닫으면 **「반환받았다」가 거짓**이 되고 분쟁에서 우리 기록이 우리에게 불리한 증거가 된다. 대신 **세어서 `WARN` 으로 드러낸다** — 안 세면 그 구멍이 아무 데도 안 남는다. **시한은 상태마다 다른 시계를 쓴다**(사용자 선택) — `preparing` 은 **법이 준 `ship_due_at`** 에서 이레(우리 여유), `shipping` 은 `shipped_at` 에서 서른 날. **강제 지점은 「배치가 도나」가 아니라 「결과가 비나」다**(`36a` 함정) — 닫힌 뒤 `closed_at` 이 채워지는 것까지 밟는다. 배치가 아홉이 돼서 `36b` 가 또 걸렸고 풀을 9로 올렸다 | 완료 |
| 2d | ~~컴파일 경고 정리~~ | **완료.** `build.gradle.kts` 에 `-Xlint:deprecation`·`-Xlint:unchecked`, `ErrorCode` 와 테스트 4파일 수정, `stack.md` 갱신. 정체는 **이름이 바뀐 것 둘**이었다 — RFC 9110 이 422 를 `UNPROCESSABLE_ENTITY` 에서 `UNPROCESSABLE_CONTENT` 로 고쳤고(테스트의 `isUnprocessableEntity()` 도 같이), Jackson 3 이 `JsonNode.asText()` 를 `asString()` 으로 바꿨다. 기능이 없어진 게 아니라 경고를 켜기 전엔 어느 줄인지도 안 나왔다. **`-Werror` 는 안 걸었다** — 라이브러리를 올릴 때 남의 코드에서 오는 경고로 빌드가 막히면 급할 때 옵션을 통째로 끄게 된다 | 완료 |
| 10b | ~~재고 경쟁 테스트~~ | **완료.** `OrderConcurrencyTest`(신설) 5개, `D15`·`stack.md` 갱신. 재고 10개에 20명이 동시에 사서 정확히 10개만 팔리고 나머지가 `OUT_OF_STOCK` 을 받는 것, 엇갈리게 담아도 데드락이 안 나는 것을 고정했다. **롤백을 껐다**(`NOT_SUPPORTED`) — 테스트가 트랜잭션을 쥐고 있으면 다른 스레드가 차감을 못 보고 잠금도 안 걸려서 경쟁 자체가 안 난다. **정렬을 지워도 통과할 수 있는 테스트라 대조 테스트를 같이 뒀다** — 조회 쿼리에 `order by` 가 없어서 담은 순서를 뒤집어도 플래너가 같은 순서를 줄 수 있다. 잠그는 순서를 손으로 엇갈리게 만들어 `40P01` 을 실제로 냈다. **그 `40P01` 이 `PessimisticLockingFailureException` 으로 온다** — `10c` 가 이걸 안다 | 완료 |
| 10c | ~~충돌 재시도~~ | **완료.** `Retries.onConflict`(`support`). SQLSTATE `40001`·`40P01` 만 50·100·200ms 로 다시 돌린다 — **예외 타입으로 안 가른다**(`stack.md`). 건 자리 둘: `OrderController.create`(멱등 **바깥**), `OrderStatusBatch.move`. **멱등 안쪽은 애초에 못 건다** — Postgres 가 그 트랜잭션을 abort 시켜서 다음 문장이 `25P02` 로 죽는다. 바깥이면 선점 기록도 같이 롤백돼 있어서 같은 키로 다시 들어오는 것이 맞다. `Runnable` 짝을 안 만들었다 — 값을 돌려주는 람다가 둘 다에 맞아 컴파일이 모호해진다. **이전 설명**: `D11` 이 `40001`·`40P01` 을 50·100·200ms 로 3회 재시도하라고 정했는데 **거는 곳이 아직 없다.** 트랜잭션 바깥에 걸어야 한다 — 안쪽이면 이미 깨진 트랜잭션을 붙들고 다시 돈다. **예외 이름으로 잡으면 안 된다**(`stack.md`) — Postgres 는 SQLSTATE 앞 두 자리로 번역돼서 데드락이 `DeadlockLoserDataAccessException` 이 아니라 상위 타입으로 온다. `OrderConcurrencyTest` 가 그 사실을 고정해 뒀다. **재시도가 멱등키 바깥인지 안쪽인지 정한다** — 안쪽이면 선점 기록까지 되돌아가고, 바깥이면 같은 키로 두 번 들어온다. **거는 곳이 하나 생겼다**: `OrderStatusBatch` 의 건 루프다(`D19` 1층) — 건마다 트랜잭션이 갈려 있어서 바깥 조건이 이미 맞다 | 완료 |
| 11-1 | ~~상태 이력 스키마~~ | **완료.** `V18__order_status_history.sql`, `OrderStatusHistorySchemaTest` 16개, `D2` R6·`D7` 갱신. `ADR 0007` 이 설계만 해 두고 테이블이 없던 자리다 — 전이 코드를 먼저 쓰면 이력을 남길 곳이 없어 두 번 고친다. **두 층을 한 테이블에 받았다**(사용자 선택) — `order_id`·`seller_order_id` 중 정확히 하나를 채우고 상태 값 목록도 층을 보고 가른다. 소비자가 보는 것이 주문 하나의 생애라 나누면 화면마다 이어 붙여야 하고 한쪽을 빠뜨리면 이력이 반만 보인다. **법이 두 갈래로 걸린다**(`D2` R6) — 제1항의 보존 5년, 그리고 **제3항의 열람 제공**(R6 에 빠져 있어서 같이 채웠다). 관리자 전이에 사유 필수, 남은 행은 트리거로 수정 불가 | 완료 |
| 11-1a | ~~공휴일 표~~ | **완료.** `V19__holiday.sql`, `D22` 에 자연키 예외. `D10` 이 영업일 계산을 `holiday` 표에 걸어 뒀는데 표가 없었다 — 기한 박제(`11-2`)가 그것을 쓴다. **2026년을 시드했다**(사용자 선택 — 비워 두면 실제 시스템을 만지는 느낌이 안 난다). **음력에서 나오는 날(설·부처님오신날·추석)과 대체공휴일은 미검증**이라 SQL 주석에 밝혔다. 토·일은 안 담는다 — 요일은 날짜에서 나온다. 기본키가 `holiday_date` 다: 값 자체가 식별자인 표는 대리키를 안 얹는다 | 완료 |
| 11-2 | ~~전이표·전이 서비스~~ | **완료.** `OrderTransitions`·`OrderStatusService`·`BusinessCalendar`(신설), `ErrorCode` 3개 추가, `TransactionPurgeService` 수정, 테스트 17개, `D7`·`D10`·`D22` 갱신. 전이표를 코드에 선언하고(`ADR 0009`) 전이마다 이력 한 줄을 남긴다. **곁가지를 서비스가 데리고 다닌다** — 배송완료는 기한 둘을 박제하고, 확정·취소·반품완료는 `closed_at` 을 채우고, 취소는 재고를 되돌린다. **`10a` 파기에 이력 삭제를 넣었다**(흡수 완료). **관리자는 사유를 적으면 표 밖으로도 옮긴다**(`D7`). **날수 계산만으로는 자동확정이 청약철회와 같은 날이 될 수 있다** — 말일 보정이 붙이는 자리라 `max(8일째, 청약철회 만료 다음날)` 로 고쳤다. **HTTP 경로는 안 만들었다** — 누가 옮길 수 있나는 `11a` 권한 축이고, 그 전에 API 를 열면 인증만 통과하면 남의 주문을 옮긴다 | 완료 |
| 11-3 | ~~상태 자동 전이 배치~~ | **완료.** `OrderStatusBatch`·`SchedulingConfig`(신설), 테스트 10개, `stack.md` 갱신. 결제 만료는 `fixedDelay` 5분(앞 회차가 끝난 뒤부터 세서 느려져도 안 겹친다), 자동확정은 `cron` 04:00 KST. **배치에 규칙이 없다** — 대상을 고르고 `OrderStatusService` 를 부를 뿐이라 재고 복구·이력·`closed_at` 이 한 군데에 남는다. **건마다 트랜잭션이 갈린다** — 배치에 `@Transactional` 을 붙이면 하나가 터질 때 그 회차가 전부 롤백된다. **한산한 회차는 `DEBUG`** — INFO 로 남기면 하루 288줄이 쌓여 진짜 처리가 묻히고, 아무것도 안 남기면 도는지를 못 본다. 실서버에서 스케줄이 실제로 도는 것을 로그로 확인했고 **배치 스레드에도 추적 ID 가 붙는다** | 완료 |
| 11c-1 | ~~구매자 주문 조회~~ | **완료** — `OrderQuery` 신설, `GET /api/orders`·`/{orderNumber}`. 상세가 셀러 묶음·항목·**이력을 한 줄로** 내린다(`D2` R6). 못 보는 주문은 **없는 주문과 같은 404**(`D5`). 배송지는 `shipping` 그룹이라 `_visible_field_groups` 가 따라 나간다. **열거값을 대문자로 내린다**(`D5` 「형식」, 사용자 선택) — 상품 응답이 소문자로 나가는 것은 `11c-3a` 이 맞춘다 | 완료 |
| 11c-2 | ~~셀러 주문 식별자와 노출 경계~~ | **완료** — `seller_order_number`(`S-20260811-K3M9P7`)와 `seller_order_visible` 뷰를 `V16` 에 넣었다. **독립 번호를 골랐다**(사용자 선택) — 이 단위를 부르는 곳이 넷(부분 취소·환불, 정산 명세, 반품 접수, 송장)이고 「주문번호+셀러코드」면 그 넷에 컬럼 둘로 나간다. 뷰는 **`paid` 만** 든다. 청크 12 가 `paid_at` 을 만들면 조건을 그쪽으로 옮긴다 | 완료 |
| 11c-2b | ~~셀러 주문 조회 API~~ | **완료** — `SellerOrderQuery`·`SellerOrderController`(`/api/seller/orders`), `ORDER_FORBIDDEN` 추가. 뷰만 읽어서 미결제 조건을 다시 안 적는다. 스코프는 `ProductQuery` 와 같은 방식으로 조건에 섞는다. **목록 거부는 403, 상세는 404** — 목록에는 가리키는 자원이 없어서 403 이 존재를 안 흘린다. **미결제 목록은 안 만들었다** — 마켓플레이스들이 「미결제확인」을 별도 탭으로 두므로 자리는 있지만 셀러가 그때 할 일이 없다. 만들면 뷰를 더 두지 말고 이 뷰의 조건을 파라미터로 받는 쪽을 먼저 본다 | 완료 |
| 11c-2c | ~~셀러 주문 계약에 발송 기한·반품 사유~~ | **완료.** `V37__seller_order_ship_overdue.sql` 신설, `SellerOrderQuery`, `SellerOrderVisibilityTest`. **`13g` 를 열다가 계약에 칸이 없는 것이 드러났다** — 목록은 `ship_due_at` 을 안 뽑고 `Summary` 에 칸도 없었으며, `V30` 이 뷰에 실어 둔 `return_reason` 은 `Detail` 이 안 받았다. **화면보다 먼저 친 이유는 비용이다**(순위 2번) — 응답 계약은 소비자 수에 비례해 값이 오르고 프론트 소비자가 아직 0이다. **「발송이 늦었나」 판정식을 뷰로 내렸다**(`is_ship_overdue`, `D22` 라 `is_` 로 시작하고 Java 필드는 `shipOverdue` 라 **JSON 은 안 바뀐다**) — 앱 SQL 두 곳에 같은 글자로 있던 것이 목록을 더하면 셋이 되는 자리였고, 기한 초과는 지연배상금 연 15% 가 걸린 도메인 규칙이라 화면마다 답이 갈리면 안 된다. **강제 지점은 뷰→계약 대조 테스트다** — 기존 `ViewColumns` 는 표→뷰만 봐서 이 구멍을 못 잡았다. 새 테스트는 뷰 컬럼마다 **계약에 싣거나 `NOT_IN_CONTRACT` 에 근거를 적거나** 둘 중 하나를 고르게 만든다. `returnReason` 을 빼고 돌려서 **실제로 빨간불이 뜨는 것까지 확인했다** | 11c-2b |
| ~~11c-2 원안~~ | 셀러 주문 조회 | 셀러 처리 화면이 부를 목록·상세. **`shop_order.status` 로 미결제 건을 거른다**(사용자 선택) — 결제 사실을 `shop_order` 한 군데에 두는 대신 셀러 조회가 주문 층을 같이 봐야 한다. **빠뜨려도 오류가 안 나서 뷰로 강제하고** 미결제 건이 안 뜨는 것을 테스트로 고정한다. **셀러 묶음의 노출 번호를 여기서 정한다** — `11c-3a` 이 전이 대상을 가리키려면 필요한데 `seller_order` 에는 내부 id 뿐이다(`D9`) | 11c-1, D9 |
| 11c-3a | ~~주문 처리 API~~ | **완료.** `OrderActionService`·`ShipmentController`(신설), `V20__order_action_permission.sql`, `OrderStatusPolicy`·`OrderStatusService`·`ErrorCode` 수정, 테스트 16개. **동작을 넷으로 갈랐다**(사용자 선택) — 상태 축의 단위가 `resource:action` 이라 `allowedStatuses` 가 역할을 못 본다. 하나로 두면 `delivered` 를 여는 순간 셀러가 대신 구매확정을 눌러 정산을 당기고, 고객에게 주면 고객이 `preparing → shipping` 을 밀어 셀러가 안 보낸 주문이 확정된다. **경로는 최상위다** — `seller_order_number` 가 전역 유일이라 주문번호를 앞에 붙여도 대상이 안 좁아진다(Stripe·Shopify 와 같은 조건). **상태는 조회한 행의 값 그대로 `Target.inStatus(...)` 로 간다**(사용자 지시). **반품에 법 조건 둘을 걸었다** — 청약철회 기한(`D2` R3)과 제한 상품(R4). 전이 서비스에 둬서 배치·관리자도 같은 검사를 지난다. **`ORDER_TRANSITION_NOT_ALLOWED` 를 422 → 409 로 고쳤다** — `D5` 「동작」과 RFC 9110 이 409 인데 `11-2` 가 422 로 넣었고, HTTP 경로가 없어서 밖으로 나간 적이 없다 | 완료 |
| 11c-3b | ~~상세의 `allowed_actions`·응답 형식 정리~~ | **완료.** `PermissionEvaluator.allowedActions` 신설, `OrderActionService.allowedActions`, `OrderQuery`·`SellerOrderQuery`·`ProductQuery`·`ErrorCode` 수정, `D5` 갱신, 테스트 8개. **조회용 판정 경로를 새로 냈다**(사용자 선택) — `decide` 로 동작 여섯을 물으면 상세 한 번에 거부가 넷씩 감사에 쌓이고 그건 침입 시도가 아니라 화면이 버튼 모양을 물어본 것이다. 감사는 3년 보존이라 진짜 시도가 묻힌다. **`decide` 에 플래그를 안 붙였다** — `4b` 가 기록을 판정 안에 넣은 이유(호출자가 빠뜨린다)를 무른다. **거르는 것이 둘이다** — 전이표와 권한·상태 축. 앞을 빼면 셀러가 `update_status` 하나로 셋을 쓰므로 `preparing` 에 「배송완료」가 뜬다. **청약철회 제한은 목록에서 안 본다** — 상품 속성이라 축이 아니고, 미리 알리는 것이 제한의 성립 요건이라(제17조제2항 단서) 화면이 상품에서 이미 받는다. **경로 이름을 동작 이름에 맞췄다**(`request-return`) — 소문자·하이픈이 곧 경로라 화면이 표를 안 든다. **구매자 상세에 `seller_order_number` 를 내린다** — 확정·반품의 단위가 묶음이라 없으면 동작을 못 부른다. **`OUT_OF_STOCK` 을 422 → 409 로**(사용자 선택) | 완료 |
| ~~11c~~ | 주문 조회·처리 API | **셋으로 쪼갰다**(위). 한 청크로는 파일이 6개라 안 떨어졌다. 원래 설명: 구매자의 주문 목록·상세와 셀러의 처리 화면이 부를 경로. 상세는 `order_status_history` 를 시간순으로 같이 내린다 — **전자상거래법 제6조 제3항이 소비자에게 거래기록 열람 방법을 요구한다**(`D2` R6). **셀러 조회는 `shop_order.status` 로 미결제 건을 거른다**(사용자 선택) — 결제 사실을 `shop_order` 한 군데에만 두는 대신, 셀러 쪽 조회가 전부 주문 층을 같이 봐야 한다. **빠뜨려도 오류가 안 나서 뷰로 강제하고** 미결제 건이 셀러 목록에 안 뜨는 것을 테스트로 고정한다. 전이를 부르는 것은 `11a` 가 권한을 붙인 뒤다. **전이 경로는 `Target.inStatus(...)` 로 상태를 실어 보낸다** — 안 실으면 상태 축이 거부한다(`11a`). **`allowed_actions` 를 상세에 같이 내린다**(`11a` 선택) — 권한 목록(`8a`)은 상태를 안 보므로 상태별로 지금 무엇이 되는지는 여기서 답한다. **청약철회 제한 상품의 반품 차단은 여기서 도메인 규칙으로 건다**(`D2` R4, `11a` 에서 옮겨 왔다) — 근거가 상품 속성이라 상태 축으로 표현이 안 된다 | 11-2, 11a, D5 |
| 11a | 상태 조건부 권한 | **완료** — `StatusPolicy`(auth)·`OrderStatusPolicy`(order) 신설, `Target` 에 `status` 추가. 축이 여섯이 됐다(`D6`). **표는 자원 패키지에 두고 `auth` 는 인터페이스만 안다**(사용자 선택) — 반대면 `auth → order` 순환이 는다. **표에 있는 동작인데 상태를 안 실으면 거부**한다. **`delivered` 를 지나면 셀러의 전이 권한이 닫힌다** — 안 닫으면 셀러가 청약철회 기산점을 조작한다(`D7`). **`8a` 권한 목록은 상태를 안 본다**(사용자 선택) — 상태별은 `11c-2` 상세가 내린다. **청약철회 제한 상품의 반품 차단은 `11c-2` 의 도메인 규칙으로 옮겼다** — 근거가 상품 속성이라 축이 아니다 | 완료 |
| 11-4 | ~~기한을 날짜로 되돌리는 자리~~ | **완료.** `OrderStatusService` 에 `END_OF_DAY` 상수(`999_999_000`)를 세우고 테스트를 왕복 검증으로 바꿨다(`keepsDeadlineDateAcrossRoundTrip`). **되돌려 보고 그 테스트만 깨지는 것을 확인**했다. 「말일이 쉬는 날이면 다음 영업일」 자체는 고정 날짜 테스트가 이미 보고 있어서, 새 테스트는 **그 계산이 DB 를 왕복해도 살아남나**를 본다 — 하루가 밀린 구간이 거기였다. 테스트 516개 통과. **이전 설명**: `OrderStatusServiceTest.movesDeadlineOffHolidays` 가 **말일이 금요일인 날에만 깨진다.** `endOfDay` 가 `LocalTime.MAX`(`23:59:59.999999999`)를 넣는데 Postgres `timestamptz` 는 마이크로초까지라 **올림해서 다음날 `00:00:00` 으로 저장한다** — 저장값은 「말일 24시」라 맞고, 그것을 `toLocalDate()` 로 되돌린 테스트가 말일+1 을 본다. **답이 정해졌다**(`D10` 「저장 정밀도가 마이크로초다」) — 문서가 처음부터 `.999999` 여섯 자리로 적어 뒀고 코드가 그것을 안 지켰다. `endOfDay` 를 `999_999_000` 으로 맞추고, **달력을 타는 단언을 고정 날짜 단위 테스트로 내린다**(`D15` 「오늘 날짜에 기대지 않는다」). `requireWithdrawable` 의 비교(`isBefore`)는 지금도 맞아서 안 건드린다 | 11-3, D10, D15 |
| 11b | 대리 관계와 미성년자 취소 | **개인정보법 제22조의2(아동 동의)를 흡수한다**(`D2-5`, `R29`) — 만 14세 미만은 법정대리인 동의를 받고 확인해야 하는데 지금은 생년월일이 없어 아동인지도 모른다. 이 청크가 만드는 두 칸(생년월일·법정대리인 관계)을 그대로 쓴다. 계정에 생년월일과 법정대리인 관계를 두고, 법정대리인이 남의 주문을 취소하는 경로를 연다. `scope=own` 의 "내 것" 정의가 하나 더 생긴다(`D2` R13)<br>**개인정보법 제38조제1항·제2항(대리인의 열람등요구)도 흡수한다**(`Q13`, `R28`) — 지금은 본인 세션만 받아서 대리인이 요구할 자리가 없다. 만 14세 미만 법정대리인과 **같은 두 칸**을 쓰므로 따로 세우면 그 스키마가 두 번 논의된다. **축**: **법**(개인정보법 제22조의2 아동 동의, 민법 제5조 미성년자 취소), `D2` R29. **강제 지점**: 「만 14세 미만이면 법정대리인 동의 행이 있다」를 트리거로 — 생년월일만 받으면 확인 없이 가입이 된다. **건드리는 자리**: `app_user` 에 생년월일·법정대리인 칸, `AuthSignupService`, 동의 항목 시드. 취소권은 주문 축(`OrderActionService`)에 붙는다 | 4, 5, 11-2 |
| 12-1 | ~~모의 결제 — 승인까지~~ | **완료.** `V22__payment.sql`·`MockPaymentGateway`·`PaymentService`. **`D11` 의 두 규칙이 부딪히는 자리였다**(사용자 선택) — 「선점·처리·저장을 한 트랜잭션」과 「트랜잭션 안에서 외부 호출 금지」가 결제에서 같이 걸린다. **PG 호출만 밖으로 빼고 우리 멱등키를 PG 에 그대로 넘겼다** — 재전송이 두 번 승인받는 구간을 우리 트랜잭션이 못 닫아서 PG 가 닫는다(실물 PG 와 같은 구조). **그 대가로 재생 확인도 밖으로 나왔다**(`IdempotencyService.replayIfPresent`) — 재전송 시점에는 주문이 이미 `paid` 라 상태 검사가 **재생에 닿기 전에 막았다.** 테스트가 잡았고 안 잡혔으면 재전송이 앞의 성공 때문에 실패를 받는다. **거절은 예외가 아니라 결과다**(사용자 선택) — 던지면 `payment_failed`·셀러 주문 자동 취소·재고 복구가 같이 롤백돼서 `D7` 이 정한 상태가 배치 말고는 안 쓰인다. **실패 유도는 테스트 카드번호**(사용자 선택, Stripe 관례) — 뒷 4자리 `0000` 거절·`0002` 무응답. `Retries` 에 판정을 받는 `on` 을 열어 결제사 무응답 재시도가 같은 간격표를 쓴다. 원래 설명: PG 흉내 모듈, 실패·재시도. **카드번호·유효기간·CVC 를 애초에 안 담는다**(`D2` R18 여신전문금융업법 제19조) — 모의 결제라 면제되는 게 아니라, 나중에 진짜 PG 를 붙일 때 스키마를 다시 파는 일이 된다 | 11-2, D11, D2, D14 |
| 12-2 | ~~결제 입구~~ | **완료.** `PaymentController`(`POST /api/payments`, 멱등키 필수·카드번호 형식·`Location`), `OrderQuery` 에 `payment` 필드, `HttpFlowTest` 둘. **결제 조회를 따로 안 냈다** — `V6` 의 `payment` 필드 그룹이 이미 그 자리고(`4d`), 경로를 새로 내면 주문 상세와 같은 것을 두 군데서 그린다. `V3` 의 `payment:read` 는 **남이 낸 결제를 보는 자리**(환불 `12a-2`·관리자 `16`)에 걸린다. **응답 표기를 고쳤다** — `Result` 가 그대로 응답이 되면서 `status`·`method` 가 `D5` 의 대문자 스네이크에 걸렸고, 저장값(`approved`)과 표기(`APPROVED`)를 상수로 갈랐다. **거절이 201 인 이유를 `D5` 에 적었다** — 4xx 로 던지면 기록이 같이 롤백된다. **`HttpFlowTest` 의 `payAll()` 을 걷어냈다** — SQL 로 상태를 밀던 자리라 전이표도 결제 기록도 안 거쳤다. 멱등 커버리지 목록(`OrderIdempotencyTest`)에 `/api/payments` 가 들어갔고, 경로마다 본문을 가른다 — 아무 본문이나 보내면 400 이 나긴 하는데 그건 키가 없어서가 아니다 | 12-1, D5 |
| 12a-1 | ~~환불 스키마·서비스~~ | **완료.** `V23__refund.sql`(`refund`·`refund_item`, 지연 트리거 3개), `RefundService`·`ExposedNumber`(신설), `MockPaymentGateway`·`BusinessCalendar`·`OrderStatusService`·`OrderService`·`ErrorCode` 수정, 테스트 18개. **요청 출처를 세 갈래로 열고 자기승인을 막았다**(사용자 선택) — 승인자와 요청자가 다르다는 것을 `refund_self_approval_check` 로 내렸고(강제 지점 2위) 앱은 403 으로 이유를 준다. **`R5` 가 코드에 닿았다** — 기한을 `due_at` 에 박제하고 **기산점을 요청 시각이 아니라 `closed_at` 으로 잡았다**: 요청 시각이면 늦게 요청할수록 기한이 밀려서 법보다 늦게 줘도 안 늦은 것이 된다. 「기한 안에 승인」은 `check` 로 못 건다 — 막으면 늦은 돈이 영영 안 나가서, 강제는 「넘긴 것이 조회로 드러난다」가 천장이다. **부분 환불은 항목·수량 단위고 수수료 절사 잔액을 마지막 수량에 몰아 준다**(사용자 선택) — 안 몰면 통째 환불인데 1원이 정산에 우리 몫으로 남는다. 배송비는 묶음이 비워질 때만 전액이다(항목별로 나눌 근거가 없다). **`12-1` 이 주석으로 남긴 구멍을 닫았다** — 조회와 기록 사이에 만료 배치가 끼면 PG 에는 승인이 남고 주문은 취소되는데, 그래서 **환불 가능 판정을 주문 상태가 아니라 결제 승인 유무로 본다**(`reason_code = payment_error`). **번호 생성을 `ExposedNumber` 로 뽑았다**(흡수) — 환불번호가 세 번째 사본이 될 자리였다. **`endOfDay` 도 `BusinessCalendar` 로 옮겼다** — 마이크로초 올림 함정(`11-4`)을 두 벌 들고 있게 된다 | 완료 |
| 12a-2 | ~~환불 입구~~ | **완료.** `V24__refund_permission.sql`·`RefundController`·`RefundQuery`(신설), `RefundService`·`OrderQuery`·`OrderFields` 수정, 테스트 19개. **권한을 둘로 갈랐다**(사용자 선택) — `payment:request_refund` 와 승인을 한 권한에 두면 요청을 여는 순간 승인이 같이 열려서 고객이 남의 환불을 승인한다(`V20` 이 `order:update_status` 를 넷으로 가른 것과 같은 자리). **승인이 관리자만인 근거는 법이다** — 제18조제2항 괄호가 「대금을 받은 자」를 통신판매업자에 넣어 환급 의무자가 우리고, 제20조의2제3항이 중개자 고지로 그 책임을 못 면한다고 한다. **목록을 기한 임박순으로 냈다** — `12a-1` 이 강제의 천장으로 둔 「넘긴 것이 조회로 드러난다」(`D2` R5)가 그 정렬로 선다. **`refund` 필드 그룹을 세워 셀러도 본다**(사용자 선택) — 정산에서 차감되는 돈이라 못 보면 명세를 확인할 자리가 없다. 요청 사유는 소비자가 쓴 자유 텍스트라 뺐다. **회귀를 하나 냈고 테스트가 잡았다** — 그룹을 조건 없이 붙이자 관리자가 배송지·결제를 잃었다(`V6`: 연결이 없는 규칙은 제한이 없는 것). 이미 연결이 있는 규칙에만 붙게 고치고 방벽을 뒀다 | 완료 |
| 12a-3 | ~~환불 요청 스위퍼와 기산점 갈래~~ | **완료.** `V25__refund_origin.sql`·`RefundSweeper`·`RefundSweeperTest`(신설), `RefundService`·`RefundController` 수정, 테스트 21개. 사유를 다섯으로 갈라(`cancelled`·`supply_failed`·`admin_cancelled`·`withdrawal`·`payment_error`) 기산점을 조문별로 맞췄다. **기산점 목록을 「닫힌 날에서 세는 것」쪽으로 만들었다** — 새 사유가 늘 때 빠뜨리면 이른 쪽으로 떨어져서, 실수가 위반이 아니라 우리 손해로 끝난다. 스위퍼는 이력의 `actor_type` 으로 사유를 고르고 **반품은 누가 눌러도 청약철회**로 본다(셀러의 반품완료는 반환 수령 사실이지 셀러가 일으킨 사건이 아니다). **`query(String.class)` 가 null 을 못 받는다** — 비었는지는 SQL 로 물었다. 원래 설명: 닫힌 묶음 중 | 완료 |
| ~~12a-3 원안~~ | 환불 요청 스위퍼 | 닫힌 묶음 중 환불 요청이 없는 것을 찾아 요청을 만든다. **법이 청약철회만으로 환급 의무를 발생시킨다**(`D2` R5) — 사람이 요청을 안 내면 3영업일이 그냥 흐르고 지연배상금이 연 15%로 붙는다(시행령 제21조의3). **전이 쪽이 아니라 배치다**(사용자 선택) — `OrderStatusService` 에 넣으면 `order → payment` 가 생겨 순환이 되고, `D23` 「의존」이 허용해 둔 순환은 `auth ↔ audit` 하나뿐이다. 몇 분 늦는 것은 3영업일 기한에 무의미하다. `OrderStatusBatch` 의 모양을 베낀다. **기산점 갈래를 같이 고친다**(점검 B) — `12a-1` 이 `reason_code = 'cancelled'` 하나로 소비자 취소와 셀러 공급 불능을 뭉쳤는데 **법의 기산점이 다르다**: 소비자 취소는 제18조제2항 3호로 청약철회한 날(`closed_at`)이고, **셀러 공급 불능은 제15조제2항이라 대금을 지급한 날**(`payment.created_at`)이다. 지금은 뒤엣것을 취소 시각에서 세서 **법보다 늦게 잡는다** | 12a-2, D19 |
| 11-7 | ~~하자 반품 접수~~ | **완료.** `V29__return_reason.sql`·`DefectReturnTest`(신설), `OrderStatusService`·`OrderActionService`·`ShipmentController` 수정, 테스트 9개(전체 656, 실패 0). **제17조제3항의 「제1항 및 제2항에도 불구하고」가 둘을 동시에 연다** — 하자 반품은 7일도 청약철회 제한도 안 걸리고 3개월이다. 그전까지 들어오는 반품을 전부 단순 변심으로 봐서 **8일째 하자 신고와 주문제작 상품의 하자를 거부**하고 있었다. **3개월은 박제 안 한다**(사용자 선택) — 역일이라 `delivered_at` 에서 세면 언제 계산해도 같고, 박제할 이유가 없는 값을 박제하면 같은 사실이 두 곳에 생긴다. **「안 날부터 30일」은 안 건다**(사용자 선택) — 소비자의 인식이라 관찰할 수 없고 제17조제5항이 입증을 우리에게 지웠다. 짧게 지는 쪽이 안전하다. **하자 주장을 안 거른다** — 같은 조항이 접수를 막을 근거를 없앤다. **제약을 `return_requested` 에만 걸었다** — 관리자가 접수를 건너뛰고 반품완료로 직행하는 경로가 있고(`D7`) 그 경우는 조항이 없는 것이 사실이다. 사실 아닌 값을 채우게 만드는 제약은 거짓말을 강요한다 | 완료 |
| 11-6 | ~~공급 기한~~ | **완료.** `V26__supply_deadline.sql`·`ShipDeadlineTest`(신설), `OrderService`·`OrderStatusService`·`OrderQuery`·`SellerOrderQuery`·`ProductService`·`ProductController` 수정, 테스트 9개(전체 631, 실패 0). **결제 승인 때 박제한다** — 선지급식이라 기산점이 대금 지급일이다. **공급시기 약정을 `product.supply_lead_days` 로 받는다**(사용자 선택) — 그 `null` 은 기본값이 아니라 「약정이 없다」는 사실이고, 없으면 법정 3영업일이 걸린다. 묶음은 그 안에서 **가장 긴 것**을 쓴다(한 번에 나가므로 가장 늦게 준비되는 항목이 발송 시점을 정한다). **`shipped_at` 을 같이 박제했다**(사용자 선택) — 없으면 늦게라도 보내는 순간 지연의 흔적이 사라져서 상습 지연과 한 번 지연이 같아 보인다. **뷰를 다시 만들어야 했다** — `seller_order_visible` 이 컬럼 목록을 굳혀서 표에 컬럼이 늘어도 안 따라오고, 셀러 조회가 그 뷰만 읽는다. **`seller_order` 쪽 기본값은 남겼다** — 빠뜨리면 법정 3영업일로 떨어지는 것이 안전한 방향이다. **약정 고지는 `14c` 다** — 약정은 고지가 성립 요건이라 상품 상세가 그려야 법적으로 선다. 원래 설명: 결제 시점에 | 완료 |
| D2-5 | ~~개인정보 보호법 조문 통독~~ | **완료.** `commerce-compliance.md` 에 `R28`~`R30` 신설, 「안 걸린다고 확인한 것」 표 아홉 줄, 「아직 안 적은 법」에 조문 넷. 코드가 안 생긴다. **`R28`(열람등요구)이 지금 어긋난다** — 제38조제4항이 「열람등요구의 방법과 절차는 **수집 방법과 절차보다 어렵지 아니하도록**」인데 가입은 화면이고 열람·정정은 API 뿐이다. **전자상거래법 제21조의2 4호와 같은 자리를 다른 법이 두 번 때린다** — `13e` 하나가 둘을 닫는다. **처리방침이 없는 경로를 가리킨다** — 「마이페이지에서 직접 하실 수 있습니다」라고 적어 뒀다. **지금 안 고친다**(사용자 선택) — 불변 트리거 때문에 개정이 곧 새 판이라 고쳤다 되돌리면 왕복이 이력에 남는다. `R29`(아동)는 `11b` 에 흡수했다(사용자 선택) — 생년월일과 법정대리인 관계 두 칸을 같이 쓴다. `R30`(유출 통지)은 통지 인프라도 **감지할 자리도** 없다 | 완료 |
| D2-3 | ~~전자상거래법 조문 통독~~ | **완료.** `commerce-compliance.md` 에 `R23`~`R27` 신설, 통독 절과 「안 걸린다고 확인한 것」 표, 「아직 안 적은 법」에 조문 일곱. **이 표가 그전까지 조문이 아니라 청크에서 왔다** — 코드를 만들다 걸린 것만 추가돼서 **코드가 없는 요건은 표에도 없었다.** 제1조부터 제46조까지 목차를 훑고 걸릴 만한 것의 본문을 확인했다. **둘이 결정적이다.** `R23`(제20조의3) — 청약 접수와 대금 수령을 다 하는 중개업자는 청약확인과 전자적 대금지급 통지를 **대신 이행하여야 한다**. 「못 면한다」가 아니라 직접 의무라 `R20` 의 급이 올라갔다. `R24`(제21조의2 1호) — **첫 화면 총액 표시**를 어긴다. 지금까지 나온 것 중 **유일한 금지행위**다 | 완료 |
| 14d | ~~첫 화면 총액 표시~~ | **완료.** `ProductQuery`·`CartService`·상품 목록·상품 상세·`purchase-panel`·장바구니 수정, `ProductQueryTest` 에 2개(전체 647, 실패 0). **총액이 큰 글자다**(사용자 선택) — 법이 막는 것이 「일부만 표시해서 **유인**」이라 상품가만 크게 두면 규제 대상이 된 그 관행이 된다. **장바구니까지 값으로 고쳤다**(사용자 선택) — 「배송지를 정해야 나오는 값」이라는 **사실 아닌 문구**를 들고 있었다. **강제 지점이 응답 계약이다** — `shippingFee` 가 빠지면 화면이 상품가만 그리고 오류가 안 나서, 값이 실리는지를 테스트가 고정한다. **3호는 안 걸린다** — 확인 대화상자가 `window.confirm` 이라 버튼 모양을 브라우저가 정한다. 브라우저로 밟아 목록·상세 22,000원(19,000+3,000), 장바구니 41,000원(38,000+3,000)을 확인했다. 원래 설명: 상품 목록과 상세가 배송비를 포함한 총액을 그린다 | 완료 |
| ~~14d 원안~~ | 첫 화면 총액 표시 | 상품 목록과 상세가 배송비를 포함한 총액을 그린다. **제21조의2 1호가 금지행위로 정한 것**이라(`D2` R24) 다른 미착수와 급이 다르다 — 안 한 것이 아니라 하고 있는 것이다. `15-2` 가 배송비를 공개 셀러 응답에 이미 더해 뒀으므로 **값은 있고 첫 화면이 안 쓰는 것**이다. **3호(선택항목의 시각적 차이)도 같이 본다** — 주문 취소·확정의 확인 대화상자가 한쪽 버튼만 눈에 띄게 그려져 있으면 걸린다 | 14, 14b, 15-2, D2 |
| D2-6 | ~~약관규제법 제8~14조 통독~~ | **완료.** `commerce-compliance.md` 에 `R31` 신설과 「안 걸린다고 확인한 것」 표 여섯 줄, 통독 대장 갱신. 코드가 안 생긴다. **`R31`(의사표시의 의제)이 지금 어긋난다** — 제12조1호가 「일정한 작위 또는 **부작위**가 있을 경우 고객의 의사표시가 표명된 것으로 보는 조항」을 무효로 하고 단서가 **「명확하게 따로 고지」**한 경우에만 살려 주는데, **자동 구매확정이 약관에 한 줄도 없고 화면은 날짜만 그린다.** 날짜는 고지가 아니다. **실질 불이익은 작다** — `autoConfirmLastDay` 가 청약철회 만료 다음날보다 앞설 수 없어 그때는 권리가 이미 끝나 있다. 그래도 제12조1호는 불이익의 크기가 아니라 **의사표시로 보는 조항인지**를 묻는다. **안 걸린 여섯 중 넷이 「그런 조항이 없어서」다** — 약관이 짧아서 안전한 것이라 조항을 더할 때마다 그 표를 다시 본다 | 완료 |
| D2-7 | ~~약관 제3판~~ | **완료.** `V36__terms_notice_wording.sql`(신설), 주문 상세 문구 수정, `MeConsentTest` 픽스처 수정. **셋을 한 판에 고쳤다** — `R31` 의 자동 구매확정 고지(제5조 신설), 탈퇴 유예가 **약관에만 30일**로 남아 있던 것(`Q10` 이 5일로 줄이고 `V34` 로 방침은 고쳤는데 약관만 안 따라왔다), 「분리하여 보관」이 **사실이 아니던 것**(제21조제3항 미착수. **하는 것만 적는다**). **시행일이 이레 뒤고 코드를 먼저 안 보냈다** — `V34` 는 덜 보관하는 방향이라 괜찮았는데 여기는 고지라 반대고, 자동확정은 청약철회 기간이 끝난 뒤에만 와서 그 이레에 권리를 잃는 사람이 없다. **개정판이 테스트 하나를 깼는데 그게 진짜였다** — 픽스처가 `order by version desc` 로 **시행 전 판**을 집었다. 운영 경로 둘은 `effective_at` 을 보는데 픽스처만 안 봤다 | 완료 |
| D2-8 | ~~정보통신망법·표시광고법·물가안정법 통독~~ | **완료.** `commerce-compliance.md`(`R32`~`R34` 신설, `R12` 출처 정정, 통독 절), `PLAN.md`(`13g-1` 신설, `58` 에 `R34`). **넷이 나왔다.** ① **`R12` 의 출처가 틀렸다** — 근거로 적혀 있던 **가격표시제 실시요령의 표시의무자가 오프라인 점포**다(매장면적 33㎡ 이상 등). 통신판매를 대상으로 적은 자리가 고시에 없고, 라벨·꼬리표로 개별 상품에 붙이라는 표시 방법도 화면에 안 옮겨진다. 전자상거래법 제13조제2항과 표시광고법 제3조로 옮겼다 — **`price_incl_vat` 는 안 바뀌고 바뀐 것은 그 이름을 정당화하는 조문**이다. ② **표시광고법 제5조 실증책임**(`R32`) — 사실과 관련한 문구는 실증할 수 있어야 하고 요청받으면 15일 이내인데 **받는 자리가 없다**. ③ **제4조 중요정보 고시**(`R33`) — 품목별 필수 항목이 비어도 안 걸린다. ②③은 `13g-1` 을 세워 달았다. ④ **정보통신망법 제50조의7**(`R34`) — 우리는 광고를 **보내는 쪽이 아니라 운영자 쪽**이고, Q&A 가 열리면 그 자리가 광고판이 된다. `58` 이 같이 닫는다. **제50조 제1~8항은 이 통독 직전에 `55`·`55a`·`55b` 가 원문으로 내렸다.** **안 걸리는 넷도 근거를 남겼다**(제50조의4·의5·의8, 물가안정법 제3조) — 빠뜨린 것과 일부러 안 넣은 것이 코드에서 구분이 안 된다. **밀도가 떨어진 것이 아니라 성격이 달라졌다** — 앞의 셋은 없는 요건을 찾았고 이번은 **있는 요건의 출처가 틀린 것**을 찾았다 | 완료 |
| D2-9 | ~~전자상거래법 제20조의3 통독~~ | **완료.** `commerce-compliance.md`(`R35` 신설, 통독 표 정정). **조문 넷을 원문으로 봤다** — 제7조(조작 실수 등의 방지)·제8조(전자적 대금지급의 신뢰 확보)·제13조제2항제5호·제14조제1항. **결론은 「새로 만들 것이 없다」다** — 넷 다 실물이 이미 있다: 제7조는 `/checkout` 이 청약 전에 판매자 정보와 금액을 보이고 장바구니로 돌아가는 길을 낸다(`15-2`), 제8조는 `payment_completed` 통지(`54a`)와 주문 상세 열람, 제13조제2항제5호는 계약내용 서면(`15-4`·`V31`), 제14조제1항은 `order_placed` 통지다. **그래도 요건을 세운 이유가 있다** — **근거가 없으면 다음 사람이 지운다.** 「법 요건은 코드에 없는 것이라 요건표에서 내려간다」의 반대 방향이다: 있는 것에 조문을 붙인다. **`D2-3`(전자상거래법 통독)이 이 조를 빠뜨린 이유도 적었다** — 조문 목차에서 「중개업자의 책임」으로만 읽혀서 **우리 얘기가 아니라고 넘어갔다.** 실제로는 **청약 접수·대금 수령을 하는 중개업자**를 지목하고 우리가 둘 다 한다 | 완료 |
| D18-1 | ~~알림 이력 보존 기간 재판정~~ | **완료.** 법 제6조와 **시행령 제6조제1항 각 호를 원문에서 확인했다** — 1호 표시·광고 6개월, 2호 계약 또는 청약철회 등 5년, 3호 대금결제 및 재화등의 공급 5년, 4호 소비자의 불만 또는 분쟁처리 3년. **거래 통지 넷을 호에 붙이면 전부 5년 칸이다**(청약 접수 제14조제1항→2호, 대금 지급 제8조제3항→3호, 공급 곤란 제15조제2항→3호, 환급 제18조제3항 단서→2호·3호). `D18` 의 3년은 감사 로그에 맞춘 우리 판단이었고 **그건 4호 칸의 값**이다. **3년 → 5년.** 여기서 `data-lifecycle.md` 의 「5년을 사는 표에는 사람 정보가 없다」와 부딪쳤다 — 본문에 이름·주소·금액이 들어간다. **두 층으로 갈랐다**(사용자 선택): 메타 5년 + **본문은 발송 + 6개월**. 법이 막는 것은 아니고(제6조제2항이 「보존할 수 있다」), **재량이라 우리가 골랐다.** 본문은 사본이고 원본이 전부 산다 — 문안은 템플릿 판, 금액은 `shop_order`·`payment`(5년), 시각·받는 사람은 메타. 6개월은 `order_shipping`·`payment_card` 선례를 따른 값이라 파기 배치가 조건 하나로 지운다. **광고성 정보 이력은 1호 6개월**(사용자 선택) — 동의 증명은 이 표가 아니라 `user_consent`(3년)가 한다. **강제 지점 둘을 `54a` 로 넘겼다**: 본문을 별도 표로 떼고 메타가 외래키로 가리킨다(한 표면 `null` 파기가 되고 안 비운 행이 섞여도 안 깨진다), 종류 컬럼 `check` 로 수명을 가른다(배치가 종류를 다시 판정하면 갈라지는 자리가 둘이 된다). 원래 설명: `D18` 이 발송 이력 보존을 **3년**(분쟁 대응 기간)으로 정했는데, 청약 접수 확인·환급 통지는 전자상거래법 제6조의 **「계약 또는 청약철회 등에 관한 기록」(5년)**으로 볼 여지가 있다. 3년이면 4~5년차 분쟁에서 **「보냈다」를 증명할 것이 사라진다**. **`54a` 보다 먼저 해야 한다** — 표를 만들 때 수명이 같이 들어가서, 뒤에 바꾸면 마이그레이션이 하나 더 든다 | 완료 |
| PLAN-1 | ~~미착수 행에 축을 채운다~~ | **완료.** `PLAN.md` 한 파일. 미착수 31행에 **걸리는 축과 강제 지점**을 적고, 표 머리에 그 기준과 완료 표시 두 형식이 같은 뜻이라는 것을 밝혔다. **「청크 하나를 어떻게 치나」를 세운 직후에 잰 것이 계기다** — 미착수 125행 중 45행에 축이 한 글자도 없었고(31행이 진짜 미착수, 14행은 완료인데 취소선만 없던 행), 그대로 두면 새 기준이 **다음 청크부터 바로 안 지켜진다**. 완료 103행은 안 고쳤다 — 이력이다. **처분 순위 2번이라 먼저 쳤다** — 청크가 늘수록 채울 행도 는다 | 완료 |
| PLAN-2 | ~~닫힌 청크가 분할표에서도 닫혔는지 대조한다~~ | **완료.** `PlanProgressConsistencyTest`(신설), `build.gradle.kts`·`PLAN.md` 수정. **`12a-2` 가 이력에는 완료인데 분할표에는 미착수로 이틀 남아 있었다** — 후속 `12a-3` 은 완료로 닫힌 채였고, 한쪽만 고쳐도 아무 명령이 안 깨져서 **다음 사람이 그 청크를 미착수로 읽는다**. 표시를 닫고 **대조를 테스트로 내렸다**(사용자 선택) — 이력에서 완료로 적힌 청크 번호를 분할표에서 찾아 그 행이 닫혔는지 본다. **한 방향만 본다**: 이력에는 점검·마무리처럼 분할표에 행이 없는 줄이 섞여 있어서 반대 방향은 통째로 거짓 경보가 된다. **`test` 의 입력에 두 문서를 신고했다** — 안 하면 문서만 고친 청크에서 Gradle 이 `UP-TO-DATE` 로 건너뛰어 대조가 한 번도 안 돈다 | 완료 |
| PLAN-3 | ~~미착수 행에 건드리는 자리를 채운다~~ | **완료.** `PLAN.md` 한 파일과 `CLAUDE.md` 한 문단. **미착수 74행 전부를 축·강제 지점·건드리는 자리 셋으로 통일했다**(사용자 선택) — 45행은 셋 다 없었고 29행은 앞의 둘만 있었다. **셋째 칸이 이 청크가 더한 것이다** — 「무엇을 신설하고 무엇을 고치나」가 어디에도 없어서, 청크를 잡기 전에 값을 잴 수가 없었다. **`PLAN-1` 이 왜 다시 필요해졌나**: 그때 채운 31행 뒤로 청크가 늘었고, 늘어난 행은 다시 비어 있었다 — **미착수가 늘수록 채울 것도 는다**는 것이 그때 순위 2로 친 이유고 여기서도 같다. 표 머리에 세 칸의 뜻과 **「예보라 치면서 달라진다」**를 적었다 — 달라진 것은 완료 행에 적고 미착수 행은 안 고친다 | 완료 |
| W1 | ~~세션 단위를 정의한다~~ | **완료**(2026-08-20) — `CLAUDE.md`. 「예열」(세션 열기 — 축을 **선언**까지 한다)·「마무리」(세션 닫기 — 대상은 **그날 친 청크 전부**)·「청크 하나를 어떻게 치나」(시작 3줄, 닫힘 5조건)를 세웠다. **`점검 F` 가 근거다** — 읽고도 안 걸린 것이 열하나라, 닫힘 조건에 **「이 청크가 남긴 강제 지점 한 줄」**을 넣었다. `W` 는 저장소를 굴리는 절차 자체를 고치는 청크다(`PLAN-1` 도 같은 종류다) | 완료 |
| W2 | 세션 스킬 둘 | `.claude/skills/preheat/`(청크 세션 열기 — 축 1·2 두 줄을 **인라인**하고, `PROGRESS` 현재 상태와 **분할표 그 행 하나**만 읽는다. `PLAN-1` 이 행에 축을 적어 둬서 `coding-rules.md` 를 안 연다)와 `.claude/skills/axis-sweep/`(점검 단위 절차 — 축 하나·저장소 전체·**코드는 안 고치고** 축 문서와 `PLAN` 에만 내린다). **알맹이는 스킬이 아니라 그 안에 박는 셋이다** — `law.go.kr` 은 평문 URL 이 제목만 주고 `print=print` 판을 받아야 한다는 것(어제 왕복으로 알아냈는데 **어디에도 안 적혔다**), 「지적 전에 그 자리 주석과 이력을 연다」(`B4` 가 그래서 철회됐다), 「스윕 산출물은 후보다」(`S5` 를 확정으로 적어서 되돌리는 글을 또 썼다). 조회법은 `external-references.md` 에 절로 같이 넣는다 — 스킬은 절차, 문서는 사실. **축**: 프로젝트 규약. **강제 지점**: 스킬 본문이 절차를 고정한다. 다만 **삭는 건 못 막는다** — 그건 여전히 제약·테스트의 몫이다. **건드리는 자리**: 신설 `.claude/skills/preheat/`·`.claude/skills/axis-sweep/` 둘. 저장소 코드와 문서는 안 건드린다 — 읽는 순서만 정한다 | W1 |
| W3 | 이력이 흩어지는 것을 막는다 | **1차 마무리가 찾았다**(2026-08-23). 이력 줄을 줄 번호로 끼워 넣다가 **엉뚱한 날짜 사이에 넣는 실수를 하루에 세 번** 했다(두 번은 커밋까지 갔다). 그러면 그 날짜가 표의 두 자리로 흩어지고 **그 날 무엇을 했는지 읽는 사람이 한쪽만 보고 만다.** 「다음에 조심한다」로 끝나면 아무것도 안 막은 것이다. **그물을 먼저 짜 봤더니 과거 어긋남 둘(`2026-08-12`·`2026-08-18`)이 이미 있었다** — 그래서 테스트만 넣으면 **빨간 채로 남아 「지금 깨져 있는 것」이 된다.** 되돌리고 청크로 세웠다. **축**: 저장소 운영 절차(`W` 계열). **정할 것**: **과거 이력을 옮기나.** ① 옮긴다(내용은 그대로 두고 자리만 — 「이력은 그때의 사실」에 안 걸린다. 대가: 줄이 여럿이라 손이 간다), ② 오늘 이후만 본다(대가: 검사에 예외 목록이 생기고 그 목록이 낡는다). **강제 지점**: `PlanProgressConsistencyTest` 에 검사 하나 — **빈 줄을 경계로 보면 안 된다**(초기 이력은 빈 줄 없이 이어져서 통째로 빨개진다). 규칙은 「한 날짜가 **연속 구간 하나**로만 나타난다」고, 날짜 **순서**는 안 본다(오름차순 구간과 내림차순 구간이 섞여 있어서 그것까지 고정하면 고칠 이유가 없는 줄을 고치게 된다). **건드리는 자리**: `PlanProgressConsistencyTest` 와 `PROGRESS.md` 이력 | |
| W4 | CI 액션 버전 올리기 | **CI 가 처음 돌면서 알려줬다**(2026-08-23). 지금 안 깨지지만 곧 깨진다 — ① `actions/checkout@v4`·`setup-node@v4`·`setup-java@v4`·`gradle/actions/setup-gradle@v4` 가 **Node 20 을 목표로 해서 러너가 24 로 강제 실행**하고 있다(deprecation), ② **`setup-java v4` 는 더 이상 업데이트를 안 받는다** — v5 로 옮기라고 한다. **축**: **업계 관례**(`D23` 축 1 의 4순위 — 따르든 버리든 근거를 적는다). 액션 메이저 판을 올리는 것은 남의 기본값을 받는 일이라 **무엇이 바뀌는지 릴리스 노트를 보고 정한다.** **강제 지점**: 없다 — CI 가 그 자리다. 올린 뒤 **푸시해서 초록을 확인하는 것**이 유일한 검증이고, 그 사실을 이력에 적는다. **건드리는 자리**: `.github/workflows/ci.yml` 하나. 코드는 안 건드린다. **`2c` 를 흡수하지 않는다** — 그 청크는 CI 를 세운 것이고 이건 유지보수다 | 2c |
| 5e-1 | 이메일 변경 확인 | **`Q13` 이 이메일 정정을 열면서 남긴 것이다**(`R28`). 지금은 바꾸는 즉시 반영돼서, 오타를 넣으면 그 계정으로는 알림을 못 받는다. **지금은 손해가 없다** — 보낼 채널이 없어서 알림 자체가 없기 때문이고, 채널이 서는 순간 그 구멍이 생긴다. 새 주소로 확인 메일을 보내고 눌러야 바뀌게 한다. **비밀번호 재확인은 이미 받는다** — 이 청크가 더하는 것은 「그 주소가 진짜 그 사람 것인가」다. **축**: **법**(`R28` 정정 요구), `D14`. **강제 지점**: 확인 전에는 `app_user.email` 을 안 바꾼다 — 대기 주소를 다른 칸에 두고 확인 때 옮기면, 오타를 넣어도 받던 주소가 살아 있다. **건드리는 자리**: 신설 `email_change_request` 표. 수정은 `AccountService` 와 `54b` 의 발송기 | 54b, 5e |
| 5i-1 | ~~보존분 분리 저장~~ | **완료.** `V39__payment_card_split.sql`·`RetainedColumnsTest`(신설), `PaymentService`·`OrderQuery`·`TransactionPurgeService`·`TransactionPurgeBatch`·`PaymentServiceTest`·`TransactionPurgeServiceTest`·`data-lifecycle.md`·`commerce-compliance.md` 수정. **5년을 사는 개인정보는 카드 정보 하나였다** — 주문·항목은 금액·상품명이라 거래 사실이고 사람은 `user_id` 연결뿐인데 그 계정의 식별 정보는 탈퇴 5일 뒤에 비워진다(`5i`). **분리를 표를 가르는 것으로 했다**(사용자 선택) — `order_shipping` 이 이미 검증한 수단이고, 아카이브 스키마는 조회 경로가 둘이 되고 참조 무결성이 끊긴다. 수명을 배송지와 맞췄다(거래 종료 + 6개월). **카드가 사라져도 대금결제 기록은 5년을 채운다** — 금액·승인번호·수단·시각이 `payment` 에 남아서 제6조제1항이 요구하는 것을 안 잃는다. **강제 지점은 컬럼 목록 대조다** — 「카드 결제면 카드 정보가 있다」는 파기가 그 불변식을 정상적으로 깨서 제약으로 못 드는 대신, `RetainedColumnsTest` 가 5년 보존 표 여섯의 컬럼을 못박아 **새 컬럼이 생기면 거래 사실인지 사람 정보인지 고르게** 만든다. **드러난 것**: 자유 텍스트 넷(`order_status_history.reason`·`refund.request_reason`·`decision_reason`·`order_item.withdrawal_restriction_reason`)은 사람이 쓴 글이라 개인정보가 섞일 수 있다 — `5i-2` 로 세웠다 | 완료 |
| 5i-2 | ~~보존 표의 자유 텍스트 — 환불 사유~~ | **완료.** `V48__refund_note.sql`·`RefundNoteSchemaTest`(둘 다 신설), `RefundService`·`RefundQuery`·`TransactionPurgeService`·`TransactionPurgeBatch`·`RetainedColumnsTest`·`RefundSweeperTest`·`data-lifecycle.md`. **원래 넷을 한 청크로 잡았는데 열 파일이 넘어서 둘로 갈랐다**(사용자 선택) — 환불 사유 둘이 여기, 전이 사유가 `5i-3`. **환불이 먼저인 이유**는 `request_reason` 이 **구매자가 직접 쓰는 유일한 칸**이라 연락처가 섞일 위험이 제일 크다. **수명을 갈라 다른 표로 밀었다**(사용자 선택) — `refund_note` 를 만들고 거래 종료 + 6개월에 지운다. `order_shipping`·`payment_card`·`notification_body` 가 쓴 수단 그대로다. **`reason_code` 는 안 옮겼다** — `check` 로 닫힌 열거값이라 사람 글이 안 들어오고 「무슨 사유로 몇 건」은 그 값이 답한다. **옮기면서 잃을 뻔한 것이 있다** — 「반려에는 사유가 필요하다」가 `check` 였는데 컬럼이 표를 넘으면서 한 행 안에서 안 끝나게 됐다. **지연 제약 트리거로 내렸고**, 파기가 여섯 달 뒤 그 행을 지우는 것을 막지 않도록 **`refund_note` 에는 안 걸었다** — 검사할 시점은 반려하는 순간이지 그 뒤가 아니다. **넷 중 하나는 원래부터 자유 텍스트가 아니었다** — `order_item.withdrawal_restriction_reason` 은 `check` 로 닫혀 있다 | 완료 |
| 5i-3 | ~~보존 표의 자유 텍스트 — 전이 사유~~ | **완료.** `V49__order_status_history_note.sql`·`HistoryNoteSchemaTest`(둘 다 신설), `OrderStatusService`·`TransactionPurgeService`·`TransactionPurgeBatch`·`RetainedColumnsTest`·`OrderStatusHistorySchemaTest`·`RefundSweeperTest`·`data-lifecycle.md`. **예보보다 작았다** — 「읽는 자리 셋」을 적었는데 **`reason` 을 읽는 자리가 하나도 없다.** 화면도 조회도 안 쓰고 쓰기만 했다 — **평소에 아무도 안 보는 글이 5년을 살던 것**이라 옮기기에 제일 좋은 모양이었다. `order_status_history_note` 로 떼고 거래 종료 + 6개월에 지운다(`5i-2` 와 같은 수단). **옮기면서 잃을 뻔한 것이 또 있었다** — 「관리자 강제 전이에는 사유가 필수」가 `check` 였는데 표를 넘으면서 한 행 안에서 안 끝나게 됐다. **지연 제약 트리거로 내렸고**, 파기를 막지 않도록 note 쪽에는 안 걸었다. **`5i-2` 가 못 고친 자바독을 여기서 고쳤다** — `RetainedColumnsTest` 의 「자유 텍스트 넷」 주석이 백틱 표기가 달라서 치환이 조용히 실패했었다. **이제 5년 표에 자유 텍스트가 없다** — 남은 `order_item.withdrawal_restriction_reason` 은 `check` 로 닫힌 열거값이라 처음부터 자유 텍스트가 아니었다 | 완료 |
| D2-4 | ~~중개자 책임 문안 수정~~ | **완료.** `V28__liability_wording.sql`(신설 — 약관 제2판·청약철회 안내 제2판), `seller-identity.tsx` 수정, 테스트 6개 고침(전체 647, 실패 0). **고치는 이유가 무효 위험이었다** — 약관 제2조의 「책임은 판매자에게 있습니다」가 약관규제법 제7조1호(법률상 책임을 배제하는 조항은 무효)에 걸린다. 전자상거래법 제20조의2제3항이 **못 면한다**고 못박은 책임을 면하는 것처럼 읽혔다. **「당사자가 아니다」는 안 지웠다** — 제20조제1항이 그 고지를 법으로 요구해서, 지우면 그쪽을 어긴다. **더하는 것이지 무르는 것이 아니다.** 「누가 무엇을 책임지나」를 표로 나눴다(사용자 선택) — 제13조제2항8호가 요구하는 것이라 한 문장으로 뭉개면 반쯤 채운 상태가 된다. **재동의는 안 받는다**(사용자 선택) — 우리 책임을 넓히는 변경이라 불리한 변경이 아니고 `current_consent` 가 코드로 묶어서 옛 판 동의도 잡힌다. **청약철회 안내의 기산점을 `12a-3` 과 맞췄다** — 문안이 둘만 적는데 코드는 셋을 알고 있었다. **`R16` 이 약관규제법 제7조를 안 들고 있었다** — `D2-3` 이 전자상거래법만 통독한 한계가 바로 드러났다 | 완료 |
| 14c | ~~공급시기 약정 고지~~ | **완료.** `V35` 신설, `ProductQuery`·`CartService`·`OrderService` 수정, 상품 상세와 주문서에 고지, `ShipDeadlineTest` 2개. **제15조제1항 단서의 「따로 약정한 것」은 고지가 성립 요건이다** — `11-6` 이 값을 받는 자리를 냈지만 안 그려서 그 날수가 법적으로 안 섰다. **약정이 없어도 그린다** — 법정 기한이 걸린다는 것도 거래조건이다(제13조제2항 4호). **「영업일」이라고 밝힌다** — 우리 계산이 `BusinessCalendar` 라 「5일」로만 쓰면 주말이 끼어 이레가 되고 화면과 실제가 갈린다. **`Q2` 가 넘긴 것을 같이 쳤다** — `seller_order.agreed_lead_days`. 계산 결과인 `supply_lead_days` 로는 「약정 3영업일」과 「약정 없어 법정 3영업일」이 안 갈리고, 그러면 「이 주문에 약정이 있었나」에 답하려고 상품의 **지금** 값을 봐야 한다. **뷰를 또 빠뜨렸는데 `Q2` 의 컬럼 대조 테스트가 잡았다** — 세운 다음 첫 마이그레이션에서 바로 걸렸다 | 완료 |
| ~~11-6 원안~~ | 공급 기한 | 결제 시점에 `seller_order` 에 공급 기한을 박제하고 넘긴 것을 조회로 드러낸다. **선지급식은 대금 지급일부터 3영업일**이다(`D2` R21, 전자상거래법 제15조제1항). **중개자 고지로 안 빠져나간다** — 제20조의2제3항이 제12조부터 제15조까지를 열거한다. 지금 `seller_order` 는 청약철회·자동확정·종료 셋을 박제하면서 **이것만 없어서** 셀러가 `preparing` 에 무한정 둘 수 있다. `11-2` 의 `freezeDeadlines` 와 같은 모양이고 박제 시점만 배송완료가 아니라 결제다 | 12-2, 11-2, D10, D2 |
| 15-4 | ~~계약내용 서면~~ | **완료.** `V27__order_contract.sql`·`OrderContractTest`(신설), `OrderService`·`OrderQuery`·`ConsentSchemaTest` 수정, 테스트 14개(전체 645, 실패 0). **주문이 그때의 판을 가리킨다**(사용자 선택 — `order_contract_document` 표). 대상이 두 표에 걸쳐 있어(`policy_document`·`consent_item`) `shop_order` 컬럼으로 두면 서로 다른 표를 가리키는 외래키가 나란히 붙고 문서가 늘 때마다 5년 보존 표에 마이그레이션이 들어간다. **호(5·6·8·9)로 담는다** — 법이 정한 것은 호지 우리 문서 구성이 아니라, 문서를 쪼개거나 합쳐도 값이 안 바뀐다. **약관도 다시 박제한다** — `user_consent` 는 가입 시점이라 가입 후 개정되면 주문 시점 약관이 없다. **불변 트리거가 이 설계의 전제였다**(법령 재확인에서 나왔다) — 전자문서법 제4조의2 2호가 「저장된 때의 형태로 보존」을 서면 요건으로 정해서, 본문을 고칠 수 있으면 교부했다는 서면이 서면이 아니다. `V21`·`V11` 이 주석으로만 적어 둔 것을 트리거로 내렸다. **시행 전인 판은 연다**(안 열면 오타 하나에 판이 하나씩 쌓인다). **8호 문서를 세웠다**(사용자 선택) — 법정 사실만 적고 없는 고객센터는 없다고 밝힌다(`13a-2` 의 선). **기존 테스트가 증명하던 외래키를 트리거가 가려서** 시행 전 판으로 그것을 다시 증명하는 테스트를 뒀다. 원래 설명: 주문 시점의 계약 조건을 그 주문에 남기고 | 완료 |
| 15-5 | ~~거래기록 보존·청약 전 표시 고정~~ | **완료.** `OrderRecordText`·`OrderRecordTextTest`·`checkout/summary.tsx`·`summary.test.tsx`(신설), `OrderController`·`orders/[orderNumber]/page.tsx`·`checkout/page.tsx`·`HttpFlowTest`·`api-guidelines.md`·`commerce-compliance.md` 수정. **둘을 닫았다.** 하나 — 제6조제1항 **후단**(보존)이 비어 있었다. `GET /api/orders/{번호}/record` 가 거래기록을 글 파일로 내리고(`Content-Disposition: attachment`) 주문 상세에 내려받기 자리를 냈다. **판정을 다시 안 한다**(사용자 선택: 서버가 파일을 만든다) — 입력이 주문 상세와 같은 조회라 남의 주문을 파일로 받는 두 번째 경로가 안 생긴다. 둘 — `R19` 의 강제 지점이 화면 코드뿐이었다. 청약 전에 보여야 하는 조각을 `checkout/summary.tsx` 로 빼고 **항목·수량·금액·배송비·상품 합계·총액·판매자 신원을 vitest 가 못박는다**. **상태 문구가 두 벌이 됐다** — 화면은 `lib/order-text.ts`, 문서는 서버 표다. 글자는 갈려도 되지만 **덮는 코드 집합**이 갈리면 새 상태가 한쪽에서 이름을 잃어서, 백엔드 테스트가 프론트 파일을 읽어 그 집합을 대조한다. `D5` 에 「파일 응답」 절을 세웠다 | 완료 |
| 12a-4 | ~~지연배상금~~ | **완료.** `V47__refund_delay_interest.sql`·`DelayInterestTest`(둘 다 신설), `RefundService`·`RefundQuery`·`RetainedColumnsTest`·`money-rules.md`. **이율을 원문에서 확인했다** — 시행령 제21조의3, **연 100분의 15**. 기준은 「3영업일을 **초과한** 시점부터 조치 시점까지의 기간」이고 `due_at` 은 `12a-3` 이 사유별 기산점으로 이미 박아 뒀다. **법이 「이자를 더한 금액의 환급 조치」라고 해서 계산이 PG 호출보다 먼저다** — 기록만 하고 안 보내면 조치를 안 한 것이다. **시각을 한 번 잡아 이자와 `decided_at` 이 같은 순간을 쓰게 했다.** **원 미만은 올린다**(사용자 선택) — `D8` 은 버림인데 그것은 **우리가 받는 돈**의 규칙이고 이것은 **물어 주는 돈**이라 방향이 반대다. `D8` 절사 절에 예외를 세웠고 **규칙은 여전히 하나다: 우리에게 불리한 쪽으로 자른다**. **하루가 안 찼어도 1일로 센다**(사용자 선택) — 한 시간 늦은 것에 0원을 물리면 「늦었는데 배상금이 0」이 된다. **`RetainedColumnsTest` 가 그 자리에서 걸렸다** — 5년 사는 표에 컬럼이 늘어서 거래 사실인지 사람 정보인지 고르게 만들었다. 금액이라 목록에 더했다 | 완료 |
| 12a-5 | ~~환불 승인 대기가 기한을 먹는다~~ | **완료.** `V51__refund_system_approval.sql`(신설), `RefundService`·`RefundSweeper`·`RefundSweeperTest`·`RetainedColumnsTest`·`RefundServiceTest`·`NotificationSweeperTest`·`batch-catalog.md`·`commerce-compliance.md` 수정. **셋 중 ①을 골랐다**(사용자 선택) — 시스템이 만든 요청은 시스템이 승인한다. 근거는 **법이 요청·승인 2단계를 요구하지 않는다**는 것이다(제18조제2항은 3영업일만 본다). 대기는 `12a-1` 이 만든 상태고 법은 그것을 기한에서 빼 주지 않는다. 스위퍼가 만든 요청은 **전량 환불**이라 사람이 판단할 여지가 애초에 없다. **강제 지점이 제약으로 내려갔다** — `refund_decision_check` 가 「승인에는 사람이 있어야 한다」를 박아 놔서(`V23`) 시스템 승인이 통과를 못 했다. 사람이 아니라 **승인자 종류**를 보게 바꾸고(`approved_by_type`, `V25` 가 요청자에서 한 것과 같은 모양), `refund_system_approval_scope_check` 로 **시스템은 자기가 만든 요청만** 승인하게 막았다 — 앱 조건으로만 두면 새 입구가 빠뜨린다. **제약이 그 자리에서 셋을 물었다** — `RefundSweeperTest.reject`·`RefundServiceTest`·`NotificationSweeperTest` 의 픽스처가 `approved_by_type` 없이 처리 상태를 만들고 있었다. **오늘 결정끼리 부딪친 자리 하나** — `RefundSweeperTest.waitsForApproval` 이 「돈이 나가는 결정은 사람이 한다」를 근거로 박아 둬서 이번 결정과 정면으로 어긋났다. 테스트는 초록이었을 것이다(만드는 회차만 보므로) — 근거를 고쳐서 만드는 회차와 승인 회차가 갈린 것으로 다시 적었다. **계량을 같이 넣었다** — `reportOverdue` 가 기한 넘긴 대기 수를 회차마다 `WARN` 으로 남긴다. **예보는 「회차에 남긴다」였는데 `batch_run` 이 아니라 로그로 갔다**(1차 마무리가 잡았다) — `RefundSweeper` 는 5분 `fixedDelay` 라 `batch_run` 의 `(배치명, 기준일)` 모델에 안 맞는다. 그 표는 기준일당 성공을 **하루 한 줄**만 담아서, 5분마다 세는 값을 넣을 자리가 없다. 지연배상금은 **승인하는 순간에만** 계산돼서(`RefundService.delayInterest`) 대기 중인 건의 이자는 어느 컬럼에도 안 쌓인다. **그 수가 사람에게 닿는 경로는 여전히 없다**(`A09` 와 같은 자리) | 완료 |
| 13-0 | ~~디자인 취향 스킬 설치~~ | **완료.** `.agents/skills/design-taste-frontend/SKILL.md`(1206줄) + `skills-lock.json`. 실행 스크립트가 없는 지침 문서 하나다. **저장소 안에 깔렸다** — `~/.claude` 가 아니라 프로젝트 `.agents/` 고, 본체와 잠금파일을 커밋했다(사용자 선택). `.claude/` 는 gitignore — 심볼릭 링크가 `core.symlinks=false` 때문에 절대경로 텍스트로 들어간다. **어디까지 쓸지를 `D20` 에 세웠다** — 기본은 쓰는 것이고, 주문 흐름·셀러 화면에서만 `§9`·`§4`·`§6` 로 좁힌다. 다이얼은 `trust-first` 줄(`3-4/2-3/4-5`)이고 값은 청크 13 이 확정한다. 스택 기본값은 우리 결정이 이긴다(`D23` 축 1). 대비비 4.5:1 은 스킬이 아니라 법에서 온 것이라 스킬을 안 써도 남는다. **이전 설명**: `npx skills add https://github.com/Leonxlnx/taste-skill --skill "design-taste-frontend"`. 생성형이 기본으로 뱉는 CSS 를 잡아 준다. **화면을 만들기 전에 깐다** — 다 만들고 깔면 이미 나온 스타일을 되돌리는 일이 된다. **팔레트가 대비비 4.5:1 을 통과하는지 여기서 본다**(`D20` 접근성) — 색을 고르고 나서 대비를 맞추면 팔레트를 다시 고르게 된다. 설치만 하는 청크라 코드가 안 생긴다 | 완료 |
| 13-1 | ~~프론트 기반~~ | **완료.** Next 16(App Router·TypeScript·Tailwind v4), `next.config.ts` 의 `/api/:path*` rewrite 프록시, `eslint-plugin-jsx-a11y` recommended 전체, `<html lang="ko">`, 백엔드 `internal-proxies` 를 루프백으로 좁힘. **화면은 안 만들었다.** 프록시는 실제로 띄워서 확인했다 — 3000 경유가 8080 직접과 같은 JSON 을 주고 401 도 그대로 온다 | 완료 |
| 13-2 | ~~로그인 화면~~ | **완료.** `src/lib/api.ts`(표기 변환·CSRF·오류를 `ApiError` 로), `/login`(서버 컴포넌트 + 클라이언트 폼), 색 토큰과 한글 글꼴(`Noto Sans KR`). **CSRF 를 래퍼가 같이 맡는다** — 안 실으면 로그인이 401 이라 화면만 만들면 안 돌아간다(원래 `5b` 자리였고 흡수했다). **키만 바꾸고 값은 안 바꾼다** — `allowed_actions` 의 값이 열거값이라서다. 프록시 경유로 CSRF→로그인→세션 유지→실패 문구까지 밟아 확인했다 | 완료 |
| 13c | ~~앱 셸과 화면 지도~~ | **완료.** `D20` 에 화면 지도(경로 14개)·자리표시 규칙·셸 절, `SiteHeader`·`SiteFooter`·`ComingSoon`, 자리표시 5개(`/products`·`/cart`·`/signup`·`/terms`·`/privacy`), 건너뛰기 링크. **지도를 그리다 빠진 것 다섯이 나왔다** — 회원가입·마이페이지·셀러 상품·셀러 주문 화면과 상품 상세 API 가 분할표에 아예 없었다(`13d`·`13e`·`13f`·`13g`·`8b` 신설). **로그인 화면의 「회원가입」 링크를 되돌렸다** — 자리표시가 있어서 이제 거짓 링크가 아니다 | 완료 |
| 13c-1 | ~~셀러 화면군의 자리와 공용 쪽 넘김~~ | **완료.** `components/pager.tsx`·`pager.test.tsx` 신설, `app/seller/layout.tsx` 신설, `products`·`orders` 목록이 그것을 쓴다. **`13g` 를 열다가 나온 청크다** — `Pager` 사본이 둘인데 셀러 목록이 셋째를 만들 자리였고, **이미 어긋나 있었다**(「색에만 기대지 않는다」 주석이 한쪽에만 남아 있었다). `pageNumberOf` 도 두 벌이었다. **공용본은 `params` 를 받는다** — 안 받으면 셀러가 셀러 하나로 걸러 놓고 2쪽을 눌렀을 때 거름이 풀린 목록이 나간다. **밀도 경계를 세웠다**: `D20` 이 `VISUAL_DENSITY` 를 구매자 5·셀러 7 로 정하고 「경계를 코드에서 가른다」고 적어 뒀는데 그 경계가 없었다. 경로 접두어가 이미 `/seller` 라 **라우트 그룹을 안 팠다**. **강제 지점은 구조다** — 사본이 하나면 어긋날 자리가 없다. 다만 데모 데이터로는 한 쪽에 다 들어가서 화면에서 `null` 만 나오므로 **검증은 테스트가 진다**(6케이스) | 13c |
| 13c-2 | ~~주문 화면의 공용 어휘를 한 자리로~~ | **완료.** `lib/order-text.ts` 신설, `lib/format.ts` 에 `priceText`, `orders/status.ts` 는 관객별 라벨만 남김, 부르는 곳 아홉 수정. **`13g` 를 열다가 범위가 늘었다** — 처음엔 `priceText` 사본 다섯이었는데 세어 보니 **여덟 자리**였다(이름 있는 사본 여섯 + `checkout-form.tsx` 의 인라인 둘). 상태 문구·`actionPath` 도 `app/orders/` 안에 갇혀 있어서 셀러 화면이 **라우트를 가로질러** 불러야 했다 — `lib/format.ts` 가 `13e` 에 「거기 두고 가져다 쓰면 화면 하나가 다른 화면군의 조각을 부르는 모양이 된다」고 적어 둔 바로 그것이다. **관객이 갈리는 것만 화면군에 남겼다**: 같은 묶음을 두고 사는 사람은 「반품 신청」을, 셀러는 「반품 완료」를 한다. `returnReasonText` 를 새로 넣었다(`11c-2c` 가 계약에 실은 것을 그릴 자리). **강제 지점은 구조다** — 사본이 없으면 어긋날 자리가 없다. 순수 이동이라 테스트를 새로 안 붙였고, `next dev` 로 상품·주문 목록·주문 상세를 띄워 금액·상태·동작 라벨이 그대로인 것을 봤다 | 13c-1 |
| 13d-1 | ~~동의 항목 공개 목록~~ | **완료.** `GET /api/consent-items`(목록), `ConsentService.listCurrent`, `V11` 에 `sort_no` 추가, `MeConsentTest` 5개. **가입 화면이 무엇을 물어야 하는지 알 방법이 없었다** — 있던 것은 `{code}` 하나짜리 조회뿐이라 화면이 코드 다섯을 박게 되고, `V11` 이 「코드가 아니라 데이터로 둔다」고 한 뜻이 사라진다. **본문을 안 내린다** — 약관 전문이 항목 수만큼 딸려 나온다. 펼칠 때 `{code}` 로 따로 받는다. **정형 넷은 내린다** — 동의받는 그 자리에서 목적·항목·기간·거부 시 불이익을 보여야 한다(개인정보법 제15조②). **`D5` 목록 규약(`items`·`page`·`size`)을 안 쓴다** — 쪽으로 자르면 2쪽의 필수 항목을 안 보여준 채 동의를 받는다. **정렬을 서버가 정한다**(사용자 선택) — 필수 먼저, 그 안에서 `sort_no`. **`sort_no` 를 새로 넣은 이유는 개정이다** — 기본키 순으로 두면 개정할 때 새 행의 id 가 커져서 **그 항목이 목록 끝으로 간다.** 이름순도 안 된다(「개인정보 수집」이 「이용약관」 앞). **종속이 부모 뒤에 오는 것은 스키마로 못 건다** — 다른 행을 봐야 해서 `check` 에 안 들어간다. 테스트가 유일한 방벽이고 **`sort_no` 를 25 로 낮춰서 진짜 깨지는 것을 확인했다.** `list(userId)` 정렬도 같이 맞췄다 — 갈리면 같은 항목이 화면마다 다른 자리에 뜬다 | 5-0, 5j, 5k |
| 13d-2 | ~~회원가입 화면~~ | **완료.** `/signup` 서버 페이지 + `signup-form.tsx`(클라이언트), `components/field.tsx` 신설(로그인 폼에서 옮겨 왔다 — 그 파일 주석이 이 자리를 예고해 뒀다), `PolicyBody` 를 `policy-document.tsx` 에서 내보냈다. **항목을 화면에 안 박는다** — `13d-1` 의 목록이 준다. **본문을 서버가 그려서 넘긴다** — 마크다운 렌더러를 클라이언트로 내리면 그만큼 브라우저로 내려가는데 약관은 읽는 것이라 그럴 이유가 없다. **정형 넷은 접지 않는다** — 개인정보법 제15조②가 동의받을 때 알리라고 한 것이라 펼쳐야 보이는 자리에 두면 안 읽고 동의한 것이 된다. 접는 것은 약관 본문뿐이고 길어서다. **종속은 보이되 `disabled` 고 이유를 글로 적는다**(사용자 선택). **부모를 끄면 종속도 같이 꺼진다**(`D2` R14) — 서버가 어차피 거두지만 켜진 채로 두면 보낼 수 없는 동의가 켜져 보인다. **필수 미동의면 버튼을 못 누른다** — 누르게 두고 422 를 받으면 왕복이 헛돈다. 가입은 로그인이 아니라 `/login?signed-up=1` 로 보낸다. 실서버로 관통했다 — 체크 상태 전이 넷, 전문 펼침(강조 살아 있음), 실제 가입 후 `user_consent` 5행(`source=signup`, IP 기록)·역할 `customer`, 중복 가입 문구. 원래 설명: `/signup`. 계정·비밀번호·동의를 한 화면에서 받는다. **항목은 `13d-1` 의 목록이 준다** — 코드를 화면에 박지 않는다. **약관 본문은 `<details>` 로 그 자리에서 펼친다**(사용자 선택) — 브라우저가 키보드·보조기술 동작을 기본으로 주므로 초점 가두기를 직접 안 만든다. 펼칠 때 `{code}` 로 본문을 받아 마크다운 강조를 살려 그린다(`13a-2` 의 `PolicyDocument` 와 같은 렌더러, `D20` 약관규제법 제3조①). **종속 항목은 보이되 누를 수 없게 하고 이유를 글로 적는다**(사용자 선택) — 숨기면 무엇을 더 켤 수 있는지 모르고, 이유 없이 회색이면 고장으로 본다. 필수 미동의와 야간 종속을 화면이 먼저 막고 서버가 다시 막는다(`5-2`). 비밀번호는 **15자**다(`D14-1`). `13c` 가 둔 `/signup` 자리표시를 지운다. **`R16` ①③ 의 마지막 칸이 이 화면이다**(점검 A) | 13d-1, 13a-2, 13c, 5-2, D2, D20, D24 |
| ~~13d 원안~~ | 회원가입 화면 | **둘로 쪼갰다**(위). 목록 API 가 없어서 백엔드 작업이 딸려 왔다. 원래 설명: `/signup`. 계정·비밀번호·동의를 한 화면에서 받는다. **약관 본문은 `13a-1` 가 쓴 것을 `5k` 의 조회 API 로 가져와 그린다** — 화면에 문구를 박으면 개정판이 갈린다. 필수 미동의와 야간 수신 종속을 화면이 먼저 막고, 서버가 다시 막는다(`5-2`). 마크다운 강조를 살려서 그린다(`D20` 약관규제법 제3조). **`R16` ①③ 의 마지막 칸이 이 화면이다**(점검 A) | 13a, 13c, 5-2, 5k, D2, D20 |
| 13e | ~~마이페이지 화면~~ | **완료.** `me/page.tsx`·`me/account-forms.tsx`·`me/consent-list.tsx`·`me/withdraw/page.tsx`·`me/withdraw/withdraw-form.tsx`·`lib/format.ts` 신설, `site-header.tsx`·`login/page.tsx`·`signup-form.tsx`·`orders/status.ts`·주문 화면 둘 수정, `D20` 화면 지도에 `/me/withdraw` 추가. **법 요건 둘을 닫았다** — `R28`(열람등요구를 수집보다 어렵지 않게)과 `R24` 4호 나목(가입과 다른 방법으로만 탈퇴). 처리방침 제7절의 「마이페이지에서 직접」이 이제 사실이다. **결정 넷을 사용자가 골랐다** — 화면은 `/me` 와 `/me/withdraw` 둘(탈퇴만 자기 화면을 갖는다), 머리 링크는 지금 건다, 처리정지·이의제기는 방침 제7·9절을 가리키는 문구로 둔다, 탈퇴 뒤에는 `/login?reason=withdrawn`. **`13b` 의 머리 링크를 흡수했다**(사용자 선택) — 화면만 만들고 링크를 안 걸면 주소를 아는 사람만 열람·탈퇴를 하게 돼서 제38조제4항이 안 닫힌다. 로그인/로그아웃 갈림과 권한 가리기는 `13b` 에 남았다. **드러난 것 둘을 같이 쳤다** — 가입이 `?signed-up=1` 로 보내는데 로그인 화면이 `reason` 만 읽어서 **가입한 사람에게 아무 말도 안 하고 있었다**(`reason=signed-up` 으로 통일), `dateText`·`dateTimeText` 에 두 번째 사용자가 생겨 `lib/format.ts` 로 올렸다(사본이 셋인 구간을 안 만든다). **안 한 것**: 처리정지(제37조)·이의제기(제38조제5항) API, `_visible_field_groups` 의 빈 배열이 「전부 본다」인지(`13b`). 화면은 키의 유무로만 가른다. 검증은 `npm run build`·`npm run lint` 까지고 **HTTP 로는 안 밟았다**(Docker 데몬이 안 떠 있었다) | 완료 |
| 13f | ~~셀러 상품 화면~~ | **완료.** `app/seller/products/page.tsx`·`app/seller/products/new/page.tsx`·`lib/product-text.ts`·`lib/product-text.test.ts`(전부 신설), `components/site-header.tsx`. **분할표가 없는 입구를 전제했다** — 「등록·수정·내림」을 적었는데 `/api/seller/products` 는 **목록만 내린다**. 화면만 그리면 눌러도 아무 일이 안 나는 폼이 되므로 **목록은 진짜로 만들고 등록은 `ComingSoon` 으로 뒀다.** 그것이 `ComingSoon` 의 **첫 사용자**고, `13c` 가 만들어 두고 안 써서 `D20` 의 「아직 없는 화면은 준비 중으로」가 셀러 경로에 한 번도 안 걸려 있던 것을 여기서 걸었다. **밀도 7 이라 표다** — `13g` 의 받은 주문과 같은 모양이고 컨테이너를 다시 안 만든다. **상태는 색으로만 안 가른다**(WCAG 1.4.1) — 막힌 것은 글자로도 적고, 「검수 대기」와 「판매 차단」이 둘 다 안 팔리는데 **할 일이 정반대**라 줄마다 안내를 붙였다. **머리의 링크 판정을 주문과 갈랐다** — 같은 역할에 붙어 있다고 한 검사로 묶으면 역할을 쪼갤 때 안 보이는 링크와 보이는 화면이 어긋난다. **강제 지점**: `npm run lint` 의 접근성 규칙 + 상태 문구를 vitest 로 고정. 데모 데이터의 상품이 전부 판매 중이라 **검수 대기·차단은 눈으로 못 밟는다** | 완료 |
| 13f-1 | ~~셀러 상품 등록·수정 입구~~ | **완료. `13g-1` 을 흡수했다**(사용자 선택) — 같은 폼·같은 컨트롤러라 따로 치면 두 번 연다. `V50__product_substantiation.sql`·`app/seller/products/new/product-form.tsx`·`ProductSubstantiationTest`(전부 신설), `ProductService`·`ProductController`·`SellerQuery`·`SellerController`·`app/seller/products/new/page.tsx`·`data-lifecycle.md`. **`13f` 가 적은 전제가 틀렸다** — 「서버에 만드는 입구가 없다」고 했는데 `/api/products` 에 `POST`·`PUT`·`submit-review` 가 **다 있었다.** `SellerProductController` 만 보고 판단한 결과고, 그래서 이 청크는 화면 + 실증자료가 됐다. **`ComingSoon` 을 지웠다** — 그것이 `13f-1` 의 목적이었다. **실증자료를 등록 시점에 받는다**(`R32`, 표시광고법 제5조) — 요청이 오면 15일 안에 내야 하는데, 등록 때 안 받으면 그 문구를 쓴 사람이 떠난 뒤에 근거를 찾게 된다. **주장 하나에 근거 하나**라 한 칸에 안 섞는다. **없어도 안 막는다** — 제5조는 「요청이 오면 낼 수 있어야」지 「모든 상품에 있어야」가 아니다. **`R33`(중요정보 고시)은 조건으로 미뤘다**(사용자 선택) — 고시가 품목별로 항목을 정하는데 **상품에 카테고리 컬럼이 없어서 강제할 축이 없다.** **어느 셀러로 올리나를 서버가 정한다** — 신설 `GET /api/me/sellers`. 화면이 셀러 번호를 고르게 두면 남의 번호를 넣어 볼 수 있고, 막는 것이 화면이 되면 판정이 두 곳이 된다. **폼은 옵션 없는 상품까지다** — 옵션 조합을 화면에서 만드는 것은 그 자체로 한 청크다. 눌러도 아무 일이 안 나는 폼보다 좁게 도는 폼이 낫다 | 완료 |
| 13g | ~~셀러 주문 화면~~ | **완료.** `seller/orders/page.tsx`(목록)·`[sellerOrderNumber]/page.tsx`(상세)·`actions.ts`(라벨 표) 신설, `site-header.tsx`·`site-header.test.tsx` 수정, `order-actions.tsx` 를 `components/` 로 옮김, `orders/status.ts`·`orders/[orderNumber]/page.tsx` 가 그것을 쓴다. **목록이 답하는 물음은 「무엇부터 보내나」**라 발송 기한이 줄마다 있다(`11c-2c` 가 실은 값). 늦었는지는 뷰가 답하고(`V37`) 화면이 날짜로 다시 안 잰다. **밀도 7 이라 카드가 아니라 표다** — 좁은 화면에서 가로로 밀고 칸을 안 접는다(접으면 기한과 상태가 다른 줄로 간다). **반품 사유를 화면 위쪽에 둔다** — 하자면 반환 비용이 셀러 몫이고 기한이 3개월이라(제17조제3항·제18조제9항) 묻어 두면 못 보고 거절한다. **동작 러너를 공용으로 뺐다** — 셀러 버튼을 새로 쓰면 100줄짜리 사본 둘이 된다. 절차는 하나가 지고 **라벨 표만 관객별**이다(사는 사람의 「반품 신청」과 셀러의 「반품 완료」). **머리는 `order:update_status` 로 가른다** — 역할 이름을 화면에 안 박고(`D24`), `/api/me` 대신 `/api/me/permissions` 하나만 불러 요청이 두 번이 되는 것을 막았다. **드러난 것**: `notFound()` 가 200 으로 나간다 — 기존 화면도 그랬고 주석은 아니라고 적혀 있었다(정보 처분, `stack.md`) | 13c-2, 11c-2b, 11c-2c, 11-2, D20 |
| 13g-1 | ~~상품 문구의 실증과 필수 항목~~ | **완료 — `13f-1` 이 흡수했다**(사용자 선택). 같은 폼·같은 컨트롤러를 두 번 열지 않으려는 것이고, `13f-1` 행에 무엇을 했는지 적었다. **`R32`(실증)는 닫혔고 `R33`(중요정보 고시)은 조건으로 남았다** — 고시가 품목별로 항목을 정하는데 상품에 카테고리 컬럼이 없어 **강제할 축이 없다.** 조건: **카테고리 축이 서면** | 완료 |
| 8b | ~~상품 상세 API~~ | **완료.** `ProductQuery.findPublicDetail`, `GET /api/products/{id}`, `WithdrawalRestrictionReason` enum, 테스트 10개. 조건이 목록과 같다 — 갈리면 목록에 없는 상품이 상세로 열린다. **재고는 수량이 아니라 있고 없고만 내린다.** 청약철회 제한 사유는 법이 고지를 요구해서 공개로 나가고 enum 을 지난다(`D2` R4). **보안 함정 하나를 밟을 뻔했다** — 같은 경로에 `PUT`·`DELETE` 가 있어서 경로만 열면 비로그인이 상품을 고치고 지운다. `GET` 만 열었고 그 조건을 테스트가 지킨다 | 완료 |
| 13b | ~~화면 권한 반영~~ | **완료.** `VisibleFieldGroups` 신설, `AccountService`·`OrderQuery`·`SellerOrderQuery`·`PermissionCatalog` 수정, `site-header.tsx` 재작성, `logout-button.tsx`·`site-header.test.tsx` 신설, `api-session.ts` 에 `apiSessionOptional`. **머리가 누구에게나 「로그인」을 보여줬다** — 주문서까지 온 사람에게도 그랬다(`15-2`). 로그인하면 로그아웃·내 정보·내 주문이 뜬다. **비로그인에게 「내 주문」을 안 그린다** — 누르면 튕기는 링크는 갈 곳이 있는 것처럼 보이게 하는 것이다(`D20`). **세션 쿠키 유무로 안 가른다**(사용자 선택) — 만료된 세션도 쿠키는 남아서 「로그아웃」을 그려 놓고 누르면 튕긴다. 대신 **401 을 `null` 로 돌려주는 입구**를 냈다(머리는 모든 화면에 있어서 보통 입구를 쓰면 공개 화면이 공개가 아니게 된다). **`_visible_field_groups` 의 빈 배열이 「다 보인다」와 「아무것도 안 보인다」 둘을 뜻하던 것을 닫았다**(사용자 선택) — 제한이 없으면 그 자원의 그룹을 **전부** 싣는다. 판정 엔진 안에서는 `Allowed` 가 타입으로 갈라 뒀는데 **직렬화하면서 뭉개진** 자리다. 권한 목록은 자원 이름이 문자열로 돌아서 **표를 읽는다** — `auth` 가 `order` 를 가리키면 의존이 거꾸로 서고, 표와 enum 의 불일치는 `FieldGroupTest` 가 이미 막는다. **셀러·관리자 링크는 아직 없다** — 걸 화면이 미착수다(`13f`·`13g`·`16`) | 완료 |
| 13a-1 | ~~정책 문안과 조회~~ | **완료.** `V21__policy_document.sql`(테이블 + 문안 둘), `policy/PolicyQuery`·`PolicyController`, `PolicyQueryTest` 11개, `POLICY_NOT_FOUND`, `PUBLIC_PATHS` 에 `/api/policies/**`. **`consent_item` 과 표를 가른 이유는 성격이다** — 저쪽은 동의받고 `user_consent` 로 남는 것이고 이쪽은 알리기만 한다. 한 표에 섞으면 「동의 안 받는 항목」을 목록 조회마다 걸러야 하고, **빠뜨리면 가입 화면이 처리방침을 동의 항목으로 띄운다.** **개정판 설계는 같다**(`code`+`version`+`effective_at`) — 처리방침은 시행 7일 전 고지 의무가 있어서 「언제부터 어느 판인가」에 답해야 한다. **`code` 에 `check` 를 안 건다** — 코드가 그 값으로 분기하지 않고 그대로 조회 조건에 넣는다(`D23`), `consent_item.code` 와 같다. **청약철회 안내를 지금 썼다**(사용자 선택) — 반품 축(`43`·`44`)이 없어도 되는 이유는 **적는 것이 우리 정책이 아니라 법 조문**이라서다. 조문은 국가법령정보센터에서 원문을 확인하고 옮겼다(제17조①②③·제18조②⑨⑩). **본문에 법이 요구한 절이 있는지를 테스트가 고정한다** — 조회만 보면 문안에서 절이 빠져도 초록이다. **이용약관은 안 고쳤다** — 제3조가 이미 정확하고, 고치면 `consent_item` 에 v2 가 생겨 동의 흐름이 딸려 온다(`13d-1` 몫) | 9, 13-2, D2, D14, 5j |
| 13a-2 | ~~정책 화면~~ | **완료.** `components/policy-document.tsx` 신설(마크다운 렌더러 + 문서 셸), `/terms`·`/privacy`·`/withdrawal-guide` 셋, `site-footer` 에 청약철회 링크, `external-references.md` 에 MIT 기록. **`react-markdown` + `remark-gfm`** — 표가 CommonMark 가 아니라 GFM 이라 둘이 같이 온다. **원시 HTML 은 안 살린다**(`rehype-raw` 미사용) — 지금 본문은 우리가 쓰지만 셀러 글을 그리게 되는 날 그 결정이 방벽이 된다. **받은 props 를 펼치지 않는다** — react-markdown 이 AST 노드를 `node` 로 같이 넘겨서 그대로 펼치면 DOM 에 `node="[object Object]"` 가 요소마다 붙는다(실제로 붙었고 고쳤다). `th`·`td` 만 `style` 을 받는다 — GFM 이 표 정렬을 그것으로 넘긴다. **접근성 린트가 `{...props}` 안을 못 봐서** 제목·링크에 내용이 없다고 읽는다 — 규칙을 끄지 않고 `children` 을 명시했다. **몰 운영자 사업자 표시는 안 넣기로 정했다** — 실제 운영 사업자가 없어서 지어 넣으면 사실 아닌 고지가 된다(`D23` 「안 넣은 것도 근거를 남긴다」). 원래 설명: `/terms`·`/privacy`·`/withdrawal-guide`. **`react-markdown` 을 깐다**(사용자 선택) — 처리방침이 목록과 표를 쓰고, 직접 쓴 렌더러는 안 다루는 문법을 **오류 없이 원문 글자로 내보낸다.** 서버 컴포넌트라 브라우저로 내려가는 JS 는 0 이고, 원시 HTML 은 `rehype-raw` 를 안 붙여서 안 살아난다. **MIT 라이선스를 `external-references.md` 에 적는다**(`D23`). `/terms` 는 `GET /api/consent-items/terms_of_service`(`5k`), 나머지 둘은 `GET /api/policies/{code}`(`13a-1`) — **출처가 갈리는 이유가 동의받느냐다.** 강조를 살려 그린다(약관규제법 제3조①). `13c` 가 둔 `/terms`·`/privacy` 자리표시를 지우고 `D20` 화면 지도에 `/withdrawal-guide` 를 더한다 | 13a-1, 14b, D20, D24 |
| ~~13a 원안~~ | 정책 문안 | **둘로 쪼갰다**(위). 한 청크로는 파일이 8개라 안 떨어졌다. 원래 설명: 이용약관·개인정보처리방침·청약철회 안내를 화면에 붙일 문서로 쓴다. **이용약관은 처음 쓰는 것이 아니다**(점검 A) — `V11` 시드의 `terms_of_service` 가 이미 초안이고 제2조에 중개자 지위 고지(`R2`)가 들어 있다. **그것을 고쳐 쓴다** — 모르고 새로 쓰면 같은 문안이 두 벌이 되거나 문구가 갈린다. 캡차가 사용자 데이터를 구글로 보내므로 제3자 제공을 처리방침에 적는다(`D14`). **자동수집장치(쿠키) 절을 쓴다** — 비로그인 장바구니가 `CART-TOKEN` 으로 사람을 구분한다(청크 9). 설치·운영 목적과 **거부 방법**까지 적는다. 동의는 안 받아도 되지만(개인정보법 제15조①4호) 고지는 해야 한다. **수집 항목에 IP 를 적는다**(`user_consent.acted_ip`) — 스키마에 있는데 처리방침에 없으면 문서와 코드가 어긋난다(`D2` R11). 항목 목록은 `5j` 가 스키마에 넣은 것과 같아야 한다 | 9, 13-2, D2, D14, 5j, D20 |
| 14a | ~~셀러 신원 공개 조회~~ | **완료.** `SellerQuery`·`SellerController`·`MailOrderExemption`, 테스트 10개, 실서버 확인. 원래 설명: `GET /api/sellers/{셀러번호}`. **`R1` 이 상품 상세에 걸려 있는데 그 값을 내리는 경로가 없었다** — `seller` 에 신원 컬럼이 다 있고(`V4`) 공개로 나가는 자리가 하나도 없다. 상품 상세에 중첩하지 않고 경로를 가른 것은 **부르는 곳이 넷이라서다** — 상품 상세(`14b`), 주문서(`15-2`, 법이 요구하는 시점이 「청약 이전」이다), 소비자 요청 시 열람(R1), 나중의 셀러 상점. 파는 셀러만 열린다(`active`·살아 있음), 아니면 404 | 3c, 8, D2, D5 |
| 14 | ~~상품 목록 화면~~ | **완료.** `apiPublic` 서버 입구 신설(`api.ts`), `/products` 목록, `/products/{번호}` 자리표시, `next.config.ts` 에 `picsum.photos` 이미지 허용. **빈 상태가 둘이다** — 상품이 하나도 없는 것과 이 쪽에만 없는 것을 가른다(뒤쪽은 주소를 직접 고쳐 온 경우라 첫 쪽으로 가는 길을 준다). **쪽 번호를 다 안 늘어놓고 창(window) 5개로 자른다.** **갈 곳 없는 「이전·다음」은 링크가 아니라 `span`** 이다 — 비활성 링크는 보조기술이 만질 수 없다고 말한다(`D20`). **브라우저로 밟다 결함을 하나 고쳤다** — 제목이 두 줄인 카드만 커져서 **가격 줄이 카드마다 어긋났다.** `D20` 이 균등 그리드를 고른 이유가 가격 비교라 `grid-rows-[auto_1fr]` + `content-between` 으로 아래에 붙였다. 실서버로 상품 4건·44건 둘 다 확인했고 초점 순서·다크모드·모바일 단일 컬럼까지 봤다. 원래 설명: `/products`. 번호 페이징과 빈 상태(`D20`). **공개 데이터라 서버 컴포넌트로 그린다** — `api.ts` 에 쿠키 없는 공개 GET 입구를 하나 더 연다(그 파일이 예고해 둔 자리다). 홈의 「상품 보기」가 실제로 닿는 곳이 된다. `13c` 가 둔 `/products` 자리표시를 지운다 | 8, 13c, D20, D24 |
| 14b | ~~상품 상세 화면~~ | **완료.** `page.tsx`(서버)·`purchase-panel.tsx`(클라이언트)·`not-found.tsx` 신설. **법 고지 셋을 다 그린다** — `R1` 신원 일곱 칸을 표로(신고번호가 없으면 면제 사유가 그 자리에), `R2` 중개자 지위, `R4` 제한 사유를 사람이 읽는 말로. **「장바구니에 담기」를 넣었다**(사용자 선택) — 옵션을 고르는 이유가 담기말고 없다. **`R2` 문안은 화면에 짧게 쓰고 약관으로 링크했다**(사용자 선택) — 약관 본문을 잘라 오면 절 제목이 바뀔 때 고지가 조용히 사라지고, 전문을 붙이면 상품마다 약관 한 벌이 딸려 나온다. **문안이 두 벌인 것을 알고 둔다.** **숫자가 아닌 주소를 앞에서 404 로 막는다** — 서버에 물으면 400 이 오는데 그건 「고칠 수 있는 요청」이라는 뜻이라 사람이 볼 답으로 틀렸다. 조합 짝짓기는 **순서를 안 믿는다**(개수가 같고 전부 들어 있으면 같은 조합). 라디오는 `sr-only` 라 키보드로 고를 수 있다. **`을(를)` 을 안 쓴다** — 받침을 보고 조사를 고른다(`D20` 문구 규칙). 실서버로 옵션 고르기·품절 조합·담기(장바구니에 행이 남는 것까지)·404 셋·모바일·다크모드를 다 밟았다. 원래 설명: `/products/{번호}`. 옵션 고르기와 SKU 짝짓기, 품절 표시. **법이 요구하는 고지 셋이 여기 다 걸린다** — 셀러 신원(`R1`, `14a` 가 내려준다), 중개자 지위(`R2` — 통신판매의 당사자가 아니라는 것), 청약철회 제한 사유(`R4`, `8b` 가 이미 내린다) — **이건 표시가 제한의 성립 요건이다**(제17조제2항 단서). 지금은 `requireWithdrawable` 이 반품을 막는데 사람에게 보이는 자리가 없어서 **집행만 있고 성립 근거가 반쪽**이다. 이 화면이 그걸 닫는다(점검 A) | 8b, 14, 14a, D2, D20, D24 |
| 15-1 | ~~장바구니 화면~~ | **완료.** `api-session.ts`(세션 서버 입구), `CartService` 응답 보완, `/cart` 화면 둘(서버 + 클라이언트 잎사귀), `login/page.tsx` 에 만료 안내. **`D24` 가 예고만 해 둔 세 번째 입구를 냈다** — 서버 컴포넌트는 브라우저가 아니라 쿠키가 자동으로 안 붙어서 백엔드가 낯선 손님으로 본다. **파일을 가른 이유는 `next/headers`** 다: `api.ts` 에 들이면 그 파일을 쓰는 클라이언트 컴포넌트가 전부 빌드에서 깨진다. **나르는 쿠키가 둘**(`SHOPSESSION`·`CART-TOKEN`) — 장바구니는 로그인 전에도 사는 자원이라 세션만 나르면 비로그인 장바구니가 빈 채로 그려진다. **401 을 입구가 잡아 `/login?reason=session-expired` 로 보낸다**(`D20`). **백엔드 구멍 둘을 닫았다** — 카트 응답에 조합 라벨과 셀러가 없어서 같은 상품의 다른 조합이 글자가 똑같은 두 줄로 보였고, 주문서가 `R1` 을 채울 재료가 없었다. **브라우저로 밟아서 잡은 것**: 수량이 2인데 `19,000원` 만 있어 단가인지 줄 합계인지 안 갈렸다 — 줄 합계를 크게, 단가를 작게 바꿨다 | 12-2, 14b, 13c, D20, D24 |
| 15-2 | ~~주문서 화면~~ | **완료.** `/checkout` 서버 컴포넌트 + 폼(클라이언트), `components/seller-identity.tsx`(상품 상세와 공유), 백엔드 공개 셀러 응답에 배송비. **`R1`·`R2` 가 여기서 닫혔다** — 셀러별로 접은 신원 일곱 칸과 중개자 고지가 청약 이전에 그려진다. **배송비를 공개로 내렸다**(사용자 선택) — `14a` 가 수수료율과 같이 묶어 뺐는데 배송비는 사는 사람이 내는 돈이라 성격이 다르고, 제13조제2항이 가격에 배송료를 포함해 표시하라고 한다. 그 결정을 방벽으로 잡고 있던 테스트도 같이 뒤집었다. **결제 실패 시 주문번호를 들고 결제만 다시 부른다**(사용자 선택) — 주문을 또 만들면 재고가 두 번 빠진다. **멱등키가 둘**(`D11`)이고 재시도에도 같은 값을 쓴다. **브라우저로 밟아서 잡은 것**: 비로그인이 결제를 누르면 401 이 「결제하지 못했습니다」로 나왔다 — `api()` 도 401 을 로그인으로 보내게 고치고, 주문서는 그리기 전에 `apiSession('/api/me')` 로 막는다. 로그인한 적 없는 사람에게 「만료되었습니다」라고 하던 문구도 `login-required` 로 갈랐다 | 15-1, 12-2, D2 |
| 15-3 | ~~내 주문 화면~~ | **완료.** `/orders`(목록·번호 페이징), `/orders/{주문번호}`(상세), `order-actions.tsx`(동작 버튼), `orders/status.ts`(표기 공유). **`R6` 제3항이 여기서 닫혔다** — 처리 내역을 그려서 「언제 무엇이 있었나」에 답한다. **동작은 `allowed_actions` 만 그린다**(`11c-3b`) — 화면이 상태로 판단하면 규칙이 두 벌이 되고 어긋난 쪽은 눌러야 403 이 난다. **모르는 값은 건너뛴다**(`D5`). 셋 다 확인 대화상자를 받는다(`D20` 되돌릴 수 없는 조작) — 브라우저 기본 대화상자를 쓴다: 초점 가두기를 다시 만들 이유가 없고 물을 것이 예·아니오뿐이다. **브라우저로 밟아서 잡은 것**: 취소 확인 문구가 「환불됩니다」라고 약속했는데 **환급 경로가 아직 없다**(`12a-1`) — 화면이 없는 것을 말하고 있었다. 결제 완료 화면의 「주문 상세 보기」 링크도 여기서 이어졌다 | 15-1, 12-2, D2, D20 |
| 16 | 관리자 화면 | 역할·권한 편집. 권한을 데이터로 둔 보람이 나오는 자리. **역할을 주거나 회수하는 모든 경로에서 `PermissionRuleLoader.evict` 를 부른다** — 안 부르면 TTL 60초 동안 회수가 안 먹는다(청크 4a). **`GET /api/users/{id}` 가 여기서 생긴다** — 가입 응답(`5-2`)의 `Location` 이 그때 붙는다. `/api/me` 는 id 로 가리키는 경로가 아니라 지목할 자원이 못 된다. **축**: `D6` 권한 모델, `D20`. **강제 지점**: 역할을 주거나 회수하는 **모든 경로**에서 `PermissionRuleLoader.evict` 를 부르는 것을 테스트로 건다 — 안 부르면 TTL 60초 동안 회수가 안 먹는다(청크 `4a`). **건드리는 자리**: 신설 `app/admin/roles/*` 와 `GET /api/users/{id}`. 수정은 역할 부여 서비스와 `permission_rule` 조회 | 4, 13-2, 4a, D20 |
| 16a | 조직 역할 편집 화면 | 셀러 안에서 멤버에게 역할을 주고 회수한다. 전역 역할 편집과 화면을 가른다. **축**: `D6`(조직 역할), `D20`. **강제 지점**: 조직 스코프를 판정 엔진이 자르게 둔다 — 화면이 셀러 번호를 넘기는 순간 남의 조직을 편집하는 경로가 생긴다. **건드리는 자리**: 신설 `app/seller/members/*` 와 멤버 편집 입구. 수정은 `5a` 가 만든 멤버십 서비스 | 5a, 16, D20 |
| 16b | 임퍼소네이션 | 관리자가 사용자 시점으로 본다. 감사 로그 필수, 쓰기 조작은 막는다. **축**: `D14`(대행 세션), `D16`(감사 로그). **강제 지점**: 대행 중 쓰기를 필터에서 막고, 감사 로그의 행위자 종류에 대행을 새 값으로 넣는다 — 「누가 시켰나」가 안 남으면 임퍼소네이션은 그냥 권한 우회다. **건드리는 자리**: `SecurityConfig` 와 세션 속성, `audit_log` 의 행위자 값 | 4b, 16, D20 |
| 16c | 관리자 강제 전이 | `order:force_status` 를 만들고 CS 경로를 연다. **상태 축을 비켜 가는 유일한 동작이다** — `OrderStatusPolicy` 가 "표에 예외를 파지 말고 동작을 새로 만든다" 고 정한 자리고, 그래야 그 권한을 누구에게 줬는지가 데이터로 남는다. 사유 필수는 이미 걸려 있다(`order_status_history_admin_reason_check`). **지금 관리자는 `11c-3a` 의 경로로 표 안에서만 옮긴다** — 사유를 적으면 전이표 밖으로도 가지만 상태 축은 못 넘는다. **축**: `D7` 상태기계, `D14` 권한. **강제 지점**: 전이표 밖 동작을 **별도 권한 코드**로 두고 감사 로그(`4b`)에 남긴다 — 표에 예외를 파지 않는다. **건드리는 자리**: 신설 마이그레이션(`order:force_status` 권한)과 강제 전이 입구. 수정은 `OrderStatusService`·`OrderActionService` 와 관리자 화면 | 11c-3a, 16 |

청크 하나가 실제로 해 보니 크거나 작으면 그때 쪼개거나 합친다.
## 재점검 처분 청크

**2026-08-20 축별 재점검이 찾은 스물아홉을 자리별로 묶은 것**이다.
발견 번호는 `F`(법, `D2`)·`S`(표준, `D5`·`D20`)·`B`(강제 지점, `D23`)·`P`(규약, `D7`·`D11`·`D15`·`D20`·`D24`)·
`C`(관례, `stack.md`·`batch-catalog.md`) 다.

**축이 아니라 자리로 묶었다.** 축 1은 **한 자리에서 두 규칙이 부딪칠 때** 쓰는 저울이고,
부딪치지 않는 여러 자리의 차례는 비용이 정한다(`CLAUDE.md` 「무엇부터」).
`F3`(법)과 `B3`(강제 지점)이 같은 자리인데 축 순서로 치면 **0행을 막기 전에 화면을 그리게 된다** —
그 순서로는 빈 서면을 그리는 화면이 먼저 서고, 그때 쓴 테스트가 그 모양을 고정한다.

**「지금 깨져 있는 것」이 없다** — 테스트가 다 초록이라 1순위가 비어 있고, 실질 시작은 2순위다.

**결정 넷은 받았다**(2026-08-20). 미등록 스킴 이름을 넣고(`S1`), 유예를 5일로 줄이고(`F4`),
화면 테스트는 Vitest + Testing Library 로 두고(`P2`), JPA 스타터를 `starter-jdbc` 로 바꾼다(`C1`).

| # | 청크 | 무엇을 하나 | 선행 |
| # | 청크 | 결과 | 커밋 |
|---|---|---|---|
| Q1 | ~~오류 `type` 스킴 교체~~ | **완료.** `urn:shop:` → `tag:projectshop.example,2026:error:`(RFC 4151). **화면은 접두어를 모른다** — `ApiError` 가 슬러그를 떼어 주고 화면 아홉이 그것으로 분기한다. 다음에 또 바꿀 때 고칠 자리가 `api.ts` 하나다. **`S5` 는 방향이 반대였다** — `about:blank` 가 아니라 프레임워크 오류가 전부 `validation-failed` 로 뭉쳐서 **상태 코드보다 `type` 이 더 뭉쳤다.** 상태 코드로 갈랐다 | `b0f4f1c` |
| Q2 | ~~`seller_order` 제약·뷰~~ | **완료.** `V30` — `ship_due_at` 지연 제약 트리거(조건이 다른 표의 상태라 `check` 로는 못 막는다), 뷰 재생성, `refund` FK 인덱스 둘. **뷰와 표의 컬럼 대조 테스트**를 같이 세웠다 — 그것이 없어서 `11-6` 이 적어 둔 함정을 하루 만에 다시 밟았다. **`B4` 는 철회했다**(`V26` 주석이 이미 답을 적어 뒀다) | `40e1438` |
| Q3 | ~~계약내용 서면 완전성~~ | **완료.** `V31` 지연 제약 트리거. 조항 이름이 제약과 트리거 두 곳에 생겨서 **제약 쪽에서 읽어 하나씩 지워 보는 테스트**로 대조한다. 새 제약이 기존 테스트 넷을 깨서 `OrderFixture` 에 붙이는 헬퍼를 모았다 | `57d0444` |
| Q4 | ~~계약내용 서면 교부~~ | **완료.** `GET /api/orders/{번호}/contract-documents/{조항}` 과 주문 상세의 `<details>`. **그 주문이 가리키는 판**을 준다 — 발 아래 약관 링크는 최신판이라 개정 뒤에는 그 사이 고친 것을 들이미는 꼴이 된다 | `db6cd30`·`ae95849` |
| Q5 | ~~청약철회 제한 스키마~~ | **완료.** `V32` — 성립한 제한을 주문 항목에 박제한다. **`copyable_media` 는 값에서 뺐다**(포장 훼손은 물건이 돌아와야 아는 사실). 동의 없는 `made_to_order` 제한은 DB 가 막는다 | `5998b99` |
| Q6 | ~~청약철회 제한 판정·표시~~ | **완료.** 판정이 주문 항목을 읽고 주문서가 동의 칸을 낸다. **테스트가 옛 규칙을 굳혀 놨던 것을 뒤집었다** — 「복제 가능 매체는 반품이 막힌다」가 「안 막는다」가 됐다 | `d50d98e` |
| Q7 | ~~배송지 형식~~ | **완료.** `V33` + `@Pattern` + 화면. 우편번호는 고시가 정한 5자리고 전화번호는 자릿수만 본다 — 형식을 고시가 안 정한다 | `6f6be30` |
| Q8 | ~~프론트 골격~~ | **완료.** `loading.tsx`·`error.tsx`, 체크박스 24×24, 카드번호 `autocomplete` 근거. **규약이 요구한 파일이 없던 것이 가장 조용한 종류였다** | `94e3427` |
| Q9 | ~~화면 테스트 층~~ | **완료.** Vitest + Testing Library. 첫 판은 동의 목록의 판단(필수 항목에 철회 버튼을 안 그린다) | `7c81f48` |
| Q10 | ~~탈퇴 유예 5일~~ | **완료.** `V34` 로 방침 새 판. **시행일이 이레 뒤인데 코드는 즉시 5일**이다 — 덜 보관하는 방향이라 고지가 늦어도 이용자에게 불리하지 않다 | `60689a5` |
| Q11 | ~~`PATCH` 미디어 타입·`Location`~~ | **완료.** `application/merge-patch+json`, 주문 201 에 `Location` | `5358679` |
| Q12 | ~~401 challenge~~ | **완료.** 미등록 스킴 이름. 버린 길 셋을 `D5` 에 적었다 | `e488584` |
| Q13 | ~~`/me` 정보주체 권리~~ | **완료.** `POST /api/me/email`, 처리정지 문구. **`F7`(대리인)은 `11b` 에 흡수** | `0e3559f` |
| Q14 | ~~근거 정정 묶음~~ | **완료.** `D2` R9·R5, `D11` 멱등키 표, `stack.md` 프론트 근거, 배치 카탈로그 행. **`S5` 방향과 `B4` 철회도 같이 기록** | `f9e2bae` |
| Q15 | ~~JPA 스타터 교체~~ | **완료.** `starter-jdbc` 로. `spring.jpa` 설정 블록도 걷어냈다 | `2205650`·`66c8715` |

**축 6(코딩 규칙)은 오늘 친 코드까지 포함해 돌렸다.** 셋이 나왔고 둘은 코드로 쳤다(`518f276`) —
**두 층에 흩어진 규칙이 갈리는 것**을 막는 테스트 둘이다(우편번호 형식, 오류 이름 접두어).
`K1`·`K2` 는 `D23` 본문에 넣었고 `K3` 는 위반이 아니라 관측이다.

**순서는 위에서 아래로 갔다.** `Q1`~`Q9` 가 2순위(쓰는 곳이 늘면 비싸진다)였고,
`Q10` 은 법이라 그 뒤에 바로 붙였고, `Q11`~`Q15` 는 값이 시간과 무관했다.

**남은 것은 셋이다** — 이메일 변경의 확인 절차(알림 축 `54a`~`56`),
처리정지 접수 창구(Q&A 축 `58`·`59`), 약정 유무의 박제(`14c`).

## 기준 문서

여러 청크가 같이 참조하는 결정을 모아 두는 문서들이다. 한 청크에서만 쓰는 결정은 ADR 로 남긴다.
어느 청크가 어느 문서를 기다리는지는 `doc/reference/document-map.md` 에 있다.

| # | 문서 | 무엇을 정하나 | 선행 |
|---|---|---|---|
| D1 | 문서 트리 규약·용어집 | 문서가 어디 가나, 도메인 용어의 한↔영 대조 | 완료 |
| D2 | 법·정책 요건표 | 전자상거래법·개인정보법 요건과 설계 지점의 매핑 | 완료 |
| D3 | 비즈니스 모델 | 누가 누구에게 무엇을 팔고 돈이 어디로 흐르나. 수수료 주체와 요율 | 완료 |
| D4 | 도메인 모델 | 엔티티 관계와 경계. 무엇이 무엇을 참조하고 무엇이 같이 태어나고 죽나 | 완료 |
| D5 | API 설계 규약 | URL·동사·페이징·정렬·필터·오류 형식·버전. 우리가 정할 것만 본문에 쓰고 나머지는 Zalando RESTful API Guidelines 를 따른다 | 완료 |
| D6 | 권한 모델 명세 | 판정 규칙, 스코프·효과의 우선순위, 필드 그룹 | 완료 |
| D7 | 상태머신 카탈로그 | 주문·상품·정산·문의·반품의 상태와 전이표. 전이표는 코드에 선언한다(ADR 0009) | 완료 |
| D8 | 금액·통화 규약 | 저장 단위, 반올림, 부가세, 할인 적용 순서 | 완료 |
| D9 | 식별자 규약 | 내부 ID 와 외부 노출 번호를 어떻게 가르나 | 완료 |
| D10 | 시각·영업일 규약 | 저장 시간대, 영업일 계산, 배치 기준 시각. 공휴일은 `holiday` 테이블에 두고 임시공휴일을 배포 없이 추가한다(ADR 0009) | 완료 |
| D11 | 동시성·트랜잭션 규약 | 락 전략, 격리 수준, 멱등키 형식, 재시도 | 완료 |
| D12 | 이벤트 카탈로그 | 이벤트 이름·페이로드·버전·발행 시점. **축**: 표준 — 이벤트 이름·버전 규약(CloudEvents 를 따를지 여기서 정한다), `D5`. **강제 지점**: 없다 — 문서다. 이름 표를 `29`·`32` 가 참조하고, 어긋나면 그 청크의 테스트가 깨진다. **건드리는 자리**: 신설 `event-catalog.md`. 코드는 안 건드린다 | D5 |
| D13 | 데이터 수명 정책 | 보존 기간, 파기 대상, 삭제 방식 | 완료 |
| D14 | 보안 기준 | 세션·시크릿·파일 접근·서명·비밀 관리 | 완료 |
| D15 | 테스트 전략 | 무엇을 어느 층에서 검증하나, 컨테이너 사용, 매트릭스 형식 | 완료 |
| D16 | 관측 규약 | 로그 형식·레벨·추적 ID·지표 이름 | 완료 |
| D17 | 파일·미디어 규약 | 업로드 제한, 저장 경로, 접근 권한, 변환. **축**: `D14`(업로드 검증), 표준(MIME 판별). **강제 지점**: 없다 — 문서다. 제한값을 `26`~`28` 이 `check` 와 필터로 내린다. **건드리는 자리**: 신설 `media-rules.md` | D14 |
| D18 | ~~알림 정책~~ | **완료.** `notification-rules.md`. **거래 통지와 광고성 정보를 먼저 갈랐다** — 같은 메일함에 도착해도 동의·야간·거부가 전부 갈린다. 거래 통지는 계약 이행이라 동의가 필요 없고 야간에도 나가며 **수신 거부 자리를 안 만든다**(안 보내면 우리가 위반이고, 사용자가 껐다는 사실이 그 위반을 안 덮는다). **채널은 이메일 하나**(사용자 선택) — 문자를 뺀 이유가 기능이 아니라 데이터다: 전화번호를 받는 자리가 없고 그건 최소수집과 부딪친다. **템플릿을 DB 에 판과 시행일로 둔다**(사용자 선택) — 코드 상수면 개정한 순간 지나간 발송의 본문을 복원 못 한다. **발송 이력에 본문을 그대로 남긴다**(사용자 선택) — 분쟁에서 「보냈다」를 증명하는 것이 이 표라서고, 대가로 개인정보가 한 벌 더 쌓여서 보존 3년에 거래기록 파기가 지운다. 강제 지점 다섯을 표로 적어 `54a`·`54b` 가 채우게 했다 | 완료 |
| D19 | 배치·스케줄 카탈로그 | **완료** — `batch-catalog.md`. 카탈로그 5개, 재실행 안전성 수단 셋, 실패 처리 **3층**(건·회차·체인), 실행 이력 `batch_run`. **체인이 있는 배치만 회차 재시도를 한다**(10분 3회) — 선행이 뒤를 막는 자리라 다음 회차까지 24시간이 밀린다. 체인 없는 것은 다음 회차에 맡긴다. 분산 잠금은 안 만든다(1대 전제) | 완료 |
| D20 | 화면·문구 규약 | **완료** — `screen-rules.md`. 권한 없는 것은 숨기고, 볼 수 없는 필드는 그 줄을 안 그리고, 오류는 자리를 가려서 보여준다. **화면 문구는 존댓말** — `CLAUDE.md` 의 평서형 규칙은 개발자가 읽는 글에만 걸린다 | 완료 |
| D21 | 성능 목표 | 응답 시간 기준, 데이터 규모 가정, 측정 방법. **측정값을 입력으로 쓴다** — 실행 계획을 한 번도 안 읽고 정하면 수치가 감이다. **축**: **측정값**이 입력이다(청크 `42` 의 실행 계획). 감으로 정하면 그 수치를 아무도 안 믿는다. **강제 지점**: 없다 — 문서다. 수치를 `70`(부하)이 검증한다. **건드리는 자리**: 신설 `performance-goals.md` | 42 |
| D23 | 코딩 규약 | 계층·예외·트랜잭션·SQL·주석·테스트 작성 방식. **바깥 표준이 아니라 코드에 이미 성립한 관례를 뽑았다.** 어긋난 둘(서비스의 HTTP 예외 → 7b, `AuditLogQuery` 문자열 결합 → 8)을 문서에 적어 뒀다 | 완료 |
| D24 | 프론트 계층 규약 | **완료** — `frontend-rules.md`. `D23` 이 백엔드 계층을 정했는데 프론트 쪽이 통째로 비어 있었다. 서버 컴포넌트가 기본이고 `"use client"` 쪽이 근거를 댄다. **`api.ts` 입구가 둘**(브라우저·서버)이고 **인증 화면도 서버 컴포넌트**다(사용자 선택) — 세션을 서버 입구가 손으로 나른다. **캐시 절은 번들된 Next 문서를 읽고 썼다** — `force-static` 이 `cookies()` 를 빈 값으로 만드는 것이 조용히 뚫리는 경로다 | 완료 |
| D22 | 명명 규칙 | DB 컬럼·테이블·기본키, Java 식별자. 기준은 `sqlstyle.guide` 와 Google Java Style. **새 테이블부터 기본키가 `<테이블>_id` 다** — 기존 13개는 안 바꾸고 벗어난 이유를 문서 맨 위에 적었다 | 완료 |

## 확장 청크

16b 이후다. 순서는 의존이 허락하는 한 자유롭게 잡는다.
각 그룹이 기다리는 기준 문서는 `document-map.md` 에 적혀 있다.

| # | 청크 | 무엇을 하나 | 선행 |
|---|---|---|---|
| 17 | ~~정산 스키마~~ | **완료.** `V52__settlement.sql`·`SettlementSchemaTest`(둘 다 신설), `money-invariants.md`·`data-lifecycle.md` 수정. 표 셋을 세웠다 — `settlement_cycle`(월 하나에 한 행, 셀러와 무관)·`settlement`(셀러 하나의 그 주기 정산서)·`settlement_item`(정산서 한 줄). **알갱이는 주문 항목 건별이다**(사용자 선택) — 종류별 합계 네 줄로 두면 정산서 안에 근거가 없어서 「이 수수료 합이 어느 주문에서 나왔나」에 답을 못 하고, 그때 주문 표를 다시 더하면 그 계산이 마감 때와 같다는 보장이 없다. **`17-1` 의 등식 다섯 중 넷이 내려갔다**: 지연 트리거(`payout_amount = sum(items)`), **`check`(`carried_over = least(0, payout_amount)`)** — 문서는 테스트라고 적었는데 한 행 안에서 결정되는 값이라 한 층 아래로 내렸고 문서를 고쳤다, `unique(주기, 셀러)`, 그리고 **부분 유니크 넷**. 남은 둘(합계 대조)은 마감 배치가 값을 만들기 시작하는 `19` 가 건다. **드러나서 등식이 하나 늘었다** — `(셀러, 주기)` 유니크는 **같은 주기를 두 번 마감하는 것만** 막는다. 같은 주문 항목이 **다른 주기의 정산서에** 실리면 두 정산서 각각은 합이 맞고 셀러가 돈을 두 번 받는다. 그래서 근거 참조 넷에 **정산서를 넘어선 전역 유니크**를 걸고 `money-invariants.md` 에 여섯째 줄로 적었다. **공급자를 받지 않고 만든다** — 부가가치세법(`D2` R17)이 상품대금(셀러→고객)과 중개수수료(플랫폼→셀러)를 갈라야 한다고 하는데, 문서가 「공급자 컬럼을 따로 받아서 채우면 종류와 어긋난 행이 생긴다」고 적어 뒀다. **생성 열**로 두면 어긋날 방법 자체가 없다(`D23` 축 2 의 1위, 구조). **부호도 종류가 정한다** — 수수료를 양수로 담고 뺄셈을 앱이 하면 「합이 곧 지급액」이 성립을 안 해서 지연 트리거가 아무것도 못 막는다. **테스트가 그 자리에서 둘을 물었다** — 이월 픽스처가 같은 주기에 정산서를 두 장 만들고 있었고(`(셀러,주기)` 유니크), 생성 열의 null 을 `query(String.class).single()` 로 읽고 있었다 | D3, D8, D10, 12, D2, 17-1 |
| 18 | ~~수수료 계산~~ | **완료.** `V55__settlement_commission_basis.sql`(신설), `SettlementService`·`SettlementSchemaTest`·`money-invariants.md` 수정. **`17` 의 정산 항목이 금액만 들고 있었다** — 셀러가 「이 수수료가 무엇에 몇 퍼센트냐」를 물으면 정산서가 답을 못 하고 주문 표를 다시 뒤져야 하는데, **그때 하는 계산이 마감 때와 같다는 보장이 없다.** 요율(`commission_bp`)과 기준 금액(`commission_base_amount`)을 정산 행에 굳혔다. **강제 지점이 한 행 안의 `check` 로 내려갔다** — `settlement_item_commission_amount_check` 가 「기준액 × 요율 = 금액」을 검사해서 **앱이 정산 시점에 다시 계산하는 경로를 안 만든다.** `order_item_commission_amount_check` 와 같은 모양이고 절사 규칙(버림, `D8`)도 같다. **환급 줄에는 안 박았다** — `refund_item.commission_refund` 는 원 수수료를 수량으로 나눈 값인데 **마지막 수량에 절사 잔액을 몰아 줘서** 「기준액 × 요율」을 넘을 수 있다(단가 10,005 × 3개 10% → 마지막 1개 환급 1,001 > 1,000). 등식은 물론 **상한으로도 못 쓴다.** 환급의 근거는 요율 재계산이 아니라 **원 수수료 금액**이고 그 등식은 환불 축이 이미 들고 있다. **`17` 의 스키마 테스트 여섯이 그 자리에서 물렸다** — 수수료 줄에 근거 없이 넣고 있었다. **비용 순위 2번으로 `59-1`·`58-1` 보다 먼저 쳤다** — 정산 항목 계약을 건드리는데 지금 소비자가 `SettlementService` 하나고, `20` 이 그 값을 내리기 시작하면 화면까지 같이 고친다 | 17 |
| 19 | ~~정산 마감 배치~~ | **완료.** `SettlementService`·`SettlementCloseBatch`·`SettlementCloseBatchTest`(셋 다 신설), `BatchRuns`·`application.yml`·`batch-catalog.md` 수정. **`D19` 3층 첫 체인이 섰다** — `auto_confirm` 이 그날 성공해야 `settlement_close` 가 돈다. 안 돌면 `WARN` + `batch_run` 에 `skipped`. **기준일을 실행일로 안 잡았다** — 판정이 「같은 기준일에 선행이 성공했나」라, 1일로 잡으면 **말일 자동 구매확정이 안 돈 것을 못 본다.** 그 말일이 이번 정산의 마지막 확정분이 나온 날이라 **기준일 = 전달 말일**이고 실행만 1일 05:00 이다(04:00 은 선행 자리라 같은 시각이면 순서가 안 보장된다). **드러난 것 하나 — `shouldRetry` 가 스킵을 못 봤다.** 카탈로그가 「스킵된 후행은 재시도 창 안에서 다시 시도한다」고 정했는데 코드는 `transient` **실패만** 봤고, 스킵은 실패가 아니라 그 조건에 안 걸렸다. **체인을 세우는 순간 그 규칙이 처음으로 실행됐고 그때 빈 것이 드러났다.** 마지막 회차가 `skipped` 여도 재시도하게 고쳤다. **줄은 주문 항목 건별이고 종류가 여섯이다**(`17` 이 깐 모양) — 판매·수수료·배송비·회수·수수료환급·이월. 대상 조건이 전부 **「아직 어느 정산서에도 안 실린 것」**이고 그것을 `V52` 의 전역 부분 유니크가 한 층 아래에서 지킨다. **지연배상금을 셀러 몫에서 안 뺐다**(`12a-4`) — 읽는 것이 `refund_item` 뿐이라 `refund.delay_interest` 가 정산에 안 닿는다. **거래가 없고 이월만 있는 셀러도 정산서를 만든다** — 빼면 그 채권이 다음 달에도 같은 이유로 빠져서 영영 회수가 안 된다. **줄이 하나도 안 붙은 정산서는 지운다** — 그 달 환불이 전부 정산 안 된 건이었던 셀러가 그렇게 되고, 빈 정산서가 서면 「거래가 없었다」와 「마감이 덜 돌았다」가 안 갈린다. **`17-1` 의 남은 등식 둘을 걸었다** — 종류별 합을 **정산과 다른 경로로 다시 더해** 원본과 대조한다. 같은 쿼리를 쓰면 그것이 틀렸을 때 양쪽이 똑같이 틀리고, 지급액 하나만 보면 두 종류가 서로의 오차를 상쇄해도 초록이 된다 | 17, D19, 36 |
| 20 | ~~정산서 조회~~ | **완료.** `V56__settlement_number.sql`·`SettlementQuery`·`SettlementController`·`SettlementQueryTest`(넷 다 신설), `SettlementService`·`SettlementSchemaTest`·`ErrorCode`·`identifier-rules.md` 수정. **`D9` 가 이미 `settlement_number` 를 요구하고 있었다** — `17` 이 표를 세우면서 빠뜨렸고 그때는 부르는 곳이 없어서 안 드러났다. **조회 입구를 여는 이 청크가 그것을 걸었다.** 접두어는 `T-` 다 — **`P-` 는 `D9` 가 결제에 예약**해 뒀고 두 글자는 나머지 넷(빈 문자열·`S-`·`R-`·`Q-`)과 형식이 어긋난다. **범위를 판정 엔진에게 물어서 조건으로 옮겼다**(`RefundQuery.visibleFor` 와 같은 모양) — 쿼리에 셀러 번호를 손으로 끼우면 스코프 규칙이 바뀔 때 고칠 곳이 판정 엔진 밖으로 흩어진다. **못 봄이 0건이 아니다** — 정산 건수는 곧 그 셀러가 거래한 달 수고 정산액은 월 거래액이라 빈 목록을 주면 개수로 샌다. 남의 정산서 단건 조회도 **404** 다. **경로를 관객별로 안 갈랐다** — `59` 는 같은 자원인데 **행 자체**가 달라야 했고(비공개 문의) 여기는 **범위만** 달라진다. 갈리지 않는데 입구를 늘리면 목록 규약을 두 벌 지킨다. **줄은 상세에서만 내린다** — 주문 항목 건별이라 목록에 실으면 한 셀러의 한 달치가 수백 줄이고 긁으면 거래 내역 전체가 된다. **고객에게 안 열고 감사자에게는 열었다**(돈이 어디로 갔나를 못 보면 감사가 성립을 안 한다). 마이그레이션에 **고객에게 안 열렸나**와 **셀러가 `seller` 범위로만 열렸나** 검증을 `do` 블록으로 넣었다. **`17` 의 스키마 테스트 스물이 그 자리에서 물렸다** — `settlement_number` 가 `not null` 이 되면서. **화면은 안 했다** — `59` 와 같이 API 까지다. **드러난 것 하나**: `payment_number` 도 `D9` 표에만 있고 실물이 없다. 다만 **결제를 URL 로 가리키는 입구가 하나도 없어서 순번이 샐 자리가 없다** — 청크를 세우지 않고 그 표 행에 사실을 적었다(조회 입구가 서는 청크가 같이 만든다) | 18, 4 |
| 21 | ~~지급 처리~~ | **완료.** `V57__settlement_payout.sql`·`SettlementPayoutService`·`SettlementPayoutTest`(셋 다 신설), `SettlementController`·`ErrorCode`·`state-machines.md` 수정. **`19` 뒤가 비어 있었다** — 마감된 정산서와 실제로 돈이 나간 정산서가 **데이터로 안 갈렸고**, 두 번 지급해도 표가 아무 말을 안 했다. 요청·승인을 **다른 권한**으로 갈랐다(`request_payout`·`payout`) — 하나로 두면 올리는 순간 승인이 같이 열리고 그때 남는 것은 자기승인 제약뿐인데 **그것은 자기 것만 막는다**(`V24` 가 환불에서 적은 문장 그대로). **둘 다 관리자다** — 셀러가 올리게 하면 **셀러가 안 눌러서 지급이 밀리는 구조**가 되는데 그건 `12a-5` 가 오늘 환불에서 고친 함정과 같은 모양이다. **강제 지점 셋**: `settlement_payout_self_approval_check`(psql 로 넣어도 걸린다), `settlement_payout_amount_check`(지급액 0 이하면 못 올린다 — 이월로 넘어가지 지급 대상이 아니다), 그리고 **상태·시각·행위자를 사슬로** 묶은 것. **오늘 밟은 구멍을 안 다시 밟았다** — 「상태 = (사람과 시각이 둘 다 있나)」로 쓰면 한쪽만 채운 행이 통과한다(`V53` 의 `inquiry_block_check` 가 그 자리였다). **반려가 종점이 아니다** — 정산서는 `(셀러, 주기)` 당 하나라 환불처럼 새 행을 못 만든다. 다시 올리면 같은 행이 `requested` 로 돌아가고 처리 칸이 비워지며, 반려 사실은 감사 로그가 든다. **`D7` 에 정산 지급과 문의 상태머신을 적었다** — 「정산은 청크 17, 문의는 `58` 에서 그때 정한다」로 남아 있던 자리다. **실제 이체 연동은 안 한다** — 셀러 계좌 송금은 은행 이체라 모의 결제의 일이 아니고, 여기서 하는 것은 「보냈다」를 기록하는 데까지다 | 19, 12a-2 |
| 20-1 | 정산 화면 | **`20`·`21` 이 세웠다**(2026-08-23). 분할표의 `20` 행이 「셀러·관리자 화면」을 적어 뒀는데 그 청크가 API 까지만 했다. 셀러 콘솔의 정산서 목록·상세와, 관리자의 **지급 올리기·승인** 자리다. **축**: `D20` 화면 규약, `D24` 프론트 규칙, WCAG, `D14`(관리자 화면에 지급 버튼이 뜨는 조건). **강제 지점**: 권한 없는 버튼을 **안 그린다** — 판정이 내려준 목록으로 그린다(`8a` 가 그 자리를 열었다). 화면에서 숨기는 방식은 안 쓴다: `59` 가 같은 판단을 했다. **건드리는 자리**: 셀러 콘솔과 관리자 화면. API 는 안 건드린다 | 20, 21 |
| 22 | ~~문의 스키마~~ | **`58` 이 대신했다**(2026-08-23 점검 G 가 중복을 찾았다). 이 행이 미착수로 남아 있으면 **다음 사람이 이미 있는 표를 다시 만든다.** `58` 이 `inquiry` 를 세웠고 상태·답변·차단 칸이 다 들어갔다. **하나가 안 옮겨졌다** — 이 행의 「대상 자원(**주문**·상품)」에서 주문 쪽이다. `58` 은 상품과 계정(처리정지·이의제기·분쟁)만 담았고 **「이 주문 언제 와요?」를 담을 값이 없다.** `58-2` 로 세웠다(사용자 선택) | 완료 |
| 23 | ~~문의 API~~ | **`59` 가 대신했다**(점검 G). 관객별 입구 셋과 답변까지 섰다. **근거 조문이 어긋난 채로 남아 있었다** — 이 행은 전자상거래법 **제20조의3**(중개자의 불만·분쟁 처리)을 적었는데 `58` 이 세운 `R25` 는 **제20조제3항**이다. 둘 중 하나가 틀렸거나 다른 조문이고 **원문을 안 봤다** — `58-1` 이 그 확인을 같이 한다 | 완료 |
| 24 | ~~문의 화면~~ | **`59-1` 이 대신한다**(점검 G). 아직 미착수지만 **행을 둘로 두면 다음 사람이 어느 쪽을 잡을지 고르는 자리가 생긴다** — `11-5` 가 `35e` 를 흡수할 때와 같은 판단이다. 이 행이 적어 둔 것(역할별로 갈리는 목록과 상세, `D20`·`D24`, `npm run lint` 의 접근성 규칙)은 `59-1` 행이 들고 있다 | 완료 |
| 25 | ~~문의 개인정보 가림~~ | **완료.** `V62__inquiry_field_groups.sql`·`InquiryFields`(둘 다 신설), `InquiryQuery`·`InquiryVisibilityTest` 수정. **`59` 가 축에 `D16` 을 걸고도 본문이 누구에게 나가는지를 안 봤다**(점검 G). `inquiry.question` 은 자유 텍스트 2000자고 사람이 직접 쓴다 — **글 안에 연락처가 섞여 들어온다**(`5i-2` 가 사유 글을 5년 표에서 뗀 이유, `V24` 가 환불 요청 사유를 셀러에게 안 준 이유와 같다). **감사자에게서 닫았다** — 감사는 「누가 언제 무엇을 처리했나」지 고객이 쓴 글이 아니다(개인정보보호법 제3조제1항 최소처리). `V6` 가 감사자에게서 `payment` 를 닫은 것과 같은 논리고, 거기 근거가 「결제 수단까지 볼 이유는 없다」였다. **연결이 없으면 제한이 없다**(`V6`)라 **감사자에게 `basic` 을 연결하는 것 자체가 `body` 를 닫는 행위**다. `do` 블록 둘이 검사한다 — 본문이 열렸나, 그리고 **연결이 실제로 생겼나**(후자가 0이면 「제한 없음」과 구분이 안 된다). **공개 Q&A 는 그대로다** — 공개로 낸 글이라 가릴 것이 없다 **마스킹이 실제로 걸리는 것은 `25-1` 이다** — 감사자만으로 남의 문의를 보는 경로가 없어서, 여기서 고정한 것은 **판정이 그 그룹을 안 열어 준다**는 사실까지다 | 59, 4d |
| 25-1 | 관리자·감사자용 문의 목록 | **마무리가 찾았다**(2026-08-23). `25` 가 문의 본문을 필드 그룹으로 묶어 감사자에게서 닫았는데, **감사자만으로 남의 문의를 보는 경로가 없어서 그 마스킹이 한 번도 안 걸린다.** 지금 목록 셋은 공개(비로그인)·내 것(`own`)·내 셀러 것(`seller`)이고 **`all` 스코프로 훑는 자리가 없다.** 그래서 `25` 에서 고정한 것은 **판정이 그 그룹을 안 열어 준다**는 사실까지고, **응답에서 실제로 비는지는 이 청크가 처음 본다.** **`59-2`(게시 중단)도 여기를 기다린다** — 관리자에게 내릴 권한을 줬는데 **광고를 찾아 훑을 자리가 없어서** 번호를 이미 아는 경우에만 부를 수 있다. **축**: `D14` 권한 스코프(`all`), `D5` 목록 규약, `D16`. **강제 지점**: `InquiryQuery` 가 이미 쥔 `bodyVisibleTo` 를 그대로 태운다 — **새 조건을 쓰지 않는다**: 목록마다 마스킹 규칙을 다시 쓰면 한 곳을 빠뜨렸을 때 조용히 샌다. **드러난 것을 같이 본다**: `findForSeller` 가 `sellers.iterator().next()` 하나로 필드 그룹을 묻는다 — 지금은 역할이 하나라 결과가 같지만 **셀러마다 규칙이 갈리면 틀린다.** 근거가 코드 주석에만 있고 검사가 없다. **건드리는 자리**: `InquiryQuery` 에 목록 하나, `InquiryController` 에 입구 하나, 관리자 화면(`16` 이 설 때). 표·권한은 안 건드린다 — `V54`·`V62` 가 이미 다 팠다 | 25, 59-2 |
| 26 | 파일 저장소 | MinIO 컨테이너, 업로드 경로 규칙, 용량·형식 제한. **저작권법 제102~103조를 `D2` 에 승격시킨다**(점검 A) — 남의 이미지가 올라올 때 신고·삭제 절차가 있어야 온라인서비스제공자 책임 제한을 받는다. **축**: `D17` 파일 규약, **법**(저작권법 제102~103조 — 신고·삭제 절차가 있어야 책임 제한을 받는다, `D2`). **강제 지점**: 컨테이너와 버킷 정책 자체 — 공개 버킷을 안 만든다. **건드리는 자리**: 신설 MinIO 컨테이너(`docker-compose.yml`)와 저장소 설정. `stack.md` 에 그 선택의 근거 **묶음 검토(2026-08-23)**: `27`·`28` 과 **안 묶는다.** 선행 `D17`(파일·미디어 규약)이 미착수인데 그 문서의 선행(`D14`)은 차 있어서 **지금 쓸 수 있어 보이지만 아니다** — 「문서가 코드보다 먼저다」의 두 번째 조건인 **「정할 근거가 실제로 생겼다」**가 안 찼다. 파일이 코드에 하나도 없어서 지금 `D17` 을 쓰면 **무엇을 정하는지 모르는 채로 정한다**(`D21` 이 막힌 것과 같은 함정). **닭·달걀이라 순서로 푼다**: `26` 을 최소로 쳐서 저장 자리를 만들고 → `D17` 을 쓰고 → `27`·`28` 을 친다. 셋을 묶으면 그 중간에 문서를 낄 자리가 없어진다 | D17, D2 |
| 27 | 상품 이미지 업로드 | 셀러가 자기 상품에만 올린다. **축**: `D14` 보안 기준선(업로드 — 크기·MIME·확장자), `D3` 스키마. **강제 지점**: 원본 이름·MIME·바이트 수를 컬럼으로 받고 `check` 로 좁힌다. 저장 경로 이름은 앱이 만든 것만 쓴다. **건드리는 자리**: 신설 업로드 입구와 상품 이미지 표. 수정은 셀러 상품 화면(`13f`)의 등록 폼 | 26, 7 |
| 28 | 파일 접근 판정 | URL 을 알면 보이는 기본값을 막는다. 서명된 URL 또는 프록시. **축**: `D14`, `D5`. **강제 지점**: 파일도 판정 엔진을 지나게 한다 — URL 을 알면 열리는 경로를 하나도 안 남기는 것이 통과 기준이다. **건드리는 자리**: 신설 파일 접근 판정 경로(서명 URL 또는 프록시). `26` 의 저장소 설정과 `27` 의 이미지 표를 읽는다 | 27, 4 |
| 29 | 웹훅 등록 | 셀러가 엔드포인트와 시크릿을 등록한다. **축**: `D12` 이벤트 이름, `D14`(시크릿 저장). **강제 지점**: 시크릿을 평문으로 못 넣게 — 저장 전 해시·암호화 여부를 스키마가 강제한다. **건드리는 자리**: 신설 웹훅 등록 표와 셀러 입구. 수정은 `permission_rule` 시드 | D12, D14, 5a |
| 30 | 웹훅 발송 | 서명 헤더, 타임아웃, 실패 기록. **축**: 표준 — HMAC 서명 헤더와 타임스탬프(재전송 차단), `D16`. **강제 지점**: 발송 기록 표 + 타임아웃 값을 한 곳에서만 정한다. **건드리는 자리**: 신설 발송기와 발송 기록 표. `32` 의 아웃박스를 읽는다 **묶음 검토(2026-08-23)**: 아웃박스와 **묶을 필요가 없다** — 이 행의 선행에 이미 `32` 가 있어서 **순서가 분할표에 박혀 있다.** 묶으면 파일이 여섯을 넘고, 아웃박스가 미완인 채로 웹훅을 얹으면 나중에 발송 경로를 갈아엎는다. **먼저 칠 것은 `32`·`33`** 이고 둘 다 `D12`(이벤트 카탈로그)를 기다린다 — `D12` 의 선행 `D5` 는 차 있다 | 29, 32 |
| 31 | 웹훅 재시도 | 지수 백오프, 최대 횟수, 수동 재발송. **축**: 표준 — 지수 백오프, `D7`. **강제 지점**: 시도 횟수와 다음 시각을 **DB 컬럼**으로 둔다. 앱 메모리에 두면 재기동에 사라진다. **건드리는 자리**: `30` 의 발송 기록 표에 시도 횟수·다음 시각 칸과 재발송 입구. `36a` 의 재시도 분류를 그대로 쓴다 | 30 |
| 32 | 아웃박스 테이블 | 도메인 변경과 이벤트 기록을 한 트랜잭션에 넣는다. **축**: `D12`, `D11`(한 트랜잭션). **강제 지점**: 도메인 변경과 이벤트 기록을 **같은 트랜잭션**에 넣는 것을 테스트로 고정 — 갈라지면 「일어났는데 안 알린」 사건이 남는다. **건드리는 자리**: 신설 아웃박스 표. 수정은 발행 시점을 부르는 서비스들(주문·결제·환불) | D12, 10-2 |
| 33 | 아웃박스 발행기 | 기록된 이벤트를 꺼내 보낸다. 최소 한 번 전달과 중복 처리. **축**: `concurrency-rules.md`(최소 한 번 전달과 중복 처리), `D7`. **강제 지점**: 아웃박스 행의 유니크 + 소비 측 멱등키. **건드리는 자리**: 신설 발행기 배치와 `batch-catalog.md` 카탈로그 행. `32` 의 아웃박스 표를 읽고 지운다 | 32 |
| 34 | 이벤트 소비 검증 | 같은 이벤트가 두 번 와도 결과가 같은지 테스트. **축**: `D11` 멱등, `D15`. **강제 지점**: 같은 이벤트를 두 번 넣어 결과가 같은지 보는 테스트가 이 청크의 전부다. **건드리는 자리**: 테스트만 신설한다. 코드는 `33` 이 만든 것을 쓴다 | 33, D11 |
| 35 | ~~Testcontainers 전환~~ | **D15 를 쓰면서 앞당겨 끝냈다.** `PostgresTestBase` 참조 | 완료 |
| 35a | ~~HTTP 계층 테스트~~ | **완료.** `HttpTestBase`·`HttpFlowTest` 참조. `D15` 가 세 층이 됐다 | 완료 |
| 35b | ~~계정 관리 축 HTTP 검증~~ | **완료.** 탈퇴의 두 기기 시나리오가 `HttpFlowTest` 에 들어갔다 | 완료 |
| 35c | ~~HTTP 층 상품 픽스처~~ | **완료.** `HttpTestBase` 에 `givenSellableProduct`·`givenSellerOwner`·`postWithIdempotencyKey` 와 주문 축 정리, `HttpFlowTest` 에 관통 4개. **버그 둘을 잡았다.** (1) **`V17` 지연 트리거가 `NEW` 를 믿어서 `POST /api/orders` 가 실서버에서 언제나 500 이었다** — `NEW` 는 트리거를 걸어 준 문장 시점의 행이라 `insert` 로 걸린 예약분이 커밋 때 `response_body = null` 을 본다. 행을 다시 읽도록 고쳤다(`V16` 의 `assert_order_amounts` 와 같은 모양). 통합 테스트 30개가 전부 롤백해서 그 트리거가 한 번도 안 돌았다 — **이 청크가 첫 커밋을 일으켜 드러났다.** (2) **`ApiExceptionHandler` 가 500 원인을 로그에 안 남겼다** — 주석엔 남긴다고 적혀 있었다. 안 고쳤으면 (1)을 못 찾았다. **정리를 한 트랜잭션으로 묶었다** — 나눠 지우면 `order_item` 만 지운 커밋에서 금액 등식이 깨진다 | 완료 |
| ~~35c 원안~~ | HTTP 층 상품 픽스처 | `HttpTestBase` 에 "팔 수 있는 상품" 을 만드는 헬퍼를 둔다. **지금 HTTP 층은 상품이 필요한 흐름을 못 덮는다** — 셀러 등록·신원 확인·검수까지 세워야 해서 청크 9 가 장바구니 병합을 서비스 층에만 남겼다. 주문(10)·결제(12)가 같은 벽을 만난다. `db/seed` 는 `local` 프로필이라 테스트가 안 태운다 | 9, 10-2 |
| 점검 A | ~~법 축 점검~~ | **완료** — `doc/notes/checkpoint-4-law.md`. 내려간 것 8·부분 5·미착수 4·주석뿐 1. 대장 18행을 채웠고 `D2-2`·`14b`·`13a-1` 로 처분했다. 원래 설명: `D2` 의 R1~R18 을 **요건표에서 코드로 내려가며** 훑고 `D2` 의 「강제 지점」 칸을 채운다. **코드에서 올라가면 안 보인다** — 없는 것이 위반이라서다(`D23`). 대장을 채우는 것이 곧 이 점검이라 둘을 안 가른다. 결과는 `doc/notes/checkpoint-4-law.md` | D2, D23 |
| D2-2 | ~~못 내린 요건에 근거 달기~~ | **완료.** `sku.price` → **`price_incl_vat`**, `order_item.unit_price` → **`unit_price_incl_vat`**(`V13`·`V16` 의 `create` 문 직접 수정, `docker compose down -v`). 제약 이름도 따라갔다. **`order_item` 까지 같이 옮긴 이유**는 그쪽이 더 나빴기 때문이다 — 부가세 언급이 주석에도 없었다. 응답 필드가 `price_incl_vat`·`min_price_incl_vat` 로 바뀌므로 **화면이 쓰기 전인 지금이 최저가다.** `R13` 근거는 `OrderActionService.Action.CANCEL` 에 남겼다 — 취소 주체가 제3자라 `scope=own` 으로 표현이 안 된다는 것까지. `D2` 대장·`D8`·`money-invariants` 갱신. 테스트 516개 통과, 실서버로 관통(옛 이름으로 보내면 400, 새 이름으로 201, DB·응답 모두 새 이름). **밟다 결함 하나가 나왔다 → `8c`.** 원래 설명: 점검 A 가 찾은 둘을 닫는다. **`R12`(부가세 포함가)의 강제 지점이 주석뿐이다** — 값만 봐서는 세금 포함 여부를 알 수 없어 `check` 로 못 막는 것으로 보이는데, **못 내린 것인지 안 내린 것인지가 코드에서 안 갈린다**(`D23` 축 2). **답이 정해졌다 — 이름으로 올린다**(사용자 선택). `sku.price` → `price_incl_vat`. 축 2 의 1순위(타입·스키마 구조)로 올라간다: 공급가액을 넣으려던 사람이 **이름을 보고 멈춘다.** 응답 필드도 같이 바뀌므로 **화면이 아직 안 쓰는 지금이 제일 싸다** — `14` 가 오기 전에 한다. **`R13`(미성년자 취소권)은 흔적이 0건이다** — `D2` 가 「안 다루기로 해도 왜 안 다루는지 적는다」고 요구했는데 없다. **근거를 취소 판정 자리에 남긴다**(사용자 선택, `OrderActionService` 의 취소 경로) — `D2` 에만 적으면 그 코드를 고치는 사람이 문서를 안 열고 지난다. `11b` 가 멀어서 그때까지 비워 두면 안 된다 | 점검 A, D2, D23 |
| 13h | ~~어느 칸인지 모르는 오류의 자리~~ | **완료.** `D20` 에 「모르는 칸을 지목하지 않는다」 절과 오류 자리 표의 새 행, 「셸」에 **`main` 표지는 셸이 한 번만 그린다**를 넣고 화면을 맞췄다. 로그인 폼이 `aria-invalid` 를 안 건다 — 그 속성의 뜻이 「이 칸의 값이 유효하지 않다」인데 **맞은 이메일까지 그렇게 낭독됐다.** `Field` 의 `invalid` 는 남겼고 기본값을 거짓으로 뒀다 — 가입 화면(`13d-1`)처럼 칸마다 검사하는 곳이 쓴다. `layout.tsx` 가 `<main id="main">` 을 그리고 화면 셋(`page`·`login/page`·`ComingSoon`)은 `div` 로 내렸다. **렌더된 마크업으로 확인**: `main` 1개·`id=main` 1개·건너뛰기 링크 1개가 같은 곳을 가리키고 `aria-invalid` 가 전부 거짓. 원래 설명: 점검 D 가 찾았다. 로그인 실패가 **두 입력칸에 모두 `aria-invalid`** 를 건다 — 서버가 어느 칸인지 일부러 안 가르는데(`D14`) 화면낭독기는 **맞은 이메일까지 「잘못된 입력」이라고 읽는다.** `D20` 의 오류 자리 표에 **「어느 칸인지 서버가 안 알려주는 오류」 줄이 없다** — 규칙을 먼저 고치고 화면을 맞춘다(`D23` 「이 문서를 고칠 때」). 같이 **건너뛰기 링크가 `<main>` 이 아니라 감싸는 `div#main` 을 가리키는 것**도 본다. 고치는 방법이 둘이고 `ComingSoon` 이 `main` 을 그리는 구조와 얽혀 있다 | 점검 D, 13-2, D20 |
| 4g | ~~필드 그룹 코드를 타입으로~~ | **완료.** `auth/FieldGroup` 인터페이스와 `order/OrderFields`·`me/UserFields` enum 신설, `Decision.canSee` 가 타입만 받는다, `FieldGroupTest` 3개. **`11a` 의 선례를 그대로 썼다** — 인터페이스만 `auth` 에 두고 목록은 자원 패키지에 둬서 `auth → order` 순환을 안 만든다. **목록이 둘인 이유는 `basic` 이 양쪽에 있는데 뜻이 달라서다** — 하나로 두면 주문 판정에 사용자 그룹을 물어도 컴파일이 통과하고 조용히 거짓이 온다. **바꾸자마자 컴파일러가 테스트 6곳의 생 문자열을 잡았다.** 대조 테스트는 `check` 가 아니라 **표**가 출처인 첫 사례고, 코드에만 있는 값을 넣어 깨지는 것을 확인했다. **`UserFields` 는 관객 패키지에 있다** — `5l` 이 `me` 를 가를 때 같이 옮긴다. 원래 설명: 점검 C 가 찾았다. `decision.canSee("basic")`·`"contact"`·`"shipping"` 다섯 곳이 **생 문자열**이다. 값 목록이 `permission_field_group.code` 에 있고 코드가 분기하므로 `D23` 이 enum 을 요구한다. **실패가 조용하다** — 오타를 내면 `canSee` 가 `false` 를 돌려주고 그 필드가 응답에서 빠지는데, 막히는 쪽이라 **오류도 로그도 안 남고** 화면에서는 「볼 수 없는 필드」와 구분이 안 된다(`D20` 이 갈라 둔 둘이 여기서 뭉개진다). 제약 대조 테스트도 같이 붙인다 | 점검 C, 4d, D23 |
| 5l | ~~`me` 패키지를 자원 단위로~~ | **완료.** `me/` 를 **`account/`**(`app_user` — `AccountService`·`WithdrawalService`·`AccountPurgeService`·`UserFields`·`MeController`)와 **`consent/`**(`ConsentService`·`ConsentItemController`)로 갈랐다. 테스트도 같이 옮겼다. **예외를 안 적고 코드를 옮긴 이유는 관례를 세어 봐서다**(사용자 선택) — Magento·Shopware·Sylius·Medusa·Spree·Saleor 가 전부 모듈은 자원 이름이고 관객은 그 안에서 가른다. **`me` 를 모듈로 판 곳이 하나도 없다**(축 1의 4위라 근거는 아니고 셈이다). **`MeController` 는 안 갈랐다** — 입구는 경로를 따라가고, 가르면 `/api/me` 를 그리는 코드가 두 패키지로 흩어진다. `D23` 「패키지」에 절을 하나 더하고 「지금 어긋난 곳」에서 이 행을 닫았다. 원래 설명: 점검 C 가 찾았다. `me` 는 자원이 아니라 **관객**("나")이라 패키지 경계 규칙(`D23`)과 어긋난다. 실제로 다루는 자원은 `app_user`(`AccountService`·`WithdrawalService`)와 `consent_item`·`user_consent`(`ConsentService`)로 둘이고, **`ConsentItemController` 는 로그인 전에 부르는 공개 조회**라 「나」와 맞지도 않는다. **그 규칙은 `product` 하나를 보고 세운 것**이라(점검 C 관찰) 규칙에 예외를 적는 쪽도 선택지다 — **옮길지 예외로 둘지를 먼저 정한다** | 점검 C, D23 |
| D14-1 | ~~보안 기준을 개정판에 맞춘다~~ | **완료.** `Password.java` 를 **15자**로(`@Size(min = 15)`), `D14` 의 비밀번호 절을 표준 등급(`SHALL`/`SHOULD`)과 준수 여부까지 적는 표로 다시 쓰고, **OWASP 매핑을 2025 판으로 다시 그렸다.** 신규 `A10`(예외 상황 처리 미흡)에 절을 하나 세웠다 — `ApiExceptionHandler`·`Retries`·**Redis 강등**·**캡차 fail-open** 이 그 항목이고 뒤의 둘이 핵심 물음(**무엇이 죽었느냐로 갈린다**)이다. `A03` 공급망 승격은 `2c` 의 요구사항을 늘렸고, `A09` 의 경보는 **사람에게 닿는 경로가 없다**(`54b`). SSRF 는 `A01` 로 흡수 — 없어진 요구가 아니라 자리가 옮았다. **테스트를 경계로 바꿨다**(14자 거부·15자 수락) — 7자로 두면 한 칸 밀려도 안 잡힌다. 되돌려서 잡히는 것을 확인했다. 한글 테스트도 15자를 넘겨서 **문자 집합만 걸리게** 고쳤다. 개발용 계정(`V901`)과 자동 채움도 정책에 맞췄다. 원래 설명: 점검 E 가 찾았다. **근거 둘이 개정됐다.** (1) **NIST SP 800-63B Rev 4** — 비밀번호가 단독 인증수단이면 **최소 15자 `SHALL`**(우리 8자), **블록리스트 대조 `SHALL`**(우리 없음). 나머지는 지킨다(최대 64·인쇄 ASCII+공백·조합 강제 안 함). **15자로 올린다**(사용자 선택) — `SHALL` 이고 출처 2순위라 규약보다 위다. `Password.java` 한 곳만 고치면 되는 것이 규칙을 유일한 출처로 모아 둔 보람이다. **같이 닿는 곳**: `D14` 문안, `V901` 개발용 테스트 계정(`1` 이라 통과 못 한다), 테스트 픽스처. 데모 시드(`demo-password-1234`, 19자)는 그대로 통과한다. **블록리스트는 여기서 안 한다** — `D14-2` 로 뺐다. (2) **OWASP Top 10 2025** — `D14` 가 2021 에 매핑돼 있다. **`A10 Mishandling of Exceptional Conditions` 가 신규**고 `ApiExceptionHandler`·`Retries`·**Redis 와 캡차의 fail-open** 이 정면으로 걸린다. **`A03 Software Supply Chain Failures` 가 3위**라 의존성 검증 절차가 필요하다. **`A09` 가 「Logging and Alerting」**으로 바뀌어 알림이 이름에 들어왔는데 우리는 없다(`D18`). 매핑을 2025 로 다시 그린다 | 점검 E, D14 |
| D13-1 | ~~표와 수명 대조~~ | **완료.** `DataLifecycleCoverageTest`(신설), `data-lifecycle.md`, `AccountPurgeService` 주석. **`information_schema` 의 표 목록과 문서를 이름으로 대조한다** — 정책 표에 「표」 칸을 더하고, 수명을 정할 것이 없는 표는 「수명을 안 정하는 표」 절에 **근거와 함께** 적게 했다. 파서는 칸 순서가 아니라 **머리글이 「표」인 칸**을 찾아서, 칸을 더해도 안 깨진다. **대조는 양방향이다** — 정책 없는 표(수명이 안 정해진 것)와 표 없는 정책 행(지워진 표를 가리키는 것)을 따로 본다. **드러난 것 셋**: ① `sku_stock` 이 빠져 있었다(이 청크가 세워진 이유), ② **`idempotency_key` 는 반대로 갔다** — 코드가 24시간마다 지우고 있는데 문서에 행이 없었다. 수명이 코드에만 있으면 문서를 읽는 사람이 그 표를 무기한으로 본다, ③ `order_contract_document` 도 없었다. **주석 하나가 코드와 어긋났다** — `AccountPurgeService` 가 `acted_ip` 유예를 30일로 적었는데 `Q10` 이 5일로 고칠 때 안 따라왔다. 날수를 지우고 `{@link #GRACE_DAYS}` 를 가리키게 해서 사본을 없앴다. **`—` 칸에는 어느 청크가 표를 세우는지 적게 했다**(알림 셋은 `54a`, 문의는 `58`) — 「나중에」로 끝나면 처분이 아니다. 35표가 양방향으로 맞는 것과, `sku_stock` 을 빼고 없는 이름을 넣어 **둘 다 빨개지는 것**을 확인했다. 원래 설명: **새 표를 만들면서 수명을 안 정해도 아무것도 안 깨진다.** 2026-08-21 에 표를 넷 만들었는데(`batch_run`·`payment_card`·`sku_stock`·`sku_stock_movement`) `data-lifecycle.md` 의 자원별 정책에 셋만 들어갔다 — 빠진 것을 잡은 것은 **사람이 센 것뿐**이다 | 완료 |
| D14-2 | 비밀번호 블록리스트 | **`D14-1` 이 남긴 나머지 `SHALL`** 이다(NIST SP 800-63B Rev 4) — 유출·사전·문맥 단어와 대조하라고 요구하는데 우리는 안 한다. **`D14-1` 에서 뺀 이유는 새 일이라서다** — 목록 출처(HaveIBeenPwned k-익명 API 냐 내려받은 목록이냐), 저장 방식, 검사 시점(가입·변경 둘 다), 그리고 **바깥을 부르면 그 장애가 우리 장애가 되는가**(캡차 fail-open 논거와 같은 물음, `D14`)를 먼저 정해야 한다. **정할 때까지 `SHALL` 하나가 열려 있다는 사실을 `D14` 에 적어 둔다**. **축**: 표준 — NIST SP 800-63B Rev 4 의 남은 `SHALL`(유출·사전·문맥 단어 대조). **강제 지점**: 검사 시점을 가입·변경 **둘 다**에 걸고 그것을 테스트로 — 한쪽만 걸면 바꿔서 우회한다. **건드리는 자리**: 신설 목록 저장소 또는 외부 조회기(`D14-2` 가 고른다). 수정은 `AuthSignupService`·`AccountService` | D14-1, D14 |
| 5m | ~~동의 쓰기를 한 입구로~~ | **완료.** `5l` 이 드러냈다 — 패키지를 가르고 나니 `account/WithdrawalService` 가 `user_consent` 에 직접 `insert` 하는 것이 경계를 넘는 쓰기로 보였다. **전에는 같은 패키지라 안 보였다.** `ConsentService.revokeAll(userId, actorIp)` 로 옮겼다. **위험은 규칙이 두 벌인 것**이었다 — append-only·종속 항목·필수 처리를 `ConsentService` 가 정하는데 탈퇴 경로가 그것을 안 지난다. 지금은 결과가 우연히 같지만 규칙을 고치는 사람이 탈퇴 경로를 못 본다. **`AccountPurgeService` 는 안 옮겼다** — 조건이 전부 `app_user.deleted_at` 이라 옮기면 `consent` 가 `app_user` 를 읽게 돼서 방향만 뒤집힌다. 그 선을 `D23` 「남의 자원 표를 언제 직접 만지나」에 적었다. 방향은 `account → consent` 단방향 그대로다 | 5l, D23 |
| 8c | ~~옵션 없는 상품의 조합이 상세에서 사라진다~~ | **완료.** `findPublicSkus` 를 `left join` 으로 바꾸고 `option_value_ids` 를 빈 배열로 내린다. **`getLong` 이 null 을 0 으로 주는 자리라 `getObject` 로 받아 null 을 거른다** — 안 거르면 옵션 없는 SKU 가 **없는 선택지 0 번을 가리킨다.** 옵션 축이 없는 상품을 만드는 픽스처와 상세 검증을 `ProductQueryTest` 에 뒀다. 원래 설명: `D2-2` 를 실서버로 밟다 나왔다. `ProductQuery.findPublicSkus` 가 `sku_option_value` 를 **inner join** 해서, **옵션이 없는 상품의 SKU 는 행이 하나도 안 나온다** — `skus: []` 로 내려간다. `ProductController` 가 「옵션이 없는 상품은 빈 배열이고 SKU 가 하나다」로 명시한 정상 상품인데 **상세에 살 수 있는 조합이 안 보인다.** 담기·주문은 `sku_id` 로 하므로 **화면만 못 그린다** — `14b` 가 이걸 모르고 만들면 그 상품이 통째로 안 팔린다. `left join` 으로 바꾸고 `option_value_ids` 를 빈 배열로 내린다. **테스트가 전부 초록인 채로 있었다** — `ProductQueryTest` 가 옵션 있는 상품만 본다 | 8b, 14b |
| 점검 D-2 | 프론트 재점검 — 실제로 밟는다 | **점검 D 는 화면이 둘일 때 한 것**이라(홈·로그인, 나머지는 자리표시) 「규칙이 지켜지며 시작했나」까지만 봤다. `15-5` 까지 끝나면 화면이 열넷이고, **코드만 읽어서 판정이 안 되는 것**을 그때 밟는다 — 실제 초점 순서, 낭독 순서, 축소 화면 배치. Puppeteer 를 쓴다(`stack.md`). `D20` 이 「수동 스크린 리더 검증」을 「청크가 서면 그때」로 미뤄 둔 자리다. **축**: `D20` 화면 규약, `D24`, WCAG. **강제 지점**: 없다 — 점검이다. 나온 것은 청크로 세운다. **건드리는 자리**: 코드는 안 건드린다. 결과는 `doc/notes/` 의 점검 노트와 `PLAN.md` 의 새 행 | 15, D20, D24 |
| D23-1 | ~~열거값을 어디에 두나~~ | **완료**(2026-08-12) — `coding-rules.md` 에 절 신설, `api-guidelines.md` 에 「모르는 열거값은 무시한다」, 청크 `7e` 신설. 코드는 안 건드렸다. **공통코드 테이블을 쓸지 묻는 데서 나왔고**, 국내 SI 관례와 대조해 **도메인 상수는 코드에 두고 운영이 늘리는 것만 표로 뺀다**로 갈랐다. **분할표에 늦게 올렸다**(`13b` 마무리) — 이력에는 있는데 여기 없어서 「끝난 청크」를 세는 표가 하나 모자랐다 | D23 |
| ~~D22-1~~ | 라이선스를 기록한다 | **점검 E 에 합쳤다**(사용자 선택) — 둘 다 `external-references.md` 를 만지므로 링크를 여는 김에 저작자·라이선스를 같이 적는다. 따로 하면 같은 45개 링크를 두 번 연다. 원래 설명: 점검 C 가 찾았다. `D23` 이 「인용 자료의 저작자·라이선스를 `external-references.md` 에 적는다」고 정했는데 **그 파일에 라이선스가 한 줄도 없다.** `D23` 이 직접 든 예(SQL Style Guide = CC BY-SA 4.0)조차 `coding-rules.md` 안에만 있다. 의존성은 훑어서 GPL 계열이 없는 것을 봤지만 **확인한 기록이 없다** — `D23` 이 말한 「판단한 적 없는 것과 판단해서 괜찮은 것은 다르다」가 지금 상태다. 공개로 돌리기 전에 닫아야 하는 자리다 | 점검 C, D23 |
| 11-5 | ~~주문 축 상태 목록에 방벽 걸기~~ | **완료.** `OrderStatusListTest` 신설 — 층마다의 제약 둘, **이력 제약은 `case` 갈래별로 갈라서** 대조한다(사용자 선택). 합집합만 보면 **배송 값이 결제 갈래에 섞인 것을 못 잡는다.** `app_user.status` 는 `auth/UserStatus` enum 으로 올리고 `UserStatusTest` 로 묶었다 — **`account` 가 아니라 `auth` 에 둔 이유는 순환이다**(`account → auth` 가 이미 있어서 반대 방향을 새로 내면 하나 는다. `StatusPolicy`·`FieldGroup` 과 같은 모양). **`seller.status` 는 안 고쳤다** — 자바 비교가 없고 SQL 리터럴뿐이라 `D23` 「SQL 텍스트 안의 리터럴은 안 바꿨다」가 이미 정한 자리다. **`35e` 를 흡수했다**(사용자 선택). **진짜 잡는지 확인했다** — `Payment` 에 값을 하나 더하니 층 제약과 이력 갈래 둘이 같이 깨졌고 원복했다. 원래 설명: 점검 B 가 찾았다. **`shop_order_status_check`·`seller_order_status_check`·`order_status_history_status_check` 셋에 대조 테스트가 없다** — `Payment`·`Shipment` enum 과 두 사본인데 `D23` 이 요구한 방벽이 상품 축(`7e`)에만 있다. **지금은 갈리지 않았다**(4/4·7/7 확인) — 방벽만 없다. **사본이 여섯이다**(enum 2 + 제약 4곳, 이력 제약이 `case` 로 목록을 한 번 더 적는다). 같이 **`app_user.status`·`seller.status` 의 생 문자열 비교를 enum 으로 올린다**(`"active".equals(...)`) — `7e` 가 상품 축에서 치운 것과 같은 모양이고 `D23` 의 「지금 어긋난 곳」이 비었다는 서술도 고친다 | 점검 B, D23 |
| 점검 B | ~~DB 제약 축 점검~~ | **완료** — `doc/notes/checkpoint-5-db.md`. 제약 55/55·트리거 16/16·금액 컬럼 16/16·`updated_at` 9/9, **사라진 제약 0**. `11-5` 로 처분했다. 원래 설명: 스키마에 실제로 걸린 제약과 문서가 말하는 제약을 대조한다. **`create` 문을 직접 고치는 관행 때문에 제약이 조용히 빠질 수 있다**(`D23` 「마이그레이션」) — 빠져도 테스트가 안 잡는다. 점검 A 가 채운 대장이 입력이다 | 점검 A |
| 점검 C | ~~규약·코딩 축 점검~~ | **완료** — `doc/notes/checkpoint-6-rules.md`. 3.9배 커지는 동안 새 위반 넷. `4g`·`5l`·`D22-1` 로 처분하고 `D22` 를 고쳤다. 원래 설명: 코드에서 규칙으로 **올라가며** 훑는다. `checkpoint-3-rules`(08-07, main 2,246줄) 이후 **8,689줄로 3.9배**가 됐고 그 사이 규칙도 늘었다(축 1 플랫폼 사실, 축 2 안 나가게 하는 것, 패키지 경계). 새로 생긴 절부터 본다 | D22, D23 |
| 점검 D | ~~프론트 점검~~ | **완료** — `doc/notes/checkpoint-7-frontend.md`. 접근성 항목 전부 초록이고 **대비비를 직접 계산해 통과**를 확인했다. `13h`·`점검 D-2` 로 처분했다. 원래 설명: `D24` 기준으로 `frontend/src` 751줄을 훑는다. **화면 축이 이제 막 열려서 대상이 짧다** — 어긋난 것을 지금 고치는 값이 제일 싸다 | D24 |
| 점검 E | ~~기준 자료가 아직 유효한가~~ | **완료** — `doc/notes/checkpoint-8-references.md`. **핵심 근거 여섯 중 둘이 개정됐다**(NIST SP 800-63B Rev 4·OWASP Top 10 2025) → `D14-1`. `D22-1` 을 합쳐 라이선스도 기록했다. **45개 중 여섯만 봤고** 나머지는 다음 회차다. 원래 설명: `external-references.md` 의 링크 확인이 **2026-08-05** 다. 표준은 개정된다 — RFC 9110 이 422 의 이름을 바꿔서 청크 `2d` 가 그걸 치웠다. **웹 조회가 필요하다.** 다른 점검과 섞지 않는다 — 섞으면 한 점검이 끝없이 불어난다. **`D22-1`(라이선스 기록)을 여기 합쳤다**(사용자 선택) — 같은 문서를 만지므로 링크를 여는 김에 저작자·라이선스를 같이 적는다 | 점검 C |
| 점검 F | ~~축 여섯 재점검~~ | **완료**(2026-08-20) — 노트 파일을 따로 안 썼다. 찾은 것이 **축 문서에 바로 내려갔다**(`D2` 의 2026-08-20 절, `api-guidelines.md`·`screen-rules.md`·`coding-rules.md`). 축 여섯(법·표준·DB 제약·규약·관례·코딩)으로 **직전 3일치**를 훑어 **서른둘**(`F1`~`F9`·`S1`~`S8`·`B1`~`B5`·`P1`~`P4`·`C1`~`C3`·`K1`~`K3`)을 찾고 자리별 처분 청크 `Q1`~`Q15` 로 잘랐다. **그중 열하나는 그 문서를 읽고도 안 걸린 것**이다(표준 여덟·관례 셋) — 「읽기는 재발을 못 막는다」의 근거가 여기서 나왔다. **둘을 되돌렸다** — `S5` 는 방향이 반대였고 `B4` 는 `V26` 주석을 안 읽고 판정한 것이라 철회했다. 오판 6% 가 이 단위의 비용이다 | 완료 |
| 점검 G | ~~묶을 청크 검토~~ | **완료**(2026-08-23) — 노트 파일을 안 썼다. 찾은 것이 **분할표 행에 바로 내려갔다**(`점검 F` 와 같은 판단). 미착수 예순셋에서 「합쳐서 칠 것」을 골랐더니 **`43`+`44` 하나만** 남았고, 나머지 셋은 **묶는 문제가 아니라 순서·분해 문제**였다 — `60`+`61` 은 사슬 넷이 막고(`42` 를 갈라야 풀린다), 파일 축은 **문서를 낄 자리**가 필요하고, 웹훅+아웃박스는 **선행 칸에 이미 순서가 박혀 있었다.** 덤으로 `43` 의 선행이 **없는 청크(`11`)를 가리키던 것**을 고쳤다 | 완료 |
| 점검 H | ~~세로 축 · 타입 줄~~ | **완료**(2026-08-29) — 노트 파일을 안 썼다. 찾은 것이 분할표 행 둘로 바로 내려가서다(`F`·`G` 와 같은 판단). **훑는 방향을 바꿔야 했다** — 타입 175개(`record` 149·`enum` 26)를 훑으면 「이 타입이 왜 있나」를 묻게 되는데, 알고 싶은 것은 **「타입으로 막을 수 있는데 안 막은 자리」**고 그건 타입 쪽에 흔적이 없다. **DB 가 값 목록을 닫아 둔 자리(`check (X in (...))`, 27개)에서 「Java 가 이 값을 무슨 타입으로 드나」를 물었다.** `CLAUDE.md` 의 세는 단위를 그렇게 고쳤다. **찾은 것 둘**: ① **열거형이 있는 열아홉 중 DB 제약과 대조되는 것이 넷뿐이다**(`app_user_status`·`shop_order_status`·`seller_order_status`·`seller_exempt_reason`). `D23` 이 「제약 정의를 읽어 enum 과 대조하는 테스트를 둔다」고 **규칙으로 적어 뒀는데 적용이 4/19** 고, **이 세션이 세운 열하나가 전부 대조 밖**이다 → `43a-19`. ② **닫힌 목록인데 열거형이 없는 컬럼이 열이다** → `43a-20`. **「걸려 있다」와 「돈다」가 또 갈렸다** — 규칙은 있고 실물은 넷이었다. **`43a-19` 가 이 수를 고쳤다**: 4/19 가 아니라 **7/18** 이다(`ProductStatusTest` 가 셋을 한 파일에서 대조하는데 파일 수로 셌다). 안 걸린 열하나는 맞았다 | 완료 |
| 점검 I | ~~가로 축 · 법 줄~~ | **완료**(2026-08-29) — 노트를 안 썼다(고친 것이 요건표 자체로 내려갔다). **요건표에서 코드로 내려갔다** — 가로는 반대로 못 간다. **셋을 물었다.** ① **요건표가 부르는 이름이 아직 실재하나** — 강제 지점 칸의 식별자 **206개를 뽑아 저장소와 대조했고 전부 실재한다**(`A09` 하나는 OWASP 항목 번호라 오탐). 이 세션이 `Actor.person`·`enumValue`·상수 여럿을 없앴는데 요건표가 부르던 것은 없었다. ② **`점검 A` 뒤에 늘어난 `R34`~`R37` 이 강제 지점을 채웠나** — 넷 다 찼다. `점검 F` 가 지적한 둘(`R34` 는 게시 중단이 `psql` 로만, `R36` 은 약관 문구에만)이 `59-2`·`43`·`43a-2` 로 닫혔다. ③ **요건이 가리키는 청크가 분할표에 있나** — **여덟 행이 없는 청크를 가리켰다.** `11`·`12`·`13a`·`54` 가 나중에 잘게 갈렸는데(`11-2`·`12-1`·`13a-1`·`54a`) **요건표가 옛 이름을 그대로 들고 있었다.** 글 참조라 **지금 고쳤고**, 왜 고쳤는지를 표 머리말에 적었다 — 가리키는 청크가 없으면 「이 요건이 누구 소관인가」에 답이 안 나오고, **「승격을 빠뜨리지 않는 방법은 선행이다」 장치도 청크 이름이 맞아야 돈다**. **「아직 안 적은 법」 열넷은 전부 「근거가 생기는 자리」를 가리킨다** — 처분이 다 돼 있다. **「미착수」 넷**(`R17`·`R27`·`R29`·`R30`)은 기능 자체가 없는 것이라 청크를 기다린다 | 완료 |
| ~~35e~~ | 제약 대조 헬퍼 합치기 | **`11-5` 에 흡수했다**(사용자 선택) — 그 청크가 세 번째 사본을 만드는 자리라 따로 두면 사본 셋인 구간이 생긴다. `test` 쪽 `support/ConstraintValues` 한 벌로 합쳤고 넓은 쪽 정규식(`[a-z0-9_]+`)을 남겼다. 원래 설명: `check` 정의를 읽어 enum 과 대조하는 헬퍼가 `ProductStatusTest`·`SellerQueryTest` 두 벌이고 **정규식이 다르다** — 앞은 소문자만 훑아서 `under_50_transactions` 같은 값을 놓친다. 공용 테스트 헬퍼로 뺀다. **`D23` 이 「세 번째 사본이 생긴다」고 경계한 것과 같은 모양의 사본**이라 그 절이 자기 도구에 안 걸려 있었다 | 14a |
| 36 | ~~스케줄러 도입~~ | **완료.** `V38__batch_run.sql`·`BatchRuns`·`AccountPurgeBatch`·`TransactionPurgeBatch`·`BatchRunsTest`(신설), `TransactionPurgeService`·`AccountPurgeService`·`batch-catalog.md`·`data-lifecycle.md` 수정. **파기 배치 둘이 운영에서 한 번도 안 돌고 있었다** — 로직은 `5i`·`10a` 에 다 서 있는데 부르는 곳이 테스트뿐이라 개인정보 파기가 실행 자체가 없었고, **아무 테스트도 안 깨져서** 두 청크를 그대로 지났다. **배치 클래스를 서비스와 갈랐다** — 파기 서비스가 `@Transactional` 이라 거기에 `@Scheduled` 를 붙이면 회차 기록이 같은 트랜잭션이 되고 **실패 행이 롤백돼서 안 남는다**(카탈로그의 클래스 칸을 그래서 고쳤다). **강제 지점 둘**: `(batch_name, baseline_date)` 부분 유니크가 성공 회차를 하나로 묶고(정산 마감이 두 번 지급하는 것을 스키마가 거부한다), **`@Scheduled` 가 붙어 있는지를 반사로 보는 테스트**가 「로직만 있고 안 도는」 상태의 재발을 막는다. `batch_run` 보존 1년은 거래기록 파기에 얹었다. **2층 회차 재시도와 자동 구매확정 이력은 `36a` 로 뺐다** | 완료 |
| 36a | ~~회차 재시도와 자동 구매확정 이력~~ | **완료.** `V42__batch_run_failure_kind.sql`·`RetryableBatch`·`BatchRetrySweeper`·`BatchRetrySweeperTest`(신설), `BatchRuns`·`OrderStatusBatch`·`AccountPurgeBatch`·`TransactionPurgeBatch`·`BatchRunsTest`·`OrderStatusBatchTest`·`batch-catalog.md` 수정. **`D19` 2층에 코드가 없던 것을 세웠다** — 스위퍼가 10분마다 오늘 회차를 훑어 **마지막 실패가 일시적이고 시도가 셋 미만인 것**만 다시 돌린다. **판단을 실패한 순간에 내린다** — SQLSTATE 로 갈라(`08*`·`40001`·`40P01` 만 일시적) `failure_kind` 에 적는다. 이력만 보고 나중에 가르면 예외 이름 문자열을 파싱하게 되고 판단이 두 벌이 된다. **모르는 실패는 결정적으로 본다** — 넓게 잡으면 같은 자리에서 세 번 죽는다. **시도 횟수를 메모리에 안 둔다** — 이력의 행 수가 곧 시도 수라 기동해도 안 사라진다. **자동 구매확정이 이력을 남기기 시작했다** — 기준일이 있는 배치라 `D19` 기준으로 대상이었고, 그 이력을 처음 쓰는 것이 `19`(정산 마감)의 체인 판정이다. **강제 지점**: `failure_kind` 의 `check` 두 개(실패 행에만·값 둘만)와 **결정적 실패를 안 돌리는 것을 고정한 테스트** | 완료 |
| 36b | ~~배치 스케줄러 스레드 풀~~ | **완료.** `application.yml`·`BatchSchedulingTest`(신설)·`batch-catalog.md`. `spring.task.scheduling.pool.size` 를 **6**(배치 수)으로 정했다. **찾은 것**: 밀리는 자리가 상시가 아니라 **매월 1일 04:00** 이고, 그 시각에 크론 셋이 같이 예약된다(개인정보 파기·자동 구매확정·거래기록 파기). 그동안 5분짜리 둘과 10분짜리 하나가 전부 멈추는데, **멈추는 쪽이 더 급한 것을 들고 있다** — 거래기록 파기는 하루 늦어도 아무 일이 없고 환불 스위퍼가 밀리면 3영업일 기한이 흐른다. **풀 하나를 배치 수만큼 잡았다**(사용자 선택) — 스케줄러를 둘로 가르는 안도 저울질했는데 `@Scheduled(scheduler = ...)` 를 배치 여섯에 다 붙여야 해서, 분할표가 「배치 코드는 안 건드린다」로 예보한 모양과 멀어진다. **`fixedDelay` 는 앞 회차가 끝나야 다음이 잡혀서 배치 하나가 스레드 둘을 안 쓴다** — 그래서 배치 수면 충분하다. **강제 지점은 설정값이 아니라 테스트 둘이다**: ① `ScheduledTaskHolder` 로 등록된 배치 수를 세서 풀과 비교(배치를 더하면 여기가 빨개지고 고칠 곳을 말해 준다), ② 그 수만큼 붙잡아 두고 **전부 시작되는지** 확인(스레드가 하나면 큐에 쌓여 시작조차 못 한다). 「새 배치를 더할 때」 절에 풀을 같이 올리는 항목을 더했다. 원래 설명: `@Scheduled` 가 여섯인데 스레드가 하나다 — `spring.task.scheduling.pool.size` 를 안 정해서 Spring 기본값 1 로 돈다 | 완료 |
| 37 | ~~미결제 주문 정리~~ | **완료 — `11-3` 이 이미 했다.** `OrderStatusBatch.expireUnpaidOrders` 가 결제대기 30분 초과 주문을 `payment_expired` 로 옮기고 재고를 되돌린다(5분 `fixedDelay`, `D19` 카탈로그 첫 줄). 이 행이 적어 둔 강제 지점(「행을 지우지 않고 상태만 옮긴다」)도 전이 제약이 이미 지고 있다. **분할표에만 미착수로 남아 있었다** — `PLAN-2` 의 대조는 방향이 하나라(이력 → 분할표) 이력에 없는 행은 못 잡는다. 원래 설명: 일정 시간이 지난 결제대기 주문을 취소한다 | 완료 |
| 38 | ~~Redis 컨테이너~~ | **완료.** `docker-compose.yml` 에 `redis` 서비스, `starter-data-redis`, `application.yml` 연결 설정, `PostgresTestBase` 에 Redis 컨테이너, `RedisConnectionTest` 4개. **선행에서 `D21` 을 뺐다** — 컨테이너를 띄워 붙이는 데 성능 목표가 필요 없다(`42` 에서 뺀 것과 같은 논리). **컨테이너를 나눈다** — 한 컨테이너에 프로세스가 둘이면 하나가 죽어도 healthy 로 보인다. **의존성만 넣어도 캐시가 Redis 로 넘어갈 뻔했다** — 명시적 `CacheManager` 빈이 막고 있고 그걸 테스트로 고정했다 | 완료 |
| 39 | 판정 캐시 이관 | 로컬 캐시를 Redis 로 옮긴다. 무효화가 여러 인스턴스에 퍼지나. **축**: `concurrency-rules.md`(무효화 전파), `D16`(강등 시 `WARN` — `5c-2` 가 남긴 자리). **강제 지점**: Redis 가 죽어도 판정이 되는 경로 + 그때 `WARN` 한 줄. **건드리는 자리**: `4a` 의 판정 캐시 구현. Redis 설정은 `docker-compose.yml` 에 이미 있다 — 컨테이너를 새로 안 만든다 | 38 |
| 40 | 집계 스키마 | 통계용 테이블 또는 뷰. 실시간 집계와 사전 집계의 갈림길. **축**: `D8` 금액, `D21`(집계 비용). **강제 지점**: 집계 결과와 원장의 합을 대조하는 테스트 — 사전 집계를 고르면 그 값이 언제 낡는지가 문제고, 대조가 그것을 드러낸다. **건드리는 자리**: 신설 집계 표 또는 뷰. 기존은 읽기만 한다 | D8, 12 |
| 41 | 통계 API·화면 | 셀러는 자기 것만. 스코프가 집계에 섞이는 자리. **축**: `D14` 스코프, `D5` 목록 규약, `D20`. **강제 지점**: 집계 쿼리도 판정 스코프를 지나게 한다 — **집계는 권한이 새기 제일 쉬운 자리다**. **건드리는 자리**: 신설 통계 입구와 셀러·관리자 화면. `40` 의 집계 표를 읽는다 | 40, 4 |
| 42 | 성능 측정·튜닝 | 실행 계획을 읽고 인덱스를 고친다. **목표 수치 없이 돈다** — 느린 쿼리를 찾는 데 기준선이 필요 없고, 여기서 나온 수치가 `D21` 의 입력이 된다. **축**: 측정 — 실행 계획을 읽는다. 목표 수치 없이 돈다(`D21` 이 이 결과를 입력으로 쓴다). **강제 지점**: 없다 — 여기서 나온 인덱스가 다음 청크의 근거가 된다. **건드리는 자리**: 인덱스 마이그레이션과 느린 쿼리(`OrderQuery`·`ProductQuery`·`SellerOrderQuery`) | 8 |
| 43 | ~~반품 스키마~~ | **완료**(`44` 흡수). `V63__return_request.sql`·`ReturnRequestSchemaTest`(둘 다 신설), `OrderTransitions`·`TransactionPurgeService`·`TransactionPurgeBatch`·`state-machines.md`·`commerce-compliance.md`(`R36`·`R37` 신설)·`data-lifecycle.md`·`identifier-rules.md` 수정. 표 넷 — `return_request`(진행)·`return_request_item`(부분 반품)·`return_pickup`(수거지, 6개월)·`return_note`(사람 글, 6개월). **묶음은 요약, 반품 표가 진행**이고 어긋남은 지연 트리거 둘이 양방향으로 막되 **상태를 안 옮긴다**. 전이 둘이 늘었다 — `return_requested → delivered`(거절 복귀), `confirmed → return_requested`(제17조제3항 3개월). 파기 둘을 **접수 입구(`43a`)보다 먼저** 넣었다. **`R36`(제18조제9·10항 부담 주체)·`R37`(제17조제5항 입증책임)이 섰다** — 그전에는 약관 문구에만 있어서 강제 지점 5순위였다 | 완료 |
| 44 | ~~반품 상태머신~~ | **`43` 이 흡수해서 같이 쳤다**(점검 G 가 묶었다). | 청약철회 기간과 제한 사유가 전이 조건이 된다. **`copyable_media` 의 판정이 여기서 선다**(`Q6`, `R4`) — 제17조제2항4호는 「복제가 가능한 재화등의 **포장을 훼손한 경우**」라 물건이 돌아와야 아는 사실이고, 제17조제5항이 그 입증을 우리에게 지웠다. **그래서 접수는 안 막는다** — 검수에서 훼손이 확인됐을 때 거절하는 전이가 이 청크의 일이다. **축**: **법**(제17조제2항 제한 사유, 제5항 입증 책임), `D7` 상태기계. **강제 지점**: 전이표에 넣어 상태 제약이 막게 한다 — 「접수는 막지 않고 판정에서 가른다」가 `Q6` 의 결론이라 접수 경로에 조건을 안 건다. **건드리는 자리**: `OrderTransitions`·`OrderStatusPolicy` 와 반품 표 **묶음**: `43` 이 이 청크를 흡수해서 같이 친다(2026-08-23 검토) — 위 행에 근거와 걸릴 것을 적었다. **닫을 때 여기도 같이 닫는다** | 43, 11a |
| 43a-1 | ~~반품 접수 행~~ | **완료**(2026-08-26). `ReturnRequestService`(신설)·`OrderStatusService`·`OrderActionTest` 수정. **`43` 이 표를 세우고 넣는 코드를 안 만들어서 반품이 통째로 막혀 있었다** — 묶음만 `return_requested` 로 가면 `V63` 의 지연 트리거가 커밋에서 거부한다. **데모 시드 기동에서 처음 드러났다**: 그 트리거는 걸린 뒤 한 번도 안 돌았고(테스트는 롤백한다) 빌드도 CI 도 초록이었다. 접수가 같은 트랜잭션에서 `return_request` + `return_request_item`(묶음 통째, `D7`)을 연다. **전이 서비스 안에서 부른다** — 입구마다 부르면 새 입구가 빠뜨린다(`D23` 축 2). **강제 지점을 새로 안 만들었다** — `V63` 의 지연 트리거가 이미 그 자리고 이 청크는 그것을 통과시키는 쪽이다. 테스트가 `set constraints all immediate` 로 커밋 검사를 그 자리에서 돌린다 | 43 |
| 43a-2 | ~~반품 입고·판정~~ | **완료**(2026-08-28). `V64__return_decision.sql`·`ReturnController`·`ReturnDecisionTest`(셋 다 신설), `OrderActionService`·`OrderStatusPolicy`·`ReturnRequestService`·`OrderStatusService`·`ErrorCode`·`ShipmentController`·`actions.ts`·`OrderActionTest`·`OrderStatusPolicyTest`·`state-machines.md`·`commerce-compliance.md`·`permission-rules.md` 수정. **동작을 셋으로 갈랐다**(`receive_return`·`approve_return`·`reject_return`)**고 `update_status` 에서 `return_requested` 를 뺐다** — 그전에는 셀러가 `DELIVER` 로 **반품 거절 복귀**를 밀 수 있었고 `D7` 은 「관리자만」이라고 문서로만 말했다. **판정 둘은 관리자다**(사용자 선택) — 제17조제5항이 훼손 책임의 입증을 우리에게 지워서(`R37`) `state-machines.md` 가 승인을 「셀러」라 적은 것을 **문서부터 고쳤다**. **부담 주체는 요청 칸을 안 만들었다** — 셋이 전부 결정적이라 계산이고, 칸을 두면 `check` 가 막아도 고를 수는 있게 된다(`R36`). **접수 취소를 조문으로 닫았다** — 제17조제4항 발신주의 + 민법 제543조제2항이라 소비자의 일방적 취소권이 없다. **드러난 것 둘**: 거절 복귀가 `freezeDeadlines` 를 다시 불러 **청약철회 기산점이 오늘로 밀렸을 것**이고(`from == SHIPPING` 으로 막았다), 재고 사유 목록(`V41`)에 반품이 없어서 복구가 `order_cancelled` 로 들어갔을 것이다(`return_restocked` 를 더했다) | 43 |
| 43a-3 | ~~확정 뒤 반품~~ | **완료**(2026-08-29). `OrderStatusPolicy`·`OrderStatusPolicyTest`·`OrderActionTest` 수정. **`43` 이 전이표에 `confirmed → return_requested` 를 냈는데 `request_return` 의 허용 상태가 `delivered` 뿐이라 길은 났고 문이 잠겨 있었다** — 확정한 사람의 하자 신고가 통째로 거부됐다. 제17조제3항의 3개월은 구매확정으로 안 끝난다(`D2` R3). **동작을 안 갈랐다**(사용자 선택) — `request_return_defect` 를 따로 만들어 상태 축으로 막는 안이 더 세지만, **사는 사람이 7일 안에 손으로 확정할 수 있어서** 그때 제17조제1항의 권리를 우리 장치가 자른다. 사유 판정은 이미 있는 `requireWithdrawable` 이 하고 강제 지점은 앱 검증(3위)으로 남는다. **경계 넷을 관통 테스트로 못박았다** — 손확정 뒤 기한 안 변심 통과 / 기한 지난 변심 409 / 확정 두 달 뒤 하자 통과 / 넉 달 뒤 하자 409. **빈 DB 기동 + curl 로 넷을 다시 밟았다** — `allowed_actions` 가 확정 뒤 `REQUEST_RETURN` 하나를 내리고, 하자 접수가 커밋돼서 `V63` 의 지연 트리거를 지났다. **드러난 것 둘**: `terminalIsEmpty` 가 구매확정을 종착으로 보고 있었고(취소로 바꿨다), `return_reason` 이 JSON 에서 `DEFECT` 만 받는다(`43a-5` 로 흡수) | 43a-2 |
| 43a-4 | 반품 처리 기한 | 소비자분쟁해결기준(공정위 고시)이 「며칠 안에 처리하나」를 정한다. `commerce-compliance.md` 「아직 안 적은 법」이 `43a` 를 가리켰는데 **`43a-2` 가 못 닫았다** — 고시 원문을 확인해야 요건 번호가 서고, 그건 표를 세우는 일이라 판정 입구와 단위가 다르다. 지금 있는 것은 `StaleBundleBatch` 의 30일 `WARN` 하나뿐이고 그 수는 근거가 없다. **축**: **법**(소비자기본법 + 고시. 원문부터 연다). **강제 지점**: 기한을 접수 때 박제할지 매번 계산할지가 `D10` 「계산한 기한은 박제한다」에 걸린다. **건드리는 자리**: `commerce-compliance.md` 요건표, `return_request`(기한 컬럼이 생기면 `V65`), `StaleBundleBatch` | 43a-2 |
| 43a-5 | 반품 수거·검수·화면 | `43a-3` 이 확정 뒤 반품을 열었다. 남은 것은 **수거지**(`return_pickup`)·**검수 소견**(`inspected`·`return_note.inspection_note`)과 **화면 셋** — 셀러의 입고·검수, 관리자의 판정, 사는 사람의 반품 진행. **수거지는 개인정보라 셋이 다 차야 컬럼을 만든다**(`D23`) — 동의 항목·`D13` 수명·파기 코드. **검수를 상태로 세울지가 갈린다** — `inspected` 를 세우면 전이표·권한·화면이 같이 늘고, 입고 안에서 소견만 받으면 상태가 안 는다. **`43a-6` 이 표기를 닫았다** — 응답도 요청도 `DEFECT` 라 화면은 대문자 하나만 알면 된다. **축**: `D20` 화면 규약, WCAG, **법**(제17조제5항 입증책임 — 검수 소견이 그 근거다). **강제 지점**: 검수 소견은 `inspected_by_user_id` 가 이미 짝을 강제한다. 화면은 `lint` 가 접근성을 받는다. **건드리는 자리**: `ReturnController`(수거·검수), `frontend/src/app/seller/orders`·`admin`, 사는 사람의 주문 상세 | 43a-3 |
| 43a-6 | ~~응답 열거값 표기~~ | **완료**(2026-08-29). `OrderStatusService`·`SellerOrderQuery`·`order-text.ts` 수정, `ResponseEnumCaseTest` 신설. **`43a-3` 이 넘긴 것을 열어 보니 방향이 반대였다** — `D5` 「값의 형식」이 열거값을 대문자 스네이크로 이미 정했고 `enumValue()` 를 지나는 15곳이 그것을 지키는데, `SellerOrderQuery` 의 `return_reason` **한 칸만 그 호출을 빠뜨려서** 저장값 `defect` 가 새 나갔다. 화면 `RETURN_REASON` 표가 그 샌 값에 맞춰 소문자로 쓰여 있었다. 규약이 아니라 코드가 틀린 것이라 **`D5` 는 안 고쳤다**. **강제 지점을 타입(1위)으로 내렸다**(사용자 선택) — `Detail.returnReason` 을 `String` 에서 `ReturnReason` 으로 바꾸면 표기를 고르는 자리가 없어져 빠뜨림이 성립하지 않는다. `ReturnReason.of` 는 `ProductQuery` 의 관례를 따라 **모르는 저장값에 터진다**. **거기에 대조 테스트를 얹었다** — 칸 이름을 안 적고 **저장값 집합을 들고 응답 전체를 훑어서** 새 칸이 생겨도 등록 없이 걸린다. **그 테스트가 자기 자신을 검증한다**(저장값을 일부러 실어 보고 잡히는지 본다). **드러난 것 둘**: 대조가 `_visible_field_groups` 의 `"shipping"` 을 헛짚었다 — 필드 그룹 이름과 배송 상태 코드가 글자만 같아서, `D5` 의 「밑줄은 메타 필드」를 근거로 메타 칸을 건너뛰게 했다. `OrderQuery`·`SellerOrderQuery` 의 `enumValue` 는 **그냥 `toUpperCase()` 라 DB 의 모르는 값을 그대로 올려 보낸다** — `ProductQuery` 는 enum 을 지나 막는다(`43a-7` 로 세웠다) | 43a-3 |
| 43a-7 | ~~주문 상태를 열거형으로 지나게 한다~~ | **완료**(2026-08-29). `OrderTransitions`(`statusName` 신설)·`OrderQuery`·`SellerOrderQuery`·`ResponseEnumCaseTest` 수정. **`enumValue` 사본이 여섯인데 `ProductQuery` 하나만 열거형을 지났다** — 나머지 다섯은 `toUpperCase()` 라 DB 에 모르는 값이 들어와 있으면 그대로 응답에 실려 나간다. **여섯을 한 청크로 못 쳐서 잘랐다**(파일 1~3개) — 여기서는 **열거형이 이미 있는 주문 쪽 상태**만 옮겼다. 결제 층은 `Payment.of`, 배송 층은 `Shipment.of`, **상태 이력은 두 층이 한 목록에 섞여 들어와서** `OrderTransitions.statusName` 이 둘 다 찾아본다(값이 안 겹친다). **강제 지점이 타입(1위)이다** — 어긋난 순간 터진다. 제약을 떼고 모르는 값을 넣어 **실제로 터지는 것을 테스트로 고정**했다(지연 트리거 때문에 `set constraints all immediate` 를 먼저 부른다). **`enumValue` 에 남은 넷을 주석에 못박았다** — `actor_type`·`clause`·`reason_code`·`method`. 넷 다 코드가 값마다 분기하는데(`"admin".equals(actor.type())`, `"card".equals(command.method())`) Java 열거형이 없다 | 43a-6 |
| 43a-8 | ~~주문 쪽 열거형 둘~~ | **완료**(2026-08-29). `ActorType`·`ContractClause`(둘 다 신설), `OrderQuery`·`ResponseEnumCaseTest` 수정. **넷을 자원별로 갈랐다**(사용자 선택) — `actor_type` 은 `Actor.` 쓰는 곳까지 14파일, `reason_code`·`method` 도 13~14파일이라 한 청크에 안 들어간다. **읽는 쪽만 바꾼다**: `enumValue` 자리에서 열거형을 지나게 하고 `Actor.type`·`Command.method` 는 문자열로 남긴다 — 그쪽까지 가는 것(`D23` 「생 문자열을 쓰지 않는다」의 완성)은 이 열거형들을 쓰기만 하면 되므로 **여기가 그 선행이고 후속이 `43a-15`·`43a-16` 이다**(2차 마무리에서 세웠다 — 그전에는 「선행이다」라고만 하고 후속이 계획에 없었다). **`clause` 입력은 안 건드렸다** — 조항 하나를 펼치는 경로는 모르는 값에 조회가 0건이라 404 로 답하는데, 거기서 `of()` 를 부르면 없는 조항이 500 이 된다. **`43a-6` 의 대조 사각지대가 절반 닫혔다** — 값 집합에 둘을 더했고, **그 훑기가 빈 응답을 본 것이 아님을 따로 못박았다**(`detailActuallyCarriesScannedFields`). 「걸려 있다」와 「돈다」는 다르고 이 저장소가 이미 밟은 함정이다 | 43a-7 |
| 43a-9 | ~~결제 쪽 열거형 둘~~ | **완료**(2026-08-29). `PaymentStatus`·`PaymentMethod`(둘 다 신설), `OrderQuery`·`ResponseEnumCaseTest` 수정. **분할표가 남은 수를 틀리게 적고 있었다** — `43a-7`·`43a-8` 이 「남은 넷/둘」이라 적으면서 **환불 상태와 결제 상태를 빼먹었다.** 세어 보니 넷이고(`payment.status`·`payment.method`·`refund.status`·`refund.reason_code`) `payment` 패키지에 열거형이 하나도 없었다. 그래서 **앞서 고른 자원별 가르기를 그대로 적용해 결제/환불로 또 갈랐다** — 여기는 결제 둘. **이름이 부딪쳤다**: `shop_order.status`(주문이 어디까지 왔나)와 `payment.status`(승인 시도 한 건의 결과)가 둘 다 `paymentStatus` 였다. **부딪쳤다는 것 자체가 둘이 헷갈린다는 증거**라 주문 쪽을 `orderPaymentStatus` 로 갈랐다. 분기 근거는 `"card".equals(...)` 두 곳(`PaymentService` 검증·`MockPaymentGateway` 판정)과 **`approved` 상수가 두 클래스에 중복 선언**된 것이다. **열거형은 `payment` 패키지에 뒀다**(`D23` 「남의 자원 표를 언제 직접 만지나」 — 값 목록은 그 자원의 규칙이다). `OrderQuery` 가 이미 그 표를 직접 읽어서 공개했다 | 43a-8 |
| 43a-11 | ~~환불 쪽 열거형 둘~~ | **완료**(2026-08-29). `RefundStatus`·`RefundReason`(둘 다 신설), `OrderQuery`·`RefundQuery`·`RefundService`·`ResponseEnumCaseTest` 수정. **`RefundService` 에 값 목록이 이미 상수 여덟으로 모여 있어서** 열거형을 그냥 세우면 **같은 목록이 두 벌**이 된다. 상수를 없애려면 부르는 자리가 서른이라 한 청크에 안 들어간다. 그래서 **상수를 열거형에 매달았다**(`APPROVED_CODE = RefundStatus.APPROVED.code()`) — 정본이 하나로 남고 호출자를 안 건드린다. **하나씩 없애는 것이 `43a-16` 이다**(2차 마무리에서 「나중에」로 끝나 있던 것을 청크로 세웠다). **`OrderQuery.enumValue` 를 지웠다** — 호출자가 0이 됐는데 남겨 두면 다음에 열거 칸을 더하는 사람이 그것을 베낀다. **`43a-6` 이 잡은 것이 정확히 그 일**이었다. 사유마다 **기산점 조문이 갈리는 것**을 열거형 주석에 실었다(제18조제2항 3호 / 제15조제2항, `V25` 의 근거). **대조 사각지대가 다 닫혔다** — 응답 열거값이 전부 열거형을 지난다 | 43a-9 |
| 43a-12 | ~~문의 쪽 열거형 둘~~ | **완료**(2026-08-29). `InquiryStatus`·`InquiryKind`(둘 다 신설), `InquiryService`·`InquiryQuery` 수정. **`43a-11` 과 다르게 상수를 없앴다** — 거기는 호출자가 서른이라 상수를 열거형에 매달았는데 여기는 **`InquiryService` 한 파일 14곳**이라 갈아탈 수 있다. `D23` 「Java 표현 — 생 문자열을 쓰지 않는다」를 실제로 달성한다. **상수가 이미 불완전했다** — `kind` 가 DB 에 다섯인데 상수는 셋뿐이었다(`access_objection`·`dispute` 없음). 열거형이 정본이면 그 어긋남이 안 생긴다. **`kind` 는 대상·기한·보존을 한꺼번에 정한다** — `product`·`order` 만 대상이 붙고, `processing_stop` 만 10일이며(개인정보 보호법 시행령 제44조제2항, `D2` R28), `dispute` 는 3년 보존이다. 그 셋을 열거형 주석에 실었다. **`ResponseEnumCaseTest` 는 안 넓혔다** — 그것은 주문 자원의 응답을 훑는 테스트고 문의는 다른 자원이다. 강제 지점이 이미 타입(1위)이라 훑기(4위)보다 세다. **문의 응답이 대조 밖에 있다는 사실은 `43a-10` 행에 적었다** | 43a-11 |
| 43a-13 | ~~정산 쪽 열거형 셋~~ | **완료**(2026-08-29). `SettlementItemKind`·`SettlementSupplier`·`PayoutStatus`(셋 다 신설), `SettlementQuery`·`SettlementPayoutService` 수정. **부호·공급자 규칙을 열거형에 안 실었다** — `V52` 가 이미 정했다: 부호는 `settlement_item_amount_sign_check`(2위), 근거는 `settlement_item_source_check`(2위), **공급자는 생성 열이라 어긋날 방법 자체가 없다**(1위, 구조). Java 필드로 옮기면 **규칙이 두 벌이 되고 강제 지점이 앱 검증(3위)으로 내려간다** — `D23` 축 2 가 「가능한 한 아래로」라 정반대다. 열거형은 **값 목록과 표기만** 맡고, 다음 사람이 옮기려 드는 것을 막는 근거를 주석에 적었다. 상수는 `43a-12` 처럼 없앴다(`SettlementPayoutService` 한 파일 10곳). **`enumValue` 사본이 둘로 줄었다** — 남은 `SellerOrderQuery`·`ProductQuery` 는 **둘 다 이미 열거형을 지난다** | 43a-12 |
| 43a-10 | ~~표기 자리를 한 곳으로 모은다~~ | **완료**(2026-08-29). `EnumValue`(신설, `support/`), `OrderQuery`·`SellerOrderQuery` 수정, `coding-rules.md`·`testing-strategy.md` 보강. **세어 보니 판단이 뒤집혔다** — 남은 사본이 둘인 줄 알았는데 **같은 모양의 헬퍼가 여섯 파일에 열여덟 벌**이었다(`code == null ? null : X.of(code).name()`). **진짜 사본은 `enumValue` 라는 이름이 아니라 null 가드 + `name()` 패턴**이고, 사본이 그만큼이면 한 벌이 패턴을 안 따라도 아무도 모른다 — `43a-6` 이 잡은 사고가 그것이다. **공용 자리 먼저, 자원별로 갈아탄다**(사용자 선택) — 여기서는 `EnumValue` 를 세우고 **주문 쪽 열 벌**만 옮겼다. **`support/` 에 뒀다**(`D23` 「자원이 아닌데 패키지를 파는 경우」의 조건 셋 중 「기존 비자원 패키지에 넣으면 순환이 된다」가 안 걸린다) — 부르는 쪽이 열거형을 적으므로 **이 자리는 열거형을 하나도 모른다**. **헬퍼 이름이 하던 문서 역할이 타입 이름으로 옮겨갔다** — `EnumValue.of(x, OrderTransitions.Payment::of)` 와 `EnumValue.of(x, PaymentStatus::of)` 가 그 자리에서 갈린다. **훑기는 안 넓혔다**(사용자 선택) — 근거를 `D15` 에 절로 세웠다: 훑기가 잡는 것은 「헬퍼를 안 부르고 생 문자열을 실은 새 칸」뿐이고, 위험은 자원마다 같지만 **비용도 자원마다 든다**. **훑기는 사고가 난 자리에 세우는 것**이지 미리 까는 것이 아니다 | 43a-13 |
| 43a-14 | ~~남은 넷을 EnumValue 로 갈아탄다~~ | **완료**(2026-08-29). `RefundQuery`·`InquiryQuery`·`SettlementQuery`·`ProductQuery` 수정. **표기 헬퍼가 0이 됐다** — 열여덟 개가 사라지고 호출 스물다섯 곳이 `EnumValue` 하나를 지난다. **섞여 있던 구간이 닫혔다**(`43a-10` 이 대가로 적어 둔 것). **`ProductQuery.reasonValue` 도 같이 갔다** — `WithdrawalRestrictionReason.of` 가 `null` 을 돌려주는 다른 모양이라 안 맞을 줄 알았는데, **`EnumValue` 가 `null` 을 먼저 거르므로** 저장값이 있으면 `of` 가 열거형을 주거나 터진다. **`OrderTransitions.statusName` 만 안 갔다** — 그것은 상태 이력 한 줄이 어느 층인지 모르는 채로 **두 층을 찾아보는** 다른 일이라 공용 자리가 대신할 수 없다 | 43a-10 |
| 43a-15 | ~~전이 주체를 열거형으로 받는다~~ | **완료**(2026-08-29). `OrderStatusService.Actor` 와 호출자 셋 수정. **팩터리를 주체별로 갈랐다** — `person(String, long)` 을 없애고 `customer(long)`·`seller(long)` 을 세웠다. `system`·`admin` 은 이미 그 모양이었고 **나머지 둘만 문자열로 뭉쳐 있었다**. 이제 **문자열 인자가 아예 없어서** 오타도 역할 코드 혼입(`seller_owner` 대 `seller`)도 성립하지 않는다. 필드도 `ActorType` 이라 `"admin".equals(actor.type())` 이 `actor.type() == ActorType.ADMIN` 이 됐고, 저장할 때만 `code()` 를 지난다. **드러난 것**: 테스트가 `Actor.person("admin", userId)` 로 **사유 없는 관리자**를 만들고 있었다 — 「관리자여도 사유가 없으면 막힌다」를 보려고 `person` 을 우회로로 쓴 것이다. `Actor.admin(userId, null)` 로 바꿔 뜻을 그대로 적었다 | 43a-8 |
| 43a-16 | ~~결제·환불 도메인의 생 문자열을 없앤다~~ | **완료**(2026-08-29). `PaymentController`·`PaymentService`·`MockPaymentGateway`·`RefundService`·`RefundSweeper`·`RefundQuery`·`DemoOrderSeeder`·`checkout-form.tsx` 수정, `api-guidelines.md` 보강, 테스트 아홉 파일. **`D5` 와 실물이 어긋난 것을 먼저 처리했다** — `D5` 는 「422 = 형식은 맞는데 값이 규칙에 안 맞음」인데 요청 필드를 enum 으로 받으면 **역직렬화에서 걸려 400** 이라 검증기까지 못 간다. **400 으로 통일하고 `D5` 에 예외를 절로 적었다**(사용자 선택) — 문자열 + `@Pattern` 이면 422 를 낼 수 있지만 값 목록이 `check`·enum·정규식 **셋**이 된다. **사본이 하나 느는 값이 오류 코드 하나보다 크다.** **계약이 바뀌었다**: `method` 요청 값이 `card` → `CARD`. 응답의 `method` 는 이미 대문자였고 요청만 소문자였다 — **화면(`checkout-form.tsx`) 한 줄과 테스트 둘을 고쳤다**(사용자 선택). **멱등키 지문은 `code()` 를 넣는다** — 열거값 이름을 넣으면 보관 중인 지문과 안 맞아 같은 요청이 다른 것으로 보인다. `RefundService` 상수 여덟을 없애고 `Pending.status` 를 `RefundStatus` 로 바꿨다 — 그전에는 `RefundStatus.APPROVED` 와 `PaymentStatus.APPROVED` 가 **글자가 같아서** 한쪽을 다른 쪽 자리에 넣어도 컴파일이 통과했다 | 43a-11 |
| 43a-17 | 환불 표의 주체도 ActorType 으로 | **`43a-15` 가 전이 주체를 타입으로 올렸는데 같은 값 집합이 문자열로 남았다**(3차 마무리에서 걸렸다). `refund.requested_by_type` 의 목록이 `customer`·`seller`·`admin`·`system` 으로 **`ActorType` 과 완전히 같다** — `V25` 가 「`order_status_history` 가 같은 문제를 이미 풀었다(`V18`), 같은 모양을 쓴다」고 적어 뒀는데 **코드는 한쪽만 타입이 됐다**. `RefundService:182,194` 가 `new Requester("customer", userId)` 로 만들고 `RefundSweeper:289,292` 가 `"customer".equals(actorType)` 로 분기한다. `approved_by_type`(`admin`·`system`, `V51`)은 그 부분집합이다. **결정**: `ActorType` 은 지금 package-private 이라(`43a-15` 가 「`order` 밖에서 안 쓴다」고 뒀다) **여는 것부터 정해야 한다** — 열거형을 열지, `payment` 에 같은 목록을 또 세울지. 후자면 `V25` 가 경계한 「같은 것을 두 곳에서 정한다」가 된다. **축**: **규약**(`D23` 「Java 표현」·「열거값을 어디에 두나」). **강제 지점**: 타입. **건드리는 자리**: `ActorType` 접근 범위, `RefundService.Requester`, `RefundSweeper.reasonOf` | 43a-15 |
| 43a-18 | 반품 표의 열거값 둘 | `43a-16` 뒤에도 반품 표에 생 문자열이 남았다(3차 마무리). ① `return_request.status`(`requested`·`received`·`approved`·`rejected`, `V63`·`V64`) — `ReturnRequestService` 가 `"approved".equals(status)` 로 분기한다. ② **반환 비용 부담 주체**(`seller`·`consumer`) — `bearerOf` 가 그 값을 문자열로 만든다. **부담 주체가 법이다** — 제18조제9항·제10항이 사유로 부담을 가르고(`D2` R36) `43a-2` 가 「요청 칸을 안 만든다, 전부 결정적이라 계산이다」로 닫은 자리다. **계산 결과를 열거형으로 두면** 그 계산이 낼 수 있는 값이 목록으로 드러난다. **축**: **법**(제18조제9·10항, `D2` R36) + **규약**(`D23`). **강제 지점**: 타입. **건드리는 자리**: `ReturnRequestService`, `ReturnController`, 신설 열거형 둘 | 43a-16 |
| 43a-19 | ~~열거형과 제약을 대조한다~~ | **완료**(2026-09-03). `EnumConstraintTest` 신설, `UserStatusTest`·`ProductStatusTest`·`OrderStatusListTest`·`SellerQueryTest` 에서 같은 단언을 걷어냈다. **한 파일에 모았다**(사용자 선택) — 결정의 이유는 대조가 아니라 **누락 감시자**다. 열거형마다 옆에 두면 「새 열거형이 대조를 안 받는 것」을 막는 것이 아무것도 없고, `점검 H` 가 찾은 구멍이 정확히 그 모양이었다. `everyEnumIsAccountedFor` 가 `main` 이 컴파일된 자리를 걸어 열거형을 전부 뽑고, `PAIRS`·`GENERATED`·`EXEMPT` 어디에도 없으면 빨개진다. **`점검 H` 의 수를 고쳤다** — 4/19 가 아니라 **7/18** 이다. `ProductStatusTest` 가 상품·조합·청약철회 사유 셋을 이미 대조하는데 파일 수로 셌다. **안 걸린 열하나는 정확했다.** **셋이 실물에서 갈렸다**: ① 감시자가 **중첩 열거형 넷**을 더 찾았다(`AuditLog$Kind`·`AdvertisingGate$Verdict`·`OrderActionService$Action`·`OrderStatusService$ReturnReason`) — 이름으로 훑은 목록에 없던 것이라 **감시자가 첫 실행에서 값을 했다.** ② 그중 `ReturnReason` 은 `seller_order_return_reason_check`·`return_request_reason_code_check` 둘에 걸려 있어 `PAIRS` 로 갔다. ③ `SettlementSupplier` 는 `check` 가 없다 — `settlement_item.supplier` 가 **생성 열**이라 목록이 생성 식 안이고, `information_schema` 의 `generation_expression` 에서 `then` 뒤만 뽑는다. **열거형이 `package-private` 이라 `code()` 를 리플렉션으로 부른다** — 대안이 대조 하나 때문에 열여덟의 접근 범위를 넓히는 것인데 그러면 `43a-15` 가 정한 「`ActorType` 은 `order` 밖에서 안 쓴다」 같은 결정이 테스트 때문에 풀린다. **실제로 도는 것을 봤다** — `main` 에 열거형 하나를 임시로 넣으니 감시자가 그 이름을 대고 빨개졌고, 지우니 초록으로 돌아왔다 | 43a-16 |
| 43a-20 | ~~열거형이 없는 닫힌 목록 열 — 권한~~ | **완료**(2026-09-03). `Scope`·`Effect`(신설), `PermissionEvaluator`·`PermissionRuleLoader`·`PermissionCatalog` 수정. **`점검 H` 가 센 열 개 컬럼을 자원별로 갈랐고**(`CLAUDE.md` 청크 상한) 이 청크는 **권한 축 둘**이다 — 나머지는 `43a-22`~`43a-25`. **두 `switch` 의 `default` 가 이 청크의 이유다** — `covers` 의 `default -> false` 와 `scopeWidth` 의 `default -> 0` 이 짝이라, 범위를 하나 더하면 **권한을 준 사람은 줬다고 믿는데 아무도 안 통과하고 오류도 로그도 안 남았다.** 지금은 `default` 가 없어서 **값이 늘면 컴파일이 안 된다**(1위). **응답 계약이 바뀌었다**(사용자 선택) — `GET /api/me/permissions` 의 `scopes` 가 `["own"]`→`["OWN"]` 이다. `D5` 「값의 형식」을 따른 것이고 `43a-16` 의 `card`→`CARD` 와 같은 자리다. **`Scope` 만 `public` 이다** — `PermissionCatalog.Entry` 가 공개 응답 record 라 그 자리를 `String` 으로 두면 아무 문자열이나 들어간다. **프론트에 합집합 타입을 안 만들었다** — 머리가 `resource`·`action` 으로만 고르고 범위 값을 비교하는 자리가 없어서, 적으면 서버와 안 맞춰지는 사본만 는다. **드러난 것 둘**: `PermissionMatrixTest` 가 `List.of("own","seller","all")` 로 **세 번째 사본**을 들고 있어서 `values()` 로 바꿨다(스냅샷은 그대로 — `.code()` 라 글자가 같다). `@Nested class Effect` 가 새 열거형을 가려서 `EffectPriority` 로 고쳤다. **`gradlew build`·프론트 넷 통과** | 점검 H |
| 43a-22 | 닫힌 목록 열 — 셀러 상태 | `seller.status`(`pending`·`active`·`suspended`, `V4`). **`43a-20` 이 가른 나머지 중 하나다.** 심사 상태라 화면과 판정이 같이 본다 — `SellerQuery` 가 `active` 만 공개하고(`14a`), 신원 `check` 도 `active` 에만 걸린다(`3c`). **응답에 나가는지부터 본다** — 나가면 `43a-20` 처럼 대문자가 되고 화면이 같이 바뀐다. **축**: **규약**(`D23` 「가르는 물음」·「열거형으로 분기할 때 `default` 를 안 둔다」). **강제 지점**: 타입. **건드리는 자리**: 신설 열거형, `SellerQuery`, `EnumConstraintTest` 의 `PAIRS` 한 줄 | 43a-20 |
| 43a-23 | 닫힌 목록 열 — 정산 배치 | `batch_run.status`(`succeeded`·`failed`·`skipped`, `V38`)와 `failure_kind`(`transient`·`permanent`, `V42`). **둘이 짝이다** — `V42` 가 `failure_kind` 를 `failed` 일 때만 채우게 걸어 놨으니 열거형도 그 짝을 들어야 한다. 재시도 판정이 `transient` 를 보고 갈린다. **축**: **규약**(`D23`). **강제 지점**: 타입. **건드리는 자리**: 신설 열거형 둘, 배치 기록·재시도 자리, `EnumConstraintTest` 두 줄 | 43a-20 |
| 43a-24 | 닫힌 목록 열 — 재고 사유 | `sku_stock_log.reason`(`initial`·`order_placed`·`order_cancelled`·`adjustment`, `V41`. `V64` 가 반품분을 더해 다섯이 됐다). **값이 마이그레이션 둘을 지나며 늘었다** — 늘 때마다 코드가 따라왔는지 아무도 안 봤다. **축**: **규약**(`D23`). **강제 지점**: 타입 + `43a-19` 의 대조. **건드리는 자리**: 신설 열거형, 재고 기록 자리, `EnumConstraintTest` 한 줄 | 43a-20 |
| 43a-25 | ~~닫힌 목록 열 — 알림 이력의 축 셋~~ | **완료**(2026-09-03). `NotificationKind`·`NotificationChannel`·`NotificationStatus`(신설), `NotificationService`·`NotificationTemplates`·`AdvertisingNotifications`·`NotificationSendTest` 수정. **넷을 한 청크로 못 쳤다** — `event_type` 은 발송 자리가 넷이라 파일이 열 개가 된다. 이력의 축 셋만 치고 사건 아홉은 `43a-26` 으로 갈랐다. **`kind` 는 법이 든다**(정보통신망법 제50조) — 광고는 사전 동의와 야간 동의를 보고 거래 통지는 안 본다. **`channel` 은 값이 하나뿐인데도 열거형이다** — 「가르는 물음」이 묻는 것은 값이 몇 개냐가 아니라 값이 늘 때 코드를 고쳐야 하나이고, 채널이 늘면 보내는 코드도 주소 찾는 코드도 갈린다. 그전에는 발송 SQL 안에 `'email'` 이 박혀 있어서 **고칠 자리를 쿼리 본문에서 찾아야 했다.** **`.query(Version.class)` 자동 매핑을 뗐다** — 저장값이 소문자라 `valueOf` 가 못 읽고, 읽더라도 모르는 값에 조용히 실패한다. **강제 지점을 하나 더 세웠다**(이 청크의 값) — 거래 통지 입구로 광고 판이 나가는 것을 막는다. 접근 범위만으로는 안 막히는 자리였다: 광고 입구는 패키지 안이지만 **공개 `send` 는 누구나 부르고** 거기 광고 판 코드가 들어오면 관문을 안 지난 광고가 나간다. **종류가 타입이 되기 전에는 비교할 것 자체가 없었다.** 관통 테스트를 붙였고 판이 없을 때도 같은 예외라 **메시지까지 고정**했다. **드러난 것 하나**: `password_reset`·`email_change` 가 `check` 에 있는데 **Java 도 시드도 그 값을 안 만든다** — `43a-26` 이 열거형을 세울 때 같이 처분한다. **`gradlew build` 통과** | 43a-20 |
| 43a-26 | 닫힌 목록 열 — 알림 사건 아홉 | `notification.event_type`(`V43` 일곱 → `V45` 여덟 → `V46` 아홉). **마이그레이션 셋을 지나며 늘었고 늘 때마다 코드가 따라왔는지 아무도 안 봤다.** 지금은 발송 자리마다 생 문자열이다 — `NotificationSweeper` 넷(`order_placed`·`payment_completed`·`supply_delayed`·`refund_completed`·`consent_result`), `AdvertisingNotifications`(`advertisement`), `ConsentReconfirmSweeper`(`consent_reconfirm`). **`43a-25` 가 찾은 것은 마무리에서 답이 났다** — `password_reset`·`email_change` 가 `check` 에 있는데 Java 도 시드도 안 만드는 이유는 **미착수 청크가 그것을 만들기 때문**이다. `D18` 「무엇을 보내나」가 둘을 `5c-1`(비밀번호 재설정)·`5e-1`(이메일 변경 확인)으로 이미 배정해 뒀다. **그러니 안 지운다** — 열거형이 아홉을 다 들고, 둘은 **아직 아무도 안 보내는 상수**로 남는다. 그 사실을 Javadoc 에 적어서 다음 사람이 「죽은 값」으로 지우지 않게 한다. **사건과 템플릿 코드가 같은 값인 것도 같이 본다**(`send(eventType, eventType, ...)`) — 광고만 갈라져 있다. **축**: **규약**(`D23` 「가르는 물음」). **강제 지점**: 타입. **건드리는 자리**: 신설 열거형, `NotificationService` 서명, 발송 자리 넷, `EnumConstraintTest` 한 줄 | 43a-25 |
| 43a-27 | 근거 없는 `public` 을 좁힌다 | **4차 마무리가 세웠다.** `43a-20` 과 `43a-25` 가 같은 물음에 **반대 답**을 내면서 규칙이 하나 생겼다 — `Scope` 는 **공개 응답 record 에 실려서** 열었고, `NotificationTemplates.Version` 은 **패키지 밖 참조가 0이라** 좁혔다. 규칙은 「`public` 에는 근거가 있어야 한다」이고, **저장소 전체가 그런지는 아무도 안 봤다.** 세는 법이 기계적이다 — `main` 의 `public` 타입 중 **자기 패키지 밖에서 참조가 0인 것**. `NotificationTemplates` 자신과 `Rendered` 가 이미 후보다(`43a-25` 가 `Version` 만 좁히고 나머지는 안 봤다). **어디까지 좁히나가 결정이다** — 테스트가 같은 패키지라 대개 안 깨지지만, `@Component` 빈이나 Jackson 이 읽는 record 는 프레임워크가 접근한다. **점검이 아니라 청크인 이유**는 훑는 것이 아니라 고치는 것이 일이어서다 — 세는 것은 컴파일러가 한다. **축**: **규약**(`D23`). **강제 지점**: 타입(1위) — 좁히면 잘못 쓰는 코드가 컴파일이 안 된다. **건드리는 자리**: 좁힐 타입들과 그것을 부르는 자리 | 43a-25 |
| 43a-21 | 열거형이 표의 행과 갈리는 것 | **`43a-19` 의 감시자가 드러냈다.** 닫힌 목록이 `check` 가 아니라 **표의 행**인 자리가 있다 — `OrderActionService.Action` 의 `permission` 문자열(`update_status`·`approve_return` 등)이 `permission` 표의 행을 가리키는데 **대조가 없다.** 권한 하나를 표에서 지우거나 이름을 고치면 그 동작이 **판정에서 조용히 거부**되고, 컴파일도 테스트도 초록이다. `FieldGroupTest` 가 `permission_field_group` 에 대해 이미 같은 모양을 세워 뒀으니 **방식이 정해져 있다**. `43a-19` 에서는 `EXEMPT` 에 이유를 적고 넘겼다 — `check` 대조와 **뽑는 자리가 달라서** 같은 표에 못 넣는다. **축**: **규약**(`D23` 「목록이 둘로 갈리는 것을 테스트가 막는다」). **강제 지점**: 테스트(4위). **건드리는 자리**: `Action` 을 대조하는 테스트, `EnumConstraintTest` 의 `EXEMPT` 한 줄 | 43a-19 |
| ~~45~~ | ~~교환 처리~~ | **안 한다**(2026-08-23 마무리, 사용자 선택). `state-machines.md` 「안 다루는 것」이 이미 「교환은 반품 후 재주문으로 대신한다」고 정해 놨는데 청크만 살아 있었다 — 문서가 이긴다. **왜 안 하나**(`D23` 「안 넣은 것도 근거를 남긴다」): ① **전이가 두 배로 는다** — 반품 상태 여섯에 재출고 갈래가 얹힌다. ② **반품 + 재주문이 같은 결과를 낸다.** 소비자가 얻는 것이 같고 우리가 지는 의무도 같다. ③ **갈래가 넷에 하나씩 붙는다** — 반품(회수), 재고(두 번 움직인다), 정산(수수료를 반환하고 다시 뗀다), 배송비(재출고분을 누가 무나). 넷 다 이미 굴러가는 축이라 각각에 예외가 생긴다. **되살릴 조건**: 동일 상품 사이즈 교환처럼 **재주문이 어색한 경우**가 실제로 세어져서 나올 때 | 안 한다 |
| 46 | 리뷰 스키마 | 평점, 본문, 사진, 대상 주문 참조. **축**: `D22` 이름 규칙, `D3`. 표시광고법(후기) 요건은 `D2-8` 이 통독한 뒤 걸린다. **강제 지점**: 주문 항목 참조를 `not null` 로 — 산 것과 안 이어진 리뷰를 만들 수 없게 한다. **건드리는 자리**: 신설 리뷰 표. 주문 항목을 `not null` 로 가리키므로 `order_item` 을 읽는다 | 6, 11 |
| 47 | 리뷰 작성 자격 | 산 사람만 쓴다. 주문 이력이 권한 조건이 된다. **축**: `D14` — **권한 조건에 데이터가 들어오는 첫 자리다**. **강제 지점**: 주문 항목당 1건 유니크 + 판정. 앱에서만 세면 두 번 눌러 두 건이 된다. **건드리는 자리**: `permission_rule` 에 리뷰 작성 규칙과 판정 조건. 리뷰 입구는 `46` 이 만든 표를 쓴다 | 46, 4 |
| 48 | 셀러 답글·신고 | 셀러는 자기 상품 리뷰에만 답한다. 신고는 관리자가 본다. **축**: `D14` 스코프, `D7`(신고 상태), `D20`. **강제 지점**: 「자기 상품」을 판정 스코프로. 신고 처리는 전이표에 넣는다. **건드리는 자리**: `46` 의 리뷰 표에 답글·신고 칸 또는 신설 표, 그리고 셀러·관리자 입구 | 47 |
| 49 | 쿠폰 스키마 | 발급 조건, 사용 조건, 할인 방식, 부담 주체. **전자금융거래법을 `D2` 에 승격시킨다**(점검 A) — 쿠폰·포인트가 선불전자지급수단이 되면 충전·잔액·환불 의무가 붙는다. **축**: **법**(전자금융거래법 — 선불전자지급수단이 되면 충전·환불 의무가 붙는다, `D2` 승격), `D8`. **강제 지점**: 부담 주체와 할인 방식을 `check` 로 좁힌다 — 자유 문자열이면 정산이 사유별로 못 가른다. **건드리는 자리**: 신설 쿠폰 표 둘(정의·발급). 기존은 안 건드린다 | D8, D3, D2 |
| 50 | 쿠폰 적용 | 적용 순서와 중복 사용 규칙. 금액 계산이 여기서 복잡해진다. **축**: 프로젝트 규약 — 금액은 원 단위 정수(`D22`), 계산 순서를 한 곳에서만 정한다. **강제 지점**: 적용 결과를 주문에 박제한다(`V26` 과 같은 이유). 화면이 다시 계산하지 않는다. **건드리는 자리**: `OrderService` 의 금액 계산과 주문 항목 박제. 쿠폰 적용 결과를 주문에 박는다 — 화면이 다시 계산하지 않는다 | 49, 10-2 |
| 51 | 쿠폰 정산 반영 | 몰 부담과 셀러 부담이 정산에서 갈린다. **축**: `18` 이 세운 정산 규약. **강제 지점**: 부담 주체 컬럼 + `check`. 몰 부담과 셀러 부담을 앱에서 나누면 정산서와 안 맞는 자리가 생긴다. **건드리는 자리**: `17`·`18` 의 정산 항목에 부담 주체 칸. 쿠폰 표는 읽기만 한다 | 50, 18 |
| 52 | ~~재고 스키마~~ | **완료.** `V40__sku_stock.sql`·`SkuStockTest`(신설), `OrderService`·`OrderStatusService`·`CartService`·`ProductQuery`·`ProductService`·`DemoOrderSeeder`·`V902` 시드·테스트 20여 파일 수정, `concurrency-rules.md`·`domain-model.md`·`coding-rules.md` 갱신. **재고를 상품 정의에서 갈랐다**(사용자 선택) — `sku.stock_count` 가 상품 표에 얹혀 있어서 재고가 바뀔 때마다 `sku.updated_at` 이 밀렸고, 이동 이력(`53`)이 붙으면 진실이 두 곳이 될 자리였다. **진실은 여전히 카운터다** — 집계로 판정하면 음수를 막던 조건부 `UPDATE`(`D11`)가 경쟁에 약해진다. **가용 재고를 생성 컬럼으로 뒀다**(`on_hand - safety_stock`) — 앱이 빼면 화면·장바구니·주문이 각자 빼서 셋이 갈린다. 조건부 `UPDATE` 의 조건이 그 컬럼이라 안전 재고가 그냥 걸린다. **재고 행 없는 sku 를 지연 제약 트리거가 막는다** — 만들어 주는 트리거로 하면 수량 채우는 문장이 반드시 뒤따라야 해서 빠뜨릴 자리가 생기고, 확인만 하면 한 문장(CTE)으로 넣을 수 있다. **강제 지점 셋**: 지연 제약, `on_hand >= 0` check, 그리고 **`sku` 에 재고 이름이 다시 생기면 빨개지는 테스트** | 완료 |
| 53 | ~~재고 이동 이력~~ | **완료.** `V41__sku_stock_movement.sql`·`SkuStockMovementTest`(신설), `OrderService`·`OrderStatusService`·`SkuStockTest`·`OrderServiceTest`·`OrderStatusServiceTest`·`OrderConcurrencyTest`·`ProductQueryTest`·`concurrency-rules.md`·`data-lifecycle.md` 수정. **이력은 설명이지 진실이 아니다** — 팔 수 있나는 여전히 `sku_stock` 의 조건부 UPDATE 가 답한다(`52` 의 결정). **이력 없는 변동을 구조에서 없앴다**(사용자 선택) — 재고를 옮기는 일을 `move_stock()` 하나로 모으고 `on_hand` 직접 UPDATE 는 트리거가 거부한다. 함수가 조건부 UPDATE 와 이력 INSERT 를 같이 해서 한쪽만 도는 자리가 없고, **0행의 뜻을 `false` 로 그대로 지킨다**(`D11`) — 예외로 올리면 재고 부족이 진짜 고장과 같은 자리로 온다. **재고 행을 만들 때도 `initial` 한 줄이 남는다** — 안 남기면 합계가 처음부터 어긋나 「얼마부터 셌나」를 따로 알아야 한다. **주문 생성에서 재고 차감을 주문 뒤로 옮겼다** — 이력이 「어느 주문 때문인가」에 답하려면 그 시점에 주문이 있어야 하고, 다투는 행을 늦게 잡는 편이 잠금을 짧게 문다. **강제 지점 넷**: 사유 `check`, `quantity <> 0`, 직접 UPDATE 차단 트리거, 그리고 **이력 합계 = `on_hand` 대조 테스트** | 완료 |
| 53a | ~~재고 이동 함수의 예외 경로~~ | **완료 — 전제가 틀렸다.** `SkuStockMovementTest`(테스트 둘)·`stack.md`. **마이그레이션은 안 만들었다.** 계획은 「플래그가 켜진 채 남아서 세이브포인트로 삼키면 차단이 뚫린다」였는데 **돌려 보니 안 뚫린다** — `set_config(..., is_local := true)` 로 켠 값은 GUC 스택에 서브트랜잭션 단위로 쌓여서 그 구간이 abort 되면 켜기 전으로 돌아가고, **예외를 삼키려면 abort 가 반드시 먼저 온다**(Postgres 는 오류 뒤 세이브포인트로 안 되돌리면 그 트랜잭션에서 아무것도 못 한다). 세이브포인트 롤백과 plpgsql `exception when others` 두 갈래로 확인했고 둘 다 플래그가 빈 값에 직접 `UPDATE` 가 거부됐다. **그래서 `exception` 블록을 안 넣었다**(사용자 선택) — 막는 것이 없는데 plpgsql `exception` 블록은 암묵 세이브포인트라 재고 이동마다 서브트랜잭션이 하나씩 생긴다(`D23` 「안 넣은 것도 근거를 남긴다」). **처분은 정보다** — 플랫폼 사실이라 `stack.md` 「기억으로 쓰면 틀리는 자리」에 절을 세웠다. **근거를 `V41` 주석에 안 적었다** — 적용된 마이그레이션을 고치면 Flyway 체크섬이 어긋난다. 테스트 둘은 `PROPAGATION_NESTED` 로 삼킨 뒤 ① 직접 `UPDATE` 가 여전히 거부되는 것과 ② 정상 이동이 여전히 되는 것을 본다. 터뜨리는 방법은 `on_hand + Integer.MAX_VALUE` 로 int 를 넘기는 것이라 **플래그를 끄기 전에 `UPDATE` 문 자체가 터진다**. 원래 설명: `move_stock()` 이 세션 플래그를 켜고 `UPDATE` 뒤에 끄는데, **그 사이에서 던지면 켜진 채로 남는다** | 완료 |
| 54a | ~~알림 스키마와 파기~~ | **완료.** `V43__notification.sql`(신설)·`NotificationSchemaTest`(신설)·`TransactionPurgeService`·`TransactionPurgeBatch`·`data-lifecycle.md`·`TransactionPurgeServiceTest`. **`54` 를 둘로 갈랐다**(사용자 선택) — 한 청크로 치면 파일 일곱이라 청크 규칙의 두 배고, 끊겼을 때 남는 중간이 스키마다. **표가 셋인 이유는 수명이 셋이라서다**(`D18-1`): 템플릿 판은 안 지우고, 메타는 거래 5년·광고 6개월, 본문은 6개월. **강제 지점을 전부 제약으로 내렸다** — 대상별 **부분 유니크 셋**, `check` 여섯(종류·채널·상태·사건·대상 하나·성공↔시각·실패↔이유), 외래키 넷, 본문은 **기본키 공유로 1:1 강제**(`D22` 「1:1 로 붙는 확장 표」, `order_shipping` 선례). **분할표의 예보를 하나 고쳤다** — 유니크를 `(사건, 대상 셋)` 하나로 두면 **대상이 없는 알림**(광고·비밀번호 재설정)이 사건 하나로 유니크가 돼서 평생 한 번밖에 못 나간다. 대상별 부분 유니크면 `null` 함정도 안 생긴다(`batch_run` 성공 회차 선례). **종류를 판에서 박제한다** — 수명이 그 값에서 나오므로 판이 고쳐져도 이미 나간 것의 보존 기간이 안 움직인다(`order_item` 이 상품명을 박제한 것과 같다). 파기는 거래기록 파기 배치에 얹었다 | 완료 |
| 54b | ~~알림 발송기와 서비스~~ | **완료.** `MockNotificationSender`·`NotificationTemplates`·`NotificationService`·`NotificationSendTest`(전부 신설). **거래 통지만 세웠다**(사용자 선택) — 광고 경로는 `55` 가 맡고 `D18` 강제 지점 둘도 그리로 옮겨 적었다. **발송기는 인터페이스 없이 구체 `@Component` 하나다** — `MockPaymentGateway` 선례고, 갈아끼울 것이 실물 SMTP 하나뿐이라 「세어 보고 하나뿐이면 안 만든다」에 걸린다. `5c-1` 이 선례로 부르던 `CaptchaVerifier` 는 **코드에 없어서**(`D14` 의 설계일 뿐) 그 문장을 고쳤다. **변수는 `{{이름}}` 치환**(사용자 선택). **강제 지점 셋**: ① 안 채워진 자리가 남으면 던진다 — 환급 금액처럼 법이 요구하는 항목이 빈 채로 나가면 제18조제3항 단서를 안 지킨 것이 된다. **이력도 안 남긴다** — 보낼 수 없는 것을 「보내는 중」으로 두면 재시도가 그것을 집는다, ② 판은 **시행일**로 고른다(`D2-7` 함정) — 판 번호로 고르면 개정판을 미리 넣은 순간 시행 전 문안이 나간다, ③ 멱등은 `54a` 의 부분 유니크가 막고 서비스는 그 예외를 **「할 일 없음」으로 받는다**. **트랜잭션 경계에서 하나 배웠다** — `TransactionTemplate` 을 기본 `REQUIRED` 로 두면 중복 예외가 **부르는 쪽 트랜잭션까지 죽여서** 삼킬 수가 없다. `PROPAGATION_NESTED` 로 바꿨고, 그것이 되는 근거가 `53a` 가 `stack.md` 에 적어 둔 사실이다. 주소가 없는 계정(파기됨)은 `no_address` 실패로 **이력을 남긴다** — 왜 안 갔는지에 답할 자리가 있어야 한다 | 완료 |
| 55 | ~~광고 경로와 수신 동의~~ | **완료.** `AdvertisingGate`·`AdvertisingNotifications`·`AdvertisingGateTest`(전부 신설), `ConsentService`·`NotificationService`·`notification-rules.md`. **조문을 원문에서 확인했다** — 제50조 제1항(명시적 사전 동의)·제2항(거부·철회하면 전송 금지)·제3항(**오후 9시부터 그 다음 날 오전 8시까지** 별도 동의)·제4항(본문에 명칭·연락처·수신거부 조치). `D18` 의 21~08 이 맞았다. **`D18` 이 넘긴 강제 지점 둘을 구조로 닫았다** — 분기가 아니라 **입구를 갈라서**, `NotificationService` 가 `AdvertisingGate` 를 안 들어 거래 통지 코드에서 동의를 볼 방법이 없다(`D23` 축 2는 타입·구조가 1순위고 분기는 앱 검증 3순위다). **강제 지점 넷**: ① 입구 분리, ② 동의·야간을 관문 한 곳에, ③ **광고 판에 제50조제4항의 자리(`sender_name`·`sender_contact`·`unsubscribe`)가 없으면 거부** — 값이 아니라 **판**을 본다(값만 보면 판이 그 자리를 안 쓸 때 조용히 빠진다), ④ 거래 통지가 동의 없이도·철회 뒤에도 나가는 것을 테스트가 밟는다. **야간에 막힌 광고는 안 보낸다**(사용자 선택) — 미루려면 예약 칸과 배치가 드는데 광고를 보내는 청크가 아직 없다. **막히면 이력을 안 남긴다** — 그 표는 「보냈다」를 증명하는 자리다. **사본을 만들 뻔했다** — `ConsentService` 에 이미 `isGranted` 가 있고 `current_consent` 뷰가 「최신 행이 이긴다」를 들고 있어서, 새로 짜는 대신 `public` 으로 열었다. **드러난 것 둘은 신규 청크로 세웠다**(`55a`·`55b`) — 제7항(동의·거부·철회 처리 결과 통지)과 제8항(2년마다 수신동의 확인) | 완료 |
| 55a | ~~동의 처리 결과 통지~~ | **완료.** `V45__notification_consent_result.sql`(신설)·`NotificationSweeper`·`NotificationService`·`NotificationSweeperTest`. **시행령을 원문에서 확인했다** — 제62조의2 가 「의사를 표시한 날부터 **14일 이내**」에 ①전송자의 명칭 ②의사표시 사실과 표시한 날짜 ③처리 결과를 알리라고 한다. **광고성 정보를 실으면 안 된다** — 처리 결과에 혜택 안내를 얹으면 그것이 광고 전송이고, 철회한 사람이면 제50조제2항 위반이다. 판이 그것을 막는다. **대상 칸이 넷이 됐다** — 어느 의사표시에 대한 통지인지가 붙어야 14일을 잰다. `notification.user_consent_id` 를 더하고 `num_nonnulls(...) <= 1` 과 부분 유니크를 같이 세웠다. **광고 항목의 의사표시만 대상이다** — 제50조는 영리목적 광고성 정보에 걸린 의무라 필수 약관에는 안 걸린다. 전부 보내면 가입할 때마다 여러 통이 나간다. **사건 목록이 닫혀 있어서 마이그레이션 없이는 못 남긴다** — 그것이 강제 지점이다. 배치를 안 늘렸다 — 거래 통지 스위퍼에 다섯째 시점으로 얹었다 | 완료 |
| 55b | ~~수신동의 정기 확인~~ | **완료.** `V46__consent_reconfirm.sql`·`ConsentReconfirmSweeper`·`ConsentReconfirmSweeperTest`(전부 신설), `AdvertisingGate`·`application.yml`(풀 8)·`batch-catalog.md`. **시행령을 원문에서 확인했다** — 제62조의3 이 「수신동의를 받은 날부터 **2년마다**」 확인하고 ①전송자의 명칭 ②동의 사실과 동의한 날짜 ③유지·철회 의사표시 방법을 알리라고 한다. **함정은 「확인이 반복된다」였다** — `55a` 가 건 `(event_type, user_consent_id)` 유니크가 **두 번째 주기를 영영 막는다**. 처리 결과는 의사표시당 한 번이고 확인은 2년마다라 성격이 달라서, 유니크를 `consent_result` 에만 걸고 확인의 중복은 `user_consent.reconfirmed_at` 이 막게 했다. **그 사실은 2년 뒤에야 드러날 것이라 테스트가 두 주기를 실제로 밟는다.** **강제 지점은 관문이다** — 확인이 밀린 동의를 `AdvertisingGate` 가 거부한다(`NOT_RECONFIRMED`). 배치가 못 돌면 확인 없는 동의가 쌓이는데 관문이 안 보면 그 동안 광고가 그대로 나가고, 그때 걸리는 것은 테스트가 아니라 과태료다. **배치가 멈추면 안 나가는 쪽으로 기운다.** 확인 시각은 **보낸 뒤에** 남긴다 — 먼저 남기면 발송 실패 시 다음 확인이 2년 뒤가 된다. 배치가 여덟이 돼서 풀을 8로 올렸고 7로 되돌려 빨간불을 확인했다 | 완료 |
| 56 | ~~알림 트리거 배치~~ | **완료.** `V44__notification_template_seed.sql`·`NotificationSweeper`·`NotificationSweeperTest`(전부 신설), `application.yml`·`batch-catalog.md`. **`D2` R20 이 닫혔다** — 법이 네 시점에 요구하는 통지가 코드는 다 있는데 하나도 안 나가고 있었다. **스위퍼로 갔다**(사용자 선택) — 상태를 훑어 **통지가 없는 건**을 찾는다. `RefundSweeper` 와 같은 모양이고 **상태 전이 코드를 안 건드린다**. 전이 자리마다 발송을 심으면 그 자리 수만큼 빠뜨릴 곳이 생긴다. **기한 초과 판정을 다시 안 짰다** — `seller_order_visible.is_ship_overdue` 가 그 식을 들고 있다(`11c-2c`). 그 뷰가 `o.status = 'paid'` 로 걸러서 **안 낸 주문에는 공급 곤란 통지가 안 나간다** — 발송 기한을 따질 이유가 없어서 맞는 동작이다. **거슬러 보내는 창을 이레로 뒀다**(우리 판단) — 보내는 자리가 없던 동안 쌓인 주문에 지금 와서 보내면 통지가 아니라 무더기 발송이다. **멱등은 여기서 안 막는다** — `54a` 의 부분 유니크가 막고 스위퍼 조건은 대상을 줄이는 것뿐이다(조회와 발송 사이에 틈이 있다). **배치가 일곱이 됐고 `36b` 의 테스트가 그 자리에서 걸렸다** — 풀을 7로 올렸고, 6으로 되돌려 빨간불을 확인했다. 문안은 `V44` 가 1판으로 시드한다(존댓말. 사용자가 읽는 글이다) | 완료 |
| 56a | ~~스위퍼 창 밖으로 나간 통지 감시~~ | **완료 — 감시 대신 구멍을 없앴다**(사용자 선택). `NotificationSweeper`·`NotificationSweeperTest`·`batch-catalog.md`. **분할표 예보가 둘 다 안 맞았다.** ① 「회차를 `failed` 로 남긴다」고 적었는데 **`NotificationSweeper` 는 `fixedDelay` 라 회차 이력이 없다**(`RefundSweeper` 와 같다). ② 「창 밖 건을 감시한다」도 답이 아니었다 — 감시해 봐야 **그 건은 여전히 안 나가고 손으로 보낼 자리도 없다.** **창을 왜 뒀는지로 돌아갔다** — 막으려던 것은 「이레」가 아니라 **「보내는 자리가 없던 동안 쌓인 주문」**이고, 그것은 **기능이 언제 섰나**로 정확히 표현된다. 바닥을 `notification_template` 첫 판 시각으로 바꿨다. **그 뒤에 생긴 것은 배치가 얼마를 멈췄든 전부 잡히므로 감시할 구멍이 없어진다.** 밀린 것이 한 번에 나가도 각 통지가 서로 다른 주문 건이라 스팸이 아니고, **안 보내는 쪽이 위반이다.** 테스트를 둘로 갈랐다 — 기능이 서기 전 주문은 안 보내고, **아흔 날 멈췄다 돌아도 다 잡는다** | 완료 |
| 57 | 배송 추적 | 송장 번호, 택배사, 상태 조회 흉내. **기사·화물의 현재 위치를 다루기로 하면 위치정보법이 걸린다**(`D2`) — 배송지 주소는 안 걸리지만 실시간 위치는 개인위치정보다. 흉내만 낼지 여기서 정한다. **축**: **법**(위치정보법 — 실시간 위치를 다루면 걸린다, `D2`), `D7`. **강제 지점**: 송장 번호 형식을 `check` 로. 위치를 안 다루는 것 자체가 결정이라 스키마에 그 칸을 안 만든다. **건드리는 자리**: `seller_order` 에 송장 칸 또는 신설 배송 표. 화면은 주문 상세 | 11, D2 |
| 58 | ~~상품 Q&A 스키마~~ | **완료.** `V53__inquiry.sql`·`InquirySchemaTest`(둘 다 신설), `TransactionPurgeService`·`TransactionPurgeBatch`·`data-lifecycle.md` 수정. **법 요건 셋을 닫았다** — `R28`(개인정보법 제37조 처리정지·제38조제5항 이의제기), `R25`(전자상거래법 제20조제3항 불만·분쟁), `R34`(정보통신망법 제50조의7 광고 게시물 차단). 앞의 둘은 **받을 자리**가 없었고 셋째는 **내릴 수단**이 없었다. **표를 안 갈랐다** — 넷 다 「받아서 답한다」는 같은 수명을 살고 같은 상태를 지나서, 가르면 접수·답변·차단이 표마다 사본이 된다. 갈리는 것은 **무엇에 붙느냐**뿐이라 `inquiry_product_check` 가 그것만 가른다: **처리정지는 대상이 사람이라 상품 번호를 요구하면 상품을 안 산 사람이 권리를 못 쓴다.** **차단 사유를 열거로 닫았다** — `advertisement`(제50조의7)와 `abuse`(약관)를 값으로 갈라, 법이 근거인 것과 우리가 정한 것이 개정될 때 안 헷갈리게 했다. **보존 3년**(시행령 제6조 4호 소비자 불만·분쟁처리 기록)이고 **파기 코드를 같이 넣었다** — 「수집하는 코드가 파기하는 코드보다 먼저 나오면 그 사이가 위반 구간이다」. 기준은 **낸 날**이다: 답변일로 잡으면 답이 안 나간 문의가 영영 안 지워지는데, 그 상태가 오래 남는 것이 바로 우리가 답을 안 한 경우다. **제약이 느슨한 것을 테스트가 물었다** — 「상태 = (시각과 사유가 둘 다 있나)」로 쓰면 **사유만 매달아 둔 행이 통과한다**(한쪽이 비어 오른쪽이 거짓, 상태도 거짓이라 등식이 맞는다). 셋을 사슬로 묶었고 답변 쪽도 같은 구멍이 있어서 같이 조였다. **안 한 것 하나** — 처리정지·이의제기의 **법정 처리 기한을 안 박았다.** 제37조·제38조의 기한을 원문으로 확인 못 했고 `D2` 에도 없다. 기억으로 박으면 틀린 날짜가 제약이 된다 — `58-1` 로 세웠다 | 6, D2-8 |
| 58-1 | ~~법정 요구의 처리 기한~~ | **완료.** `V59__inquiry_due.sql`(신설), `InquiryService`·`InquiryQuery`·`InquiryVisibilityTest`·`InquirySchemaTest`·`commerce-compliance.md` 수정. **조문을 원문으로 봤다** — 법 제37조는 「**지체 없이**」라고만 하고 **일수가 조문에 없으며**, **시행령 제44조제2항**이 「처리정지 요구서를 **받은 날부터 10일 이내**」로 정한다(제41조제4항 열람·제43조제3항 정정도 같은 10일). **달력일이다** — 「영업일」이 안 적혀 있어서 `D10` 이 안 걸린다: `refund.due_at`(3영업일)과 갈리는 자리다. **접수하는 순간 박제한다** — 답한 날에서 세면 늦게 답할수록 기한이 밀려 「늦었다」가 성립을 안 한다. **기한을 못 찾은 것도 근거를 남겼다**(`D23`) — 제38조제5항(이의제기)과 전자상거래법 제20조(분쟁)는 일수를 못 찾았고 후자는 「신속히」다. **모르는 것을 지어서 박느니 비워 둔다**: 틀린 날짜가 박히면 그것이 지켜야 할 값이 된다. **「기한 안에 답」은 `check` 로 못 건다** — 막으면 늦은 답이 영영 안 나간다. 강제는 **「넘긴 것이 조회로 드러난다」가 천장**이고(`Entry.overdue`) 그 위는 사람이 본다(`R5` 와 같은 자리). **조문 번호 충돌도 풀었다** — 원문을 보니 **제20조의3 은 「통신판매의 중요한 일부 업무를 수행하는 통신판매중개업자의 책임」**이고 불만·분쟁과 무관하다. 없어진 `23` 행이 틀렸고 `R25`(제20조제3항)가 맞다. **그 조사가 새 요건을 하나 물었다** — 제20조의3 은 **청약 접수를 받거나 대금을 지급받는 중개업자**에게 제7조·제8조·제13조제2항제5호·제14조제1항을 **대신 이행**하게 하는데 **우리가 둘 다 한다**(`R5` 에서 「대금을 받은 자」로 이미 정했다). `D2-9` 로 세웠다 | 58 |
| 58-2 | ~~주문에 붙는 문의~~ | **완료.** `V61__inquiry_order.sql`·`orders/[orderNumber]/order-inquiry-form.tsx`(둘 다 신설), `InquiryService`·`InquiryQuery`·`InquiryController`·`InquiryVisibilityTest`·`me/inquiries/page.tsx`·`orders/[orderNumber]/page.tsx` 수정. **`dispute` 로 대신하면 안 되는 이유가 이 청크의 근거다** — 그 표는 시행령 제6조 4호의 **불만·분쟁처리 기록**(3년)이라 단순 문의까지 쌓이면 「분쟁이 몇 건이었나」에 답을 못 한다. **`inquiry_target_check` 가 종류와 대상을 묶는다** — 상품 문의는 상품에, 주문 문의는 묶음에, 계정에 붙는 셋은 아무 데도. **공개가 성립하지 않는다** — `V54` 의 비공개 강제가 그대로 걸려서 조건을 안 바꿔도 막힌다. **없는 묶음과 남의 묶음이 같은 404 다** — 갈라 주면 묶음 번호를 훑어 실재하는 주문의 지도를 그릴 수 있다. **드러난 것 하나** — 셀러 목록이 **상품의 셀러만** 보고 있었다: 주문 문의는 셀러가 묶음에서 나와서 그대로 두면 **셀러가 자기 주문에 온 질문을 못 본다.** `coalesce` 로 둘 다 보게 했다 | 58, 59 |
| 59 | ~~Q&A 공개·비공개~~ | **완료.** `V54__inquiry_visibility.sql`·`InquiryService`·`InquiryQuery`·`InquiryController`·`InquiryVisibilityTest`(다섯 다 신설), `ErrorCode`·`SecurityConfig` 수정. **`58` 이 표만 세워서 `R25`·`R28` 의 법정 창구가 아직 방침 문구였다** — 입구가 서면서 「어디로 받나」에 코드가 답한다. **목록을 관객별로 갈랐다**(사용자 선택): `GET /api/products/{id}/inquiries`(공개분, 비로그인)·`/api/me/inquiries`(내가 낸 것)·`/api/seller/inquiries`(내 셀러 상품). 하나로 두고 로그인 상태로 행을 거르면 **응답 record 가 하나가 되고 비공개 본문을 담을 칸이 상시 존재한다** — 규칙 한 줄을 빠뜨리면 남의 비공개가 그대로 나간다. 갈라 두면 조건이 **「숨기는 것」이 아니라 「고르는 것」**이 돼서 빠뜨렸을 때 **결과가 더 나오는 게 아니라 빈다.** `PublicEntry` 에는 **낸 사람을 실을 칸이 아예 없고** 테스트가 그것을 리플렉션으로 못박는다. **기본값을 층마다 다르게 뒀다** — `is_public` 의 DB 기본값은 **비공개**고 API 는 상품 Q&A 를 공개로 본다. 처음엔 DB 도 공개로 뒀다가 **`58` 의 스키마 테스트 둘이 물었다**: 계정 요구를 그냥 넣으면 `inquiry_visibility_check` 에 걸리고, 그때 고치는 사람이 「`true` 로 넣으면 되나」를 먼저 시도하게 된다. **아래층은 안전한 쪽으로, 위층은 쓰기 좋은 쪽으로** 두고 근거를 마이그레이션에 적었다. **`R34` 가 목록 조건으로 내려갔다** — 내려간 게시물이 공개 목록에서 빠지고 **작성자에게는 그대로 보인다**(제50조의7 은 게시 중단을 요구하지 작성자에게서 감추라고 하지 않는다). **권한 자원을 새로 팠다** — `product:read` 에 얹으면 계정에 붙는 요구가 상품 권한으로 열리는데, 그 요구는 상품을 안 산 사람도 낸다. 마이그레이션에 **감사자 deny 검증**과 **셀러가 `seller` 아닌 범위로 열렸나 검증**을 `do` 블록으로 넣었다 | 58, 4 |
| 59-1 | ~~문의 화면~~ | **완료.** `V60__inquiry_public_index.sql`·`/me/inquiries`(`page`·`inquiry-form`·`withdraw-button`)·`/seller/inquiries`(`page`·`answer-form`)·`products/[productId]/product-inquiries`·`ask-form`(전부 신설), `InquiryService`·`InquiryController`·`InquiryQuery`·`InquiryVisibilityTest`·`me/page.tsx`·`products/[productId]/page.tsx`·`site-header.tsx`·`state-machines.md`·`commerce-compliance.md` 수정. **`24` 를 흡수했다**(점검 G 가 중복으로 닫아 둔 행). **`R28` 이 여기서 끝났다** — 제38조제4항이 「열람등요구의 방법과 절차는 **수집보다 어렵지 아니하도록**」인데 수집은 `/signup` **화면**이고 이쪽은 **API 뿐**이었다. `/me` 의 「권리 행사와 탈퇴」 절이 그 화면을 가리켜 **셸의 머리에서 두 번 눌러 닿는다**(`13e` 와 같은 판단). **처리 기한을 화면이 말한다**(`58-1`) — 넘긴 건은 붉게 뜬다: 「넘긴 것이 조회로 드러난다」가 천장이라 그 사실이 **사람에게 닿는 자리**가 여기다. **`withdrawn` 전이도 닫았다** — 대상을 **낸 사람으로 좁혔다**: 문의 그대로 판정하면 셀러가 실려 `seller` 스코프가 걸리고 **셀러가 불리한 질문을 지우는 자리**가 된다. **거두기를 만들자 진짜 버그가 드러났다** — 공개 목록이 `blocked` 만 빼고 있어서 **거둔 질문이 남에게 그대로 보였다.** `withdrawn` 이 그때 **도달 불가능해서 안 드러난** 것이다. 조건을 **「뺄 상태」에서 「보일 상태」로 뒤집었다**(`V60`): 「이 상태만 뺀다」면 상태가 늘 때마다 그 자리를 고쳐야 하고 빠뜨리면 **안 보여야 할 것이 보이는 쪽으로 샌다.** **검증**: `./gradlew build`·`npm run build`·`lint`·`test` 전부 통과 | 59, 13e |
| 59-2 | ~~광고 게시물을 내리는 수단~~ | **완료.** `V58__inquiry_block_permission.sql`(신설), `InquiryService`·`InquiryController`·`InquiryVisibilityTest`·`commerce-compliance.md`·`state-machines.md` 수정. **`58` 이 만든 것은 「자리」였고 「수단」이 아니었다** — `blocked` 로 옮기는 코드가 없어 `psql` 로만 내려졌고, 요건표에는 「내릴 수단이 생겼다」고 적혀 있었다(1차 마무리가 잡았다). **관리자만이다**(사용자 선택) — 조문의 의무자가 운영자라 그 판단도 운영자가 한다. 셀러에게 열면 **불리한 질문을 광고로 몰아 내리는 자리**가 같이 생기고, 내린 근거가 `advertisement` 로만 남아 **「광고였다」와 「불리해서 내렸다」가 같은 값**이 된다. 마이그레이션의 `do` 블록이 관리자 밖으로 나갔는지 검사한다. **제약 하나를 고쳤다** — `58` 이 `inquiry_answer_check` 에 「`status='answered'` 인 것과 `answered_at` 이 있는 것은 같다」를 박아 놔서 **답이 나간 글을 못 내렸다.** 광고에 답을 달았다고 그 광고가 남을 이유가 없고, **`answered_at` 은 사건 시각이지 상태 표현이 아니다** — 방향을 하나만 걸게 바꿨다(답변 상태면 답이 있다. 답이 있다고 답변 상태인 것은 아니다). **테스트가 보는 것을 바꿨다** — 「내린 글이 목록에서 빠지는 것」이 아니라 **「내리는 경로가 있는 것」**이다. 앞은 `59` 가 이미 고정했고 그 테스트가 **SQL 로 상태를 밀어 넣고 시작해서** 이 구멍이 초록인 채로 남았다 | 59, D2-8 |
| 60 | 검색 인덱스 | Postgres 전문검색, 형태소 처리의 한계. **축**: 표준 — Postgres 전문검색의 한계(한국어 형태소), `D21`. **강제 지점**: 인덱스와 조회 방식을 한 곳에 두고 화면이 직접 SQL 을 안 짜게 한다. **건드리는 자리**: 신설 인덱스 마이그레이션과 검색 조회기. 기존 상품 조회는 안 건드린다 **묶음 검토(2026-08-23)**: `61`(검색 API)과 **안 묶는다.** 묶여서가 아니라 **지금 못 쳐서**다 — 선행 `D21`(성능 목표)이 미착수고 그 `D21` 의 선행이 `42`(성능 측정·튜닝)라 사슬이 `42 → D21 → 60 → 61` 로 넷이다. **먼저 할 일은 묶는 것이 아니라 `42` 를 가르는 것**이다: 측정과 튜닝은 다른 일이고, `D21` 이 「측정값이 없어서 못 쓴다」로 막혀 있으니 **측정만 떼어 내면 그 사슬이 풀린다** | D21, 6 |
| 61 | 검색 API | 페이징·정렬·필터 규약을 따른다. 스코프가 섞인다. **축**: `D5` 목록 규약(페이징·정렬), `D14` 스코프. **강제 지점**: 목록 규약을 지키는지 응답 계약 테스트로 — 검색만 다른 모양이면 화면이 두 벌이 된다. **건드리는 자리**: 신설 검색 입구. 수정은 상품 목록 화면의 검색창 | 60, D5 |
| 62 | 지표 노출 | Actuator 지표를 Prometheus 형식으로. **축**: `D16` 관측 규약, 표준(Prometheus 노출 형식). **강제 지점**: 지표 이름을 `D16` 규약으로 고정하고 그것을 테스트로 — 이름이 갈리면 대시보드가 조용히 빈다. **건드리는 자리**: `build.gradle.kts` 의존성과 Actuator 설정 | D16 |
| 63 | 대시보드 | Grafana 컨테이너, 판정 지연과 오류율 그래프. **축**: `D16` 관측 규칙, `D21` 성능 목표. **강제 지점**: 없다 — **`D21` 을 쓸 근거(측정값)가 여기서 처음 생긴다**. **건드리는 자리**: `docker-compose.yml` 에 Grafana 와 대시보드 정의 파일. 앱 코드는 안 건드린다 | 62 |
| 64 | 백업·복구 리허설 | 덤프하고 날리고 되살린다. 마이그레이션만으론 데이터가 안 돌아온다. **축**: `D13` 데이터 수명, `stack.md`(Postgres 판). **강제 지점**: 되살린 DB 로 테스트를 돌리는 것 자체 — 「덤프가 있다」와 「되살아난다」는 다른 말이다. **건드리는 자리**: 신설 덤프·복구 스크립트와 `stack.md` 절. 앱 코드는 안 건드린다 | D13 |
| 65 | 마이그레이션 롤백 | Flyway 무료판에 undo 가 없다. 되돌리는 마이그레이션을 손으로 쓴다. **축**: `stack.md` 의 Flyway 절(로컬은 `down -v`, 배포는 다르다). **강제 지점**: 되돌리는 파일이 있는지 자체를 테스트로 건다. **건드리는 자리**: 신설 되돌리는 마이그레이션 파일들과 그 존재를 확인하는 테스트. 기존 마이그레이션은 안 고친다 — 적용된 것을 고치면 체크섬이 깨진다 | 0 |
| 66 | ERD 자동 생성 | 스키마에서 그림을 뽑는다. 손으로 그리면 어긋난다. **축**: `D4` 도메인 모델, 업계 관례(SchemaSpy·mermaid 중 고른다). **강제 지점**: 그림을 스키마에서 뽑는다 — 손으로 그리면 다음 마이그레이션에서 바로 어긋난다. **건드리는 자리**: 신설 생성 스크립트와 산출물 경로. `doc/` 에 그림이 들어갈 자리 | D4 |
| 67 | ADR 회고 | 지금까지의 ADR 을 다시 읽고 틀린 것에 `상태: 폐기` 를 붙인다. **축**: `D1` 문서 트리 규약. **강제 지점**: 없다 — 문서 청크다. **폐기한 ADR 을 지우지 않는다**(이력은 그때의 사실이다). **건드리는 자리**: `doc/adr/` 의 기존 ADR 들. 폐기 표시만 더하고 본문은 안 고친다 — 그때의 판단이 기록이다 | 0 |
| 68 | 장애 주입 | DB 를 죽이고 앱이 어떻게 죽는지 본다. 헬스체크가 잡나. **축**: `D16`(헬스체크·로그), `stack.md`. **강제 지점**: 없다 — 실험이다. 나온 것은 정보로 `stack.md` 에 적거나 청크로 세운다. **건드리는 자리**: 실행 절차만. 코드는 결과에 따라 | D16 |
| 69 | 변이 테스트 | 코드를 일부러 망가뜨려 테스트가 잡는지 본다. **축**: `D15` 테스트 전략. **강제 지점**: 살아남은 변이를 목록으로 남기고 그 자리에 테스트를 더한다 — 커버리지 수치가 아니라 「무엇을 못 잡나」가 산출물이다. **건드리는 자리**: `build.gradle.kts`(PIT 같은 도구)와 약한 자리의 테스트 | D15, 4c |
| 70 | 부하 테스트 | 데이터 10만 건에서 목록 조회가 어떻게 되나. **축**: `D21` 성능 목표(그 수치가 판정 기준), `D15`. **강제 지점**: 목표를 넘기면 실패하는 기준선 — 수치 없이 돌리면 결과를 읽는 사람마다 다르게 읽는다. **건드리는 자리**: 신설 부하 스크립트와 시드 확대. 앱 코드는 결과에 따라 | D21, 42 |
| 71 | Rate limiting | 로그인 시도 제한을 일반화한다. **축**: `D14` 보안 기준선, 표준(RFC 6585 — 429 와 `Retry-After`). **강제 지점**: 제한을 필터 한 곳에 두고 상태 코드·헤더를 응답 계약 테스트로 고정한다. **건드리는 자리**: 신설 제한 필터. 수정은 `5c` 의 로그인 시도 제한을 그 필터로 일반화 | D14, 5c |
| 72 | API 버전 정책 | v1 과 v2 를 어떻게 공존시키나. **축**: `D5` 의 버전 절이 **이미 정해 둔 것**을 따른다 — 지금은 안 붙이고 붙일 때 `/api/v2` 를 더한다. **강제 지점**: 없다 — 문서다. 호환을 깨는 변경의 기준(Zalando)을 여기서 확정한다. **건드리는 자리**: `api-guidelines.md` 의 버전 절 | D5 |
| 73 | 버전 공존 구현 | 같은 자원에 두 응답 모양을 유지한다. **축**: `D5` API 규약, RFC 9457(타입 URI 는 안 바뀐다). **강제 지점**: 두 모양을 **같은 테스트가 돌게** 한다 — 한쪽만 도는 순간 다른 쪽이 조용히 깨진다. **건드리는 자리**: 컨트롤러의 경로와 응답 조립. `D5` 가 정한 대로 기존 경로를 v1 으로 남기고 v2 를 더한다 | 72 |
| 74 | 판정 엔진 재구현 (1) | Spring Security `AuthorizationManager` 로 같은 명세를 짠다. **축**: `D6` 권한 모델(같은 명세를 다른 수단으로), 업계 관례(Spring Security `AuthorizationManager`). **강제 지점**: `4c` 의 권한 매트릭스를 **두 구현에 같이** 돌린다 — 그것이 「같은 명세」의 정의다. **건드리는 자리**: 신설 두 번째 판정 구현. 기존 판정 엔진은 안 건드린다 | D6, 4 |
| 75 | 판정 엔진 비교 | 두 구현을 같은 매트릭스로 검증하고 차이를 ADR 로 남긴다. **축**: `D15` 테스트 전략. **강제 지점**: `4c` 의 권한 매트릭스 회귀 테스트를 **두 구현에 같이** 돌린다. 그것이 비교의 정의다. **건드리는 자리**: `4c` 의 권한 매트릭스 테스트를 두 구현에 돌리게 고친다. 차이는 ADR 로 남긴다 | 74, 4c |
| 76 | GraphQL 재구현 | 필드 단위 권한(4d)이 GraphQL 에서 어떻게 달라지나. **축**: `D5`(스키마가 응답 계약을 대신한다), `D6` 필드 단위 권한. **강제 지점**: 필드 판정을 리졸버가 아니라 판정 엔진에 두는 것 — 리졸버마다 걸면 새 필드가 조용히 열린다. **건드리는 자리**: 신설 GraphQL 스키마와 리졸버. `4d` 의 필드 그룹을 읽기만 한다 | D5, 4d |

## 이 계획을 고칠 때

이 파일은 확정문서가 아니다. 청크를 해 보고 설계가 틀렸다고 판단되면 여기를 고치고,
왜 바꿨는지를 `doc/adr/` 에 남긴다. 계획을 지키는 것보다 틀린 걸 아는 게 낫다.
