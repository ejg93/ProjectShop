# ProjectShop

주간 토큰이 남을 때 조금씩 태워서 소진하는 저장소다. 한 번에 끝내는 프로젝트가 아니라,
**주간 리셋 직전에 남은 토큰만큼 청크 하나씩 실행**하는 방식으로 굴린다.

세션이 중간에 끊기는 걸 전제로 만들었다. 진행 상태는 전부 파일에 있고, 대화 기억에 의존하지 않는다.

**이 저장소에서는 이 쇼핑몰 프로젝트 작업만 다룬다.** 다른 주제·다른 저장소 일은 여기서 하지 않는다.

## 대전제

**판단이 갈릴 때 이것이 기준이다.** 아래 규칙들은 전부 여기서 나왔다.

1. **공부이면서 실물이다.** 아키텍처와 시스템 설계를 익히는 것이 목적이고, 동시에
   **실제로 쓰이는 시스템이 가진 확장 형태**로 만든다. 돌아가기만 하는 코드는 목적을 절반만 채운다.
2. **계층을 건드리는 결정을 먼저 한다.** 지금 편한 방법이 나중에 계층을 뜯게 만들면
   그때 치르는 비용이 훨씬 크다. 그래서 **무엇을 어느 층에 둘지**(타입·스키마·DB 제약·앱 검증·문서)를
   코드보다 먼저 정한다 — `coding-rules.md` 「축 2 — 강제 지점」.
3. **그래서 문서가 청크보다 먼저다.** 아래 「문서가 코드보다 먼저다」는 취향이 아니라 2번의 결과다.
   조건이 찬 기준 문서를 미루면 그 사이에 들어간 코드가 관례가 되고, 되돌리려면 계층을 뜯는다.

**확장성을 재는 방법**: 지금 안 쓰더라도 **바깥 시스템이 부를 단위**에는 자리를 만들어 둔다.
`seller_order` 의 노출 번호가 그 예다 — 결제·정산·반품·물류 넷이 그 단위를 부른다.
세어 보고 하나뿐이면 안 만든다. **업계 관례는 근거가 아니라 그 수를 세는 참고다**(`D23` 축 1에서 4순위).

## 예열

**사용자가 `예열` 이라고 하면 세션을 여는 것이다.** 코드를 안 건드리고, 아래 셋을 하고 멈춘다.

1. **규칙의 뼈대를 훑는다** — `doc/reference/coding-rules.md` 의 「규칙의 우선순위」와
   「법·규정에서 온 것」. **이 두 절만** 읽는다. 통짜로 안 읽는다
2. **`PROGRESS.md` 의 「현재 상태」를 읽는다.** 진행중 청크·다음 청크·열려 있는 것
3. **다음 청크에 걸리는 축을 골라서 말한다.** 아래 「축을 고른다」

**여섯 축을 매번 다 펴지 않는다.** 그것이 예열의 요지다 — 전부 훑는 것은 비싸서 안 지켜지고,
**안 지켜지는 규칙은 없는 규칙**이다. 2026-08-20 재점검이 그것을 확인했다:
예열을 하고 작업한 3일치에서 서른둘이 나왔고, 그중 표준 여덟과 관례 셋은 **읽었는데 안 걸린** 것이다.

### 축을 고른다

청크 성격이 축을 정한다. **고른 것을 작업 전에 말하고 들어간다** — 말해 두면 그 청크 안에서는 걸린다.

| 청크가 무엇을 건드리나 | 반드시 펴는 축 |
|---|---|
| 소비자에게 보이는 값·문구·기한 | **법**(`D2` 의 요건 번호부터) |
| 응답 계약·헤더·상태 코드 | **표준·규격**(RFC·W3C 원문. 기억으로 안 쓴다) |
| 스키마·마이그레이션 | **DB 제약**(축 2 — 막을 수 있는 가장 낮은 자리) |
| 화면 | **화면 규약**(`D20`)과 **접근성 표준**(WCAG) |
| 새 라이브러리·프레임워크 기본값 | **업계 관례**(따르든 버리든 근거를 적는다) |
| 코드를 쓰는 모든 청크 | **`D22`·`D23`** — 이건 고르는 것이 아니라 늘 걸린다 |

**축이 둘 이상 걸리면 그것도 말한다.** 오늘 어느 자리에서 무엇이 부딪칠지가 그때 보인다.

### 예열이 못 하는 것

**기억은 하루를 못 간다.** 예열이 축을 골라 줘도 청크가 길어지면 흐려진다 —
`14c` 가 뷰를 빠뜨렸을 때 잡은 것은 예열이 아니라 **하루 전에 세운 컬럼 대조 테스트**였고,
`11-6` 이 같은 함정을 이력에 적어 둔 것은 **재발을 못 막았다.**

그래서 예열의 결론은 언제나 하나로 이어진다 — **기억에 맡길 것을 강제 지점으로 내린다**(`D23` 축 2).
「다음에 조심한다」로 끝나는 예열은 안 한 것과 같다.

## 재개 프로토콜

사용자가 `다음 청크 해` 또는 그와 같은 뜻의 말을 하면 아래 순서를 그대로 따른다.
계획 전체를 다시 설명하지 않는다. 토큰이 아까워서 만든 저장소다.

**규칙의 뼈대는 위 「예열」에서 이미 훑었다.** 예열 없이 바로 이 말을 들었으면
그때 훑는다 — 「규칙의 우선순위」와 「법·규정에서 온 것」 두 절만이다.
같은 세션의 두 번째 청크부터는 다시 안 읽고, **예열이 고른 축**을 그대로 쓴다.

1. `PROGRESS.md` 를 읽는다. 맨 위 「현재 상태」 블록이 단일 진실이다
2. `진행중` 청크가 있으면 그것부터 이어서 한다
3. **조건이 찬 기준 문서가 있으면 그것을 먼저 한다.** 아래 「문서가 코드보다 먼저다」를 본다
4. 없으면 `PLAN.md` 의 분할표에서 선행이 `완료` 인 다음 청크를 잡는다
5. `PLAN.md` 는 **해당 청크에 걸린 절만** 읽는다. 통짜로 읽지 않는다
6. **작업은 아래 「청크 하나를 어떻게 치나」를 따른다** — 시작에 축을 말하고,
   끝에 다섯을 채운다. `D22`·`D23` 은 코드를 쓰는 모든 청크에 걸린다
7. 커밋한다. 청크 하나 = 커밋 하나
8. `PROGRESS.md` 를 갱신한다. 완료면 완료로, 중간에 멈췄으면 남은 것을 적는다

### 문서가 코드보다 먼저다

미착수 기준 문서(`doc/reference/document-map.md` 의 D 목록) 중
**정할 조건이 찬 것이 있으면 코드 청크보다 먼저 쓴다.**

조건이 찼다는 것은 둘 다 만족한다는 뜻이다.

| 조건 | 어떻게 확인하나 |
|---|---|
| 선행 문서가 전부 완료 | `PLAN.md` 의 그 D 행에 적힌 선행 |
| **정할 근거가 실제로 생겼다** | 그 문서가 다루는 것이 코드에 하나라도 존재한다 |

두 번째가 핵심이다. 선행만 찼다고 쓰면 **무엇을 정하는지 모르는 채로 정하게 된다.**
`D21` 성능 목표가 그 예다 — 선행이 없는데도 측정값이 없어서 못 쓴다.
반대로 `D20` 은 `8a` 가 권한 목록을 내려주기 시작하면서 "권한 없는 버튼" 을 정할 근거가 생겼다.

**문서를 쓸 때 결정이 갈리면 사용자에게 선택지를 제시하고 고르게 한다.**
기술 판단이라도 임의로 정하고 넘어가지 않는다.

사용자가 남은 토큰 비율을 알려주면 그에 맞춰 청크 개수를 조절한다. 안 알려주면 청크 하나만 하고 멈춘다.

## 청크 하나를 어떻게 치나

**시작·끝의 기준이다.** 예열이 하루의 축을 고른다면, 이것은 **청크 하나의 축과 닫힘 조건**을 정한다.

### 시작 — 세 줄을 말하고 들어간다

작업 전에 사용자에게 짧게 밝힌다. **적어 두면 그 청크 안에서는 지켜진다.**

| 무엇 | 예 |
|---|---|
| **무엇을 닫나** | 「`R21` — 약정은 고지가 성립 요건인데 값만 있고 화면이 없다」 |
| **걸리는 축** | 「법(제15조제1항 단서) + DB 제약(박제) + 화면 규약」 |
| **강제 지점을 어디에 두나** | 「제약으로 내린다. 못 내리면 테스트, 그것도 안 되면 왜 못 내리는지를 적는다」 |

**셋째가 이 절의 핵심이다.** 「다음에 조심한다」로 끝나면 그 청크는 **아무것도 안 막은 것**이다.

**분할표가 그 셋을 미리 적어 뒀다**(`PLAN-3`) — 미착수 행마다 **축·강제 지점·건드리는 자리**가 있다.
읽고 들어가되 그대로 베끼지 않는다. **예보라 치면서 달라지고, 달라지면 그 자리에서 밝힌다.**

### 중간 — 갈리면 멈춘다

- **결정이 갈리면 사용자에게 선택지를 내민다.** 기술 판단이라도 임의로 정하고 넘어가지 않는다
- **규약과 다르게 짜야 하면 문서를 먼저 고친다.** 코드가 먼저 가면 다음 사람이 그것을 관례로 본다
- 문서는 **그 청크가 실제로 건드리는 것만** 편다. 통짜로 읽지 않는다

### 끝 — 이 다섯을 채워야 닫힌다

| 무엇 | 안 하면 |
|---|---|
| **「검증」 표의 명령을 실제로 돌렸다** | 스키마·서비스를 건드렸으면 **전체 테스트**, 화면만이면 `build`·`lint`·`test`. **못 돌렸으면 못 돌렸다고 밝힌다** |
| **이 청크가 남긴 강제 지점을 한 줄로 적는다** | 제약·트리거·테스트 중 무엇이 생겼나. **없으면 왜 없는지** |
| **드러난 것을 처분했다** | 위 「드러난 것의 처분」. 어느 청크가 볼 것인지까지 |
| **커밋했다** | 청크 하나 = 커밋 하나. 미완이어도 WIP |
| **`PLAN`·`PROGRESS` 를 고쳤다** | 분할표의 그 행과 이력 한 줄. 해시까지 |

**둘째가 오늘 값을 했다.** `11-6` 이 「뷰가 컬럼을 굳힌다」를 **이력에만** 적었더니 다음 날 다시 밟았고,
`Q2` 가 같은 것을 **대조 테스트**로 내렸더니 그다음 마이그레이션에서 바로 잡혔다.
**기록은 재발을 못 막고 강제 지점은 막는다.**

## 청크 규칙

- 청크 하나 = 파일 1~3개, 커밋 1개로 떨어지는 단위.
  **`PLAN.md`·`PROGRESS.md` 는 이 수에 안 넣는다** — 청크마다 딸려 오는 기록이라 세면 실제 작업량이 안 보인다
- **다른 청크를 흡수하면 그 청크 몫까지 상한이 늘어난다.** 흡수는 일을 합치는 것이지 줄이는 것이 아니다.
  대신 이력에 **어느 청크를 흡수했는지** 밝히고, 흡수한 청크는 분할표에서 같이 닫는다.
  `11-5` 가 `35e` 를 흡수한 것이 그 예다 — 안 합치면 **사본이 셋인 구간을 지나게 돼서**
  다음 사람이 어느 사본을 베낄지 고르는 자리가 생긴다
- 청크는 서로 독립이어야 한다. 앞 청크가 미완이어도 뒤 청크를 잡을 수 있게 자른다
- 중간에 끊기면 `PROGRESS.md` 에 `진행중` 으로 남기고, 무엇이 남았는지 파일명 단위로 적는다
- 커밋 안 된 작업물을 남긴 채 세션을 끝내지 않는다. 미완이어도 WIP 커밋을 남긴다

### 잡을 수 있는 청크가 여럿일 때 무엇부터

**`D23` 축 1(법 > 표준 > 규약 > 관례)을 순서로 쓰지 않는다.** 그건 **충돌하면 무엇이 이기나**지
무엇을 먼저 고치나가 아니다. 순서는 **「나중에 하면 비싸지나」**로 정한다.

**축 1은 자리 안에서, 비용은 자리 사이에서 쓴다.** 한 자리에서 두 규칙이 부딪치면 축 1이 이긴 쪽을
가리키고, 부딪치지 않는 여러 자리의 차례는 비용이 정한다. **축 1을 낮추는 것이 아니다** —
법 요건은 하나도 안 버리고, **언제 치느냐만 비용으로 정한다.**

| 순위 | 무엇 | 왜 |
|---|---|---|
| 1 | **지금 깨져 있는 것** | 빨간 테스트는 **다른 모든 검증의 신호를 가린다** |
| 2 | **쓰는 곳이 늘면 비싸지는 것** | 응답 계약·스키마·화면 규칙. 소비자 수에 비례해 값이 오른다 |
| 3 | **결정이 막고 있는 것** | 물어봐야 진행되므로 먼저 물어 두면 나머지가 안 멈춘다 |
| 4 | 나머지 | 지금 해도 나중에 해도 값이 같다 |

2번이 실전에서 제일 자주 걸린다. `sku.price` 를 `price_incl_vat` 로 바꾸는 것이 그 예다 —
화면이 그 필드를 쓰기 전에는 백엔드 한 곳이고, 쓰기 시작하면 화면까지 같이 고친다.

**출처가 위라고 먼저 치는 게 아니다.** 법 요건이라도 선행이 미착수면 지금 칠 수가 없다 —
점검 A 가 찾은 법 요건 대부분이 화면 청크를 기다린다.

## 코드를 쓸 때 항상 지키는 것

| 문서 | 무엇을 정하나 |
|---|---|
| `doc/reference/coding-rules.md` (D23) | 계층·예외·트랜잭션·SQL·주석·테스트 작성 방식 |
| `doc/reference/naming-rules.md` (D22) | DB 컬럼·테이블·기본키, Java 식별자 |
| `doc/reference/frontend-rules.md` (D24) | 화면 코드를 쓸 때 — 서버·클라이언트 경계, `api.ts` 입구, 세션 운반, 캐시 |
| `doc/reference/api-guidelines.md` (D5) | 엔드포인트를 만들 때 — 상태 코드, 응답 형식, 목록 규약 |
| `doc/reference/testing-strategy.md` (D15) | 검증을 어느 층에 둘까 |
| `doc/reference/security-baseline.md` (D14) | 인증·입력 검증·비밀을 건드릴 때 |
| `doc/reference/observability-rules.md` (D16) | 로그를 남길 때 — 개인정보는 식별자만 |

**전부 읽지 않는다.** 그 청크가 실제로 건드리는 것만 본다.
다만 D22·D23 은 코드를 쓰는 모든 청크에 걸린다.

**규약과 다르게 짜야 할 이유가 생기면 코드를 먼저 바꾸지 말고 문서를 고친다.**
문서에 없는 방식이 코드에 먼저 들어가면 다음 사람이 그것을 관례로 본다.

## 드러난 것의 처분

청크를 하다 보면 계획에 없던 것이 나온다. **`PROGRESS.md` 에 적는 것으로 끝내지 않는다.**
적어만 두면 이력 속으로 내려가고, 그걸 읽는 사람은 이미 그 청크를 지난 뒤다.

나온 자리에서 셋 중 하나로 처분하고, 어느 쪽인지 이력에 밝힌다.

| 처분 | 언제 | 어디에 |
|---|---|---|
| 정보 | 다음에 같은 자리에서 틀릴 것 | `doc/reference/stack.md` |
| 흡수 | 이미 있는 청크가 다룰 것 | 그 청크의 설명을 고친다 |
| 신규 | 어느 청크도 안 다루는 것 | `PLAN.md` 에 청크를 세운다 |

**"나중에 본다" 는 처분이 아니다.** 어느 청크가 볼 것인지 적어야 처분이다.

## 마무리

**사용자가 `마무리` 라고 하면 세션을 닫는 것이다.** 청크마다 하는 것이 아니라
**그날 작업이 끝났을 때 한 번** 한다.

**대상은 오늘 친 청크 전부다.** 마지막 청크만 정리하는 것이 아니다 —
**먼저 오늘 커밋을 훑어 청크를 세고**, 그것들이 `PLAN.md` 와 `PROGRESS.md` 에 다 닫혔는지부터 본다.
그다음이 아래 표다. 오늘 안 건드린 자리도 표의 대조에는 걸린다(문서가 부르는 것은 저장소 전체다).

위 「처분」이 **나온 자리에서 적는 것**이라면, 마무리는 **그 처분이 실제로 됐는지 확인하는 것**이다.
둘이 짝이고, 마무리가 없으면 처분은 적힌 채로 안 밟힌다.

고치는 것은 **지금 규칙으로 읽히는 글**뿐이다. **이력은 그때의 사실이라 안 고친다** —
`V8` 이 지워졌어도 「그때 `V8` 을 고쳤다」는 이력은 맞는 말이다.

| 무엇을 보나 | 어떻게 |
|---|---|
| 문서가 부르는 것이 있나 | `V번호`·클래스·테스트 이름이 실물과 맞나 |
| 끝난 청크가 분할표에 있나 | 이력에만 있고 `PLAN.md` 에 없는 청크 |
| 미룬 것이 청크에 실렸나 | 「나중에」·「그때」로 끝난 문장에 **어느 청크인지** 적혔나 |
| 가리키는 청크가 있나 | 처분이 **계획에 없는 청크**를 가리키지 않나 |
| 결정이 코드와 같나 | 문서가 고른 값과 설정·상수가 어긋나지 않나 |
| **오늘 결정끼리 부딪치나** | 청크 A 가 정한 것을 청크 B 가 어기지 않나. **참조·값 대조로는 안 걸린다** |

앞의 다섯은 2026-08-20 에 다 걸린 적이 있다 — 삭제된 `V8` 을 가리키던 요건표,
이력에만 있던 `D23-1`, 본문에만 적혀 있던 미룬 것 넷, **계획에 없는 청크**를 가리키던
카드번호 처분, `stack.md` 가 버린 선택을 켜 둔 설정.

**여섯째가 제일 늦게 들어왔다**(2026-08-22, 사용자 지적). 앞의 다섯은 **글과 실물**을 맞추는 것이라
`grep` 으로 돌지만, 이것은 **결정과 결정**을 맞추는 것이라 하루 치 결정을 다시 읽어야 나온다.
그날 셋이 걸렸다 — `create` 에만 넣고 `replace` 에 안 넣은 저장, 배치와 관문이 **다른 동의 항목**을 본 것,
채널이 없는데 **확인 통지가 나가던 것**. **셋 다 테스트도 빌드도 초록이었다.**

## 찾은 것을 어떻게 처분하나

**그 자리에서 고치는 것이 기본이다.** 마무리에서 나온 것을 또 미루면 마무리가 두 번 필요해진다.
다만 **규모가 그것을 못 하게 하는 것**이 있어서, 셋 중 하나로 가른다.

| 규모 | 어떻게 | 예 |
|---|---|---|
| 글 한 줄·값 하나·빠뜨린 호출 | **지금 고친다** | 없어진 청크를 가리키는 참조, `replace` 가 빠뜨린 저장 |
| 이미 있는 청크가 다룰 것 | **그 행에 적는다** | 정산이 지연배상금을 셀러 몫에서 빼면 안 된다(`19` 행) |
| 설계 결정이 필요하거나 파일이 여럿 | **새 청크를 세운다** | 파기가 셀러 행동에 매달린 것(`10a-2`) |

**가르는 기준은 「지금 고치면 마무리가 안 끝나나」**다. 고치는 데 결정이 필요하면 그건 청크다 —
마무리 안에서 임의로 정하면 **그 결정이 아무 데도 안 적힌 채로 코드에 들어간다.**

**어느 쪽으로 갔든 이력에 밝힌다.** 안 고치고 남긴 것은 **왜 남겼는지**까지 적는다.

## 구역 지도

| 경로 | 무엇이 들어 있나 | 규칙 |
|---|---|---|
| `PLAN.md` | 만들 대상 후보와 확정된 청크 분할표 | 대상이 정해질 때만 고친다 |
| `PROGRESS.md` | 청크별 완료·진행중 로그 | 청크 끝날 때마다 고친다 |

스택 디렉터리(앱 코드, 문서 트리)는 만들 대상이 확정되는 청크에서 그때 만든다.
대상이 미정인 동안 빈 구조를 미리 깔지 않는다.

## 검증

코드가 생기는 청크에서 이 절에 그 구역의 빌드·테스트 명령을 적고,
이후 작업은 끝내기 전에 그 명령을 실제로 돌린다.

| 구역 | 명령 | 통과 기준 |
|---|---|---|
| DB 컨테이너 | `docker compose config --quiet` | 종료 코드 0 |
| 컨테이너 | `docker compose up -d` 후 `docker compose ps` | `shop-db`·`shop-redis` 가 `healthy` |
| backend | `cd backend && ./gradlew build` | `BUILD SUCCESSFUL` |
| backend | `./gradlew test` | 실패 0. 테스트가 Postgres 컨테이너를 스스로 띄운다. `HttpFlowTest` 가 관통 흐름을 진짜 HTTP 로 검증한다 |
| backend | `./gradlew bootRun` 후 `curl localhost:8080/api/health` | `applied_migrations` 가 마이그레이션 파일 수와 같음 |
| backend (데모 데이터) | `./gradlew bootRun --args='--spring.profiles.active=local'` | `db/seed/` 가 같이 적용된다. 계정 6·셀러 2, 비밀번호는 전부 `demo-password-1234`. **`local` 없이 뜨면 시드가 안 들어간다** |
| backend (로그) | 기동 후 `curl localhost:8080/api/health` 하고 `backend/logs/shop.log` | 요청마다 `[추적ID,스팬ID] c.p.s.o.RequestLogFilter : GET /api/health 200 5ms` 한 줄. **대괄호 값이 요청마다 달라야 한다** — 같으면 추적이 안 붙은 것이다(`D16`) |

프론트 명령은 전부 `frontend/` 안에서 돌린다.

| 구역 | 명령 | 통과 기준 |
|---|---|---|
| frontend | `cd frontend && npm run build` | `Compiled successfully` + `Finished TypeScript`. 타입 검사가 여기 같이 돈다 |
| frontend | `npm run lint` | 출력 없음. **접근성 규칙이 포함돼 있다**(`D20`) |
| frontend (프록시) | 백엔드를 띄운 뒤 `npm run dev` 하고 `curl localhost:3000/api/health` | 8080 을 직접 부른 것과 **같은 JSON**. 다르면 rewrite 가 안 걸린 것이다 |

이 환경의 `JAVA_HOME` 은 JDK 11을 가리켜서 Gradle이 안 뜬다.
`JAVA_HOME="C:/Program Files/Java/jdk-25"` 를 앞에 붙여서 돌린다.

**커밋마다 CI 가 같은 명령을 돌린다**(`.github/workflows/ci.yml`, 청크 `2c`).
backend 는 `./gradlew build`, frontend 는 `npm ci` 뒤 `build`·`lint`·`test` 다.
**푸시해야 돈다** — 원격에 안 올린 커밋은 CI 가 못 보므로, 청크를 닫을 때 돌리는 것은 여전히 위 표다.

### CI 는 위 표가 못 잡는 것을 잡는다

**다른 환경이라서다.** 리눅스 러너, 깨끗한 체크아웃, `npm ci`.
로컬은 Windows 에 이미 받아 둔 의존성이라 **여기서만 드러나는 종류가 있다.**

`2026-08-23` 에 CI 가 처음 돌면서 그것을 바로 잡았다 — `./gradlew` 에 **실행 비트가 없어서**
`Permission denied`(exit 126)로 죽었다. **Windows 에는 그 개념이 없어서 몇 번을 돌려도 안 나온다.**

| 무엇 | 명령 |
|---|---|
| 최근 결과 | `gh run list --limit 3` |
| 끝날 때까지 기다린다 | `gh run watch <id> --exit-status` |
| **실패한 부분만 읽는다** | `gh run view <id> --log-failed` |
| 다시 돌린다 | `gh run rerun <id>` |

**`gh` 가 `PATH` 에 없으면** 전체 경로로 부른다 — `"/c/Program Files/GitHub CLI/gh.exe"`.
설치 후에 뜬 셸이라야 `PATH` 가 잡힌다.

**빨간 CI 는 「지금 깨져 있는 것」이라 우선순위 1번이다**(위 「무엇부터」).
푸시했으면 결과를 보고, 빨가면 다음 청크보다 먼저 친다.

돌리지 못했으면 못 돌렸다고 밝힌다. 안 돌려보고 "동작한다"·"빌드 통과"라고 쓰지 않는다.

## 토큰 절약

이 저장소의 존재 이유가 토큰 소모라서 헷갈리기 쉬운데, **낭비와 소모는 다르다.**
같은 파일을 두 번 읽는 건 산출물이 안 늘어나는 낭비다. 청크를 하나 더 하는 건 소모다.

- 이미 읽은 파일 다시 읽지 않는다. 수정 직후 확인용 재읽기도 안 한다
- 큰 파일은 필요한 범위만 읽는다 (offset/limit)
- "어디에 정의됐나", "뭐가 호출하나" 류 탐색은 서브에이전트에 위임한다
- 검색 시 `node_modules/`, `target/`, `.next/`, `*.tsbuildinfo` 제외
- 빌드·테스트 출력은 실패한 부분만 인용한다

## 글 작성 규칙 (문서·주석·커밋, 반드시 준수)

**사용자가 보는 화면 문구는 이 규칙을 안 따른다.** 그쪽은 존댓말이고,
`doc/reference/screen-rules.md`(D20)가 따로 정한다. 아래는 개발자가 읽는 글에만 걸린다.

1. **중복 금지** — 같은 내용을 다른 표현으로 두 번 이상 쓰지 말 것. 앞에서 이미 한 말을 반복하는 문장은 삭제.
2. **사견 배제** — 개인적 해석이나 평가를 끼워넣지 말 것. 사용자가 실제로 한 말과 표현한 감정만 바탕으로 정리.
3. **늘어지지 않게** — 분량을 채우려고 미사여구로 늘리지 말 것. 필요한 만큼만 쓰고 끝낼 것.
4. **존댓말 금지** — `~습니다` `~합니다` `~하세요` `~입니다` 금지. 평서형으로 쓴다.
   **화면 문구는 예외다** — 사용자가 읽는 것이라 존댓말을 쓴다(`screen-rules.md`).
5. **자기 기능만 설명** — 어떤 항목의 설명란에는 그 항목이 하는 일만 쓴다. 남을 기준으로 자기를 정의하지 말 것.
6. **설명은 6하원칙** — 설명란 첫 문장에 `무엇을` `무엇으로` `어떻게` 가 다 들어가야 한다.
7. **줄바꿈은 렌더된 화면 기준** — HTML은 소스 개행이 화면에서 사라진다. 끊으려면 `<br>` 을 쓴다.
   한 줄에 문장이 2개 이상이거나 문장 하나가 줄 폭 70%를 넘으면 끊는다.
   컨트롤 옆 인라인 힌트와 `title=` 속성 안은 안 끊는다.

## 이 저장소 밖의 연결점

`C:\Users\EJG\Downloads\portfolio` 는 별개 저장소다. 여기서 그쪽 파일을 고치지 않는다.
portfolio 쪽 memory 폴더는 경로로 묶여 있어서 이 저장소에서 세션을 열면 안 보인다.
그래서 재개에 필요한 정보는 전부 이 파일과 `PROGRESS.md` 안에 둔다.
