# ProjectShop

주간 토큰이 남을 때 조금씩 태워서 소진하는 저장소다. 한 번에 끝내는 프로젝트가 아니라,
**주간 리셋 직전에 남은 토큰만큼 청크 하나씩 실행**하는 방식으로 굴린다.

세션이 중간에 끊기는 걸 전제로 만들었다. 진행 상태는 전부 파일에 있고, 대화 기억에 의존하지 않는다.

## 대전제

**판단이 갈릴 때 이것이 기준이다.** 아래 규칙들은 전부 여기서 나왔다.

1. **공부이면서 실물이다.** 아키텍처와 시스템 설계를 익히는 것이 목적이고, 동시에
   **실제로 쓰이는 시스템이 가진 확장 형태**로 만든다. 돌아가기만 하는 코드는 목적을 절반만 채운다.
2. **계층을 건드리는 결정을 먼저 한다.** 지금 편한 방법이 나중에 계층을 뜯게 만들면
   그때 치르는 비용이 훨씬 크다. 그래서 **무엇을 어느 층에 둘지**(타입·스키마·DB 제약·앱 검증·문서)를
   코드보다 먼저 정한다 — `coding-rules.md` 「축 2 — 강제 지점」.
3. **그래서 문서가 청크보다 먼저다.** 아래 「문서가 코드보다 먼저다」는 취향이 아니라 2번의 결과다.
   조건이 찬 기준 문서를 미루면 그 사이에 들어간 코드가 관례가 되고, 되돌리려면 계층을 뜯는다.

**확장성을 재는 방법**: 지금 안 쓰더라도 **바깥 시스템이 부를 단위**에는 자리를 만들어 둔다.
`seller_order` 의 노출 번호가 그 예다 — 결제·정산·반품·물류 넷이 그 단위를 부른다.
세어 보고 하나뿐이면 안 만든다. **업계 관례는 근거가 아니라 그 수를 세는 참고다**(`D23` 축 1에서 4순위).

## 재개 프로토콜

사용자가 `다음 청크 해` 또는 그와 같은 뜻의 말을 하면 아래 순서를 그대로 따른다.
계획 전체를 다시 설명하지 않는다. 토큰이 아까워서 만든 저장소다.

1. `PROGRESS.md` 를 읽는다. 맨 위 「현재 상태」 블록이 단일 진실이다
2. `진행중` 청크가 있으면 그것부터 이어서 한다
3. **조건이 찬 기준 문서가 있으면 그것을 먼저 한다.** 아래 「문서가 코드보다 먼저다」를 본다
4. 없으면 `PLAN.md` 의 분할표에서 선행이 `완료` 인 다음 청크를 잡는다
5. `PLAN.md` 는 **해당 청크에 걸린 절만** 읽는다. 통짜로 읽지 않는다
6. 작업한다. **코드를 쓰는 청크면 `doc/reference/coding-rules.md`(D23)와 `naming-rules.md`(D22)를 지킨다**
7. 커밋한다. 청크 하나 = 커밋 하나
8. `PROGRESS.md` 를 갱신한다. 완료면 완료로, 중간에 멈췄으면 남은 것을 적는다

### 문서가 코드보다 먼저다

미착수 기준 문서(`doc/reference/document-map.md` 의 D 목록) 중
**정할 조건이 찬 것이 있으면 코드 청크보다 먼저 쓴다.**

조건이 찼다는 것은 둘 다 만족한다는 뜻이다.

| 조건 | 어떻게 확인하나 |
|---|---|
| 선행 문서가 전부 완료 | `PLAN.md` 의 그 D 행에 적힌 선행 |
| **정할 근거가 실제로 생겼다** | 그 문서가 다루는 것이 코드에 하나라도 존재한다 |

두 번째가 핵심이다. 선행만 찼다고 쓰면 **무엇을 정하는지 모르는 채로 정하게 된다.**
`D21` 성능 목표가 그 예다 — 선행이 없는데도 측정값이 없어서 못 쓴다.
반대로 `D20` 은 `8a` 가 권한 목록을 내려주기 시작하면서 "권한 없는 버튼" 을 정할 근거가 생겼다.

**문서를 쓸 때 결정이 갈리면 사용자에게 선택지를 제시하고 고르게 한다.**
기술 판단이라도 임의로 정하고 넘어가지 않는다.

사용자가 남은 토큰 비율을 알려주면 그에 맞춰 청크 개수를 조절한다. 안 알려주면 청크 하나만 하고 멈춘다.

## 청크 규칙

- 청크 하나 = 파일 1~3개, 커밋 1개로 떨어지는 단위.
  **`PLAN.md`·`PROGRESS.md` 는 이 수에 안 넣는다** — 청크마다 딸려 오는 기록이라 세면 실제 작업량이 안 보인다
- 청크는 서로 독립이어야 한다. 앞 청크가 미완이어도 뒤 청크를 잡을 수 있게 자른다
- 중간에 끊기면 `PROGRESS.md` 에 `진행중` 으로 남기고, 무엇이 남았는지 파일명 단위로 적는다
- 커밋 안 된 작업물을 남긴 채 세션을 끝내지 않는다. 미완이어도 WIP 커밋을 남긴다

## 코드를 쓸 때 항상 지키는 것

| 문서 | 무엇을 정하나 |
|---|---|
| `doc/reference/coding-rules.md` (D23) | 계층·예외·트랜잭션·SQL·주석·테스트 작성 방식 |
| `doc/reference/naming-rules.md` (D22) | DB 컬럼·테이블·기본키, Java 식별자 |
| `doc/reference/api-guidelines.md` (D5) | 엔드포인트를 만들 때 — 상태 코드, 응답 형식, 목록 규약 |
| `doc/reference/testing-strategy.md` (D15) | 검증을 어느 층에 둘까 |
| `doc/reference/security-baseline.md` (D14) | 인증·입력 검증·비밀을 건드릴 때 |
| `doc/reference/observability-rules.md` (D16) | 로그를 남길 때 — 개인정보는 식별자만 |

**전부 읽지 않는다.** 그 청크가 실제로 건드리는 것만 본다.
다만 D22·D23 은 코드를 쓰는 모든 청크에 걸린다.

**규약과 다르게 짜야 할 이유가 생기면 코드를 먼저 바꾸지 말고 문서를 고친다.**
문서에 없는 방식이 코드에 먼저 들어가면 다음 사람이 그것을 관례로 본다.

## 드러난 것의 처분

청크를 하다 보면 계획에 없던 것이 나온다. **`PROGRESS.md` 에 적는 것으로 끝내지 않는다.**
적어만 두면 이력 속으로 내려가고, 그걸 읽는 사람은 이미 그 청크를 지난 뒤다.

나온 자리에서 셋 중 하나로 처분하고, 어느 쪽인지 이력에 밝힌다.

| 처분 | 언제 | 어디에 |
|---|---|---|
| 정보 | 다음에 같은 자리에서 틀릴 것 | `doc/reference/stack.md` |
| 흡수 | 이미 있는 청크가 다룰 것 | 그 청크의 설명을 고친다 |
| 신규 | 어느 청크도 안 다루는 것 | `PLAN.md` 에 청크를 세운다 |

**"나중에 본다" 는 처분이 아니다.** 어느 청크가 볼 것인지 적어야 처분이다.

## 구역 지도

| 경로 | 무엇이 들어 있나 | 규칙 |
|---|---|---|
| `PLAN.md` | 만들 대상 후보와 확정된 청크 분할표 | 대상이 정해질 때만 고친다 |
| `PROGRESS.md` | 청크별 완료·진행중 로그 | 청크 끝날 때마다 고친다 |

스택 디렉터리(앱 코드, 문서 트리)는 만들 대상이 확정되는 청크에서 그때 만든다.
대상이 미정인 동안 빈 구조를 미리 깔지 않는다.

## 검증

코드가 생기는 청크에서 이 절에 그 구역의 빌드·테스트 명령을 적고,
이후 작업은 끝내기 전에 그 명령을 실제로 돌린다.

| 구역 | 명령 | 통과 기준 |
|---|---|---|
| DB 컨테이너 | `docker compose config --quiet` | 종료 코드 0 |
| 컨테이너 | `docker compose up -d` 후 `docker compose ps` | `shop-db`·`shop-redis` 가 `healthy` |
| backend | `cd backend && ./gradlew build` | `BUILD SUCCESSFUL` |
| backend | `./gradlew test` | 실패 0. 테스트가 Postgres 컨테이너를 스스로 띄운다. `HttpFlowTest` 가 관통 흐름을 진짜 HTTP 로 검증한다 |
| backend | `./gradlew bootRun` 후 `curl localhost:8080/api/health` | `applied_migrations` 가 마이그레이션 파일 수와 같음 |
| backend (데모 데이터) | `./gradlew bootRun --args='--spring.profiles.active=local'` | `db/seed/` 가 같이 적용된다. 계정 6·셀러 2, 비밀번호는 전부 `demo-password-1234`. **`local` 없이 뜨면 시드가 안 들어간다** |
| backend (로그) | 기동 후 `curl localhost:8080/api/health` 하고 `backend/logs/shop.log` | 요청마다 `[추적ID,스팬ID] c.p.s.o.RequestLogFilter : GET /api/health 200 5ms` 한 줄. **대괄호 값이 요청마다 달라야 한다** — 같으면 추적이 안 붙은 것이다(`D16`) |

`frontend/` 는 아직 비어 있어서 명령이 없다. 청크 13에서 여기에 추가한다.

이 환경의 `JAVA_HOME` 은 JDK 11을 가리켜서 Gradle이 안 뜬다.
`JAVA_HOME="C:/Program Files/Java/jdk-25"` 를 앞에 붙여서 돌린다.

돌리지 못했으면 못 돌렸다고 밝힌다. 안 돌려보고 "동작한다"·"빌드 통과"라고 쓰지 않는다.

## 토큰 절약

이 저장소의 존재 이유가 토큰 소모라서 헷갈리기 쉬운데, **낭비와 소모는 다르다.**
같은 파일을 두 번 읽는 건 산출물이 안 늘어나는 낭비다. 청크를 하나 더 하는 건 소모다.

- 이미 읽은 파일 다시 읽지 않는다. 수정 직후 확인용 재읽기도 안 한다
- 큰 파일은 필요한 범위만 읽는다 (offset/limit)
- "어디에 정의됐나", "뭐가 호출하나" 류 탐색은 서브에이전트에 위임한다
- 검색 시 `node_modules/`, `target/`, `.next/`, `*.tsbuildinfo` 제외
- 빌드·테스트 출력은 실패한 부분만 인용한다

## 글 작성 규칙 (문서·주석·커밋, 반드시 준수)

**사용자가 보는 화면 문구는 이 규칙을 안 따른다.** 그쪽은 존댓말이고,
`doc/reference/screen-rules.md`(D20)가 따로 정한다. 아래는 개발자가 읽는 글에만 걸린다.

1. **중복 금지** — 같은 내용을 다른 표현으로 두 번 이상 쓰지 말 것. 앞에서 이미 한 말을 반복하는 문장은 삭제.
2. **사견 배제** — 개인적 해석이나 평가를 끼워넣지 말 것. 사용자가 실제로 한 말과 표현한 감정만 바탕으로 정리.
3. **늘어지지 않게** — 분량을 채우려고 미사여구로 늘리지 말 것. 필요한 만큼만 쓰고 끝낼 것.
4. **존댓말 금지** — `~습니다` `~합니다` `~하세요` `~입니다` 금지. 평서형으로 쓴다.
   **화면 문구는 예외다** — 사용자가 읽는 것이라 존댓말을 쓴다(`screen-rules.md`).
5. **자기 기능만 설명** — 어떤 항목의 설명란에는 그 항목이 하는 일만 쓴다. 남을 기준으로 자기를 정의하지 말 것.
6. **설명은 6하원칙** — 설명란 첫 문장에 `무엇을` `무엇으로` `어떻게` 가 다 들어가야 한다.
7. **줄바꿈은 렌더된 화면 기준** — HTML은 소스 개행이 화면에서 사라진다. 끊으려면 `<br>` 을 쓴다.
   한 줄에 문장이 2개 이상이거나 문장 하나가 줄 폭 70%를 넘으면 끊는다.
   컨트롤 옆 인라인 힌트와 `title=` 속성 안은 안 끊는다.

## 이 저장소 밖의 연결점

`C:\Users\EJG\Downloads\portfolio` 는 별개 저장소다. 여기서 그쪽 파일을 고치지 않는다.
portfolio 쪽 memory 폴더는 경로로 묶여 있어서 이 저장소에서 세션을 열면 안 보인다.
그래서 재개에 필요한 정보는 전부 이 파일과 `PROGRESS.md` 안에 둔다.
